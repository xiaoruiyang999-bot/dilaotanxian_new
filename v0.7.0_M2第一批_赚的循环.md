# v0.7.0 M2 第一批「赚的循环」（钱包 + 金币掉落 + HUD）

> 状态：**代码已落盘 · Play 零错误 · 待实战验收**
> 日期：2026-08-22
> 里程碑：M2·收益循环（[长期计划](计划书/项目长期计划_Roguelite路线图.md) § M2 批次一：2.1/2.2/2.3）
> 关联：[v0.6.8_狼人角色系统开发文档](v0.6.8_狼人角色系统开发文档.md)

---

## 1. 本批交付（打怪 → 掉钱 → 捡钱 → 看钱 四环）

| # | 任务 | 实现 |
|---|---|---|
| 2.1 | 钱包 | `PlayerStats` 新增 `Coins` / `AddCoins` / `TrySpendCoins`（不足不扣）+ `OnCoinsChanged` 事件 |
| 2.1 | HUD | 新 `UI/CoinHUD.cs`：左下角金币图标+数字（纯代码，挂 `CoinHUDSystem` 空对象），订阅事件实时刷新 |
| 2.2 | 掉落物 | 新 `Common/CoinDrop.cs`：零资产纯代码——程序绘制金色硬币 Sprite（32px 描边圆）、Rigidbody2D 弹跳散落（共享弹跳材质）、**磁吸**（1.3 范围吸向玩家，0.45 拾取入账）、12s 寿命 + 末尾 2s 闪烁 |
| 2.3 | 挂点 | `EnemyController.OnEnemyDeath` → `CoinDrop.Spawn(pos, Random.Range(coinsMin, coinsMax+1))`；默认 1~3 枚，Inspector 可调（Boss/精英 prefab 上调大） |

**架构说明**：
- 金币挂 `DungeonRoot` 下随楼层清理（找不到则场景根），自带的寿命兜底防堆积
- 拾取音效挂点 `AudioManager.PlaySFX("coin")`——**未配置静默**，后续在 AudioManager 的 Inspector 加 coin 条目即可
- 掉落数量用 UnityEngine.Random（战斗表现随机，不进地牢生成的 seed 复现流，同 EnemyCombat 攻击随机策略）
- `MinimapController.BuiltinFont` 转公开静态（纯代码 UI 共享字体缓存）

## 2. 验收清单（Play 实测）

- [ ] 左下角（血条面板上方）出现金币图标 + 数字 0
- [ ] 杀敌 → 金币弹跳散落（金色小圆）
- [ ] 走近金币 → 磁吸飞向玩家 → 数字跳动 +1
- [ ] 金币 12 秒后闪烁消失（不捡不堆积）
- [ ] 死亡重开 / 换层 → 金币清零（局内资产）、HUD 复位

## 3. 过程事故记录（教训）

- CoinHUD 初版把 `pivot` 写在 Image/Text 上（属 RectTransform 属性）→ **Safe Mode**（Unity 编译错误安全模式）。修复两处。红线补记：**纯代码 UI 的 RectTransform 属性（pivot/anchor/sizeDelta）一律走 `.rectTransform.xxx`**；强编译前先本地过一遍 API 层自查（括号检查抓不住这类错）。

## 4. 后续批次

- **批次二**：宝箱真奖励（LootTable SO）+ 还债"宝箱奖励被切层吞"（2.5/2.7）
- **批次三**：商店第一版（ShopInteractable + 货架，2.4）
- **批次四**：三选一升级 + PlayerStats 乘数体系 + 兽化数值接入（2.6）
