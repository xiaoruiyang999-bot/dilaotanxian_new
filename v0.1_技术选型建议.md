# v0.1 技术选型建议：初始小屋搭建（程序员美术版）

## v0.1 目标回顾
> 搭建初始小屋，实现小屋基本碰撞与绘制

美术策略已确定为**纯色几何图形（Programmer Art）**：色块=墙壁，圆球=角色，方块=敌人。以下技术选型基于这一策略重新调整。

---

## 1. 地图构建方式：如何绘制小屋？

### 选项 A：Unity Tilemap 系统（⭐强烈推荐）
**简介**：Unity内置的2D网格地图系统，通过"瓦片(Tile)"拼合地图。使用Unity内置的白色Square Sprite作为底图，通过Color属性染色。

**优势**：
- 专为2D游戏设计，与碰撞、导航深度集成
- 画墙=自动有碰撞（配合Tilemap Collider）
- 后续做随机地牢生成时，Tilemap是唯一可扩展的方案
- 美术替换时只需把瓦片的Sprite从白色方块换成正式素材，地图布局完全不变
- 支持**规则瓦片(Rule Tile)**：自动根据邻居瓦片选择正确贴图（后续美术替换时启用）

**劣势**：
- 需要学习Tile Palette、Grid、Tile Asset的概念（但非常基础）

**适合场景**：几乎所有2D俯视角地牢/房间类游戏。**本项目必选**。

---

### 选项 B：单独 Sprite 拼接
**简介**：每个地砖、墙壁都是独立的GameObject + SpriteRenderer（白色方块+染色）。

**优势**：
- 概念简单，不需要学习Tilemap

**劣势**：
- 小屋有100块地砖 = 100个GameObject，性能差
- 碰撞需要手动一个个加Collider
- 后续地牢生成几乎无法扩展
- 美术替换时需要逐个替换

**适合场景**：极简原型、场景很小的项目。**本项目不推荐**。

---

### 选项 C：3D Quad 平面
**简介**：用3D平面作为地板/墙壁，俯视角看起来像2D。

**优势**：
- 可以利用3D光照、阴影

**劣势**：
- 过度设计，2D游戏不需要3D
- 碰撞系统用3D Physics，与2D角色控制混用容易出问题
- 渲染性能差于纯2D

**适合场景**：需要动态光照/阴影的2.5D游戏。**本项目不推荐**。

---

## 2. 碰撞系统：如何让墙壁"挡住"玩家？

### 选项 A：Tilemap Collider 2D + Composite Collider 2D（⭐强烈推荐）
**简介**：给墙壁Tilemap添加两个组件，自动合并碰撞器。

**工作原理**：
1. **Tilemap Collider 2D**：为每个有碰撞属性的瓦片自动生成碰撞形状
2. **Composite Collider 2D**：将相邻瓦片的碰撞合并成一个大碰撞体，大幅减少物理计算

**优势**：
- 画墙 = 自动有碰撞，零额外操作
- Composite合并后性能极佳（一个房间=1个碰撞体，不是N个）
- 后续随机生成地图时，碰撞自动跟随地图变化
- 不需要任何手动放置

**注意**：需要配合Rigidbody2D(Static)使用。

---

### 选项 B：手动放置 BoxCollider2D
**简介**：在墙壁位置手动放空的GameObject + BoxCollider2D。

**优势**：
- 最直观

**劣势**：
- 每改一次地图都要手动调整碰撞
- 工作量大且容易遗漏
- 后续随机生成不可能实现

**适合场景**：极少量固定障碍物。**本项目不推荐**。

---

### 选项 C：EdgeCollider2D 描边
**简介**：用一条连续的线段勾勒出房间轮廓。

**优势**：
- 碰撞体最少

**劣势**：
- 手动描边繁琐
- 内部有障碍物时不好处理
- 地图变化时需重新描边

**适合场景**：不规则形状的单一场景。**本项目不推荐**。

---

## 3. 相机系统：不需要 Pixel Perfect Camera

由于采用纯色几何图形而非像素美术，**Pixel Perfect Camera 不需要**。

### 推荐配置

| 设置项 | 值 | 说明 |
|--------|-----|------|
| Projection | Orthographic | 2D俯视角必须 |
| Size | 根据房间大小调整 | 使Game视图能看到完整小屋 |
| Background | 深色 | 形成"房间在黑暗中"的氛围 |
| 其他 | 默认 | 纯色块不存在模糊/抖动问题 |

> 💡 什么时候需要Pixel Perfect Camera？只有当你使用**像素美术（Pixel Art）**时才需要。正式美术替换阶段再添加即可。

---

## 4. 角色/敌人图形：Unity 内置 Sprite Shape

### 可用内置形状

| 形状 | 用途 | 创建路径 |
|------|------|----------|
| **Circle** | 玩家角色、血瓶、法球 | `Hierarchy` → `2D Object` → `Sprites` → `Circle` |
| **Square** | 敌人、墙壁、道具 | `Hierarchy` → `2D Object` → `Sprites` → `Square` |
| **Triangle** | 箭头、拾取提示 | `Hierarchy` → `2D Object` → `Sprites` → `Triangle` |
| **Capsule** | 细长敌人、子弹 | `Hierarchy` → `2D Object` → `Sprites` → `Capsule` |
| **Diamond** | 宝物、金币 | `Hierarchy` → `2D Object` → `Sprites` → `Diamond` |
| **Hexagon** | 特殊地形/机关 | `Hierarchy` → `2D Object` → `Sprites` → `Hexagon` |

### 区分对象的策略

**形状 + 颜色 + 尺寸** 三重编码：
- 形状：玩家=圆（友善），敌人=方（危险），道具=三角/菱形（可收集）
- 颜色：绿=玩家/安全，红=敌人/危险，蓝=魔法，黄=宝物
- 尺寸：大=强/Boss，小=弱/普通

---

## 我的综合推荐（针对你的项目）

### 推荐技术栈（v0.1 程序员美术版）

| 功能 | 技术选择 |
|------|----------|
| 地图绘制 | **Tilemap**（Grid + Tilemap + Tile Palette），纯色瓦片 |
| 墙壁碰撞 | **Tilemap Collider 2D + Composite Collider 2D** |
| 角色图形 | **Circle Sprite** + SpriteRenderer.Color染色 |
| 敌人图形 | **Square Sprite** + SpriteRenderer.Color染色 |
| 相机 | **普通Orthographic Camera** |
| 动画 | **代码驱动**（缩放、颜色闪烁、位移），无需Animator |
| 分辨率 | 无特殊要求，Game视图能完整显示场景即可 |

### 文件夹结构（简化版）

```
Assets/
├── Scenes/
│   └── v0_1_InitialHouse.unity
├── Scripts/
│   └── v0_1/
│       └── (v0.2开始放代码)
├── Sprites/
│   └── v0_1/
│       ├── WhiteSquare.asset      # 地板/墙壁底图（内置）
│       └── WhiteSquare_Wall.asset # 墙壁专用（复制）
├── Tilemaps/
│   └── v0_1/
│       ├── FloorTile.asset        # 浅色染色瓦片
│       └── WallTile.asset         # 深色染色瓦片
├── Palettes/
│   └── v0_1/
│       └── InitialHouse.prefab    # 调色板
└── Settings/                       # 已有：渲染管线设置
```

---

## 关键认知：现在的选择与未来的关系

| 现在的做法 | 对未来的影响 |
|------------|-------------|
| 用Tilemap画地图 | → 随机地牢生成直接复用，美术替换时只换瓦片Sprite |
| 用Tilemap Collider | → 随机地图的碰撞自动生成，永远不需要手动调整 |
| 用SpriteRenderer染色 | → 美术替换时把Sprite换了就行，Color可以恢复为白色 |
| 用代码驱动动画 | → 以后加Animator时，只需替换表现逻辑，状态机结构不变 |
| 用Orthographic相机 | → 以后加Pixel Perfect Camera只需加一个组件，零代码改动 |

**一句话总结**：现在搭好的**结构**（Tilemap层级、碰撞系统、对象关系）= 永久资产。现在用的**美术**（纯色方块）= 临时占位符，随时可以无损替换。

---

## 下一步

技术选型已确定。请查阅 **`v0.1_任务文档.md`** 获取逐步操作指令，完成v0.1的搭建。

如需讨论后续版本（v0.2战士系统、v0.3职业扩展、v0.4敌人AI等）的技术选型，随时告知！
