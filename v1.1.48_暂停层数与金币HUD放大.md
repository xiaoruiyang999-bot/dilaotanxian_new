# v1.1.48 暂停层数与金币 HUD 放大

## 需求

- 在暂停面板左上角增加纯文字层数计数，不引入新图片或装饰素材；
- 放大屏幕左下角金币计数器，提高 1080p 下的辨识度。

## 实现

### 暂停面板层数

`PausePanel.Open` 每次创建面板时读取 `RunManager.FloorNumber`，显示：

```text
当前层数：第 N 层
```

文本锚定于石板左上角，与中央“已暂停”标题处于同一高度。楼层唯一真源仍是
`RunManager.FloorNumber`，没有新增缓存、轮询或第二份计数状态。准备大厅不存在
`RunManager`，因此不显示层数，避免把大厅误写成第一层。

### 金币 HUD

| 项目 | 修改前 | 修改后 |
|---|---:|---:|
| 金币图标 | 16 px | 28 px |
| 金币数字字号 | 16 | 24 |
| 文本区域 | 90×18 | 130×32 |
| 图标文字间距 | 6 px | 8 px |

金币数据链路保持不变：`PlayerStats.OnCoinsChanged` 驱动 `CoinHUD`，本次只调整表现尺寸。
