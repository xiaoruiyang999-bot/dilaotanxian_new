# v1.0.7 BGM 全局常驻

> 日期：2026-08-31  
> 状态：已落盘；编译 0/0；门禁 9/9；跨场景切换实测通过（常驻实例存活、重复实例静默禁用）。

## 问题

BGM 资产（`Assets/Audio/BGM/DungeonBGM.mp3`，v1.0.5 已接三场景）此前随**场景对象** AudioManager 播放：每次切场景（准备房↔地牢、死亡回城）旧实例销毁、新实例从头播——断档且不算全局生效。

## 修复（AudioManager.cs）

- 首实例 `DontDestroyOnLoad(gameObject)`：BGM 从游戏启动连播到底，跨场景不打断；SFX 静态入口（PlaySFX）同源全局化。
- 后续场景的 AudioSystem 重复实例**静默禁用**（`enabled = false`，不跑 Start 不抢播；原 LogWarning 移除防每次切场景刷屏），随各自场景卸载销毁。
- BGM 起播时打一条日志：`[Audio] BGM 开始全局循环播放（跨场景常驻，切换场景不打断）`。
- 暂停面板音量契约不变（SetBgmVolume/SetSfxVolume 直改常驻实例，PlayerPrefs 持久化）。

## 验证

- 实测路径：Play v0_5_Dungeon →（无职业）触发统一入口重定向切到 v0_7_PrepRoom——**播放中的真实场景切换**。
- 结果：切到准备房后，准备房自己的 AudioSystem 组件为 `enabled: false`（静默重复实例），常驻实例（v0_5 启动时创建）在后台持续播放 BGM——跨场景不断档成立。
- 编译 0/0；EditMode 门禁 9/9。

## 备注

- 三场景音效表/BGM 配置一致，从任意场景启动效果相同；后续若按场景差异化 BGM（如 Boss 战换曲），在常驻实例上加切曲接口即可，勿回退到场景挂载模式。
- 同步更新：AGENTS.md 反馈挂点行（BGM 全局常驻）、AudioManager 类注释。
