# v1.1.43 真编译验收门禁（_dev/compile_check.py）

## 背景（用户严厉批评：每次编译都有 bug，验收是真做还是假做）

此前会话无 Unity 编译器可调，"验收"= 文本级静态复查（grep/配平/断言）——抓不住类型级错误，
导致 CS0117/CS1503×2/CS0103/CS1061 一路漏到用户手里。本轮把**真实类型级编译**接入会话。

## 检查器构成

- 编译器：Unity 自带 Roslyn（`Editor/Data/DotNetSdk/sdk/8.0.318/Roslyn` + Unity 自带 dotnet，
  系统 dotnet 运行时过旧不可用）；
- 引用集（358 DLL）：UnityEngine 全模块 + UnityEngine/UnityEditor 转发面 + UnityEditor 模块
  （MenuItem）+ Mono 4.7.1 BCL 三主库 + Facades + Library/ScriptAssemblies（InputSystem/
  TextMeshPro/DOTween 等）+ Assets/Plugins 预编译（DOTween）+ Unity.Scripting（Preserve）；
- 源集：Assets/Scripts/now_use 全部（Game 程序集）；csc 响应文件调用（绕 32K 命令行上限）；
- 防假通过三保险：退出码异常即判检查器失败（exit 2）；空输出判失败；**阳性对照**流程
  （注入已知错误必须被抓出才算检查器本身可用）。

## 阳性对照记录（三类历史真实 bug 注入，全部抓出）

注入 HashSet.AddRange（CS1061）、ContactFilter2D 版 CircleCast（CS1503）、
Transition.SpriteTransition（CS0117）→ 检查器全数报错 exit=1；移除后干净编译 exit=0。

## 顺带修掉的真 bug（检查器首跑即抓到两个）

1. PlayerController.Respawn 方法体被补丁插出多余 `{`（CS0106/CS1513 ×5）——已修；
2. EnemyPathfinder 的集合容量构造器与 netstandard BCL 不兼容（CS1503）——改为无参构造
   （容量预分配仅优化，静态复用已在，零行为差异）。

## 检查器搭建过程中自身排掉的五个坑（防复用者再踩）

shell=True 拆带空格路径（假通过）→ 列表参数；系统 dotnet 运行时过旧 → Unity 自带 dotnet；
命令行超 32K → rsp 文件；netstandard 与 mscorlib 混引 → CS0518 风暴（只用 Mono profile 主库）；
UnityEditor 模块在 Managed/UnityEngine 而非 Managed/UnityEditor。

## 使用约定（写入必读）

**今后每次改 .cs 的交付前必跑 `python _dev/compile_check.py`，exit 0 才算验收**；
阳性对照不必每次跑（本记录在案），但检查器改动后必须重跑对照。
