# v1.1.16 设置两级导航 + 滑钮纯代码光晕

## 需求（用户两条）

1. 滑钮悬停光晕改**纯代码渲染**——不加载 hover 大图省运行时内存；
2. 设置改**两级导航**：点"设置"→ 设置主页（音量设置 / 操作设置两个大功能键）→ 点音量设置才进音量滑钮页。

## 滑钮视觉（SliderHandleState 重写）

- 悬停：程序生成 **64×64 径向三次方衰减光晕**（静态缓存全项目一份纹理，暖金 tint α0.55），
  挂滑条根下渲染于滑钮之前、逐帧跟随滑钮（子物体必渲染于钮体之上，故不能做滑钮子物体）；
- 按下：滑钮 Image 颜色调暗 **0.65**（=按键规范 Pressed 值），不再换 pressed 图；
- `SliderSprites` 精简为三图（轨道/黄条/默认钮）；**Resources 运行时副本删除 4 张状态图**
  （hover/pressed/selected/disabled，母版 Assets/UI/PauseMenu 保留归档）。

## 设置两级导航（PausePanel）

- `SettingsPage { Main, Audio, Controls }` + `ShowSettingsPage`：
  - **主页**：`button_audio_settings` / `button_controls_settings` 两个大键（300×65=原图 4.63:1，居中纵排）；
  - **音量页**：石槽滑条 ×2 + 收起提示 + 返回键；
  - **操作页**：键位速查 9 行 + 返回键；
- 返回键 = 三态石板小操作条（150×36）+ TMP"返 回"（无专属美术，走 ApplyStoneButton 兜底风格）；
- 每次"设置"展开重置回主页；关闭暂停时全部引用置空（v1.1.12 防悬挂规范）。

## 验收

1. 编译无错 → Esc → 设置：右侧先是两个大按钮；点"音量设置"进滑条页，点"操作设置"进键位页，"返 回"回主页；
2. 悬停滑钮：光晕出现并跟随拖动；按下钮体压暗；离开光晕消失；
3. Resources/UI/PauseMenu 仅 8 张运行时图（3 暂停键 + 2 设置键 + 3 滑条件）。

## 关联

v1.1.10 按键规范 / v1.1.15 滑条件适配（260×32 轨道、40×40 滑钮、16px 黄条）不变。
