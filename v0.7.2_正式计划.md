# v0.7.2 正式计划：主 UI 四槽 + 道具背包（3 格）+ 武器背包（1 格）

> 版本线：《v0.7_Demo玩法计划书.md》§四 + §九 v0.7.2 执行卡。总纲决策 4：主 UI 四槽（小技能/大招/武器技能/道具栏）、道具背包 3 格（同类叠加无上限、数量角标）、武器背包 1 格（无叠加）。
> 前置：v0.7.1 已完成。本版是 UI/数据框架版：**技能三槽做空占位（v0.7.4 填内容），消耗品效果与正式投放归 v0.7.3**，本版只把"槽位-背包-拾取-换武器入包"链路跑通。
> 开工前必读《版本实施自检清单.md》§1。UI 规范：运行时查找+兜底创建；缓存复用 UI 外观参数每帧/每次显示应用；布局单点收口；TMP 走 TMPFontProvider。

---

## 一、目标与范围

1. **主 UI 四槽**（屏幕右下横排）：小技能 / 大招 / 武器技能 / 道具栏——本版交付槽位 UI 框架 + 数据接口（SetIcon/SetCount/SetCooldown 占位），技能三槽显示"空"占位；
2. **道具背包 3 格**：数据结构 + UI + 拾取分流（同类叠加 → 道具栏 → 背包空位 → 满提示）；数量角标（槽位右下角）；
3. **武器背包 1 格**：地牢换武器旧武器**入包**替代原地掉落；包满时包内武器原地掉落（默认规则，见 §五确认项）；准备场景"自动归位展台"规则不动；
4. **道具使用键**：新增 UseItem action（默认 C，可重绑），使用道具栏激活项（本版接测试消耗品）。

**明确不做（本版）**：技能内容与 CD 扫面（v0.7.4）、三种正式道具与投放（v0.7.3）、拖拽/右键等复杂背包交互、武器背包扩展。

---

## 二、改动清单

### 2.1 新建脚本（5，含 .meta，建后确认进 csproj）

| 文件 | 职责 |
|------|------|
| `Item/ConsumableData.cs` | 消耗品 SO：displayName / effectType(HP/Armor/Mana，v0.7.3 接线) / value / iconColor / 占位图标。本版只建数据结构与 1 份**测试资产**（测试药·Mana，+10）供链路验证 |
| `Item/ItemInventory.cs` | 玩家背包组件（纯数据+事件）：道具栏激活位（ItemStack）+ 背包 3 格（ItemStack=data+count，同类叠加无上限）；`Add(data)` 分流：道具栏同类叠加 → 栏空入栏 → 背包同类叠加 → 背包空位 → 返回 false（满）；`UseActive()` 扣数清零移位；`SwapWithBackpack(index)` 道具栏与背包格互换（鼠标点击背包格调）；事件 OnChanged |
| `Item/ItemPickup.cs` | 消耗品拾取物（IPickupable）：E 拾取 → ItemInventory.Add；满则提示"背包已满"且**不消耗拾取物**；运行时色块占位视觉 |
| `Player/SlotBarUI.cs` | 主 UI 四槽 + 背包 3 格（运行时构建，锚定屏幕右下）：槽 = 矩形框（职业色描边占位）+ 名称文字 + 右下数量角标；订阅 ItemInventory.OnChanged；技能三槽本版显示"空"；面板定位只读不改既有 UI（自检 #15） |
| `Item/WeaponSatchel.cs` | 武器背包（1 格）：`Stored`（WeaponData，可空）；`Store(data)` 返回被挤出的原存武器（可空）；供 PlayerWeaponHolder 调用 |

### 2.2 修改脚本（3）

| 文件 | 改动 |
|------|------|
| `Weapon/PlayerWeaponHolder.cs` | Equip 换武器分流：`dropOldWeaponOnEquip` 开关**退役删除**——地牢规则改"旧武器入 WeaponSatchel，被挤出者原地掉落（WeaponPickup.Drop）"；准备场景归位仍走 OnWeaponChanged 订阅（PrepRoomManager 逻辑零改动）；Unequip（死亡重开）**清空武器背包** |
| `Player/PlayerController.cs` | +UseItem action 订阅转发 → ItemInventory.UseActive()（未装备消耗品时无副作用） |
| `Class/PrepRoomManager.cs` | 仅核对归位订阅不受 dropOldWeaponOnEquip 删除影响（字段引用删除则同步改订阅条件），**展台归位规则不变** |

### 2.3 输入资产

- `PlayerInputActions.inputactions`：+`UseItem` action（默认绑定 C，可重绑预设，与既有 action 同结构）；**禁 KeyCode 硬编码**。

### 2.4 数据资产

- 新建 `Assets/Data/Item/Consumable_Test.asset`（测试药）：effectType=Mana、value=10、蓝色占位——**仅本版验证用**，v0.7.3 建正式三包后停用删除；
- 测试投放：准备场景地面放 3~4 个 ItemPickup（运行时创建，PrepRoomManager 测试摆放，同 v0.6.5 出生房测试摆放思路）——**改场景 YAML**，用代码运行时摆放。

---

## 三、UI 规格（占位美术，无新美术需求）

```
屏幕右下（锚定 BottomRight，留 20px 边距）：
[小技能] [大招] [武器技能] [道具栏]      ← 四槽横排，64×64 框+名称文字
                          [包1][包2][包3]  ← 道具栏上方竖排/横排 3 格（实现时取齐整者）
```

- 槽框：深色底 (0.12,0.12,0.12,0.9) + 白描边 1px 占位；技能槽空占位显示"—"；
- 数量角标：槽位右下角 TMP 小字（14pt），count≥2 才显示；
- 背包格点击互换：Button 点击 → SwapWithBackpack；无拖拽；
- 全部运行时构建 + 外观参数每次刷新应用（自检 §1 缓存 UI 规则）。

---

## 四、结算链路（本版落地后）

| 动作 | 链路 |
|------|------|
| 拾取消耗品 | E → ItemPickup → ItemInventory.Add 分流（叠加/入栏/入包/满拒） |
| 使用道具 | C → UseActive → 效果（v0.7.3 接 Health/PlayerStats）→ count−1，清零出槽 |
| 换武器（地牢） | 旧武器 → WeaponSatchel；包满挤出者原地掉落可捡回 |
| 换武器（准备场景） | 旧武器自动归位展台（原规则，不变） |
| 死亡重开 | Unequip + 武器背包清空（总纲决策：武器不保留） |

---

## 五、外部/用户确认需求

1. **换武器规则确认**：默认"旧武器入包、包满时包内武器原地掉落"——若你想改成"包满时旧武器直接原地掉落"或其他，实施前说一声（改动很小）；
2. **按键确认**：UseItem 默认 C；背包格交互 = 鼠标点击与道具栏互换【可改】；
3. **编辑器刷新**：切回 Unity 导入新脚本（含 .inputactions 变更，注意 Input System 资产重新导入）；
4. 本版**无美术需求**（槽框/图标全色块文字占位；正式技能图标在《资产需求总清单.md》P1，第 3 周）。

---

## 六、验收清单

> 场景：`v0_7_PrepRoom`（主测，测试道具摆放在此）→ `v0_7_ClassWeapon`；Game 1920×1080；回归 `v0_4_EnemySystem`。

1. `dotnet build Assembly-CSharp.csproj`：0 警告 0 错误（5 个新文件已补 csproj）；
2. **四槽**：屏幕右下出现四槽+背包 3 格；技能三槽显示"空"占位；布局不压弹药 UI（AmmoUI 与 SlotBarUI 相对位置核算，截图验证）；
3. **拾取分流**：连捡测试药——第 1 个入道具栏；同类再捡叠加、角标 2/3…；捡满 4 格（栏+3 包）后提示"背包已满"且地上拾取物保留；
4. **使用**：按 C 道具栏数量 −1；减到 0 槽位清空；点击背包格与道具栏互换成功；
5. **武器背包**：地牢内捡新武器 → 旧武器入包（无掉落物生成）；再捡一把 → 包内武器原地掉落可捡回；
6. **准备场景归位**：展台换初始武器仍自动归位（不生成掉落物、不入包）；
7. **死亡重开**：武器与背包清空、展台重置照旧；
8. **回归**：宝箱/法力球/祭坛/补给/换武器战斗全通；木桩场景无损。

---

## 七、风险与注意

| 风险 | 对策 |
|------|------|
| dropOldWeaponOnEquip 退役破坏准备场景归位 | 归位走 OnWeaponChanged 订阅不依赖该字段；验收 6 覆盖 |
| 四槽与 AmmoUI/拾取列表抢右下空间 | 布局时读 AmmoUI 位置避让；自检 §1 面板边界核算+截图 |
| .inputactions 改 JSON 格式错误 | 改完编辑器刷新验证 action 出现；结构照抄既有 Skill action |
| ItemStack 叠加与"暂无叠加上限"语义 | count 无上限 int；UI 角标超 99 显示 99+ |
| 测试道具/摆放残留进正式版 | v0.7.3 收尾清单明确删除 Consumable_Test 与测试摆放 |

## 八、收尾义务

- 更新 `now_use/README.md`（+Item/ 目录 5 脚本，清单 78→83，目录结构同步）；
- 自检清单 §3：输入行 +UseItem；新增"背包/槽位"行（4 槽+3 格+1 格规则）；
- 写 `v0.7.2操作.md`（三段式+待验收清单）；
- 总纲/玩法计划书 v0.7.2 执行卡标完成。

---

## 九、token 成本标注（本版）

- 🔴 无；
- 🟡 `PlayerController.cs`（输入分发，局部读）+ `PlayerInteractor.cs`（拾取候选链路，局部读，确认 ItemPickup 接入点）——本版最大读取，中等偏小；
- 🟢 5 个新文件全部纯写（UI/数据结构无既有代码依赖）；`PlayerWeaponHolder.cs` 已读（62 行小文件）；
- **实施方式：派 coder 子代理执行**，主对话只留计划与验收结论；
- build 策略：全部改完一次性 `dotnet build`。
