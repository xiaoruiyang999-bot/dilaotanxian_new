# v1.1.32 敌人 AI 升级：A* 寻路绕障 + 视野阻挡与丢失掷骰

## 需求

1. 敌人贴近障碍/墙时应更新**最短接近路径**，不再贴墙滑行；
2. 障碍物阻挡视野 → 敌人丢失目标；**50% 概率不丢失**、继续靠近角色。

## 实现

### 1. EnemyPathfinder（新文件，Enemy/）

- **局部网格 A***：起终点包围盒外扩 10 格（上限 44×44），8 向（对角需两正交邻格可走）、
  octile 启发式、整型代价；起/终点格被挡时环形扩搜贴最近可走格；
- **可行走判定**：逐格 OverlapBoxNonAlloc（Default|Obstacle）过滤——**静态阻挡 = 墙 TilemapCollider2D
  + Obstacle 层（石块/障碍物）**；玩家/敌人等动态体不算阻挡（敌人不需要绕开玩家）；
- **拉绳简化**：A* 折线用体宽三射线（中心±0.32）验证可跳段，输出稀疏航点；
- 零持续分配：网格字典/开放表/路径表全静态复用。

### 2. EnemyAI 集成

- `MoveWithPathing(goal)`：**直线体宽通畅→直接走（原行为保留）**；被挡→A* 绕行
  （0.35s 节流 + 目标漂移>1.5 强制重算），沿航点推进；寻路失败回退直线。
  替换四处追击移动：Melee / Skirmisher 靠近段 / Ranged 接近段 / Charger 追击段
  （后撤/横移/冲锋仍直线——与原手感一致）；
- **视觉子状态 `UpdateVision`**（每帧节流复用 EnemyPerception 缓存，mask=Default 墙|Obstacle）：
  - 看得见：复位丢失态，记录目击点；
  - **刚丢失一次性掷骰（behaviorRng）**：50% `ignoringLos` 忽略丢失继续追（重见目标才复位）；
    50% 进入目击点搜索——`MoveWithPathing(lastSeenPosition)` 走向最后目击点，
    3 秒超时或抵达即 `combat.SetTarget(null)` → ReturnToPatrol；
  - 各行为 UpdateChase 统一经 `ChasePrelude`（判定+弃目标），追击目标点 = ChaseGoal
    （可见/忽略丢失→玩家实时位置；搜索期→目击点）；
- 距离丢失（LosePlayerRange）语义不变，与视觉丢失并存（先到先弃）。

## 验收

1. 编译无错 → Play：引敌到房内石块/短墙后侧——被挡的敌人会**绕行最短路径**（不再贴墙滑）；
2. 躲到大石后：约半数敌人立刻停下→走向你最后出现的位置→搜索 3 秒后返回巡逻；
   另一半无视遮挡继续逼近（同一敌人单次丢失只掷一次骰，重见后重置）；
3. 直线无障时移动与原版一致；远程风筝/游击后撤/冲锋行为不变；
4. 同 seed 敌人 rng（SetBehaviorRng/behaviorRng）复现不变。

## 关联

EnemyPerception（LOS 查询/缓存复用，未改动本体）；R10 零 GC（全 NonAlloc+静态缓冲）；
障碍层语义 R6（Obstacle=可破坏障碍：挡移动+挡视野，与石块/障碍物一致）。
