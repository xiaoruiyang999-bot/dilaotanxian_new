# v1.0.2 死代码清理

> 日期：2026-08-25  
> 状态：已落盘；asset_refresh 后全量重编译零错误零警告；归档 GUID 反扫无悬空引用。

## 清理目标

删除项目中确认无消费方的死代码：现用脚本里的零调用方法，以及仍参与编译但只被旧版本场景引用的归档脚本。缩小 Assembly-CSharp 体积，消除"归档代码被误引用"的隐患面。

## 调查结论（删除依据）

- `WerewolfTransformation.ExitWolf()`：私有方法，全项目零调用。v0.6.8 起 T 键改为"狼↔兽化"切换，人形态退出路径已废弃，此方法为残留。
- v0_2/v0_3 共 5 个脚本（CharacterInput、WarriorCharacter、Enemy、WarriorAttack、IAttack）：仍编译进 Assembly-CSharp（无 asmdef），代码层零引用，仅被 `v0_2_Warrior.unity` / `v0_3_CharacterSystem.unity` 两个旧场景挂载，且两场景均不在 Build Settings。
- `v0_5_Dungeon.unity` 与全部 24 个 prefab：对归档代码零引用、无 missing script（逐 GUID 反查验证）。
- Framework/、v0_1/、v0_4/ 为空目录残留。
- 其余 70 个现用脚本经全量扫描无孤儿类、无注释掉的代码块、无 Debug_ 遗留脚本，`#if UNITY_EDITOR` 共 8 处均为有意的编辑器专用代码，保留。

## 删除清单

**代码**
- `Assets/Scripts/now_use/Player/WerewolfTransformation.cs`：删除 `ExitWolf()` 方法（原 263-272 行）

**归档脚本（含 .meta，整目录）**
- `Assets/Scripts/v0_2/`（CharacterInput.cs、WarriorCharacter.cs）
- `Assets/Scripts/v0_3/`（Enemy.cs、WarriorAttack.cs、Interfaces/IAttack.cs）
- `Assets/Scripts/Framework/`、`Assets/Scripts/v0_1/`、`Assets/Scripts/v0_4/`（空目录）及 Scripts/ 下对应 5 个文件夹 .meta

**旧场景（含 .meta）**
- `Assets/Scenes/v0_1_InitialHouse.unity`、`v0_2_Warrior.unity`、`v0_3_CharacterSystem.unity`、`v0_4_EnemySystem.unity`

**保留未动**：`Assets/_Recovery/`（恢复副本）、`SampleScene.unity`、`Palettes/v0_1/`（图块调色板可复用）。全部删除项均已提交 git（0788d23），可随时找回。

## 验证

- 删除前预检：now_use 全部 .cs 对 IAttack/CharacterInput/WarriorCharacter/WarriorAttack 及 v0_3 `Enemy` 类型零代码引用（grep 命中均为注释与 LayerMask 字符串）。
- 删除后反扫：5 个被删脚本的 GUID 在 Assets 全部 .unity/.prefab/.asset/.mat 中零残留。
- UnitySkills `asset_refresh` → 全量重编译（65.9s）：**success，0 error / 0 warning**，Domain Reload 后服务器正常恢复。
- Build Settings 仅含 `v0_5_Dungeon.unity`，删除无构建影响。

## 文档同步

按"三者同步"惯例更新了 AGENTS.md、`.zcode/skills/project-dev-rules/SKILL.md`、《开发必读_核心信息整合.md》中"v0_1~v0_4 归档、Framework 死代码"的表述（已删除，历史在 git），并同步 `now_use/README.md` 的"明确不在用"章节。

## 备注

- `AttackQuery.cs.meta` 的 GUID 为手写规律串（`f4e5d6c7...`），当前有效，仅提示未来有冲突风险，本次未动。
- now_use 根目录 7 个脚本（EnemyAffix 系、Projectile 系等）语义上可归入 Enemy/、Combat/ 子目录，属组织性整理，本次未动。
