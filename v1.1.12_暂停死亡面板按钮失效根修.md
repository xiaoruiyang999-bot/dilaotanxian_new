# v1.1.12 暂停/死亡面板按钮失效根修（GraphicRaycaster 缺失）

## 现象

ESC 暂停菜单"继续游戏"等按钮点击无效；Esc 本身能正常开/关菜单。死亡面板"点击任意处返回"同样无效。

## 根因

`PausePanel.Open()` 与 `DeathPanel.Show()` 创建的 Screen Space Overlay Canvas **没有挂 `GraphicRaycaster`**——
没有该组件，EventSystem 的指针射线不采集此画布上的任何 Graphic，**全部按钮点击静默失效**；
Esc 不受影响是因为它走 `PlayerInput.onActionTriggered`（"Cancel" 动作），不经过 UI 事件系统。
这正是"Esc 能关、按钮全灭"的完美解释。旧版暂停按钮大概率从未生效过（此前一直用 Esc 关闭，未暴露）。

对照：ClassSelectUI / CharacterSelectUI / MainMenuBar / SlotBarUI 均正确添加了 GraphicRaycaster（所以正常）。

## 修复

- `PausePanel.Open()`：`canvasGo.AddComponent<GraphicRaycaster>()`（v1.1.12）。
- `DeathPanel.Show()`：同款补齐。
- 全项目审计：所有"含按钮的代码画布"均已具备 GraphicRaycaster；
  `PlayerInteractor`/`MinimapController` 为纯展示画布**故意不加**（无 Raycaster = 点击穿透，不挡游戏操作）。

## 规范沉淀（速查已回写）

**代码构建的 Overlay Canvas 若含任何可交互控件，必须 `AddComponent<GraphicRaycaster>()`**；
纯展示画布不要加（防吞点击）。模板照 ClassSelectUI.Build。

## 验收

1. 编译无错 → Play 地牢 → Esc → 鼠标点"继续游戏"：面板关闭、游戏恢复、Console 出现
   `[Pause] 继续游戏：面板已销毁，timeScale=1，SuppressByUI=False`（v1.1.11 日志）。
2. "设置"按钮展开/收起音量区、"返回大厅"回准备场景，均应鼠标可点。
3. 死亡结算页点击任意处可返回。
