# v0.5.4.4.2 远程 AI 攻击失效修复

> 状态：**已完成 · 全部修复已落盘**
> 日期：2026-08-18
> 关联版本：v0.5.4.4.x（远程 AI 与战斗平衡设计）
> 关联文档：[v0.5.4.4_远程AI与战斗平衡设计计划](v0.5.4.4_远程AI与战斗平衡设计计划.md) / [v0.5.4.4.1_远程攻击执行链解耦计划](v0.5.4.4.1_远程攻击执行链解耦计划.md)

---

## 1. 问题描述

### 1.1 现象

Runtime 表现：远程射手（`Enemy_Ranged`）**所有攻击手段都失效**，但伴随
"前后踱步 / 反复蓄力 / 视觉上好像在准备攻击" 的假象，体验上比"完全不动"
更糟糕。Console 没有 Exception 或 NullReference，问题在参数与时序层。

### 1.2 用户原始假设

> 「是不是由于太依靠原来配置的状态机，导致所有的攻击手段都失效了？」

**结论**：**部分对**。新攻击框架（多招式 + 5 种 AI 行为 + 投射物 + 召唤 + 冲锋）
的**代码链路整体正确**，但底层参数（`EnemyStats` + `BehaviorConfig_Ranged`）
是一套"近战默认参数套到了远程" 的遗留配置，导致远程根本进不了 `Attack` 状态。
加上 Windup 期间 LOS 抖动 + 强制失败时**不进入冷却**，形成"反复蓄力反复取消"
的死循环，掩盖了"根本不攻击" 的真实根因。

---

## 2. 根因分析（端到端）

整个调用链的代码层面已被验证正确：

```
EnemyAI.UpdateChase_Ranged           [EnemyAI.cs:253-312]
  └─ 3 个 AND 条件（dist/LOS/CanAttack）
     └─ combat.TryStartAttack       [EnemyCombat.cs:281-316]
        └─ PickAttack + 距离/LOS 二次校验
           └─ EnterWindup          [EnemyCombat.cs:318-353]
              └─ UpdateWindup tick  [EnemyCombat.cs:365-384]
                 └─ EnterActive    [EnemyCombat.cs:386-453]
                    └─ FireProjectile → ProjectileEmitter.Emit → Projectile.Launch
```

GUID 链路、`behaviorType` 读取、Layer 映射、prefab 引用全部正确。
**断层在参数与状态机边界条件**。

### 2.1 三个高概率 bug（按概率排序）

#### 🅰 Bug A：**AI 永远走不到 Attack 分支**

| 字段 | 文件 | 改前 |
|------|------|------|
| `moveSpeed` | `Enemy_Ranged.prefab` (fileID `6461820464174567309`) | `2` |
| `detectionRange` | `Enemy_Ranged.prefab` (fileID `6461820464174567309`) | `8` |
| `preferredDistance.y` | `BehaviorConfig_Ranged.asset:16` | `7` |

玩家速度通常 ≥ 2，`detectionRange=8` 紧贴 `preferredDistance.y=7`：
玩家只要保持距离 ≥ 8，敌人就永远停在 `Approach` 状态，**根本进不了 Hold**，
更不会 `TryStartAttack`。

#### 🅱 Bug B：**Hold 区间是「纸面」Hold**

| 字段 | 改前 |
|------|------|
| `preferredDistance` | `(4, 7)` |
| `approachExitDistance` | `6.2`（迟滞带太靠左） |
| `retreatExitDistance` | `4.8`（迟滞带太靠右） |

迟滞带 `[4.8, 6.2]` 把 `preferredDistance.[4,7]` 进一步缩窄到实际 Hold 窗口
**只有 ≈ 0.8 单位宽**。玩家走位 → Approach↔Hold 抖跳 → 永远凑不齐一次 `TryStartAttack`。

位置：[EnemyAI.cs:266-275](Assets/Scripts/now_use/EnemyAI.cs#L266-L275)

#### 🅲 Bug C：**Windup→EnterActive 强制 LOS 失败不进入冷却**

| 字段 | 改前 |
|------|------|
| `windupTime` | `0.3` |
| `lineOfSightGraceTime` | `0.15` |
| `EnterActive` 强制 LOS 失败时 | `CancelAttack(false)`（不进入冷却） |

蓄力 0.3s，LOS 容忍 0.15s。Windup 期间 LOS 断 >0.15s 就开始累积 timer；
到 `EnterActive` 不看 grace、**直接强制刷新 LOS**，失败就
`CancelAttack(false)`——**且不进入冷却**，立刻切回 Approach→再蓄力→
再取消。每秒尝试攻击好几次，但**没有任何一次真的发射**。

**这是用户看到「所有攻击手段都失效」 的核心视觉表现**。

位置：[EnemyCombat.cs:386-395](Assets/Scripts/now_use/EnemyCombat.cs#L386-L395)

### 2.2 次要 bug

#### 次要 1：Projectile 扫掠半径偏小

`Projectile.cs:92` 用 `Physics2D.CircleCastAll(..., 0.12f, ...)`，但
`Projectile.prefab` 的 `CircleCollider2D radius=0.5`。`projectileSpeed=8`
+ `dt=0.016` → 步进 ≈ 0.128，扫掠直径 0.24 接近步进长度，**边缘命中
率偏临界**。不是 0，但偶尔"看起来打到了却没伤害"。

#### 次要 2：行为配置缩窄

不构成 bug，只是 `BehaviorConfig_Ranged` 的 `preferredDistance` / hysteresis
/ graceTime 三个参数互相挤压，把可攻击窗口几乎压缩到 0。

---

## 3. 修复方案

### 3.1 Fix 1：`Enemy_Ranged.prefab` 调整 `EnemyStats`

**目标**：让远程单位的移动速度能追上玩家，拉大检测范围让 `PreferredDistance`
有缓冲。

| 字段 | 改前 | 改后 | 理由 |
|------|------|------|------|
| `moveSpeed` | `2` | `3.5` | 玩家通常 ≥ 2，2 追不上；3.5 给玩家压力但不"瞬移" |
| `detectionRange` | `8` | `10` | 比 `preferredDistance.y` 大 3+，留反应窗口 |
| `losePlayerRange` | `12` | `14` | 跟 `detectionRange` 同比例 |

实际改动：[`Assets/Prefabs/Enemy_Ranged.prefab`](Assets/Prefabs/Enemy_Ranged.prefab)
第 35-46 行的 `m_Modifications` 中，针对 `fileID 6461820464174567309` 的
`moveSpeed` / `detectionRange` / `losePlayerRange` 三个 modification。

### 3.2 Fix 2：`BehaviorConfig_Ranged.asset` 调整距离与 LOS 容忍度

**目标**：放宽 Hold 区间，加宽 LOS grace time 让蓄力期间容忍短时遮挡。

| 字段 | 改前 | 改后 | 理由 |
|------|------|------|------|
| `preferredDistance` | `(4, 7)` | `(3, 6)` | Hold 区间 3 单位宽，对玩家仍是"安全区" |
| `approachExitDistance` | `6.2` | `6.0` | 贴 max，让 Hold 覆盖整个 preferred |
| `retreatExitDistance` | `4.8` | `4.0` | 贴 min，retreat 边界精确 |
| `lineOfSightGraceTime` | `0.15` | `0.2` | 小于 Windup 0.3s，让躲墙追踪能在蓄力内触发（见 Fix 3 说明） |

实际改动：[`Assets/Data/Dungeon/BehaviorConfig_Ranged.asset`](Assets/Data/Dungeon/BehaviorConfig_Ranged.asset)
第 16-24 行。

### 3.3 Fix 3：`EnemyCombat.EnterActive` 删除发射前强制 LOS 复查

**目标**：修复「预警已显示、但子弹被拦下不发射」（预警弹道常驻、子弹轨道不出现）。

**根因**：预警在 `EnterWindup` 就显示（只做了一次缓存 LOS 检查），但真正发射前
`EnterActive` 还有一次 `force-refresh` 的强制 LOS 复查，失败就 `CancelAttack`。
两套不同的 LOS 门控导致「预警先亮 → 子弹被拦」。

**修复**：删除 `EnterActive` 里的强制复查，让躲墙判定完全交给
`UpdateWindup` 的 `lineOfSightLostTimer + grace` 追踪。这样「预警显示」与
「子弹发射」由**同一套门控**决定。

```diff
 private void EnterActive()
 {
-    // Windup 结束、真正发射之前强制刷新 LOS，防止玩家在蓄力期间躲到墙后。
-    if (currentAttackData.IsProjectile && (currentTarget == null || perception == null
-        || !perception.HasLineOfSight(currentTarget, currentAttackData.TargetLayer,
-            currentAttackData.ObstacleLayer, true)))
-    {
-        CancelAttack(false);
-        return;
-    }
-
+    // v0.5.4.4.2 修复：删除 EnterActive 的强制 LOS 复查。
+    // 躲墙判定已由 UpdateWindup 里的 lineOfSightLostTimer + grace 持续追踪，
+    // 无需在这里二次复查；预警显示与发射现在由同一套 LOS 追踪门控。
     currentState = AttackState.Active;
```

实际改动：[`Assets/Scripts/now_use/EnemyCombat.cs`](Assets/Scripts/now_use/EnemyCombat.cs)
`EnterActive` 函数（约 386-397 行）。

**依赖**：`lineOfSightGraceTime` 必须**小于** `windupTime`，否则
`UpdateWindup` 的躲墙追踪在蓄力结束前永远触发不了（这也是 Fix 2 里
grace 从第一轮的 `0.5` 改回 `0.2` 的原因——`0.5 > 0.3` 会让追踪失效）。

### 3.4 Fix 4：`Projectile.Advance` 扫掠半径 0.12 → 0.25

**目标**：与 `Projectile.prefab` 的 `CircleCollider2D radius=0.5` 匹配，
避免弹道擦边漏命中。

```diff
-        RaycastHit2D[] hits = Physics2D.CircleCastAll(currentPosition, 0.12f,
+        // v0.5.4.4.2 修复：扫掠半径 0.12f → 0.25f，与 Projectile.prefab 的 CircleCollider2D
+        // radius=0.5 更匹配；0.12 在 8 m/s × dt 下容易擦边漏命中 Player。
+        RaycastHit2D[] hits = Physics2D.CircleCastAll(currentPosition, 0.25f,
             flightDirection, step, queryMask);
```

实际改动：[`Assets/Scripts/now_use/Projectile.cs`](Assets/Scripts/now_use/Projectile.cs)
`Advance` 函数（约 92 行）。

### 3.5 Fix 5：`EnemyCombat.UpdateWindup` 预警跟手

**目标**：让预警线在 Windup 期间实时指向目标当前方向/距离，而不是冻结在
起手那一刻（玩家一动预警就失效，变成误导）。

```diff
             if (lineOfSightLostTimer > grace)
             {
                 CancelAttack(false);
                 return;
             }
+
+            // v0.5.4.4.2 修复：预警跟手。Windup 期间每帧让预警线指向目标当前方向/距离，
+            // 避免预警冻结在 Windup 开始那一刻的位置（玩家一动预警就失效）。
+            if (attackIndicator != null && currentTarget != null)
+            {
+                Vector2 dirToTarget = (Vector2)(currentTarget.position - transform.position);
+                attackIndicator.SetDirection(dirToTarget);
+                attackIndicator.SetRadius(dirToTarget.magnitude);
+            }
         }
```

实际改动：[`Assets/Scripts/now_use/EnemyCombat.cs`](Assets/Scripts/now_use/EnemyCombat.cs)
`UpdateWindup` 函数（约 365-384 行）。

> 说明：预警线起点仍在敌人身上（`EnterWindup` 里 `Show()` 时脱离父物体时的世界位置），
> 敌人进入 `Hold` 后 `StopMoving()` 停住，起点不变；此处更新的是**方向 + 长度**，
> 覆盖「玩家移动导致目标方向变化」这一主要场景。

---

## 4. 修复清单（最终落盘）

| 编号 | 文件 | 改前 | 改后 |
|------|------|------|------|
| Fix 1.1 | `Assets/Prefabs/Enemy_Ranged.prefab` | `moveSpeed: 2` | `moveSpeed: 3.5` |
| Fix 1.2 | `Assets/Prefabs/Enemy_Ranged.prefab` | `detectionRange: 8` | `detectionRange: 10` |
| Fix 1.3 | `Assets/Prefabs/Enemy_Ranged.prefab` | `losePlayerRange: 12` | `losePlayerRange: 14` |
| Fix 2.1 | `Assets/Data/Dungeon/BehaviorConfig_Ranged.asset` | `preferredDistance: {x: 4, y: 7}` | `preferredDistance: {x: 3, y: 6}` |
| Fix 2.2 | `Assets/Data/Dungeon/BehaviorConfig_Ranged.asset` | `approachExitDistance: 6.2` | `approachExitDistance: 6.0` |
| Fix 2.3 | `Assets/Data/Dungeon/BehaviorConfig_Ranged.asset` | `retreatExitDistance: 4.8` | `retreatExitDistance: 4.0` |
| Fix 2.4 | `Assets/Data/Dungeon/BehaviorConfig_Ranged.asset` | `lineOfSightGraceTime: 0.15` | `lineOfSightGraceTime: 0.2` |
| Fix 3 | `Assets/Scripts/now_use/EnemyCombat.cs` | `EnterActive` 发射前 `force-refresh` LOS 复查，失败 `CancelAttack` | 删除该复查，发射与预警共用 Windup 追踪门控 |
| Fix 4 | `Assets/Scripts/now_use/Projectile.cs` | `Physics2D.CircleCastAll(..., 0.12f, ...)` | `Physics2D.CircleCastAll(..., 0.25f, ...)` |
| Fix 5 | `Assets/Scripts/now_use/EnemyCombat.cs` | 预警线冻结在 Windup 起手方向 | `UpdateWindup` 每帧 `SetDirection`/`SetRadius` 跟手 |

> 备注：未涉及的 `Enemy_Skirmisher` / `Enemy_Charger` / `Enemy_Summoner`
> 走的是相同 `EnemyAI` + `EnemyCombat` 框架，但它们各自的 behavior config
> 没有 Bug A/B 类型的参数失配。这一轮仅修远程。

---

## 5. 验证步骤

1. **打开 Unity 编辑器**，等资产重新导入（prefab + .asset 修改后 Unity 会自动
   刷新 meta/Prefab Variant）。
2. **Play 模式**进入有远程射手的房间（例如 `Room_Combat`），观察：
   - 玩家在 `[3, 6]` 距离内时，敌人应稳定进入 `Hold` 并尝试攻击
   - 命中玩家时 `DamagePopup` 出现，console 出现 `IDamageable.TakeDamage` 调用
   - 玩家走到墙后时，敌人蓄力期间不应反复重试，至少 1.5s 冷却
3. **Console 验证**：LogLevel 调到 Verbose，搜索 `[EnemyCombat]` /
   `[ProjectileEmitter]`，应无越界/NullReference 警告。
4. **回归测试**：
   - 近战敌人（如果场景里有）不受影响
   - 玩家被命中时 `PlayerHealth` 正常扣减
   - 投射物命中障碍物（Layer 7）正确自毁
   - 投射物命中目标（Layer 0）正确消除目标

---

## 6. 已知遗留 / 后续优化（不阻塞，没做）

- **Windup 内部取消（躲墙）不进冷却**：当前 `UpdateWindup` LOS 丢失超过 grace
  时 `CancelAttack(false)`，不进冷却、立即回 Chase 重新 `TryStartAttack`。
  `EnterActive` 复查删除后，这是**唯一的躲墙门控**。如果玩家在 Windup 内
  频繁遮挡，远程单位会反复「蓄力→取消→再蓄力」，视觉上又变回原来的抖动。
  后续若想避免，可让 `UpdateWindup` 的 LOS 失败也按 `applyCooldown=true` 处理。
- **Projectile 寿命**：当前 `maxLifetime=3s` 写死在 `Launch` 参数默认值。
  如果远程射手的 `projectileSpeed` 拉到 12+ 仍可能让子弹飞过半个房间。
  视后续平衡再加字段。
- **Hysteresis 参数化**：当前 `approachExitDistance` / `retreatExitDistance`
  是手动填的，未来可以让它们自动跟随 `preferredDistance`（例如
  `approachExit = preferredDistance.y - 0.1`），避免类似这次手动算错的
  情况。

---

## 7. 调查方法复用

这次调查把整个攻击链路（12 个节点，30+ 行号引用）全部梳了一遍，结论
分成"代码层没问题"和"配置层有问题"两个层次，**避免了一次大规模重写**。
后续任何远程攻击问题（含 Skirmisher、Charger、Summoner）都可以用同一
流程：

1. 读 `EnemyAI.cs` / `EnemyCombat.cs` 锁定调用链
2. 读 BehaviorConfig 与 AttackData 资产确认参数
3. 用 Unity Editor 的 Scene Gizmo 叠加 `OnDrawGizmos`（如果 EnemyAI/Combat
   实现了）现场观察 preferredDistance / 实际距离 / LOS 状态
4. 改完用 Console Verbose 跑一次完整循环

---

## 8. 变更摘要

```
Assets/Prefabs/Enemy_Ranged.prefab                                | moveSpeed / detectionRange / losePlayerRange 调整
Assets/Data/Dungeon/BehaviorConfig_Ranged.asset                   | preferredDistance / hysteresis / lineOfSightGraceTime 调整
Assets/Scripts/now_use/EnemyCombat.cs                             | 删除 EnterActive 强制 LOS 复查 + UpdateWindup 预警跟手
Assets/Scripts/now_use/Projectile.cs                              | Advance 扫掠半径 0.12 → 0.25
v0.5.4.4.2_远程AI攻击失效修复.md (本文档)                        | 新建
```

合计 4 个源文件改动 + 1 个文档新建；不改其他战斗框架 / 状态机本体。
改动分两类：**参数微调**（prefab + behavior config）与**攻击发射门控重构**
（EnemyCombat：删除发射前强制复查、预警跟手），后者是「预警出现但子弹不发射」
根因的修复。
