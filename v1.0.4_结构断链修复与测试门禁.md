# v1.0.4 结构断链修复与测试门禁（v1.0.3 审查报告执行）

> 日期：2026-08-31  
> 状态：已落盘；编译 0 错误 0 警告；EditMode 测试门禁 9/9 绿；三场景 Play 冒烟 0 运行时错误；准备房播放中层级正向验证通过。  
> 输入：`v1.0.3_第一轮项目结构审查报告.md` 第十节执行顺序。当前正式流程：**v0_7_PrepRoom（启动）→ v0_7_ClassWeapon（地牢）**（Build Settings 已收口，v0_5_Dungeon 为旧直连测试场景）。

## 一、执行总览（对照报告第十节）

| 报告步骤 | 结果 |
|---|---|
| 1. 5 个 Missing Script 产品决策与替代 | Pause 保留并重建；金币/三选一/结算/Meta 判定延期并清理残留（见二） |
| 2. 编译 Console 收尾 | 全程 0 错误 0 警告（含两次程序集重构） |
| 3. 唯一正式启动场景 + Build Settings | 收口为 v0_7 双场景（见三） |
| 4. Golden Path 冒烟 | 三场景 8-10 秒 Play 全部 healthy=0 错误；准备房运行时层级正向验证（见六） |
| 5. Resources 构建资源化 | 完成：28 个资产迁入 Resources 同构目录 + 5 个加载文件路径统一（见五） |
| 6. Missing Script + 纯逻辑最小测试 | EditMode 门禁 9/9 绿，首轮即抓到 2 个漏网孤儿 prefab（见四） |

## 二、P1-1：5 个 Missing Script 处置

### 产品决策（依据现行 v0.7.5 主循环）

| 场景对象（均在 v0_5_Dungeon） | 决策 | 依据 |
|---|---|---|
| PauseSystem | **保留，重建 PausePanel** | 报告推荐保留；AudioManager 仍保留 `SetSfxVolume/SetBgmVolume` + PlayerPrefs(`sfx_volume`/`bgm_volume`) 契约；输入资产无 "Pause" 动作 → 改听 Cancel(Esc)，ClassSelectUI 打开时让位 |
| CoinHUDSystem | **清理残留，金币延期** | 合并方有意删除 CoinDrop/PlayerStats 金币字段（七维数值无金币），v0.7.5 奖励循环=武器/法力包/消耗包掉落 |
| UpgradeSystem | **清理残留，三选一延期** | 同上：宝箱三权重直落拾取物，无升级券概念 |
| VictorySystem | **清理残留，结算延期** | 现行 RunManager 为无限楼层循环，无第 9 层通关概念 |
| MetaSystem | **清理残留，Meta/存档延期** | SaveSystem/MetaManager 均随合并删除；是否进下轮待定 |

### PausePanel 重建要点（`UI/PausePanel.cs`）

- 挂载模式同 MinimapSystem（场景空对象 PauseSystem），UI 运行时构建，零场景 YAML 手术。
- Esc(Cancel) 开关；职业选择 UI 打开时让位；玩家死亡流程中不暂停（重开协程 WaitForSeconds 受 timeScale 影响会卡死）。
- 修复旧版隐患：OnDisable 退订后不再补订（现 Update 惰性补订，订阅/退订严格配对）。
- 重开本局 = 与死亡重开同链路（ClearWeapon + ClassSelectUI.Close + LoadScene 准备场景）。
- 已挂载：v0_5_Dungeon（原位愈合）、v0_7_PrepRoom、v0_7_ClassWeapon（新增，顺带补 AudioSystem+AudioManager——此前 v0_7 正式双场景无音频系统）。

### 意外发现：GUID 复活自愈（教训回写）

在**原路径**重建同名脚本时，Unity 会把新 .meta 复用旧 GUID（Library 路径缓存），场景里的丢失引用**自动愈合**。本例 PausePanel.cs 重建后 v0_5_Dungeon 的 PauseSystem 断链无操作自愈。含义：误删脚本的黄金恢复期内在原路径重建同名类即可救回全部场景引用。

## 三、P1-2：Build Settings 收口

- 启用清单：`v0_7_PrepRoom`（索引 0，启动）→ `v0_7_ClassWeapon`（地牢）。
- 移除：v0_6 双场景、SampleScene（文件保留在工程，仅出构建清单）。v0_5_Dungeon 保留在工程作直连测试，不入构建。
- 依据代码链路：PrepRoomManager/PrepPortalInteractable 默认目标 `v0_7_ClassWeapon`，RunManager 死亡回 `v0_7_PrepRoom`。

## 四、P2：EditMode 测试门禁（9/9 绿）

`Assets/Tests/EditMode/`：MissingScriptScanTests（正式 3 场景 + 全部 Prefab 扫描）、DamageResolverTests（ApplyArmor 5 组边界值）、DungeonGeneratorTests（固定 seed 确定性 + BFS 全房连通）。

**门禁首轮即抓到漏网合并事故**：`Shop.prefab`（挂已删 ShopInteractable）与 `Projectile.prefab`（挂已删旧 Projectile）——全场景/prefab 零外部引用（新投射物走 Combat/Projectile 运行时代码构建），确认孤儿后删除。静态扫描能抓到 Play 冒烟抓不到的问题（missing script 不抛异常）。

### 程序集架构变更（排障结论，回写规则）

- **asmdef 按名字引用预定义程序集（Assembly-CSharp）静默无效**——Bee 生成的 rsp 里根本没有该引用。测试要引用游戏代码，游戏代码必须先有自己的 asmdef。
- 新增 `Assets/Scripts/now_use/now_use.asmdef`（程序集名 **Game**，引用 Unity.InputSystem / Unity.TextMeshPro / DOTween.Modules；DOTween 为老格式 DLL meta，走 overrideReferences + precompiledReferences=["DOTween.dll"] 显式引用）。
- 新增 `Assets/Plugins/Demigiant/DOTween/Modules/DOTweenModules.asmdef`（**DOTween.Modules**，等价 DOTween 官方 asmdef 安装）：DOFade/DOMove 等 Image/SpriteRenderer/Rigidbody2D 扩展在模块源码里（原编译进 Assembly-CSharp-firstpass，自定义程序集不可见）。
- 场景/Prefab 脚本引用走 GUID，程序集改名零影响（MissingScriptScanTests + 三场景冒烟复核通过）。

## 五、P2：Resources 构建资源化（完成）

- 迁移（连 .meta，GUID 不变、序列化引用不断）：`Data/Class`(3 SO)、`Data/Skill`(6)、`Data/Item`(3)、`Art/Characters/Warrior` 全目录（15 帧）、`Art/UI/SlotFrame.jpg` → `Assets/Resources/` 同构子目录。
- 路径统一原则：**Resources 相对路径 = 编辑器路径镜像**。更新 5 个文件的编辑器路径常量：ClassCatalog、SkillCatalog、PrepRoomManager、SlotBarUI、FrameAnimator（7 个 Editor*Dir 常量）。
- WalkFront/AttackSword/AttackSpear/Skill 帧目录尚未产出（代码本有兜底警告），新美术直接产到 Resources 下对应目录即可。
- 验证：编译 0 错误；测试 9/9；准备房 Play 运行时层级含 3 展台 + 3 消耗包 + 技能指示器（新路径正向加载证据）。

## 六、Golden Path 冒烟（报告步骤 4）

- v0_7_PrepRoom 8s：healthy，0 错误。v0_7_ClassWeapon 10s：healthy，0 错误。v0_5_Dungeon 8s（手术后回归）：healthy，0 错误。
- 正向验证：播放中层级 = 职业选择台 + 双武器展台 + 三消耗包 + Portal/Wall 结构完整。
- 未做：交互级全流程（点选职业→拾取武器→传送门进地牢→战斗→死亡重开）需真人 GUI 操作，留人工验收。**第一个运行时阻断点：无（启动层零阻断）。**

## 七、延期清单（进入下轮决策）

1. 金币经济 / 局内三选一 / 通关结算 / Meta+存档——后端已随 v0.7.5 合并删除，如恢复需按现行七维数值/职业武器技能体系重新设计，不是回滚旧代码。
2. 输入资产残留动作（Dash/Sprint 已下线、狼人 T 已退役）清理 + 教程文案键位表定稿（报告 §五）。
3. v0_6 双场景文件退役（是否删除）。
4. PlayMode 冒烟测试（玩家生成→首击→死亡重开）。
5. AudioManager 音效表 Clip 的 Inspector 配置（工程侧人工）。

## 八、修改文件清单

**代码**：新增 `UI/PausePanel.cs`、`now_use.asmdef`、`DOTweenModules.asmdef`、`Assets/Tests/EditMode/*`（asmdef + 3 测试类）；路径常量更新 ClassCatalog / SkillCatalog / PrepRoomManager / SlotBarUI / FrameAnimator。  
**场景**：v0_5_Dungeon（删 4 残留系统对象）；v0_7_PrepRoom、v0_7_ClassWeapon（各 +PauseSystem/PausePanel +AudioSystem/AudioManager）。  
**资产**：28 文件迁入 Assets/Resources；删 Shop.prefab、Projectile.prefab（孤儿）。  
**配置**：EditorBuildSettings.asset 收口。
