# v1.1.54 狼人兽化 UI 动态高度

## 行为

- 保留普通状态调参：`heightOffset=0.46`、`canvasSize=150×36`、`canvasScale=0.01`。
- 普通 UI 的最终高度在组件 Awake 时由 `HealthBarAnchor.localY + heightOffset` 快照，之后不再跟随兽化血条锚点跳变。
- 兽化目标高度独立为 `beastWorldHeight=1.91`（相对玩家根节点的世界高度）。
- Transform 演出每帧把视觉膨胀进度同步给能量 UI，UI 高度按同一 0→1 进度从普通高度插值到兽化高度。
- 主动退兽、能量耗尽和 Respawn 均把目标高度切回普通状态，并以 `heightReturnSmoothTime=0.18s` 平滑回落。

## 验证

- `_dev/compile_check.py`：145 个脚本，0 错误。
- `git diff --check`：通过。
- UnitySkills 运行验证曾因编辑器本地服务在脚本域重载后未重新注册而中止；未修改场景或预制体。
