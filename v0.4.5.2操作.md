# v0.4.5.2 Attack Framework Refactor 操作报告

## 一、版本目标

在 v0.4.5.1「攻击时机框架」基础上，完成**攻击动画与判定系统的架构级重构**：

- 统一 Player 与 Enemy 的攻击框架
- 拆分 `WeaponAnimator` 过重职责，实现动画、朝向、视觉、判定四层解耦
- 使攻击动画范围与实际判定范围 100% 一致
- 为 v0.5 技能系统、Boss 系统、武器系统预留完整扩展接口

## 二、相比 v0.4.5.1 的核心变化

| 维度 | v0.4.5.1 | v0.4.5.2 |
|---|---|---|
| 动画控制 | `WeaponAnimator` 负责动画 + 初始化 + 缩放 + 朝向 | `WeaponAnimator` 只负责 Tween 播放；`WeaponController` 负责朝向与视觉 |
| 攻击配置 | `AttackData` 保存 `StartAngle/EndAngle` | `AttackData` 纯配置，动画偏移由 `WeaponController` 根据 `AnimationType` 计算 |
| 判定方式 | `EnemyCombat.TryAttack()` 圆形距离判定 | `AttackQuery` 统一扇形/圆形判定，Player/Enemy 共用 |
| 攻击状态机 | `EnemyAI` 维护 Windup/Active/Recovery | `EnemyCombat` 接管完整攻击状态机，`EnemyAI` 只负责高层决策 |
| 输入系统 | 旧 `UnityEngine.Input` | 新 `UnityEngine.InputSystem`（`PlayerController`/`PlayerAimController`） |
| 攻击预警 | 圆形 Sprite，Scale 拉伸模拟扇形 | Mesh 运行时生成真正扇形；Mesh 不可用时回退到运行时生成的扇形 Sprite |
| 武器朝向 | `WeaponPivot` 多重控制，容易漂移 | `WeaponController` 唯一管理 `WeaponPivot`；Enemy 攻击期间锁定方向，禁止转身 |

## 三、新增文件

### 3.1 脚本

- `Assets/Scripts/v0_4/AttackAnimationType.cs`（已合并至 `AttackData.cs`）
  - `AttackAnimationType`：`Arc` / `FullCircle` / `Thrust` / `Spin` / `Throw`
  - `AttackShape`：`Fan` / `Circle` / `Box`

- `Assets/Scripts/v0_4/PlayerAimController.cs`
  - 玩家瞄准输入层
  - 使用 `UnityEngine.InputSystem` 读取鼠标位置，输出标准化 `AimDirection`
  - 预留 `Gamepad` / `Auto` 瞄准接口，供 v0.5 扩展

- `Assets/Scripts/v0_4/WeaponController.cs`
  - 武器朝向与视觉唯一管理者
  - 负责 `WeaponPivot` 基础朝向、`WeaponSprite` 长度/宽度/位置/缩放
  - 根据 `AttackData` 计算攻击动画局部偏移（`GetAttackStartAngle/EndAngle/RotateMode`）
  - 提供 `LockAttackDirection/UnlockAttackDirection/ResetAimToForward`

- `Assets/Scripts/v0_4/AttackQuery.cs`
  - 统一攻击判定组件
  - 根据 `AttackData.AttackShape` 执行扇形或圆形范围检测
  - 在 `ActiveMoment` 由 `PlayerCombat` / `EnemyCombat` 调用

### 3.2 配置文件

- `Assets/Data/PlayerAttackData.asset`
  - 玩家默认近战攻击配置
  - `AttackRange = 1.8`，`AttackAngle = 120°`，`AnimationType = Arc`

- `Assets/Data/EnemyMeleeAttackData.asset`
  - 更新数值：`AttackRange = 1.5`，`AttackAngle = 120°`，`AnimationType = Arc`

### 3.3 Prefab

- `Assets/Prefabs/Player.prefab`
  - Player 根节点 + `PlayerController` + `PlayerCombat` + `WeaponController` + `WeaponAnimator` + `AttackQuery` + `PlayerAimController`
  - `WeaponPivot` / `WeaponSprite` 子物体

- `Assets/Prefabs/Enemy.prefab`
  - Enemy 根节点 + `EnemyController` + `EnemyAI` + `EnemyCombat` + `WeaponController` + `WeaponAnimator` + `AttackQuery` + `AttackIndicator`
  - `WeaponPivot` / `WeaponSprite` / `AttackIndicator` 子物体

## 四、修改文件

### 4.1 `Assets/Scripts/v0_4/AttackData.cs`

- 删除 `AttackStartAngle` / `AttackEndAngle` 字段（动画算法不应放在配置中）
- 新增 `AttackShape` 与 `AttackAnimationType` 枚举
- 保留 `AttackRange`（判定半径）与 `AttackAngle`（判定扇形全角）
- 保留 `WindupTime` / `ActiveDuration` / `RecoveryTime` / `AttackEase` / `ActiveMomentRatio`
- 新增 `AttackDamage` / `AttackCooldown` / `TargetLayer`

### 4.2 `Assets/Scripts/v0_4/WeaponAnimator.cs`

- 彻底重写为**纯动画播放器**
- 删除对 `AttackData`、`EnemyAI`、`Player`、`Combat` 的任何引用
- 不再负责武器缩放、位置、朝向管理
- `Play()` 接口接收相对当前 `WeaponPivot.localRotation` 的局部偏移
- 支持 `RotateMode.FastBeyond360`，实现真正 360°/720° 旋转
- `Stop()` 只 Kill Tween，不修改 Transform

### 4.3 `Assets/Scripts/v0_4/AttackIndicator.cs`

- 彻底重写
- 使用运行时生成的 Mesh 显示圆形/扇形，替代 Scale 拉伸 Sprite 的椭圆方案
- 新增 `SetAngle()` / `SetDirection()` / `SetRadius()` / `SetColor()`
- 支持 `ShapeType.Circle` / `ShapeType.Sector`
- 修复 2D 相机下 Mesh 背面剔除问题（三角形顶点顺序反转）
- Mesh 初始化失败时自动回退到 `SpriteRenderer`，运行时生成圆形/扇形 Sprite
- `Show()` 时临时脱离父物体，固定世界位置与方向，避免跟随 Enemy 移动/旋转漂移；`Hide()` 时恢复原层级
- `ApplyTransform()` 在脱离父物体后重置 `rotation = identity`，避免父物体旋转与扇形内部方向叠加导致双重旋转
- `ApplyTransform()` 不再重置 `localScale`，避免覆盖 `UpdateSpriteVisual()` 设置的半径缩放
- `SpriteRenderer` 支持运行时生成任意角度扇形 Sprite（`CreateSectorSprite`）
- `sortingOrder = 100`，确保攻击预警在最上层显示
- 移除全部调试日志

### 4.4 `Assets/Scripts/v0_4/EnemyAI.cs`

- 删除内部 Windup/Active/Recovery 状态机与动画调用
- 只保留 `Patrol / Chase / Attack / ReturnToPatrol` 高层状态
- `Chase` 状态判断距离与冷却，调用 `EnemyCombat.TryStartAttack(target)`
- `Attack` 状态每帧调用 `controller.StopMoving()`，确保攻击期间完全静止，不再调用 `FaceTowards`
- 被攻击时强制切换到 `Chase` 状态

### 4.5 `Assets/Scripts/v0_4/EnemyCombat.cs`

- 彻底重写，接管完整攻击状态机
- 新增 `None / Windup / Active / Recovery` 子状态
- 持有 `WeaponController` / `WeaponAnimator` / `AttackIndicator` / `AttackQuery` 引用
- `EnterWindup`：锁定 `transform.right` 为攻击方向，显示扇形预警，调用 `WeaponController.LockAttackDirection()`
- `EnterWindup` 调用 `WeaponController.SetAimDirection(attackDirection, applyRotation: false)`，避免 Enemy transform 旋转与 `WeaponPivot.localRotation` 叠加导致双重旋转
- `EnterActive`：切换 `AttackIndicator` 为 DangerColor，调用 `WeaponAnimator.Play()`
- `OnActiveMoment`：隐藏预警，调用 `AttackQuery.ExecuteAttack()` 执行扇形判定
- `EndAttack`：停止动画，调用 `WeaponController.ResetAimToForward()`，进入冷却

### 4.6 `Assets/Scripts/v0_3/PlayerCombat.cs`

- 新增 `WeaponController` / `AttackQuery` / `AttackIndicator` 引用
- 攻击流程与 `EnemyCombat` 保持一致：`Windup → Active → Recovery`
- `StartWindup` 时缓存 `attackDirection`，防止攻击期间鼠标移动导致判定方向漂移
- `PerformFanAttack` 改为调用 `AttackQuery.ExecuteAttack()`

### 4.7 `Assets/Scripts/v0_3/PlayerController.cs`

- 删除 `HandleMouseAiming()`，不再直接旋转 `transform`
- 鼠标瞄准完全交给 `PlayerAimController` + `WeaponController`
- 解决 `transform.rotation` 与 `WeaponPivot.localRotation` 叠加导致的「武器转得比鼠标快」问题
- 输入系统全面切换为 `UnityEngine.InputSystem`（`PlayerInput` + C# Events）

### 4.8 `Assets/Scripts/v0_4/WeaponController.cs`

- `SetAimDirection(Vector2 direction, bool applyRotation = true)` 新增 `applyRotation` 参数
  - `true`：立即将角度写入 `WeaponPivot.localRotation`（用于 Player，Player transform 不旋转）
  - `false`：只记录 `aimAngle` 供 `GetAimDirection()` 使用，不修改 `localRotation`（用于 Enemy，避免双重旋转）
- `GetAttackStartAngle/EndAngle` 返回相对当前锁定朝向的局部偏移，不再包含 `aimAngle`
- `ResetAimToForward()` 将 `aimAngle` 清零并应用 `identity`，使 Enemy 武器攻击结束后自然跟随父物体

### 4.9 `Assets/Scenes/v0_4_EnemySystem.unity`

- 场景对象改用 `Player.prefab` 与 `Enemy.prefab`
- 配置 `PlayerCombat` / `EnemyCombat` 的 `AttackData`、`WeaponController`、`WeaponAnimator`、`AttackIndicator`、`AttackQuery` 引用
- 配置 `WeaponController` 的 `WeaponPivot`、`WeaponSprite`、`AttackData` 引用
- `Enemy.prefab` 中 `AttackIndicator` GameObject 改为默认 active（`m_IsActive = 1`），不再通过 `SetActive` 控制显隐

## 五、未修改文件

以下系统保持原样，未做任何修改：

- `Assets/Scripts/v0_4/EnemyController.cs`
- `Assets/Scripts/v0_4/EnemyStats.cs`
- `Assets/Scripts/v0_4/EnemyHealth.cs`
- `Assets/Scripts/v0_4/TrainingDummy.cs`
- `Assets/Scripts/v0_3/Health.cs`
- `Assets/Scripts/v0_3/PlayerStats.cs`
- `Assets/Scripts/v0_3/WorldSpaceHealthBar.cs`
- `Assets/Scripts/v0_3/Interfaces/IDamageable.cs`
- Camera、UI、Movement、Physics 设置

## 六、架构对比

### v0.4.5.1

```
EnemyAI
  ├── 攻击状态机（Windup/Active/Recovery）
  ├── 调用 WeaponAnimator.PlayAttack()
  ├── 调用 EnemyCombat.TryAttack()
  └── 直接控制 AttackIndicator

WeaponAnimator
  ├── 读取 AttackData
  ├── 控制 WeaponPivot 旋转
  ├── 控制 WeaponSprite 缩放/位置
  └── 负责命中时机回调
```

### v0.4.5.2

```
EnemyAI（决策层）
  └── 调用 EnemyCombat.TryStartAttack(target)

EnemyCombat（攻击状态机）
  ├── Windup：锁定方向，显示 AttackIndicator
  ├── Active：调用 WeaponAnimator.Play()，触发 AttackQuery
  └── Recovery：停止动画，恢复武器朝向

WeaponController（朝向与视觉层）
  ├── 管理 WeaponPivot 基础朝向
  ├── 管理 WeaponSprite 长度/宽度/位置
  └── 提供攻击动画局部偏移

WeaponAnimator（纯动画层）
  └── 播放 Tween 修改 WeaponPivot.localRotation

AttackQuery（判定层）
  └── 根据 AttackData 执行扇形/圆形范围检测

AttackIndicator（表现层）
  └── 运行时生成 Mesh/Sprite 显示攻击范围
```

## 七、解决的问题

| 问题 | 原因 | 修复 |
|---|---|---|
| WeaponAnimator 职责过重 | 同时负责动画、缩放、朝向、初始化 | 拆分为 `WeaponController`（朝向/视觉）+ `WeaponAnimator`（纯 Tween） |
| Player/Enemy 攻击逻辑不一致 | 两套代码路径 | 统一为 `WeaponController` + `WeaponAnimator` + `AttackQuery` |
| Player 武器初始位置错误 | `WeaponAnimator` 每次攻击才初始化 | `WeaponController.Awake/OnValidate` 中 `RefreshWeaponVisual()` |
| Player 武器不跟随鼠标 | `WeaponPivot` 无人持续控制 | `PlayerAimController` 每帧输出 `AimDirection`，`WeaponController.SetAimDirection()` 应用 |
| 武器转得比鼠标快 | `PlayerController` 与 `WeaponController` 双重旋转 | 删除 `PlayerController.HandleMouseAiming()` |
| Enemy 武器不能 360° 旋转 | `DOLocalRotate` 未使用 `FastBeyond360` | `WeaponAnimator` 支持 `RotateMode.FastBeyond360`，`WeaponController` 根据 `AnimationType` 选择模式 |
| 攻击判定与动画范围不一致 | 判定用圆形，动画用扇形，数据不共享 | `AttackQuery` 与 `WeaponController` 共用 `AttackData.AttackRange/AttackAngle` |
| AttackIndicator 显示椭圆/位置漂移 | Scale 拉伸 Sprite 模拟扇形；作为子物体跟随 Enemy 移动 | 运行时生成真正扇形 Mesh/Sprite；`Show()` 时脱离父物体固定世界位置 |
| Enemy 攻击时武器方向漂移 | `EnemyAI` 攻击期间持续 `FaceTowards` | `EnemyAI.UpdateAttack` 删除 `FaceTowards`，每帧 `StopMoving()` |
| Enemy 武器挥动方向错位/每次旋转后不同 | `EnemyController.FaceTowards` 与 `WeaponController.SetAimDirection` 双重旋转（θ + θ = 2θ） | `WeaponController.SetAimDirection` 新增 `applyRotation` 参数；Enemy 传 `false` 只记录方向不修改 `localRotation` |
| AttackIndicator 扇形方向随 Enemy 旋转变化 | `AttackIndicator` 脱离父物体时保持 Enemy 旋转，与扇形内部方向叠加 | `ApplyTransform()` 在脱离父物体后重置 `rotation = identity` |
| AttackIndicator 显示后 scale 被重置为 1 | `ApplyTransform()` 覆盖 `UpdateSpriteVisual()` 设置的半径缩放 | `ApplyTransform()` 在 `isDetached` 时不再修改 `localScale` |
| AttackIndicator 激活/去激活报错 | `OnDisable()` 调用 `Hide()` 导致 `SetActive` 递归 | `GameObject` 改为默认 active，显隐完全通过 `renderer.enabled` 控制 |
| Input System 报错 | 旧代码使用 `UnityEngine.Input` | 全面替换为 `UnityEngine.InputSystem` |

## 八、v0.5 扩展预留

本次重构已为以下系统预留完整接口：

- **技能系统**：替换 `AttackData` 即可改变攻击范围、角度、动画类型、命中时机。
- **Boss 系统**：`AnimationType.Spin/FullCircle` 支持 360°/720° 旋转攻击。
- **武器系统**：`WeaponController.RefreshWeaponVisual()` 支持动态切换武器长度与宽度。
- **多段攻击**：`AttackQuery` 可多次调用，`AttackData` 可配置不同 `ActiveMomentRatio`。
- **投射物/突刺**：`AnimationType.Throw/Thrust` 已预留枚举。
- **手柄/自动瞄准**：`PlayerAimController` 已预留 `Gamepad` / `Auto` 分支。

## 九、验证清单

### 玩家

- [ ] 武器始终跟随鼠标方向
- [ ] 攻击开始锁定方向，攻击期间不随鼠标移动
- [ ] 攻击结束后恢复跟随鼠标
- [ ] Arc 动画为 120° 扇形，与判定范围一致
- [ ] 无漂移、无瞬移、无双重旋转

### 敌人

- [ ] 追击时武器随 Enemy 正常朝向
- [ ] 进入攻击范围后停止移动，不再转身
- [ ] Windup 显示黄色 120° 扇形预警
- [ ] Active 显示红色扇形，武器沿扇形方向挥动
- [ ] ActiveMoment 执行一次扇形判定，扇形外目标不受伤害
- [ ] Recovery 结束后恢复追击

### 通用

- [ ] Console 无 `NullReferenceException`
- [ ] Console 无 `GameObject is already being activated or deactivated`
- [ ] Console 无 Input System 报错
- [ ] AttackIndicator 显示为真正扇形，非椭圆
- [ ] UI、血条、Camera、TrainingDummy 均正常

## 十、后续工作

- v0.4.5.3 将补充音效、特效、屏幕震动与命中反馈。
- v0.5 将基于本框架实现技能系统与 Boss 技能。
