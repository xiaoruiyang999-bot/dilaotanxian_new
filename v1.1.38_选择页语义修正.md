# v1.1.38 选择页交互语义修正（卡=选中，选择键=确定）

## 更正

v1.1.30 我把"点击即确定"同时绑给了颜色卡和选择键——理解错了用户意图。正确语义（用户澄清）：

- **颜色卡区域点击 = 选中**（高亮预览，可反复切换比较，不应用）；
- **"选 择"键点击 = 确定**（应用并关闭——继承原确认键职责）。

## 改动（CharacterSelectUI / ClassSelectUI 对称）

- 整卡 Button 的点击回调退回 `selected/Select(data)` 仅高亮；
- "选 择"键保持 `Pick / Select+ApplyAndClose` 确定；
- 独立"确 认"键维持移除（v1.1.30 正确部分）；Esc 取消不变。

## 验收

点卡片只切换高亮可反复比较；点某卡的"选 择"键直接应用该卡并关闭（角色页接续职业页）。

## 关联

v1.1.30（确认键移除，保留）/ v1.1.9 卡片布局。
