# v0.7.5.1 AttackIndicator 合并重复修复

> 日期：2026-08-31  
> 原因：战斗/MCP 分支与 v0.7.5 main 合并后，`AttackIndicator` 同时保留两套 Box Mesh 实现。

## 修复

- 删除重复的 `BuildBoxMesh()`，解决 `CS0111`。
- 删除 `RebuildMesh()` 中重复的 `case ShapeType.Box`，避免后续 `CS0152`。
- `SetBox(length, width)` 统一写入保留实现消费的 `currentWidth`，防止矩形预警宽度回退到默认值。
- 保留 `SetBoxSize` / `currentWidth` 路径，矩形表现仍由调用方提供的判定几何尺寸驱动。

## 验证

- 文件级检查：`BuildBoxMesh` 仅 1 个定义，`ShapeType.Box` 仅 1 个 switch 分支。
- Unity 编辑器进程正常响应；UnitySkills REST 服务在 Domain Reload 后未自动恢复，因此本次未取得最终编译回报，需在 Unity Console 继续确认下一条错误。
