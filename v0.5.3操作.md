# v0.5.3 房间类型系统 操作报告

> 本文档记录 v0.5.3 的实际执行内容与结果，对应《v0.5 Dungeon 系统开发计划书》五-D 节与《v0.5.3_正式计划》。
> 前置版本：v0.5.2（随机内容生成，见 `v0.5.2操作.md`）。
> **合并说明（2026-07-23）**：本文档已并入原 `v0.5.3.1操作.md`（尺寸分级 / 精英混编 / 商店陈列迭代）与「全部房间 1.5×」调整，描述 v0.5.3 的**最终交付形态**。原 v0.5.3.1 文档已废止。

## 一、版本目标

1. 七种房间类型（Start / Combat / Elite / Treasure / Shop / Event / Boss）落地：`RoomTypeAssigner`（纯 C#）选址、`RoomTypeConfig`（SO）驱动地板着色 / 清房条件 / 内容装配；
2. **房间尺寸分级**（整格扩展，图结构不动、零走廊）：普通 30×18 / Elite 2×1 或 1×2 长条（62×18、30×38）/ Boss 2×2 竞技场（62×38）；
3. **精英房混编**：普通 + 精英穿插，至少 1 精英（`minCount` 保底）；Boss 房严格 1 Boss + 1~2 小怪；
4. **商店陈列**：商品一列排放 + 球下展台层（拾取后展台保留）；
5. 宝箱 / 祭坛 walk-over 占位交互；精英 / Boss 用 prefab 变体占位。

随机流：布局流 `Random(seed)`（不动）＋ 类型/尺寸流 `Random(seed*31+7)`（Assign → Expand 依次消耗）＋ 房间内容流 `Random(seed*7919+roomId)`（不动）；祭坛 ± 属运行时事件。

## 二、核心设计落实

| 设计 | 落实情况 |
|---|---|
| RoomTypeAssigner（纯 C#） | Start/Boss 由 Generator 定死不碰；Treasure→Shop→Event 固定顺序选址（叶子优先 → distance≥2 → 任意余房）；剩余 Combat 中 distance≥2 按 eliteChance 抽 Elite |
| 独立类型/尺寸流 | `seed*31+7` 派生，不接布局流尾部——实测同 seed（12345）布局与 v0.5.2 逐房一致（rooms=8、bossRoom=#7、bossDist=5 完全相同） |
| RoomSizeExpander（纯 C#） | Boss 优先 2×2（四角 rng 打乱，失败回退 2×1/1×2 再回退 1×1）；Elite 四方向扩展 1 格；只吞空闲格、占用即时登记；`RoomNode` +`spanX/spanY`（默认 1×1） |
| Builder 变尺寸 + 着色 | `TileRect`/`InteriorRect` 取代单格算术；`CarveDoor` 按含墙矩形判向、门洞在重叠段居中（1×1 与旧算术逐点等价）；`Tilemap.SetColor` 按类型着色（**前置修复：FloorTile m_Flags LockColor→0**）；门洞格保持米黄作「门槛」 |
| RoomTypeConfig ×7 | 类型→floorTint / clearCondition / contentProfile；Builder 硬编码 `ConditionFor` 与 `defaultContentProfile` 已移除 |
| 精英混编保底 | `SpawnTable.Entry` +`minCount`；`EnemySpawner` 两段式（保底先行 → 权重抽满 → 洗牌）；精英表 = 精英 minCount1 w1 + 普通 w2，count 3~4 |
| Boss 严格 1 个 | Boss 表 = Boss **权重 0** minCount1 + 普通 w1，count 2~3（权重 0 不被 PickEntry 选中但 minCount 生效，见 5.1） |
| 商店陈列 | `SpawnTable` +`SpawnLayout{Random,Row}` + `rowSpacing`；`InteractableSpawner.SpawnRow` 房中心横轴一列；Shop 表 = [Heal, Armor, Heal] Row 2.5 |
| 展台 | Supply prefab 重构：根（trigger + Interactable）→ Pedestal（深石灰 (0.38,0.34,0.30)，sortingOrder 1）+ Orb（y+0.45，sortingOrder 2）；visual 指 Orb——消耗后球压暗、展台保留 |
| 精英 / Boss 占位 | Prefab Variant：Elite（1.3×、暗红、HP 6、伤 2、索敌 8）；Boss（2×、深红、HP 20、伤 2、windup 0.4、range 2.5、索敌 12）；5 处 attackData 引用全覆盖；武器视觉长度由 `WeaponController.OnValidate` 按 attackRange 自动同步（预期行为） |
| 大房间内容 | `ObstacleTable_Large` / `DecorationTable_Large`（count 3~5）供 Elite/Boss Profile；Boss 房无障碍（竞技场留空） |

## 三、新增文件

### 3.1 脚本（`Assets/Scripts/v0_5/`）

| 文件 | 说明 |
|---|---|
| `Generation/RoomTypeAssigner.cs` | 类型分配（纯 C#，rng 由 Generator 传入） |
| `Generation/RoomSizeExpander.cs` | 整格尺寸扩展（纯 C#） |
| `Core/RoomTypeConfig.cs` | 类型配置 SO（着色/清房条件/Profile） |
| `Interaction/Interactable.cs` | walk-over 交互基类（触发/一次性/压暗收口） |
| `Interaction/ChestInteractable.cs` | 宝箱 +2 HP |
| `Interaction/ShrineInteractable.cs` | 祭坛 ±（运行时随机） |
| `Interaction/SupplyInteractable.cs` | 补给（Heal/Armor 枚举） |
| `Content/InteractableSpawner.cs` | 交互物生成（散点间距自维护+冲突重试 / Row 一列） |

### 3.2 Prefab

- `Assets/Prefabs/Dungeon/`：`Chest`（金黄）、`Shrine`（紫）、`Supply_Heal` / `Supply_Armor`（Pedestal+Orb 结构）
- `Assets/Prefabs/`：`Enemy_Elite` / `Enemy_Boss`（Enemy.prefab 变体）

### 3.3 SO 资产

- `Assets/Data/`：`EnemyEliteMeleeAttackData`（伤2/程1.7）、`EnemyBossMeleeAttackData`（伤2/程2.5/摇0.4/冷1.2）
- `Assets/Data/Dungeon/`：`EnemyTable_Elite/Boss`、`ObstacleTable_Large`、`DecorationTable_Large`、`InteractableTable_Treasure/Shop/Event`、`RoomContentProfile_Elite/Boss/Treasure/Shop/Event`、`RoomTypeConfig_×7`

## 四、修改文件

| 文件 | 改动 |
|---|---|
| `Assets/Tilemaps/v0_1/FloorTile.asset` | `m_Flags` 1（LockColor）→ 0，解锁 `Tilemap.SetColor`（视觉零变化） |
| `Generation/DungeonLayout.cs` | RoomNode +`spanX/spanY` |
| `Generation/DungeonGenerator.cs` | 两条 return 路径：typeRng → Assign → Expand；TryGrow 零改动 |
| `Core/DungeonConfig.cs` | +`bossCellSpan/eliteCellSpan` |
| `Core/DungeonBuilder.cs` | `defaultContentProfile`→`roomTypeConfigs[]`；TileRect/InteriorRect；PaintRoom 着色；CarveDoor 矩形判向+重叠段居中；清房条件数据驱动；SpawnContent 按类型装配 + InteractableSpawner |
| `Core/DungeonManager.cs` | Gizmos 7 色；Validate 扩展（特殊房规则 + 占用格不相交断言 + Boss/Elite 扩展达成率统计） |
| `Content/RoomContentProfile.cs` | +`interactableTable` 字段 |
| `Content/SpawnTable.cs` | Entry +`minCount`；+`SpawnLayout`/`layoutMode`/`rowSpacing` |
| `Content/EnemySpawner.cs` | 两段式保底混编 + 洗牌 |
| `DefaultDungeonConfig.asset` | roomWidth 18→20→**30**、roomHeight 11→12→**18**（用户确认整体 1.5×）；span=2 |
| 场景 `v0_5_Dungeon.unity` | DungeonBuilder 接入 7 份 RoomTypeConfig；验收后 seed 复位 0 |

未改动：生长/BFS/Boss 选址/特殊房选址算法、Room/Door/RoomTrigger 脚本、Player/Enemy/Combat/Weapon/UI 全部现有脚本、Enemy.prefab 本体、物理碰撞矩阵、Input Actions。

## 五、遇到的问题与修复

### 5.1 Boss 房生成 3 个 Boss（权重语义误用，已修）

Boss 表初配 [Boss w1 minCount1 + Enemy w1]，权重抽取会再中 Boss → 一房 3 Boss。修复：Boss 条目权重改 **0**（`PickEntry` 跳过 0 权重，minCount 保底不受影响）→ 严格 1 Boss + 1~2 小怪。注意：**Play 模式中改 SO 停止后回滚**，首改在 Play 中，停止后重改并读回 YAML 确认。

### 5.2 商店补给丢件（间距冲突不重试，已修）

`InteractableSpawner` 初版间距冲突直接 `continue` 丢件（trigger 不被 OverlapCircle 看见）→ 商店 3 商品只出 2 个。修复：冲突整体重试（上限 10 次）。后商店改 Row 陈列，房中心排放天然规避。另一教训：脚本外部编辑后必须 `asset_refresh` 再进 Play。

### 5.3 REST 批量设置含私有字段整批静默失败（复发两次，经验固化）

`component_set_property_batch` 混入私有字段（如 `supplyType`）→ 整批不落盘且无逐项报错。**固化规范**：批量只放公共属性；私有 [SerializeField] 一律 `component_set_serialized_property` 单项；资产改动后必读回 YAML 核验。REST 加碰撞体必须显式设 size（v0.5.1 Door 以来第四次同类）。

### 5.4 变体「可疑」Transform 覆盖实为预期行为

变体设 attackData 后出现的 WeaponSprite 缩放/位置覆盖，系 `WeaponController.OnValidate` 按 attackRange 同步武器视觉的既有逻辑，非错误。

### 5.5 Console 1 条 frustum Error 为环境伪影（非游戏 bug）

`Screen position out of view frustum`（UnityEditor.GUIView）：`PlayerAimController` 每帧 `Camera.ScreenToWorldPoint(鼠标)`，自动验收期鼠标停在 Game 视图外所致，本版本之前即存在，构建版无此问题，不动代码。

### 5.6 REST 验收伪影（沿用前版结论）

瞬移玩家不触发 OnTriggerEnter2D；禁用 PlayerController + linearVelocity 注入可推动（首次读取可能因刚体睡眠未动，需二次注入）。宝箱走触验收即以此完成。真实 WASD 走路无此问题。

## 六、验证清单

### 自动化验收（已通过 ✅；seed=12345，8 房 7 连接）

- [x] 编译 0 Error / 0 Warning（游戏代码 Console 干净，仅 5.5 环境伪影）
- [x] 每层必有且仅有 1 Start + 1 Boss；Treasure/Shop = 1/1 符合配置；Treasure 落叶子房
- [x] **同 seed 复现**：两次 Play 房型/span/内容完全一致；与 v0.5.2 布局逐房零偏差（布局流零污染）
- [x] 精英抽取规则：eliteChance=1 时 distance≥2 三房全 Elite、distance=1 保持 Combat
- [x] **尺寸分级**：普通 30×18（最终 1.5×）；Elite 62×18（横）与 30×38（纵）均出现；Boss 62×38（Bounds 实测）
- [x] **精英混编**：精英房 2 精英 + 2 普通，每房 ≥1 精英；洗牌生效（精英索引不固定）
- [x] **Boss 房严格 1 Boss + 1~2 小怪**（5.1 修复后实测）；Boss HP=20、Elite attackData=新资产
- [x] **商店一列 3 展台**（y 相同、间距 2.5、居房中心、Heal-Armor-Heal），Pedestal+Orb 层级正确（截图 `v0531_shop.png`）
- [x] 宝箱 walk-over：+2 HP、一次性、压暗消耗态；Treasure 进房直接 Cleared 门不关
- [x] 地板着色：Start 绿灰 + 米黄门槛（`v053_spawn.png`）；Boss 深红竞技场（`v0531_bossroom.png`）；30×18 普通房（`v0531_big_combat.png`）
- [x] 门洞在重叠墙段居中：7 门齐建（含 Boss 2×2 连接）
- [x] Validate 1000 Seeds：失败 0/1000（含占用格不相交断言）；**Boss 2×2 达成 100%，Elite 扩展 95%（501/527，失败回退 1×1 符合尽力满足）**

### 待人工 Play Mode 验收

- [ ] 各类型地板着色观感；30×18 普通房与大房间的空间感
- [ ] 祭坛 ± 交互；商店治疗/护甲球拾取（球压暗、展台保留）
- [ ] 精英混编与 Boss 竞技场战斗手感；杀 Boss 后房间 Cleared
- [ ] Gizmos 7 色标记；调 DungeonConfig/SpawnTable 参数即改变生成（数据驱动）

## 七、扩展预留（已为后续版本埋好的接口）

| 预留 | 用途 |
|---|---|
| `RoomTypeConfig` | 新房间类型 = 加枚举值 + 一份资产 |
| `Interactable` 基类 | v0.5.4 `PortalInteractable` 直接继承；商店经济挂货币检查 |
| `ChestInteractable.ApplyEffect` | 技能/装备系统奖励发放点 |
| Boss 房 `OnRoomCleared` | v0.5.4 奖励结算 + 传送门监听点 |
| `SpawnTable.Entry.minCount` | 任何「保底 N 个」需求（宝箱保底、阶段召唤） |
| `SpawnLayout.Row` + Pedestal | 商店经济：价格牌/售罄态挂展台，与商品解耦 |
| `RoomNode.spanX/spanY` | 未来房间模板/多尺寸房型；1.5× 方形精英房待走廊化后支持 |

## 八、后续工作

- **v0.5.4 完整楼层循环**（计划书五-E）：RunManager、Cleanup、PortalInteractable、ApplyFloorScale、死亡重开。
- 已知占位接受项：Elite 为长条房（1.5× 方形需走廊化，列入未来扩展）；Boss 血条/探测圈随体型放大；祭坛受伤被护甲全吸收时惩罚无感；5.5 编辑器噪音。
