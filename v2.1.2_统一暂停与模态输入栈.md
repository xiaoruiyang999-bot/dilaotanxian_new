# v2.1.2 统一暂停与模态输入栈

> 完成日期：2026-09-21  
> 设计真源：`计划书/v2.1_三流派肉鸽核心与单向DAG总计划.md`

## 实际完成

- 新增 `GameModalService`：以令牌管理暂停原因，以 LIFO 栈管理顶层输入焦点。
- `TimePauseArbiter` 合并 UI Pause 与 Hit Stop；并发 Hit Stop 取最长，到期不会恢复仍被 UI 暂停的世界。
- 暂停菜单及设置页使用独立嵌套令牌；关闭设置只返回暂停菜单，不恢复战斗。
- 叙事、叙事档案、DAG 地图、路线选择、奖励选择、职业角色选择、技能树、死亡与胜利结算接入统一模态栈。
- `PlayerController` 成为 Cancel 单一分发点：Esc 先关闭顶层模态，无模态时才打开暂停菜单。
- 模态打开时统一屏蔽移动、攻击、Dash、技能、道具与世界交互；关闭最后一层的同帧保持屏蔽，防止确认点击穿透。
- Hit Stop 兼容入口保留，但不再直接写 `Time.timeScale`。
- 保留既有开局入口：Build Settings 第 0 场景仍为 `v0_7_PrepRoom`；玩家先在准备房间苏醒/复活、确认配置并主动交互传送门，随后才进入地牢。

## 奖励与路线合同

- 贤者/物资临时三选一允许用 Esc 暂退，暂退不结算、不改节点状态。
- 候选继续由节点 `RewardSeed` 确定，重新打开保持同一组候选。
- 清房后贤者节点生成临时石碑，物资节点生成临时宝箱；再次按 E 可重新打开奖励。
- 未确认奖励时 `Completed=false`，右出口不能打开路线选择，也不能前往下一节点。
- 奖励确认后交互物进入已领取表现，节点完成并保存；路线图可关闭，再次与右出口交互可重开。
- 临时石碑/宝箱只承担 v2.1.2 的模态与门禁验证，正式资源和正式奖励职责留给 v2.1.4/v2.1.5。

## 验证

- `Game.csproj` 编译：0 错误。
- `EditModeTests.csproj` 编译：0 错误。
- v2.1.2 专项 EditMode：6/6 通过。
- 完整 EditMode：205/205 通过。
- Unity 6000.5.4f1 在线验证：`v0_7_ClassWeapon` 场景校验 0 问题，10 秒 Play smoke 0 运行时错误并正常退出。
- Unity Console 错误 0；项目场景与 Prefab Missing Script 0。
- 已覆盖：嵌套关闭、顶层 Esc、不可取消层输入消费、Hit Stop 最长请求/UI 优先、同 Seed 候选复现、未领奖路线锁定。

## 2026-09-23～24 回归修复与原因更正

- Game 视图边框原因已由用户确认：Game 视图的 Gizmos 开关会叠加白线与绿色框；关闭 Gizmos 后消失。`ColliderOutlineDebug` 默认开启也可能另外绘制碰撞线及图例，现改为默认关闭、F9 显式开启，作为独立降噪改进。曾为边框加入的 `PlayerUI` 运行时 Canvas 脱离父节点代码与根因无关，已撤销。
- 原因二：地牢入口无条件加载旧 ActiveRun。`RunManager` 先应用职业满属性，随后又以旧快照覆盖当前生命/护甲与 `currentNodeId`，所以出现半血、0 护甲以及首个分支已被代选。准备房传送门现固定开始新 Run，并在切场景前删除旧 ActiveRun；未来续局必须使用独立入口。
- 补齐了暂停菜单手动重开与死亡面板立即返回的同步清理。此前死亡返回可能先卸载场景，使 `RunManager.RestartRun` 的延迟协程来不及执行删除。
- 新增门禁断言：初始化后当前节点必须是 Start，三个直接后继均未 Selected/Visited。专项 `DungeonGraphGeneratorTests` 9/9、完整 EditMode 205/205 通过；准备房 Play 观察 0 运行时错误。
- 已补齐 `计划书/v2.1.2_统一暂停与模态输入栈_计划书.md` 与 `计划书/v2.1.2_问题汇总与修复记录.md`，区分目标合同、实际完成和问题证据。

## 遗留

- 需要在 Game View 人工走查实际按键、鼠标点击与视觉层级。
- 正式贤者刻印、物资背包和商店交易分别在 v2.1.3～v2.1.6 接入；它们必须复用本版令牌与模态栈，不得重新直接写 `Time.timeScale`。
- `project-dev-rules` 位于 `.zcode/skills/project-dev-rules/SKILL.md`；后续版本按该规则和新增的逐版本文档门禁执行。
