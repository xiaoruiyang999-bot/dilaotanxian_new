# v1.1.25 地皮恢复原套件逐格铺装（等比例 + 大小自适应）

## 现象与根因

用户反馈地面需要更改：当前整片地面为**单张底图无限平铺**（RoadMask mesh 路径用 grass_base/dirt_base
各一张 85px 纹理 shader 整层 repeat），观感单一无变化；v1.1.20 预览版 TerrainPainter 又临时关闭了
全部边/角/贯通过渡。

## 恢复方案（用户拍板：等比例放回原素材 + 大小自适应）

- **逐格铺装回归**：`BuildRoadMaskSurface` 改为对每个地面格 `TerrainPainter.PaintCell`；
  RoadMask mesh+shader 路径**休眠保留**（底图升级多变体图集后恢复一行调用即可重启）。
- **过渡全套恢复**：v1.1.4 的 8 邻域 Bitmask 映射回归——土格/三面围合→dirt_base；
  单侧→edge、相邻双侧→corner（旋转补全四象限）、相对双侧→multi（±90°）、对角→象限角块。
- **新基底融合**：主基底用新图（grass_base/dirt_base），装饰池沿用 v1.1.20 池名
  （grass_decor=原 clean+斑驳 / dirt_decor=dirt_core 8 变体——纯土区主体第一次有了专用变体图）。
- **大小自适应**：85px@PPU85 契约（=1 格 1 单位）+ GroundTileImportContract 后处理器 Folder 级
  强制（新图自动配好导入），任意 85px 素材放对名字即生效。
- 距离场装饰概率同 v1.1.4（D=2→30%、D≥3→12% 装饰斑）；土格 35% 概率 dirt_decor 变体点缀。
- 逻辑层 TerrainMask 零改动（草/土二值+距离场），同 seed 复现不变。

## 验收

Reload → Play：地面为逐格铺装的草地/土斑，土斑边缘有贴边/转角/贯通过渡，土区为纯土+变体点缀；
无整片单纹理平铺感；跨房间土斑连续；同 seed 重开一致。

## 关联

v1.1.4（Bitmask 铺装原版）/ v1.1.20（RoadMask 预览版，休眠）/ GroundTileImportContract（导入契约）。
