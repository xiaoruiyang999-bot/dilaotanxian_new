# v0.6 开发计划书：职业与武器系统（含玩家性能升级）

## 版本信息
- **版本号**：v0.6（含 v0.6.0 ~ v0.6.5 六个子版本）
- **版本名称**：职业与武器系统 —— 玩家性能升级 + 三职业六武器 + 技能 + 道具/宠物框架
- **前置版本**：v0.5.4（随机地牢 + 楼层循环已完成）
- **美术策略升级**：由"单色块"升级为**模块化多色块组合**（多个独立 Sprite 拼接、职业色/敌我识别规则化），详细规格见姊妹文档《v0.6_美术清单.md》；仍不设 Animator，动效靠代码驱动
- **总目标**：把玩家从"单一近战角色"升级为"三职业、六武器、有闪避/奔跑/技能"的完整可玩角色，并搭好道具/宠物两大系统的框架接口
- **约束**：
  1. 沿用现有门面模式、事件驱动、SO 数据驱动架构，不做推翻式重构；
  2. 对已有模块的改动全部在 1.3 表中提前列明，未列明的脚本零改动；
  3. 武器、职业、技能数值全部走 ScriptableObject，禁止硬编码；
  4. 六把基础武器强度配平原则：**大差不大**（DPS 差异控制在 ±20% 以内，见第六章）；
  5. 除 WASD 移动与鼠标左键攻击外，**全部按键为可自定义预设**（见 4.11）。

> 本计划书是 v0.6 的总纲。各子版本开工时按第五章对应小节出执行文档（含完整代码），风格沿用 v0.4.6/v0.5 正式计划；每个子版本收尾时补执行记录。

---

## 〇、需求确认与决策记录（2026-07-27，含修订）

| # | 议题 | 决策 |
|---|------|------|
| 1 | 移动"一闪一闪"问题 | 根因已定位：Player.prefab Rigidbody2D **插值关闭**（`m_Interpolate: 0`）+ FixedUpdate 写速度；敌人侧 EnemyAI 在 Update 里写 `linearVelocity` 同样错位。修复方案见 4.1 |
| 2 | 闪避/奔跑机制 | **体力条驱动**：闪避单次固定消耗体力（带无敌帧 + **内置短 CD**，动作完整结束后才进入 CD），奔跑持续消耗体力，体力自动回复。**闪避不打断蓄力进度**（蓄力状态保留，见 4.6） |
| 3 | F 键技能 | 采纳：**战士·旋风斩 / 射手·后跃射击 / 法师·奥术法阵**。**法力不可自动回复**（见 4.4）；技能均有 CD 与技能图标 CD 表现（见 4.7） |
| 4 | E 键交互 | **全部交互统一走 E 键**；装备/道具/宠物为**两段式拾取**（靠近提示 → 按 E → 列表选择，见 4.3）；**单物品按 E 直接拾取不弹列表**；**小资源球（法力球）走 walk-over 自动吸附，不占 E 键** |
| 5 | 道具/宠物系统范围 | **框架 + 少量示例**：修饰器/效果接口与宠物接口 + 各 1~2 个最小示例 |
| 6 | 武器职业限制 | 武器带职业标签，**不可跨职业混拿**；后续所有武器必须带职业标签 |
| 7 | 开局流程 | **初始准备房间**：出生房放置三个展台（1 职业选择台 + 2 武器展示台），交互式选择，**不再用开局暂停弹窗**（见 4.5） |
| 8 | 死亡重开规则 | 职业默认上一次选择（可重选确认）；**武器不保留**（重新在展台选取）；道具/宠物清空（见 4.5） |
| 9 | 换武器规则 | 旧武器**原地掉落**可捡回；地图上的武器掉落物**略微缩小 + 固定图标呈现**（见 4.3/4.6） |
| 10 | 按键自定义 | 除 WASD/左键外均为预设绑定，后续可通过自定义修改（Input System Rebind，见 4.11） |
| 11 | 敌人攻击检测范围 bug（v0.5 遗留） | 三种敌人的攻击**触发**范围都被固定成普通怪数值，与实际打击范围（AttackData）不一致：玩家已在精英/Boss 打击范围内，敌人仍要走近才出手。v0.6.0 修复（见 4.1.1） |
| 12 | 美术策略 | 升级为多色块模块化拼接；剑类采用 WeaponRoot + Blade + Guard + Handle 结构；角色/敌人/UI 全部重新设计（见《v0.6_美术清单.md》） |
| 13 | 字体方案 | 微软雅黑动态字体：标题 28pt / 职业按钮 22pt / 确认 20pt / 展台名 16pt（已确认，实际效果进游戏后再微调） |

---

## 一、现状分析（v0.5.4 完成度）

### 1.1 可复用资产（本版本在其上扩展，不推翻）

| 资产 | 路径 | 复用方式 |
|------|------|----------|
| 玩家五件套 | `PlayerController/PlayerStats/Health/PlayerCombat/PlayerAimController` | 保留，扩展体力/法力/闪避/蓄力 |
| 武器攻击框架 | `AttackData/WeaponController/WeaponAnimator/WeaponHitbox/AttackIndicator` | 近战武器直接复用；`SetAttackData()` 是换武器现成挂点 |
| 敌人系统 | Enemy 五件套 + AI + 房间休眠制 | 保留；移动节奏与攻击触发范围两处修复（1.3） |
| 地牢系统 | Dungeon 全套 | 保留；出生房改造为准备房间（三展台） |
| 交互基类 | `Interactable` + 4 个派生类 | 范式改造：walk-over → E 键两段式（见 4.3） |
| 商店展台表现 | Supply 陈列（Row 布局 + 基座） | 展台美术与陈列方式参考（见 4.5） |
| 输入系统 | `PlayerInputActions.inputactions`（Move/Attack） | 新增 Dash/Sprint/Interact/Skill 四个**可重绑** action |
| UI | `PlayerUI` + 世界空间双条 | 屏幕面板加体力条/法力条/弹药/技能图标 |

### 1.2 现有架构与 v0.6 需求的冲突点

| 冲突 | 现状 | 后果 | 对策（所属子版本） |
|------|------|------|--------------------|
| 刚体插值关闭 | Player.prefab `m_Interpolate: 0` | 渲染帧采样物理位置跳变，移动"一闪一闪" | 开插值 + 敌人移动挪 FixedUpdate（v0.6.0） |
| 敌人攻击触发范围错误 | 三种敌人共用普通怪的攻击触发距离，与实际打击范围（各自 AttackData.range）脱节 | 精英/Boss 手长优势失效，玩家站在其打击范围内却不挨打 | 触发距离改读当前 AttackData 范围（v0.6.0，见 4.1.1） |
| 攻击系统是单击近战 | PlayerCombat 单击三阶段挥击 | 无法支持蓄力、远程子弹、弹夹 | 蓄力态 + 子弹系统 + 武器行为层（v0.6.2/v0.6.3） |
| 没有子弹概念 | 全项目无投射物 | 弓箭/连弩/能量法杖无法实现 | 新建 Projectile 系统（v0.6.3） |
| 交互是 walk-over | Interactable 基类 OnTriggerEnter2D 一次性触发 | 与"按 E 拾取"需求冲突 | Interactable 基类改造（v0.6.1） |
| 属性只有 HP/护甲/移速 | PlayerStats 三字段 | 无法支撑技能（法力）与体力条 | 四属性体系（v0.6.0/v0.6.2） |
| `IDamageable` 只有 `TakeDamage(float)` | 治疗法杖无法回血 | Health 加 `Heal(float)`（不进接口） | v0.6.3 |

### 1.3 对已有模块的全部预期改动（小口径接入点，提前列明）

| 模块 | 改动 | 所属子版本 |
|------|------|------------|
| `Player.prefab` / Enemy 各 prefab | Rigidbody2D `Interpolate = Interpolate`（**修复闪烁的最大头**） | v0.6.0 |
| `EnemyAI` | ① 移动写入从 Update 挪到 FixedUpdate；② **攻击触发距离改读 EnemyCombat 当前 AttackData 的范围 + 小缓冲**（bug 修复，见 4.1.1） | v0.6.0 |
| `PlayerStats` | +maxStamina/stamina 回复、+maxMana/mana（不可自动回复）+`ApplyClass(ClassData)` | v0.6.0 / v0.6.2 |
| `PlayerController` | +Dash/Sprint/Skill 输入分发与状态 | v0.6.0 / v0.6.4 |
| `PlayerCombat` | +蓄力状态（长按蓄力、松手释放、**闪避保留蓄力**）、+按 WeaponData 分支近战/远程 | v0.6.2 / v0.6.3 |
| `Interactable` 基类 + 4 派生类 | walk-over → E 键交互（基类改触发方式，派生类逻辑不动） | v0.6.1 |
| `PlayerUI` | +体力条、+法力条、+弹药/换弹显示、+技能图标（CD 扫面）、+拾取提示与列表 | v0.6.0 ~ v0.6.4 |
| `RunManager` | +初始准备房间流程（展台状态、一局一次职业选择、死亡重开默认上次职业）；**删除原"开局 timeScale=0 弹窗"方案** | v0.6.2 |
| `Health` | +`Heal(float)`；+无敌帧标记（闪避用，TakeDamage 早退） | v0.6.0 / v0.6.3 |
| `ChestInteractable` | 奖励从 +2HP 占位改为武器/道具掉落（启用既定挂点） | v0.6.3 |
| `PlayerInputActions` | +Dash/Sprint/Interact/Skill，全部为**可重绑预设**（Move/Attack 固定不可改） | v0.6.0 起分批 |
| 场景 | 复制 `v0_5_Dungeon.unity` → `v0_6_ClassWeapon.unity` | v0.6.0 |
| `Health`/`PlayerUI` | 清理遗留调试 `Debug.Log` | v0.6.0 |

除上表外，Dungeon 侧、Combat 判定层（WeaponHitbox）**零改动**。

---

## 二、总体目标与设计原则

### 2.1 总体目标

1. 移动手感修复与升级：消除闪烁，新增闪避（无敌帧、内置短 CD、不打断蓄力）与奔跑，体力条驱动；修复敌人攻击触发范围 bug；
2. 交互统一：全部交互物 E 键两段式拾取，带范围提示与可拾取列表；小资源球 walk-over 吸附；
3. 职业系统：战士/射手/法师三职业，属性差异化，准备房间展台式选择；
4. 武器系统：六把基础武器（每职业两把），职业锁定，蓄力/弹夹/换弹机制；
5. 技能系统：F 键，每职业一个技能，法力不可自动回复 + 技能 CD 图标；
6. 道具/宠物系统：框架与接口 + 各 1~2 个示例；
7. 美术升级：模块化多色块，三职业角色可区分、不与怪物混淆，怪物普通/精英/Boss 三档可区分。

### 2.2 设计原则（沿用 v0.5 并新增）

1. **数据与表现分离**、**数据驱动**、**低耦合**、**最小侵入**：同 v0.5，不变。
2. **武器 = 数据，行为 = 策略**：WeaponData SO 描述全部参数；近战/远程/蓄力/弹夹差异由行为组件按数据分支。
3. **强度配平可验证**：每把武器理论 DPS 可核算，六把差异 ±20% 以内（第六章）。
4. **先框架后内容**：道具/宠物只建接口与最小示例。
5. **按键即预设**（新增）：除 WASD/左键外，新按键一律走 Input System 独立 action + 可重绑绑定，杜绝硬编码 KeyCode。

---

## 三、总体架构

### 3.1 新增/扩展模块全景

```
now_use/
├── Player/
│   ├── PlayerController     （扩展：Dash/Sprint/Skill 输入分发）
│   ├── PlayerStats          （扩展：四属性 HP/护甲/体力/法力 + ApplyClass）
│   ├── PlayerCombat         （扩展：蓄力态 + 武器行为分支）
│   ├── PlayerMovement.cs    【新】移动执行层：基础移动/奔跑/闪避冲刺统一写速度
│   ├── PlayerInteractor.cs  【新】E 键交互：候选探测 + 提示 + 可拾取列表 + 触发
│   └── Health               （+Heal、+无敌帧标记）
├── Class/                   【新目录】
│   ├── ClassType.cs         枚举：Warrior / Archer / Mage
│   ├── ClassData.cs         SO：职业属性/技能引用/可用武器列表/角色美术引用
│   ├── ClassSelectUI.cs     "选择你的职业"界面（三职业按钮 + 确认按钮）
│   └── PrepPedestal.cs      展台：职业选择台 / 武器展示台（E 交互）
├── Weapon/                  【新目录】
│   ├── WeaponData.cs        SO：职业标签/行为类型/蓄力规则/弹夹换弹/子弹引用/AttackData 引用/美术引用
│   ├── WeaponInstance.cs    当前武器运行时状态（弹夹/换弹计时/蓄力计时）
│   ├── WeaponBehavior.cs    行为基类：MeleeBehavior（复用现有三件套）/ RangedBehavior（发射子弹）
│   └── WeaponPickup.cs      武器掉落物（E 拾取，校验职业标签；地图上缩小+固定图标）
├── Combat/
│   ├── Projectile.cs        【新】子弹：直线飞行 + Trigger 命中 IDamageable + 撞墙销毁
│   ├── ProjectileData.cs    【新】SO：速度/伤害/存活时间/尺寸/美术
│   └── （现有 WeaponController/Animator/Hitbox/Indicator 不动）
├── Skill/                   【新目录】
│   ├── SkillData.cs         SO：法力消耗/冷却/技能类型/图标名字
│   ├── SkillExecutor.cs     按类型执行：Whirlwind / LeapShot / ArcaneZone
│   ├── SkillIconUI.cs       技能图标：CD 秒表扫面 + 剩余秒数（uGUI Radial360 Fill）
│   └── ArcaneZone.cs        法阵：持续 OverlapCircle 跳伤害 + 减速
├── Item/                    【新目录，v0.6.5】
│   ├── ItemData.cs / IItemEffect.cs / StatModifier.cs / ItemPickup.cs
└── Pet/                     【新目录，v0.6.5】
    ├── PetData.cs / PetBase.cs / PetPickup.cs
```

### 3.2 数据流（新增部分）

```
开局：第 1 层出生房 = 准备房间（三展台）→ E 交互职业选择台 → ClassSelectUI
      → 点职业（边框高亮）→ 点确认（边框短暂高亮）→ UI 关闭、刷新两个武器展台
      → E 拾取展台上武器 → 出房开始探索

攻击：输入 → PlayerCombat（蓄力计时，闪避不清空）→ WeaponBehavior 分支
      ├─ 近战 → 现有三件套（AttackData 运行时副本按蓄力缩放范围）
      └─ 远程 → Projectile.Launch(ProjectileData 按蓄力修正伤害/速度)

技能：F → SkillExecutor（验法力、验冷却）→ 扣法力进 CD → 三技能执行器
      → SkillIconUI 进入 CD 扫面（Radial360 fill + 剩余秒数）

交互：PlayerInteractor 探测 → 候选高亮 + "按 E"提示 → 按 E
      ├─ 普通交互物（宝箱/祭坛/补给/传送门/展台）→ 直接触发
      └─ 可拾取物 1 个 → 直接拾取；≥2 个 → 弹出列表 → 选择 → 拾取

法力：不可自动回复；来源 = 法力瓶（大量）/ 击杀掉落法力球（少量，walk-over 吸附）/ 特定技能或宠物
```

---

## 四、核心设计

### 4.1 移动优化（v0.6.0）

**问题根因**（代码勘察确认）：
1. `Player.prefab` Rigidbody2D `m_Interpolate: 0` —— 物理 50Hz 改位置、相机渲染帧跟随，采样错位导致角色相对相机周期性跳变；
2. `EnemyAI` 在 `Update()` 里每渲染帧写 `rb.linearVelocity` —— 敌人与玩家相对运动同样错位。

**修复方案**：
- Player 与三种敌人 prefab 的 Rigidbody2D 全部开 `Interpolate`；
- EnemyAI 改为"Update 决策存移动意图、FixedUpdate 统一写速度"；
- `CameraFollow` 维持 LateUpdate + SmoothDamp 不变（插值开启后采样即对齐）；验证后不达标再评估 Cinemachine（本版本不引入新包）。

**验收**：60fps 与 144fps 下平移、斜向移动、穿门均无肉眼可见抖动/闪烁。

#### 4.1.1 敌人攻击触发范围 bug 修复（v0.5 遗留，随 v0.6.0 一起修）

- **现象**：三种敌人（普通/精英/Boss）的攻击**触发**距离相同（普通怪数值），但三者的实际打击范围（各自 AttackData.range，Boss 武器更长）不同——玩家已站在精英/Boss 打击范围内，敌人却要继续走近到"普通怪距离"才出手。
- **根因**：EnemyAI 的"是否进入攻击范围"判定用了固定/共用数值，没有读当前装备的 AttackData 范围。
- **修复**：EnemyAI 攻击触发距离 = `EnemyCombat.CurrentAttackData.AttackRange + 0.3（缓冲，防边界抖动）`；三份敌人 AttackData（普通/精英/Boss）本已存在且范围不同，接线即修复。
- **验收**：玩家分别站在三种敌人的打击范围边缘（略小于武器实际范围）时，敌人站定即出手；不再多走一段。

### 4.2 闪避 / 奔跑 / 体力条（v0.6.0）

**体力条（Stamina）**：
- maxStamina = 100；UI 加在屏幕面板 HP/护甲条旁；
- 回复：停止消耗 0.8 秒后开始回复，30/s；体力不足时无法闪避/无法继续奔跑。

**闪避（Dash，默认 Space，可重绑）**：
- 向当前移动方向冲刺；无移动输入时向瞄准方向冲刺；
- 冲刺距离 ≈ 3 格，时长 ≈ 0.18s（期间由 PlayerMovement 直接写速度，覆盖常规移动）；
- **无敌帧**：冲刺全程无敌（Health 加 invincible 标记，TakeDamage 早退）；
- 消耗体力 35；
- **内置冷却**：闪避动作**完整结束后**才进入短 CD（建议 0.5s，可调），CD 期间无法再次闪避；体力消耗与内置 CD 双重限制——CD 偏短保证手感连贯，体力是主要限制；
- **不打断蓄力**：闪避期间蓄力计时与蓄力等级原样保留，闪避结束继续（见 4.6）；
- 表现：角色半透明 + 拖尾残影（DOTween 纯色残影）。

**奔跑（Sprint，默认按住 Shift，可重绑）**：
- 移速 ×1.6；持续消耗体力 20/s；松开或体力耗尽恢复常速；
- 奔跑中可攻击；蓄力期间移速 ×0.5（不与奔跑叠加，蓄力减速优先）。

### 4.3 E 键交互与两段式拾取（v0.6.1）

**Interactable 基类改造**：删除 OnTriggerEnter2D walk-over 触发，改为 `Interact(PlayerContext)` 虚方法；一次性消耗/压暗/`OnConsumed` 钩子保留；宝箱/祭坛/补给/传送门四派生类逻辑零改动。

**PlayerInteractor 与两段式拾取流程**：

```
靠近可交互物（半径 ≈1.2，OverlapCircle 0.1s 一次）
  → 候选高亮（呼吸放大 1.1）+ 提示"按 E"
按 E：
  ├─ 普通交互物（宝箱/祭坛/补给/传送门/展台）→ 直接触发
  └─ 可拾取物（武器/道具/宠物）：
        仅 1 个 → 直接拾取（不弹列表）
        ≥2 个  → 角色旁边弹出周围小半径内全部可拾取物列表
                 （纯文字，每行物品名；默认高亮最近项；鼠标滚轮或数字键切换；
                   再按 E 拾取高亮项；Esc 或走开关闭列表）
```

- **例外**：小资源球（法力球）走 walk-over 自动吸附，不占 E 键、不进列表（元气骑士式）；
- 武器掉落在地图上：**略微缩小（≈0.7×）+ 固定小图标呈现**（每把武器一个固定图标，见美术清单），与手持形态区分；
- 换武器：拾取新武器时旧武器**原地掉落**（生成对应 WeaponPickup），可捡回；
- 拾取列表 UI 本版本纯文字；图标与属性显示为后续扩展（接口预留）。

### 4.4 属性体系：四属性（v0.6.0 / v0.6.2）

| 属性 | 作用 | 回复 |
|------|------|------|
| HP | 生命，扣完死亡 | 不自动回复（治疗法杖/补给可回） |
| 护甲 | 优先吸收伤害 | 脱战 3 秒后 0.5/s（现有逻辑不变） |
| 体力 | 闪避/奔跑消耗 | 停止消耗 0.8s 后 30/s |
| 法力 | 技能消耗 | **不可自动回复**。来源仅三种：①法力瓶拾取（大量回复）；②击杀敌人掉落法力球（少量，**walk-over 吸附**）；③特定技能/宠物回复（框架预留） |

- PlayerStats 扩展为四属性 + `ApplyClass(ClassData)`；全部变更走事件刷新 UI；
- 击杀掉蓝：敌人死亡 → 掉落小法力球（建议数值：普通怪 +3、精英 +8、Boss +20，SO 可调），玩家走近自动吸附；
- 法力瓶：作为新掉落物加入 SpawnTable 体系（补给/宝箱内容），大回复（建议 +40）。

### 4.5 职业系统与初始准备房间（v0.6.2）

**三职业数值（初始建议值，SO 可调）**：

| 职业 | maxHP | maxArmor | maxMana | 职业色 | 特色 |
|------|-------|----------|---------|--------|------|
| 战士 Warrior | 8 | 8 | 30 | 红 | 近战高生存，技能应急（一局次数有限，靠击杀回蓝） |
| 射手 Archer | 4 | 5 | 60 | 绿 | 远程风筝，技能拉开距离 |
| 法师 Mage | 3 | 3 | 100 | 蓝紫 | 法力驱动，法阵控场 |

**初始准备房间**（取代原"开局暂停弹窗"方案）：
- 一局的第 1 层出生房 = 准备房间，放置**三个展台**（参考 v0.5 商店展示台的基座表现，多色块组合美术，见美术清单）：1 个**职业选择台** + 2 个**武器展示台**（初始为空）；
- 职业选择台与武器展示台**外观明确不同**；走近职业选择台时头顶显示名称"**职业选择台**"（世界空间标签，复用 WorldUIRoot 模式）；
- **职业选择流程**：E 交互职业选择台 → 展开矩形 UI，标题"**选择你的职业**" → 三个职业按钮纵向排列 → 点击某职业，该按钮**边框高亮** → 点击 UI 中间偏下的**确认按钮** → 确认按钮边框短暂高亮（DOTween 闪烁 ≈0.3s）→ UI 关闭 → **两个武器展台刷新**，各呈现该职业的一把初始武器 → 玩家走近按 E 拾取其中一把（另一把留在展台可换）；
- **字体（已确认）**：Legacy Text + 系统动态字体"微软雅黑"；标题 28pt、职业按钮 22pt、确认按钮 20pt、展台名称 16pt；进游戏审查效果后再微调；
- **死亡重开**：职业默认上一次选择（职业选择台显示上次职业为已选状态，可直接确认或更换）；武器**不保留**（两个武器展台刷新为当前职业的两把武器，重新选取）；道具/宠物清空；
- 职业在游戏内不可更换（本版本）；武器只能拾取本职业武器（职业标签校验，不匹配提示"职业不符"）。

### 4.6 武器系统（v0.6.2 框架 / v0.6.3 六武器）

**WeaponData SO 字段（设计冻结点）**：

```
displayName, requiredClass(ClassType), behaviorType(Melee/Ranged),
attackData(AttackData 引用，近战形状/范围/三阶段),
projectileData(ProjectileData 引用，远程),
chargeRule(none/fanScale/rectScale/projectileBoost：蓄力曲线与上限),
clipSize(0=无弹夹), reloadTime, autoReloadIdleTime(连弩用，0=不自动),
fireInterval, manaCostPerShot(0，预留),
visualPrefab(模块化多色块武器 prefab), mapIcon(地图掉落固定图标)
```

**六把基础武器**：

| 武器 | 职业 | 类型 | 机制 | 弹夹 |
|------|------|------|------|------|
| 刀 | 战士 | 近战扇形 | 单击挥击；**长按蓄力放大扇形**（角度+半径，上限 ≈ ×1.8） | 无 |
| 枪（矛） | 战士 | 近战矩形 | 单击戳击；**蓄力主要加长长度、宽度略增**（长 ×2、宽 ×1.2 上限） | 无 |
| 弓箭 | 射手 | 远程 | 单击射一箭（间隔较慢、伤害偏高、弹速快）；**长按蓄力提升伤害与弹速**（上限 ≈ ×2） | 1（射后自动装填，装填较快） |
| 连弩 | 射手 | 远程 | **长按连发**（间隔短、伤害低、弹速很快）；弹夹 12，**换弹时间长**（≈2.5s）；弹空自动换弹，闲置 3s 不攻击也自动换弹 | 12 |
| 治疗法杖 | 法师 | 自身 | 平击只回自身 HP（建议 +2），绿色低透明圆环闪一下特效 | 1，换弹较快（≈1s） |
| 能量法杖 | 法师 | 远程 | 向前方射单颗能量弹，伤害/弹速中等 | 1，换弹较快（≈0.8s） |

**实现要点**：
- 近战两把复用现有 WeaponController/Animator/Hitbox 三件套；蓄力缩放通过 AttackData **运行时实例副本**改 range/angle，不污染磁盘资产；
- **蓄力规则**：蓄力期间可移动但移速 ×0.5；**闪避不打断蓄力**（蓄力计时与等级保留，闪避结束继续蓄力/释放）；松手释放；
- 远程三把新建 `Projectile`：直线飞行、Trigger 命中 `IDamageable`、撞 Walls 层销毁、存活时间兜底；对象池本版本不做（记入技术债）；
- 弹夹/换弹状态在 `WeaponInstance`；UI 显示"弹夹 x/y"与换弹进度；
- 治疗法杖走 `Health.Heal(float)`（玩家侧新增，不进 IDamageable 接口）；
- `WeaponHitbox` 判定层零改动。

### 4.7 技能系统（v0.6.4）

F 键释放（可重绑），消耗法力 + 冷却，技能与职业绑定（ClassData 持有引用）：

| 职业 | 技能 | 效果 | 建议消耗/冷却 |
|------|------|------|----------------|
| 战士 | 旋风斩 | 以自身为中心 360° 范围伤害（复用 AttackData Circle 形状走现有判定链），伤害 ≈ 平击 ×1.5 | 20 法力 / 6s |
| 射手 | 后跃射击 | 向移动反方向（无输入则背向瞄准方向）后翻 ≈2.5 格（带短无敌帧），同时向瞄准方向射出强化箭（伤害 ×2、弹速 ×1.5） | 25 法力 / 5s |
| 法师 | 奥术法阵 | 瞄准位置生成半径 ≈2 法阵，持续 4s，每 0.5s 对范围内敌人跳一次伤害并减速 30% | 40 法力 / 8s |

**技能图标与 CD 表现（SkillIconUI，屏幕面板右下角）**：
- 图标 = 矩形方框 + 内部写技能名字（纯文字占位，后续换美术）；
- 技能就绪：正常亮度；CD 中：整体变暗，中心 Text 显示剩余秒数（>1s 整数，最后 1s 一位小数）；
- **秒表扫面**：两张叠加的 uGUI Image——底层全暗图标，顶层正常亮度图标 `type=Filled / fillMethod=Radial360 / 起点顶部 / 顺时针`，CD 期间 `fillAmount = 已转比例`，扫过部分恢复亮度，转满正好全部高亮。**该效果用 uGUI Filled Image 即可实现，无需 DOTween**；DOTween 只用于 CD 转好瞬间的 PunchScale 强调（≈0.2s 小弹跳）；
- 法力不足时按 F：图标红色闪一下 + 数字不变（提示反馈）。

### 4.8 道具系统框架 + 示例（v0.6.5）

```
IItemEffect { void Apply(PlayerContext ctx); void Remove(PlayerContext ctx); }
```
- `PlayerContext`：道具/宠物可触达的一切的聚合引用（Stats、WeaponInstance、Movement、临时队友生成器）——框架核心交付物；
- `StatModifier`：通用效果实现（目标属性 + 加算/乘算 + 值），覆盖"改攻击范围/数值/移速"需求；
- 本版本拾取即生效、死亡清空；道具栏 UI 不做（未来扩展）。

**示例 2 个**：① 力量徽章：攻击力 +20%；② 轻盈之靴：移速 +15%（验证双修饰器共存）。

### 4.9 宠物系统框架 + 示例（v0.6.5）

- `PetBase`：跟随玩家（SmoothDamp 跟随点）+ 行为 tick 虚方法；`PetData` SO 定义行为类型/数值/持续时间（0=永久）；临时队友计时自毁；辅助类挂 StatModifier（复用道具修饰器链）。

**示例 2 个**：① 小精灵（临时队友）：跟随 30 秒，每 2s 向最近敌人射一颗小子弹（复用 Projectile）；② 风之环（辅助）：跟随期间玩家移速 +10%。

### 4.10 掉落物来源（最小闭环）

- 武器：Treasure 房宝箱奖励（启用 `ChestInteractable.ApplyEffect` 挂点）+ 准备房间展台；
- 法力瓶：加入补给/宝箱生成表；小法力球：击杀掉落、walk-over 吸附（见 4.4）；
- 道具/宠物示例：出生房地面测试摆放；随机掉落表不做（未来扩展）。

### 4.11 按键自定义预设（v0.6.0 起）

- 新增四个独立 action：**Dash（默认 Space）、Sprint（默认 Shift）、Interact（默认 E）、Skill（默认 F）**，与 Move（WASD 固定）/Attack（左键固定）并列；
- 技术路线：Input System 官方 Rebind API（`InputActionRebindingExtensions`），绑定覆盖用 `SaveBindingOverridesAsJson` 持久化；本版本交付"独立 action + 覆盖存取接口"，设置界面（改键 UI）后续版本做；
- 代码内**禁止 KeyCode 硬编码**，全部走 action 引用。

---

## 五、子版本规划

### v0.6.0 移动优化 + 闪避/奔跑/体力条（含敌人攻击范围 bug 修复）
- 内容：Player/Enemy 刚体插值；EnemyAI 移动挪 FixedUpdate；**EnemyAI 攻击触发距离改读 AttackData 范围（bug 修复 4.1.1）**；PlayerMovement；体力属性 + UI；Dash/Sprint 输入与实现（无敌帧 + 内置短 CD）；清理调试日志；复制 v0_6 场景。
- 验收：移动无闪烁（60/144fps 复验）；闪避无敌帧、体力扣回、**动作结束后进入短 CD** 均正确；奔跑耗体力；**玩家站在精英/Boss 打击范围边缘时敌人站定即出手**；v0_4 场景回归无损。

### v0.6.1 E 键交互统一 + 两段式拾取
- 内容：Interactable 基类改造；PlayerInteractor（候选高亮 + "按 E"提示）；可拾取列表 UI（纯文字、滚轮/数字键切换；单物品直拾）；四个既有交互物回归；Interact action（可重绑）。
- 验收：宝箱/祭坛/补给/传送门走近按 E 触发，功能与 v0.5.4 一致；列表正确枚举周围可拾取物并可选中拾取；单物品不弹列表直拾；无候选时按 E 无副作用。

### v0.6.2 职业系统 + 武器框架 + 初始准备房间
- 内容：ClassType/ClassData 三份资产；法力属性 + UI（不可自动回复）；WeaponData/WeaponInstance/WeaponBehavior 框架；**准备房间三展台**（职业选择台 + 两武器展台）+ ClassSelectUI（选择→高亮→确认→刷新展台）；RunManager 一局一次选择/死亡默认上次职业；Skill action 占位；**TMP 迁移**（已确认并入本版：生成中文 TMP 字体资产，职业选择 UI 直接用 TMP 编写，并迁移现有 4 处 Legacy Text——"按 E"标签、拾取列表行、列表底注等）。
- 验收：展台流程全程可用（含死亡重开默认职业、武器重选）；三职业属性差异正确；职业/武器选择结果换层保留、死亡按 4.5 规则处理；迁移后文字任意缩放清晰。

### v0.6.3 六把基础武器
- 内容：Projectile/ProjectileData；蓄力系统（移动 ×0.5、**闪避保留蓄力**）；弹夹/换弹/自动换弹；Health.Heal；六份 WeaponData 资产 + 模块化多色块美术；WeaponPickup（职业校验、地图缩小图标、旧武器原地掉落）；宝箱奖励接武器掉落；法力瓶/法力球（吸附）；弹药 UI。
- 验收：六武器机制全部符合 4.6 表；跨职业拾取被拒；换武器旧武器原地掉落可捡回；六武器打木桩 DPS 配平 ±20%；既有近战（敌人侧）回归无损。

### v0.6.4 技能系统
- 内容：SkillData/SkillExecutor；旋风斩/后跃射击/奥术法阵；**SkillIconUI（Radial360 CD 扫面 + 剩余秒数 + 转好 DOTween 强调）**；法力消耗校验与不足提示；ArcaneZone tick 组件。
- 验收：F 键正确扣法力、走 CD；CD 图标扫面与数字正确、转好正好全亮；法力不足/CD 中按 F 有反馈无副作用；三技能效果符合 4.7 表。

### v0.6.5 道具/宠物框架 + 示例
- 内容：IItemEffect/StatModifier/PlayerContext；ItemData/ItemPickup；PetBase/PetData/PetPickup；四示例（力量徽章/轻盈之靴/小精灵/风之环）；出生房测试摆放。
- 验收：四示例 E 拾取后效果正确；临时宠物按时销毁；双修饰器共存数值正确；死亡重开后道具/宠物清空。

---

## 六、强度配平原则

1. **DPS 中轴**：以"现有默认玩家平击 DPS"为 1.0 基准，六武器理论 DPS 落在 0.8 ~ 1.2；理论 DPS = 单发伤害 × 射速 ÷（1 + 换弹时间占比），写进每份 WeaponData 资产注释；
2. **风险补偿**：近战手短允许 DPS 靠上限 + 高生存；弓箭蓄力有站定成本，蓄满才到上限；连弩 DPS 靠下限但有换弹真空期；
3. **蓄力收益 ≠ 线性**：配平按"50% 时间蓄力"的实战模型估算；
4. **技能不计入武器配平**：技能是职业差异化，数值按 4.7 表先落地后调；法力不可自动回复意味着技能次数 = 资源管理，战士 30 法力 + 旋风斩 20 消耗 ≈ 无补给一局 1 次，靠击杀回蓝补充（设计意图：战士技能是应急技）。

---

## 七、技术债与风险

| 项 | 说明 | 处置 |
|----|------|------|
| 子弹无对象池 | 连弩高频生成，理论有 GC 压力 | 本版本不做；触发条件：实测掉帧或连弩连发卡顿 |
| 武器攻击穿墙 | v0.5.x 遗留；子弹必须撞墙销毁（否则远程穿墙过强） | 子弹撞墙 = 本版本做；近战穿墙维持现状 |
| AttackData 运行时副本 | 蓄力缩放需 Instantiate 副本，需确保 WeaponController/Indicator 读的都是副本 | v0.6.3 实现时显式处理 |
| 四属性 UI 信息密度 | 屏幕面板四条 + 弹药 + 技能图标 + 拾取列表 | 体力/法力窄条贴 HP 条下；世界空间条维持双条不加 |
| 准备房间与楼层循环 | 展台只在第 1 层出生房；NextFloor 的后续楼层出生房不生成展台；死亡重开回第 1 层重新布置 | RunManager 按 floorNumber==1 判定注入展台 |
| 中文动态字体 | 微软雅黑动态字体跨平台打包需换 TMP 中文资产 | 本版本接受（已确认），记入未来扩展 |

---

## 八、决策记录与待确认项

**已决（2026-07-27 全部确认）**：
- A 法力不可自动回复（瓶/击杀法力球/技能宠物三来源）；
- B 蓄力移动 ×0.5 + 闪避不打断蓄力 + 闪避内置短 CD（动作结束后进入）；
- C 旧武器原地掉落 + 两段式拾取 + 地图固定图标；
- D 死亡重开武器不保留 + 默认上次职业 + 道具宠物清空 + 准备房间展台流程；
- E 小法力球 walk-over 自动吸附，不占 E 键；
- F 单物品按 E 直接拾取，≥2 个才弹列表；
- G 字体方案（微软雅黑动态字体，28/22/20/16pt）。

**待确认**：无。后续新设计点随开发提出随记录。

---

## 九、未来扩展（本版本只做预留）

- 改键设置 UI（rebind 接口已留）、道具栏/背包、拾取列表加图标与属性显示；
- 更多技能/技能树；更多武器（WeaponData + 行为枚举扩展）、武器品质/词条、模块化武器随机组合（WeaponRoot 部件化已铺路）；
- 宠物图鉴/养成；子弹对象池；手柄瞄准；TMP 中文字体资产；
- 存档（RunManager + ClassData/WeaponData 引用即可还原）。

---

## 十、维护与更新记录

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| v0.6 计划 | 2026-07-27 | 计划书初版 |
| v0.6 计划 R2 | 2026-07-27 | 修订：法力不可自动回复；技能 CD 图标（Radial360 秒表扫面）；闪避不打断蓄力；两段式拾取 + 可拾取列表 + 武器地图固定图标 + 旧武器原地掉落；开局改为初始准备房间三展台流程；按键可自定义预设；敌人攻击触发范围 bug 修复并入 v0.6.0；美术策略升级（另立《v0.6_美术清单.md》） |
| v0.6 计划 R3 | 2026-07-27 | 确认 E/F/G 三项建议（法力球吸附、单物品直拾、字体方案）；闪避新增内置短 CD（动作完整结束后进入，建议 0.5s）；第八章全部决策闭环 |
