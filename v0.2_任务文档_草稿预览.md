# v0.2 任务文档：战士角色系统（草稿预览版）

## 版本信息
- **版本号**：v0.2
- **版本名称**：战士角色
- **美术策略**：程序员美术（纯色几何图形）
- **目标**：将v0.1的TestPlayer替换为可操控战士，实现移动、瞄准、攻击、状态表现
- **前置版本**：v0.1（小屋场景 + Tilemap碰撞已就绪）

---

## 总体目标
在v0.1小屋基础上，实现战士职业的核心游玩体验：
1. **WASD移动**：用键盘控制绿色圆球在房间内行走，碰到墙壁被阻挡
2. **鼠标瞄准**：角色始终面向鼠标方向
3. **点击攻击**：鼠标左键发起近战攻击，命中红色方块敌人造成伤害
4. **四状态表现**：Idle/Walk/Attack/Dead 用代码驱动视觉变化（缩放、颜色、闪烁）
5. **战士属性**：HP、护甲、移动速度、攻击力、攻击冷却
6. **测试敌人**：1个红色方块敌人，被攻击会受伤/死亡

> 💡 本版本不引入Animator、动画剪辑、Sprite图集。所有"动画"均为代码实时修改Transform和SpriteRenderer属性。

---

## 技术选型（供审阅确认）

### 选型A：移动方式

| 方案 | 原理 | 优点 | 缺点 | 推荐度 |
|------|------|------|------|--------|
| **Rigidbody2D.velocity** | 每帧设置rb.velocity = direction * speed | 有物理碰撞、被墙阻挡自然、支持后续击退效果 | 需要理解物理系统 | ⭐⭐⭐ |
| Transform.Translate | 直接修改position | 简单直观 | 无物理碰撞，会穿墙 | ⭐ |
| CharacterController | 3D组件移植 | 不适合2D | 不适用 | - |

**建议**：采用 **Rigidbody2D.velocity**。v0.1已为墙壁配置Tilemap Collider + Rigidbody Static，velocity移动会自动被阻挡。

---

### 选型B：角色朝向控制

| 方案 | 原理 | 优点 | 缺点 | 推荐度 |
|------|------|------|------|--------|
| **Transform.rotation（Z轴旋转）** | 计算鼠标角度，设置rotation.z | 直观，圆球不需要左右翻转 | 后续换Sprite时可能需要额外处理四向/八向朝向 | ⭐⭐⭐ |
| Scale.x 翻转 | 鼠标在左则scale.x=-1，在右则scale.x=1 | 2D横版游戏标准做法 | 不适合俯视角360度朝向 | ⭐ |
| 四向离散朝向 | 按角度分4/8个方向，每个方向切不同Sprite | 正式版需要 | 程序员美术版用不上 | - |

**建议**：采用 **Transform.rotation（Z轴旋转）**。俯视角游戏角色需要360度面向鼠标。

---

### 选型C：攻击伤害判定

| 方案 | 原理 | 优点 | 缺点 | 推荐度 |
|------|------|------|------|--------|
| **Physics2D.OverlapCircle/Box** | 在角色前方创建检测区域，获取所有Collider | 简单、精确、可配置攻击范围 | 需要每帧检测（仅在攻击瞬间检测即可） | ⭐⭐⭐ |
| 发射Projectile（子弹） | 创建飞行物，用OnTriggerEnter2D检测 | 适合远程攻击（法师/弓箭手） | 战士是近战，不需要飞行物 | ⭐（后续v0.3用） |
| 射线检测Raycast | 发射射线检测命中 | 精确到点 | 不适合范围攻击 | ⭐ |

**建议**：采用 **OverlapCircle**。战士近战攻击是在角色周围一个圆形/扇形范围内造成伤害。

---

### 选型D：状态机模式

| 方案 | 原理 | 优点 | 缺点 | 推荐度 |
|------|------|------|------|--------|
| **简单枚举 + switch** | enum PlayerState { Idle, Walk, Attack, Dead }，每帧switch处理 | 代码量最少，足够v0.x使用 | 状态多了以后难以维护 | ⭐⭐⭐ |
| 状态模式（State Pattern） | 每个状态一个类，实现IState接口 | 扩展性好，适合复杂AI | 当前过度设计 | ⭐ |
| 分层状态机 | 基础层（移动）+ 行为层（攻击/受击） | 后续敌人AI需要 | 当前过度设计 | ⭐（v0.4敌人用） |

**建议**：采用 **简单枚举 + switch**。玩家角色的状态逻辑相对简单，枚举完全够用。此模式在v0.5之前不需要升级。

---

### 选型E：职业架构（为v0.3法师/弓箭手铺垫）

| 方案 | 原理 | 优点 | 缺点 | 推荐度 |
|------|------|------|------|--------|
| **继承：PlayerController基类 + 子类** | PlayerController处理移动/HP/状态，WarriorController覆盖Attack() | 代码复用高，结构清晰 | 需要规划好虚方法 | ⭐⭐⭐ |
| ScriptableObject配置 | PlayerController统一，用SO配置不同职业的属性和攻击方式 | 配置灵活，无需改代码 | 攻击逻辑差异大时SO难以表达 | ⭐⭐ |
| 完全独立三个脚本 | Warrior.cs / Mage.cs / Archer.cs，各写各的 | 最简单 | 大量重复代码 | ⭐ |

**建议**：v0.2先写一个完整的 **WarriorController.cs**（不拆基类），v0.3添加法师时再抽象出 **PlayerController基类**。避免过早抽象。

---

## 任务框架预览（共6个任务）

### 任务1：整理v0.2文件夹与场景准备
- 复制v0.1场景为v0.2版本
- 创建 `Scripts/v0_2/` 文件夹
- 删除/重命名v0.1的TestPlayer，保留小屋场景

### 任务2：编写战士核心脚本 WarriorController.cs
- 属性：HP、MaxHP、Armor、MoveSpeed、AttackDamage、AttackCooldown
- 输入：WASD读取、鼠标位置读取、鼠标左键检测
- 移动：Rigidbody2D.velocity驱动
- 朝向：Z轴旋转面向鼠标
- 状态机：Idle/Walk/Attack/Dead 枚举 + 状态切换逻辑

### 任务3：编写战士视觉表现脚本 WarriorVisuals.cs
- 挂载在战士的SpriteRenderer所在GameObject上
- 监听状态变化，执行对应视觉表现：
  - **Idle**：Scale = (0.6, 0.6)，Color = 绿色
  - **Walk**：Scale在(0.55, 0.6)和(0.6, 0.55)之间波动（模拟"迈步"）
  - **Attack**：瞬间Scale放大到(0.8, 0.8)再缩回，Color闪烁白色0.1秒
  - **Dead**：Color渐变到灰色，Scale缩小到0，然后销毁对象

### 任务4：编写攻击与伤害系统
- WarriorController.Attack()方法：触发攻击状态，检测前方OverlapCircle
- IDamageable接口：TakeDamage(float damage)方法
- Enemy脚本实现IDamageable
- 攻击冷却计时器防止连点

### 任务5：创建测试敌人（红色方块）
- 场景中放置红色方块（Square Sprite + Color=红色）
- 挂载Enemy.cs：实现IDamageable，有HP，受击时闪烁白色
- 敌人死亡：变灰 + 缩小 + 销毁

### 任务6：添加血条UI
- Canvas + Slider制作头顶血条（跟随战士移动）
- 或屏幕角落的固定血条
- HP变化时Slider.value同步更新

---

## 核心脚本设计草稿

> 以下脚本结构供Kimi Code实现时参考。字段、方法签名、关键逻辑均已定义，具体实现由Kimi Code完成。

### WarriorController.cs
```csharp
using UnityEngine;

[RequireComponent(typeof(Rigidbody2D))]
public class WarriorController : MonoBehaviour, IDamageable
{
    // ========== 属性配置（Inspector中可调整）==========
    [Header("基础属性")]
    [SerializeField] private float maxHP = 100f;
    [SerializeField] private float armor = 0f;
    [SerializeField] private float moveSpeed = 5f;

    [Header("攻击配置")]
    [SerializeField] private float attackDamage = 20f;
    [SerializeField] private float attackRange = 1.5f;
    [SerializeField] private float attackCooldown = 0.5f;
    [SerializeField] private float attackDuration = 0.2f; // Attack状态持续时间

    // ========== 运行时状态 ==========
    private float currentHP;
    private float currentArmor;
    private Vector2 moveInput;
    private Vector2 mouseWorldPos;
    private float attackTimer = 0f;
    private float attackCooldownTimer = 0f;
    private bool isDead = false;

    // ========== 组件引用 ==========
    private Rigidbody2D rb;
    private WarriorVisuals visuals;

    // ========== 状态机 ==========
    public enum PlayerState { Idle, Walk, Attack, Dead }
    public PlayerState CurrentState { get; private set; } = PlayerState.Idle;

    // ========== 事件 ==========
    public System.Action<PlayerState> OnStateChanged;
    public System.Action<float, float> OnHPChanged; // current, max
    public System.Action OnDeath;

    void Awake()
    {
        rb = GetComponent<Rigidbody2D>();
        visuals = GetComponent<WarriorVisuals>();
        currentHP = maxHP;
        currentArmor = armor;
    }

    void Update()
    {
        if (isDead) return;

        ReadInput();
        HandleAttackInput();
        UpdateState();
        UpdateTimers();
    }

    void FixedUpdate()
    {
        if (isDead) return;
        HandleMovement();
        HandleRotation();
    }

    // --- 输入读取 ---
    private void ReadInput()
    {
        // 旧版Input Manager
        moveInput = new Vector2(Input.GetAxisRaw("Horizontal"), Input.GetAxisRaw("Vertical"));
        mouseWorldPos = Camera.main.ScreenToWorldPoint(Input.mousePosition);
    }

    // --- 移动（物理帧执行） ---
    private void HandleMovement()
    {
        rb.velocity = moveInput.normalized * moveSpeed;
    }

    // --- 朝向（物理帧执行） ---
    private void HandleRotation()
    {
        Vector2 direction = mouseWorldPos - (Vector2)transform.position;
        float angle = Mathf.Atan2(direction.y, direction.x) * Mathf.Rad2Deg;
        transform.rotation = Quaternion.Euler(0, 0, angle);
    }

    // --- 攻击输入检测 ---
    private void HandleAttackInput()
    {
        if (Input.GetMouseButtonDown(0) && attackCooldownTimer <= 0 && CurrentState != PlayerState.Attack)
        {
            TriggerAttack();
        }
    }

    // --- 状态更新 ---
    private void UpdateState()
    {
        if (CurrentState == PlayerState.Attack)
        {
            if (attackTimer <= 0)
                TransitionToState(moveInput.magnitude > 0.1f ? PlayerState.Walk : PlayerState.Idle);
            return;
        }

        PlayerState newState = moveInput.magnitude > 0.1f ? PlayerState.Walk : PlayerState.Idle;
        if (newState != CurrentState)
            TransitionToState(newState);
    }

    private void TransitionToState(PlayerState newState)
    {
        PlayerState oldState = CurrentState;
        CurrentState = newState;
        OnStateChanged?.Invoke(newState);

        // 状态进入时的逻辑
        switch (newState)
        {
            case PlayerState.Attack:
                ExecuteAttack();
                break;
        }
    }

    // --- 攻击执行 ---
    private void TriggerAttack()
    {
        attackTimer = attackDuration;
        attackCooldownTimer = attackCooldown;
        TransitionToState(PlayerState.Attack);
    }

    private void ExecuteAttack()
    {
        // 在角色位置周围检测敌人
        Collider2D[] hits = Physics2D.OverlapCircleAll(transform.position, attackRange, LayerMask.GetMask("Enemy"));
        foreach (var hit in hits)
        {
            if (hit.TryGetComponent<IDamageable>(out var damageable))
            {
                damageable.TakeDamage(attackDamage);
            }
        }
    }

    // --- 计时器更新 ---
    private void UpdateTimers()
    {
        if (attackTimer > 0) attackTimer -= Time.deltaTime;
        if (attackCooldownTimer > 0) attackCooldownTimer -= Time.deltaTime;
    }

    // --- 受伤接口实现 ---
    public void TakeDamage(float damage)
    {
        if (isDead) return;

        // 护甲减伤逻辑（简单版：护甲直接减去伤害）
        float actualDamage = Mathf.Max(0, damage - currentArmor);
        currentHP -= actualDamage;
        OnHPChanged?.Invoke(currentHP, maxHP);

        if (currentHP <= 0)
        {
            Die();
        }
    }

    private void Die()
    {
        isDead = true;
        rb.velocity = Vector2.zero;
        TransitionToState(PlayerState.Dead);
        OnDeath?.Invoke();
    }

    // 可视化攻击范围（Scene视图调试）
    void OnDrawGizmosSelected()
    {
        Gizmos.color = Color.red;
        Gizmos.DrawWireSphere(transform.position, attackRange);
    }
}
```

### WarriorVisuals.cs
```csharp
using UnityEngine;

[RequireComponent(typeof(SpriteRenderer))]
public class WarriorVisuals : MonoBehaviour
{
    [Header("颜色配置")]
    [SerializeField] private Color idleColor = new Color(0.2f, 0.8f, 0.3f);    // 绿色
    [SerializeField] private Color attackFlashColor = Color.white;
    [SerializeField] private Color deadColor = new Color(0.3f, 0.3f, 0.3f);     // 灰色

    [Header("缩放配置")]
    [SerializeField] private Vector3 idleScale = new Vector3(0.6f, 0.6f, 1f);
    [SerializeField] private float walkPulseAmount = 0.05f;
    [SerializeField] private float walkPulseSpeed = 10f;
    [SerializeField] private Vector3 attackScale = new Vector3(0.8f, 0.8f, 1f);
    [SerializeField] private float attackFlashDuration = 0.1f;

    private SpriteRenderer sr;
    private WarriorController controller;
    private Vector3 baseScale;

    void Awake()
    {
        sr = GetComponent<SpriteRenderer>();
        controller = GetComponent<WarriorController>();
        baseScale = idleScale;
    }

    void OnEnable()
    {
        if (controller != null)
            controller.OnStateChanged += HandleStateChanged;
    }

    void OnDisable()
    {
        if (controller != null)
            controller.OnStateChanged -= HandleStateChanged;
    }

    void Update()
    {
        if (controller == null) return;

        switch (controller.CurrentState)
        {
            case WarriorController.PlayerState.Idle:
                UpdateIdleVisuals();
                break;
            case WarriorController.PlayerState.Walk:
                UpdateWalkVisuals();
                break;
            case WarriorController.PlayerState.Dead:
                UpdateDeadVisuals();
                break;
        }
    }

    private void HandleStateChanged(WarriorController.PlayerState newState)
    {
        switch (newState)
        {
            case WarriorController.PlayerState.Attack:
                StartCoroutine(AttackFlashCoroutine());
                break;
            case WarriorController.PlayerState.Dead:
                sr.color = deadColor;
                break;
        }
    }

    private void UpdateIdleVisuals()
    {
        sr.color = idleColor;
        transform.localScale = idleScale;
    }

    private void UpdateWalkVisuals()
    {
        sr.color = idleColor;
        // 走路时缩放轻微波动，模拟"迈步"
        float pulse = Mathf.Sin(Time.time * walkPulseSpeed) * walkPulseAmount;
        transform.localScale = idleScale + new Vector3(pulse, -pulse, 0);
    }

    private System.Collections.IEnumerator AttackFlashCoroutine()
    {
        sr.color = attackFlashColor;
        transform.localScale = attackScale;
        yield return new WaitForSeconds(attackFlashDuration);
        
        if (controller.CurrentState != WarriorController.PlayerState.Dead)
        {
            sr.color = idleColor;
        }
    }

    private void UpdateDeadVisuals()
    {
        // 逐渐缩小
        transform.localScale = Vector3.Lerp(transform.localScale, Vector3.zero, Time.deltaTime * 3f);
    }
}
```

### IDamageable.cs（接口）
```csharp
public interface IDamageable
{
    void TakeDamage(float damage);
}
```

### Enemy.cs（测试敌人）
```csharp
using UnityEngine;

[RequireComponent(typeof(SpriteRenderer))]
public class Enemy : MonoBehaviour, IDamageable
{
    [SerializeField] private float maxHP = 50f;
    [SerializeField] private float hitFlashDuration = 0.15f;

    private float currentHP;
    private SpriteRenderer sr;
    private Color originalColor;

    void Awake()
    {
        currentHP = maxHP;
        sr = GetComponent<SpriteRenderer>();
        originalColor = sr.color;
    }

    public void TakeDamage(float damage)
    {
        currentHP -= damage;
        StartCoroutine(HitFlashCoroutine());

        if (currentHP <= 0)
        {
            Die();
        }
    }

    private System.Collections.IEnumerator HitFlashCoroutine()
    {
        sr.color = Color.white;
        yield return new WaitForSeconds(hitFlashDuration);
        sr.color = originalColor;
    }

    private void Die()
    {
        // 简单死亡表现：变灰、缩小、销毁
        sr.color = Color.gray;
        // 可用协程做缩小动画，或直接销毁
        Destroy(gameObject, 0.3f);
    }
}
```

---

## 场景对象结构（预期）

```
Hierarchy (v0.2场景)
├── Main Camera
├── Grid
│   ├── Floor              (Tilemap)
│   └── Walls              (Tilemap + Colliders)
├── Warrior                (绿色圆球，玩家角色)
│   ├── SpriteRenderer     (Circle, Color=Green)
│   ├── Rigidbody2D        (Dynamic, Gravity=0, Freeze Rotation)
│   ├── CircleCollider2D
│   ├── WarriorController  (脚本)
│   └── WarriorVisuals     (脚本)
├── Enemy_Test             (红色方块，测试敌人)
│   ├── SpriteRenderer     (Square, Color=Red)
│   ├── BoxCollider2D
│   └── Enemy              (脚本)
└── Canvas                 (血条UI)
    └── HPBar_Slider
```

---

## 需要审阅确认的问题

1. **输入系统**：采用旧版Input Manager（`Input.GetAxis/GetMouseButton`）是否OK？后续升级到Input System的成本是后期改几行代码。

2. **攻击范围形状**：当前设计为圆形Overlap（角色周围一圈），是否需要改为**扇形**（只检测角色前方一定角度内）？扇形更真实但需要更多代码。

3. **血条位置**：
   - A. 角色头顶跟随血条（World Space Canvas）
   - B. 屏幕角落固定血条（Screen Space Canvas）
   - C. 两者都要

4. **护甲恢复机制**：你提到"护甲脱战后可自然恢复"。v0.2是否要实现这个？还是仅显示护甲值，恢复机制放到后续版本？

5. **死亡后行为**：当前设计为角色变灰缩小后销毁。是否需要：
   - 按R键重生？
   - 还是游戏直接结束（之后做Game Over UI）？

6. **攻击指示器**：是否需要给玩家一个视觉反馈，显示攻击范围（比如攻击时短暂显示红色圆圈）？

---

## 与后续版本的衔接

| v0.2 产出 | v0.3 法师/弓箭手复用 | v0.4 敌人系统复用 | v0.5+ 道具系统复用 |
|-----------|---------------------|-------------------|-------------------|
| WarriorController.cs | 抽象为PlayerController基类，子类覆盖Attack() | — | — |
| IDamageable接口 | 所有职业实现 | 所有敌人实现 | 可破坏物体实现 |
| 状态机枚举 | 基类状态通用 | 敌人用更复杂的状态机 | — |
| OverlapCircle攻击判定 | 法师改为Projectile | 敌人攻击复用 | 道具效果触发 |
| 血条UI系统 | 三个职业共用 | 敌人也加上 | — |

---

## 文档变更记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2025-07-09 | v0.2-draft | 草稿预览版，待审阅确认 |

