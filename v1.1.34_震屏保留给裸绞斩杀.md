# v1.1.34 震屏保留给裸绞斩杀效果

## 需求

击杀后的屏幕抖动效果**保留**，但不随每次击杀触发——作为技能组**裸绞**的斩杀效果专属。

## 改动

- **WeaponHitbox 常规命中反馈**：移除 `CameraFollow.ShakeMain`（当前 0.00225/0.026），
  保留音效 "hit" + 停帧 0.03（手感三件套变两件，日常打击不再震屏）；
- **新增 `Common/ExecuteFeedback.cs`**：斩杀反馈统一入口 `Play(Vector2 position)` =
  强震屏 **0.05/0.08**（击杀震原版参数，即用户要求保留的数值）+ 停帧 0.08 + 命中音；
  参数集中常量便于技能联调；position 为击杀点（预留特效挂点）。
- **裸绞技能结算**：阈值处决或本次真实伤害致死后调用 `ExecuteFeedback.Play(hitPoint)`，
  常规攻击链路零震屏、裸绞实际击杀时才有重震。

## 验收

日常攻击/击杀无震屏；裸绞实际击杀目标时触发 0.05/0.08 强震+加长停帧。

## 关联

v1.1.17（震屏调参史）/ 反馈挂点速查已同步。
