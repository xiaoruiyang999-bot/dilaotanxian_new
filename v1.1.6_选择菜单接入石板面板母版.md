# v1.1.6 选择菜单接入石板面板母版

## 1. 改动范围（选角色 + 选职业两菜单，同款母版）

| 文件 | 改动 |
|---|---|
| `Class/ClassSelectUI.cs`（职业选择） | 主面板纯色块 → `PanelSprite.ApplyStonePanel`（石板 9-Slice）；标题 -40→-68 让出顶框；按钮/确认底色 0.95 不透明 → 0.5 半透明（透出石板纹理） |
| `Class/CharacterSelectUI.cs`（角色外形选择） | 同上（面板→母版；标题 -42→-68、副标题 -74→-100；按钮底 0.5 半透明；确认键绿 0.95→0.6） |

选择高亮（职业色/角色色边框）、确认闪烁、Esc 关闭、IsOpen 输入屏蔽、接续弹职业页等交互逻辑**零改动**——只换了皮肤与让框间距。

## 2. 布局核算（面板 600×500，砖框 T49/B34/L34/R37）

- 内容安全区：x ∈ [34, 563]、y ∈ [34, 466]（面板局部系）。
- 标题字面让出顶部 49px 砖框（-68 起）；职业按钮列 156..-96、确认 -137..-183，均高于底框线 -216；按钮宽 420 < 两侧框内宽 529 ✓。
- 后续加行：直接向 panel 的 RectTransform 追加按钮（两 UI 头注释已写明），样式自动继承母版。

## 3. 断链兜底

`PanelSprite.ApplyStonePanel(img, fallback)`：素材缺失时 Stone 为 null → 面板落回原纯色观感（黑 0.82/0.85），不抛错不布局崩。

## 4. 验收

1. Unity 编译无错 → Play v0_7_PrepRoom：首进自动弹**角色选择**（石板面板）→ 确认接续弹**职业选择**（同款石板面板）。
2. 面板四角砖块完整不拉伸；600×500 下边框只沿单轴拉伸；标题/按钮不压砖框。
3. 选中高亮、确认闪烁、Esc 关闭、死亡重开默认上次选择等行为与改前一致。

## 5. 关联

母版资产与 Border 实测见 [v1.1.5_石板面板9Slice母版](v1.1.5_石板面板9Slice母版.md)。暂停/设置/背包/结算面板的接入属布局重构（内容从全屏锚点改面板局部锚点），未在本轮范围。
