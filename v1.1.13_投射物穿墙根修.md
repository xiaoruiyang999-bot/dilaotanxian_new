# v1.1.13 新手弩间歇性穿墙根修（投射物位移段扫射）

## 现象

弓箭手新手弩（Bolt，速度 18 u/s，蓄力后更高）子弹**偶尔**穿过墙体，无规律。

## 根因：快速运动体隧穿（tunneling），三因素叠加

1. 子弹在 `Update` 用 `transform.position +=` **传送式**移动，而 Trigger 重叠检测只在物理步（FixedUpdate 50Hz）采样**瞬时位置**；
2. Kinematic 刚体默认 Discrete 检测，且 **Box2D 的连续检测（Continuous）不覆盖 Trigger 形状**——引擎层对触发器子弹无扫掠保护；
3. 正常帧每物理步位移 ≈0.36 格 < 墙厚 1 格（所以大多数时候正常）；**帧尖峰**（加载/特效瞬间单帧 0.1s+）时单帧位移可达 1.8~2.7 格，物理采样点直接跨过整面墙 → 间歇性穿墙。

## 修复（Projectile.cs，v1.1.13）

- **Update 先测后动**：每帧对整段位移做 `Physics2D.CircleCast(oldPos, Radius, dir, stepDist)`——
  单结果结构体返回**零 GC**（R10）；过滤器 `useTriggers=false`（跳过探测圈/其他子弹，符合
  "投射物物理查询跳过 trigger"红线）。命中即在命中点结算销毁，任何帧长下都零隧穿。
- **结算入口统一**：原 `OnTriggerEnter2D` 主体抽为 `ResolveHit(collider, hitPoint)`；
  Trigger 通道保留（静止贴墙/目标主动撞入等非位移接触），两通道共用，`resolved` 标志防同帧双结算。
- 结算后当帧不再前进（Destroy 帧末执行），防箭矢视觉穿出墙面一帧。
- 行为兼容：伤害管线（玩家 DamageResolver/敌人原路径）、TargetLayer 根级兼容、防自伤、
  "非 Trigger 实体一律挡弹"全部原样保留；`Launch` 公开签名未动（测试无断言交集）。

## 验收

1. 编译无错 → 弓箭手进地牢贴墙射击：近距/远距/斜角均应在墙面出命中特效并销毁，不穿墙。
2. 蓄力满档（速度上限）连射斜角打墙角，反复尝试无穿透。
3. 对敌人伤害数字、命中特效、蓄力倍率不变；敌人子弹（EnemyBolt）同样受益。

## 关联

R6（Trigger 语义）/ R10（物理查询零 GC）复核通过。EditMode 测试无 Projectile 断言，不受影响。

## v1.1.13.2 二次编译修正（最终方案）

数组版 CircleCast 同样报 CS1503——本 Unity 版本 ContactFilter2D 系 **Cast 重载整体不存在**。
最终改用项目已验证签名的同族 API：**`Physics2D.OverlapBox(center, size, angle, filter, buffer)`
（WeaponHitbox.Tick 在用）做"扫掠盒"**——旋转矩形从起点圆心覆盖到终点圆心（长=步长+2r，宽=2r，
角=飞行方向），防隧穿语义与扫射等价且顺带覆盖起点内嵌场景；结果无排序，跳过 owner 后取
bounds 最近者（敌人挡墙前优先结算敌人），命中点用 ClosestPoint 近似（Trigger 路径同款）。
**教训（已入必读）：ContactFilter2D 只在 Overlap 系有数组重载，Cast 系本版本全无——跨 API
选型先抄项目内已编译通过的调用形状。**
