# v1.0.3 Demo 整合验收与打磨计划

> 制定日期：2026-08-31  
> 当前基线：main 已合并战斗与 MCP 工作，正在处理合并兼容问题  
> 阶段目标：停止以“模块数量”为完成标准，把现有系统收束成一条可交付、可复测、可持续迭代的 Demo 主流程。

---

## 一、阶段判断

项目目前不是“缺少代码”，而是处在**系统很多、完成口径不统一、整体验收不足**的阶段。

现有文档中同时存在以下情况：

- 路线图将 M2～M4 标记为完成，但部分原始验收框仍未勾选。
- `v1.0.1_前后端系统配对修复.md` 记录了存档、Meta、升级、结算等配对成果，但随后为兼容 main v0.7.5 清理了若干旧实现，必须重新确认当前版本仍有等价链路。
- 部分功能有脚本、数据类或接口，但未必具有场景挂载、Prefab 组件、配置资产、UI 入口和实际反馈。
- 合并可以通过 Git，却仍可能产生重复方法、旧 API 调用和自动合并区域的语义不兼容。

因此，本阶段不再用“文件存在”“代码写完”“合并成功”作为完成依据，而以玩家实际可完成一局为最终标准。

### 本阶段一句话目标

> 让一个不了解工程的人，从启动游戏开始，不借助 Inspector 和 Console，能够完成选择、战斗、成长、Boss、结算、重开，并在重新启动游戏后看到应该保留的进度。

---

## 二、统一完成状态

所有系统以后使用以下六级状态，避免笼统写“已完成”。

| 状态 | 含义 | 可否计入完成 |
|---|---|---:|
| 0. 规划 | 只有文档或想法 | 否 |
| 1. 后端存在 | 有数据结构或业务逻辑 | 否 |
| 2. 资产接线 | 场景、Prefab、SO、Input Action 已配置 | 否 |
| 3. 前端可用 | 玩家有入口、显示和反馈 | 否 |
| 4. 实机通过 | 在 Unity Play 中按用例验证通过 | 基本完成 |
| 5. 回归稳定 | 重载场景、死亡重开、重启游戏后仍通过 | 正式完成 |


文档中的“✅ 已完成”原则上必须达到状态 5；只完成代码的项目统一写“代码完成，待实机验收”。

---

## 三、范围与边界

### 本阶段必须做

1. 建立可编译、可运行、可提交的稳定基线。
2. 对玩家主流程涉及的后端、前端、资产和场景进行逐项配对。
3. 完成一条从启动到结算、再到第二局的闭环。
4. 修复阻断流程、造成状态错误或明显误导玩家的问题。
5. 完成基础 UI、教程、反馈、数值节奏和临时美术一致性打磨。
6. 形成可重复执行的冒烟验收清单与发布前清单。

### 本阶段暂不扩张

- 不为了数量新增大量敌人、武器、技能和房型。
- 不在主流程稳定前全面替换正式美术。
- 不恢复 v0_1～v0_4 或 Framework 历史代码。
- 不为已经取消的旧架构补“空兼容接口”。
- 不把“未来可能需要”当成当前必须开发。

---

## 四、总体执行顺序

```text
稳定基线
  ↓
系统配对审计
  ↓
单局主流程贯通
  ↓
跨局与存档验证
  ↓
体验与内容打磨
  ↓
构建、回归与交付
```

任何阶段出现编译阻断、主流程卡死或数据丢失，应先回退处理阻断项，不带病扩展下一阶段。

---

## 五、阶段 A：稳定基线

### 目标

得到一个职责明确、Unity 零编译错误、可以安全继续开发的 main 基线。

### 工作项

- [ ] 清除战斗/MCP 合并产生的所有编译错误。
- [ ] 搜索并清理重复成员、旧 API、丢失类型和冲突标记。
- [ ] 区分用户已有改动、合并改动和本轮修复，禁止误覆盖用户资产。
- [ ] Unity 重新加载场景后再次编译，不能只依赖文本检查。
- [ ] 主场景进入 Play 30 秒，无启动异常、空引用刷屏和关键组件缺失。
- [ ] 整理当前未提交文件：代码修复、用户素材、文档、场景改动分别归类。
- [ ] 创建一个可回溯的稳定提交；推送由用户决定。

### 完成定义

- Unity Console：0 个编译错误。
- 主场景可进入 Play。
- 停止 Play、重新加载场景、再次 Play，结果一致。
- 工作区中每个修改文件都能说明来源和用途。

---

## 六、阶段 B：前端—后端—资产配对审计

每个系统按“数据/逻辑 → 场景/Prefab → UI/输入 → 表现反馈 → 重置/存档”五段检查。

| 系统 | 后端与数据 | 场景/Prefab/资产 | 玩家入口与前端 | 关键验收 | 当前判定 |
|---|---|---|---|---|---|
| 职业选择 | `ClassData`、`PlayerStats` | 职业资产、展台 | 选择与确认 UI | 选择后属性与技能正确 | 待复验 |
| 武器 | `WeaponData`、持有状态 | 武器资产、拾取物、视觉 | 拾取/切换/弹药 UI | 两类武器行为和被动可区分 | 待复验 |
| 战斗 | `AttackData`、`WeaponHitbox` | 玩家/敌人 Prefab 组件 | 输入、预警、命中反馈 | 预警=判定=视觉 | 合并后重点复验 |
| 技能 | `SkillData`、`SkillExecutor` | 技能 SO、玩家组件 | 三技能槽与按键 | 法力/CD/效果/失败提示完整 | 待复验 |
| 道具与背包 | `ItemInventory` | 三种道具资产、拾取物 | 道具栏、背包、使用键 | 拾取、叠加、交换、消耗正确 | 待复验 |
| 地牢 | 布局、房间状态机 | 主场景、Tilemap、内容表 | 地图与房间反馈 | 进房、锁门、清房、开门 | 待复验 |
| 掉落与奖励 | 掉落/奖励逻辑 | 宝箱、商店、掉落资产 | 拾取、购买、奖励 UI | 奖励不会静默丢失 | 待复验 |
| 金币经济 | 钱包与消费 | 商店配置 | 金币 HUD、余额不足提示 | 获取、消费、重开清零 | 需确认当前实现 |
| 局内升级 | 升级定义与应用 | 升级系统挂载 | 三选一面板 | 暂停、选择、生效、死亡清空 | 需确认当前实现 |
| Boss 与通关 | Boss 阶段/结算逻辑 | Boss Prefab、楼层配置 | Boss 条、通关面板 | 阶段变化、死亡结算、继续 | 待复验 |
| 死亡重开 | Run 状态重置 | 玩家与场景引用 | 死亡/重开提示 | 不销毁玩家，临时状态全部重置 | 待复验 |
| Meta 与存档 | JSON、永久资源/升级 | Save 配置 | HUD、商店、结算入口 | 重启游戏后仍保留 | 旧实现清理后重审 |
| 暂停与设置 | 暂停、音量数据 | AudioManager、Mixer/Clip | 暂停菜单与滑条 | 暂停恢复、音量实际变化 | 待配置音频资产 |
| 教程 | 教程状态 | 固定提示点或引导资产 | 清楚的文字/图标 | 新玩家无需口头解释 | 未正式验收 |

### 配对判定规则

出现以下任一情况，系统都不能标记完成：

- 有逻辑类但场景或 Prefab 未挂载。
- 有 UI 但按钮/输入没有订阅实际逻辑。
- 有字段但没有对应 SO 或 Inspector 配置。
- 数值变化了但玩家没有可理解的反馈。
- 正常流程可用，但死亡、切层或重启后状态错误。
- 仅在 Editor 下通过，构建后因 `AssetDatabase` 路径失效。

---

## 七、阶段 C：Golden Path 单局主流程

本阶段只保证一条代表性路线完整，不追求所有随机组合一次覆盖。

### 标准路线

1. 启动游戏，进入准备房/大厅。
2. 看到基础键位、目标和交互提示。
3. 选择战士和一把已完整配置的武器。
4. 进入地牢并完成第一个普通房。
5. 获得一次可见奖励：金币、法力、道具或武器。
6. 使用一次道具和一次技能，观察数值及 UI 同步变化。
7. 经过宝箱或商店，完成一次成长选择或购买。
8. 完成精英/Boss 战，确认预警、判定、伤害与死亡流程。
9. 到达版本定义的终局层，显示结算。
10. 从结算进入下一局或返回大厅。

### 主流程阻断级别

| 等级 | 定义 | 示例 | 处理原则 |
|---|---|---|---|
| P0 | 无法启动或继续 | 编译错误、进不了场景、卡死 | 立即修复 |
| P1 | 主流程错误 | 不能攻击、门不开、Boss 不结算、存档损坏 | 当前阶段必须修复 |
| P2 | 功能可用但明显错误 | UI 不刷新、奖励反馈缺失、状态重置不全 | Demo 前必须修复 |
| P3 | 体验与一致性问题 | 间距、颜色、轻微动画问题 | 集中打磨 |
| P4 | 扩展建议 | 更多内容、额外特效 | 进入后续池 |

### 完成定义

- 连续完成 3 次标准路线，不出现 P0/P1。
- 三次中至少覆盖一次死亡重开、一次正常通关。
- Console 不持续刷 Error/Exception。

---

## 八、阶段 D：跨局、存档与重置

### 必测状态分类

| 状态 | 死亡后 | 通关后 | 退出重进后 |
|---|---|---|---|
| 当前 HP/护甲/法力 | 重置 | 重置 | 不恢复房内瞬时值 |
| 金币/局内升级/临时 Buff | 清空 | 清空 | 清空 |
| 职业与合法局外选择 | 按设计保留 | 按设计保留 | 保留 |
| Meta 货币/永久升级 | 保留 | 保留并结算 | 必须保留 |
| 当前攻击状态、无敌、CD、协程 | 全部停止并复位 | 全部停止并复位 | 无残留 |
| 房间敌人、预警和临时对象 | 清理 | 清理 | 无孤儿对象 |

### 存档破坏测试

- [ ] 首次启动无存档时可正常创建。
- [ ] 正常保存后退出重进，值一致。
- [ ] 存档缺少新增字段时能使用默认值迁移。
- [ ] 存档内容损坏时不崩溃，并按设计备份或重建。
- [ ] 连续保存不会重复发放结算奖励。

---

## 九、阶段 E：体验完善（不以代码为中心）

### 1. 信息表达

- 玩家始终知道：当前生命、护甲、法力、金币、楼层、技能状态和当前目标。
- 交互物靠近后明确显示按键与名称。
- 购买失败、法力不足、冷却中、背包已满必须有即时反馈。
- Boss 阶段变化和通关条件不能只存在于后台数值。

### 2. 操作与手感

- 攻击起手、有效帧和收招具有可读节奏。
- 预警范围和实际命中范围一致。
- 命中、受击、击杀分别有声音、颜色、停顿或震动层级。
- 不允许反馈过量遮挡危险信息。

### 3. 视觉一致性

- 暂不追求正式换皮，但统一字体、字号、面板边距、色彩含义和图标尺寸。
- 普通、精英、Boss、可交互物、危险预警应有稳定的视觉语义。
- 临时素材必须控制世界尺寸，避免 720px 图片以 7.2 单位直接进入场景。
- 左右朝向、序列帧锚点、武器挂点按同一规范验收。

### 4. 声音

- 为 `AudioManager` 配置实际 Clip，而不是仅保留调用接口。
- 最小集合：按钮、拾取、购买、攻击、命中、受击、敌人死亡、开门、Boss、BGM。
- 暂停菜单音量滑条必须改变真实输出，并在重新启动后按设计恢复。

### 5. 教程与上手

最小教程只覆盖完成第一局必要的信息：

- WASD 移动。
- 左键攻击。
- E 交互/拾取。
- 技能键与道具键。
- T 狼人变身（若当前版本保留）。
- 房间清理目标、Boss/终局目标。

教程完成后不应每局强制重复；是否永久跳过需进入存档验收。

### 6. 数值节奏

- 第一房让玩家安全理解操作。
- 中段至少产生一次可感知的构筑选择。
- 精英负责机制考试，不只是血量更高。
- Boss 必须有玩家可描述的阶段变化。
- 记录死亡原因、通关用时和关键资源，作为调参依据。

---

## 十、阶段 F：构建与交付

### 构建前检查

- [ ] 搜索全部 `#if UNITY_EDITOR`、`AssetDatabase` 和编辑器专用加载路径。
- [ ] 必需的职业、武器、技能、道具、字体和动画资产已进入运行时可加载位置。
- [ ] 所有 Build Settings 场景顺序正确。
- [ ] 目标分辨率和常见宽高比下 UI 不越界。
- [ ] 关闭开发用 Debug 对象、临时传送和作弊入口。
- [ ] 连续游玩 30 分钟无明显对象泄漏、孤儿预警或持续 GC 抖动。
- [ ] 使用独立构建完成一整局，而非只在 Editor 验证。

### Demo 交付完成定义

1. 新玩家不看工程即可完成一局。
2. 主流程连续三轮无 P0/P1。
3. 死亡、通关和退出重进三条状态路径均正确。
4. 独立构建与 Editor 行为一致。
5. 已知 P2/P3 有记录、负责人和是否延期的明确决定。
6. 当前版本有 Git 标签/提交、变更记录和验收记录。

---

## 十一、建议排期

| 批次 | 重点 | 预期产物 |
|---|---|---|
| 批次 1 | 合并兼容、编译、工作区整理 | 稳定 main 基线 |
| 批次 2 | 配对矩阵逐项实查 | 系统真状态表、P0～P4 问题单 |
| 批次 3 | Golden Path 打通 | 可从启动走到结算的一局 |
| 批次 4 | 死亡/通关/重启回归 | 可靠的跨局与存档行为 |
| 批次 5 | UI、教程、音频、手感、数值 | 可给外部玩家体验的版本 |
| 批次 6 | 独立构建与发布检查 | Demo 候选包与验收记录 |

排期不以“每天写多少代码”为指标，每个批次只有通过完成定义后才能进入下一批。

---

## 十二、首轮执行清单

下一轮建议直接执行以下事项：

1. [ ] 清完当前 Unity Console 全部编译错误。
2. [ ] 对 main 当前脚本、主场景和 Prefab 做一次健康扫描。
3. [ ] 按第六节矩阵填写每个系统的真实状态和证据路径。
4. [ ] 选择“战士 + 长剑”作为第一条 Golden Path 固定配置。
5. [ ] 从准备房开始跑到首次战斗，记录第一个阻断点。
6. [ ] 按 P0 → P1 → P2 顺序修复，不在途中扩展新内容。
7. [ ] 跑通后再覆盖长枪、狼人、死亡和通关分支。
8. [ ] 完成三轮回归后建立稳定提交。

---

## 十三、文档维护规则

- 本文是当前“整合验收与打磨”阶段的执行总账。
- 《项目长期计划_Roguelite路线图》继续保存长期方向，但其中的完成状态要在本轮验收后校准。
- 《项目进度报告》应在每个批次结束后更新真实状态，不再沿用未经复验的历史结论。
- 每次修复仍按项目惯例增加对应版本记录；普适教训同步写回《开发必读_核心信息整合.md》和 `project-dev-rules`。
- 每个已完成条目必须附至少一种证据：Unity Console、Play 验收、截图、日志、测试结果或独立构建记录。

---

## 十四、本阶段成功标准

本阶段成功不等于“所有功能都做完”，而是做到以下三点：

1. **工程可信**：文档状态与实际工程一致，不再出现“写着完成但无法使用”。
2. **游戏可交付**：现有内容组成一条完整、清楚、稳定的一局体验。
3. **后续可迭代**：未来新增敌人、美术、技能或房型时，有明确验收入口，不再形成新的孤立模块。

