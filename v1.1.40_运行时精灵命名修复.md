# v1.1.40 修复：运行时精灵未命名（GetSprite ArgumentException）

## 现象

`ArgumentException: GetSprite(32,45): SpriteAtlas does not contain a sprite with that name`——
(32,45) 为 Tilemap 格坐标；引擎在 Tilemap 内部按精灵名做图集查询，命中**空名精灵**即抛。

## 根因

v1.1.29 WallPropTileset 用 `Sprite.Create` 运行时重建墙精灵（R20 修复的 Sprite 烘焙方案）——
`Sprite.Create` 产出的精灵 **name 为空**；Tilemap 渲染/刷新的内部命名查询单到空名即抛
ArgumentException。v1.1.33（狼人 Awake 提前初始化）只是把时序提前、让潜伏问题更易暴露，
非直接原因（用户怀疑方向合理但取证排除了 FrameAnimator 路径——全项目无自写 GetSprite/SpriteAtlas 调用，
Assets/Packages 全量扫描确认）。

## 修复（全项目一类清零）

**所有运行时 `Sprite.Create` 结果显式命名**（tilemap/图集命名查询不再命中空名）：
- WallPropTileset（墙精灵，本 bug 主源）`xxx_wall_norm`；
- CoinDrop（金币）/ ManaOrb（法力球圆）/ SliderHandleState（滑钮光晕）；
- AttackIndicator（预警圆）/ PrepPortal·PrepPedestal·PrepRoomManager（白块）`RT_xxx`。

**教训（入必读）**：运行时 Sprite.Create 后必须立即 `.name = ...`——空名精灵进 Tilemap/UI
即触发引擎内部图集按名查询的 ArgumentException。

## 验收

Play 进地牢（生成墙/预警/金币/法力球/白块全路径）：Console 无 GetSprite ArgumentException；
墙贴图/碰撞/预警/掉落物视觉不变。

## 关联

R20（tile.transform 碰撞放大——同为运行时瓦片精灵工程化的坑）。
