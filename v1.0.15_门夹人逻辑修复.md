# v1.0.15 门夹人逻辑修复

> 日期：2026-09-01  
> 状态：已修复；Unity 编译 0 错误；全套 EditMode 23/23 通过。

## 问题与根因

`RoomTrigger` 原先在 `OnTriggerEnter2D` 中立即激活战斗房。Unity 的 Enter 事件表示两个碰撞体刚开始相交，并不表示玩家已经完整进入触发区域。玩家胶囊仍横跨门洞时，房间进入 Active，`Door` 随即启用实体碰撞体，于是门会生成在玩家身上，表现为被门夹住、卡在门口或被物理系统挤动。

原注释所称“四边内缩 0.5 格保证完全进入”并不成立：触发区内缩量固定，而玩家碰撞体有实际宽高与偏移，且 Enter 判断只要求发生重叠。

## 修复

- `RoomTrigger` 在 `OnTriggerEnter2D` 和 `OnTriggerStay2D` 中统一检查玩家世界空间碰撞包围盒。
- 只有玩家碰撞体的四条边都进入 `Room.Bounds` 后，才调用 `Room.Enter()` 并关闭战斗房门。
- 保留 `Door` 的状态唯一真源和既有门洞坐标，不再次引入半格坐标偏移。
- 新增 `RoomTriggerTests`，覆盖完全进入、纵向跨门、横向跨门三种边界情况。

## 自动验证

- `RoomTrigger.cs` 编译反馈：0 个错误。
- 全套 EditMode：23/23 通过。
- 首次测试尝试发生在旧 Play 会话中，Unity Test Runner 因不能创建 EditMode 测试场景而返回 0 项；停止 Play 后重跑正常通过。这不是游戏代码断言失败。

## 人工验收

- 从四个方向进入战斗房时，门应在角色身体完全越过门洞后关闭。
- 关闭后的门仍应阻止玩家退出，清房后视觉与碰撞同时关闭。
- 快速斜向穿门、贴墙穿门不应再把角色夹在门体内。
