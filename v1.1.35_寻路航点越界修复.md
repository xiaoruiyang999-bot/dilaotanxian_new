# v1.1.35 修复：MoveWithPathing 越界（ArgumentOutOfRangeException@271）

## 现象

近战敌人追击时每帧刷 `ArgumentOutOfRangeException: Index was outside the bounds of the array`
于 `EnemyAI.MoveWithPathing` 271 行（航点推进循环）。

## 根因与修复（双保险）

1. **FindPath 契约漏洞**：`start==goal` 早退路径返回 true 时结果仅 1 个航点，而调用方
   `pathIndex` 从 1 起步——依赖"count≥2"的隐含契约。修复：早退补起点，**true ⇒ 恒 ≥2 航点**。
2. **MoveWithPathing 防御钳制**：航点索引越界（任何极端时序/中间态）不再上抛——
   索引出界即作废整条路径（清空+重置节流），当帧回退直线移动。错误不可能再进入画面。

## 验收

Play 引敌追击（含贴身 start==goal 场景）：无 ArgumentOutOfRangeException 刷屏；
寻路绕障行为不变；直线通畅路径手感不变。

## 关联

v1.1.32 寻路系统；防御层遵循"规则拒绝坏方案"原则——路径状态异常自愈而非崩溃。
