# v0.6.2 预警与判定对齐修复（同源原则回归）

> 状态：**已完成 · 编译通过 · 待 Play 验收**
> 日期：2026-08-20
> 历史溯源：[v0.4.5.2 Fix Final5](v0.4.5.2%20Fix%20Final5.md)——"使攻击动画、攻击范围显示、实际伤害判定三者严格基于同一份 AttackData"
> 关联：[v0.6.1_手感与反馈包](v0.6.1_手感与反馈包.md)（本次为 M1 验收中发现的回归）

---

## 1. 问题

近战敌人（玩家侧同样）的**攻击范围预警与实际伤害判定不对齐**：
预警显示一大片扇形危险区，实际只有中间一条窄矩形有伤害（"预警一大片、实际打一线"），
或者反过来在预警边缘外被击中。

## 2. 根因：v0.5 重构时"三者同源"原则断裂

| 时期 | 预警 | 判定 | 状态 |
|---|---|---|---|
| v0.4.5.2 Final5 | AttackIndicator 扇形 Mesh（读 AttackData） | AttackQuery 扇形语义 | ✅ 同源 |
| v0.5 重构后 | 仍是**扇形**（`SetRadius(AttackRange)` + `SetAngle(AttackAngle)`） | WeaponHitbox 改为**矩形 OverlapBox**（长=AttackRange×lossyScale，宽=weaponWidth×lossyScale） | ❌ 形状分叉 |

量化（EnemyMeleeAttackData）：预警 = **120° 扇形 × 半径 1.5**，判定 = **1.5 长 × 0.2 宽矩形**。
`AttackIndicator.ShapeType.Box` 枚举自重构起就是空壳（无 Mesh 实现），从未接上。

## 3. 修复：预警直接复刻判定矩形（4 处改动）

| 文件 | 改动 |
|---|---|
| `Combat/AttackIndicator.cs` | 补上 `BuildBoxMesh()`（从攻击者中心沿方向延伸的矩形 Mesh，与 BuildLineMesh 同构）+ `SetBoxSize(length, width)` API + RebuildMesh 分发；新增 `currentWidth` 字段 |
| `Combat/WeaponHitbox.cs` | 新增 `CurrentBoxGeometry` 只读属性（x=长度, y=宽度，**已含 lossyScale**）——与 `Tick()` 同一套公式的单一数据源 |
| `Enemy/EnemyCombat.cs` | `EnterWindup` 近战分支：扇形 → `SetShape(Box)` + `SetBoxSize(weaponHitbox.CurrentBoxGeometry)` |
| `Player/PlayerCombat.cs` | 玩家攻击预警同样改 Box（玩家根 scale=0.6，扇形与矩形判定偏差更明显） |

远程（Line 预警线）与召唤分支不变。

## 4. 验收清单（UnitySkills 编译检查已通过：4 脚本零诊断）

- [ ] 近战敌人预警显示为窄矩形（与武器视觉同宽同长），不再是 120° 扇形
- [ ] 站在预警矩形内 → 被打；贴着矩形边缘外 → 安全（所见即所判）
- [ ] 玩家自己的攻击预览同样为矩形
- [ ] 远程敌人的预警线行为不变（跟手 + LOS 门控）
- [ ] 精英/变体（scale≠1）的预警大小与判定同步缩放

## 5. 备注

- `AttackAngle` 字段自此不再被近战预警消费（仅存于 AttackData；若后续做真正的扇形判定招式可恢复 Sector 预警）。
- 本次由 UnitySkills（REST 直操 Unity）完成编译验证——新工具链首次实战。
