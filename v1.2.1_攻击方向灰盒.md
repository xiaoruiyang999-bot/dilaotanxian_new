# v1.2.1 攻击方向灰盒(T-01)

> 对应 GDD §6【待定项 T-01】与 §27 版本表 v1.2.1"战斗方向原型"。
> 本记录只写实际改动、验证与遗留;设计决策回写 GDD,不在本文另立。

## 实际改动

### 基线恢复(前置,提交 b5907fd)
- 恢复被 v1.2.0 提交误删的 `Packages/com.besty.unity-skills/` 编辑器工具包(12 万行文档清理时误伤,
  导致编辑器侧验证链路全断);
- 狼人兽化 UI 动态高度(v1.1.54,另一会话工作)与《开发必读》v1.2 重构合并(旧红线析出为
  `开发必读_红线归档_v1.1.md`)入库;地牢相机 targetOrthoSize=9+亚像素档(用户定案);
- 门禁:Roslyn 0 错 + EditMode 100/100。

### T-01 灰盒(提交 e14d9d2)
- **方向解析核心**(另一会话实现,本轮审查确认接入完整):
  `PlayerAimController.cs` 内 `AttackDirectionPrototypeMode { Horizontal, FourWay }` +
  `AttackDirectionResolver.Resolve` 纯函数(主轴离散、精确对角固定优先水平、零输入回退朝向);
- **同源链**:`PlayerCombat.ResolveMeleeAttackDirection()` 为唯一方向入口——瞄准预览(UpdateAiming)、
  起手锁定(StartWindup)、踏步与 Hitbox(EnterActive→SetLaneMode(Vector2))全部消费同一结果;
  `WeaponHitbox` 攻击带几何升级为任意四向矩形(方向向量版 SetLaneMode,标量版转发兼容旧测试);
- **切换方式**:Player 组件 PlayerCombat → `Melee Direction Mode`(Play 中切换即时生效,不需要重启);
- **测试门禁**(本轮补齐):`AttackDirectionResolverTests` 5 项——Horizontal 恒左右、四向主轴离散、
  精确对角优先水平、零输入回退、全模式恒四正向。锁定"预览=踏步=判定"的方向合同,
  防灰盒只转视觉不转判定的回归。

## 验证

- Roslyn 真编译 0 错(145 源文件);EditMode 全量 **105/105**;
- A/B 双模式 Play 可切换;鼠标瞄准旧链未删(定案前保留,符合 GDD 要求)。

## 遗留 / 下一步

- **T-01 定案待用户试玩**(A 仅左右 vs B 四方向);定案后:
  1. 回写 GDD §6 与 §24 登记表(移出待定项);
  2. 输入与判定规格随定案写入(斜向取主轴/对角规则已由测试锁定);
  3. 进入 v1.2.2 狼人 Animator 单武器灰盒。
- 对角邻域跨界翻转无迟滞(方向整段锁定,实战无感;灰盒阶段明确不做带状态迟滞——测试注释已注明口径)。
