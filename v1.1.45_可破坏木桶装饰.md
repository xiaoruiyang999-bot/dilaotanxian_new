# v1.1.45 可破坏木桶装饰(Barrels 素材接入)

## 需求(用户)

木桶素材(`E:\WeGameApps\The animation of Unity\主界面素材\水桶素材`,barrel_NN 命名 36 张)接入地牢内容生成:
素材入 `Resources/Art/Decor/Barrels`,生成器调用挂在 `StoneDecorSpawner.Spawn` 之后:
`BarrelDecorSpawner.Spawn(room, rng);`(bucket.cs,用户手写初版)。

## 素材分档(实测 @PPU85)

| 档 | 素材 | 世界尺寸 | 语义 |
|---|---|---|---|
| 小件 | barrel_37~43 | ≈0.44~0.54 | 无需单独处理——按 bucket.cs 设计落入 Trigger 装饰档 |
| 单桶/桶堆 | barrel_00~17、24~28 | ≈0.57~0.85 | **Trigger 可穿过装饰桶**:Obstacle 层 + isTrigger 碰撞盒(bounds×0.8)+ ObstacleHealth(3)——可穿过、可被攻击打碎 |
| 大型木箱堆 | barrel_18~23 | ≈0.93~1.05 | **实体障碍**:同上但 isTrigger=false,阻挡通行 |

## 对用户初版的两处必要修正

1. **实体档阈值 1.2 → 0.85**:阈值 1.2 抄自石块门槛,但木桶素材池实测最大 ≈1.05——
   全池无 ≥1.2 图,实体桶分支永不触发(木箱堆全按可穿过的 Trigger 处理,视觉大件却能穿,违和)。
2. **实体件避让骨架格**(R22):`Spawn` 增加可选参数 `avoidCells`(默认 null,与用户两参调用形式兼容),
   实体木箱堆落点 ∈ 骨架格则换点重试 ≤6——不回堵 v1.1.44 玩家口径验证留出的路网/入口。
   Trigger 装饰桶可穿过,无需避让。

其余设计(HP3、Brightness 1.75、数量:装饰 1~4 + 实体 0~2、sortingOrder 3、flipX 变体)保持用户初版。

## 素材导入(R3 红线)

36 张 png 平铺入 `Assets/Resources/Art/Decor/Barrels/`(主目录 18 张 + `大型木箱堆` 子目录 18 张——
`Resources.LoadAll` 不递归,子目录素材必须平铺)。meta 按石块模板批量生成:
**spriteMode=Single**(R3:默认 Multiple 会自动切片成碎片)+ PPU 85 + Point + 无压缩 + pivot 中心。
UnitySkills `texture_get_import_settings` 抽查 barrel_00/20/40 全部确认 Single/85 生效。

## 接入点

`DungeonBuilder.SpawnContent`(v1.1.44 骨架避让链之后):
```
StoneDecorSpawner.Spawn(room, rng, skeleton);
BarrelDecorSpawner.Spawn(room, rng, skeleton);   // v1.1.45 可破坏木桶/木箱堆
```

## 验收

- 编译 0 错 0 警;`_dev/compile_check.py` 真编译通过;全量 EditMode **31/31**。
- Play 走查:各房出现可打碎的装饰桶(Trigger 碰撞、能穿过)与实体木箱堆(挡路、敲 3 下碎);
  掉落/音效走 ObstacleHealth 既有链路(若木桶需要单独掉落表/音色,后续在 ObstacleHealth 分型)。

## 关联

- v1.1.26 StoneDecorSpawner(架构参照)、v1.1.44 R22(骨架避让)。
- 素材源:`E:\WeGameApps\The animation of Unity\主界面素材\水桶素材\`(含"大型木箱堆"子目录)。
