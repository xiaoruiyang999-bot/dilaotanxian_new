# v0.6.0 操作记录

> 版本主题：操作手感（移动抖动修复 + 体力/奔跑/闪避）+ 敌人攻击触发范围修复。
> 新场景：`Assets/Scenes/v0_6_ClassWeapon.unity`（由 v0_5_Dungeon 复制，新 GUID）。

## 实现内容

### 1. 移动"一闪一闪"修复（最高优先级）
- 四个单位 prefab 的 Rigidbody2D `m_Interpolate` 0 → 1（插值开启）。
  - `Player.prefab`、`Enemy.prefab` 直接改 YAML；`Enemy_Elite.prefab` / `Enemy_Boss.prefab` 是 Enemy 的 prefab 变体，自身无 Rigidbody2D 覆盖，自动继承基体的插值设置。
- `EnemyAI` 移动节奏：Update 只做决策并记录移动意图（`SetMoveIntent` / `SetStopIntent`），新增 `FixedUpdate` 统一调用 `EnemyController.MoveTowards/StopMoving` 写 `rb.linearVelocity`，意图每物理帧消费一次。行为与现状一致，只改写入时机。

### 2. 敌人攻击触发范围 bug 修复
- 触发判定集中在 `EnemyCombat.IsInAttackRange`，改为 `AttackData.AttackRange + attackRangeBuffer`（缓冲 0.3f，序列化可调）。
- 新增 `EnemyCombat.CurrentAttackRange` 访问器。
- 删除 `EnemyAI` 上从未被代码使用的序列化字段 `attackData`（误导配置来源）。
  - 残留说明：`Enemy.prefab` 的 EnemyAI 组件块、两个变体 prefab 中针对 EnemyAI 的 attackData 覆盖条目仍在 YAML 里，字段删除后 Unity 会忽略这些残留，不会 Missing、无运行时影响；下次在编辑器里保存 prefab 时会自动清理。

### 3. 体力条（Stamina，PlayerStats）
- `maxStamina = 100`、`staminaRegenRate = 30/s`、`staminaRegenDelay = 0.8s`（任何消耗重置延迟计时）。
- `TryConsumeStamina(amount): bool`（不足不扣返回 false）、`ConsumeStaminaOverTime(ratePerSec)`（奔跑每帧调用）。
- 体力变化走现有 `OnStatsChanged` 事件。

### 4. PlayerMovement.cs（新脚本，now_use/Player/）
- 移动执行层，`FixedUpdate` 统一写 `rb.linearVelocity`；`PlayerController` 仍是输入唯一入口（`SetMoveInput` / `SetSprintHeld` / `TryDash` 转发），其 FixedUpdate 已删除。
- 奔跑：按住 Sprint，移速 ×1.6，耗体力 20/s，体力耗尽或松开自动恢复常速（无输入时不耗体力）。
- 死亡速度清零；`StopImmediately()` 供死亡/Respawn/OnDisable 调用。
- 组件已通过 YAML 直接挂到 `Player.prefab`（fileID 5500000000000000010），并加了 `RequireComponent`。

### 5. 闪避（Dash）
- 输入：新 action "Dash"，绑定 `<Keyboard>/space`，performed 触发。
- 条件：非死亡、不在冲刺中、CD 结束、体力 ≥ 35。
- 行为：方向 = 移动输入方向（无输入用 `PlayerAimController.AimDirection`）；距离 3、时长 0.18s（速度 3/0.18 ≈ 16.7）；耗体力 35；冲刺期间 `Health.SetInvincible(true)` 无敌。
- CD：冲刺完整结束后进入 0.5s 冷却（`dashCooldown` 序列化）。
- 表现：冲刺期间 SpriteRenderer alpha = 0.5，途中均匀生成 3 个残影（复制当前 sprite，DOTween `DOFade(0, 0.3f)` 淡出销毁，tween 均 `SetLink`）。

### 6. Health 无敌标记
- `SetInvincible(bool)` / `IsInvincible`；`TakeDamage` 无敌时早退（不扣血、不触发受伤事件、不重置脱战计时）。

### 7. 输入系统
- `Assets/PlayerInputActions.inputactions`：新增 Dash（Space）/ Sprint（LeftShift）两个 Button action，id 均为全新 GUID；JSON 已用 node 校验通过。
- `PlayerController.OnActionTriggered` 新增 Dash（performed → TryDash）、Sprint（performed → 按住 / canceled → 松开）分支。

### 8. PlayerUI 体力条
- 新增 `staminaBar` 引用 + `RecoverStaminaBar()`（自动查找 `PlayerStatsPanel/StaminaBar`，旧场景没有则仅 Warning）。
- v0_6 场景 PlayerStatsPanel 新增 StaminaBar（黄色 #F4D03F，窄条 200×6，位于护甲条上方），Filled Image，订阅 `OnStatsChanged` 刷新。
- 同时清掉了 Health.cs / PlayerUI.cs 里的调试 Debug.Log（保留 Error/Warning）。

### 9. 场景
- `v0_5_Dungeon.unity` → `v0_6_ClassWeapon.unity`，meta 使用新 GUID（59105a2f…），未沿用旧 GUID。
- `ProjectSettings/EditorBuildSettings.asset` 里只有 SampleScene，v0_5 不在构建列表，按约定未添加 v0_6（如需打包请自行加入）。

## 改动文件清单

新建：
- `Assets/Scripts/now_use/Player/PlayerMovement.cs`（+ .meta，guid 5b962d61…）
- `Assets/Scenes/v0_6_ClassWeapon.unity`（+ .meta，新 guid）
- `v0.6.0操作.md`（本文件）

修改：
- `Assets/Scripts/now_use/Player/PlayerController.cs`（输入转发 Dash/Sprint，FixedUpdate 移除）
- `Assets/Scripts/now_use/Player/PlayerStats.cs`（体力系统）
- `Assets/Scripts/now_use/Player/Health.cs`（无敌标记 + 删调试日志）
- `Assets/Scripts/now_use/Player/PlayerUI.cs`（体力条 + 删调试日志）
- `Assets/Scripts/now_use/Enemy/EnemyAI.cs`（移动意图 + FixedUpdate，删无用 attackData 字段）
- `Assets/Scripts/now_use/Enemy/EnemyCombat.cs`（触发范围 + 0.3 缓冲 + CurrentAttackRange）
- `Assets/Scripts/now_use/README.md`（清单更新，51 个脚本）
- `Assets/PlayerInputActions.inputactions`（Dash/Sprint）
- `Assets/Prefabs/Player.prefab`（插值 + PlayerMovement 组件 + 体力字段）
- `Assets/Prefabs/Enemy.prefab`（插值）

## 关键数值

| 项 | 值 |
|---|---|
| 体力上限 / 回复 / 延迟 | 100 / 30 每秒 / 0.8s |
| 奔跑 | 移速 ×1.6，耗体力 20/s |
| 闪避 | 距离 3，时长 0.18s，耗体力 35，CD 0.5s，无敌全程，alpha 0.5，残影 3 个 |
| 敌人攻击触发 | AttackData.AttackRange + 0.3（普通 1.5 / 精英 1.7 / Boss 2.5） |

## 验证情况

- 编译：`dotnet build Assembly-CSharp.csproj` 通过，0 警告 0 错误（运行中的 Unity 编辑器已自动重新生成 csproj 并包含新脚本 PlayerMovement.cs）。
- inputactions JSON：node `JSON.parse` 校验通过，4 个 action / 新绑定路径正确。
- prefab YAML：Player/Enemy 各 1 个 Rigidbody2D，`m_Interpolate: 1` 各 1 处；精英/Boss 为变体自动继承。
- 场景 YAML：StaminaBar 块（GameObject/RectTransform/Image/CanvasRenderer）已插入并挂到 PlayerStatsPanel 子物体列表，PlayerUI 组件已引用。

## 需要在 Unity 编辑器里做的事

1. 切到编辑器让它完成一次脚本编译与资源导入（自动生成/刷新 meta），确认 Console 无报错。
2. 打开 `v0_6_ClassWeapon.unity` 试玩验证：WASD 移动、Shift 奔跑（体力条下降）、Space 闪避（半透明+残影+无敌）、精英/Boss 进入攻击圈即抬手。
3. Enemy prefab 上 EnemyAI 残留的 attackData 序列化值无害，编辑器里重新保存 prefab 即可自动清理。

---

## 追加：体力条 UI 显示修复（v0.6.0 补丁）

**问题**：用户反馈体力条没有 UI 显示。
**排查结论**：Editor.log 显示测试时编辑器加载的是 `v0_5_Dungeon.unity`，而 StaminaBar 只加在了新场景 `v0_6_ClassWeapon.unity` 的 YAML 里——跑 v0_5 场景自然没有体力条。磁盘上 v0_6 的 StaminaBar 块完好，未被编辑器覆盖。
**修复方式**（改为代码侧保证，不依赖场景 YAML，任何场景都生效）：

1. `PlayerUI.cs`：`RecoverStaminaBar()` 在面板里找不到 StaminaBar 时，运行时克隆护甲条自动创建一条（Filled 横条样式一致），黄色 #F4D03F、高 5px 窄条，放在 HP 条下方（间距 2px）。
2. `PlayerWorldStatusBar.cs`（玩家头顶世界空间状态条）：新增体力条，结构为"上蓝护甲(80×5) + 中红 HP(80×8) + 下黄体力(80×4)"，新增序列化字段 `staminaColor`（#F4D03F）与 `staminaBarSize`，订阅 `OnStatsChanged` 刷新 `fillAmount`。

两处均读 `PlayerStats.CurrentStamina / MaxStamina`。头顶条的 canvasSize（84×20）未改，三条总高 23px 略超出但世界空间 Canvas 无裁剪，显示正常。

**改动文件**：`Assets/Scripts/now_use/Player/PlayerUI.cs`、`Assets/Scripts/now_use/Player/PlayerWorldStatusBar.cs`（纯代码，未动场景/prefab YAML）。
**验证**：`dotnet build Assembly-CSharp.csproj` 通过，0 警告 0 错误。需用户切回编辑器重新编译后，在任意场景（含 v0_5）进入 Play 验证两条体力条显示与消耗/回复刷新。

**体力条位置修复（补丁2）**：用户反馈屏幕面板体力条显示在护甲条上方。根因：v0_6 场景 YAML 里的 StaminaBar 当初就加在护甲条上方（y=21），且旧场景运行时克隆条的定位也不够稳。修法：PlayerUI 新增 `RepositionStaminaBar()`，Start 阶段无论体力条来自场景还是运行时创建，都强制把 RectTransform 重设到 HP 条正下方（锚点/枢轴对齐 HP 条，间距参考护甲-HP 的 5px，空间不足时收紧到面板底边内）；`CreateStaminaBarAtRuntime()` 不再自行定位。头顶世界空间条（PlayerWorldStatusBar）检查后确认顺序本就是"上护甲、中HP、下体力"，无需修改。纯代码改动（PlayerUI.cs），未动场景/prefab YAML；dotnet build 通过（0 警告 0 错误）。
