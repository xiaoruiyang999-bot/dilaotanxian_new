# v0.4.5.2 Enemy 攻击系统统一修复计划（Final）

> 版本：v0.4.5.2 EnemyAttackSystem Fix Final
>
> 目标：将 Enemy 攻击状态机从 `EnemyAI` 迁移到 `EnemyCombat`，使 `PlayerCombat` 与 `EnemyCombat` 处于同一层级，统一负责攻击状态机与命中判定。

---

## 一、当前问题

`PlayerCombat` 与 `EnemyCombat` 职责不一致：

- `PlayerCombat`：完整攻击状态机（Windup / Active / Recovery）+ 命中判定。
- `EnemyCombat`：仅冷却 + 简单伤害调用。
- `EnemyAI`：同时承担 AI 决策与攻击子阶段状态机。

导致：

1. Enemy 攻击逻辑分散在 `EnemyAI` 与 `EnemyCombat` 中。
2. Player 与 Enemy 无法复用同一套攻击流程。
3. `EnemyAI` 职责过重。

---

## 二、最终目标架构

```
PlayerAimController
        │
        ▼
PlayerCombat ──────── 攻击状态机 + 命中判定
        │
        ▼
WeaponController → WeaponAnimator → AttackIndicator


EnemyAI
   │
   ▼
EnemyCombat ───────── 攻击状态机 + 命中判定
   │
   ▼
WeaponController → WeaponAnimator → AttackIndicator
```

**关键约定：**

- `PlayerCombat` 与 `EnemyCombat` 处于同一层级。
- 两者都负责：
  - 攻击状态机（Windup / Active / Recovery）
  - 调用 `WeaponController` / `WeaponAnimator`
  - 控制 `AttackIndicator`
  - 执行命中判定
- `EnemyAI` 只负责：
  - Idle / Chase 等高层状态
  - 判断是否在攻击范围内
  - 调用 `EnemyCombat.TryAttack(target)`

---

## 三、脚本职责表

| 脚本 | 负责 | 不负责 |
|---|---|---|
| **PlayerAimController** | 鼠标/手柄瞄准输入，输出 `AimDirection` | 攻击状态、伤害 |
| **PlayerCombat** | 玩家攻击状态机、命中判定、调用 WeaponController/WeaponAnimator/AttackIndicator | AI、移动、输入读取 |
| **EnemyAI** | 高层 AI（Idle/Chase/ReturnToPatrol）、判断攻击条件、调用 `EnemyCombat.TryAttack()` | 攻击子阶段、武器动画、伤害判定 |
| **EnemyCombat** | 敌人攻击状态机、命中判定、调用 WeaponController/WeaponAnimator/AttackIndicator | AI 决策、移动 |
| **WeaponController** | 武器朝向、方向锁定、角度计算、Sprite 视觉 | Tween、状态机、伤害 |
| **WeaponAnimator** | 攻击期间 Tween 播放 | 攻击逻辑、方向、Sprite |
| **AttackIndicator** | 攻击范围预警显示 | 状态机、伤害 |

---

## 四、修改文件列表

1. **`Assets/Scripts/v0_4/EnemyCombat.cs`**
   - 新增完整攻击状态机。
   - 新增 `WeaponController` / `WeaponAnimator` / `AttackIndicator` 引用。
   - 新增 `TryAttack(Transform target)` 入口。
   - 新增 `PerformFanAttack()` 判定方法。

2. **`Assets/Scripts/v0_4/EnemyAI.cs`**
   - 删除内部 `AttackSubPhase`（Windup / Active / Recovery）。
   - 删除对 `WeaponController` / `WeaponAnimator` / `AttackIndicator` 的直接引用和调用。
   - 保留高层状态切换与攻击条件判断。
   - 攻击触发改为 `combat.TryAttack(target)`。

3. **`Assets/Prefabs/Enemy.prefab`**
   - `EnemyCombat` 组件新增引用：
     - `weaponController` → `WeaponController`
     - `weaponAnimator` → `WeaponAnimator`
     - `attackIndicator` → `AttackIndicator`
   - `EnemyAI` 组件移除上述引用。

---

## 五、不修改文件

以下文件保持 Final3 版本不变：

- `AttackData.cs`
- `PlayerAimController.cs`
- `WeaponController.cs`
- `WeaponAnimator.cs`
- `PlayerCombat.cs`
- `Player.prefab`
- `PlayerAttackData.asset`
- `EnemyMeleeAttackData.asset`

---

## 六、完整执行步骤

### Step 1：修改 `EnemyCombat.cs` —— 接管攻击状态机

**操作：**

1. 新增引用字段：
   ```csharp
   [SerializeField] private AttackData attackData;
   [SerializeField] private WeaponController weaponController;
   [SerializeField] private WeaponAnimator weaponAnimator;
   [SerializeField] private AttackIndicator attackIndicator;
   [SerializeField] private Transform weaponPivot;
   ```

2. `Awake()` 中自动获取缺失组件：
   ```csharp
   if (weaponController == null) weaponController = GetComponent<WeaponController>();
   if (weaponAnimator == null) weaponAnimator = GetComponentInChildren<WeaponAnimator>(true);
   if (attackIndicator == null) attackIndicator = GetComponentInChildren<AttackIndicator>(true);
   ```

3. 新增攻击子阶段枚举：
   ```csharp
   private enum AttackSubPhase { None, Windup, Active, Recovery }
   private AttackSubPhase attackSubPhase = AttackSubPhase.None;
   private float windupTimer;
   private float activeTimer;
   private float recoveryTimer;
   private bool activeMomentTriggered;
   private Transform currentTarget;
   ```

4. 新增 `TryAttack(Transform target)` 入口：
   ```csharp
   public bool TryAttack(Transform target)
   {
       if (attackSubPhase != AttackSubPhase.None) return false;
       if (!CanAttack) return false;
       if (attackData == null) return false;

       currentTarget = target;
       StartWindup();
       return true;
   }
   ```

5. 新增 `StartWindup()`：
   - 设置 `attackSubPhase = Windup`
   - `windupTimer = attackData.WindupTime`
   - `activeMomentTriggered = false`
   - `weaponController.LockAttackDirection()`
   - 显示 `AttackIndicator`

6. 新增 `Update()` 驱动状态机：
   ```csharp
   void Update()
   {
       switch (attackSubPhase)
       {
           case AttackSubPhase.Windup: UpdateWindup(); break;
           case AttackSubPhase.Active: UpdateActive(); break;
           case AttackSubPhase.Recovery: UpdateRecovery(); break;
       }
   }
   ```

7. 新增 `UpdateWindup()`：
   - 计时结束后调用 `EnterActive()`

8. 新增 `EnterActive()`：
   - 设置 `attackSubPhase = Active`
   - `activeTimer = attackData.ActiveDuration`
   - 调用 `weaponAnimator.Play(...)`，参数由 `weaponController` 提供

9. 新增 `UpdateActive()`：
   - 计时结束后调用 `EnterRecovery()`

10. 新增 `OnActiveMoment()`：
    - 调用 `PerformFanAttack()`
    - 隐藏 `AttackIndicator`

11. 新增 `EnterRecovery()`：
    - 设置 `attackSubPhase = Recovery`
    - `recoveryTimer = attackData.RecoveryTime`

12. 新增 `UpdateRecovery()`：
    - 计时结束后：
      - `weaponAnimator.Stop()`
      - `weaponController.UnlockAttackDirection()`
      - `attackSubPhase = None`
      - `currentTarget = null`

13. 新增 `PerformFanAttack()`：
    - 复制 `PlayerCombat.PerformFanAttack()` 逻辑。
    - 使用 `weaponController.GetAimDirection()` 作为朝向。
    - 对 `AttackData.TargetLayer` 目标造成伤害。

14. 保留原有冷却逻辑：
    - `CanAttack`
    - `lastAttackTime`
    - 命中后记录冷却。

**目的：**

- 让 `EnemyCombat` 与 `PlayerCombat` 同层级。
- 统一负责攻击状态机与判定。

---

### Step 2：修改 `EnemyAI.cs` —— 只负责 AI 决策

**操作：**

1. 删除字段：
   - `WeaponController weaponController`
   - `WeaponAnimator weaponAnimator`
   - `AttackSubPhase attackSubPhase`
   - `float windupTimer / activeTimer / recoveryTimer`
   - `bool activeMomentTriggered`

2. 保留字段：
   - `EnemyCombat combat`
   - `EnemyController controller`
   - `EnemyStats stats`
   - `EnemyHealth health`
   - `Transform target`
   - 高层状态 `State`

3. `Awake()` 中删除对 `WeaponController` / `WeaponAnimator` 的获取。

4. 删除 `UpdateAttack()` 中的子阶段切换逻辑。

5. `UpdateChase()` 中：
   - 保持面向玩家。
   - 保持调用 `weaponController?.SetAimDirection(dir)` **直到 Step 3 明确迁移**。
   - 当满足攻击条件时，调用 `combat.TryAttack(target)`。

6. `ChangeState(State.Attack)` 进入逻辑简化：
   - 不再启动 Windup/Active/Recovery。
   - 由 `EnemyCombat` 内部处理。

7. `ChangeState` 从 Attack 退出时：
   - 不再调用 `weaponAnimator.Stop()` / `weaponController.UnlockAttackDirection()`。
   - 由 `EnemyCombat` 内部处理。

**注意：**

由于 `EnemyAI` 普通状态仍需更新武器朝向，因此 `weaponController.SetAimDirection(dir)` 暂时保留在 `EnemyAI` 中。攻击期间由 `EnemyCombat` 调用 `LockAttackDirection()`，`WeaponController` 会自动忽略后续 `SetAimDirection` 调用。

**目的：**

- `EnemyAI` 只负责是否发起攻击的决策。
- 攻击流程完全下沉到 `EnemyCombat`。

---

### Step 3：调整 `Enemy.prefab`

**操作：**

1. `EnemyCombat` 组件新增引用：
   - `attackData` → `EnemyMeleeAttackData.asset`
   - `weaponController` → `WeaponController`
   - `weaponAnimator` → `WeaponAnimator`
   - `attackIndicator` → `AttackIndicator`
   - `weaponPivot` → `WeaponPivot`（如需要）

2. `EnemyAI` 组件：
   - 移除 `weaponController` 引用（如果添加了）。
   - 移除 `weaponAnimator` 引用（如果添加了）。
   - 保留 `combat` 引用。

**注意：**

由于 `EnemyAI` 仍需要在 Chase 状态更新武器朝向，`weaponController` 引用可以选择：

- 方案 A：`EnemyAI` 保留 `weaponController` 引用，仅用于 `SetAimDirection`。
- 方案 B：`EnemyAI` 不保留引用，由 `EnemyCombat` 在 `TryAttack` 前设置一次方向。

推荐方案 A，以保持普通状态下武器持续朝向玩家。

---

### Step 4：编译验证

**操作：**

- `dotnet build Assembly-CSharp.csproj`
- 目标：`0 警告，0 错误`

---

## 七、PlayerCombat / EnemyCombat 最终同层级对比

### PlayerCombat

```csharp
// 输入来源
PlayerAimController aimController

// 攻击流程
UpdateAiming()
TryAttack()
StartWindup() / EnterActive() / EnterRecovery()
PerformFanAttack()

// 武器控制
WeaponController weaponController
WeaponAnimator weaponAnimator
AttackIndicator attackIndicator
```

### EnemyCombat

```csharp
// 目标来源
Transform currentTarget（由 EnemyAI 传入）

// 攻击流程
TryAttack(Transform target)
StartWindup() / EnterActive() / EnterRecovery()
PerformFanAttack()

// 武器控制
WeaponController weaponController
WeaponAnimator weaponAnimator
AttackIndicator attackIndicator
```

两者结构完全一致。

---

## 八、验证标准

### Player（保持 Final3 行为）

- [ ] 待机时武器跟随鼠标。
- [ ] 攻击时以鼠标方向为中心挥动 120°。
- [ ] 攻击结束恢复跟随鼠标。

### Enemy

- [ ] Chase 状态武器朝向玩家。
- [ ] 进入攻击范围后调用 `EnemyCombat.TryAttack()`。
- [ ] Windup 阶段显示 `AttackIndicator`。
- [ ] Active 阶段播放武器动画。
- [ ] ActiveMoment 执行扇形判定并造成伤害。
- [ ] Recovery 阶段结束后恢复朝向玩家。

### 架构

- [ ] `EnemyAI` 不直接控制武器动画。
- [ ] `EnemyCombat` 与 `PlayerCombat` 结构一致。
- [ ] 编译 0 警告 0 错误。

---

## 九、Input System 问题列表（本次不修复）

以下问题单独记录，不在本次任务中处理：

1. 项目中仍存在使用 `UnityEngine.Input` 的代码（如 `PlayerAimController` 当前使用 `Input.mousePosition`）。
2. 项目已启用新 Input System，但部分代码未迁移。
3. 需要统一使用 `UnityEngine.InputSystem` 的 `Mouse.current.position.ReadValue()` 或 `PlayerInput` 组件。
4. 涉及文件可能包括：
   - `PlayerAimController.cs`
   - `PlayerController.cs`
   - 其它使用 `Input.GetKey` / `Input.GetAxis` 的脚本

建议作为独立任务：`v0.4.5.x_InputSystem_Migration.md`。

---

## 十、待确认

1. 是否确认按本计划执行？
2. `EnemyAI` 是否保留 `weaponController` 引用用于普通状态朝向更新？
3. `EnemyAttackData.asset` 的 `activeDuration`、`recoveryTime`、`windupTime` 是否需要调整？
