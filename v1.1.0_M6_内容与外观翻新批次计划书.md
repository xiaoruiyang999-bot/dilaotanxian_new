# v1.1.0 起 · M6 批次计划书 —— 内容与外观翻新（槽位 / 金币 / 狼人技能 / 换装 / 地皮 / 牛头马面）

> 日期：2026-08-31 ｜ 版本基线：v1.0.8（打击手感恢复与角色选择页，代码已落盘编译 0/0）
> 定位：路线图「v1.0+ 长线池」首次成批立项（原 M5 为"最后一个正式里程碑"，本批次按长线池
> 条目"美术素材全面替换（决策③）"+ 系统补全成批启动，内部沿用 **M6** 编号记账）。
> 性质：**总账文档**（同《项目长期计划_Roguelite路线图》粒度）；每个子版本开工时按项目惯例
> 另建 `vX.Y.Z操作.md` 记录当日细节。完成后勾验收清单、回写路线图 M6 条目。
> 关联文档：[项目长期计划_Roguelite路线图](项目长期计划_Roguelite路线图.md) ｜
> [数值与角色设计任务书](数值与角色设计任务书.md) §5.3 ｜ [开发必读_核心信息整合](开发必读_核心信息整合.md)

---

## 1. 现状基线（2026-08-31 已核实）

| # | 主题 | 现状 | 关键事实 |
|---|---|---|---|
| A | 槽位 UI | 能用但糙 | `SlotBarUI` 四主槽（F/Q/R/C）+背包 3 格全运行时构建；全部槽共用一张 `SlotFrame.jpg`；技能图标=文字+冷却文本，道具图标=纯色块；无武器槽/装备栏概念 |
| B | 金币系统 | **代码零残留** | CoinDrop / CoinHUD / ShopInteractable / PlayerStats 金币字段在合并清理（`8011025`）中整体删除，**完整实现可从 git `0788d23` 找回**；物品类掉落物当前无世界拾取载体（宝箱直接结算入包）；`TutorialSigns.cs:20`"杀怪掉金币"文案成了空话 |
| C | 地皮 | 几何+染色 | FloorTile/WallTile = v0.1 的 WhiteSquare 染色资产；无任何地皮贴图；决策③"几何染色用到 v1.0"**已到期**，现在换皮名正言顺 |
| D | 红色近战小兵 | 纯色方块 | `Enemy.prefab` = WhiteSquare 染纯红，是全部 7 个敌人变体的本体；**"奶丸"资源本机全域零命中（阻塞项）** |
| E | 狼人技能 | 纯外形（v1.0.6） | 狼人=CharacterSkin 视觉切换，无任何技能差异；旧 T 键兽化系统已删（实现同在 `0788d23`）；兽化/变身帧源素材+处理工具仍在 `E:\WeGameApps\The animation of Unity\狼人\` |
| F | 牛头马面 Boss | 无框架无素材 | 数值/外观设计已有（任务书 §5.3）；马面仅 1 张模板图+3 个动作参考 mp4，牛头零素材；阶段机 `BossPhaseController` 与弹幕 `ProjectileEmitter` 同批被删可找回 |

## 2. 批次目标与原则

**目标**：六项工作全部落地，游戏从"几何占位能玩"推进到"内容与外观成型"。

**原则（本批次共同约束）**：
1. **数值占位先行**：所有新内容数值一律走 SO 占位常量，不在本批次做平衡（用户已拍板：数值效果非首当其冲）。集中留给路线图 M5.6 平衡 pass。
2. **管线先行、占位兜底**：每项美术工作先打通"素材→导入→接入"管线并用占位资源跑通，真素材到位后替换，美术缺失不阻塞机制开发。
3. **机制与美术解耦**：Boss/小兵/地皮一律先灰盒（几何占位）验证机制，再做换装，避免美术返工。
4. 红线全遵守：见 §5 对照表；写代码任务开工时逐条过 `project-dev-rules` skill。

## 3. 版本编排与依赖

```
v1.1.0 ── T1 金币回归 + T1b 物品拾取通道（两套拾取区分）──┐
        ── T3a 技能框架改造 ──→ T2 槽位翻新（同版本收尾）
v1.1.1 ── T3b 狼人技能内容扩充（嚎叫/月刃/兽化大招）
v1.1.2 ── T4 红色小兵换装（⛔ 等奶丸资源到位，可后跳）
v1.2.0 ── T5 地皮 Tile 化 + 三主题（等素材选型拍板）
v1.3.0 ── T6-1 Boss 框架（阶段机找回+双 Boss 联动）
v1.3.1 ── T6-2 灰盒机制全招式可玩（AttackData 驱动）
v1.3.2 ── T6-3 牛头马面美术管线换装（模板重生成→序列帧）
```

| 依赖关系 | 说明 |
|---|---|
| T1 → T2 | 金币 HUD 的落位影响槽位区/左下 UI 布局，先定金币再翻新槽位 |
| T1 ↔ T1b | 金币（磁吸自动）与物品（E 交互+呼吸缩放）两条拾取通道并行不混用；散落物理可共享基类，分叉在拾取判定 |
| T3a → T2 | 槽位图标内容取决于技能数据模型（SkillData 加图标位），框架先改 |
| T3a → T6 | SkillExecutor 多态化后，Boss 招式/新技能同架构受益 |
| T4 ⛔ 外部输入 | 奶丸资源未找到，需用户提供并约定落位路径 |
| T5 ⛔ 决策待拍板 | 地皮素材来源三方案选一（§4-T5），风格统一是前置决策 |
| T6 内部三步串行 | 框架→灰盒→美术，任何一步不因美术缺失而停摆 |

## 4. 任务详情

### T1 · 金币系统回归（v1.1.0，预计 1~2 个工作日）

**方案：从 git `0788d23` 整体找回，按 v1.0.8 架构适配。**

找回清单（删除前路径）：
| 组件 | 原路径 | 职责 |
|---|---|---|
| `CoinDrop` | `Assets/Scripts/now_use/Common/CoinDrop.cs` | 掉落物：程序生成 32px 金币 Sprite、弹跳散落→磁吸→触碰入账 |
| `PlayerStats` 金币字段区 | `Assets/Scripts/now_use/Player/PlayerStats.cs` | `Coins/OnCoinsChanged/AddCoins/TrySpendCoins` |
| `CoinHUD` | `Assets/Scripts/now_use/UI/CoinHUD.cs` | 纯代码金币图标+数字，订阅 `OnCoinsChanged` |
| `ShopInteractable` | `Assets/Scripts/now_use/Dungeon/Interaction/ShopInteractable.cs` | walk-over 购买（治疗 8/护甲 10/升级券 15） |
| 掉落调用点 | 旧 `EnemyController.cs:139`、`ChestInteractable.cs:62` | 敌死/开箱掉 `CoinDrop.Spawn` |

适配点（对照现架构）：
- 掉落挂点迁移：v1.0.8 敌死反馈挂点在 **EnemyHealth.OnDeath**（AGENTS.md 第 8 条），金币掉落从旧 EnemyController 挂点迁到 EnemyHealth/EnemyController 现行死亡门面，一行式调用不侵入逻辑。
- **金币=本局货币，死亡清零**：`Respawn` 重置字段（红线：新增玩家状态字段必须进 Respawn）。跨局货币属 M4.2"魂"领域，不混淆。
- 磁吸拾取=Trigger 逻辑事件，合规；拾取查询 NonAlloc+静态缓冲（零 GC 红线）；同帧多枚拾取音效 `PlayOneShot`（R10），拾取音接 `AudioManager.PlaySFX("coin")` 占位表。
- `CoinHUD` 与 `PlayerUI`（左下三属性条）、`SlotBarUI`（右下槽位）布局协调：金币数字建议放左下 HP 条下沿或右上（实施时截图定）。
- 顺带：`TutorialSigns.cs:20` 金币文案从"空话"转真。

**金币美术缺失的处理**：旧版 CoinDrop 本身就是程序生成金币贴图，作为占位**立即可用**；真金币贴图并入 T5 素材选型（若采用外部素材包则顺带取，若自产管线则后补）。**美术不阻塞本任务。**

验收：
- [ ] 杀敌/开箱掉金币，磁吸拾取，HUD 实时更新
- [ ] 商店房能购买、余额不足有反馈；金币死亡清零、重开局归零
- [ ] `TutorialSigns` 文案与实际玩法一致
- [ ] EditMode 三测试全绿 + 新增建议：钱包 Add/Spend/清零 EditMode 测试

### T1b · 物品列表掉落物：拾取通道区分 + 呼吸缩放（v1.1.0）

**定位**：拾取体系拆成两条明确通道，与金币（T1）彻底区分：

| 通道 | 对象 | 拾取方式 | 表现 |
|---|---|---|---|
| 金币通道 | `CoinDrop` | **磁吸自动拾取**（靠近吸附入账，无需按键），适合大量散落 | 弹跳散落 → 磁吸飞向玩家 |
| 物品通道 | `ItemDrop`（新） | **E 键交互拾取**（复用 `Interactable` 基类，与 ChestInteractable/SupplyInteractable 同族） | 落地静止 + **呼吸缩放** + 近距按键提示 |

**ItemDrop 设计**：
- 通用物品掉落载体：持有物品引用（`ConsumableData`，未来扩 `WeaponData`）与数量；来源接 LootTable（宝箱既有）与敌人掉落（M5.1 武器掉落预留挂点）。
- E 拾取入 `ItemInventory`（ActiveSlot / Backpack[3]）；背包满 → 飘字/音效反馈、不掉包（复用 ShopInteractable 余额不足反馈模式）。
- 与金币共享散落物理基类（弹出散布+落地），**分叉点在拾取判定**：金币=磁吸 Trigger 吸收；物品=Interactable 半径+E 键。

**呼吸缩放采用的技术（明确写入）**：
- **主选：DOTween**（项目已装，DOTween.Modules 程序集，零新增依赖）：
  `transform.DOScale(baseScale * 呼吸系数, 半周期).SetEase(Ease.InOutSine).SetLoops(-1, LoopType.Yoyo)`
  - **基准 scale 持久缓存**：落地瞬间记录 baseScale，呼吸永远从基准出发（同 R9 变色协程"基线缓存"思路）——防止与 720px→7.2 校正 scale、生成落地弹跳等其他 scale 动画互相覆盖跳变。
  - **Tween 互斥**：落地弹跳（DOJump/DOPunchScale）完成后才启动呼吸；启动前先 Kill 旧 Tween（同协程互斥红线）。
  - **OnDisable/随层清理时 Kill**：掉落物随层清理是既有行为，DOTween 针对失效对象的 Tween 会悬挂泄漏；池化复用则回收时重置基准再重启。
  - 半径提示（加分项）：同一 DOTween 思路做头顶物品名/E 键提示淡入，文本照 `CreateText` 运行时构建模式。
- 备选（不推荐为默认）：纯协程 `Mathf.Sin(t × 频率)` 每帧写 localScale——零依赖但常驻协程+手写互斥/生命周期，项目已有 DOTween 惯例，无理由弃用。

验收：
- [ ] 金币磁吸自动入账、物品必须按 E 拾取，两通道互不误触
- [ ] 物品掉落物呼吸缩放平滑：落地弹跳结束后启动、无 scale 跳变、基准不受其他动画污染
- [ ] 背包满时按 E 有反馈且物品不消失
- [ ] 换层清理/scene_load 重载后无悬挂 Tween（反复进出 Play 验证）

### T2 · 装备槽与技能槽翻新（v1.1.0，随 T1/T3a 收尾）

**范围拍板（建议）**：本次"装备槽"= 新增**武器槽**（展示当前武器，为 M5.1 武器掉落/切换预留）+ 道具主动槽强化；**不做防具栏**（M5.1 武器系统落地后再扩展，避免空转 UI）。

改动挂点（全在 `Assets/Scripts/now_use/Player/SlotBarUI.cs` + `Skill/SkillExecutor.cs`）：
1. **数据模型**：槽位推送结构（`PushSlotUI`，SkillExecutor.cs:169）扩展图标字段；`SkillData` SO 增加图标引用或约定路径 `Resources/Art/UI/Skills/{skillId}`（推荐路径约定，缺图回退现有文字方案——沿用 SlotFrame 缺图回退惯例）。
2. **视觉翻新**：
   - 槽位框差异化：技能槽/道具槽/武器槽三种框型（或框+角标区分），保留缺图回退白描边。
   - 冷却表现升级：冷却遮罩扇形/纵向填充（现为冷却秒数文本，保留文本叠加）。
   - 就绪高亮：技能 CD 转好时边框闪一下（DOTween 一行式，注意协程/ Tween 互斥）。
   - 道具图标从纯色块 → `ConsumableData` 图标位（路径约定同理）。
3. **武器槽**：新槽位显示当前武器名/图标/按键提示；M5.1 未实装前展示静态武器，切换键位预留。

红线：运行时构建 UI 照 `ClassSelectUI.CreateText` 模式；**禁止组件塞 GameObject 构造参数**（TMP NRE）；Canvas sortingOrder 与现有层级（SlotBar 50 / Minimap 100）协调；全部改动不进场景 YAML（运行时构建体系本身就是为不改场景）。

验收：
- [ ] 三类槽位视觉可辨，技能/道具有图标（无图自动回退不报错）
- [ ] 冷却有遮罩表现，转好有提示；武器槽展示当前武器
- [ ] 无新增场景依赖，scene_load 重载后 UI 正常自举

### T3 · 狼人技能槽深化（T3a 框架 v1.1.0 → T3b 内容 v1.1.1）

**用户意图**：狼人除"特定变身"外还要更多技能；**数值平衡明确不在本批次**。

**T3a · 框架改造（一次性架构投资）**：
- 现状：`SkillExecutor.Execute()` 是 switch 硬分派（SkillExecutor.cs:210-230），新增技能类型必改主体；仅战士实装，Archer/Mage 返回 null+Warning（SkillCatalog.cs:35）；六武器共用占位旋风斩。
- 改造：switch 分派演进为**多态 SkillEffect**（每技能一个效果实现，SO 关联效果类型），保持 **AttackData 同源原则不变**（预警=判定=视觉同一份 SO）；`SkillCatalog` 补 Archer/Mage 兜底目录（哪怕先复用战士分支占位，消除 null+Warning）。
- 收益：T3b 狼人技能、T6 Boss 招式、后续职业全部只加类不动主体。

**T3b · 狼人技能内容（占位数值）**：
- 机制：**角色专属技能分支**——按 `RunStateCarrier.ChosenCharacter` 在 `ClassData.SkillBranches` 装配处（SkillExecutor.AssembleSlots）注入狼人分支表，职业+外形两维正交，不动 ClassData 本体。
- 候选技能（占位常量，后续平衡 pass 调）：
  | 槽位 | 技能 | 说明 | 素材状态 |
  |---|---|---|---|
  | Q 大招 | 兽化 | 临时变身：体型/攻速/移速增益+专属外观，倒计时还原 | 兽化帧源在 `The animation of Unity\狼人\狼人兽化形态动画帧组`（⚠️ 项目内 `Assets/Art/Werewolf\` 旧帧目录 8-31 正被清理，需从源工具目录重走导入管线） |
  | R 武器技能 | 月刃 | 挥出月牙形斩击/回旋刃 | 源素材 `月刃_武器透明.png` 已有 |
  | F 小技能 | 嚎叫 | 范围 debuff（敌减速/恐惧）或自身 buff，走 `BuffManager` | 无需新帧（光环特效可用粒子/DOTween 占位） |
  | 普攻强化 | 撕裂爪击 | 顺劈/流血词条（占位） | 复用现有攻击帧 |
- 红线重点：兽化状态字段（计时/buff/体型）**必须进 Respawn 重置**；`GetComponentsInChildren` 排除子树（狼人视觉误藏教训）；变身视觉走 `FrameAnimator` 通道，与职业武器视觉（WeaponVisualBuilder）叠加规则先想清楚再动手；Buff 订阅/退订配对。

验收：
- [ ] 新增技能不改 SkillExecutor 主体即接入（多态化生效）
- [ ] 狼人外形下 F/Q/R 三槽为狼人专属技能，战士外形不受影响
- [ ] 兽化大招有视觉变化、有倒计时、死亡/重生后状态干净
- [ ] Archer/Mage 选职不再出 null Warning（兜底目录就位）

### T4 · 红色基础近战小兵换装（v1.1.2，⛔ 阻塞：奶丸资源）

**阻塞项（需用户提供）**："奶丸"的游戏资源在本机（含 `E:\WeGameApps\` 全域）文件名与文档内容**零命中**。开工前置条件：
1. 用户提供资源并落位（建议源目录 `E:\WeGameApps\The animation of Unity\奶丸\`，项目内落 `Assets/Resources/Art/Characters/RedMelee/`，与 Warrior/Werewolf 惯例一致）；
2. 资源需含：待机/行走（左右两套或单套镜像）、攻击前摇-命中-收招（如无攻击帧则攻击时回退 tint/缩放表现）。

接入管线（复用狼人三工具：remove_bg / mirror_frames / resize+pixel_outline）：
- 导入显式 `spriteMode=Single`（R3：默认 Multiple 自动切片=透明碎片）；720px 序列帧默认 7.2 世界单位，scale/PPU 校正（R6）。
- 敌人侧动画接入：Enemy.prefab 现为单 SpriteRenderer，需加帧动画组件（复用 `FrameAnimator` 敌人化或轻量帧循环）；**组件必须进 prefab 本体**（R1：实例上 component_add 会被 scene_load 丢弃，走 `prefab_apply_overrides`）。
- ⚠️ Enemy.prefab 是 7 个变体的 Variant 本体：换帧后变体仍靠 tint 区分（紫/红等），**先只换本体帧组+tint 保留**，各变体独立帧组留长线。
- 攻击表现与 R8 三态协调：攻击帧阶段（Windup/Active/Recovery）与预警框同源自 AttackData，帧时长对齐 SO 数值。

验收：
- [ ] 红色小兵有待机/行走/攻击动画，720px 比例正确（不巨型不碎片）
- [ ] scene_load 重载后动画组件仍在（prefab 本体生效）
- [ ] 其余 6 个变体外观不回归纯白方块（tint 机制保留）

### T5 · 地皮美术（v1.2.0，⛔ 待选型拍板）

**现状**：`Assets/Tilemaps/v0_1/FloorTile|WallTile.asset` 引 WhiteSquare 染色；`DungeonBuilder` 的 `floorTile/wallTile` 注入点+`FloorTheme.floorTint` 主题染色已就位——**换皮只需换 Tile 资产，生成/着色架构零改动**。

**选型三方案（需先拍板，影响 T4/T6 美术路线）**：
| 方案 | 来源 | 优点 | 风险 |
|---|---|---|---|
| A. 免费 CC0 包 | Kenney Roguelike/Tiny Dungeon、0x72 Dungeon Tileset II 等 | 即拿即用、零成本 | **像素风与现有手绘描边角色割裂**，拼贴感 |
| B. 手绘风采购 | itch.io 等手绘 dungeon tileset（付费） | 风格接近、快 | 版权核对、尺寸/色调未必合，仍可能与角色不统一 |
| C. 自产管线 | 复用角色模板生成管线（AI 模板+三工具）自产手绘 tile | **风格统一可控**；与牛头/马面/小兵同管线，边际成本低 | 工作量最大、周期长 |
| **推荐** | **C 为主**（角色手绘描边风已确立，Boss/小兵都走自产，tile 同管线最不违和）；**A 可作过渡占位**（若想快速见效，先 Kenney 后自产替换） | | |

**落地要点**：
- 三主题（1-3/4-6/7-9 层）各一套 Tile 组：`FloorTheme` 从单一 floorTint 扩展为 Tile 组引用（注入点不变）。
- Tile atlas 导入：与 R3 相反，tile 图集**恰需 `spriteMode=Multiple`+固定网格切片**（R3 针对的是单帧角色图误切），切片尺寸与 Tilemap cell（1 单位）对齐，PPU 按图集分辨率换算。
- 门洞 `OpenDoorTile`、墙 Tile 同套替换，保持 `DungeonBuilder` 接口不动。
- 金币贴图若走方案 A 顺带解决；方案 C 则金币并入自产清单。

验收：
- [ ] 三主题地皮肉眼可辨，风格与角色统一（拍板后对照模板评审）
- [ ] Tilemap 性能无回归（渲染路径不变，仅资产替换）
- [ ] `DungeonGeneratorTests` / `MissingScriptScanTests` 全绿

### T6 · 牛头马面双 Boss（v1.3.0~v1.3.2，批次最大项）

**既有设计（数值任务书 §5.3，直接引用）**：牛头 10000HP 高攻低速近战 / 马面 8000HP 高速低攻弹幕；一方死亡→另一方狂暴（伤害×1.5、攻速+40%、红光涨大外观）；**大招前摇预警特效是硬需求，不可降级为程序员美术**；剪影强对比（牛头壮硕重型 vs 马面纤细敏捷）。

**T6-1 框架（v1.3.0）**：
- 找回并适配 `BossPhaseController`（git `0788d23`，HP≤50% 切 P2：换招式池/冷却×0.7/染红脉冲）——挂现行 `EnemyHealth.OnHealthChanged`。
- 双 Boss 联动：新 `DualBossLink` 组件，互订阅 `OnDeath`→触发幸存者狂暴 Buff（数值占位走 BuffManager，外观红光/涨大走 DOTween+scale）。
- Boss 房已就位（`SelectBossRoom` 2×2、第 9 层 HP×1.5+"终焉"强化）：双生成只改 `EnemyTable_Boss`/Spawn 配置。
- 弹幕能力找回 `ProjectileEmitter`/`Projectile`（同批删除）适配现物理规则（Trigger 逻辑事件/挡弹销毁口径）。

**T6-2 灰盒机制（v1.3.1，几何占位全招式可玩）**：
| Boss | P1（100%~50%） | P2（≤50%）追加 |
|---|---|---|
| 牛头（近战重） | 冲锋（预警线+R8 远程预警 LOS 门控）、横扫（Box 预警=判定同源）、跺地 AOE（圆预警+震屏） | 召唤小怪、狂暴冲锋连段 |
| 马面（敏捷远程） | 突刺连击、锁链拉拽（直线牵引+预警线）、索命弹幕（ProjectileEmitter 扇形） | 瞬移背刺、弹幕密度翻倍 |
- 每招式=独立 `AttackData` SO（**预警=判定=武器视觉同一份**，判定几何唯一真源 `WeaponHitbox.CurrentBoxGeometry`）；三态攻击 Windup→Active→Recovery 全走现有框架。
- 招式选择器：阶段化招式池+权重冷却（BossPhaseController 结构天然支持）。

**T6-3 美术管线（v1.3.2）**：
- 用户已明示**模板几乎需重新生成**：马面有模板图+3 个动作参考 mp4 作基底；牛头从零。**模板先行评审**（剪影对比过任务书 §5.3 标准后再产序列帧，防返工）。
- 管线：模板图 → 动作参考视频 → 抽帧 → 三工具处理（去底/镜像/描边）→ 导入（spriteMode=Single、720px→7.2 校正）。
- 动作组清单（每 Boss 6~8 组）：待机/行走/每招式前摇-命中-收招/狂暴态/死亡消散。

验收：
- [ ] 双 Boss 同场，阶段切换（半血换池）可感知；一方死另一方狂暴（数值+外观）
- [ ] 每个招式有前摇预警且预警范围=判定范围（同源抽检）
- [ ] 击杀双 Boss 走现行 Boss 房结算（奖励宝箱+传送门）
- [ ] 灰盒阶段（T6-2 完成）即可玩性验收一次，美术完成后二次验收
- [ ] 建议新增 EditMode：阶段阈值切换测试（伪 Health 驱动 BossPhaseController）

## 5. 红线对照表（写代码前逐条过 skill，此处仅预警高命中项）

| 任务 | 高命中红线 |
|---|---|
| T1 金币 | 玩家新字段进 Respawn；Trigger 仅逻辑事件；NonAlloc 零 GC；同帧多拾取 PlayOneShot |
| T1b 物品拾取 | DOTween 互斥 + 基准 scale 持久缓存（同 R9 思路）；掉落物随层清理时 Kill Tween 防悬挂；Trigger 仅逻辑事件；运行时文本 CreateText 模式 |
| T2 槽位 | 运行时 UI 构建 CreateText 模式；禁组件入构造参数；#if UNITY_EDITOR 便利代码资源化 |
| T3 狼人 | AttackData 同源；新状态字段进 Respawn；GetComponentsInChildren 排除子树；订阅退订配对；协程/Tween 互斥 |
| T4 小兵 | spriteMode=Single；720px→7.2 校正；组件进 prefab 本体（R1）；R8 三态对齐 |
| T5 地皮 | tile 图集用 Multiple 网格切片（勿套用 R3 的 Single）；#if UNITY_EDITOR 资源化；注入点不动则零场景手术 |
| T6 Boss | AttackData 同源+CurrentBoxGeometry 唯一真源；R8 三态+LOS 门控；YAML 手术规则（若动 prefab/场景）；敌死 OnDisable 用 IsDead 区分 |

## 6. 风险与待决事项

| # | 事项 | 状态 | 处理 |
|---|---|---|---|
| 1 | **奶丸资源缺失** | ⛔ 阻塞 T4 | 用户提供资源+落位路径后开工；T4 可与其他任务换序（v1.1.2 槽位留给最先就绪的美术项） |
| 2 | **地皮选型未拍板** | ⛔ 阻塞 T5 | 三方案（推荐 C 自产为主）拍板后开工；金币贴图随选型一并解决 |
| 3 | 素材目录正在变动 | ⚠️ 观察 | 8-31 调查期间 `Assets/Art/Werewolf\` 帧目录被实时清空、`Assets/Art/Characters\Warrior` 帧为删除态（git D）——疑似素材整理中。**建议先 git 提交留档当前素材变动**，源素材目录（The animation of Unity）无版本控制，整理前备份 |
| 4 | v0.5 找回代码的架构漂移 | ⚠️ 中 | CoinDrop/BossPhaseController/ProjectileEmitter 删除时架构已演进（敌死挂点/HUD 体系），找回≠直接粘贴，逐个按适配点改造 |
| 5 | 美术风格统一 | ⚠️ 决策 | 小兵/地皮/Boss/金币四处素材同期引入，**风格基调建议先拍板再批量开工**（否则拼贴感风险） |
| 6 | 数值占位口径 | ✅ 已拍板 | 本批次不做平衡，占位常量集中 SO，M5.6 平衡 pass 统一收口 |

## 7. 建议汇总（给用户的排序理由）

1. **金币最先做**：代码全在 git 里，1~2 天量级，立刻恢复"打怪→捡钱→消费"闭环；占位贴图现成（程序生成），美术零阻塞。
2. **技能框架（T3a）是本批次最值的架构投资**：一次多态化改造，狼人技能/槽位图标/Boss 招式/后续职业四处受益；拖到 Boss 开工后再改会重复动刀。
3. **槽位翻新放在金币+框架之后收尾**：它的内容（金币 HUD 落位、技能图标数据）依赖前两者，先做会返工。
4. **美术类全部"管线先行"**：奶丸小兵/地皮/牛头马面共同的风险是素材时序不可控——机制与灰盒先行，素材到位即插即拔。
5. **Boss 严格三步走**：框架→灰盒→美术。最大最不确定的项目拆开推进，灰盒可玩即可验收一半价值。
6. **开工第一件事**：拍板 §6 两个 ⛔（奶丸资源、地皮选型）+ 提交当前素材目录变动留档。

---

> 维护约定：各子版本完成后勾验收清单、建 `vX.Y.Z_主题.md` 落盘文档；教训回写《开发必读_核心信息整合.md》；批次完成回写《项目长期计划_Roguelite路线图》M6 总账。
