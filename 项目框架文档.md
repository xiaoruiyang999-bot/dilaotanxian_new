# 项目框架文档

> 目标架构：v1.2  
> 更新：2026-09-08  
> 说明：本文描述迁移目标，不伪称当前代码已完成。

## 1. 产品结构

本项目采用“二维微观战斗 + 一维宏观推进”：

- 宏观：纯数据 DungeonGraph，DAG 从左向右分叉、汇聚并最终进入 Boss。
- 微观：RoomManager 只运行当前节点对应的横向俯视房间。
- 选择：PlayableCharacterDefinition 合并角色与职业功能。
- 表现：角色和武器一体化 Sprite，Unity Animator 驱动；Attack Hitbox 独立。

完整设计见《计划书/v1.2.0_俯视横向单向DAG地牢_GDD与开发计划.md》。

## 2. 六个模块

| 模块 | 核心职责 | 不负责 |
|---|---|---|
| Bootstrap / GameFlow | 初始化、Run 生命周期、场景/房间流转 | 战斗公式、UI 排版 |
| DungeonGraph | DAG 生成、节点状态、Seed、可达性 | 实际房间 GameObject |
| RoomRuntime | RoomPlan、房间阶段、Bounds、生成/卸载 | 路线选择规则 |
| Combat | 输入意图、攻击状态、Hitbox、伤害、状态、敌人行为 | 地图 UI |
| Build / Economy | 职业角色、武器、遗物、奖励、商店、Run Build | 动画播放 |
| Presentation / UI | Animator、镜头、音频、特效、HUD、地图 | 修改玩法真值 |

依赖方向以稳定数据和接口为中心。UI、Animator、音效只消费状态，不反向成为游戏逻辑真源。

## 3. 关键数据

### PlayableCharacterDefinition

合并旧角色和职业：ID、显示名、基础属性、可用武器类型、职业资源、技能集合、Animator/Override 集合、专属奖励标签。

### DungeonGraph / DungeonNode

纯 C# 可序列化数据。节点包含 Generated、Discovered、Visited、Completed；Active 由当前 RoomSession 表示。

### RoomPlan

最终房间几何、入口/出口、阶段、SpawnCells、Bounds、模板标签和生成 Seed。先验证，后绘制。

### AttackDefinition

前摇、有效帧、后摇、方向规则、Hitbox 几何、位移、伤害倍率、反馈 ID。动画与判定共用。

### RunState

PlayableCharacterId、当前节点、已完成路径、生命/资源、武器、遗物、金币、随机状态和 SchemaVersion。

## 4. 运行流程

Bootstrap → 职业角色选择 → 创建 RunState → 生成 DungeonGraph → 地图选择可达节点 → 创建 RoomSession → 生成/加载 RoomPlan → 绑定镜头 Bounds → 完成目标 → 写回节点与奖励 → 卸载 RoomSession → 下一节点/Boss/结算。

## 5. 场景建议

- Bootstrap：持久服务和唯一入口。
- Prep/CharacterSelect：职业角色选择和 Run 初始化。
- DungeonShell：HUD、地图、RoomManager、Camera 服务。
- Room 内容：Prefab 或附加场景按节点加载。

Vertical Slice 优先使用 DungeonShell + 选中房间生成，避免预建整层房间。

## 6. 当前迁移边界

现有 now_use 代码仍包含旧 Class/Character、四方向网格 DungeonGenerator、WeaponPivot 与 FrameAnimator。它们在对应新模块接管并通过回归测试前保留，之后按引用链退役。

迁移次序：

1. 职业角色数据合并。
2. 攻击方向灰盒定案。
3. 狼人 Animator/Hitbox。
4. 横向 RoomPlan 与镜头。
5. DAG 数据和地图。
6. 节点—房间—奖励闭环。
7. 内容扩展和旧链清理。

## 7. 性能与测试

- 纯 C# 规则可 EditMode 测试。
- 高频物理查询 NonAlloc，引用缓存。
- 只激活当前房间。
- 动画与 Sprite 按职业角色和当前武器加载。
- 同 Seed 的 DAG、RoomPlan 与奖励入口可复现。
- Save 使用版本化 DTO，不直接序列化场景组件。

