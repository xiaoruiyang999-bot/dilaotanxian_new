# 地牢探险 · 项目框架文档

> 本文档归纳项目截至 **v0.4 系列（含 v0.4.5.2 / v0.4.6 迭代）** 的代码框架，介绍各模块的内容与作用。
> **后续版本更迭时，请同步更新本文档**（更新方法见文末「维护与更新记录」）。
>
> 最近更新：v0.4.6（2026-07）

---

## 1. 项目总览

- **类型**：2D 俯视角地牢探险（Unity 6，URP 2D）
- **美术策略**：程序员美术（纯色几何图形 + Color 染色，"形状+颜色+尺寸"三重编码），不设 Animator，表现全靠代码驱动（DOTween / 变色 / 协程），未来可无损替换为正式美术
- **技术底座**：Input System（新输入系统）、DOTween、2D 物理（Rigidbody2D / Physics2D）、Tilemap、uGUI、ScriptableObject 数据驱动
- **核心理念**（源自 v0.1 技术选型）：现在搭好的**结构**（Tilemap 层级、碰撞、对象关系、组件架构）= 永久资产；美术 = 临时占位符

### 设计原则（贯穿全项目）

| 原则 | 体现 |
|---|---|
| 门面模式（Facade） | `PlayerController` / `EnemyController` 聚合全部子组件，`[RequireComponent]` 强制依赖 |
| 事件驱动解耦 | `System.Action` 事件（OnHealthChanged / OnDeath / OnTakeDamage / OnStatsChanged / OnHit），UI/AI 订阅而非轮询 |
| 接口契约 | `IDamageable` 是全项目统一伤害入口 |
| 数据驱动 | `AttackData`（ScriptableObject）是攻击数值唯一数据源 |
| 职责严格分层 | 输入层 / 决策层 / 执行层 / 判定层 / 表现层分离（见第 3 节） |
| 动画与判定解耦 | DOTween 挥动纯视觉，伤害由 WeaponHitbox 按武器真实姿态逐帧物理检测 |
| 对称架构 | 玩家侧与敌人侧各一套 Controller/Stats/Health/Combat 组件，接口与 UI 共用 |

---

## 2. 目录结构与版本约定

```
Assets/
├── Scripts/
│   ├── now_use/        ★ 当前版本实际在用的全部脚本（v0.4.6 起设立，配 README 清单）
│   ├── Framework/      跨版本公共系统层（v0.5+ 规划，当前为占位）
│   ├── v0_1/ ~ v0_4/   历史版本归档（v0_1、v0_4 目前为空目录；v0_2、v0_3 保留弃用脚本不删除）
├── Scenes/             场景按版本命名：v0_X_名称.unity，每版本从上一场景复制演进
├── Prefabs/            Player.prefab、Enemy.prefab 等
├── Palettes/           Tilemap 调色板
└── PlayerInputActions.inputactions   输入配置（Action Map "Player"：Move / Attack）
```

**归档规则**（now_use/README.md 约定）：`now_use/` 只放当前版本在用脚本；被替换的旧脚本留在原版本文件夹归档，不删除（防 Missing Script、保留演进痕迹）。

---

## 3. 框架分层与数据流

```
输入层    PlayerInputActions → PlayerController（输入分发）/ PlayerAimController（瞄准）
决策层    EnemyAI（四态状态机，只决策不执行）
执行层    PlayerCombat / EnemyCombat（三阶段攻击状态机）· EnemyController（移动/朝向）
判定层    WeaponHitbox（武器矩形逐帧 OverlapBox，伤害结算核心）
数据层    AttackData（SO，攻击配置）· PlayerStats / EnemyStats（属性）· Health / EnemyHealth（生命）
表现层    WeaponController（武器朝向/锁定）· WeaponAnimator（DOTween 挥动，纯视觉）
          AttackIndicator（范围预警，纯视觉）· 受击闪白/死亡变灰
UI 层     PlayerUI（屏幕固定面板）· PlayerWorldStatusBar / WorldSpaceHealthBar（世界空间血条）
```

典型数据流（事件驱动，反向不依赖）：

```
输入 → Controller → Combat/Stats → 事件(OnHealthChanged/OnTakeDamage/OnDeath/OnStatsChanged) → UI / AI / 反馈表现
```

---

## 4. 各系统详解

### 4.1 地图系统（v0.1 建立）

- **结构**：Grid → Floor + Walls 双 Tilemap 分层（sortingOrder 0/1）
- **碰撞**：Tilemap Collider 2D + Composite Collider 2D + Rigidbody2D(Static)（`usedByComposite=true`）
- **相机**：Orthographic（size=6）+ `CameraFollow`（LateUpdate + SmoothDamp 平滑跟随）
- 选用 Tilemap 的原因：后续随机地牢生成的唯一可扩展方案（v0.5+ 地牢生成计划依赖此结构）

### 4.2 玩家系统（v0.3 组件化，v0.4.5.2 攻击框架重构）

玩家对象 `Player`（Tag="Player"），由以下组件构成：

| 组件 | 职责 |
|---|---|
| **PlayerController** | 玩家门面。强制 `InvokeCSharpEvents` 输入模式，通过 `onActionTriggered` 分发输入：Move → 存向量，FixedUpdate 以 `rb.linearVelocity` 移动；Attack → `combat.TryAttack()`。死亡后清零速度、Sprite 变灰 |
| **PlayerAimController** | 瞄准输入层，每帧输出归一化 `AimDirection`。当前仅实现鼠标；Gamepad / Auto（自动索敌）为 v0.5 预留 |
| **PlayerStats** | 属性：maxHP=5、maxArmor=5、moveSpeed=5。**护甲优先吸收伤害 + 脱战 3 秒后 0.5/s 自动回甲** |
| **Health** | 通用生命值（实现 `IDamageable`）。受伤时先走 PlayerStats 护甲吸收，剩余扣 HP；HP≤0 触发 `OnDeath`。HP 不自动恢复 |
| **PlayerCombat** | 攻击三阶段状态机（Windup 前摇锁方向+黄色预警 → Active 驱动命中判定与挥动 → Recovery 解锁）。不读鼠标、不管武器视觉，职责边界明确 |

### 4.3 武器 / 攻击框架（Player 与 Enemy 共用，v0.4.5.2 重构确立）

这是当前攻击系统的核心，**动画与判定完全解耦**：

| 组件 | 职责 |
|---|---|
| **AttackData**（ScriptableObject） | 全项目攻击数值唯一数据源：形状（Fan/Circle/Box）、三阶段时长、范围/角度/伤害/冷却/目标层、动画类型（Arc/FullCircle/Thrust/Spin/Throw）、DOTween Ease、命中时刻比例。Box 与 Thrust/Spin/Throw 为预留 |
| **WeaponController** | WeaponPivot 与 WeaponSprite 的唯一管理者：普通朝向、攻击方向锁定/解锁、起止角度计算、武器视觉刷新（Sprite 长度=攻击范围） |
| **WeaponAnimator** | ⚠️ 名字有误导性，实为 **纯 DOTween 武器旋转播放器**（非 Unity Animator）。`DOLocalRotate` 挥动 + `DOVirtual.DelayedCall` 在命中时刻回调。攻击期间 WeaponPivot 姿态的唯一修改入口 |
| **WeaponHitbox** | **伤害结算核心（v0.4.6 起）**。由 Combat 驱动 BeginSwing/Tick/EndSwing：Active 阶段每帧沿武器真实姿态做 `Physics2D.OverlapBox`（零分配缓冲），HashSet 保证每次挥击每目标只结算一次，命中调 `IDamageable.TakeDamage()`。`OnHit` 事件为击退/特效/HitStop 等反馈预留 |
| **AttackIndicator** | 攻击范围预警纯视觉层：运行时生成扇形/圆形 Mesh，Show 时脱离父物体保持世界位置，Windup 黄色 → Active 红色 |
| **AttackQuery** | 旧式瞬时扇形判定工具。**v0.4.6 起已无调用方**，保留在 prefab 上留待 v0.5 技能系统，属"待用"状态 |

### 4.4 敌人系统（v0.4 建立，与玩家侧对称）

敌人对象 `Enemy`（Layer="Enemy"），五件套 + AI：

| 组件 | 职责 |
|---|---|
| **EnemyController** | 敌人门面。`MoveTowards / StopMoving / FaceTowards`（旋转整个 transform，武器靠父子关系跟随）；受伤闪白协程；死亡→变灰、禁用 AI/Combat/Collider、0.5s 后销毁 |
| **EnemyStats** | 属性：maxHP / moveSpeed / detectionRange=5 / losePlayerRange=8（OnValidate 保证 lose>detect）。原攻击三字段已标 `[Obsolete]`，迁移至 AttackData |
| **EnemyHealth** | 敌人生命（实现 `IDamageable`，无护甲）。比玩家 Health 多 **`OnTakeDamage` 事件——仇恨联动关键**：被打立即通知 AI 切 Chase |
| **EnemyAI** | 决策状态机 `Patrol → Chase → Attack → ReturnToPatrol`，**只决策不执行攻击**。巡逻用 PatrolSystem 随机点；距离 ≤ detectionRange 追击，> losePlayerRange 返回；进攻击范围且冷却完毕交 EnemyCombat |
| **EnemyCombat** | 攻击状态机（三阶段 + 冷却），结构与 PlayerCombat 对称。差异：攻击方向取 `transform.right`、WeaponController 不叠加旋转、结束重置朝向并进入冷却 |
| **TrainingDummy** | 伤害测试木桩：不移动不攻击（AI/Combat 挂载但禁用），被击闪烁、死亡变灰 3 秒后自动复活。EnemyController 对其特判跳过闪烁/死亡流程 |

**敌人 AI 完整链路**：

```
Patrol（随机点巡逻）
  │  距离 ≤ DetectionRange，或巡逻中被打（OnTakeDamage 仇恨）
  ▼
Chase（追击 + SetTarget）
  │  进入攻击范围且 CanAttack
  ▼
Attack（EnemyCombat 三阶段：黄预警→红预警+挥击→冷却）
  │  冷却恢复回 Chase；距离 > LosePlayerRange
  ▼
ReturnToPatrol（回原点，中途可再发现玩家）→ Patrol
```

### 4.5 伤害流转

- 统一入口：**`IDamageable.TakeDamage(float)`**；实现者：`Health`（玩家）与 `EnemyHealth`（敌人/木桩）
- 伤害来源：`WeaponHitbox.Tick()` 直接调用，数值取自 `AttackData.AttackDamage`
- 玩家侧多一层：`Health.TakeDamage → PlayerStats.AbsorbDamageWithArmor（护甲优先扣）→ 剩余扣 HP`，同时刷新 UI、重置脱战回甲计时
- ⚠️ 已知重复点：`Health` 与 `EnemyHealth` 是平行实现的两个类（敌人版多 OnTakeDamage、无护甲），未来可在 Framework 层合并

### 4.6 UI 系统（事件驱动，三条独立 UI）

| UI | 空间 | 数据源 | 说明 |
|---|---|---|---|
| **PlayerUI** | Screen Space（屏幕左下固定面板） | Health + PlayerStats | HP 条 + 护甲条（`Image.fillAmount`），带引用自动恢复机制 |
| **PlayerWorldStatusBar** | World Space（玩家头顶） | Health + PlayerStats | 上蓝护甲 + 下红 HP 双条（v0.4.6 新增） |
| **WorldSpaceHealthBar** | World Space（敌人头顶） | EnemyHealth（兼容 Health） | 红色血条 |

两个世界空间条共用一套模式：运行时动态创建 World Space Canvas 挂全局 `WorldUIRoot`，记录去旋转锚点偏移，LateUpdate **只平移不旋转**，OnDestroy 销毁自建 Canvas。

### 4.7 公共框架层 `Scripts/Framework/`（占位，未接入）

v0.4 建立的空壳，为 v0.5+ 规划的公共系统，**当前零引用**：

- **DamageSystem**（静态类）：规划中的统一伤害结算层（护甲穿透/暴击/元素/飘字），当前 ApplyDamage 仅透传
- **DetectionSystem**（静态类）：距离/扇形视野判定已有基础实现，预留视线遮挡 Raycast、听觉。EnemyAI 目前自行距离判定，未走这里
- **PatrolSystem**（静态类，now_use 内）：随机巡逻点生成，EnemyAI 在用；预留 v0.5+ 路径点巡逻

### 4.8 历史归档（不在用，保留不删除）

| 位置 | 内容 | 现状 |
|---|---|---|
| `v0_2/` | CharacterInput、WarriorCharacter | v0.2 一体式设计，已被组件化拆分取代（输入方案也从 Unity Event 演进为 C# Events） |
| `v0_3/` | Enemy、WarriorAttack、IAttack | 已弃用；后两者带 `[Obsolete]`，仅作防 Missing Script 占位 |

---

## 5. 版本演进脉络

| 版本 | 主题 | 框架贡献 |
|---|---|---|
| v0.1 | 初始小屋 | Tilemap 双层地图 + Composite 碰撞、相机、目录/场景命名约定 |
| v0.2 | 战士基础 | Input System 接入、输入解耦思路、CameraFollow、屏幕空间属性 UI |
| v0.3 | 角色系统架构 | 单体重构为组件化五件套（Controller/Stats/Health/Combat）、IDamageable 契约、扇形攻击、护甲脱战恢复、世界空间血条、事件驱动 UI |
| v0.4 | 敌人系统 | 敌人对称五件套、AI 四态状态机、OnTakeDamage 仇恨联动、TrainingDummy、Framework 空壳层 |
| v0.4.5.2 | 攻击框架重构 | AttackData（SO 数据驱动）、WeaponController/Animator/Indicator 三件套、三阶段攻击状态机、引入 DOTween |
| v0.4.6 | 判定改革 | 伤害结算改为 WeaponHitbox 武器矩形逐帧真实接触判定（动画回归纯视觉）；PlayerWorldStatusBar；**now_use 目录约定确立** |

> 注：`项目进度报告.md`（2025-07-10）中 v0.4 标记为"待实现"，已过时——实际代码已完成并迭代至 v0.4.6，以本文档为准。

---

## 6. v0.5 预留扩展点（代码已就位）

- `SetAttackData(AttackData)`：PlayerCombat / EnemyCombat / AttackQuery 三处，技能/武器切换
- `AimInputDevice.Gamepad / Auto`：手柄瞄准与自动索敌（字段已就位）
- `AttackShape.Box`、`AttackAnimationType.Thrust/Spin/Throw`：新攻击形态
- `WeaponHitbox.OnHit` 事件：击退/特效/音效/HitStop
- `Framework/DamageSystem`、`Framework/DetectionSystem`：公共结算与侦测层填充
- `PatrolSystem`：路径点巡逻 TODO
- 对应规划见根目录 `v0.5_Dungeon系统开发计划书.md`

## 7. 已知待办 / 技术债

- `Health` 与 `EnemyHealth` 平行重复，可在 Framework 层合并
- `AttackQuery` 闲置待 v0.5 接管或移除
- `Framework/` 两系统零引用，待接入
- `Health`、`PlayerUI` 中留有调试 `Debug.Log`，发布前需清理

---

## 8. 维护与更新记录

**更新规则**：每个版本（含 x.y.z 修补版本）完成时，若涉及以下任一项，必须同步更新本文档并追加更新记录：

1. 新增/删除/重命名脚本或目录约定变更 → 更新第 2、4 节
2. 数据流或协作链路变化 → 更新第 3、5 节链路图
3. 架构决策变更（如接入 Framework 层、引入新插件） → 更新第 1、5 节
4. 预留扩展点被启用或新增 → 更新第 6、7 节

| 版本 | 日期 | 更新内容 |
|---|---|---|
| v0.4.6 | 2026-07 | 文档初版建立（归纳 v0.1 ~ v0.4.6 框架现状） |
