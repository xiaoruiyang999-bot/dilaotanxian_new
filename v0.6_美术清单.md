# v0.6 美术清单：模块化多色块美术规格

> 姊妹文档：《v0.6_职业与武器系统开发计划书.md》。
> 本清单覆盖 v0.6 全部新增/重做的美术资产。风格从 v0.5 的"单色块"升级为**模块化多色块组合**：每件资产由多个独立 SpriteRenderer 拼接，限定调色板内 2~4 色，轮廓识别优先，不设 Animator（动效仍由 DOTween/协程驱动），未来可无损替换正式美术。

---

## 一、总则与规范

### 1.1 五条硬规范

1. **模块化拼接**：所有武器/角色/敌人/展台均为"Root + 部件"层级，每个部件独立 SpriteRenderer（便于程序化换部件、染色、做受击闪白）；
2. **PPU 统一 = 32**（32 像素 = 1 Unity 单位 = 1 格）；武器基准尺寸 32×64 或 48×96，角色 32×48 左右，边缘清晰、不用复杂纹理；
3. **调色板限定**：每件资产只用 1.2 全局调色板中的颜色，主色 + 阴影色 + 高光色（+ 职业/稀有度点缀色），单件不超过 4 色；
4. **轮廓识别优先**：低分辨率/远距离下先看剪影识类别（人形=玩家、非人形=怪物、长条=武器、基座=展台），再看颜色识职业/档次；
5. **视觉与判定解耦**：武器视觉长度固定档位，**攻击范围永远由数据（AttackData/WeaponData）决定**，美术只是表现——换肤绝不影响判定。

### 1.2 全局调色板

| 用途 | 色 | 说明 |
|------|-----|------|
| 职业色 · 战士 | 红 `#C0392B` / 暗红 `#7B241C` | 战士角色、战士武器点缀 |
| 职业色 · 射手 | 绿 `#27AE60` / 暗绿 `#1E8449` | 射手角色、射手武器点缀 |
| 职业色 · 法师 | 蓝紫 `#8E44AD` / 靛蓝 `#5B2C6F` | 法师角色、法师武器点缀、法阵 |
| 金属 · 刃面 | 银灰 `#BDC3C7` / 深灰 `#7F8C8D` | 剑刃/枪头/弩臂（刃面亮、边缘暗） |
| 金属 · 装饰 | 金黄 `#F1C40F` / 暗金 `#B7950B` | 护手、宝石、稀有度点缀 |
| 皮革/木质 | 棕 `#8D6E63` / 深棕 `#5D4037` | 握柄、弓身、弩身 |
| 生命/治疗 | 翠绿 `#2ECC71` / 浅绿 `#A9DFBF` | 治疗法杖特效、HP 条 |
| 法力 | 蓝 `#3498DB` / 浅蓝 `#85C1E9` | 法力条、法力瓶/球、能量弹 |
| 体力 | 黄 `#F4D03F` | 体力条 |
| 护甲 | 钢灰 `#708090` | 护甲条、战士肩甲 |
| 敌人 · 普通 | 土橙 `#CA6F1E` | 普通怪主色 |
| 敌人 · 精英 | 紫黑 `#6C3483` + 金边 | 精英怪主色 + 稀有度描边 |
| 敌人 · Boss | 深红 `#922B21` + 暗黑 `#1B2631` | Boss 主色 + 核心 |
| 通用 | 白 `#ECF0F1` / 黑 `#17202A` | 高光、描边、UI 底 |

> 受击闪白、死亡变灰、休眠压暗等现有表现逻辑不变，直接作用于各部件 SpriteRenderer.color。

### 1.3 敌我识别规则

- **玩家**：人形（头 + 躯干两段式）+ 手中可见武器 + 职业色部件 + 头顶状态条 → 绝不与怪物混淆；
- **怪物**：**非人形剪影**（圆滚/漂浮/多边形），无武器、无人形结构；档次靠"体型 + 部件数量 + 描边"三档递进。

---

## 二、武器（6 把，全部模块化 prefab）

### 2.0 统一层级与剑类改进意见

统一层级（所有近战武器，远程武器同理换成 BowBody/String/ArrowRest 等部件名）：

```
WeaponRoot            ← 旋转/瞄准中心，pivot 必须设在"手握处"（护手略下方），不是几何中心
 ├── Blade / Head     ← 伤害部件（刃、枪头、弓臂、杖头）
 ├── Guard / Mid      ← 过渡部件（护手、弓把、杖环）
 ├── Handle           ← 握持部件（柄、杖杆）
 └── Effect(optional) ← 蓄力发光等特效挂点（SpriteRenderer.color 变亮即可）
```

**对"模块化像素剑"设计稿的 5 点改进意见**（同样适用于其余武器）：

1. **Pivot 位置**：挥砍的弧心必须在手上。WeaponRoot 的 pivot 放护手下方握柄处，Blade 向上展开——否则挥砍时武器像绕中心自转的棍子，不像挥舞；
2. **锥形剑刃的低成本做法**：不必手绘锥形纹理——Blade 用两个 Sprite 拼（矩形主体 + 三角尖端），或三段矩形递减宽度，即得"向剑尖收窄"的剪影；
3. **尺寸档位化**：刃长只做 2~3 个固定档位（短/标准/长），实际攻击范围由数据决定（见 1.1-5），防止"换部件 = 偷偷改判定"；
4. **职业色放点缀位**：刃面保持金属中性色，职业色放在护手宝石/柄尾——一眼识职业，又不破坏"这是金属武器"的认知；
5. **命名规范**：`Blade_Sword_A`、`Guard_Gold_A`、`Handle_Leather_A`，部件名_款式_变体，为未来随机组合武器铺路。

### 2.1 刀（战士 · 近战扇形）

- 结构：WeaponRoot → Blade（矩形主体银灰 + 三角尖端浅银，刃背一侧深灰描边）→ Guard（金黄横条 + 中央红色宝石色块）→ Handle（深棕皮革 + 柄尾红色小块）；
- 尺寸：32×64；剪影要点：单刃微弯（刃尖三角向一侧偏 1~2px 即可暗示弧度）；
- 蓄力表现：Effect 层刃尖色块渐亮（银灰 → 白）。

### 2.2 枪/矛（战士 · 近战矩形）

- 结构：WeaponRoot → SpearHead（长三角枪头，银灰 + 中脊深灰线，占全长 ~25%）→ Collar（暗金环 + 红缨色块，识别点）→ Shaft（细长深棕杆，占 ~65%）→ ButtEnd（柄尾金属小包）；
- 尺寸：24×96（细长剪影，与刀明确区分）；
- 蓄力表现：枪头中脊线发亮。

### 2.3 弓箭（射手 · 远程蓄力）

- 结构：WeaponRoot → BowLimbTop / BowLimbBottom（两段深棕弧臂，各为折线形长条）→ BowString（白色细条，拉弓时随蓄力后移——代码控制位置即可，不做骨骼）→ Grip（绿皮缠把，职业色）→ ArrowRest（搭箭位，蓄力时显示箭矢部件）；
- 尺寸：16×64（竖持）；
- 蓄力表现：弦后移 + 箭矢从绿到金的渐亮。

### 2.4 连弩（射手 · 远程连发）

- 结构：WeaponRoot → CrossbowArm（横向金黄双短臂，左右两 Sprite）→ Rail（深棕机身长条）→ Magazine（机身下方灰色方盒弹匣，**连弩的识别点**）→ Grip（绿皮握把）；
- 尺寸：40×48（横宽剪影，与弓的竖长对立）；
- 换弹表现：Magazine 部件掉落 + 弹入的位移动画（DOTween）。

### 2.5 治疗法杖（法师 · 自身回复）

- 结构：WeaponRoot → StaffHead（翠绿圆珠 + 浅绿小环两片，**生命色的头部是识别点**）→ HeadRing（金色细环）→ Shaft（靛蓝杖杆）→ ButtEnd；
- 尺寸：20×72；
- 平击特效：角色身边绿色低透明圆环闪一下（独立特效 prefab，DOTween 缩放+淡出）。

### 2.6 能量法杖（法师 · 远程单发）

- 结构：WeaponRoot → OrbCore（蓝色圆珠 + 浅蓝内芯，两色同心）→ Prongs（三瓣金色爪托，**与治疗杖的单珠区分**）→ Shaft（靛蓝杖杆）→ ButtEnd；
- 尺寸：20×72（与疗杖同杆不同头，强化"法杖家族"感）；
- 换弹表现：Orb 熄灭 → 重新点亮（color 渐变）。

### 2.7 武器地图掉落图标（6 个）

- 每把武器一个**固定小图标**：武器本体缩小至 ≈0.7× + 下方加一小块职业色底板色块（与手持形态区分，远距离能认出"地上有把战士武器"）；
- 图标 prefab：`WeaponMapIcon_<武器名>`，WeaponPickup 引用。

---

## 三、子弹（4 种）

| 子弹 | 结构 | 配色 | 识别 |
|------|------|------|------|
| 箭矢（弓箭） | 箭杆（棕细条）+ 箭头（银灰三角）+ 箭羽（绿色两片） | 棕/银/绿 | 细长、有尾羽 |
| 弩矢（连弩） | 短粗杆（深灰）+ 小三角头 | 深灰/银 | 短粗，与箭矢区分 |
| 能量弹（能量法杖） | 蓝圆 + 浅蓝内芯 + 白色高光点 | 蓝系 | 圆形发光 |
| 精灵弹（小精灵宠物） | 小一号蓝圆 + 金色高光 | 蓝/金 | 最小号圆弹 |

- 统一 prefab 结构：`ProjectileRoot → Body（+ Trail 可选拖尾色块）`；命中特效为 3~5 个小方块炸开（DOTween 飞散+淡出，通用一个 prefab 染色复用）。

---

## 四、玩家角色（3 职业，人形两段式 + 职业标识）

统一基底：`PlayerRoot → Body（圆角矩形躯干）→ Head（圆形头）→ ClassMark（职业标识部件）→ WeaponPivot（现有，挂武器）`。基底体型三职业一致（公平性 + 减少资产量），**靠 ClassMark 与职业色区分**：

| 职业 | ClassMark | 配色 | 剪影要点 |
|------|-----------|------|----------|
| 战士 | 双肩方块肩甲 + 红色短披风条（躯干后） | 红/钢蓝/深灰 | 肩宽体壮，横向最宽 |
| 射手 | 三角兜帽（头部上方）+ 绿色腰带 + 背后箭筒小条 | 绿/棕/深灰 | 头部三角剪影 |
| 法师 | 尖顶法师帽（长三角）+ 蓝紫法袍下摆（躯干下方加宽摆） | 蓝紫/靛蓝/金 | 最高剪影，下摆外扩 |

- 尺寸：整体 ≈32×48；头身双色（头浅、身主色），受击闪白逻辑不变；
- 移动/闪避/死亡动效沿用代码驱动（拉伸挤压、残影、变灰），不做帧动画。

---

## 五、敌人重做（3 档递进，全部非人形）

现有敌人（含精英/Boss 变体）按"体型 + 部件数 + 描边"三档重做，全部用非人形剪影与玩家绝对区分：

| 档位 | 设计 | 配色 | 识别点 |
|------|------|------|--------|
| 普通怪 | 圆滚史莱姆形：圆身体 + 双眼两个小方块 + 底部压扁裙边 | 土橙 + 深橙阴影 | 单色圆滚，无额外部件 |
| 精英怪 | 多角浮游体：菱形身体 + 顶部双角 + 单眼（大色块）+ **金色描边环** | 紫黑 + 金边 | 有角、有金环、体型 ×1.3 |
| Boss | 重装多边形核心：六边形躯干 + 中央红色核心圆 + 四片暗色浮游护甲片环绕（代码驱动缓慢绕转） | 深红 + 暗黑 + 金纹 | 体型 ×2+、多部件、绕转护甲片 |

- 统一 prefab 结构：`EnemyRoot → Body → Eye(s) → Mark(角/环/甲片)`；现有休眠压暗、受击闪白、死亡变灰销毁逻辑直接复用；
- 武器（敌人手持色条）维持现有 WeaponPivot 体系，仅按新调色板染色。

---

## 六、展台（2 种，外观必须明确不同）

| 展台 | 结构 | 配色 | 识别点 |
|------|------|------|--------|
| 职业选择台 | 三层基座（大方座 + 中柱 + 顶部悬浮三面棱晶，代码驱动缓转+浮动） | 石灰基座 + 三面棱晶分别红/绿/蓝紫（对应三职业色） | 唯一带"三色棱晶"的物体；走近显示名称"职业选择台" |
| 武器展示台 ×2 | 单层矮基座 + 金色托盘 + 悬浮展示位（武器图标缓浮动，被拾取后空托盘压暗） | 石灰 + 金托盘 + 职业色描边 | 矮、平顶、有托盘 |

- 结构：`PedestalRoot → Base → Pillar(职业台) / Tray(武器台) → DisplayItem`；
- 选中/确认反馈：棱晶或托盘 DOTween 发光闪烁。

---

## 七、UI（uGUI，全部纯色块 + 文字，无图片资源）

| UI | 规格 |
|----|------|
| 技能图标 | 矩形框（职业色描边）+ 内部技能名文字；双层 Image（底暗/顶亮，顶层 Radial360 Filled 做 CD 扫面）+ 中心剩余秒数 Text；转好 DOTween PunchScale |
| 职业选择界面 | 矩形面板（深色半透明底）+ 标题"选择你的职业"（28pt）+ 三个职业按钮（22pt，各带职业色方块图标；选中=职业色边框高亮）+ 确认按钮（中间偏下，20pt，确认时边框短暂高亮） |
| 拾取列表 | 角色旁小面板（深色底 + 白色文字行），纯文字物品名列表，高亮行反色；后续扩展图标/属性列 |
| 四属性条 | HP（红）/护甲（钢蓝）/体力（黄，窄条）/法力（蓝，窄条），屏幕左下竖排四条 |
| 弹药显示 | 武器图标小色块 + "x/y"文字 + 换弹进度细条 |
| 交互提示 | 世界空间小标签"按 E"（白字深色底），展台名"职业选择台"（16pt）同款 |

- 字体：系统动态字体"微软雅黑"（推荐字号见上表，待审查）。

---

## 八、特效（程序员美术，DOTween/粒子色块）

| 特效 | 做法 |
|------|------|
| 闪避残影 | 角色 Sprite 快照 2~3 个，依次淡出（职业色半透明） |
| 蓄力发光 | 武器 Effect 层 color 渐亮 + 角色微震 |
| 治疗绿环 | 翠绿低透明圆环，缩放 0.5→1.2 + 淡出（≈0.4s） |
| 旋风斩 | 武器整圈旋转（现有 FullCircle）+ 红色扇形残影一圈 |
| 后跃射击 | 后翻残影 + 强化箭金色拖尾 |
| 奥术法阵 | 蓝紫半透明圆盘 + 内圈符文色块环（缓转）+ 每跳伤害一次脉冲缩放；持续 4s 后淡出 |
| 子弹命中 | 通用小方块炸开（按子弹色染色） |
| 法力球/瓶拾取 | 蓝色小光点飞向玩家（DOTween 弧线） |

---

## 九、道具 / 宠物 / 资源掉落物

| 资产 | 设计 | 配色 |
|------|------|------|
| 力量徽章（道具） | 金色八边形徽章 + 中央红色小剑形块 | 金/红 |
| 轻盈之靴（道具） | 绿色小靴形（L 形两块拼）+ 白色翅膀小片 | 绿/白 |
| 小精灵（宠物） | 蓝色小圆球 + 双眼点 + 两片金色小翼（扇动 = DOTween 上下） | 蓝/金 |
| 风之环（宠物） | 绿色细环（环绕玩家缓转）+ 环上三片小叶块 | 绿/浅绿 |
| 法力瓶 | 蓝色小瓶（瓶身方 + 瓶颈窄 + 浅蓝液面色块） | 蓝/浅蓝 |
| 小法力球 | 蓝色小圆点 + 白色高光点（walk-over 自动吸附，不占 E 键，已确认） | 蓝/白 |

---

## 十、资产落地清单（制作时逐项勾验）

- [ ] 武器 prefab ×6（模块化层级 + 调色板染色）+ 地图图标 ×6
- [ ] 子弹 prefab ×4 + 通用命中特效 ×1
- [ ] 玩家角色 prefab ×3（基底复用 + ClassMark 差异）
- [ ] 敌人 prefab 重做 ×3（普通/精英/Boss）
- [ ] 展台 prefab ×2（职业选择台/武器展示台）
- [ ] UI：技能图标、职业选择界面、拾取列表、四属性条、弹药显示、交互提示标签
- [ ] 特效 prefab ×8（残影/蓄力/绿环/旋风/后跃/法阵/命中/吸取）
- [ ] 道具 ×2、宠物 ×2、法力瓶 ×1、法力球 ×1
