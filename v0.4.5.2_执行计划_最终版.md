# v0.4.5.2 执行计划（最终版）

> 版本：v0.4.5.2
>
> 目标：彻底解耦 WeaponAnimator 与 Combat/AI，修正 Arc/FullCircle 动画围绕当前瞄准方向播放，避免攻击结束后角度跳跃。

---

## 一、旧计划存在的问题

### 问题 1：WeaponAnimator 仍耦合 AttackData

旧计划允许 `WeaponAnimator` 持有 `AttackData` 引用，用于 `RefreshWeapon()` 读取 `AttackRange`。这导致：

- `WeaponAnimator` 仍与配置数据耦合。
- 未来更换武器、Buff 改变攻击范围时，必须通过修改 `AttackData` 间接影响 `WeaponAnimator`。
- `WeaponAnimator` 不是真正可复用的动画播放器。

### 问题 2：Arc 动画不是围绕当前瞄准方向

旧计划（以及之前实现）的 Arc 动画：

```
-AttackAngle/2 → +AttackAngle/2（局部坐标）
```

由于 `WeaponPivot` 是角色子物体，角色本身一直在旋转，因此动画实际上是在"已经旋转过的局部坐标"上继续旋转。结果是：

- 玩家移动鼠标后攻击，扇形中心不是当前鼠标方向。
- 敌人转身后攻击，扇形中心不是当前面向。
- 攻击判定与动画表现不同步。

### 问题 3：FullCircle 动画固定从 0° 开始

旧实现中 FullCircle：

```
0° → 360°
```

当 Enemy 已经旋转到 90° 时，动画仍然从 0° 开始，导致：

- 武器不是围绕当前朝向旋转。
- 视觉上像是武器脱离了敌人身体。

### 问题 4：Stop() 恢复 identity 导致跳跃

旧实现中 `Stop()` 将 `WeaponPivot.localRotation` 恢复为 `identity`。攻击结束后：

- 武器突然跳回父对象当前朝向。
- 如果父对象在攻击期间旋转，会出现明显抖动感。

### 问题 5：SetAimDirection 与 Tween 可能抢 Transform

旧设计中 `SetAimDirection()` 每帧直接修改 `WeaponPivot` 旋转，而 `Play()` 同时启动 Tween 修改同一 Transform，存在潜在冲突。

---

## 二、新方案如何解决以上问题

### 解决 1：WeaponAnimator 不再读取 AttackData

- `WeaponAnimator` 的所有参数均由调用方传入。
- `RefreshWeapon(float length)` 接收长度参数，由 `PlayerCombat` / `EnemyAI` 传入 `AttackRange`。
- `Play(...)` 接收 `AnimationType`、`arcAngle`、`duration`、`ease`、`momentRatio`、`callback`。
- `WeaponAnimator` 完全不知道 `AttackData`、`Damage`、`Cooldown`、`TargetLayer` 的存在。

### 解决 2/3：Arc / FullCircle 围绕当前瞄准方向 θ

- `SetAimDirection(Vector2 direction)` 维护内部 `aimAngle`。
- `Play()` 开始时记录当前 `aimAngle` 为 `playStartAimAngle`。
- 使用**世界旋转** `weaponPivot.rotation` 播放动画：
  - **Arc**：`θ - AttackAngle/2` → `θ + AttackAngle/2`
  - **FullCircle**：`θ` → `θ + 360°`
- 动画中心始终是当前瞄准方向，不受父对象后续旋转影响。

### 解决 4：Stop() 恢复到播放前记录的 AimRotation

- `Play()` 开始时记录 `playStartAimAngle`。
- `Stop()` 将 `WeaponPivot.rotation` 恢复为 `playStartAimAngle`，而非 `identity`。
- 攻击结束后武器保持在攻击开始时的瞄准方向，再等待 Combat/AI 重新同步。

### 解决 5：SetAimDirection 只在安全时应用旋转

- `SetAimDirection()` 仅在 `!IsPlaying` 时修改 `WeaponPivot.rotation`。
- 播放期间 `SetAimDirection()` 被忽略，Tween 独占 Transform。
- `PlayerCombat` / `EnemyAI` 不在播放期间调用 `SetAimDirection()`。

---

## 三、最终职责划分

| 模块 | 负责 | 不负责 |
|---|---|---|
| **AttackData** | 保存配置：Range、Angle、Type、Damage、Cooldown、TargetLayer、Windup、Active、Recovery、Ease、MomentRatio | 任何计算、动画逻辑 |
| **PlayerCombat** | 读取 AttackData、维护 Windup/Active/Recovery、调用 WeaponAnimator、执行伤害判定 | 直接控制 WeaponPivot Tween |
| **EnemyAI** | 读取 AttackData、维护状态机、调用 WeaponAnimator、调用 EnemyCombat | 直接控制 WeaponPivot Tween |
| **WeaponAnimator** | WeaponPivot、WeaponSprite、Tween、SetAimDirection、Play、Stop、RefreshWeapon | 读取 AttackData、判断伤害/冷却/TargetLayer/Combat 状态 |
| **EnemyCombat** | 冷却、范围判定、伤害 | 保持原样 |

---

## 四、修订后的执行步骤

### Step 1：修订 AttackData

**修改文件：**
- `Assets/Scripts/v0_4/AttackData.cs`

**操作：**
- 保留所有配置字段：`AttackRange`、`AttackAngle`、`AnimationType`、`AttackDamage`、`AttackCooldown`、`TargetLayer`、`WindupTime`、`ActiveDuration`、`RecoveryTime`、`AttackEase`、`ActiveMomentRatio`。
- `AttackAnimationType` 枚举包含：`Arc`、`FullCircle`、`Thrust`、`Spin`、`Throw`。
- 不新增 `StartAngle` / `EndAngle`。
- 不添加任何动画计算或逻辑方法。

**与旧计划差异：**
- 无实质变化，进一步强调纯配置职责。

---

### Step 2：重写 WeaponAnimator

**修改文件：**
- `Assets/Scripts/v0_4/WeaponAnimator.cs`

**操作：**
- **删除** `AttackData` 字段引用。
- 保留字段：
  - `Transform weaponPivot`
  - `Transform weaponSprite`
  - `float weaponWidth`
- 新增内部状态：
  - `float aimAngle`（当前瞄准角度）
  - `float playStartAimAngle`（播放开始时记录的角度）
- 方法：
  - `SetAimDirection(Vector2 direction)`：
    - 若 `IsPlaying` 为 `true`，直接返回。
    - 计算角度并存入 `aimAngle`。
    - 将 `weaponPivot.rotation` 设置为该角度。
  - `Play(AttackAnimationType type, float arcAngle, float activeDuration, float activeMomentRatio, Ease ease, Action onActiveMoment)`：
    - 调用 `Stop()`。
    - 记录 `playStartAimAngle = aimAngle`。
    - 根据 `type` 计算世界空间起止角度：
      - `Arc`：`start = θ - arcAngle/2`，`end = θ + arcAngle/2`，使用 `RotateMode.Fast`。
      - `FullCircle`：`start = θ`，`end = θ + 360`，使用 `RotateMode.FastBeyond360`。
    - 设置 `weaponPivot.rotation = startAngle`。
    - 使用 `DORotate` 在世界空间播放 Tween。
    - 按 `activeMomentRatio` 触发 `onActiveMoment`。
  - `Stop()`：
    - 停止 Tween。
    - 将 `weaponPivot.rotation` 恢复为 `playStartAimAngle`（不是 identity）。
  - `RefreshWeapon(float length)`：
    - 根据 `length` 和 `weaponWidth` 设置 `WeaponSprite.localScale`。
    - 设置 `WeaponSprite.localPosition = (length/2, 0, 0)`。
    - 不修改 `WeaponSprite.localRotation`。

**与旧计划差异：**
- 删除 `AttackData` 引用。
- `Play()` 改为接收全部参数，不再内部读取配置。
- `Play()` 使用世界旋转，动画围绕当前 `aimAngle`。
- `Stop()` 恢复到 `playStartAimAngle`。
- `RefreshWeapon(float length)` 接收长度参数。

---

### Step 3：修改 PlayerCombat

**修改文件：**
- `Assets/Scripts/v0_3/PlayerCombat.cs`

**操作：**
- 引用 `AttackData` 与 `WeaponAnimator`。
- 维护 `Windup → Active → Recovery`。
- `UpdateAiming()`：
  - 计算鼠标方向。
  - 若 `!weaponAnimator.IsPlaying`，调用 `weaponAnimator.SetAimDirection(direction)`。
- `StartWindup()`：
  - 调用 `weaponAnimator.SetAimDirection(mouseDirection)` 确保瞄准已同步。
  - 调用 `weaponAnimator.RefreshWeapon(attackData.AttackRange)`。
- `EnterActive()`：
  - 从 `AttackData` 读取参数。
  - 调用 `weaponAnimator.Play(...)`：
    ```csharp
    weaponAnimator.Play(
        attackData.AnimationType,
        attackData.AttackAngle,
        attackData.ActiveDuration,
        attackData.ActiveMomentRatio,
        attackData.AttackEase,
        OnActiveMoment
    );
    ```
- `OnActiveMoment()`：
  - 执行扇形伤害判定（`AttackRange`、`AttackAngle`、`TargetLayer`、`AttackDamage`）。
- `UpdateRecovery()` 结束：
  - 调用 `weaponAnimator.Stop()`。
  - `subPhase = None`。
  - 触发 `OnAttackEnd`。

**与旧计划差异：**
- 显式从 `AttackData` 拆解参数传给 `WeaponAnimator.Play(...)`。
- 调用 `RefreshWeapon(attackData.AttackRange)`。
- `SetAimDirection()` 调用时机限制在未播放状态。

---

### Step 4：修改 EnemyAI

**修改文件：**
- `Assets/Scripts/v0_4/EnemyAI.cs`

**操作：**
- `UpdateChase()`：
  - `controller.FaceTowards(dir)` 后，若 `!weaponAnimator.IsPlaying`，调用 `weaponAnimator.SetAimDirection(dir)`。
- `UpdateAttack()`：
  - `controller.FaceTowards(dir)` 后，若处于 `Windup` 且 `!weaponAnimator.IsPlaying`，调用 `weaponAnimator.SetAimDirection(dir)`。
  - `Active` / `Recovery` 期间不调用 `SetAimDirection()`。
- `ChangeState(Attack)`：
  - 调用 `weaponAnimator.SetAimDirection(playerDir)`。
  - 调用 `weaponAnimator.RefreshWeapon(attackData.AttackRange)`。
- `EnterActive()`：
  - 从 `AttackData` 读取参数。
  - 调用 `weaponAnimator.Play(...)`。
- `OnActiveMoment()`：
  - 调用 `EnemyCombat.TryAttack(damageable)`。
- `ChangeState(Attack → Chase)`：
  - 调用 `weaponAnimator.Stop()`。

**与旧计划差异：**
- 显式拆解 `AttackData` 参数传给 `WeaponAnimator.Play(...)`。
- 调用 `RefreshWeapon(attackData.AttackRange)`。
- `SetAimDirection()` 只在 Windup/Idle 调用，Active/Recovery 期间不调用。

---

### Step 5：调整 Prefab

**修改文件：**
- `Assets/Prefabs/Player.prefab`
- `Assets/Prefabs/Enemy.prefab`

**操作：**
- `WeaponPivot`：
  - `localPosition = (0, 0, 0)`，位于角色中心。
- `WeaponSprite`：
  - `localRotation = -90°`（固定，代码不修改）。
  - `localPosition` 和 `localScale` 由 `RefreshWeapon()` 在运行时设置，Prefabs 中可保留任意初始值。
- `WeaponAnimator` 组件字段：
  - 仅保留 `weaponPivot`、`weaponSprite`、`weaponWidth`。
  - 移除 `attackData` 字段引用。

**与旧计划差异：**
- `WeaponAnimator` 不再持有 `AttackData` 引用。

---

### Step 6：更新 Asset 配置

**修改文件：**
- `Assets/Data/PlayerAttackData.asset`
- `Assets/Data/EnemyMeleeAttackData.asset`

**操作：**
- `PlayerAttackData.asset`：
  - `animationType = 0`（Arc）
  - `attackAngle = 90`（扇形幅度）
  - `attackRange = 1.5`（武器视觉长度 + 判定半径）
- `EnemyMeleeAttackData.asset`：
  - `animationType = 1`（FullCircle）
  - `attackAngle` 可任意（FullCircle 不使用）
  - `attackRange = 1.2`

**与旧计划差异：**
- 无变化。

---

### Step 7：编译与验证

**操作：**
- 执行 `dotnet build Assembly-CSharp.csproj`。
- 目标：`0 警告，0 错误`。

**运行时验证清单：**

#### Player
- [ ] 待机时武器始终跟随鼠标方向。
- [ ] 攻击开始时锁定当前鼠标方向。
- [ ] Arc 动画以当前鼠标方向为中心，左右各挥 `AttackAngle/2`。
- [ ] 攻击结束后武器保持在攻击开始方向，随后平滑恢复跟随鼠标。
- [ ] 无角度跳跃、无 identity 复位感。

#### Enemy
- [ ] 待机时武器始终朝向玩家。
- [ ] Windup 期间继续朝向玩家。
- [ ] FullCircle 从当前朝向开始，完整旋转 360°。
- [ ] 动画结束后武器恢复朝向玩家。
- [ ] 360° 动画视觉上围绕敌人身体。

#### 通用
- [ ] `WeaponAnimator` 不读取 `AttackData`。
- [ ] `PlayerCombat` / `EnemyAI` 显式传参给 `WeaponAnimator`。
- [ ] `WeaponSprite` 长度由 `RefreshWeapon(AttackRange)` 控制。
- [ ] `EnemyCombat`、`PlayerController`、移动框架未被修改。
- [ ] 无编译错误。

---

## 五、为什么新方案不会导致旧问题

### 为什么 Player 攻击结束不会跳角度？

- `Play()` 记录 `playStartAimAngle`。
- `Stop()` 恢复到 `playStartAimAngle`，不是 `identity`。
- 武器不会突然跳回父对象当前朝向。

### 为什么 Enemy 能完整 360° 旋转？

- FullCircle 使用 `θ → θ + 360°`。
- `θ` 是播放开始时记录的实际朝向。
- 使用 `RotateMode.FastBeyond360`，DOTween 会真正旋转一整圈。

### 为什么 Arc 围绕瞄准方向？

- Arc 起止角度为 `θ - AttackAngle/2` 和 `θ + AttackAngle/2`。
- 使用世界旋转 `weaponPivot.rotation`，动画中心锁定在播放瞬间的瞄准方向。
- 父对象后续旋转不影响本次攻击动画中心。

### 为什么 Tween 和 Update 不会抢 Rotation？

- `WeaponAnimator` 内部没有 `Update()`。
- `SetAimDirection()` 仅在 `!IsPlaying` 时修改旋转。
- `Play()` 期间 `SetAimDirection()` 调用被忽略。
- `PlayerCombat` / `EnemyAI` 保证 Active/Recovery 期间不调用 `SetAimDirection()`。

---

## 六、待确认

1. 是否确认以上最终版执行计划？
2. 是否可以从 **Step 1** 开始执行代码修改？
3. `PlayerAttackData` 的 `attackAngle` 保持 `90°` 还是改为 `120°`？
