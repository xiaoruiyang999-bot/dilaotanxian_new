# v0.5.4.4.4 战斗判定与场景恢复修复

> 状态：**已完成 · 全部修复已落盘，待 Unity 人工验收**
> 日期：2026-08-19
> 关联版本：v0.5.4.4.x（战斗生态）→ 本次收口游玩实测暴露的隐藏问题
> 关联文档：[v0.5.4.4.2_远程AI攻击失效修复](v0.5.4.4.2_远程AI攻击失效修复.md) / [开发调试规范_场景摄像头位置管控](开发调试规范_场景摄像头位置管控.md)

---

## 1. 问题描述

正常游玩过程中暴露三类症状，全量代码审查定位到 1 个致命 + 4 个高危根因，
另清理了场景调试残留（违反《开发调试规范》2.3/2.4 条）：

| 游玩症状 | 根因 | 严重度 |
|---|---|---|
| Boss 清完不出传送门、死亡后软锁 | 场景 `v0_5_Dungeon.unity` 在编辑器崩溃恢复时**丢失 RunManager 组件**（HEAD 有、工作区无），楼层循环/死亡重开整套失效 | 致命 |
| 远程敌人被近身后站桩，不后撤不侧移 | `EnemyPerception.IsDirectionClear` 不跳过 Trigger：RoomTrigger/敌人探测圈挡住探测射线（v0.5.4.4.3 只修了 `QueryLineOfSight` 那一半） | 高 |
| 敌人子弹飞过宝箱/传送门凭空消失、两弹对射互销 | `Projectile.ResolveHit` 命中 targetLayer 但无 `IDamageable` 时一律自毁——Default 层的交互物触发器、投射物互撞全被"吞弹" | 高 |
| 战斗房杀光敌人门不开（偶发软锁） | `EnemyCombat.SummonMinions` 纯随机落点无合法性校验，召唤师贴墙时小兵嵌墙；小兵已计入清房计数 | 高 |
| 越玩越卡（跨楼层累积） | `AttackIndicator` 预警中脱离父物体，敌人死亡时仅 Hide 不销毁，GameObject+Mesh 孤儿常驻场景根逐层累积 | 高 |
| （连带发现）相机不动/玩家传送失效风险 | `CameraFollow.target` 与 `DungeonManager.player` 均为 `fileID: 0` 空引用，仅靠 tag 兜底半条链路 | 高 |

场景另有序列化残留：`Debug_Player` / `Debug_RealPlayer` / `Debug_EnemyRanged`
三个调试对象（94 个关联块）与 7 个 Play 模式泄漏的 `DamagePopup` 实例，
且 **Debug_RealPlayer 与正式 Player 实例同时存在且都 tag=Player**（双玩家
正是《调试规范》第 4 节"调试假象"的温床）。

---

## 2. 修复明细

### 2.1 EnemyPerception.cs — `IsDirectionClear` 跳过 Trigger
与 `QueryLineOfSight`（v0.5.4.4.3）同款规则：遍历命中时 `isTrigger` 直接
`continue`。Trigger 只负责逻辑事件，不作为移动阻挡。远程敌人的 Retreat/
Strafe 探测恢复有效。

### 2.2 EnemyCombat.cs — 召唤物落点合法性校验
新增 `TryGetMinionSpawnPosition`：随机取点后 `OverlapCircle(0.4,
ContactFilter2D{Default|Enemy|Obstacle, useTriggers=false})` 校验（与
`SpawnPositionHelper` 同一规则、缓冲复用无 GC），最多重摇 8 次，全失败则
**跳过该只不生成**——宁可少召唤一只，不可卡死清房条件。

### 2.3 AttackIndicator.cs + EnemyCombat.cs — 孤儿泄漏修复
- `RestoreParent` 重构：父引用 fake-null（已被 Destroy）→ 立即自毁；
  父停用 → 保持脱离状态并交由新增 `Update` 监视：父复活则归位、父销毁则
  自毁（覆盖"休眠敌人随楼层 Cleanup 回收"路径）。
- 新增 `OnDestroy`：回收运行时创建的 Mesh/Material（此前永不释放）。
- 新增 `DestroyIndicator()`；`EnemyCombat.OnDisable` 改为按 `EnemyHealth.IsDead`
  分流：死亡停用 → 销毁指示器；未进房休眠/场景卸载 → 只 Hide（复活可复用）。
  玩家路径（`PlayerCombat`）不动——RunManager 死亡重开**从不停用玩家对象**，
  已核实。

### 2.4 Projectile.cs — Trigger 穿透
`ResolveHit` 命中 targetLayer 且无 `IDamageable` 时：`other.isTrigger` 为真
（交互物触发器、另一颗投射物）→ `return false` 穿透继续飞；非 Trigger
（墙 TilemapCollider）→ 维持挡弹销毁。`obstacleLayer` 分支不变。

### 2.5 场景手术 v0_5_Dungeon.unity（YAML 级，备份于 `_backup/`）
1. **恢复 RunManager**：DungeonSystem（&1357356017）补回组件引用
   （&1357356021），组件块自 HEAD 原样恢复（dungeonManager / rewardChestPrefab
   = Chest.prefab / portalPrefab = Portal.prefab / restartDelay=2，GUID 均已
   验证存在）。
2. **删除调试残留**：3 个 Debug_ 对象全子树 + 7 个 DamagePopup 泄漏实例
   共 94 块，`SceneRoots` 根列表同步清理 10 个引用。
3. **重接空引用**：新建 stripped Transform（&1474215305 → Player.prefab 根
   Transform，挂到正式实例 &2002241262 名下）；`CameraFollow.target` 与
   `DungeonManager.player` 由 `fileID: 0` 改接该引用。tag 兜底仍作后备。
4. **验证**：无悬空 fileID 引用；Debug_/DamagePopup 计数归零；RunManager
   guid 计 1、fileID 计 2。场景相对 HEAD 的 diff 从 3351 行收敛到 325 行。

---

## 3. 验收清单（Unity 打开场景后逐项确认）

> **⚠️ 验收前置（必读）**：本次场景是外部脚本直接改写的。Unity 编辑器若在修改前
> 已打开该场景，内存里仍是旧副本（含双玩家：Debug_RealPlayer + 正式实例，两个
> PlayerInput 抢键盘、相机 target 为空），不 Reload 就 Play 会表现为
> **"方向移动键全部失灵"**。正确顺序：
> 1. 弹出"Scene has been changed externally"时选 **Reload**（没弹就 `File → Open Scene` 重开）；
> 2. **先看 Console 有无红色编译错误**（有编译错误 = 全部脚本失效，任何输入都不会响应）；
> 3. Hierarchy 用 tag 搜 Player，确认**有且只有一个**激活玩家（名为 Player 的 prefab 实例）；
> 4. 再进 Play 按下表验收。
> 详见《开发调试规范》2.5 节。

1. 打开 `v0_5_Dungeon.unity` 无报错；DungeonSystem 挂有 RunManager 且 4 个引用非空
2. Play：进战斗房杀光敌人 → 门开
3. 杀 Boss → 房中心左右生成奖励宝箱与传送门 → 踩门进下一层（敌人 +1/层）
4. 玩家死亡 2 秒后自动回第 1 层（HP/护甲回满）
5. 远程敌人被近身后会后撤/侧移，不再站桩
6. 敌弹飞过宝箱/传送门不消失；两颗敌弹对射互穿
7. 召唤师贴墙召唤不嵌墙；极端情况下房间仍可正常清空开门
8. 连续击杀预警中的敌人，Hierarchy 场景根不残留 AttackIndicator / DamagePopup
9. 相机跟随正常（出生与楼层切换后均跟随正式 Player）

---

## 4. 已知遗留（本次明确不修，按优先级排期）

| 问题 | 位置 | 严重度 |
|---|---|---|
| 受击闪烁协程重叠 → 敌人永久变白 | EnemyController.OnHitFlash 未停旧协程 | 中 |
| EnemyPerception 每帧多次 RaycastAll + GetComponentInChildren 分配（GC） | EnemyPerception.cs:49/73/80 | 中 |
| Charger `chargerCollisionLayer=256` 指向空层；冲锋射线不跳 Trigger 对宝箱"撞墙" | AttackData_Charger.asset + EnemyCombat.UpdateActive | 中 |
| 玩家死亡后进行中的攻击不结算中断；PlayerCombat 无 OnDisable 清理 | PlayerCombat.cs | 中 |
| DungeonGenerator 保底路径 fallback 可能为 null → NRE | DungeonGenerator.cs:33-38 | 中低 |
| 宝箱开盖动画期间切层，治疗奖励被吞且 consumed 已置 true | ChestInteractable.cs:30-34 | 中低 |
| 巡逻/追击无寻路，巡逻点可落在墙内；PatrolSystem 用 UnityEngine.Random 破坏 seed 确定性 | PatrolSystem.cs / EnemyAI.cs | 中 |
| Windup 内部取消（LOS 丢失）不进冷却 → 反复蓄力抖动 | EnemyCombat.UpdateWindup（v0.5.4.4.2 遗留） | 中 |
| Projectile 寿命 3s 写死在 Launch 默认参数 | Projectile.cs（v0.5.4.4.2 遗留） | 低 |
| EnemyHealth.TakeDamage 不拒绝负伤害 | EnemyHealth.cs:35-47 | 低 |
| Enemy_Ranged.prefab WeaponSprite 残留误导性缩放脏数据 | Enemy_Ranged.prefab | 低 |
| 召唤师死亡同帧 Active 期召唤的小兵可登记进已 Cleared 房间 | EnemyCombat / Room.RegisterEnemy | 低 |
| 传送门生成在房中心 +1.5，易被无意识踩中 | RunManager.OnBossCleared | 低 |

> 备份与手术脚本：`_backup/v0_5_Dungeon_手术前备份_20260819.unity`、
> `_backup/surgery_v0_5_Dungeon.py`。验收通过后可删除整个 `_backup/` 目录。
