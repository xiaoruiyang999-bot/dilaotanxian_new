# v1.1.2 战士武器挂载点对齐狼人视觉挂载（容器中心）

## 1. 变更内容

`Assets/Prefabs/Player.prefab` 的 `WeaponPivot`（Transform fileID 283850676589024314）：

| 字段 | 旧值 | 新值 |
|---|---|---|
| m_LocalPosition | (0, 0.3, 0) | **(0, 0, 0)** |

依据（用户拍板）：挂载点对齐狼人视觉的挂载位置——`WerewolfVisual` 子物体挂在角色容器 (0, 0, 0)（容器中心）。该位置与"战士图心公式"计算结果一致：战士三组帧（idle/Walk/WalkBack，各 5 帧）alpha 可见内容包围盒中心 = 整图高度的 51.6%（帧为居中锚点 0.5/0.5，256px@50PPU=5.12 局部单位），换算到容器局部 ≈ y 0。

## 2. 影响面（同源链整体下移 0.109 世界单位）

WeaponPivot 是唯一挂载真源，以下全部同步：
- 武器视觉（WeaponSprite 色块 + 职业武器 SetCustomVisual）
- 攻击判定盒（WeaponHitbox.ComputeSwingBox，从 pivot 世界位置延伸）
- 远程枪口（PlayerCombat：pivot + aimDir×0.6）
- 攻击预警（消费同一几何）
- 狼人形态：pivot 仅被 WerewolfTransformation 放大 localScale（×1.5），位置共享本值，同步下移。

判定安全校验：玩家攻击带（宽≈0.15~0.2）中心从根上方 +0.109 → 0，带域 [−0.1, +0.1] 完全落在敌人碰撞体 [−0.5075, +0.5075] 内，横向攻击命中不受影响（上限约局部 +1.2，本次远未触及）。

## 3. 手术记录（R2 流程）

- 备份：`_backup/Player.prefab.bak_v1.1.2_*`
- 行尾：prefab 全 CRLF，二进制替换保持 564 CRLF / 0 裸 LF ✓
- 唯一性：目标行全 prefab 唯一（fileID 块锚定替换）✓
- 场景实例：v0_6/v0_7 四场景均无 WeaponPivot 位置覆盖（仅根 Transform/WeaponSprite 无实效同值覆盖）→ prefab 修改自然生效。
- **外部改 prefab 后必须 Reload 场景再 Play（R4）**。

## 4. 验收清单

1. Unity 重新编译无错 → 打开 v0_7_PrepRoom → Reload → Play。
2. F9 开红线可视化：橙色挥击判定盒中心应与红色胶囊… 注意玩家胶囊本身仍悬浮（R19 遗留，未在本版处理）；看**武器视觉**应从身体中心稍上移到正中心。
3. 战士横向攻击仍能命中敌人（判定带校验如上）。

## 5. 遗留

狼人形态"武器贴脚"现象的运行时机制未定位（静态变换链无可疑代码，见 v1.1.1 取证）。若对齐后狼人形态武器仍明显偏低，用 F9 实测截图（战士/狼人各一张）再排查——下一步嫌疑是兽形态视觉与挂载的运行时错位或截图误判。
