# v1.0.5 游戏完善轮：连接性修复 + 入口统一 + 死亡结算 + 音频闭环

> 日期：2026-08-31  
> 状态：已落盘；编译 0 错误 0 警告；EditMode 门禁 9/9 绿；三处功能冒烟通过（准备房自动弹职业选择 / 死亡结算面板弹出 / 死亡后自动返回准备房且再次自动弹选择）。  
> 本轮目标：后端完善为主，美术缺失处已标注。

## 一、模块连接性审查结果（修复前 → 修复后）

| 模块 | 修复前状态 | 修复后 |
|---|---|---|
| AmmoUI（弹药面板） | 代码完整但**无任何场景挂载**（断链） | 挂 Canvas（三场景） |
| MinimapSystem（小地图） | 仅 v0_5 有，官方地牢 v0_7_ClassWeapon **缺失** | 已补（自动找 DungeonManager，零配置） |
| AudioManager 音效表 | 全部场景为空（Kenney 音效从未接线） | 5 条占位音效 ×3 场景 |
| AudioManager BGM | 空 | DungeonBGM.mp3 ×3 场景 |
| 音效代码挂钩 | 全代码只剩 `door` 一处（hit/hurt/enemyDie/chest 随 v0.7.5 合并被删） | 四处挂点恢复（见下） |
| DeathPanel（死亡结算） | 不存在（死亡 2 秒后硬切场景） | 新建，闭环验证通过 |
| PausePanel（Esc 暂停） | v1.0.4 已重建挂载 | 本轮保留（见待人工项） |
| SlotBarUI / ClassSelectUI / PlayerUI / PickupList | 自挂载/静态构建，连接正常 | 无需动 |
| 入口 | 首次进入需自己找展台按 E | 无职业时自动弹出职业选择 |

## 二、本轮改动明细

### 1. 统一初始入口（v0.7 职业选择）
- `PrepRoomManager.Start`：`LastChosenClass == null` 时自动 `ClassSelectUI.Open()`——首次进入即弹职业选择；死亡回来（职业保留）不弹，直接拿武器。
- 冒烟验证：Play 准备房 5 秒，层级出现 ClassSelectUI+Canvas ✓；死亡返回后再次自动弹出 ✓。

### 2. 死亡结算面板（新 `UI/DeathPanel.cs` + `Dungeon/Core/RunTracker.cs`）
- RunTracker：静态击杀数 + 开局时间；`RunManager.InitDelayed` 调 `BeginRun`（每次地牢场景加载重置，NextFloor 不重置=整局累计）；`EnemyHealth.Die` 调 `OnEnemyKilled`。楼层数唯一真源仍是 `RunManager.FloorNumber`。
- DeathPanel：订阅玩家 `Health.OnDeath` → 弹出"本局结算"（抵达楼层/击杀/存活时长）；Esc 或点击任意处立即回准备房间；`RunManager.restartDelay` 2→6 秒作超时兜底（面板主动返回时场景卸载终止协程，无二次加载）。挂 DeathSystem 空对象（v0_7_ClassWeapon + v0_5_Dungeon）。
- 功能验证：Debug Kill Player 后 1 秒内 DeathCanvas 弹出 ✓；6 秒兜底自动回准备房 ✓。

### 3. 音频闭环
- BGM：`C:\Users\17864\Downloads\2026-08-23-14-48-44.mp3` → `Assets/Audio/BGM/DungeonBGM.mp3`，接入三场景 `AudioManager.bgm`（循环播放走既有 bgmSource.loop）。
- SFX 挂钩恢复（v1.0.0 设计、合并时丢失）：`WeaponHitbox` 命中→"hit"、`Health.TakeDamage` 实际扣血→"hurt"、`EnemyHealth.Die`→"enemyDie"、`ChestInteractable.OnConsumed`→"chest"（既有 "door" 不变）。
- 占位音效表（Kenney ImpactSounds，三场景同表，**均为临时占位待换正式音效**）：

| id | 占位 clip | 音量 |
|---|---|---|
| hit | impactMetal_light_000 | 0.5 |
| hurt | impactPunch_heavy_000 | 0.6 |
| enemyDie | impactBell_heavy_002 | 0.6 |
| door | impactWood_light_002 | 0.5 |
| chest | impactPlate_light_001 | 0.6 |

### 4. 场景接线汇总
v0_7_PrepRoom：Canvas+AmmoUI、AudioSystem（BGM+5SFX）。  
v0_7_ClassWeapon：Canvas+AmmoUI、AudioSystem（BGM+5SFX）、DeathSystem+DeathPanel、MinimapSystem+MinimapController、RunManager.restartDelay=6。  
v0_5_Dungeon：Canvas+AmmoUI、AudioSystem（BGM+5SFX）、DeathSystem+DeathPanel、restartDelay=6。

## 三、【美术资产缺失清单】（本轮以纯色块+内置字体占位）

1. **死亡结算面板**：背景框/标题字效/数据图标（楼层·骷髅·沙漏）/死亡灰度滤镜（现仅角色变灰）。代码注释已标注。
2. **暂停面板**：背景框/按钮图/滑条皮肤（沿用户李 v1.0.0 占位样式）。
3. **职业选择/武器展台/道具**：既有纯色块占位体系，沿现状。
4. **正式音效**：上表 5 条均为 Kenney 占位；BGM 已用用户提供的正式文件。
5. UI 全部使用 `MinimapController.BuiltinFont` 内置字体，未引入字体资产。

## 四、其余待补齐清单（本轮未做，按优先级）

1. **PausePanel 人工验证**：Esc 开关暂停/音量滑条/重开按钮——无键鼠模拟通道，需开一次 Play 实测（编译+挂载+代码审查已过）。
2. **交互级 Golden Path 人工验收**：点选职业→拿武器→传送门→战斗→Boss→宝箱→下一层→死亡结算全链。
3. **金币经济/三选一局内升级/通关结算/Meta 存档**：后端随 v0.7.5 删除，恢复需按现行七维数值+职业武器技能体系重设计（非回滚）。
4. **输入资产清理**：Dash/Sprint 动作无消费方仍留在 .inputactions；键位表定稿后同步教程文案。
5. **正式音效替换**（见占位表）与音量调平；脚步声/受击多样本随机等表现层增强。
6. v0_6 双场景与 v0_5_Dungeon 旧场景退役决策；PlayMode 冒烟测试；独立构建实测（Resources 化后首次出包验证）。

## 五、事故与教训（回写规则）

- **并发编辑冲突**：本轮期间编辑器被手动切场景+进 Play 两次，导致两轮场景写入丢失/回滚。教训：**批量场景改动必须单原子 batch（Transactional）+ 立即 scene_save + 保存后按 GUID 文件级校验**；发现"对象找不到/保存路径不对"时第一步查当前活动场景名。按资产名 grep 场景 YAML 是无效校验（场景只存 GUID）。
- 场景文件级校验正确姿势：`grep <资产.meta 的 guid> scene.unity`。

## 六、验证汇总

- 编译：0 error / 0 warning（含 2 新类 + 6 处挂点改动）。
- EditMode 门禁：9/9（场景扫描复含本轮三场景改动，无 Missing Script）。
- 冒烟 A（准备房）：自动弹职业选择 ✓、BGM/音效配置就位 ✓。
- 冒烟 B（地牢）：Debug 击杀 → DeathCanvas 1 秒内弹出 ✓ → 自动返回准备房 ✓ → 再次自动弹选择 ✓。
