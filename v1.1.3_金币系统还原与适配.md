# v1.1.3 金币系统自主分支（MCP）还原

## 1. 背景与范围

`fix/ranged-attack-and-mcp` 分支含完整金币系统（M2·v0.7.0 原型：CoinDrop 磁吸掉落 + PlayerStats 钱包 + CoinHUD + 敌死/宝箱产出 + 商店消费）。该分支当年随 v0.5 旧体系清理被移除，本次按用户要求重新导入，并适配 v1.1.x 主分支现状。

**导入范围（核心闭环）**：掉落物、钱包、敌死产出、召唤物防刷、宝箱权重、HUD、音效。
**不导入（主分支已删/无消费方）**：ShopInteractable（v0.5 商店已删）、MetaManager（局外.meta 已删）、UpgradePanel（升级面板已删）、VictoryPanel（胜利结算已删，现行走 DeathPanel+RunTracker）。`TrySpendCoins` 钱包 API 已保留，未来商店等付费交互直接接入。

## 2. 文件清单

| 文件 | 类型 | 说明 |
|---|---|---|
| `Scripts/now_use/Common/CoinDrop.cs` | 新增 | 纯代码金币掉落物：随机滑散→磁吸→触碰入账；12s 生命周期+末 2s 闪烁；程序生成 32px 金币 Sprite（CoinSprite 静态共享，HUD 复用） |
| `Scripts/now_use/UI/CoinHUD.cs` | 新增（重写） | 金币 HUD：**TMP 文本**（ClassSelectUI.CreateText 模式，先 text 后 font=TMPFontProvider.Font）+ RuntimeInitializeOnLoadMethod 自装隐藏常驻（零场景手术）+ 0.5s 轮询重绑玩家实例 |
| `Scripts/now_use/Player/PlayerStats.cs` | 修改 | 金币钱包：`coins` / `Coins` / `OnCoinsChanged` / `AddCoins` / `TrySpendCoins`；专用事件不并 OnStatsChanged（防整块属性 UI 逐币重刷） |
| `Scripts/now_use/Enemy/EnemyController.cs` | 修改 | `DropCoins`（自动属性，不序列化）+ `coinsMin/coinsMax`（序列化，默认 1~3）；OnEnemyDeath 法力球后掉金币 |
| `Scripts/now_use/Enemy/EnemyCombat.cs` | 修改 | SummonMinions：召唤物 `DropCoins=false`（防无限刷币） |
| `Scripts/now_use/Dungeon/Interaction/ChestInteractable.cs` | 修改 | 三权重→四权重：**金币 25% / 武器 30% / 法力包 20% / 消耗包 25%**（金币 6~10 枚，散落自带表现不走 PopIn）；无职业旧场景路径不变 |
| `Scenes/v0_7_PrepRoom.unity` + `v0_7_ClassWeapon.unity` | 修改（R2 手术） | AudioManager.sfxEntries 追加 `coin` → impactTin_medium_000.ogg（Kenney 锡金属叮声，vol 0.5）；双场景都加（见 §4 常驻机制） |

## 3. 对主分支的适配点（与 MCP 原版的差异）

1. **挂载策略**：MCP 版金币挂 `GameObject.Find("DungeonRoot")` 随楼层清理；主分支 dungeonRoot 是场景接线私有字段无固定名 → 改为 ManaOrb 同款**场景根直挂 + 12s 生命周期自清**（楼层切换/场景卸载零残留）。
2. **HUD 文本栈**：Legacy UGUI Text + MinimapController.BuiltinFont（已不存在）→ **TMP + TMPFontProvider**（红线：禁构造参数塞组件、先 text 后 font）。
3. **HUD 生命周期**：MCP 版场景空物体挂载 → **运行时自装**（R1/R4 免疫）；死亡重开/进地牢均为新玩家实例（RunManager.RestartRun 整场景重载），故 0.5s 轮询重绑 + 订阅随实例配对退订。
4. **单局语义天然成立**：金币在 PlayerStats 上，死亡重开=新实例→清零；NextFloor 原地重建不换场景→局内跨层累积。无需手动重置代码（R12 不适用：无跨场景常驻玩家）。
5. **排序**：金币 sortingOrder 4（法力球 1/2 之上、玩家 10/武器 11 之下）。
6. **音效**：`PlaySFX("coin")`（未配置静默）；已在两正式场景表接 impactTin_medium_000。

## 4. 踩坑记录：AudioManager 首实例常驻

AudioManager"首个实例 DontDestroyOnLoad，后续实例静默禁用"——正式流程从 PrepRoom 进，**地牢里生效的 SFX 表是 PrepRoom 实例的**。只给 ClassWeapon 加 coin 条目会静默失效，必须双场景同加（本次已处理；未来新增 SFX id 记得改 PrepRoom 的表）。

## 5. 验收清单

1. Unity 编译无错 → PrepRoom Reload → Play（R4：外部改了场景）。
2. 击杀普通敌人：散落 1~3 金币 → 靠近磁吸 → 左下角 HUD 数字增加 + 叮声。
3. 开宝箱：约 1/4 概率出 6~10 金币；其余三分支行为与 v0.7.5 一致。
4. 召唤师小兵死亡不掉金币。
5. 上一层楼（传送门）：金币保留；死亡重开回准备房间：金币清零、HUD 归 0。
6. EditMode 测试全绿（本改动无测试断言交集，已核）。

## 5. 调参入口

- 敌人金币量：Enemy prefab Inspector 的 coinsMin/coinsMax（Boss/精英可在各自 prefab 调大，默认未动）。
- 宝箱权重：Chest prefab Inspector 四个 Chance 字段（序列化可调，默认 25/30/20/25）。
- 磁吸/滑散手感：CoinDrop.cs 顶部常量（MagnetRange 1.95 / PickupRange 0.45 / Lifetime 12s）。

## 6. 红线遵守

- 分层：CoinDrop/CoinHUD 纯表现+数据入口（AddCoins 走 PlayerStats 唯一钱包），无战斗逻辑侵入。
- 事件配对：CoinHUD 换绑/销毁均退订 OnCoinsChanged ✓。
- 每帧零 GC：CoinDrop.Update 无分配（player 缓存、仅 null 时 Find）；HUD 0.5s 轮询仅 Tag 查找一次。
- R2：双场景 LF 手术有备份（`_backup/*.bak_v1.1.3_*`）、锚点唯一断言、行尾零污染；R4：改场景后需 Reload 再 Play。
- R1：零 prefab/组件新增到场景实例，HUD 自装规避。
