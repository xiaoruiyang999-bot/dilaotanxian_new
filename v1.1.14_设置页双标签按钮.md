# v1.1.14 设置页双标签按钮（音频/控制）

## 素材

源：`E:\WeGameApps\The animation of Unity\狼人\设置菜单按钮_两张\button_{audio,controls}_settings.png`
（1282×277，4.63:1，与暂停三键同源规格）。双份入库 `Assets/UI/PauseMenu/` + `Assets/Resources/UI/PauseMenu/`，
Single/Bilinear/不压缩/PPU1/Border0（v1.1.10 规范；meta 从同目录现有按键克隆，修正负数内部 ID 与双 spriteID 模板坑）。

## 功能（PausePanel）

设置面板（点"设置"展开的右侧 390×390 面板）改为**双标签页**：

- **音频**（默认）：原音量双滑条 + 收起提示；
- **控制**（新增）：v0.7.5 键位速查只读展示（WASD/左键/E/F/Q/R/C/T/Esc 九行）。
- 两张标签按钮走 `CreateMenuButton` 统一模式：完整功能图 + **ColorTint 亮度三态
  （Normal 0.88 / Hover 1.00 / Pressed 0.65，Selected 0.96 / Disabled 0.45α0.75，fade 0.08s）**，
  断链回退三态石板+文字；尺寸参数化 184×40（preserveAspect 保比例），主菜单键仍 520×110。
- `ShowSettingsTab(bool)` 切换内容根；`Close()` 同步置空 audio/controls 根（防悬挂引用，v1.1.12 规范延续）。

## 验收

1. 编译无错 → Esc → 点"设置"：右侧面板顶部两张标签键；
2. 悬停恢复原亮度、按下压暗；点"控制"切换键位表，点"音频"切回滑条；
3. 滑条调音量即时生效且持久化；再点"设置"收起整个面板。

## 关联

v1.1.10 按键规范 / v1.1.12 GraphicRaycaster（已具备，标签按钮直接可点）。
后续做"按键改键"功能时，控制页从只读展示升级为可交互列表，按钮图无需更换。
