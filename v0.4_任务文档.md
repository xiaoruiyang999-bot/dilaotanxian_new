# v0.4 任务文档：Enemy敌人系统架构

## 版本信息
- **版本号**：v0.4（含v0.4.1木桩 + v0.4.2实装敌人）
- **版本名称**：Enemy敌人系统架构
- **美术策略**：程序员美术（纯色几何图形）
- **目标**：建立完整的模块化敌人架构，含AI状态机、攻击系统、公共框架
- **前置版本**：v0.3（玩家角色系统 + 扇形攻击 + 敌人世界空间血条已完成）

> **v0.4.1**：搭建架构 + 配置TrainingDummy木桩（无AI，仅测试玩家攻击）  
> **v0.4.2**：在场景中直接部署一个**启用了AI和Combat的完整敌人**，验证状态机与战斗交互。

---

## 项目当前状态（v0.3完成度验证）

### ✅ 已完成的v0.3资产（保留复用）
| 资产 | 路径 | 处理方式 |
|------|------|----------|
| 场景 | `Scenes/v0_3_CharacterSystem.unity` | 复制为v0.4基础 |
| 玩家系统 | `PlayerController/Stats/Health/Combat` + `WarriorAttack` | **保留不变** |
| 相机 | `CameraFollow.cs` | **保留不变** |
| Input System | `PlayerInputActions` | **保留不变** |
| 小屋地图 | Grid/Floor/Walls | **保留不变** |
| 玩家UI | `PlayerUI.cs` + Canvas | **保留不变** |
| 世界空间血条 | `WorldSpaceHealthBar.cs` | **修改适配**（支持EnemyHealth） |

### ❌ 将被删除/替换的v0.3资产
| 资产 | 路径 | 处理方式 |
|------|------|----------|
| Enemy.cs | `Scripts/v0_3/Enemy.cs` | **删除**（替换为EnemyController+EnemyStats+EnemyHealth+EnemyAI+EnemyCombat） |
| v0.3旧Enemy_Test | 场景中旧配置 | **移除组件重新配置** |

---

## 总体目标

在v0.3玩家系统的基础上，建立**完整的敌人模块化架构**，同时创建**伤害测试木桩（TrainingDummy）**用于测试玩家攻击：

1. **EnemyController**（门面）：持有所有子组件，提供移动/停止/面向接口，敌人版的"PlayerController"
2. **EnemyStats**（属性）：maxHP、移速、检测范围、攻击范围等配置
3. **EnemyHealth**（生命）：TakeDamage/Heal/Die + **OnTakeDamage事件通知AI** + OnDeath事件
4. **EnemyAI**（核心状态机）：**Patrol → Chase → Attack → ReturnToPatrol**
5. **EnemyCombat**（攻击管理）：攻击CD + 是否能攻击 + 调用攻击（预留近战/远程/Boss技能扩展）
6. **TrainingDummy**（伤害测试木桩）：不移动、不攻击、可受伤、闪烁反馈、**血条归零后自动重置**，用于测试玩家攻击伤害
7. **公共框架**（先建立不实现）：DamageSystem、DetectionSystem、PatrolSystem
8. **保留**：敌人头顶世界空间血条（WorldSpaceHealthBar适配EnemyHealth）

> 💡 **Enemy架构脚本（Stats/Health/AI/Combat/Controller）全部编写保存，但v0.4场景中不挂载AI和Combat组件，只作为木桩使用。后续v0.5+直接复用。**

---

## 架构设计

### Enemy组件依赖图

```
Enemy (GameObject: 红色方块)
├── SpriteRenderer (红色)
├── Rigidbody2D (Dynamic, Gravity=0, FreezeRotation)
├── BoxCollider2D
├── CircleCollider2D (可选，检测范围可视化)
│
├── EnemyController ──────────────┐
│   • MoveTowards(dir)           │ 门面
│   • StopMoving()               │ 供EnemyAI调用
│   • FaceTowards(dir)           │
│   • 持有Stats/Health/AI/Combat │
│                                 ▼
├── EnemyStats ←──────────────────┤
│   • maxHP / moveSpeed          │ 属性配置
│   • detectionRange             │
│   • attackRange                │
│                                 ▼
├── EnemyHealth ←─────────────────┤
│   • TakeDamage / Heal / Die    │ 生命管理
│   • OnTakeDamage → 通知AI      │
│   • OnDeath → 销毁/变灰        │
│   • OnHealthChanged → 血条     │
│                                 ▼
├── EnemyAI ←─────────────────────┤
│   • Patrol（巡逻）             │ 核心状态机
│   • Chase（追击）              │
│   • Attack（攻击）             │
│   • ReturnToPatrol（返回）     │
│                                 ▼
├── EnemyCombat ←─────────────────┤
│   • TryAttack(target)          │ 攻击管理
│   • 冷却计时                   │
│   • 预留：近战/远程/Boss技能   │
│                                 │
└── WorldSpaceHealthBar ←─────────┘
    • 监听EnemyHealth.OnHealthChanged
    • 头顶显示深灰背景+红色填充
```

### EnemyAI状态机设计（核心）

```
                    [检测到玩家进入detectionRange]
    ┌──────────┐  ─────────────────────────────────→  ┌──────────┐
    │  Patrol  │                                       │  Chase   │
    │ （巡逻） │  ←─────────────────────────────────   │ （追击） │
    └──────────┘     [到达巡逻原点]                    └────┬─────┘
         ↑                                                │
         │                                                │ [距离 <= attackRange]
         │                                                ▼
         │                                          ┌──────────┐
         │                                          │  Attack  │
         │                                          │ （攻击） │
         │                                          └────┬─────┘
         │                                               │ [距离 > attackRange]
         │                                               │
         │          [玩家远离 > losePlayerRange]         │
         └───────────────────────────────────────────────┘
```

**状态说明**：
| 状态 | 行为 | 转换条件 |
|------|------|----------|
| **Patrol** | 在原点附近徘徊（或静止） | 检测到玩家 → Chase |
| **Chase** | 向玩家方向移动 | 进入攻击范围 → Attack；玩家丢失 → ReturnToPatrol |
| **Attack** | 停止移动，面向玩家，尝试攻击 | 玩家跑出攻击范围 → Chase |
| **ReturnToPatrol** | 向巡逻原点移动 | 到达原点 → Patrol |

---

## 技术选型确认

| 决策项 | 选定方案 | 说明 |
|--------|----------|------|
| AI状态机 | **枚举 + switch状态机** | Patrol/Chase/Attack/ReturnToPatrol四状态 |
| 移动驱动 | **Rigidbody2D.velocity** | EnemyController提供接口，EnemyAI调用 |
| 检测方式 | **距离判定（Vector2.Distance）** | 先用距离，后续公共框架DetectionSystem扩展视线/听觉 |
| 巡逻 | **固定原点 + 随机偏移** | 敌人生成位置=巡逻原点，后续PatrolSystem扩展路径点 |
| 攻击 | **EnemyCombat.TryAttack(IDamageable)** | 预留近战/远程/Boss技能接口 |
| 伤害接口 | **IDamageable（已有）** | 玩家Health、敌人EnemyHealth都实现 |
| 血条 | **WorldSpaceHealthBar（修改适配）** | 支持Health（玩家）和EnemyHealth（敌人） |

---

## 任务清单

---

### 任务0：场景准备与文件夹创建
**目标**：从v0.3复制场景，创建v0.4脚本文件夹和公共框架文件夹。

**步骤**：
1. 复制场景：`Ctrl+D` `v0_3_CharacterSystem.unity` → `v0_4_EnemySystem.unity`
2. 双击打开 `v0_4_EnemySystem.unity`
3. 创建文件夹：
   - `Assets/Scripts/v0_4/`
   - `Assets/Scripts/Framework/`（公共框架文件夹）
4. 在Hierarchy中找到 `Enemy_Test`：
   - **移除** `Enemy.cs` 组件（v0.3旧脚本）
   - **移除** `Health.cs` 组件（旧版）
   - 保留：SpriteRenderer、Rigidbody2D、BoxCollider2D、WorldSpaceHealthBar（稍后重新配置）
   - **重命名**为 `TrainingDummy`

**验收标准**：
- [ ] `v0_4_EnemySystem.unity` 已创建并打开
- [ ] `Scripts/v0_4/` 和 `Scripts/Framework/` 文件夹存在
- [ ] `Enemy_Test` 上已移除Enemy.cs和旧Health.cs
- [ ] Console无红色报错

---

### 任务1：建立公共框架（先建接口，不实现细节）
**目标**：创建三个公共框架脚本，定义基础结构，后续版本填充实现。

**文件**：`Assets/Scripts/Framework/DamageSystem.cs`
```csharp
using UnityEngine;

/// <summary>
/// 公共伤害系统。
/// TODO v0.5+: 实现统一伤害计算（护甲穿透、暴击率、元素伤害、伤害飘字等）
/// </summary>
public static class DamageSystem
{
    /// <summary>
    /// 应用伤害。当前为直接传递，后续加入伤害计算逻辑。
    /// </summary>
    public static void ApplyDamage(IDamageable target, float rawDamage)
    {
        // TODO: 后续加入护甲减伤、暴击判定、抗性计算等
        target.TakeDamage(rawDamage);
    }

    /// <summary>
    /// 计算最终伤害。当前直接返回原始值，后续加入公式。
    /// </summary>
    public static float CalculateFinalDamage(float rawDamage, float armor, float resistance)
    {
        // TODO: 实现伤害公式，例如：final = rawDamage * (1 - armor/(armor+100))
        return rawDamage;
    }
}
```

**文件**：`Assets/Scripts/Framework/DetectionSystem.cs`
```csharp
using UnityEngine;

/// <summary>
/// 公共检测系统。
/// TODO v0.5+: 实现视线检测（Raycast障碍物遮挡）、听觉检测（玩家噪音）、锥形视野等
/// </summary>
public static class DetectionSystem
{
    /// <summary>
    /// 简单距离检测。当前仅用距离判定，后续扩展视线/听觉。
    /// </summary>
    public static bool IsInRange(Vector2 from, Vector2 to, float range)
    {
        return Vector2.Distance(from, to) <= range;
    }

    /// <summary>
    /// 检测目标是否在扇形视野内。
    /// TODO: 实现锥形视野判定（用于远程敌人、Boss等）
    /// </summary>
    public static bool IsInFieldOfView(Vector2 origin, Vector2 forward, Vector2 target, float range, float angle)
    {
        if (!IsInRange(origin, target, range)) return false;
        float halfAngle = angle * 0.5f;
        float targetAngle = Vector2.Angle(forward, target - origin);
        return targetAngle <= halfAngle;
    }
}
```

**文件**：`Assets/Scripts/Framework/PatrolSystem.cs`
```csharp
using UnityEngine;

/// <summary>
/// 公共巡逻系统。
/// TODO v0.5+: 实现路径点巡逻（Waypoint）、区域随机巡逻、巡逻路径可视化Gizmos等
/// </summary>
public static class PatrolSystem
{
    /// <summary>
    /// 获取随机巡逻点（以原点为中心，半径内随机）。
    /// </summary>
    public static Vector2 GetRandomPatrolPoint(Vector2 origin, float radius)
    {
        return origin + Random.insideUnitCircle * radius;
    }

    /// <summary>
    /// 获取圆形巡逻边界上的一个点（用于固定半径巡逻）。
    /// </summary>
    public static Vector2 GetPatrolPointOnCircle(Vector2 origin, float radius, float angleDegrees)
    {
        float rad = angleDegrees * Mathf.Deg2Rad;
        return origin + new Vector2(Mathf.Cos(rad), Mathf.Sin(rad)) * radius;
    }
}
```

**创建步骤**：
1. 创建文件夹 `Assets/Scripts/Framework/`
2. 创建三个脚本，替换为上述代码
3. 保存

**验收标准**：
- [ ] `DamageSystem.cs`、`DetectionSystem.cs`、`PatrolSystem.cs` 存在
- [ ] 三个脚本编译无报错（虽然是空壳框架）
- [ ] 包含 `TODO` 注释标记后续实现点

---

### 任务2：修改WorldSpaceHealthBar.cs（适配EnemyHealth）
**目标**：让WorldSpaceHealthBar同时支持Health（玩家/通用）和EnemyHealth（敌人）。

**文件**：`Assets/Scripts/v0_3/WorldSpaceHealthBar.cs`（修改）
> 在v0.3基础上修改，添加EnemyHealth字段支持。

```csharp
using UnityEngine;
using UnityEngine.UI;

/// <summary>
/// 世界空间血条。挂载在需要显示血条的物体上。
/// v0.4修改：支持Health（玩家/通用）和EnemyHealth（敌人）。
/// v0.4.2修改：引入HealthBarAnchor，血条不再跟随敌人旋转，始终保持水平。
/// </summary>
public class WorldSpaceHealthBar : MonoBehaviour
{
    [Header("血条锚点")]
    [Tooltip("血条将挂载到这个锚点下。如果未指定，自动查找名为'HealthBarAnchor'的子物体；若仍找不到，则使用当前Transform。")]
    [SerializeField] private Transform healthBarAnchor;

    [Header("数据源（二选一）")]
    [SerializeField] private Health playerHealth;      // 给玩家或有Health的对象用
    [SerializeField] private EnemyHealth enemyHealth;  // 给有EnemyHealth的敌人用

    [Header("血条外观")]
    [SerializeField] private Sprite backgroundSprite;
    [SerializeField] private Color backgroundColor = new Color(0.2f, 0.2f, 0.2f, 1f);
    [SerializeField] private Color fillColor = new Color(0.9f, 0.1f, 0.1f, 1f);

    [Header("血条尺寸")]
    [SerializeField] private float barWidth = 0.8f;
    [SerializeField] private float barHeight = 0.12f;

    private Transform canvasTransform;
    private Image fillImage;

    void Awake()
    {
        // 1. 确定血条锚点
        if (healthBarAnchor == null)
        {
            healthBarAnchor = transform.Find("HealthBarAnchor");
        }
        if (healthBarAnchor == null)
        {
            healthBarAnchor = transform;
        }

        // 2. 自动查找数据源（如果没在Inspector中设置）
        if (playerHealth == null && enemyHealth == null)
        {
            playerHealth = GetComponent<Health>();
            enemyHealth = GetComponent<EnemyHealth>();
        }

        // 至少需要一个数据源
        if (playerHealth == null && enemyHealth == null)
        {
            Debug.LogWarning($"[WorldSpaceHealthBar] {gameObject.name} 上未找到Health或EnemyHealth组件！");
            enabled = false;
            return;
        }

        CreateHealthBar();
    }

    void OnEnable()
    {
        if (playerHealth != null)
            playerHealth.OnHealthChanged += OnHealthChanged;
        else if (enemyHealth != null)
            enemyHealth.OnHealthChanged += OnHealthChanged;
    }

    void OnDisable()
    {
        if (playerHealth != null)
            playerHealth.OnHealthChanged -= OnHealthChanged;
        else if (enemyHealth != null)
            enemyHealth.OnHealthChanged -= OnHealthChanged;
    }

    void LateUpdate()
    {
        if (canvasTransform != null && healthBarAnchor != null)
        {
            // 血条始终位于锚点位置（不跟随敌人旋转）
            canvasTransform.position = healthBarAnchor.position;
            canvasTransform.rotation = Quaternion.identity;
        }
    }

    private void CreateHealthBar()
    {
        GameObject canvasGo = new GameObject("HealthBarCanvas");
        canvasGo.transform.SetParent(healthBarAnchor);
        canvasTransform = canvasGo.transform;
        canvasTransform.localPosition = Vector3.zero;
        canvasTransform.localRotation = Quaternion.identity;

        Canvas canvas = canvasGo.AddComponent<Canvas>();
        canvas.renderMode = RenderMode.WorldSpace;
        canvas.sortingOrder = 10;

        CanvasScaler scaler = canvasGo.AddComponent<CanvasScaler>();
        scaler.dynamicPixelsPerUnit = 100f;

        // 背景
        GameObject bgGo = new GameObject("Background");
        bgGo.transform.SetParent(canvasGo.transform, false);
        Image bgImage = bgGo.AddComponent<Image>();
        bgImage.color = backgroundColor;
        if (backgroundSprite != null) bgImage.sprite = backgroundSprite;
        bgImage.rectTransform.sizeDelta = new Vector2(barWidth * 100f, barHeight * 100f);

        // 填充
        GameObject fillGo = new GameObject("Fill");
        fillGo.transform.SetParent(bgGo.transform, false);
        fillImage = fillGo.AddComponent<Image>();
        fillImage.color = fillColor;
        if (backgroundSprite != null) fillImage.sprite = backgroundSprite;
        fillImage.type = Image.Type.Filled;
        fillImage.fillMethod = Image.FillMethod.Horizontal;
        fillImage.fillOrigin = 0;
        fillImage.fillAmount = 1f;

        RectTransform fillRect = fillImage.rectTransform;
        fillRect.anchorMin = Vector2.zero;
        fillRect.anchorMax = Vector2.one;
        fillRect.offsetMin = Vector2.zero;
        fillRect.offsetMax = Vector2.zero;
    }

    private void OnHealthChanged(float current, float max)
    {
        if (fillImage != null)
            fillImage.fillAmount = max > 0 ? current / max : 0f;
    }
}
```

> ⚠️ 注意：此修改需要 `EnemyHealth.cs` 已经存在（或至少有 `OnHealthChanged` 事件）。如果编译报错，可以先注释掉 `enemyHealth` 相关代码，等EnemyHealth创建后再启用。

**验收标准**：
- [ ] WorldSpaceHealthBar.cs 已修改，添加 `enemyHealth` 字段
- [ ] `OnEnable`/`OnDisable` 中同时处理两种数据源
- [ ] 编译无报错

---

### 任务3：编写EnemyStats.cs（敌人属性配置）
**目标**：集中管理敌人所有数值属性。

**文件**：`Assets/Scripts/v0_4/EnemyStats.cs`
```csharp
using UnityEngine;

/// <summary>
/// 敌人属性配置。与PlayerStats对应，但针对敌人AI需求设计。
/// </summary>
public class EnemyStats : MonoBehaviour
{
    [Header("基础属性")]
    [SerializeField] private float maxHealth = 3f;
    [SerializeField] private float moveSpeed = 3f;

    [Header("AI检测")]
    [SerializeField] private float detectionRange = 5f;     // 发现玩家的距离
    [SerializeField] private float losePlayerRange = 8f;    // 丢失玩家的距离（需大于detectionRange）

    [Header("攻击")]
    [SerializeField] private float attackRange = 1.2f;      // 攻击距离
    [SerializeField] private float attackDamage = 1f;       // 每次攻击伤害
    [SerializeField] private float attackCooldown = 1f;     // 攻击冷却（秒）

    // 公开访问
    public float MaxHealth => maxHealth;
    public float MoveSpeed => moveSpeed;
    public float DetectionRange => detectionRange;
    public float LosePlayerRange => losePlayerRange;
    public float AttackRange => attackRange;
    public float AttackDamage => attackDamage;
    public float AttackCooldown => attackCooldown;

    void OnValidate()
    {
        // 确保丢失距离大于检测距离
        if (losePlayerRange < detectionRange)
            losePlayerRange = detectionRange + 1f;
    }
}
```

**验收标准**：
- [ ] `EnemyStats.cs` 存在，编译无报错
- [ ] 包含：maxHealth, moveSpeed, detectionRange, losePlayerRange, attackRange, attackDamage, attackCooldown

---

### 任务4：编写EnemyHealth.cs（敌人生命管理）
**目标**：管理敌人生命值，**被攻击时通知EnemyAI**，死亡时触发事件。

**文件**：`Assets/Scripts/v0_4/EnemyHealth.cs`
```csharp
using UnityEngine;

/// <summary>
/// 敌人生命管理。实现IDamageable，提供受伤/治疗/死亡，被攻击时通知EnemyAI。
/// </summary>
public class EnemyHealth : MonoBehaviour, IDamageable
{
    [SerializeField] private float maxHealth = 3f;

    public float CurrentHealth { get; private set; }
    public float MaxHealth => maxHealth;
    public bool IsDead { get; private set; }

    // 事件
    public System.Action<float, float> OnHealthChanged; // (current, max) 供血条监听
    public System.Action OnDeath;                        // 死亡通知
    public System.Action OnTakeDamage;                   // 被攻击时通知AI（关键！）

    void Awake()
    {
        CurrentHealth = maxHealth;
        IsDead = false;
    }

    /// <summary>实现IDamageable接口</summary>
    public void TakeDamage(float damage)
    {
        if (IsDead) return;

        CurrentHealth = Mathf.Max(CurrentHealth - damage, 0f);
        OnHealthChanged?.Invoke(CurrentHealth, maxHealth);
        OnTakeDamage?.Invoke(); // 通知AI：我被打了！

        if (CurrentHealth <= 0f)
        {
            Die();
        }
    }

    public void Heal(float amount)
    {
        if (IsDead) return;
        CurrentHealth = Mathf.Min(CurrentHealth + amount, maxHealth);
        OnHealthChanged?.Invoke(CurrentHealth, maxHealth);
    }

    public void Die()
    {
        if (IsDead) return;
        IsDead = true;
        OnDeath?.Invoke();
    }

    public void ResetHealth()
    {
        CurrentHealth = maxHealth;
        IsDead = false;
        OnHealthChanged?.Invoke(CurrentHealth, maxHealth);
    }
}
```

**验收标准**：
- [ ] `EnemyHealth.cs` 存在，实现 `IDamageable`
- [ ] 包含 `OnTakeDamage` 事件（被攻击时通知AI）
- [ ] 包含 `OnDeath` 和 `OnHealthChanged` 事件
- [ ] 无自动恢复逻辑（敌人目前不恢复）

---

### 任务5：编写EnemyCombat.cs（敌人攻击管理）
**目标**：管理攻击冷却，提供攻击接口，预留近战/远程/Boss技能扩展。

**文件**：`Assets/Scripts/v0_4/EnemyCombat.cs`
```csharp
using UnityEngine;

/// <summary>
/// 敌人攻击管理。负责冷却计时、距离判定、执行攻击。
/// 后续扩展：近战/远程/Boss技能通过不同攻击模式实现。
/// </summary>
public class EnemyCombat : MonoBehaviour
{
    private EnemyStats stats;
    private float cooldownTimer = 0f;
    private bool canAttack = true;

    void Awake()
    {
        stats = GetComponent<EnemyStats>();
    }

    void Update()
    {
        if (!canAttack)
        {
            cooldownTimer -= Time.deltaTime;
            if (cooldownTimer <= 0f)
                canAttack = true;
        }
    }

    /// <summary>
    /// 检查目标是否在攻击范围内。
    /// </summary>
    public bool IsInAttackRange(Transform target)
    {
        if (target == null || stats == null) return false;
        return Vector2.Distance(transform.position, target.position) <= stats.AttackRange;
    }

    /// <summary>
    /// 尝试攻击目标。冷却好且范围内才能攻击。
    /// </summary>
    public bool TryAttack(IDamageable target)
    {
        if (!canAttack) return false;
        if (target == null) return false;

        target.TakeDamage(stats.AttackDamage);
        canAttack = false;
        cooldownTimer = stats.AttackCooldown;
        return true;
    }

    /// <summary>
    /// 是否能攻击（冷却已好）。
    /// </summary>
    public bool CanAttack => canAttack;
}
```

**验收标准**：
- [ ] `EnemyCombat.cs` 存在，编译无报错
- [ ] `TryAttack` 包含冷却检查 + 伤害传递
- [ ] `IsInAttackRange` 使用EnemyStats的AttackRange

---

### 任务6：编写EnemyAI.cs（核心状态机）
**目标**：实现敌人AI的核心状态流转：Patrol → Chase → Attack → ReturnToPatrol。

**文件**：`Assets/Scripts/v0_4/EnemyAI.cs`
```csharp
using UnityEngine;

/// <summary>
/// 敌人AI状态机核心。管理四个状态：Patrol/Chase/Attack/ReturnToPatrol。
/// 被攻击时（OnTakeDamage）强制切换到Chase（发现玩家）。
/// </summary>
public class EnemyAI : MonoBehaviour
{
    public enum State { Patrol, Chase, Attack, ReturnToPatrol }

    [Header("AI配置")]
    [SerializeField] private float patrolRadius = 2f;       // 巡逻半径
    [SerializeField] private float patrolWaitTime = 2f;     // 巡逻点间等待时间

    // 组件引用
    private EnemyController controller;
    private EnemyStats stats;
    private EnemyCombat combat;
    private EnemyHealth health;

    // 目标与状态
    private Transform target;           // 玩家
    private Vector3 patrolOrigin;       // 巡逻原点
    private State currentState;
    private float patrolWaitTimer = 0f;
    private Vector3 currentPatrolTarget;

    void Awake()
    {
        controller = GetComponent<EnemyController>();
        stats = GetComponent<EnemyStats>();
        combat = GetComponent<EnemyCombat>();
        health = GetComponent<EnemyHealth>();

        patrolOrigin = transform.position;
        currentPatrolTarget = patrolOrigin;
        currentState = State.Patrol;

        // 查找玩家
        GameObject player = GameObject.FindWithTag("Player");
        if (player != null) target = player.transform;

        // 监听被攻击事件：被打了立刻发现玩家
        if (health != null)
            health.OnTakeDamage += OnDamaged;
    }

    void OnDestroy()
    {
        if (health != null)
            health.OnTakeDamage -= OnDamaged;
    }

    void Update()
    {
        if (target == null) return;
        if (health != null && health.IsDead) return;

        float distToTarget = Vector2.Distance(transform.position, target.position);
        float distToOrigin = Vector2.Distance(transform.position, patrolOrigin);

        switch (currentState)
        {
            case State.Patrol:
                UpdatePatrol(distToTarget);
                break;
            case State.Chase:
                UpdateChase(distToTarget);
                break;
            case State.Attack:
                UpdateAttack(distToTarget);
                break;
            case State.ReturnToPatrol:
                UpdateReturnToPatrol(distToOrigin);
                break;
        }
    }

    // ========== 被攻击回调 ==========
    private void OnDamaged()
    {
        // 被攻击时，如果不在Chase/Attack状态，切换到Chase（发现攻击者）
        if (currentState == State.Patrol || currentState == State.ReturnToPatrol)
        {
            ChangeState(State.Chase);
        }
    }

    // ========== Patrol（巡逻） ==========
    private void UpdatePatrol(float distToTarget)
    {
        controller.StopMoving();

        // 等待计时
        patrolWaitTimer -= Time.deltaTime;
        if (patrolWaitTimer <= 0f)
        {
            // 选新巡逻点
            currentPatrolTarget = PatrolSystem.GetRandomPatrolPoint(
                patrolOrigin, patrolRadius);
            patrolWaitTimer = patrolWaitTime;
        }

        // 向巡逻点移动（简单版：直接用MoveTowards）
        Vector2 dir = (currentPatrolTarget - transform.position).normalized;
        if (Vector2.Distance(transform.position, currentPatrolTarget) > 0.2f)
        {
            controller.MoveTowards(dir);
            controller.FaceTowards(dir);
        }

        // 检测玩家
        if (distToTarget <= stats.DetectionRange)
        {
            ChangeState(State.Chase);
        }
    }

    // ========== Chase（追击） ==========
    private void UpdateChase(float distToTarget)
    {
        Vector2 dir = (target.position - transform.position).normalized;
        controller.MoveTowards(dir);
        controller.FaceTowards(dir);

        // 进入攻击范围
        if (combat.IsInAttackRange(target))
        {
            ChangeState(State.Attack);
        }
        // 玩家跑太远，丢失
        else if (distToTarget > stats.LosePlayerRange)
        {
            ChangeState(State.ReturnToPatrol);
        }
    }

    // ========== Attack（攻击） ==========
    private void UpdateAttack(float distToTarget)
    {
        controller.StopMoving();

        // 面向玩家
        Vector2 dir = (target.position - transform.position).normalized;
        controller.FaceTowards(dir);

        // 尝试攻击
        if (combat.IsInAttackRange(target))
        {
            // 获取玩家的IDamageable
            if (target.TryGetComponent<IDamageable>(out var damageable))
            {
                combat.TryAttack(damageable);
            }
        }

        // 玩家跑出攻击范围
        if (!combat.IsInAttackRange(target))
        {
            ChangeState(State.Chase);
        }
    }

    // ========== ReturnToPatrol（返回巡逻点） ==========
    private void UpdateReturnToPatrol(float distToOrigin)
    {
        Vector2 dir = (patrolOrigin - transform.position).normalized;
        controller.MoveTowards(dir);
        controller.FaceTowards(dir);

        // 回到原点
        if (distToOrigin <= 0.3f)
        {
            controller.StopMoving();
            ChangeState(State.Patrol);
        }

        // 返回途中又发现玩家
        float distToTarget = Vector2.Distance(transform.position, target.position);
        if (distToTarget <= stats.DetectionRange)
        {
            ChangeState(State.Chase);
        }
    }

    // ========== 状态切换 ==========
    private void ChangeState(State newState)
    {
        if (currentState == newState) return;

        // 退出旧状态
        switch (currentState)
        {
            case State.Patrol:
                patrolWaitTimer = 0f;
                break;
            case State.Chase:
                break;
            case State.Attack:
                break;
            case State.ReturnToPatrol:
                break;
        }

        currentState = newState;

        // 进入新状态
        switch (newState)
        {
            case State.Patrol:
                currentPatrolTarget = patrolOrigin;
                patrolWaitTimer = 0f;
                break;
            case State.Chase:
                break;
            case State.Attack:
                break;
            case State.ReturnToPatrol:
                break;
        }
    }

    public State CurrentState => currentState;
}
```

**验收标准**：
- [ ] `EnemyAI.cs` 存在，编译无报错
- [ ] 四个状态：`Patrol`、`Chase`、`Attack`、`ReturnToPatrol`
- [ ] `OnTakeDamage` 强制切换到Chase（被攻击时发现玩家）
- [ ] `Update` 中正确流转状态

---

### 任务7：编写EnemyController.cs（敌人门面）
**目标**：整合所有敌人子组件，提供移动/停止/面向接口供EnemyAI调用。

**文件**：`Assets/Scripts/v0_4/EnemyController.cs`
```csharp
using UnityEngine;

/// <summary>
/// 敌人门面控制器。持有所有子组件引用，提供统一的移动控制接口。
/// 类似PlayerController，但输入由EnemyAI驱动而非Input System。
/// </summary>
[RequireComponent(typeof(Rigidbody2D))]
[RequireComponent(typeof(EnemyStats))]
[RequireComponent(typeof(EnemyHealth))]
[RequireComponent(typeof(EnemyAI))]
[RequireComponent(typeof(EnemyCombat))]
public class EnemyController : MonoBehaviour
{
    private Rigidbody2D rb;
    private EnemyStats stats;
    private EnemyHealth health;
    private EnemyAI ai;
    private EnemyCombat combat;
    private SpriteRenderer sr;

    [Header("受伤反馈")]
    [SerializeField] private float hitFlashDuration = 0.15f;
    [SerializeField] private Color hitFlashColor = Color.white;

    void Awake()
    {
        rb = GetComponent<Rigidbody2D>();
        stats = GetComponent<EnemyStats>();
        health = GetComponent<EnemyHealth>();
        ai = GetComponent<EnemyAI>();
        combat = GetComponent<EnemyCombat>();
        sr = GetComponent<SpriteRenderer>();

        // 监听事件
        if (health != null)
        {
            health.OnDeath += OnEnemyDeath;
            health.OnTakeDamage += OnHitFlash; // 受伤闪烁
        }
    }

    void OnDestroy()
    {
        if (health != null)
        {
            health.OnDeath -= OnEnemyDeath;
            health.OnTakeDamage -= OnHitFlash;
        }
    }

    // ========== 移动接口（供EnemyAI调用）==========

    /// <summary>向指定方向移动</summary>
    public void MoveTowards(Vector2 direction)
    {
        if (stats == null || rb == null) return;
        rb.velocity = direction.normalized * stats.MoveSpeed;
    }

    /// <summary>停止移动</summary>
    public void StopMoving()
    {
        if (rb != null) rb.velocity = Vector2.zero;
    }

    /// <summary>面向指定方向</summary>
    public void FaceTowards(Vector2 direction)
    {
        if (direction.sqrMagnitude < 0.001f) return;
        float angle = Mathf.Atan2(direction.y, direction.x) * Mathf.Rad2Deg;
        transform.rotation = Quaternion.Euler(0, 0, angle);
    }

    // ========== 受伤反馈 ==========

    private void OnHitFlash()
    {
        if (sr == null) return;
        StartCoroutine(HitFlashCoroutine());
    }

    private System.Collections.IEnumerator HitFlashCoroutine()
    {
        Color original = sr.color;
        sr.color = hitFlashColor;
        yield return new WaitForSeconds(hitFlashDuration);
        // 如果还没死，恢复颜色
        if (health != null && !health.IsDead)
            sr.color = original;
    }

    // ========== 死亡处理 ==========

    private void OnEnemyDeath()
    {
        StopMoving();
        // 变灰
        if (sr != null) sr.color = Color.gray;

        // 禁用AI和碰撞
        if (ai != null) ai.enabled = false;
        if (combat != null) combat.enabled = false;
        if (TryGetComponent<Collider2D>(out var col)) col.enabled = false;

        // 延迟销毁
        Destroy(gameObject, 0.5f);
    }

    // ========== 外部访问接口 ==========

    public EnemyStats GetStats() => stats;
    public EnemyHealth GetHealth() => health;
    public EnemyAI GetAI() => ai;
    public EnemyCombat GetCombat() => combat;
}
```

**验收标准**：
- [ ] `EnemyController.cs` 存在，编译无报错
- [ ] `MoveTowards` / `StopMoving` / `FaceTowards` 三个接口
- [ ] `OnEnemyDeath` 中停止移动、变灰、禁用组件、延迟销毁

---

### 任务8：配置TrainingDummy对象（伤害测试木桩）
**目标**：将v0.3的旧Enemy_Test升级为**TrainingDummy（伤害测试木桩）**，用于测试玩家攻击。

**TrainingDummy特性**：
- 不移动、不攻击、无AI（禁用EnemyAI和EnemyCombat）
- 可受伤，头顶显示世界空间血条
- 受伤时闪烁白色反馈
- **血条归零后3秒自动重置回满**（方便反复测试伤害）
- 用不同颜色（棕色/灰色）区别于红色敌人

**步骤**：
1. 选中Hierarchy中的 `Enemy_Test`，**重命名**为 `TrainingDummy`
2. 确认已移除：`Enemy.cs`（v0.3旧脚本）、`Health.cs`（旧版）
3. 挂载组件（按顺序）：
   - `EnemyStats.cs`（maxHealth=10，比敌人更多血方便测试）
   - `EnemyHealth.cs`
   - `EnemyController.cs`（可选，木桩不需要移动但可保留门面结构）
4. **禁用**以下组件（右键 → 取消勾选，不删除，后续版本启用）：
   - `EnemyAI`（v0.4不启用，后续版本直接勾选启用）
   - `EnemyCombat`（v0.4不启用，后续版本直接勾选启用）
5. 添加 `TrainingDummy.cs` 脚本（见任务8A）
6. 重新配置 `WorldSpaceHealthBar`：
   - 选中 `TrainingDummy`
   - 找到 `WorldSpaceHealthBar` 组件
   - 将 `EnemyHealth` 拖到 `Enemy Health` 字段
   - 确认 `Player Health` 字段为空
7. 修改外观：
   - SpriteRenderer的Color改为棕色/灰色（如 R=0.5, G=0.35, B=0.2），区别于红色敌人
   - SortingOrder保持2

#### 8A. 编写TrainingDummy.cs
**文件**：`Assets/Scripts/v0_4/TrainingDummy.cs`
```csharp
using System.Collections;
using UnityEngine;

/// <summary>
/// 伤害测试木桩。不移动、不攻击，被攻击时闪烁反馈，死亡后自动重置。
/// 用于v0.4测试玩家攻击伤害和敌人架构（EnemyHealth/WorldSpaceHealthBar）。
/// </summary>
[RequireComponent(typeof(EnemyHealth))]
[RequireComponent(typeof(SpriteRenderer))]
public class TrainingDummy : MonoBehaviour
{
    [Header("木桩配置")]
    [SerializeField] private float resetDelay = 3f;         // 死亡后几秒重置
    [SerializeField] private float flashDuration = 0.15f;     // 受伤闪烁时间
    [SerializeField] private Color flashColor = Color.white;  // 闪烁颜色

    private EnemyHealth health;
    private SpriteRenderer sr;
    private Color originalColor;
    private bool isResetting = false;

    void Awake()
    {
        health = GetComponent<EnemyHealth>();
        sr = GetComponent<SpriteRenderer>();
        originalColor = sr.color;

        // 监听受伤和死亡事件
        health.OnTakeDamage += OnHit;
        health.OnDeath += OnDied;
    }

    void OnDestroy()
    {
        if (health != null)
        {
            health.OnTakeDamage -= OnHit;
            health.OnDeath -= OnDied;
        }
    }

    private void OnHit()
    {
        if (isResetting) return;
        StopAllCoroutines();
        StartCoroutine(HitFlashCoroutine());
    }

    private void OnDied()
    {
        if (isResetting) return;
        StartCoroutine(ResetCoroutine());
    }

    private IEnumerator HitFlashCoroutine()
    {
        sr.color = flashColor;
        yield return new WaitForSeconds(flashDuration);
        if (!isResetting) sr.color = originalColor;
    }

    private IEnumerator ResetCoroutine()
    {
        isResetting = true;

        // 变灰表示"死亡"
        sr.color = Color.gray;
        yield return new WaitForSeconds(resetDelay);

        // 重置生命
        health.ResetHealth();
        sr.color = originalColor;
        isResetting = false;

        Debug.Log("[TrainingDummy] 木桩已重置");
    }
}
```

**创建步骤**：
1. 右键 `Scripts/v0_4/` → `C# Script` → `TrainingDummy` → 替换代码
2. 保存后将脚本拖到 `TrainingDummy` 对象上

**TrainingDummy最终组件结构**：
```
TrainingDummy (原Enemy_Test)
  - SpriteRenderer (棕色/灰色方块, SortingOrder=2)
  - Rigidbody2D (Dynamic, Gravity=0, FreezeRotation)
  - BoxCollider2D
  - EnemyStats (maxHealth=10, moveSpeed=0, detectionRange=0, attackRange=0)
  - EnemyHealth (maxHealth=10)
  - EnemyController (可选，保留结构)
  - TrainingDummy (resetDelay=3, flashDuration=0.15)
  - HealthBarAnchor (空物体, 位于敌人头顶上方 y=0.6)
  │   └── WorldSpaceHealthBar (挂载在此，enemyHealth字段引用父对象的EnemyHealth)
  │       └── HealthBarCanvas (WorldSpace)
  │           ├── Background (深灰色)
  │           └── Fill (红色, Filled)
  - [禁用] EnemyAI (保留但取消勾选，后续版本启用)
  - [禁用] EnemyCombat (保留但取消勾选，后续版本启用)
```

> 💡 **HealthBarAnchor**：创建空物体 `HealthBarAnchor` 作为 `TrainingDummy` 的子物体，将其位置设为 `(0, 0.6, 0)`。WorldSpaceHealthBar 将自动查找此锚点，血条挂载到锚点下，始终跟随锚点位置且不跟随敌人旋转。

**验收标准**：
- [ ] `TrainingDummy` 对象存在，棕色/灰色方块
- [ ] 组件：EnemyStats + EnemyHealth + TrainingDummy + HealthBarAnchor + WorldSpaceHealthBar
- [ ] `HealthBarAnchor` 空物体已创建，作为 TrainingDummy 的子物体，位于头顶上方 `(0, 0.6, 0)`
- [ ] `WorldSpaceHealthBar` 的 `Health Bar Anchor` 字段已引用 `HealthBarAnchor`
- [ ] `WorldSpaceHealthBar` 的 `Enemy Health` 字段已引用父对象的 EnemyHealth
- [ ] EnemyAI和EnemyCombat已挂载但**禁用**（取消勾选）
- [ ] 木桩颜色与红色敌人明显区分
- [ ] 敌人旋转时，血条保持水平，不跟随旋转

---

### 任务8.5（v0.4.2）：部署完整敌人进行AI测试
**目标**：在v0.4场景中直接创建一个**启用了AI和Combat的完整敌人**（区别于TrainingDummy），验证Patrol/Chase/Attack/ReturnToPatrol状态机是否正常工作。

> 💡 v0.4.1只测试了木桩（无AI），v0.4.2直接部署一个真敌人，测试完整的敌人行为。

**步骤**：
1. 在Hierarchy中右键 → `Create Empty` → 命名为 `Enemy`
2. 添加 `SpriteRenderer` 组件：
   - Sprite：选择 `WhiteSquare`（64×64 白色方块）
   - Color：红色（R=1, G=0, B=0）
   - SortingOrder：2（与TrainingDummy同层）
3. 添加 `Rigidbody2D` 组件：
   - Body Type：`Dynamic`
   - Gravity Scale：`0`
   - Constraints → Freeze Rotation：✅
4. 添加 `BoxCollider2D` 组件（默认即可）
5. 添加 `CircleCollider2D` 组件：
   - Is Trigger：✅（仅用于检测范围可视化）
   - Radius：`5`（等于detectionRange）
6. 在 `Enemy` 下创建空子物体，命名为 `HealthBarAnchor`，将其位置设为 `(0, 0.6, 0)`（头顶位置，可按不同敌人身高调整）
7. 按顺序添加脚本组件（Unity会自动按RequireComponent补全依赖）：
   - `EnemyStats.cs`
     - maxHealth：`3`
     - moveSpeed：`3`
     - detectionRange：`5`
     - losePlayerRange：`8`
     - attackRange：`1.2`
     - attackDamage：`1`
     - attackCooldown：`1`
   - `EnemyHealth.cs`
   - `EnemyCombat.cs`（启用，勾选组件）
   - `EnemyAI.cs`（启用，勾选组件）
   - `EnemyController.cs`
   - `WorldSpaceHealthBar.cs`
     - 将 `HealthBarAnchor` 拖到 `Health Bar Anchor` 字段
     - 将 `EnemyHealth` 拖到 `Enemy Health` 字段
     - `Player Health` 字段保持为空
8. 将 `Enemy` 对象移动到距离 `Player` 约8~10格的位置（例如位置 (7, 4, 0)）
9. 确认 `Player` 对象的Tag = `"Player"`（EnemyAI通过此Tag查找玩家）

**Enemy最终组件结构**：
```
Enemy (GameObject: 红色方块)
  - SpriteRenderer (红色, SortingOrder=2)
  - Rigidbody2D (Dynamic, Gravity=0, FreezeRotation)
  - BoxCollider2D
  - CircleCollider2D (IsTrigger=true, Radius=5) [可视化检测范围]
  - EnemyStats (maxHealth=3, moveSpeed=3, detectionRange=5, losePlayerRange=8, attackRange=1.2, attackDamage=1, attackCooldown=1)
  - EnemyHealth (maxHealth=3)
  - EnemyCombat (启用)
  - EnemyAI (启用, patrolRadius=2, patrolWaitTime=2)
  - EnemyController
  - HealthBarAnchor (空物体, 位于敌人头顶上方 y=0.6)
  │   └── WorldSpaceHealthBar (挂载在此，healthBarAnchor字段引用HealthBarAnchor，enemyHealth字段引用父对象的EnemyHealth)
  │       └── HealthBarCanvas (WorldSpace)
  │           ├── Background (深灰色)
  │           └── Fill (红色, Filled)
```

**验收标准**：
- [ ] `Enemy` 对象存在，红色方块，位于距离玩家较远的位置
- [ ] 组件完整：EnemyStats + EnemyHealth + EnemyCombat + EnemyAI + EnemyController + HealthBarAnchor + WorldSpaceHealthBar
- [ ] `HealthBarAnchor` 空物体已创建，作为 Enemy 的子物体，位于头顶上方 `(0, 0.6, 0)`
- [ ] `WorldSpaceHealthBar` 的 `Health Bar Anchor` 字段已引用 `HealthBarAnchor`
- [ ] `WorldSpaceHealthBar` 的 `Enemy Health` 字段已引用本对象的 EnemyHealth
- [ ] EnemyAI和EnemyCombat组件**已启用**（与TrainingDummy的区别）
- [ ] Player对象的Tag = `"Player"`
- [ ] 敌人旋转时，血条保持水平，不跟随旋转

---

### 任务9：验证玩家攻击能正确命中木桩
**目标**：确保玩家攻击木桩时，EnemyHealth和WorldSpaceHealthBar正常工作。

**步骤**：
1. 确认 `Player` 对象有Tag = "Player"
2. 确认 `TrainingDummy` 对象的Layer不是 `Enemy`（避免碰撞冲突）
3. 确认 `TrainingDummy` 有 `BoxCollider2D`（用于被OverlapCircle命中）
4. 确认 `TrainingDummy` 有 `EnemyHealth` 且实现 `IDamageable`
5. 运行游戏，走近木桩，鼠标左键攻击

**验收标准**：
- [ ] 玩家攻击命中木桩，木桩闪烁白色
- [ ] 木桩头顶血条减少
- [ ] Console有伤害日志
- [ ] 木桩血条归零后变灰，3秒后自动回满并恢复颜色
- [ ] 木桩始终不移动、不攻击
- [ ] 玩家WASD移动、墙壁碰撞、扇形攻击、鼠标瞄准均正常工作
---

### 任务10：运行测试与验收
**测试步骤（TrainingDummy）**：
1. 点击 **Play** 运行
2. **观察木桩**：TrainingDummy应静止不动，头顶血条满格
3. **攻击木桩**：走近木桩，鼠标左键攻击
4. **受伤反馈**：木桩闪烁白色，头顶血条减少，Console有伤害日志
5. **持续攻击**：连续攻击直到血条归零
6. **自动重置**：木桩变灰，3秒后自动回满血并恢复颜色
7. **重复测试**：再次攻击，确认重置后可再次受伤
8. **验证扇形**：站在木桩侧面/背后攻击，确认只有前方扇形范围内才能命中

**测试步骤（v0.4.2 Enemy）**：
9. **观察Patrol**：敌人应在原点（生成位置）附近徘徊
10. **靠近触发Chase**：控制玩家走进敌人detectionRange（5格），敌人应开始追击
11. **进入Attack范围**：继续靠近到attackRange（1.2格）内，敌人应停止移动并尝试攻击
12. **玩家受伤**：观察玩家血条是否减少，Console是否有伤害日志
13. **离开攻击范围**：玩家后退，敌人应切回Chase继续追
14. **丢失玩家**：玩家跑远超过losePlayerRange（8格），敌人应停止追击，返回巡逻点
15. **返回Patrol**：敌人回到原点后，恢复巡逻状态
16. **被攻击反击**：在Patrol状态时攻击敌人（玩家近战），敌人应立刻切换为Chase追击
17. **敌人死亡**：持续攻击敌人3次，敌人变灰、血条归零、0.5秒后销毁
18. **玩家功能**：WASD移动、墙壁碰撞、鼠标瞄准均正常工作（v0.3功能未损坏）

**验收清单（TrainingDummy）**：
- [ ] TrainingDummy静止不动，头顶血条满格（深灰背景+红色填充）
- [ ] 玩家攻击命中木桩，木桩闪烁白色反馈
- [ ] 木桩头顶血条随受伤减少
- [ ] Console显示伤害日志
- [ ] 木桩血条归零后变灰，3秒后自动重置回满
- [ ] 重置后的木桩可再次被攻击
- [ ] 玩家扇形攻击：只有面向方向扇形内能命中
- [ ] 玩家WASD移动、墙壁碰撞、鼠标瞄准均正常工作
- [ ] 血条始终位于敌人头顶，敌人旋转时血条保持水平，不跟随旋转
- [ ] Console无红色报错

**验收清单（v0.4.2 Enemy）**：
- [ ] 敌人初始处于Patrol状态，在原点附近移动
- [ ] 玩家进入detectionRange（5格），敌人切换为Chase追击
- [ ] 玩家进入attackRange（1.2格），敌人切换为Attack并尝试攻击
- [ ] 玩家受到攻击，血条减少
- [ ] 玩家离开attackRange，敌人切回Chase
- [ ] 玩家远离超过losePlayerRange（8格），敌人切回ReturnToPatrol
- [ ] 敌人回到原点，恢复Patrol
- [ ] 在Patrol状态时攻击敌人，敌人立刻切换Chase（OnTakeDamage触发）
- [ ] 敌人死亡：变灰、停止移动、血条归零、0.5秒后销毁
- [ ] 血条始终位于敌人头顶，敌人旋转时血条保持水平，不跟随旋转
- [ ] Console无红色报错

---

## 脚本清单

| 脚本 | 路径 | 挂载对象 | 职责 |
|------|------|----------|------|
| **DamageSystem** | `Framework/DamageSystem.cs` | — | 公共伤害系统（TODO框架） |
| **DetectionSystem** | `Framework/DetectionSystem.cs` | — | 公共检测系统（TODO框架） |
| **PatrolSystem** | `Framework/PatrolSystem.cs` | — | 公共巡逻系统（TODO框架） |
| EnemyStats | `v0_4/EnemyStats.cs` | TrainingDummy / **Enemy** | 敌人属性配置 |
| EnemyHealth | `v0_4/EnemyHealth.cs` | TrainingDummy / **Enemy** | 敌人生命管理（含OnTakeDamage事件，供AI复用） |
| EnemyCombat | `v0_4/EnemyCombat.cs` | —（TrainingDummy禁用）/ **Enemy（启用）** | 敌人攻击CD管理 |
| EnemyAI | `v0_4/EnemyAI.cs` | —（TrainingDummy禁用）/ **Enemy（启用）** | **核心**状态机：Patrol/Chase/Attack/ReturnToPatrol |
| EnemyController | `v0_4/EnemyController.cs` | TrainingDummy / **Enemy** | 门面：移动/停止/面向接口 + 受伤闪烁 + 死亡处理 |
| **TrainingDummy** | `v0_4/TrainingDummy.cs` | **TrainingDummy** | **木桩**：受伤闪烁、死亡后自动重置 |
| WorldSpaceHealthBar | `v0_3/WorldSpaceHealthBar.cs`（v0.4.2修改） | HealthBarAnchor（子物体） | 适配EnemyHealth的头顶血条，不跟随敌人旋转 |

---

## 常见问题排查

### Q1：攻击木桩没有反应
**排查**：
1. 确认 `TrainingDummy` 有 `BoxCollider2D`（被OverlapCircle命中需要Collider）
2. 确认 `TrainingDummy` 有 `EnemyHealth` 组件（实现IDamageable）
3. 确认 `TrainingDummy` 在玩家的攻击扇形范围内
4. 确认 `TrainingDummy` 的Layer与玩家攻击检测的LayerMask匹配

### Q2：木桩血条不更新
**排查**：
1. 确认 `WorldSpaceHealthBar` 的 `Enemy Health` 字段已引用 `EnemyHealth`
2. 确认 `EnemyHealth.TakeDamage` 中调用了 `OnHealthChanged?.Invoke()`
3. 确认 `WorldSpaceHealthBar.OnEnable` 中正确订阅了事件

### Q3：木桩死亡后不重
**排查**：
1. 确认 `TrainingDummy` 脚本已挂载
2. 确认 `TrainingDummy.OnDied` 方法中调用了 `StartCoroutine(ResetCoroutine())`
3. 确认 `EnemyHealth.OnDeath` 事件已订阅 `TrainingDummy.OnDied`

### Q4：v0.2/v0.3旧脚本冲突
**排查**：
1. 确认 `TrainingDummy` 上已移除 `Enemy.cs`（v0.3旧脚本）
2. 确认 `TrainingDummy` 上没有两个脚本同时控制血量
3. 确认 `Canvas` 上的旧PlayerUI已移除，新PlayerUI已挂载

### Q1：敌人不移动/不追击
**排查**：
1. 确认 `Enemy_Test` 有 `Rigidbody2D`（Dynamic, Gravity=0）
2. 确认 `EnemyStats` 的 `moveSpeed` > 0
3. 确认Player对象的Tag = "Player"
4. 确认 `EnemyAI.Awake` 中成功找到Player（`GameObject.FindWithTag("Player")`）

### Q2：敌人不攻击玩家
**排查**：
1. 确认Player有 `Health` 组件（实现IDamageable）
2. 确认 `EnemyCombat.IsInAttackRange` 返回true
3. 确认 `EnemyAI.UpdateAttack` 中正确调用 `combat.TryAttack`
4. 确认 `EnemyStats.attackRange` 和 `attackCooldown` 设置合理

### Q3：被攻击时敌人不反击（不切换Chase）
**排查**：
1. 确认 `EnemyHealth.OnTakeDamage` 事件已订阅 `EnemyAI.OnDamaged`
2. 确认 `EnemyHealth.TakeDamage` 中调用了 `OnTakeDamage?.Invoke()`
3. 确认玩家的 `WarriorAttack.Execute` 正确命中了 `IDamageable`（EnemyHealth实现了IDamageable）

### Q4：敌人血条不更新
**排查**：
1. 确认 `WorldSpaceHealthBar` 的 `Enemy Health` 字段已引用 `EnemyHealth`
2. 确认 `EnemyHealth.TakeDamage` 中调用了 `OnHealthChanged?.Invoke()`
3. 确认 `WorldSpaceHealthBar.OnEnable` 中正确订阅了事件

---

## v0.4 → v0.5 衔接说明

v0.4完成了以下工作：
1. **玩家攻击系统**：扇形攻击、伤害判定、红色指示器
2. **敌人架构脚本**：EnemyController/Stats/Health/AI/Combat 全部编写保存
3. **公共框架**：DamageSystem/DetectionSystem/PatrolSystem 建立（待实现）
4. **伤害测试木桩**：TrainingDummy用于验证玩家攻击（v0.4.1）
5. **完整敌人测试**：v0.4.2在场景中部署了一个启用了AI和Combat的Enemy，验证了Patrol/Chase/Attack/ReturnToPatrol状态机

v0.5将在v0.4基础上：
- **多种敌人类型**：通过EnemyStats配置不同参数创建不同敌人（快速近战、远程法师、高血Boss）
- **敌人生成系统**：在地图上随机/定点生成敌人波次
- **受伤反馈**：敌人受击时的击退效果、硬直时间
- **玩家受击反馈**：玩家受伤时的屏幕闪烁、无敌帧
- **公共框架实现**：填充DamageSystem（护甲减伤公式）、DetectionSystem（视线Raycast检测）、PatrolSystem（路径点巡逻）

> 💡 **v0.4的关键决策**：所有敌人架构脚本已编写并保存。v0.4.1场景中只挂载了木桩（无AI/Combat），v0.4.2直接部署了一个完整功能的Enemy。v0.5将在此基础上扩展多种敌人类型和生成系统。


---

## 文档变更记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2025-07-09 | v0.4-draft | 初始草稿：完整Enemy架构（Controller/Stats/Health/AI/Combat）+ 公共框架（DamageSystem/DetectionSystem/PatrolSystem） |
| 2025-07-09 | v0.4-r2 | 修改：测试对象改为TrainingDummy（伤害测试木桩），EnemyAI/EnemyCombat脚本保留但不挂载，新增TrainingDummy.cs（受伤闪烁+自动重置） |
| 2025-07-10 | v0.4-r3 | 新增v0.4.2：在场景中直接部署一个启用了AI和Combat的完整Enemy，验证Patrol/Chase/Attack/ReturnToPatrol状态机 |
