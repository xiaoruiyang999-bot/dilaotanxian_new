# v0.4.5.2 Fix Final5 — 攻击表现与判定统一修复

> 目标：统一 `WeaponAnimator`、`AttackIndicator`、`AttackQuery` 数据来源，使攻击动画、攻击范围显示、实际伤害判定三者严格基于同一份 `AttackData`。

---

## 一、当前问题

1. `AttackIndicator` 通过 `Scale` 拉伸 Sprite 模拟扇形，显示为椭圆而非真正扇形。
2. `EnemyCombat` 仍使用距离判定（圆形），与扇形动画不一致。
3. `WeaponAnimator`、`AttackIndicator`、`EnemyCombat` 三者数据不统一。

---

## 二、最终架构

```
AttackData
    │
    ├───────────────┬───────────────┐
    ▼               ▼               ▼
WeaponAnimator  AttackIndicator  AttackQuery
```

- `WeaponAnimator`：只播放旋转动画，不读取 `AttackData`。
- `AttackIndicator`：根据 `AttackData` 动态生成真正扇形 Mesh。
- `AttackQuery`：根据 `AttackData` 执行扇形/圆形攻击检测。
- `EnemyCombat`：接管完整攻击状态机，Active 阶段调用 `AttackQuery`。

---

## 三、修改步骤

### Step 1：重写 `AttackIndicator.cs`

- 删除 `Scale` 模拟扇形逻辑。
- 新增 `MeshFilter` + `MeshRenderer`，运行时动态生成扇形/圆形 Mesh。
- 支持 `Circle` 和 `Sector` 两种形状。
- `Sector` 参数：中心点 `(0,0,0)`、半径 `AttackRange`、角度 `AttackAngle`、方向 `AttackDirection`。

### Step 2：新增 `AttackQuery.cs`

- 职责：只负责攻击检测。
- 输入：`Vector2 origin`、`Vector2 attackDirection`、`AttackData attackData`。
- 检测逻辑：
  - 距离 `<= AttackRange`
  - 角度 `<= AttackAngle / 2`
- 禁止：控制动画、AI、`WeaponPivot`。

### Step 3：重写 `EnemyCombat.cs`

- 接管完整攻击状态机：`Windup` / `Active` / `Recovery`。
- 持有 `WeaponController`、`WeaponAnimator`、`AttackIndicator`、`AttackQuery`。
- `Active` 阶段调用 `AttackQuery.ExecuteAttack()` 造成伤害。
- 禁止 `EnemyAI` 直接造成伤害。

### Step 4：简化 `EnemyAI.cs`

- 删除内部 `Windup` / `Active` / `Recovery` 子状态机。
- 删除对 `WeaponController` / `WeaponAnimator` / `AttackIndicator` 的直接控制。
- 只负责：寻找目标、移动、判断距离、调用 `EnemyCombat.TryAttack(target)`。

### Step 5：调整 `PlayerCombat.cs`

- 使用 `AttackQuery` 执行扇形判定，替代 `PerformFanAttack()` 中的手动判定。
- 保持现有攻击状态机不变。

### Step 6：调整 Prefab

- `Enemy.prefab`：
  - `EnemyCombat` 新增 `AttackQuery` 引用。
  - `AttackIndicator` 组件添加 `MeshFilter` + `MeshRenderer`（或在运行时生成）。
  - `EnemyAI` 移除武器相关引用。

### Step 7：测试数据

- `EnemyMeleeAttackData.asset`：
  - `AttackRange = 1.5`
  - `AttackAngle = 120`
  - `AnimationType = Arc`
  - `AttackShape = Fan`

---

## 四、验证标准

1. 红色攻击区域必须是 120° 真正扇形。
2. `WeaponSprite` 旋转轨迹必须覆盖整个扇形区域。
3. 武器碰到目标时才能造成伤害。
4. 边缘目标不能被圆形范围误伤。

---

## 五、修改文件列表

| 文件 | 操作 |
|---|---|
| `Assets/Scripts/v0_4/AttackIndicator.cs` | 重写，使用 Mesh 生成扇形 |
| `Assets/Scripts/v0_4/AttackQuery.cs` | 新增 |
| `Assets/Scripts/v0_4/EnemyCombat.cs` | 重写，接管攻击状态机 |
| `Assets/Scripts/v0_4/EnemyAI.cs` | 简化，移除攻击子阶段 |
| `Assets/Scripts/v0_3/PlayerCombat.cs` | 使用 AttackQuery |
| `Assets/Prefabs/Enemy.prefab` | 调整组件引用 |
| `Assets/Data/EnemyMeleeAttackData.asset` | 测试数值 |
