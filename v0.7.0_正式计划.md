# v0.7.0 正式计划：拆除与重构 —— 体力/闪避下线 + 六维属性 + 伤害管线与暴击

> 版本线：《v0.7_Demo玩法计划书.md》§九 v0.7.0 执行卡。总纲：《Demo_一个月总计划与分工.md》决策 2/3/6。公式口径：《伤害计算公式文档.md》。
> 前置：v0.6.3 已完成（六武器/职业/准备场景）。本版是转型地基，**不加新玩法、不换美术**，只拆旧账、立新规。
> 开工前必读《版本实施自检清单.md》§1。

---

## 一、目标与范围

1. **下线通用闪避/疾跑/体力**（决策 2）：删代码逻辑，Space/Shift 输入解绑（action 保留在 .inputactions 备用）；
2. **玩家属性改六维**（决策 6）：HP/护甲/攻击/魔力/暴击率/暴击伤害；护甲双倍率字段（免伤 R、扣甲 L）与职业资产同步加字段（本版接线 R/L 在 v0.7.1）；
3. **伤害管线 + 暴击**：新建 DamageContext/DamageResolver，玩家造成伤害走"（角色攻击+武器攻击）×倍率×暴击"；敌人侧本版保持原样（v0.7.1 起接护甲/攻击力）。

**明确不做（本版）**：减伤甲双倍率结算（v0.7.1）、敌人护甲/攻击力接入（v0.7.1/v0.7.8）、技能/背包/道具（后续子版本）、任何美术替换。

---

## 二、改动清单

### 2.1 新建脚本（2，含 .meta，建后确认进 csproj）

| 文件 | 职责 |
|------|------|
| `Combat/DamageContext.cs` | 伤害上下文 struct：`baseAttack`（角色攻击+武器攻击）、`multiplier`（倍率区：蓄力/技能等）、`critRate`、`critDamage`；方法 `Roll()` = 一次暴击判定并返回最终伤害 `baseAttack × multiplier × (isCrit ? critDamage : 1)`，`IsCrit` 结果外露供表现层 |
| `Combat/DamageResolver.cs` | 静态结算入口：`Deal(IDamageable target, DamageContext ctx)` → `target.TakeDamage(ctx.Roll())`。单点收口，v0.7.1 在此处分流 Health 的减伤甲结算 |

### 2.2 修改脚本（9）

| 文件 | 改动 |
|------|------|
| `Player/PlayerStats.cs` | **删**体力全部内容（maxStamina/CurrentStamina/两个回复计时/TryConsumeStamina/ConsumeStaminaOverTime/体力段 Update 回复逻辑）；**删** Debug.Log（AbsorbDamageWithArmor 内残留）；**加**六维字段 `attack`（占位 5）/`critRate`（占位 0.2）/`critDamage`（占位 1.5）+ 护甲双倍率字段 `armorReduceMul`（占位 0.3）/`armorLossMul`（占位 1.0，本版不接线）；ApplyClass 写入新字段；护甲脱战回复**本版保留**（v0.7.1 删） |
| `Player/PlayerMovement.cs` | **重写为纯移动**：删闪避冲刺/无敌帧写速度/残影/奔跑×1.6/体力消耗分支；保留常规移动 + `SetChargeSlow`（蓄力 ×0.5，与体力无耦合，确认保留）；FixedUpdate 写速度不变 |
| `Player/PlayerController.cs` | 删 Dash/Sprint 输入订阅与分发；Skill action 占位订阅保留（v0.7.4 用）；TakeDamage 转发不变 |
| `Player/PlayerUI.cs` | 删体力条逻辑（创建/更新/面板扩展计数）；**运行时找到既有体力条 GameObject 则 SetActive(false)**（布局归场景 YAML，代码不删场景对象，见 §五场景处理） |
| `Player/PlayerWorldStatusBar.cs` | 删头顶黄体力条（保留钢灰护甲+红 HP 双条） |
| `Class/ClassData.cs` | +`attack`/`critRate`/`critDamage`/`armorReduceMul`/`armorLossMul` 五字段（序列化，占位默认值同 PlayerStats） |
| `Class/ClassSelectUI.cs` | 职业按钮描述文本从旧三属性改六维显示（含暴击率%/暴击伤害倍率） |
| `Combat/WeaponHitbox.cs` | 结算分流：攻击者根 `GetComponentInParent<PlayerStats>()`（Awake 缓存）→ **玩家**走 `DamageResolver.Deal(target, ctx)`；**非玩家**（敌人）保持 `TakeDamage(attackData.AttackDamage)` 原路径。+`DamageMultiplier` 字段（默认 1，BeginSwing 复位，LengthMultiplier 同款先例） |
| `Combat/Projectile.cs` | 玩家发射的子弹走 DamageResolver（owner 根查 PlayerStats）；`damageMul` 参数映射到 `ctx.multiplier`（弓箭蓄力 ×2 语义不变）；敌人子弹路径不变 |

### 2.3 蓄力倍率归位（公式口径修正）

- 现状：近战满蓄 ×1.5 是 PlayerCombat 对 AttackData 副本 `SetDamage(base×1.5)`——进入新管线后会把角色攻击力也一起放大，与公式文档 §2.1（倍率区独立于基础攻击）不符；
- 改法：PlayerCombat 蓄力副本**不再改伤害**（SetDamage 调用移除，副本只管范围/角度），满蓄时改设 `WeaponHitbox.DamageMultiplier = chargeFullDamageMul`（1.5），挥击结束随 BeginSwing 复位；
- 弓箭蓄力 `damageMul` 同理走 ctx.multiplier（已在 2.2 Projectile 行覆盖）；
- 范围/角度/宽度缩放走副本的逻辑**不动**。

### 2.4 数据资产

- 三份 `Assets/Data/Class/*.asset`：新字段取脚本默认值（攻击 5/暴击 0.2/暴伤 1.5/R 0.3/L 1.0），**占位数值，待设计 D 定稿后填**——资产无需立即改，下次编辑器序列化自动补字段；
- `Player.prefab`：PlayerStats 新字段取默认值，无需改 YAML；被删的体力字段在 YAML 残留无害（Unity 忽略失配字段）。

### 2.5 输入

- `PlayerInputActions.inputactions` **不动**：Dash/Sprint action 保留（后续技能化闪避回归时复用绑定）；代码侧解绑即可。

---

## 三、伤害结算口径（本版落地后）

| 方向 | 链路 | 状态 |
|------|------|------|
| 玩家近战→敌 | (PlayerStats.attack + AttackData 伤害) × DamageMultiplier(蓄力) × 暴击 → TakeDamage | ✅ 新管线 |
| 玩家子弹→敌 | (attack + ProjectileData 伤害) × damageMul(蓄力) × 暴击 | ✅ 新管线 |
| 敌→玩家 | AttackData 伤害直扣（护甲吸收旧逻辑） | 原样（v0.7.1 改） |
| 环境（祭坛自伤/木桩/调式秒杀） | float 直扣 | 原样 |

暴击表现：本版仅数值生效 + `DamageContext.IsCrit` 外露；暴击视觉（大字/放大闪白）【待补充】，记入 v0.7.11 UI 完善。

---

## 四、美术需求（本版）

**无新增美术需求。** 唯一关联项：体力条下线后 UI 少一条，属布局微调；ClassSelectUI 六维文案变长，按钮/面板尺寸程序按自检 §1 半高边界核算，无需美术出图。

## 五、外部/用户配合需求

1. **场景清理（需用户编辑器操作，二选一）**：`v0_6_ClassWeapon` / `v0_6_PrepRoom` 场景 YAML 里的体力条对象——代码侧已兜底隐藏（§2.2 PlayerUI），用户可在方便时进场景手动删除体力条对象（不删也不影响运行）；
2. **编辑器刷新**：切回 Unity 导入新脚本；若编辑器处于运行状态，勿保存旧内存覆盖 YAML；
3. **数值确认（不阻塞）**：六维占位数值（攻击 5/暴击 0.2/暴伤 1.5/R 0.3/L 1.0）仅跑链路用，设计 D 定稿后填《数值与角色设计任务书.md》§一。

---

## 六、验收清单

> 测试场景：`v0_6_PrepRoom` → 传送门进 `v0_6_ClassWeapon`；回归场景 `v0_4_EnemySystem`；Game 视图 1920×1080。

1. `dotnet build Assembly-CSharp.csproj`：0 警告 0 错误（2 个新文件已补 csproj）；
2. **体力零残留**：全项目 grep `stamina|Stamina` 仅余 README/文档；游戏内屏幕面板与头顶均无体力条；Space/Shift 按下无任何效果；
3. **移动**：常速移动/蓄力减速 ×0.5 正常；无体力后奔跑不存在（移速恒定）；
4. **六维**：选战士后 ClassSelectUI 显示六维；PlayerStats Inspector 可见攻击/暴击/暴伤/R/L 字段；
5. **暴击**：木桩测试——CR=0 时伤害恒为 (5+武器)；CR=1 时恒为 ×1.5；长剑满蓄 = (5+武器)×1.5（×暴击）；与公式文档 §2.4 算例一致；
6. **回归**：敌人近战/精英/Boss 行为与伤害不变；祭坛自伤/治疗球/法力球/宝箱/换武器全通；`v0_4_EnemySystem` 木桩场景无损。

---

## 七、风险与注意

| 风险 | 对策 |
|------|------|
| 体力引用删漏（8 文件 117 处匹配，含文档） | 改完全项目 grep 逐项核对；prefab YAML 残留字段无害不处理 |
| 蓄力倍率归位改坏蓄力手感 | 范围/角度副本逻辑不动，仅伤害改走 multiplier；验收 5 覆盖 |
| WeaponHitbox 玩家/敌人分流误判 | 按根上 PlayerStats 存在性判定（敌人根无 PlayerStats）；回归验收 6 覆盖 |
| 场景体力条残留 | 代码兜底隐藏 + §五用户清理，双保险 |
| ClassSelectUI 六维文案溢出 | 按自检 §1 面板半高边界核算 + 截图验证 |

## 八、收尾义务

- 更新 `now_use/README.md`（+DamageContext/DamageResolver，PlayerStats/PlayerMovement/PlayerUI 等职责描述同步）；
- 《版本实施自检清单.md》§2 补本版错误记录（如有）、§3 更新"体力/闪避数值"行为已下线；
- 写 `v0.7.0操作.md`（改动清单/验收/遗留）；
- 《美术策略变更说明.md》本版无美术变更，不更新。

---

## 九、token 成本标注（2026-07-29 核查存档）

- 🔴 `PlayerMovement.cs`：体力耦合最深（42 处匹配），全文读+重写，本版最大单笔成本，一次写对；
- 🟡 `PlayerStats`/`PlayerUI`/`PlayerController`：只读相关段局部改；`PlayerCombat.cs`（735 行）**只读蓄力 SetDamage 段，不全文读**；
- 🟢 其余小文件局部改；DamageContext/DamageResolver 纯写不读；
- build 策略：9 个文件改完一次性 `dotnet build`，避免报错循环；
- **实施方式：整体派 coder 子代理执行**（读改过程不进主对话），主对话只留计划与验收结论。
