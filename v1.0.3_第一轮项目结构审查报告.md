# v1.0.3 第一轮项目结构审查报告

> 审查日期：2026-08-31  
> 审查范围：工程技术基线、当前主场景、Build Settings、脚本组织、运行时资源路径、测试设施  
> 审查方式：只读文件扫描 + UnitySkills `project_stack_detect` / `scene_analyze` / `scene_health_check` / `scene_context` / 编译状态检查  
> 当前结论：**工程能够完成脚本编译，但尚不满足“可交付 Demo 基线”；存在 2 个 P1 和 3 个 P2 结构问题。**

---

## 一、技术基线

| 项目 | 实际状态 | 结论 |
|---|---|---|
| Unity | 6000.5.4f1 | 与 Unity 6 目标一致 |
| 渲染 | URP 17.6.0，2D Renderer | 正常 |
| 输入 | Input System 1.19.0；未启用 Legacy Input | 保持现有路线 |
| UI | UGUI | 与现有运行时 UI 代码一致 |
| 动画/补间 | DOTween + 自有 FrameAnimator | 已存在 |
| MCP | CoplayDev Unity MCP + UnitySkills | UnitySkills 8090 当前可用 |
| asmdef | 项目脚本无 asmdef | 全部进入 Assembly-CSharp，规模扩大后编译边界不足 |
| 自动化测试 | Test Framework 已安装，但未发现项目测试 | 目前无回归保护 |
| 当前场景 | `v0_5_Dungeon`，24 个对象、14 个根对象 | 场景规模不大，适合先完成接线治理 |
| 编译状态 | `isCompiling=false` | 表示当前未在编译；不等同于实机链路全部正确 |

---

## 二、P1：主场景存在 5 个 Missing Script

Unity 场景健康检查发现以下对象的业务组件已经丢失：

| 场景对象 | 原职责 | 当前状态 | 影响 |
|---|---|---|---|
| `CoinHUDSystem` | 金币 HUD | 只剩 Transform | 金币前端断链 |
| `VictorySystem` | 通关结算 | 只剩 Transform | 第 9 层/终局缺少前端 |
| `MetaSystem` | Meta HUD/永久升级 | 只剩 Transform | 跨局成长入口断链 |
| `UpgradeSystem` | 局内三选一升级 | 只剩 Transform | 奖励触发可能静默失效 |
| `PauseSystem` | 暂停与设置 | 只剩 Transform | Escape/暂停链路断链 |

### 根因

`v1.0.1_前后端系统配对修复.md` 曾记录这些系统已经接入主场景；后续合并兼容清理删除了对应旧脚本，但场景对象和序列化组件仍保留。Git 没有把这种“场景仍引用已删除脚本”作为文本冲突报告。

### 判定

这是当前最重要的结构问题。不能简单批量删除 Missing Script，因为其中包含玩家仍需要的产品功能；也不能直接恢复旧归档代码，因为它可能重新引入与 main v0.7.5 不兼容的接口。

### 建议动作

先逐项确定当前 Demo 是否保留该功能，再基于现行 `PlayerStats`、`RunManager`、输入和 UI 架构重建最小版本。重建完成后替换丢失组件；明确延期的功能才清理场景残留。

---

## 三、P1：Build Settings 不包含当前地牢主场景

当前 Build Settings 启用：

1. `v0_6_PrepRoom`
2. `v0_6_ClassWeapon`
3. `v0_7_PrepRoom`
4. `v0_7_ClassWeapon`
5. `SampleScene`

但当前实际主场景 `Assets/Scenes/v0_5_Dungeon.unity` 不在构建列表中。

### 影响

- 编辑器中直接打开地牢可以运行，不代表独立构建能进入地牢。
- `LoadScene` 使用名称切换时可能在构建中失败。
- 同时保留 v0.6 和 v0.7 两套准备/职业场景，启动场景与正式流程不明确。

### 建议动作

在 Golden Path 定稿后，把 Build Settings 收口为唯一正式流程：正式启动/大厅场景 → 地牢主场景。旧场景是否保留在工程中与是否进入 Build 是两件事；非正式回归场景不应默认启用。

---

## 四、P2：运行时 Resources 配对尚未完成

当前 `Assets/Resources` 中仅发现 DOTween 设置资产。代码中已经明确存在以下 Editor/Build 双路径：

- `ClassCatalog`：构建需要 `Resources/Class/`。
- `SkillCatalog`：构建需要 `Resources/Skill/`。
- `PrepRoomManager`：构建需要运行时可加载的道具资产。
- `FrameAnimator`：构建需要 `Resources/Art/Characters/Warrior/...`。
- `SlotBarUI`：构建需要运行时可加载的槽位框美术。

### 影响

这些功能可能在 Editor 内正常，但独立构建中资产加载为空，形成典型“编辑器通过、打包失效”。

### 建议动作

在阶段 F 前必须完成资源化。短期可按现有 Resources 约定整理；若未来改 Addressables，应单独立项，不在本轮同时改两套加载架构。

---

## 五、P2：文档、输入与实现存在版本语义漂移

输入资产仍包含：

- `Dash`：Space
- `Sprint`：Left Shift
- `Cancel`：Escape
- `Skill`：F
- `UseItem`：C
- `Ultimate`：Q
- `WeaponSkill`：R

但不同规划文档对 Dash、Sprint、暂停、Meta 商店和 Demo 范围有互相矛盾的历史描述。

### 影响

- 输入 Action 存在不代表 PlayerController 已消费。
- 教程和 UI 图标无法依据历史文档直接制作。
- 已取消功能可能仍留在 Input Asset，新增功能又可能缺少入口。

### 建议动作

以当前可玩设计重新确认唯一键位表，逐项核对 `PlayerController.OnActionTriggered` 和对应系统，再同步更新 AGENTS、开发必读、输入资产和教程文案。

---

## 六、P2：缺少项目级自动化回归

Unity Test Framework 已安装，但未发现 `Assets/Tests`、测试脚本或测试 asmdef。

### 当前风险

- 合并后只能依靠 Console 截图逐个暴露错误。
- Save JSON、伤害公式、地牢 seed 等纯逻辑已有良好测试价值，却没有持续验证。
- Prefab/场景 Missing Script 没有提交前门禁。

### 建议的最小测试集

1. EditMode：`DamageResolver.ApplyArmor` 边界值。
2. EditMode：地牢固定 seed 生成与连通性。
3. EditMode：存档 JSON 往返与字段缺失迁移（存档重建后）。
4. EditMode：正式场景/Prefab Missing Script 扫描。
5. PlayMode：玩家生成、首次攻击、死亡重置的最小冒烟测试。

本轮不先追求覆盖率，先建立能阻止同类合并事故再次进入 main 的门禁。

---

## 七、非问题与工具误报

以下扫描结果不应直接当作缺陷处理：

- `DungeonRoot` 是运行时内容容器，空 Transform 合理。
- `HealthBarAnchor` 是挂点，空 Transform 合理。
- UnitySkills 报告“没有 Light”，但场景实际存在 `Global Light 2D`；这是通用检查器未把 URP `Light2D` 计入传统 Light 数量。
- “缺少 Systems/Managers/Gameplay/UIRoot”来自工具默认场景合同，不是本项目既定命名规范，不应为了消除警告强行重构根节点。

---

## 八、目前确认仍然有效的结构

- 当前脚本集中在 `Assets/Scripts/now_use/`，共 93 个 C# 文件，历史 v0_1～v0_4 未重新混入当前脚本目录。
- 主场景有 Main Camera、AudioListener、Canvas、EventSystem、InputSystemUIInputModule。
- 玩家有 PlayerInput、PlayerStats、Health、PlayerCombat、PlayerController、WeaponController 等核心组件。
- `PlayerInputActions` 正确绑定到 Player，默认 Action Map 为 `Player`。
- `AudioManager` 当前存在于主场景；音频目录中存在 OGG 资产，但仍需审查实际 Clip 映射与音量入口。
- 当前战斗核心依赖图仍以 `AttackData`、`PlayerCombat`、`WeaponHitbox` 为中心，没有发现需要推倒重来的理由。

---

## 九、配对矩阵首轮状态

| 系统 | 当前等级 | 证据/问题 |
|---|---:|---|
| 战斗核心 | 2～3 | 代码与玩家组件存在；合并修复后待 Play 验证 |
| 职业选择 | 2 | 资产与准备场景存在；Build 流程和实机待确认 |
| 武器 | 2～3 | 数据/Prefab/输入存在；待固定长剑 Golden Path |
| 技能 | 2～3 | 数据与执行器存在；运行时资源路径未完成 |
| 道具/背包 | 2～3 | 代码与资产存在；待拾取—UI—使用实测 |
| 地牢 | 2～3 | 主场景与生成器存在；主场景未进 Build Settings |
| 金币经济 | 1 | HUD 场景组件丢失，后端现状需重审 |
| 局内升级 | 1 | 场景组件丢失，当前脚本目录未发现对应类 |
| Boss/通关 | 1～2 | Boss 内容存在，Victory 前端丢失 |
| Meta/存档 | 0～1 | 旧前后端已清理，场景仅剩丢失引用 |
| 暂停/设置 | 0～1 | Pause 前端丢失；AudioManager 存在 |
| 教程 | 0～1 | 未发现已验收的完整新手入口 |

---

## 十、下一步执行顺序

1. **先处理 5 个 Missing Script 的产品决策与替代设计。** 推荐保留 Pause；金币/升级/结算按当前 Demo 主循环重建最小版本；Meta/存档确认是否进入本轮。
2. 核对当前编译 Console，完成战斗合并兼容收尾。
3. 确定唯一正式启动场景和地牢场景，随后修正 Build Settings。
4. 用“战士 + 长剑”跑第一条 Golden Path，记录第一个运行时阻断点。
5. 主流程跑通后处理 Resources 构建资源化。
6. 增加 Missing Script + 纯逻辑最小测试，形成合并门禁。

---

## 十一、首轮审查结论

项目现有核心框架可以继续使用，不建议推倒重构。当前首要任务也不是新增内容，而是修复“场景仍宣称功能存在、对应实现已经消失”的结构性断链，并明确唯一构建流程。

在这两个 P1 解决前，路线图中的金币、升级、通关、Meta、暂停等项目都不能维持“正式完成”状态。

