# v0.4.5.2 Fix Final4 — 兼容性修复计划

> 版本：v0.4.5.2 Fix Final4
>
> 类型：Bug 修复
>
> 目标：修复 Final3 修改后出现的兼容性错误、引用错误、逻辑遗漏，不重新设计系统。
>
> 基准架构：v0.4.5.2 Final3

---

## 执行顺序

必须按以下顺序修改，避免循环依赖和接口缺失：

| 顺序 | 文件 | 说明 |
|---|---|---|
| Step 1 | `AttackData.cs` | 确认字段完整 |
| Step 2 | `WeaponAnimator.cs` | 确保纯 Tween 播放器接口 |
| Step 3 | `WeaponController.cs` | 确保角度计算与锁定接口 |
| Step 4 | `PlayerCombat.cs` | 修复玩家攻击流程 |
| Step 5 | `EnemyAI.cs` | 修复敌人攻击流程 |
| Step 6 | `AttackIndicator.cs` | 新增 SetAngle / SetDirection |
| Step 7 | `PlayerController.cs` / `PlayerAimController.cs` | 修复 Input System |
| Step 8 | `Player.prefab` / `Enemy.prefab` | 检查引用 |
| Step 9 | `AttackData.asset` | 确认数值 |
| Step 10 | 运行测试 | 验证玩家与 Enemy |

---

## 关键架构约定

### WeaponController 是 WeaponPivot 唯一所有者

- 普通状态：`WeaponController.SetAimDirection()` 修改 `WeaponPivot.localRotation`。
- 攻击状态：`WeaponController.LockAttackDirection()` 锁定后，禁止任何外部脚本修改 `WeaponPivot.localRotation`。
- 攻击结束：`WeaponAnimator.Stop()` 只 Kill Tween，**不恢复** `WeaponPivot.localRotation`；由 `WeaponController.UnlockAttackDirection()` 恢复朝向。

**禁止：**

- `WeaponAnimator` 恢复 `WeaponPivot` 角度。
- `PlayerCombat` / `EnemyAI` 直接修改 `WeaponPivot.localRotation`。

---

## 一、当前问题分析

### Bug 1：Input System 报错

**现象：**

运行时出现 `InputAction` / `PlayerInput` / `Action Map` 相关错误。

**原因：**

- `PlayerInput` 组件配置的 Action Map 名称与代码中期望的不一致。
- `Input Actions` 文件中的 Action 名称（Move / Attack）与代码回调不匹配。
- 可能存在的旧 `UnityEngine.Input` 调用与新 Input System 冲突。

**影响：**

- 玩家无法移动或攻击。
- 鼠标瞄准失效。

---

### Bug 2：Enemy 攻击范围不显示

**现象：**

Enemy 攻击时 `AttackIndicator` 不显示。

**原因：**

- `EnemyAI` 中 `indicator` 引用可能为空。
- `AttackIndicator` 组件初始状态可能未激活。
- 进入 Attack 状态时 `Show()` / `SetRadius()` 调用时机或参数错误。
- `AttackIndicator` 的 `SpriteRenderer` 层级 / Z 轴 / Sorting Order 配置错误。

**影响：**

- 玩家无法预判 Enemy 攻击范围。
- 攻击预警功能失效。

---

### Bug 3：Weapon 动画与攻击范围不一致

**现象：**

`WeaponSprite` 可以显示，但动画旋转范围与实际攻击判定范围存在偏差。

**原因：**

- `WeaponController.GetAttackStartAngle()` / `GetAttackEndAngle()` 计算错误。
- `WeaponSprite` 长度未按 `AttackRange` 正确缩放。
- `PlayerCombat.PerformFanAttack()` 使用 `transform.right` 而非实际武器朝向。

**影响：**

- 玩家/Enemy 攻击动画与命中判定不同步。
- 攻击体验不一致。

---

### Bug 4：WeaponAnimator 职责越界

**现象：**

`WeaponAnimator` 可能读取 `AttackData` 或参与攻击逻辑。

**原因：**

- Final3 后未彻底清理旧逻辑。
- 部分代码仍通过 `WeaponAnimator` 推导攻击参数。

**影响：**

- 违反单一职责原则。
- Player 与 Enemy 动画逻辑耦合。

---

### Bug 5：Player 攻击方向问题

**现象：**

- 攻击时武器方向错误。
- 初始位置偏移。
- 攻击结束后不能恢复鼠标方向。

**原因：**

- `PlayerCombat` 未在普通状态持续更新 `WeaponController.SetAimDirection()`。
- `WeaponController.LockAttackDirection()` / `UnlockAttackDirection()` 调用时机错误。
- `WeaponAnimator.Stop()` 重置了 `WeaponPivot` 角度。

**影响：**

- 玩家攻击方向不可控。
- 攻击结束后武器朝向异常。

---

### Bug 6：Enemy 武器不能完整旋转

**现象：**

Enemy 武器无法完整旋转 360°。

**原因：**

- `WeaponController.GetAttackEndAngle()` 未使用 `aimAngle + 360`。
- `WeaponAnimator` 未使用 `RotateMode.FastBeyond360`。
- `WeaponController` 在攻击期间仍更新朝向，与 Tween 冲突。

**影响：**

- Enemy 旋转攻击动画不正确。
- 动画与圆形攻击范围不匹配。

---

## 二、修改文件列表

| 操作 | 文件 | 原因 |
|---|---|---|
| 检查/修改 | `Assets/Scripts/v0_3/PlayerController.cs` | 修复 Input System 回调配置 |
| 检查/修改 | `Assets/Scripts/v0_4/PlayerAimController.cs` | 确保使用 New Input System API |
| 修改 | `Assets/Scripts/v0_3/PlayerCombat.cs` | 修复攻击方向锁定与恢复 |
| 修改 | `Assets/Scripts/v0_4/EnemyAI.cs` | 修复 AttackIndicator 显示与攻击流程 |
| 修改 | `Assets/Scripts/v0_4/WeaponController.cs` | 修复角度计算与方向锁定 |
| 修改 | `Assets/Scripts/v0_4/WeaponAnimator.cs` | 确保纯 Tween 播放器职责 |
| 检查/修改 | `Assets/Scripts/v0_4/AttackIndicator.cs` | 确保显示/隐藏逻辑正确 |
| 检查 | `Assets/Prefabs/Player.prefab` | 确认 Input / Weapon 引用 |
| 检查 | `Assets/Prefabs/Enemy.prefab` | 确认 AttackIndicator / Weapon 引用 |
| 检查 | `Assets/Data/PlayerAttackData.asset` | 确认 AttackRange / AttackAngle |
| 检查 | `Assets/Data/EnemyMeleeAttackData.asset` | 确认 AttackRange / AttackAngle |

---

## 三、每个文件修改方案

### 1. PlayerController.cs

**修改：**

- 检查 `PlayerInput` 组件回调方法签名：
  - `OnMove(InputValue value)`
  - `OnAttack(InputValue value)`
- 确保回调方法为 `public`。
- 检查 `Input Actions` 文件中 Action 名称：
  - Move
  - Attack
- 检查 Action Map 名称是否为 `Player`。

**原因：**

`PlayerInput` 通过反射绑定 Action 名称到同名方法，名称不一致会导致运行时错误。

---

### 2. PlayerAimController.cs

**修改：**

- 确保只使用 `Mouse.current.position.ReadValue()` 或 `Camera.main.ScreenToWorldPoint(Input.mousePosition)`。
- 如果 `Input.mousePosition` 报错，替换为：
  ```csharp
  Vector2 mouseScreen = Mouse.current.position.ReadValue();
  Vector2 mouseWorld = mainCamera.ScreenToWorldPoint(mouseScreen);
  ```
- 避免使用 `Input.GetKey` / `Input.GetAxis`。

**原因：**

项目已启用 New Input System，旧 `UnityEngine.Input` API 会抛出异常。

---

### 3. PlayerCombat.cs

**修改：**

- 确认 `UpdateAiming()` 在 `subPhase == None` 时调用 `weaponController.SetAimDirection(aimController.AimDirection)`。
- 确认 `StartWindup()` 调用 `weaponController.LockAttackDirection()`，并缓存攻击方向：
  ```csharp
  attackDirection = weaponController.GetAimDirection();
  ```
- 确认 `UpdateRecovery()` 结束时调用：
  ```csharp
  weaponAnimator?.Stop();
  weaponController?.UnlockAttackDirection();
  ```
- 确认 `PerformFanAttack()` 使用缓存的 `attackDirection`，而非 `transform.right` 或运行时 `weaponController.GetAimDirection()`。

**原因：**

- 保证普通状态跟随鼠标、攻击时锁定方向、攻击后平滑恢复。
- 缓存 `attackDirection` 可防止攻击期间鼠标移动导致动画方向与判定方向不一致。

---

### 4. EnemyAI.cs

**修改：**

- 检查 `indicator` 引用是否正确赋值（通过 `GetComponentInChildren<AttackIndicator>(true)`）。
- 进入 `Attack` 状态时确保调用：
  ```csharp
  if (indicator != null && attackData != null)
  {
      Vector2 dir = weaponController != null
          ? weaponController.GetAimDirection()
          : (Vector2)(target.position - transform.position).normalized;

      indicator.SetRadius(attackData.AttackRange);
      indicator.SetAngle(attackData.AttackAngle);
      indicator.SetDirection(dir);
      indicator.SetColor(indicator.WarningColor);
      indicator.Show();
  }
  ```
- `OnActiveMoment()` 中调用 `indicator.Hide()`。
- 退出 `Attack` 状态时确保 `indicator.Hide()`。
- 检查 `WeaponController` / `WeaponAnimator` 引用是否正确。

**原因：**

`AttackIndicator` 不显示通常是因为引用缺失或 `Show()` 未被调用。

---

### 5. WeaponController.cs

**修改：**

- 确认 `GetAttackStartAngle()`：
  - `Arc`：`aimAngle - AttackAngle * 0.5f`
  - `FullCircle` / `Spin`：`aimAngle`
- 确认 `GetAttackEndAngle()`：
  - `Arc`：`aimAngle + AttackAngle * 0.5f`
  - `FullCircle`：`aimAngle + 360f`
  - `Spin`：`aimAngle + 720f`
- 确认 `GetAttackRotateMode()`：
  - `Arc`：`RotateMode.Fast`
  - `FullCircle` / `Spin`：`RotateMode.FastBeyond360`
- 确认 `LockAttackDirection()` 设置 `isDirectionLocked = true`。
- 确认 `UnlockAttackDirection()` 设置 `isDirectionLocked = false` 并调用 `ApplyAimRotation()`。
- 确认 `SetAimDirection()` 在 `isDirectionLocked == true` 时直接返回。

**原因：**

保证 Arc 以瞄准方向为中心、FullCircle 完整旋转 360°、攻击期间方向不被覆盖。

---

### 6. WeaponAnimator.cs

**修改：**

- 确认接口为：
  ```csharp
  public void Play(
      float startAngle,
      float endAngle,
      float duration,
      Ease ease,
      RotateMode rotateMode,
      float activeMomentRatio,
      Action onActiveMoment = null)
  ```
- 确认不读取 `AttackData`。
- 确认不管理 `WeaponSprite`。
- 确认 `Stop()` 只 Kill Tween，**不修改 `weaponPivot.localRotation`**。
- 确认 `Stop()` 不负责恢复武器默认姿态；恢复由 `WeaponController.UnlockAttackDirection()` 处理。

**原因：**

`WeaponAnimator` 必须是纯 Tween 播放器，不参与攻击逻辑，也不拥有 `WeaponPivot` 的恢复权。

---

### 7. AttackIndicator.cs

**修改：**

- 检查 `Show()` 方法是否设置 `gameObject.SetActive(true)` 或 `spriteRenderer.enabled = true`。
- 检查 `Hide()` 方法是否设置 `gameObject.SetActive(false)` 或 `spriteRenderer.enabled = false`。
- 检查 `SetRadius(float radius)` 是否正确缩放视觉范围。
- **新增 `SetAngle(float angle)`：**
  - 扇形指示器使用，设置扇形全角。
  - 圆形攻击可忽略或设置为 360°。
- **新增 `SetDirection(Vector2 direction)`：**
  - 扇形指示器使用，设置扇形朝向。
  - 圆形攻击可忽略。
- 检查 `SpriteRenderer` 的 `Sorting Order` 是否高于角色，确保可见。

**调用方式：**

```csharp
// PlayerCombat / EnemyAI 进入攻击时
indicator.SetRadius(attackData.AttackRange);
indicator.SetAngle(attackData.AttackAngle);
indicator.SetDirection(attackDirection);
indicator.Show();
```

**原因：**

当前 `AttackIndicator` 只显示圆形，无法正确表现扇形攻击范围。必须让指示器支持角度与方向，才能与动画和判定完全同步。

---

### 8. Player.prefab / Enemy.prefab

**检查：**

- `WeaponController` 组件：
  - `weaponPivot` 引用正确
  - `weaponSprite` 引用正确
  - `attackData` 引用正确
- `WeaponAnimator` 组件：
  - `weaponPivot` 引用正确
- `PlayerCombat` 组件：
  - `aimController` 引用正确
  - `weaponController` 引用正确
  - `weaponAnimator` 引用正确
- `EnemyAI` 组件：
  - `weaponController` 引用正确
  - `weaponAnimator` 引用正确
  - `indicator` 引用正确
- `WeaponPivot`：
  - `localPosition = (0, 0, 0)`
- `WeaponSprite`：
  - `localRotation = (0, 0, -90)`

**原因：**

引用缺失或层级错误会直接导致运行时 NullReference 或动画异常。

---

### 9. AttackData Assets

**检查：**

- `PlayerAttackData.asset`：
  - `attackShape = Fan`
  - `animationType = Arc`
  - `attackAngle = 120`
  - `attackRange = 1.5` 或 `1.8`
- `EnemyMeleeAttackData.asset`：
  - `attackShape = Fan`
  - `animationType = Arc`
  - `attackAngle = 120`
  - `attackRange = 1.2` 或 `1.5`

**说明：**

- 普通近战敌人默认使用 `Arc`，避免所有敌人都原地旋转 360°。
- `FullCircle` / `Spin` 仅用于特殊旋转攻击敌人或 Boss。

**原因：**

确保动画角度、武器长度、判定范围来自同一份数据。

---

## 四、修改后的完整攻击流程

### Player 攻击流程

```
Input System
    ↓
PlayerController.OnMove() / OnAttack()
    ↓
PlayerCombat.TryAttack()
    ↓
weaponController.LockAttackDirection()  ← 记录当前鼠标方向
attackDirection = weaponController.GetAimDirection()  ← 缓存方向
    ↓
Windup 阶段
    ↓
EnterActive()
    ↓
weaponAnimator.Play(
    weaponController.GetAttackStartAngle(),
    weaponController.GetAttackEndAngle(),
    ...
)
    ↓
ActiveMoment
    ↓
PlayerCombat.PerformFanAttack()  ← 使用缓存的 attackDirection
    ↓
Recovery 阶段
    ↓
weaponAnimator.Stop()
weaponController.UnlockAttackDirection()  ← 恢复鼠标跟随
```

### Enemy 攻击流程

```
EnemyAI.UpdateChase()
    ↓
weaponController.SetAimDirection(dir)  ← 持续朝向玩家
    ↓
判断攻击条件
    ↓
EnemyAI.ChangeState(State.Attack)
    ↓
weaponController.LockAttackDirection()
    ↓
indicator.SetRadius(attackData.AttackRange)
indicator.SetAngle(attackData.AttackAngle)
indicator.SetDirection(weaponController.GetAimDirection())
indicator.Show()
    ↓
Windup 阶段
    ↓
EnterActive()
    ↓
weaponAnimator.Play(...)
    ↓
ActiveMoment
    ↓
EnemyAI.OnActiveMoment()
    ↓
EnemyCombat.TryAttack(damageable)
    ↓
Recovery 阶段
    ↓
weaponAnimator.Stop()
weaponController.UnlockAttackDirection()
indicator.Hide()
    ↓
ChangeState(State.Chase)
```

---

## 五、验证清单

### 玩家

- [ ] 鼠标移动时武器持续跟随鼠标方向。
- [ ] 攻击开始时武器方向锁定，不再跟随鼠标。
- [ ] 攻击动画以锁定方向为中心，角度等于 `AttackAngle`。
- [ ] 武器长度等于 `AttackRange`。
- [ ] 攻击判定范围与动画范围一致。
- [ ] 攻击结束后武器恢复跟随鼠标。
- [ ] 无 Input System 报错。

### Enemy

- [ ] Chase 状态武器持续朝向玩家。
- [ ] 进入 Attack 状态时 `AttackIndicator` 显示范围。
- [ ] Windup 期间指示器保持显示。
- [ ] Active 期间武器动画播放：
  - `Arc`：按 `AttackAngle` 挥舞（默认近战敌人）
  - `FullCircle`：完整旋转 360°（仅旋转攻击敌人）
- [ ] ActiveMoment 时造成伤害。
- [ ] Recovery 结束时指示器隐藏，武器恢复朝向玩家。

### 系统

- [ ] `dotnet build Assembly-CSharp.csproj`：`0 警告，0 错误`。
- [ ] 无 `MissingReferenceException`。
- [ ] 无 `NullReferenceException`。
- [ ] 无 Input System 异常。
- [ ] `WeaponAnimator` 不读取 `AttackData`。
- [ ] `WeaponAnimator` 不执行伤害逻辑。
- [ ] `WeaponController` 是 `WeaponPivot` 唯一管理者。

---

## 六、禁止事项

本次修复严格禁止：

- 删除 `AttackData`
- 删除 `WeaponController`
- 删除 `WeaponAnimator`
- 恢复旧 `WeaponAnimator` 结构
- 恢复旧 `EnemyCombat` 结构
- 将攻击逻辑写回 `PlayerController`
- 将动画逻辑写入 `PlayerCombat` / `EnemyAI`
- 增加新动画类型
- 增加技能系统
- 增加新职业/角色

---

## 七、最终目标

完成 Fix Final4 后：

- Player 与 Enemy 使用 Final3 统一攻击框架。
- Input System 正常运行。
- Enemy 攻击范围正确显示。
- 武器动画与攻击判定范围严格同步。
- 无运行时错误。
- v0.4.5.2 可稳定进入测试阶段。

---

## 八、待确认

1. 是否确认按 Fix Final4 执行 Bug 修复？
2. `PlayerAttackData.asset` 的 `AttackRange` 保持 `1.5` 还是改为 `1.8`？
3. `EnemyMeleeAttackData.asset` 的 `AttackRange` 保持 `1.2` 还是改为 `1.5`？
