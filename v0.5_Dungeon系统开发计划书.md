# v0.5 开发计划书：随机地牢（Dungeon）系统

## 版本信息
- **版本号**：v0.5（含 v0.5.0 ~ v0.5.4 五个子版本）
- **版本名称**：随机地牢（Dungeon）系统 —— 可长期扩展的 Dungeon Framework
- **前置版本**：v0.4.6（武器实体碰撞判定 + Player/Enemy 世界空间状态条已完成）
- **美术策略**：程序员美术（纯色几何图形 + Tilemap 着色）
- **总目标**：建立一套数据驱动、模块解耦的地牢框架，支撑 Boss、商店、宝箱、事件房、精英房、多层地牢等玩法，持续开发至 v1.0
- **约束**：
  1. 保持现有 Player、Enemy、Combat、Weapon、UI 架构，不做推翻式重构；
  2. 优先新增 Dungeon 模块，对已有模块的修改仅限「小口径接入点」（每个改动需在本计划中列明）；
  3. 房间类型、敌人生成规则等全部走 ScriptableObject 配置，禁止硬编码；
  4. 参考《元气骑士》《挺进地牢》的房间式 RogueLike 结构，但不照搬，按本项目 Unity 2D 现状重新设计。

> 本计划书是 v0.5 的总纲。各子版本开工时，按本计划书第五章对应小节出**执行文档**（含完整代码），风格沿用 v0.4.6_正式计划；每个子版本收尾时在对应小节补「执行记录」。

---

## 〇、对原始五阶段计划的评审与调整

**总体结论：原始拆分（生成 → 流程 → 内容 → 类型 → 循环）节奏合理，先框架后内容、每阶段独立可验收，符合 v0.3/v0.4 的演进方式，予以保留，不需要推翻重排。** 但有 8 处必须补充/调整，全部已并入本计划书正文：

| # | 评审意见 | 处理 |
|---|----------|------|
| 1 | **v0.5.0 必须包含种子系统与布局自检**。随机生成没有可复现性就无法调试——报 bug 时给一个 seed 就能还原整张图。这是所有随机系统的地基，不能拖到后面补 | 并入 v0.5.0 模块设计（四、五-A） |
| 2 | **必须「数据与表现分离」**：DungeonGenerator 产出纯 C# 布局数据（不碰场景），DungeonBuilder 才负责实例化。这是 Isaac/EtG 类架构可长期扩展的核心，也让生成逻辑可以脱离场景做 1000 种子自检 | 确立为总体设计原则（二、三） |
| 3 | **原计划遗漏了最大的隐性冲突：敌人休眠机制**。现有 EnemyAI 是 Vector2.Distance 距离检测（detectionRange≈5），**无视线遮挡**——不处理的话，隔壁房间的敌人会隔墙索敌、追房。房间制玩法必须配「未进入房间敌人休眠、进房激活」 | 并入 v0.5.1（与门/状态机是同一套流程） |
| 4 | **v0.5.0 的布局数据必须暴露图信息**（BFS 距离、叶子房间、连接度），否则 v0.5.3 的特殊房间选址（Boss=最远、宝箱/商店=叶子房）要返工 | 并入四、数据模型 |
| 5 | **房间实例化方案必须在 v0.5.0 定死**：共享 Tilemap 运行时绘制 vs 房间 Prefab。本计划选定共享 Tilemap 绘制（理由见 3.4 选型表），后续所有阶段都建立在这个决策上 | 3.4 选型表 |
| 6 | **v0.5.3 的商店/事件房只做占位交互，不做经济系统**。货币、掉落物、购买结算是一条独立的系统线，塞进 v0.5.3 会导致范围膨胀 | v0.5.3 范围声明 |
| 7 | **v0.5.4 需明确三件事**：单场景重建（不加载新场景）、玩家对象跨楼层保留、旧楼层对象统一清理防泄漏 | v0.5.4 模块设计 |
| 8 | 交互方式统一为 **walk-over（走过去触发）**，不新增互动按键、不改 Input Actions——把输入系统的改动留给真正需要它的版本 | 各子版本交互设计 |

另：原始描述称「前四个小版本」，实际列出的是五个（v0.5.0~v0.5.4），本计划书按五个处理。

### 附：外部评审处置记录（2026-07-20）

> 两轮外部评审建议的处置结论。已并入正文的不再赘述，此处重点记录**降级/不采纳**的决策与理由，防止日后重复争论。

**第一轮（通用五点建议）：**

| 建议 | 处置 | 理由 |
|------|------|------|
| Minimap 提前到 v0.5 | 降级为 v0.5.4 可选拉伸任务（见五-E） | 8~12 房 + 自由跟随相机，迷路成本低；小地图的价值在房间类型出现后（看图标找房）才显现；钩子（Layout 纯数据 + 房间状态事件）已预留 |
| Generation Validator | 已在计划内（五-A 自检 + 五-D 扩展断言）；种子数 100→1000 | EditMode 测试框架 + CI 不采纳：单人开发无测试基建，右键菜单已够，属零收益基建膨胀 |
| RandomManager 全局随机管理器 | 不采纳 | 全局单例本身即污染源；现有「主 seed → 房间子 seed → 楼层 seed」派生链无全局状态，更确定、可离线自检 |
| Door/Room 动画音效接口 | 不采纳（当前版本） | 不引入无当前需求的间接层（v0.4.6 评审先例）；Door.Open/Close 单一调用点即钩子，音效版本到来时直接挂 |
| Dungeon Save/Load | 不采纳（当前版本），洞察记入十、未来扩展 | 一局流程短，无中断恢复需求；seed 驱动使未来存档成本极低 |

**第二轮（落地三点建议）：**

| 建议 | 处置 | 理由 |
|------|------|------|
| Room 记录 RoomBounds | 已覆盖，正文补明文 | Room 自 v0.5.0 起持有世界边界 Rect；数据/表现分离决定边界来自布局算术，Tilemap 是下游产物、不是数据源 |
| Room 用 Guid / 自增 ID | 采纳自增 ID，不采纳 Guid | id 语义明确为「生成器分配、同 seed 稳定」；Guid 的全局唯一性无消费场景，调试可读性差、存档体积更大 |
| 新增性能规划章 | 采纳为决策记录（见九），机制不提前实现 | 休眠制本身即是 v0.5 性能方案；激活剔除/对象池只记触发条件，测到问题再做 |

---

## 一、现状分析（v0.4.6 完成度）

### 1.1 可复用资产（本版本不动的部分）

| 资产 | 路径 | 复用方式 |
|------|------|----------|
| 玩家系统 | `PlayerController/PlayerStats/Health/PlayerCombat/PlayerAimController` | **保留不变**，跨楼层持久存在 |
| 敌人系统 | `EnemyController/EnemyStats/EnemyHealth/EnemyAI/EnemyCombat` | **保留不变**，由房间系统控制休眠/激活 |
| 武器攻击框架 | `AttackData/WeaponController/WeaponAnimator/WeaponHitbox/AttackIndicator` | **保留不变** |
| 血条 UI | `WorldSpaceHealthBar/PlayerWorldStatusBar/PlayerUI` | **保留不变** |
| 瓦片资产 | `FloorTile.asset`（浅色）、`WallTile.asset`（深色） | **直接用于运行时绘制** |
| Tilemap 碰撞结构 | Walls：TilemapCollider2D + CompositeCollider2D + Rigidbody2D(Static) | 结构照搬，瓦片改由代码绘制 |
| 相机 | `CameraFollow.cs` | 保留自由跟随，仅加 1 个 Snap 小方法（见 1.3） |
| 场景范式 | Grid/Floor/Walls + Player + Global Light 2D | v0_5 场景沿用 |

### 1.2 现有架构与房间制玩法的三个冲突点（必须先认清）

| 冲突 | 现状 | 后果 | 对策（所属子版本） |
|------|------|------|--------------------|
| 敌人索敌无遮挡 | EnemyAI 用纯距离判定，detectionRange≈5 | 隔壁房间敌人隔墙追击，房间制失效 | 房间休眠制（v0.5.1） |
| 地图是静态手绘 | v0.1 小屋 Floor/Walls 手工绘制 | 无法随机 | 运行时 Tilemap 绘制（v0.5.0） |
| 单场景一次性内容 | 无对象清理与重建概念 | 楼层循环会泄漏 | 统一 dungeonRoot 挂载 + 单场景重建（v0.5.4） |

### 1.3 对已有模块的全部预期改动（小口径接入点，提前列明）

| 模块 | 改动 | 所属子版本 |
|------|------|------------|
| `CameraFollow` | +`SnapToTarget()`（2 行，楼层切换/出生时相机瞬移，避免镜头横穿全图） | v0.5.0 |
| `EnemyStats` | +`ApplyFloorScale(float hpMul, float dmgMul)` 一类的小方法（难度缩放注入点，不改任何现有逻辑） | v0.5.4 |
| 场景 | 复制 `v0_4_EnemySystem.unity` → `v0_5_Dungeon.unity`，删除静态小屋 Grid，换空 Tilemap + DungeonSystem 对象 | v0.5.0 |

除上表外，Player/Enemy/Combat/Weapon/UI 任何脚本**零改动**。TrainingDummy 木桩保留在原场景供回归测试，不进地牢。

---

## 二、总体目标与设计原则

### 2.1 总体目标

1. 每局游戏随机生成 8~12 个连通房间，含出生房与 Boss 房；
2. 房间有完整流程：未进入 → 战斗中（关门）→ 已完成（开门）；
3. 房间内容由配置驱动随机生成（敌人、障碍物、装饰）；
4. 房间有类型系统：Start / Combat / Elite / Treasure / Shop / Event / Boss；
5. 打通楼层循环：Boss → 奖励 → 下一层 → 重新生成 → 难度递增。

### 2.2 设计原则（全版本通用，写进每个子版本验收）

1. **数据与表现分离**：生成算法是纯 C#（不依赖场景/MonoBehaviour），实例化是另一层。生成器可以脱离场景被批量自检。
2. **数据驱动**：房间类型、生成规则、敌人生成表全部 ScriptableObject，改内容不改码。
3. **种子可复现**：一切随机走独立的 `System.Random` 实例（**禁止用 `UnityEngine.Random`**，避免全局随机流被污染），同一个 seed 生成同一张图。
4. **低耦合**：Room 不认识 EnemyAI，只认「注册了若干个会死的东西」；Spawner 不认识 Room 状态机，只负责在房间里放东西。
5. **对已有模块最小侵入**：只允许 1.3 表中的小口径改动。

---

## 三、总体架构

### 3.1 模块全景图

```
┌─ 纯 C# 数据层（无场景依赖，可离线自检）────────────────┐
│  DungeonGenerator  ──产出──►  DungeonLayout           │
│   （网格邻接生长算法）          ├─ List<RoomNode>        │
│  RoomTypeAssigner (v0.5.3)    │    · gridPos / type    │
│   （按规则填房间类型）          │    · distanceFromStart │
│                               │    · connections       │
│                               └─ List<RoomConnection>  │
└──────────────────────────────┬───────────────────────┘
                               │ 唯一数据流：布局 → 实例化
┌─ 场景实例化层 ───────────────▼───────────────────────┐
│  DungeonManager（门面：Generate/Cleanup、房间注册表、  │
│                   全局事件 OnRoomEntered/OnRoomCleared）│
│  DungeonBuilder（读 Layout：画 Tilemap、建 Room/      │
│                  Door/RoomTrigger、放玩家）            │
└──────────────────────────────┬───────────────────────┘
                               │
┌─ 运行时层 ───────────────────▼───────────────────────┐
│  Room（状态机 Unvisited/Active/Cleared、门管理、      │
│        敌人注册与清房判定、内容挂载点 contentRoot）     │
│  Door（开/关：碰撞体 + 视觉）                         │
│  RoomTrigger（玩家进入检测）                           │
│  Enemy/Obstacle/Decoration（挂在 contentRoot 下，     │
│        未进入时整体 SetActive(false) 休眠）            │
└──────────────────────────────┬───────────────────────┘
                               │
┌─ 配置层（ScriptableObject，Assets/Data/Dungeon/）─────┐
│  DungeonConfig（房间数/尺寸/门宽/种子/特殊房数量/      │
│                 楼层难度缩放参数）                     │
│  SpawnTable（敌人/障碍物/装饰 权重表）(v0.5.2)         │
│  RoomContentProfile（房间类型→内容表映射）(v0.5.2)     │
│  RoomTypeConfig（类型→着色/清房条件/内容配置）(v0.5.3) │
└──────────────────────────────────────────────────────┘
```

### 3.2 一次生成的完整数据流

```
seed ──► DungeonGenerator.Generate(config, seed)
           ──► DungeonLayout（纯数据，可自检：连通性/房间数/Boss距离）
             ──► DungeonBuilder.Build(layout)
                   ──► 画 Floor/Walls Tilemap + 开门洞
                   ──► 每房间建 Room 对象（Room + RoomTrigger + contentRoot）
                   ──► 每条连接建 Door 对象
                   ──► (v0.5.2) Spawner 按配置表往 contentRoot 填内容
                   ──► 玩家传送至起始房中心 + CameraFollow.SnapToTarget()
```

### 3.3 目录结构

```
Assets/Scripts/v0_5/
├── Core/         DungeonManager, DungeonBuilder, DungeonConfig
├── Generation/   DungeonGenerator, DungeonLayout, RoomNode, RoomConnection,
│                 RoomTypeAssigner（v0.5.3）
├── Runtime/      Room, Door, RoomTrigger, RoomState（枚举）
├── Content/      SpawnTable, RoomContentProfile,
│                 EnemySpawner, ObstacleSpawner, DecorationSpawner（v0.5.2）
└── Interaction/  Interactable, ChestInteractable, ShrineInteractable,
                  PortalInteractable（v0.5.3~v0.5.4）

Assets/Data/Dungeon/        全部 SO 资产（Config/SpawnTable/RoomTypeConfig）
Assets/Prefabs/Dungeon/     Room、Door、宝箱、传送门等预制体
Assets/Scenes/v0_5_Dungeon.unity
```

> 按 `now_use/README.md` 维护约定：v0.5 开发期脚本放 `v0_5/`，v0.5 全部完成后，将在用脚本（连带 .meta）迁入 `now_use/` 并更新 README 清单。

### 3.4 核心技术选型

| # | 决策项 | 选定方案 | 备选 | 理由 |
|---|--------|----------|------|------|
| 1 | 布局表示 | **抽象网格图（纯 C#）**：房间占一个粗网格格子，连接即图边 | 直接在场景里摆 | 数据/表现分离是全部扩展性的前提；可离线自检 |
| 2 | 生成算法 | **网格邻接生长法**（Isaac 式）：从起点向外随机贴房，天然连通 | BSP 分割 | 贴合「房间制」；实现量小；连通性由构造保证，无需补走廊 |
| 3 | 房间实例化 | **共享 Tilemap 运行时绘制**：全楼层共用一套 Floor/Walls，代码画房间 | 房间 Prefab 拼装 | 复用 v0.1 已验证的碰撞结构；单份碰撞器；绘制逻辑集中在 Builder |
| 4 | 房间连接 | **直接邻接 + 共享墙开门洞，无走廊**（v0.5.x） | 走廊连接 | 最简；数据模型里连接是「边」，未来加走廊只改 Builder 不改模型 |
| 5 | 房间尺寸 | **全楼层统一房间尺寸**（内部 18×11 瓦片，可配置） | 每房随机尺寸 | 邻接开门洞要求共享墙对齐，统一尺寸省去对齐问题；多尺寸/手工房间模板列为未来扩展 |
| 6 | 随机源 | **显式 seed 的 `System.Random` 实例**，逐层传递 | UnityEngine.Random | 可复现、可自检、不污染全局随机流 |
| 7 | 敌人激活 | **房间休眠制**：contentRoot.SetActive(false)，进房激活 | 全程激活 | 解决隔墙索敌 + 省性能；实现只需一个挂载点 |
| 8 | 相机 | **保持自由跟随**（不做房间锁屏相机） | Isaac 式切房相机 | 零改动；锁屏相机属于手感打磨，未来再说 |
| 9 | 楼层切换 | **单场景重建**：清 Tilemap + 销毁 dungeonRoot，原地重新生成 | 加载新场景 | 无场景加载问题；玩家对象天然保留；强制走通「清理」这条链路 |
| 10 | 清房条件 | **按房间类型可配置**（None / AllEnemiesDead），枚举字段 | 写死杀光 | 宝箱房/商店房不关门不战斗；Boss 房未来可插自定义条件 |

> 选型 3 的代价：未来若要《挺进地牢》式手工设计房间（预设布局的房间块），需补一层「房间模板 Prefab」支持。已在十、未来扩展中预留钩子，v0.5.x 不实施。

---

## 四、核心数据模型（v0.5.0 定死，全版本通用）

### 4.1 布局数据（纯 C#，仅用 UnityEngine 基础类型，不碰场景）

```csharp
// Assets/Scripts/v0_5/Generation/DungeonLayout.cs
public enum RoomType { Start, Combat, Elite, Treasure, Shop, Event, Boss }  // v0.5.0 只用 Start/Combat/Boss

public class RoomNode
{
    public int id;                      // 生成器分配的自增 ID，同 seed 重生成保持稳定（存档/回放引房依据）
    public Vector2Int gridPos;          // 粗网格坐标
    public RoomType type;               // v0.5.0 填 Start/Combat/Boss，v0.5.3 由 Assigner 填全
    public int distanceFromStart;       // BFS 距离（Boss/特殊房选址依据）
    public List<RoomConnection> connections = new();
    public bool IsLeaf => connections.Count == 1;   // 叶子房（宝箱/商店选址依据）
}

public class RoomConnection
{
    public RoomNode a, b;               // 无向边
    // 未来扩展钩子：corridor 数据、门类型（普通/锁/秘密）都加在这里
}

public class DungeonLayout
{
    public int seed;
    public List<RoomNode> rooms = new();
    public List<RoomConnection> connections = new();
    public RoomNode startRoom, bossRoom;
}
```

### 4.2 楼层配置（ScriptableObject）

```csharp
// Assets/Scripts/v0_5/Core/DungeonConfig.cs
[CreateAssetMenu(menuName = "Dungeon/DungeonConfig")]
public class DungeonConfig : ScriptableObject
{
    [Header("地图")]
    public int roomCountMin = 8, roomCountMax = 12;
    public int roomWidth = 18, roomHeight = 11;   // 房间内部尺寸（瓦片数）
    public int doorWidth = 2;                      // 门洞宽（瓦片数）

    [Header("特殊房间（v0.5.3 启用）")]
    public int treasureCount = 1;
    public int shopCount = 1;
    public int eventCount = 0;
    [Range(0f,1f)] public float eliteChance = 0.15f;
    public int bossMinDistance = 3;                // Boss 房距出生房最小 BFS 距离（尽力满足）

    [Header("楼层缩放（v0.5.4 启用）")]
    public int enemyCountBonusPerFloor = 1;        // 每层敌人数量 +N（封顶另定）
    public float hpMultiplierPerFloor = 0.15f;     // 每层敌人 HP ×(1 + N×(floor-1))
}
```

### 4.3 生成算法（网格邻接生长法）

```
1. 在粗网格 (0,0) 放置起始房间
2. 候选列表 = 所有与已放置房间正交相邻的空格子
3. target = rand(roomCountMin, roomCountMax+1)
4. 循环，直到房间数达到 target 或连续失败次数超限：
     - 从候选列表随机取一格（System.Random）
     - 放置房间，与所有相邻的已放置房间建立连接（自然产生环路，地图不是纯树）
     - 更新候选列表
5. 若房间数 < roomCountMin → 整张图重roll（最多 50 次，实际几乎不会触发）
6. 从起始房 BFS，填每个房间的 distanceFromStart
7. bossRoom = distanceFromStart 最大的房间；并列时优先叶子房；尽量满足 bossMinDistance
```

- **连通性由构造保证**（每个房间都贴在已有房间上），无需事后修补。
- 关键不变量（写进自检）：房间数 ∈ [min,max]；任意房间可达 start；boss ≠ start。

### 4.4 世界坐标换算

- 一个粗网格格子 = `(roomWidth + 2) × (roomHeight + 2)` 瓦片（内部 + 四周各 1 格墙）。
- 相邻房间的共用墙是同一条瓦片线，只画一次；门洞在共用墙中段 `doorWidth` 格。
- 房间 (0,0) 内部左下角为世界原点，格子坐标 × 格子尺寸 = 世界偏移。

---

## 五、子版本开发计划

---

### 五-A、v0.5.0：随机地图生成基础

**目标**：随机生成 8~12 个连通房间，玩家出生在起始房，Boss 房位置确定（仅标记，无内容）。
**范围**：只生成地图。**不生成敌人、不做房间逻辑、不做门开关交互**（门洞敞开）。
**不碰**：任何现有游戏逻辑脚本（唯一例外：CameraFollow +SnapToTarget）。

#### 模块设计

| 模块 | 类型 | 职责 |
|------|------|------|
| `DungeonGenerator` | 纯 C# | `Generate(DungeonConfig, int seed) → DungeonLayout`，算法见 4.3 |
| `DungeonLayout/RoomNode/RoomConnection` | 纯 C# | 见 4.1 |
| `DungeonConfig` | SO | 见 4.2（本阶段只用地图部分字段） |
| `DungeonBuilder` | MB | 读 Layout：清旧图 → 画 Floor/Walls → 开门洞 → 建 Room 空壳（仅 Room 组件 + 边界数据，无状态机）→ 返回起始房中心 |
| `Room` | MB（基础版） | 持有 RoomNode 数据、世界边界 Rect（**唯一边界来源，布局算术得出，禁止从 Tilemap 反算**）、类型；**本阶段无状态机** |
| `DungeonManager` | MB（门面） | 持有 Config 与 seed；`Generate()` 串起 Generator→Builder→传送玩家；房间注册表；Scene 视图 Gizmos 画布局（房间框 + 连线 + Start 绿/Boss 红标记） |
| `CameraFollow` | 修改 | +`SnapToTarget()`：瞬移到玩家（2 行） |

#### 调试与自检（评审意见 #1 的落地，本阶段必备）

- Inspector 暴露 `seed`（0 = 每次随机）；Play 后用同一个 seed 重进 = 同一张图。
- `DungeonManager` 右键菜单 **「Validate 1000 Seeds」**：纯代码跑 1000 个 seed，逐个断言连通性/房间数/Boss≠Start，输出统计（平均房间数、平均 Boss 距离）。**不依赖场景**。
- Gizmos 实时显示布局框线，Scene 视图一眼看全图。

#### 任务分解

| 任务 | 内容 | 涉及文件 |
|------|------|----------|
| 任务0 | 复制场景 `v0_4_EnemySystem.unity` → `v0_5_Dungeon.unity`；删静态小屋；建空 Grid/Floor/Walls（碰撞结构照抄 v0.1）+ DungeonSystem 空对象；建 `Scripts/v0_5/` 与 `Data/Dungeon/` 目录 | 场景、目录 |
| 任务1 | 编写数据模型（DungeonLayout/RoomNode/RoomConnection/RoomType） | `Generation/`（新建） |
| 任务2 | 编写 DungeonGenerator（算法 4.3 + BFS + Boss 选址） | `Generation/DungeonGenerator.cs`（新建） |
| 任务3 | 编写 DungeonConfig SO + 创建默认资产 `DefaultDungeonConfig.asset` | `Core/DungeonConfig.cs`、`Data/Dungeon/` |
| 任务4 | 编写 DungeonBuilder（Tilemap 绘制 + 门洞 + Room 空壳） | `Core/DungeonBuilder.cs`、`Runtime/Room.cs`（新建） |
| 任务5 | 编写 DungeonManager（门面 + Gizmos + Validate 1000 Seeds） | `Core/DungeonManager.cs`（新建） |
| 任务6 | CameraFollow +SnapToTarget；玩家传送至起始房；跑 Validate 自检 | `now_use/CameraFollow.cs`（+2 行） |
| 任务7 | 运行验收（见下） | 场景/Console |

#### 验收清单

- [ ] 每次 Play 生成 8~12 个房间，布局互不相同
- [ ] 同 seed 重进 → 布局完全一致
- [ ] 「Validate 1000 Seeds」全部通过：连通、数量达标、Boss≠Start、Boss 距离尽量 ≥3
- [ ] 玩家出生在起始房中心，相机不横穿地图
- [ ] 墙体碰撞生效：走不出房间外边界，门洞可通行
- [ ] Gizmos 中 Start 房/Boss 房标记正确
- [ ] 场景内无敌人、无任何房间逻辑（本阶段范围声明）
- [ ] Console 无 Error / 无 Warning；Player.prefab / Enemy.prefab 无引用变更

---

### 五-B、v0.5.1：房间流程系统

**目标**：完整房间状态机 + 门开关流程 + 敌人休眠/唤醒。为后续所有房间玩法提供统一流程控制。
**前置**：v0.5.0。
**范围**：敌人由**手工摆放**进房间测试（生成器是 v0.5.2 的事）；清房条件枚举就位但本阶段只用 None / AllEnemiesDead。

#### 模块设计

**房间状态机（Room 升级为完整版）**：

```
                玩家首次进入（RoomTrigger）
   ┌───────────┐ ──────────────────────► ┌───────────┐
   │ Unvisited │                         │  Active   │
   │ （未进入） │                         │ （战斗中） │
   └───────────┘                         └─────┬─────┘
        ▲                                      │ 清房条件满足
        │                                      │（注册敌人全灭 / 无条件直接完成）
        │                                      ▼
        │                                 ┌───────────┐
        └───────── 不可返回 ──────────────│  Cleared  │
                                          │ （已完成） │
                                          └───────────┘
```

| 模块 | 职责 |
|------|------|
| `RoomState` | 枚举：Unvisited / Active / Cleared |
| `Room`（完整版） | 状态机；`clearCondition`（None / AllEnemiesDead）；持有 doors、contentRoot；`RegisterEnemy(EnemyHealth)` / `NotifyEnemyDied()`；事件 `OnRoomEntered/OnRoomCleared`（C# Action，风格同 WeaponHitbox.OnHit） |
| `Door` | `Open()/Close()`：切换 Collider2D + 视觉（纯色块显隐，程序员美术）；`RefreshState()`：**仅当相邻两个 Room 都不是 Active 时才开**（避免隔壁 Active 时门被误开） |
| `RoomTrigger` | Trigger 碰撞体覆盖房间内部（**四边内缩 0.5 格**，保证玩家完全进房后才触发关门）；只认 Player 层 |
| 休眠机制 | contentRoot：战斗类房间初始 `SetActive(false)`，进 Active 时 `SetActive(true)`。敌人 AI/血条/动画随之整体休眠，**对敌人脚本零改动** |

**进房流程**：
```
RoomTrigger 检测到 Player
  → Room.Enter()：仅 Unvisited 响应
    → state = Active，广播 OnRoomEntered
    → clearCondition == None → 直接 SetCleared()（门一直开着）
    → clearCondition == AllEnemiesDead →
        contentRoot.SetActive(true)（敌人激活）
        关闭所有 Door（RefreshState）
        注册敌人全灭 → SetCleared()：门全部 RefreshState（开），广播 OnRoomCleared
```

#### 任务分解

| 任务 | 内容 | 涉及文件 |
|------|------|----------|
| 任务0 | 编写 Door（含 Door Prefab：2 格宽色块 + BoxCollider2D）；Builder 在门洞处实例化 Door 并登记到相邻 Room | `Runtime/Door.cs`、`Prefabs/Dungeon/Door.prefab` |
| 任务1 | 编写 RoomTrigger；Builder 为每个 Room 挂 Trigger（内缩 0.5） | `Runtime/RoomTrigger.cs` |
| 任务2 | Room 升级完整状态机 + 清房条件 + 敌人注册/通知 | `Runtime/Room.cs` |
| 任务3 | Builder 接入休眠：战斗类房间 contentRoot 初始隐藏 | `Core/DungeonBuilder.cs` |
| 任务4 | 手工测试：往 2~3 个房间的 contentRoot 下拖入 Enemy prefab 实例，验证全流程 | 场景 |
| 任务5 | 运行验收（见下） | 场景/Console |

#### 验收清单

- [ ] 进入有敌人的房间 → 门关闭（视觉 + 碰撞同时生效），敌人激活索敌
- [ ] 杀光房内敌人 → 门打开；离开、再进入不再触发任何变化
- [ ] 起始房/空房间进门不关门（clearCondition=None）
- [ ] 未进入房间的敌人完全静止（休眠），不会隔墙追击（**重点回归项**）
- [ ] 相邻房间一清一未清时，共用门的开闭状态正确（隔壁 Active 时门保持关）
- [ ] 关门瞬间玩家不会被门碰撞体卡住（Trigger 内缩生效）
- [ ] Console 无 Error / 无 Warning

---

### 五-C、v0.5.2：随机内容生成

**目标**：敌人、障碍物、装饰全部由配置表随机生成；改内容只改 SO 资产，不改代码。
**前置**：v0.5.1（内容挂在 contentRoot 下才有休眠与清房判定）。
**测试内容物**：现有 `Enemy.prefab` 作为唯一敌人种（后续版本再加新敌人，框架先行）。

#### 模块设计

```csharp
// Assets/Scripts/v0_5/Content/SpawnTable.cs
[CreateAssetMenu(menuName = "Dungeon/SpawnTable")]
public class SpawnTable : ScriptableObject
{
    [System.Serializable] public class Entry
    {
        public GameObject prefab; public int weight = 1;
        [Tooltip("仅障碍物表使用：可被玩家武器破坏")] public bool destructible;
        [Tooltip("仅障碍物表使用：可破坏时的血量（≈需要砍的刀数）")] public int hp = 3;
    }
    public List<Entry> entries;
    public int countMin = 2, countMax = 4;    // 每个房间生成数量区间
}

// Assets/Scripts/v0_5/Content/RoomContentProfile.cs
[CreateAssetMenu(menuName = "Dungeon/RoomContentProfile")]
public class RoomContentProfile : ScriptableObject
{
    public SpawnTable enemyTable;        // 可空（空 = 无战斗）
    public SpawnTable obstacleTable;     // 可空
    public SpawnTable decorationTable;   // 可空
}
```

| 模块 | 职责 |
|------|------|
| `EnemySpawner` | 静态方法 `Spawn(Room, SpawnTable, System.Random)`：按权重抽 prefab、找合法位置、实例化到 contentRoot 下、`room.RegisterEnemy()` 接管死亡计数。**不直接 SetActive**——休眠由 contentRoot 统一管 |
| `ObstacleSpawner` | 同上，放障碍物（纯色方块 + BoxCollider2D，挡移动）；条目 `destructible=true` 时实例化后按条目 `hp` 挂 `ObstacleHealth` |
| `ObstacleHealth` + `DestructibleObstacle` | 可破坏障碍物（放 `now_use/`，非地牢专属）：`ObstacleHealth` 实现 `IDamageable`（HP + `OnTakeDamage`/`OnDestroyed` 事件），直接接入现有 WeaponHitbox 判定链；`DestructibleObstacle` 监听事件做受击闪白、随血量颜色变深（裂纹占位）、破坏时销毁 GameObject |
| `DecorationSpawner` | 同上，放纯装饰（无碰撞，sortingOrder 低于角色） |
| `DungeonBuilder` | 建完房间后按房间的 ContentProfile 调三个 Spawner（v0.5.2 阶段所有战斗房用同一份默认 Profile，v0.5.3 才按类型分） |

**生成位置合法性规则（三个 Spawner 共用）**：
1. 位置在房间内部，距墙 ≥1 格；
2. 距所有门洞中心 ≥2.5 格（**防堵门**，写进验收）；
3. `Physics2D.OverlapCircle(r=0.4)` 无碰撞体（防重叠、防刷进墙里）；
4. 每个位置最多重试 20 次，失败放弃该个（不卡死）；
5. 玩家进房位置不可预知，因此规则 2 同时兼任「别刷在玩家脸上」。

**可破坏障碍物规则**：
1. 障碍物（无论可否破坏）**一律不 `RegisterEnemy`**——破坏障碍物不是清房条件，`AllEnemiesDead` 只数敌人；
2. 新建 `Obstacle` Layer，可破坏障碍物放该层；**玩家与敌人双方武器均可摧毁**（用户决策，2026-07-22）：玩家 `AttackData.targetLayer` = Enemy + Obstacle（192），敌人 = Default + Obstacle（129）；
3. 伤害判定零新代码：`WeaponHitbox` 命中 Obstacle 层 → `TryGetComponent<IDamageable>` → `ObstacleHealth.TakeDamage`；
4. 可破坏与不可破坏必须视觉可区分：可破坏 = 浅棕木箱色，不可破坏 = 灰石块色；
5. 掉落物（打碎掉宝）不在本阶段：`OnDestroyed` 事件就是挂点，v0.5.3 宝箱/掉落落地后再接。

**随机流**：`DungeonManager` 从主 seed 派生每个房间的子 seed（如 `seed*7919 + roomId`）传给 Spawner——**同 seed 下地图与内容完全一致**（验收项）。

#### 任务分解

| 任务 | 内容 | 涉及文件 |
|------|------|----------|
| 任务0 | 编写 SpawnTable / RoomContentProfile SO + 创建默认资产（敌人表=Enemy.prefab 权重1，数量2~4；障碍物表含可破坏/不可破坏条目；装饰一套） | `Content/`、`Data/Dungeon/` |
| 任务1 | 制作占位 prefab：Obstacle（灰方块+碰撞，不可破坏）、Obstacle_Destructible（浅棕木箱色+碰撞+ObstacleHealth）、Decoration×2（地面污渍色块，无碰撞）；新建 Obstacle Layer 并把玩家 AttackData.targetLayer 加上该层 | `Prefabs/Dungeon/`、层与 AttackData 配置 |
| 任务2 | 编写 EnemySpawner / ObstacleSpawner / DecorationSpawner（共用位置合法性工具方法）+ ObstacleHealth / DestructibleObstacle（放 `now_use/`） | `Content/`、`now_use/` |
| 任务3 | Builder 接入内容生成 + 房间子 seed 派生 | `Core/DungeonBuilder.cs`、`Core/DungeonManager.cs` |
| 任务4 | 运行验收（见下） | 场景/Console |

#### 验收清单

- [ ] 每个战斗房生成 2~4 个敌人（配置区间），数量与种类随 seed 变化
- [ ] 同 seed 重进 → 敌人位置/种类/数量完全一致
- [ ] 敌人不刷在门洞 2.5 格内、不刷进墙、不互相重叠
- [ ] 障碍物能挡住玩家移动；装饰无碰撞、渲染在角色脚下
- [ ] 可破坏障碍物能被玩家武器击毁：受击闪白、血尽销毁、销毁后可通行；不可破坏障碍物不受武器影响
- [ ] 敌人和玩家武器均可摧毁可破坏障碍物；可破坏障碍物不计入清房条件（房内障碍物未破坏也可开门）
- [ ] 敌人在 contentRoot 下：未进房休眠、进房激活、全灭开门（v0.5.1 流程无回归）
- [ ] **改 SpawnTable 资产（数量/权重）即改变生成结果，全程不改代码**（数据驱动验收）
- [ ] Console 无 Error / 无 Warning

---

### 五-D、v0.5.3：房间类型系统

**目标**：七种房间类型落地，按类型自动装配内容，地图具备 RogueLike 探索感。
**前置**：v0.5.2（类型靠 ContentProfile 装配内容）。
**范围声明（评审意见 #6）**：商店/事件房**只做占位交互**（walk-over 触发），**不做货币、掉落物、购买结算**；Boss 用「大一号敌人」占位，Boss 专属技能属未来版本。

#### 模块设计

| 模块 | 职责 |
|------|------|
| `RoomTypeAssigner` | 纯 C#，Generator 产出布局后运行：Start=原点；Boss=最远（优先叶子）；Treasure/Shop/Event 按 DungeonConfig 数量**优先叶子房**（叶子不够则取 distanceFromStart≥2 的普通房）；其余 Combat；Combat 中按 eliteChance 抽 Elite（要求 distance≥2） |
| `RoomTypeConfig` | SO（每类型一份）：地板着色 tint、clearCondition、ContentProfile 引用 |
| `DungeonBuilder` | 按类型用 `Tilemap.SetColor` 给该房地板着色（程序员美术，零素材成本）；按类型取 Profile 生成内容 |
| `Interactable` | walk-over 交互基类：Trigger 检测 Player → 一次性触发 → 视觉变为已消耗态。**不改输入系统** |
| `ChestInteractable` | 宝箱：+2 HP 治疗（调现有 `Health.Heal`），日志提示 |
| `ShrineInteractable` | 事件祭坛：随机 ±（治疗 2 / 受伤 1），日志提示 |
| 商店占位 | 3 个基座各放一个免费补给（治疗/护甲球），walk-over 拾取 |
| 精英敌人 | `Enemy_Elite.prefab`：现有 Enemy prefab 变体（1.3× 体型、2× HP、更高伤害、深色），**不改敌人脚本** |
| Boss 占位 | `Enemy_Boss.prefab`：2× 体型、高 HP、沿用现有近战攻击框架（WeaponHitbox 天然支持） |

#### 房间类型一览（数据存于 RoomTypeConfig 资产，可随时调）

| 类型 | 地板色 | 清房条件 | 内容 |
|------|--------|----------|------|
| Start | 绿灰 | None | 空（安全房） |
| Combat | 默认 | AllEnemiesDead | 敌人表 + 障碍 + 装饰 |
| Elite | 暗红 | AllEnemiesDead | 精英敌人 + 更多奖励占位 |
| Treasure | 金黄 | None | 宝箱 ×1 |
| Shop | 蓝 | None | 补给基座 ×3（免费占位） |
| Event | 紫 | None | 祭坛 ×1 |
| Boss | 深红 | AllEnemiesDead | Boss 占位 ×1 |

#### 任务分解

| 任务 | 内容 | 涉及文件 |
|------|------|----------|
| 任务0 | 编写 RoomTypeAssigner 并接入 Generator 流程；自检扩展：断言每层特殊房数量达标 | `Generation/RoomTypeAssigner.cs` |
| 任务1 | 编写 RoomTypeConfig SO + 创建 7 份类型资产；Builder 接入地板着色与按类型装配 | `Core/`、`Data/Dungeon/` |
| 任务2 | 编写 Interactable 基类 + Chest/Shrine/补给基座；制作对应占位 prefab | `Interaction/`、`Prefabs/Dungeon/` |
| 任务3 | 制作 Enemy_Elite / Enemy_Boss prefab 变体 + 对应 SpawnTable | `Prefabs/`、`Data/Dungeon/` |
| 任务4 | 运行验收（见下） | 场景/Console |

#### 验收清单

- [ ] 每层必有 1 Start + 1 Boss；Treasure/Shop 数量符合配置且优先落在叶子房
- [ ] 各类型地板着色正确，Gizmos 中类型标记正确
- [ ] 宝箱/祭坛/商店占位交互生效（走过去触发、一次性、状态可视）
- [ ] 精英房敌人明显更强；Boss 房有大 Boss 占位，杀死后房间 Cleared
- [ ] 特殊房（Treasure/Shop/Event）进门不关门、无战斗
- [ ] 调 DungeonConfig 的 specialCount/eliteChance 即改变生成，不改代码
- [ ] 「Validate 1000 Seeds」扩展断言全部通过
- [ ] Console 无 Error / 无 Warning

---

### 五-E、v0.5.4：完整楼层循环

**目标**：Boss → 奖励结算 → 下一层 → 重新生成 → 难度递增 → 死亡重开，形成完整 Dungeon Loop。
**前置**：v0.5.3。

#### 模块设计

| 模块 | 职责 |
|------|------|
| `RunManager` | 新 MB（挂在 DungeonSystem 对象上）：`floorNumber` 与主 seed 持有；监听 Boss 房 `OnRoomCleared` → 触发奖励结算与传送门生成；`NextFloor()` 流程总控 |
| `DungeonManager` | +`Cleanup()`：销毁 dungeonRoot（房间/门/内容全部挂在其下，**一次销毁零残留**）+ `Tilemap.ClearAllTiles()` |
| 奖励结算 | Boss 房中央生成奖励宝箱（walk-over：治疗 + 占位强化，强化内容为日志提示，接技能/装备系统前一直是占位） |
| `PortalInteractable` | 传送门（walk-over）→ `RunManager.NextFloor()` |
| `EnemyStats` | +`ApplyFloorScale(hpMul, dmgMul)` 小方法（1.3 表已列）；EnemySpawner 生成时按当前楼层注入 |
| 难度缩放 | 每层楼：敌人数量 +enemyCountBonusPerFloor（封顶 8/房），HP ×(1+hpMultiplierPerFloor×(floor-1)) |
| 楼层 seed | `floorSeed = mainSeed + floorNumber*104729`（质数步长），每层布局可复现且互不雷同 |
| 死亡重开 | 玩家死亡 → 2 秒后 floor=1、主 seed 重roll、玩家 HP/护甲回满、重新生成（最简循环；GameOver 界面属打磨项，未来版本） |

**NextFloor 时序**：
```
玩家踩传送门
  → Cleanup()：销毁 dungeonRoot + 清 Tilemap（旧楼层零残留）
  → floorNumber++
  → 计算 floorSeed → Generate()
  → 玩家（保留当前 HP/护甲，不回复——数值手感后续再调）传送至新起始房
  → CameraFollow.SnapToTarget()
```

#### 任务分解

| 任务 | 内容 | 涉及文件 |
|------|------|----------|
| 任务0 | DungeonManager +Cleanup()；dungeonRoot 挂载审计（所有生成物都在其下） | `Core/` |
| 任务1 | 编写 RunManager（楼层数、主 seed、NextFloor 流程） | `Core/RunManager.cs` |
| 任务2 | 编写 PortalInteractable + 奖励宝箱；Boss 房 Cleared 时在房中心生成二者 | `Interaction/`、`Prefabs/Dungeon/` |
| 任务3 | EnemyStats +ApplyFloorScale；EnemySpawner 注入楼层缩放；数量递增 | `now_use/EnemyStats.cs`（+方法）、`Content/EnemySpawner.cs` |
| 任务4 | 死亡重开流程（floor=1 重开 + 玩家状态重置） | `Core/RunManager.cs`、对接 `PlayerController` 死亡事件（只订阅，不改其逻辑） |
| 任务5 | 连续 3 层跑图验收（见下） | 场景/Console |

> **可选拉伸任务**：简易小地图（DungeonLayout 数据驱动 UI，核心循环验收完再做；做不完顺延 v0.6，不阻塞版本，不占主线任务号）。

#### 验收清单

- [ ] 杀死 Boss → 房间 Cleared → 房中心出现奖励宝箱 + 传送门
- [ ] 踩传送门 → 下一层：旧地图/敌人/门全部消失（Hierarchy 无残留），新地图生成，玩家在起始房，楼层数 +1
- [ ] 第 2 层起敌人数量/HP 按配置递增（打起来明显感觉更硬）
- [ ] 玩家 HP/护甲跨楼层保留（不自动回复）
- [ ] 玩家死亡 → 回到第 1 层重开，HP/护甲回满，地图换新
- [ ] 连续推进 3 层：无对象泄漏、无 Missing 引用、帧率无可见下降
- [ ] Console 无 Error / 无 Warning

---

## 六、开发顺序与依赖关系

```
v0.5.0 地图生成 ──► v0.5.1 房间流程 ──► v0.5.2 内容生成 ──► v0.5.3 房间类型 ──► v0.5.4 楼层循环
   （布局数据模型）    （状态机+休眠）      （填内容）           （类型装配）          （循环闭环）
```

**严格串行，不可跳步**，关键依赖：

- v0.5.1 依赖 v0.5.0 的 Room 对象与门洞位置数据（Door 挂在哪）；
- v0.5.2 依赖 v0.5.1 的 contentRoot（休眠）与 RegisterEnemy（清房判定）——**没有流程系统就生成内容，等于回到隔墙索敌的老问题**；
- v0.5.3 依赖 v0.5.2 的 ContentProfile 装配机制（类型只是「选哪份 Profile」），以及 v0.5.0 暴露的 BFS 距离/叶子房数据（评审意见 #4）；
- v0.5.4 依赖 v0.5.3 的 Boss 房类型（Cleared 事件触发结算）。

**跨阶段预埋清单**（前一阶段必须为后一阶段留的接口，写代码时对照检查）：

| 预埋物 | 埋于 | 用于 |
|--------|------|------|
| RoomNode.distanceFromStart / IsLeaf | v0.5.0 | v0.5.3 特殊房选址 |
| Room.clearCondition 枚举 | v0.5.1 | v0.5.3 各类型清房规则 |
| contentRoot / RegisterEnemy | v0.5.1 | v0.5.2 内容挂载 |
| RoomContentProfile 装配点 | v0.5.2 | v0.5.3 按类型分配置 |
| RoomType 字段（枚举已含全部类型） | v0.5.0 | v0.5.3 |
| OnRoomCleared 事件 | v0.5.1 | v0.5.4 Boss 结算 |
| dungeonRoot 统一挂载 | v0.5.0 | v0.5.4 一键清理 |

---

## 七、预期效果（每个子版本完成后的可玩体验）

| 子版本 | 玩起来是什么感觉 |
|--------|------------------|
| v0.5.0 | 每次进游戏看到一张全新的空地牢，能在房间里自由走动、穿门洞，但空无一物——地图骨架成立 |
| v0.5.1 | 走进有怪（手工摆的）的房间门会关上，杀光才放行——**第一次有「房间制战斗」的仪式感** |
| v0.5.2 | 每个房间的敌人/障碍/摆设都不一样，重开一局全变——**第一次有「随机地牢」的重复可玩性** |
| v0.5.3 | 地图上有金色的宝箱房、蓝色的商店房、紫色的祭坛房、暗红的精英房、深红的 Boss 房——**第一次有「探索与抉择」的 RogueLike 体验** |
| v0.5.4 | 杀 Boss → 开宝箱 → 踩传送门 → 下一层更难 → 死了从头再来——**完整的一局游戏闭环成立** |

---

## 八、风险与注意事项

| 风险 | 影响 | 应对 |
|------|------|------|
| 运行时绘制 Tilemap 后碰撞不更新 | 玩家穿墙 | Walls 沿用 v0.1 已验证的 CompositeCollider2D（同步生成）；v0.5.0 验收含碰撞测试 |
| 敌人隔墙索敌（现有 AI 无视线检测） | 房间制失效、性能浪费 | v0.5.1 房间休眠制（contentRoot.SetActive）；验收单列回归项 |
| 关门时玩家正站在门洞上 | 卡住/被推挤 | RoomTrigger 四边内缩 0.5 格，保证玩家完全进房后才关门；验收单列 |
| 敌人/障碍物刷在门洞旁堵门 | 流程卡死 | 生成位置规则：距门 ≥2.5 格 + OverlapCircle 校验 + 重试上限；验收单列 |
| 随机流污染（UnityEngine.Random 全局共享） | 布局不可复现、联调互相干扰 | 全链路独立 System.Random 实例 + 房间子 seed 派生；同 seed 验收 |
| 楼层切换对象泄漏 | 内存增长、引用残留 | 一切生成物挂 dungeonRoot，Cleanup 销毁根 + ClearAllTiles；连续 3 层验收 |
| 生成极端情况（候选格耗尽/房间数不足） | 偶发坏图 | 算法 4.3 第 5 步整体重 roll；Validate 1000 Seeds 常态化跑 |
| Boss 房紧邻出生房（小图时） | 体验差 | bossMinDistance 尽力满足 + BFS 最远优先；自检统计 Boss 平均距离 |
| 武器攻击可以穿墙（WeaponHitbox 按 targetLayer 检测，不看墙） | 隔着薄墙打到隔壁房 | v0.5.x 接受（敌人休眠，打了也没反应）；v0.6+ 视手感决定是否给攻击加墙体遮挡 |
| v0.5.3 范围膨胀（商店经济、Boss 技能） | 版本失控 | 范围声明：全部占位实现；经济/技能属独立系统线，列入未来扩展 |
| now_use 目录腐化 | 脚本管理约定失效 | v0.5 完成后按 README 约定迁移在用脚本并更新清单 |

---

## 九、性能规划（决策记录）

> 本章不实现任何机制，只记录性能相关的决策与**触发条件**——将来做这些优化时必须因为测到了问题，而不是因为「感觉会慢」。

### 9.1 当前性能方案（v0.5.x 已内置，无需新增）

| 机制 | 覆盖的开销 |
|------|-----------|
| 房间休眠制（contentRoot.SetActive(false)） | 未进房敌人的 AI / 动画 / 血条 / Trigger 全部停止，CPU 归零 |
| 已清房只剩静态装饰 | 渲染由相机视锥剔除处理 |
| WeaponHitbox 的 OverlapNonAlloc 静态缓冲（v0.4.6 已有） | 攻击检测无 GC 分配 |

规模估算：每层 8~12 房 ×（2~8 敌人 + ~10 道具）≈ 100~300 对象，其中活跃对象仅限当前房。此规模下性能不是问题。

### 9.2 房间激活剔除（只保留当前房 + 相邻房 Active）——不实施

- **不做的理由**：相对休眠制的增量收益只是关掉已清房（纯静态装饰），边际趋零；反复 SetActive 大批对象有卡顿尖峰风险；宝箱/传送门等交互物的激活状态会引入新 bug 面（走回已清房时交互物必须是活的）。
- **触发条件**（满足任一才做）：单层房间数上 50；或 Profiler 实测掉帧且定位到房间对象。
- **实施时的落点**：OnRoomEntered 已天然追踪玩家当前房，改动局部于 DungeonManager / Room，不波及其他模块。

### 9.3 对象池——不实施

- **不做的理由**：v0.5 的实例化每层楼发生一次、总量几十个，不是高频路径，池化零收益。
- **触发条件**：出现高频生成销毁路径时（弹幕、伤害飘字、命中特效）。
- **已预留的挂点**：所有 Instantiate 收口在 Enemy / Obstacle / Decoration 三个 Spawner，届时只改一处，其余代码零改动——这才是有效的「提前预留」。

---

## 十、未来扩展方向（v0.6+，本版本不实施，仅列钩子位置）

| 方向 | 说明 | 框架已预留的钩子 |
|------|------|------------------|
| 小地图 | 用 DungeonLayout 数据直接驱动 UI 绘制（v0.5.4 可选拉伸任务可先做简版） | Layout 纯数据、房间状态事件（OnRoomEntered/Cleared 驱动迷雾揭开） |
| 走廊连接 | 房间不再必须邻接，Builder 在连接边上画 L 形走廊 | RoomConnection 是图边，模型不变只改 Builder |
| 房间尺寸多样化 / 手工房间模板 | 预设布局的房间块随机抽取（EtG 式） | Builder 的「按模板填充」替代「按矩形绘制」，Room 数据结构不变 |
| 秘密房 | 炸弹炸开隐藏墙进入 | RoomConnection 加门类型字段（预留注释） |
| Boss 专属技能 | 多阶段、弹幕、召唤 | 现有 Windup/Active/Recovery + AttackIndicator 预警框架天然支持；AttackQuery 留作 AoE 工具 |
| 商店经济 | 金币掉落 → 购买结算 | Interactable 基类 + ShopRoom 类型已就位 |
| 技能/装备系统 | 宝箱奖励从占位变真货 | ChestInteractable 的发放点即钩子 |
| 多层地牢生态 | 不同楼层换 tileset/敌人池 | DungeonConfig 换成按楼层索引的 Config 列表 |
| 每日挑战 | 全服同 seed 竞速 | 种子系统已就位 |
| 存档/局外成长 | RunManager 扩展持久化 | RunManager 是天然挂点；seed 驱动使存档廉价：只需存 主seed + floor + 已清房间 id 列表 + 玩家状态即可还原整局 |

---

## 十一、修改文件清单（预估）

**新建（全部位于 `Assets/Scripts/v0_5/` 子目录、`Assets/Data/Dungeon/`、`Assets/Prefabs/Dungeon/`）**：
- Core：`DungeonManager.cs`、`DungeonBuilder.cs`、`DungeonConfig.cs`、`RunManager.cs`（v0.5.4）
- Generation：`DungeonGenerator.cs`、`DungeonLayout.cs`（含 RoomNode/RoomConnection/RoomType）、`RoomTypeAssigner.cs`（v0.5.3）
- Runtime：`Room.cs`、`Door.cs`、`RoomTrigger.cs`、`RoomState.cs`
- Content：`SpawnTable.cs`、`RoomContentProfile.cs`、`EnemySpawner.cs`、`ObstacleSpawner.cs`、`DecorationSpawner.cs`（v0.5.2）；可破坏障碍物脚本 `ObstacleHealth.cs`、`DestructibleObstacle.cs` 放 `now_use/`（v0.5.2）
- Interaction：`Interactable.cs`、`ChestInteractable.cs`、`ShrineInteractable.cs`、`PortalInteractable.cs`（v0.5.3~v0.5.4）
- 资产：`DefaultDungeonConfig.asset`、7 份 `RoomTypeConfig`、若干 `SpawnTable`/`RoomContentProfile`、Door/Obstacle（含可破坏变体）/Decoration/Chest/Portal/Elite/Boss prefab

**修改（仅 1.3 表中的小口径改动）**：
- `Assets/Scripts/now_use/CameraFollow.cs`（+SnapToTarget，v0.5.0）
- `Assets/Scripts/now_use/EnemyStats.cs`（+ApplyFloorScale，v0.5.4）
- `Assets/Scenes/v0_5_Dungeon.unity`（新建，复制自 v0_4 场景）
- 玩家 `AttackData` 资产 targetLayer 增加 Obstacle 层 + 新建 Obstacle 层定义（v0.5.2 可破坏障碍物）

**不改**：Player/Enemy/Combat/Weapon/UI 全部现有脚本（除上两行）、物理碰撞矩阵、Input Actions。

---

## 十二、执行记录

> 各子版本完成时在此回填，格式参照 v0.4.6_正式计划第十节。

- 2026-07-20：计划书发布；两轮外部评审处置结论已并入（见第〇章附、第九章）。
### v0.5.0 随机地图生成基础 —— ✅ 代码完成（2026-07-20），待人工 Play 验收

| 任务 | 状态 | 说明 |
|------|------|------|
| 任务0 场景与目录 | ✅ | `v0_5_Dungeon.unity`（复制自 v0_4，删除小屋 Grid/TrainingDummy/Enemy）；新建空 Grid/Floor/Walls（碰撞结构照抄 v0.1：Walls 含 TilemapCollider2D(usedByComposite)+CompositeCollider2D+Rigidbody2D(Static)，sortingOrder Floor=0/Walls=1）；DungeonSystem + DungeonRoot；`Scripts/v0_5/`（5 个子目录）、`Data/Dungeon/`、`Prefabs/Dungeon/` 已建 |
| 任务1 数据模型 | ✅ | `Generation/DungeonLayout.cs`（RoomType/RoomNode/RoomConnection/DungeonLayout 同文件，纯 C#） |
| 任务2 DungeonGenerator | ✅ | `Generation/DungeonGenerator.cs`：网格邻接生长 + 全邻居连接（自然环路）+ BFS + Boss 选址（最远、并列叶子优先） |
| 任务3 DungeonConfig | ✅ | `Core/DungeonConfig.cs` + `Data/Dungeon/DefaultDungeonConfig.asset`（默认值即计划书 4.2 数值） |
| 任务4 DungeonBuilder + Room | ✅ | `Core/DungeonBuilder.cs`（清图→画房间→开门洞→建 Room 空壳）；`Runtime/Room.cs` 基础版（Id/Type/Bounds，无状态机） |
| 任务5 DungeonManager | ✅ | `Core/DungeonManager.cs`（Generate 门面 + Gizmos + ContextMenu/MenuItem 双入口 Validate 1000 Seeds） |
| 任务6 相机与出生 | ✅ | `CameraFollow` +`SnapToTarget()`；玩家传送至起始房中心（实测落点 (10, 6.5) = 房间 (0,0) 中心） |
| 编译 | ✅ | 0 Error / 0 Warning（修复 1 条：FindFirstObjectByType→FindAnyObjectByType） |
| Validate 1000 Seeds | ✅ | 失败 0/1000；平均房间数 10.0；Boss 距离 min=2 / avg=3.9 / max=7；耗时 30ms |
| Play 实测 | ✅ | seed=25172078 生成 10 房 11 连接；Room_0_Start~Room_6_Boss 挂载 DungeonRoot 下；截图 `Assets/Screenshots/v050_play.png`（出生房）、`v050_fullmap.png`（全图，门洞/墙体/连通性目视正确） |

**实施期偏差记录**：无设计偏差。Boss 距离 min=2 < bossMinDistance=3 符合「尽力满足」定义（该布局最远房即 2，不存在更远选择）。

**待人工 Play Mode 验收**（计划书五-A 清单）：走动穿门洞、墙体碰撞、同 seed 重进布局一致（Inspector 填固定 seed 复验）。

- [x] v0.5.0 随机地图生成基础（代码完成，待人工验收）
- [x] v0.5.0 随机地图生成基础（✅ 已完成；人工验收随 v0.5.1 一并覆盖）

### v0.5.1 房间流程系统 —— ✅ 已完成（2026-07-21，人工验收通过）

| 任务 | 状态 | 说明 |
|------|------|------|
| 任务0 Door + Door Prefab | ✅ | `Runtime/Door.cs`（RefreshState：仅当两侧房间都非 Active 才开）；`Prefabs/Dungeon/Door.prefab`（白块染色 + BoxCollider2D） |
| 任务1 RoomTrigger | ✅ | `Runtime/RoomTrigger.cs`，Builder 为每个 Room 挂 Trigger（BoxCollider2D isTrigger，四边内缩 0.5 格） |
| 任务2 Room 完整状态机 | ✅ | `Runtime/Room.cs` 重写：Unvisited/Active/Cleared + clearCondition（None/AllEnemiesDead）+ RegisterEnemy/NotifyEnemyDied + OnRoomEntered/OnRoomCleared |
| 任务3 休眠接入 | ✅ | **实现变更**：由计划的 contentRoot.SetActive 改为**逐敌人禁用 EnemyAI/EnemyCombat + 刚体冻结**——敌人未进房时可见但不动（用户验收反馈驱动，见偏差记录①） |
| 任务4 调试摆敌 | ✅ | DungeonManager +debugPopulateEnemies 等 4 字段（最近 2 战斗房 × 2 敌人，v0.5.2 移除） |
| 任务5 验收 | ✅ | 自动化：进房关门（碰撞+色块同步）、休眠态（active+AI禁）、空房自动清房、0 Error/0 Warning；人工：用户全流程验收通过 |

**实施期偏差/问题记录**：
1. **休眠实现变更（验收反馈）**：原设计 contentRoot.SetActive(false) 导致进房前看不到房内敌人。改为逐敌人禁用 AI/Combat + 刚体 FreezeAll——敌人可见、可被武器打到，但不动不攻击；进房唤醒。计划书 9.1 休眠制表述以此为准。
2. **预警扇形消失（prefab 缺陷，顺手修复）**：Enemy.prefab 的 AttackIndicator 上挂有 SpriteRenderer，与脚本要 AddComponent 的 MeshFilter 互斥 → 回退分支逻辑不自洽（meshRenderer 非空走 Mesh 路径但无网格）→ 扇形完全不渲染 + 每次激活刷两条警告。修复：prefab 删除 SpriteRenderer（组件块/列表条目/脚本序列化引用三处），Mesh 主路径生效，警告归零。
3. **Door 碰撞尺寸漂移**：prefab 上 BoxCollider2D.size 为 0.0001 → Door.Init 强制 size=Vector2.one（随 scale 铺满门洞）。
4. **REST 验收伪影（非游戏 bug）**：transform 瞬移不触发 OnTriggerEnter2D（刚体睡眠 + PlayerController 覆写速度）；真实走路触发完全正常。验收手法：禁用 PlayerController + linearVelocity 注入唤醒。
5. **编辑器崩溃**：v0.5.1 编译期 Unity 崩溃重启一次，REST 服务器被模态对话框阻塞，用户手动处理后恢复；无数据损失。
6. **VSCode 缓冲区覆盖**：用户未关闭已改脚本导致保存时回滚 DungeonBuilder.cs 一处修改，已重新落盘并约定先 Revert File 再保存。

**验收后清理**：Room/RoomTrigger 临时诊断日志已删除，编译 0/0。

- [x] v0.5.1 房间流程系统（✅ 已完成）

### v0.5.2 随机内容生成 —— ✅ 已完成（2026-07-22，人工验收通过）

> 含原 v0.5.2.1「可破坏障碍物双方可摧毁」——用户决策并入 v0.5.2，v0.5.2.1 版本号取消；三类箱子框架设计储备并入 `v0.5.2_正式计划.md` 第十章。

| 任务 | 状态 | 说明 |
|------|------|------|
| 任务0 层与 AttackData | ✅ | layer 7 = `Obstacle`；`PlayerAttackData.targetLayer` 64→192；`EnemyMeleeAttackData.targetLayer` 1→129（**双方武器均可摧毁**，用户设计扩展，编辑器内三重确认生效） |
| 任务1 占位 prefab ×4 | ✅ | `Obstacle`（灰石块）/ `Obstacle_Destructible`（浅棕木箱 + ObstacleHealth + DestructibleObstacle）/ `Decoration_Stain_A/B`（地面污渍，无碰撞，sortingOrder=1） |
| 任务2 SpawnTable/Profile + SO ×4 | ✅ | `Content/SpawnTable.cs`、`RoomContentProfile.cs`；`EnemyTable/ObstacleTable/DecorationTable/RoomContentProfile_Combat`（敌人 2~4、障碍 1~3、装饰 1~3） |
| 任务3 Spawner ×3 + 可破坏脚本 | ✅ | 静态 `EnemySpawner/ObstacleSpawner/DecorationSpawner` + 共用 `SpawnPositionHelper`（距墙≥1、距门≥2.5、OverlapCircle r=0.4、重试 20 上限）；`now_use/ObstacleHealth.cs`（IDamageable，HP=刀数）+ `DestructibleObstacle.cs`（闪白+变深+销毁） |
| 任务4 Builder/Manager 接入 | ✅ | Room +`Doors` 只读访问器（2 行）；`Build(layout, config, layoutSeed)` + `SpawnContent`（子 seed = seed*7919+roomId）；Manager 移除 v0.5.1 调试摆敌 4 字段 |
| 编译 | ✅ | 0 Error / 0 Warning |
| 自动化验收 | ✅ | seed=12345（8 房）：各房敌人 2~4 达标；同 seed 两次 Play 50 内容物坐标零差异；距门最近 2.98；休眠/进房关门/唤醒全流程无回归；敌人挥击穿箱 3 刀打空销毁实测通过；Validate 1000 Seeds 0/1000；截图 `Assets/Screenshots/v052_room_content.png` |
| 人工验收 | ✅ | 用户全流程验收通过（含双方拆箱、装饰抖动、玩家血条放大后观感） |

**实施期偏差/问题记录**：
1. **双方可摧毁（设计扩展，并入 v0.5.2）**：原计划「仅玩家武器可破坏」被用户推翻——敌人 `targetLayer` 加 Obstacle；三类箱子（特殊效果/物资/普通）框架设计储备见 `v0.5.2_正式计划.md` 第十章，v0.5 后续子版本落地。
2. **WeaponHitbox 自身判定误伤（根因修复）**：敌人与障碍物同挂 `DungeonSystem` 根下，`hit.transform.root == transform.root` 把木箱误判为「攻击者自身」→ 敌人武器打不到木箱。改为 `hit.transform.IsChildOf(transform)`（语义不变、对层级免疫）。实测敌人挥击穿箱 3 HP 打空销毁。
3. **AttackIndicator 归位报错（v0.4.5.2 潜伏 bug）**：敌人挥击前摇中死亡 → 销毁时 `EnemyCombat.OnDisable → Hide → SetParent` 被 Unity 拒绝。`RestoreParent` 加守卫：父物体停用/销毁中放弃归位。
4. **障碍物碰撞体尺寸漂移**（v0.5.1 Door 同类）：REST 添加的 BoxCollider2D 默认 size=0.0001 → 不挡人且重叠检测穿透；手改 YAML 未触发重导入导致首轮 Play 仍为旧值，最终经 `prefab_set_property` 由 Unity 自身写入修复。经验：REST 加碰撞体必须显式设 size；资产修改优先走 REST 而非手改 YAML。
5. **装饰丰富度（验收反馈驱动）**：污渍仅 2 种固定轮廓 → DecorationSpawner 增加随机非等比缩放抖动（×0.7~1.4）+ 颜色深浅抖动（×0.85~1.15），同 seed 复现性不变。
6. **玩家世界空间血条偏小（验收反馈）**：`Player.prefab` 的 `PlayerWorldStatusBar.canvasScale` 0.01→0.014（1.4×）。
7. **装饰可互相靠近**（接受）：装饰无碰撞体（设计），位置规则 3 不约束装饰，视觉重叠可接受。
8. **个别生成点放弃**（设计行为）：碰撞体生效后某房 1 个内容 20 次重试失败按设计放弃，内容总数 50→49。

详见 `v0.5.2操作.md`。

- [x] v0.5.2 随机内容生成（✅ 已完成，含双方可摧毁与三类箱子设计储备）
- [ ] v0.5.3 房间类型系统（待执行）
- [ ] v0.5.4 完整楼层循环（待执行）
