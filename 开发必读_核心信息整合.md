# 开发必读 核心信息整合

> 状态：V2 MVP 迁移中
> 更新：2026-09-09  
> 当前设计真源：计划书/无名之地_完整游戏开发文档_V2.md
> 历史完整版：往期版本文档/旧设计与报告/开发必读_核心信息整合_v1.1旧版.md

## 1 当前产品边界

- 游戏为 2D 俯视角房间制 Roguelite。
- 房间内上下左右自由移动；宏观 DAG 只从左向右推进。
- DAG 上下分支是抽象路线，不生成场景上门和下门。
- 战士已经进入退役决策；角色与职业合并为职业角色。首发职业角色以新版总文档确认结果为准。
- 选择职业角色同时确定外观、属性、武器池、技能、职业资源、动画和专属 Build。
- 武器画入角色 Sprite；身体碰撞与攻击 Hitbox 分离。
- 新序列帧统一使用 Unity Animation 和 Animator；FrameAnimator 与 WeaponPivot 只作为旧实现迁移参考。
- v2.0.1 首轮只实现左右攻击：鼠标相对角色的 X 正负定向，Y 仅负责走位；是否最终保留仍由灰盒数据决定。
- 狼人怒痕 0～100，战斗内时间慢充并按对敌实际伤害加速；满怒后 Q 消耗整条进入兽化。旧 T 停用，R/C 不进入首轮 MVP。
- 狼人美术列为保留资产。其余现有美术暂作占位，后续按内容批次替换。

## 2 当前实现状态

Assets/Scripts/now_use 正处于 V2 渐进迁移：v2.0.1 的左右攻击、怒痕/Q 兽化与 HUD 已接入；独立 Character/Class、WeaponPivot、FrameAnimator、永久数值技能树与旧经济仍是待替换兼容链。

迁移期间必须遵守：

1. 保持一个可运行入口，逐条替换，不一次删除全部旧链路。
2. 已被用户修改或仍被场景引用的旧代码，先标记迁移状态，等替代链通过验证后再退役。
3. 明确无引用且与新玩法无关的代码可以移出 Assets 归档；删除前必须核对 GUID、代码引用和动态创建入口。
4. 旧代码只修阻断迁移的问题，不再增加与 V2 冲突的新功能。

## 3 新架构红线

1. **单一职业角色入口**：选择、存档、动画和奖励过滤只消费 PlayableCharacterId。禁止重新并存 CharacterId 与 ClassId 两套真值。
2. **数据先于对象**：DungeonGraph、DungeonNode、RunState、Build 和奖励种子是纯 C# 数据。地图逻辑不依赖房间 GameObject 是否激活。
3. **攻击同源**：预警、动画窗口、Hitbox、伤害和反馈消费同一 AttackDefinition。视觉像素不是判定真源。
4. **碰撞分离**：Body Hurtbox、Movement Collider、Attack Hitbox 分责。武器烘入 Sprite 不得扩大身体碰撞。
5. **分层**：输入意图 → 决策和状态 → 执行 → 判定 → 数据 → 表现和 UI。Animator 与 UI 不计算伤害、货币或路线逻辑。
6. **一个启动入口**：Bootstrap 和 GameFlow 负责持久服务与流程，初始化必须可诊断并防止重复实例。
7. **稳定小模块**：Bootstrap、DungeonGraph、RoomRuntime、Combat、Build 和 Economy、Presentation 和 UI。禁止继续增加万能 Manager。

## 4 继续有效的工程红线

- Trigger 只参与逻辑事件。LOS、投射物和移动探测跳过 isTrigger，实体墙才阻挡。
- 高频物理查询使用 NonAlloc 和复用缓冲；每帧缓存 GetComponent，避免 LINQ 和字符串分配。
- 玩家死亡走 Respawn；新增运行状态必须在重生和新 Run 时显式重置。
- 事件订阅与退订必须配对；协程和 Tween 必须互斥并绑定生命周期。
- Prefab 实例上临时加组件会被场景重载丢失。正式组件写入 Prefab 本体并验证引用。
- 外部修改 Unity 场景后必须 Reload，再检查编译、唯一 Player 和 Hierarchy，最后进入 Play。
- 图片导入显式设置 Sprite Mode、PPU、Pivot、过滤和压缩。序列帧尺寸必须统一。
- Editor-only 资源扫描在打包前必须转换为运行时资源索引。
- 场景或 Prefab 的 YAML 手术必须备份、感知行尾、验证 fileID 双向引用并重载。
- 受击碰撞体必须与攻击平面匹配，不能把 Sprite 可见范围误当成 Hurtbox。

## 5 房间与生成门禁

- 一个 DungeonNode 对应一个运行房间；默认左入右出。
- 房间先生成最终 RoomPlan，再一次性绘制。
- 每次生成尝试使用独立临时集合，失败不得污染下一次尝试。
- 入口到出口必须按玩家碰撞体口径可达。
- SpawnCells 满足玩家净空、距门、距墙和用途约束后，再做 NonAlloc 物理检查。
- Room 激活关门前必须确认玩家 Collider2D.bounds 已完整进入 RoomBounds，不能只依赖 OnTriggerEnter2D。
- 房间阶段触发可重复初始化；离开节点后不得残留敌人、订阅、Tween、协程或奖励状态。
- v1.1.52 的固定南入口 Boss 房只作为历史实现。新版房间方向合同确认后才能决定是否迁移其模板数据。

## 6 Animator 约束

- 每个职业角色一个基础 Animator Controller，同武器模组用 Animator Override Controller 替换 Clip。
- Animator 参数保持少量稳定；Animation Clip 不直接写游戏数值。
- Animation Event 或 StateMachineBehaviour 只开关判定、位移或反馈窗口。
- 标准攻击 Clip 必须验证攻击开始、Hitbox 开、Hitbox 关和攻击结束事件。
- v2.0.1 只制作左右攻击灰盒 Clip；没有用户确认不得并行量产四方向或最终攻击 Sprite。
- FrameAnimator 只有在新 Animator 链接管、引用清零并通过回归后才能删除。

## 7 清理与迁移门禁

- 先区分保留、暂用、迁移、退役、删除五种状态。
- 狼人 Sprite、序列帧和能量条素材默认保留。
- 其余美术默认暂用，不以“将来替换”为理由现在删除。
- 永久百分比技能树与“内容解锁型局外成长”冲突，列为优先退役候选。
- CharacterSelect 和 ClassSelect 双入口、旧 WeaponPivot 表现链、旧四向 DungeonGenerator 与旧 Minimap 列为迁移候选，不得在替代链完成前硬删。
- 删除脚本时同时处理 .meta、场景或 Prefab 引用、运行时 AddComponent 入口、测试和文档。
- 清理后必须通过真编译和 Missing Script 检查。

## 8 测试与提交门禁

- 脚本编译无错，Console 无新增红错。
- EditMode：DAG 无环与可达、Seed、伤害公式、节点状态、奖励职业过滤、Missing Script。
- PlayMode 或人工：职业角色选择、节点进入完成、房间卸载、死亡重开、镜头 Bounds。
- 外部改场景后 Reload 验证。
- 不遗留 Debug_ 对象、临时 Player、错误 Camera target 或未说明的脏场景。
- 每次功能落盘一份 v2.0.x 实施记录；玩法决策回写 V2 主开发文档，不另起竞争版本。

## 9 文档优先级

1. 最新完整游戏开发文档：玩法、世界观、内容与版本范围。
2. 本文：工程红线与迁移约束。
3. AGENTS.md 和 project-dev-rules：代理执行摘要与完整规则。
4. 项目框架文档：模块职责。
5. 项目进度报告：实际完成状态。
6. 往期版本文档：只读历史。
