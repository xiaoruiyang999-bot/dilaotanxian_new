# 开发必读 · 核心信息整合（写代码/改资产前先过一遍）

> 状态：**强制生效** · 2026-08-20 建立
> 用途：浓缩全部计划/修复文档中的架构规则与实战教训，新会话写代码前必读，减少重复踩坑
> 详细版索引见文末；本文件是唯一需要日常维护的"活文档"

---

## 一、项目概览与架构铁律

**2D 俯视角地牢 Roguelite**（Unity 6 + URP 2D，Input System + DOTween）。当前 v0.7.5：正式流程 `v0_7_PrepRoom`（选职业+拿武器+投放消耗包）→ `v0_7_ClassWeapon`（带职业加成的地牢：打怪→清房→Boss 宝箱→传送门无限下楼→死亡回准备房）。技能三槽 F/Q/R + 道具背包 C + 武器掉落换装。

1. **分层**：输入层 → 决策层(AI/Room 状态机) → 执行层(Combat 状态机) → 判定层(WeaponHitbox) → 数据层(AttackData SO) → 表现层/UI。**禁跨层**（AttackIndicator 注释里写着"禁止：攻击逻辑、AI逻辑…"）。
2. **事件驱动解耦**：System.Action 事件（OnHealthChanged/OnRoomCleared/OnGenerated…），订阅/退订必须配对（OnEnable/OnDisable 或 OnDestroy）。
3. **SO 数据同源原则**（v0.4.5.2 Final5 确立，v0.5 重构曾破坏、v0.6.2 修复）：**预警 = 判定 = 武器视觉，三者消费同一份 AttackData**。改判定几何必须同步预警（WeaponHitbox.CurrentBoxGeometry 是单一数据源）。
4. **地牢生成纯 C# 数据层**（DungeonLayout/RoomNode），seed 驱动可离线 Validate1000Seeds；场景 Room 组件由 DungeonBuilder 实例化，`builder.Rooms`（id→Room）是运行时真源。
5. 脚本目录：`Assets/Scripts/now_use/`，v1.0.4 起编译为 **Game** 程序集（DOTween 模块单独为 DOTween.Modules 程序集）。**asmdef 按名字引用 Assembly-CSharp 静默无效**，测试/工具程序集引游戏代码必须走 Game。v0_1~v0_4 归档已删除（历史在 git——**别捞旧代码**）。

## 二、写代码前 Checklist（10 条）

- [ ] 我改的东西属于哪一层？有没有跨层引用？
- [ ] 新事件订阅是否有配对退订？协程是否互斥（StopCoroutine 旧的再启新的）？
- [ ] 是否每帧分配（GC）？物理查询用 **NonAlloc + 静态缓冲**；GetComponent 类调用要缓存
- [ ] 涉及物理检测：**Trigger 只参与逻辑事件**（RoomTrigger/探测圈/交互物），**不挡 LOS 射线/子弹/移动探测**；实体碰撞（墙 TilemapCollider）才阻挡。新投射物/射线必须 `isTrigger` 跳过
- [ ] 涉敌人生死/休眠：`EnemyCombat.OnDisable` 里用 `EnemyHealth.IsDead` 区分（死亡→销毁指示器；休眠→只 Hide 复用）；敌人根被 Destroy 时，已脱离父物体的对象（AttackIndicator 等）要自毁，防孤儿泄漏
- [ ] 涉玩家：玩家对象**从不被销毁/停用**（死亡走 ResetHealth+Respawn）——依赖此假设的清理逻辑安全
- [ ] 收集子物体组件时（GetComponentsInChildren 递归）：**先想清楚要不要排除特定子树**（WerewolfTransformation 曾因把狼人 SR 算进"玩家视觉"导致按 T 角色消失）
- [ ] 序列帧/图片资产：720px 图默认 7.2 世界单位（PPU=100），**必须设缩放或 PPU**；导入必须显式 spriteMode=Single（见红线 R3）
- [ ] 编辑器便利代码（AssetDatabase/UNITY_EDITOR 条件块）：打包前必须资源化（Resources/Addressables）——搜 `#if UNITY_EDITOR` 找到所有待办
- [ ] 改完的编译验证：UnitySkills `script_get_compile_feedback`（服务不可用则本地括号自查 + 用户报 Console）

## 三、踩坑红线（全部实战教训，按代价排序）

### Unity 资产 / 场景 / Prefab

**R1 · 往 prefab 实例上加组件会被丢弃**：场景里的 Player 是 PrefabInstance，`component_add` 到它上面的组件没有写入 override 结构，`scene_load` 重载时被 Unity 当垃圾清除（症状：组件"挂上了"但运行时不存在、无 Awake 日志）。
→ **正解**：在场景实例上建好 override 后用 `prefab_apply_overrides` 写入 prefab **本体**；或直接改 prefab 资产。

**R2 · YAML 直接手术场景/prefab 的规则**（已安全执行多次，规则必须全守）：
① 先备份（`_backup/` + 脚本留档）② **行尾感知**：场景是 LF，Player.prefab 是 **CRLF**——正则统一 `\\r?\\n` 处理再写回 ③ **fileID 先查重**再选新 id（曾用 550…003 撞车）④ 加组件要**三处双向**：组件块 + GO 的 m_Component 列表 + （Transform 还要父的 m_Children）⑤ 删场景根对象要同步 **SceneRoots** 列表 ⑥ 写盘后 `scene_load` 重载验证 + Console 查错 ⑦ UnitySkills 属性设置（component_set_property）用**公开属性名**（`sprite` 不是 `m_Sprite`）+ 资产引用走 `assetPath=` 参数

**R3 · `textureType=Sprite` 默认 Multiple 自动切片**：UnitySkills 设 Sprite 类型时默认把透明图自动切成碎片（001_0…），引用看似成功实为透明小碎片 → "一点痕迹都没有"。→ **必须显式 `spriteMode=Single`**，且 meta 的 internalIDToNameTable 应为 `213: 21300000 → 文件名`（Single 主图注册）。修复套路：删 meta 重生成 + 显式 Single + reimport。

**R4 · 外部改场景后必须 Reload**（调试规范 2.5）：Unity 开着时磁盘被改，不 Reload 就 Play = 测的旧内存副本（双玩家抢键盘假象）。顺序：Reload → 查 Console 编译错误 → 确认唯一激活 Player → Play。

**R5 · Play 模式中的编辑操作是运行时的**：退出即还原且部分技能被事务拦截；Domain Reload（编译/进退 Play）会断开 REST 服务——**等待+重试**，服务长时间不恢复去 Unity 面板重开服务开关。

### 物理 / 战斗语义

**R6 · 层的现状**：墙/玩家/交互物**全在 Default 层**，Obstacle 层给可破坏障碍。`AttackData.obstacleLayer` 指 Obstacle；墙挡子弹靠"Default 非 Trigger 且无 IDamageable → 销毁"（Projectile.ResolveHit：**Trigger 命中物穿透**，实体碰撞才挡弹——防宝箱/传送门/弹对弹吞弹）。敌人本体非 Trigger、探测圈 Trigger（半径 5）。

**R7 · 判定几何单一数据源**：WeaponHitbox.Tick 用 `AttackRange × weaponPivot.lossyScale.x`（长）+ `weaponWidth × scale`（宽）的 **OverlapBox**，从攻击者中心沿攻击方向延伸；预警（AttackIndicator Box 形状）必须消费 `WeaponHitbox.CurrentBoxGeometry`，不得自算。

**R8 · 三态攻击流程**（Windup 预警 → Active 判定 → Recovery）：预警在 Windup 显示并**脱离父物体挂场景根**（防跟随移动），结束归位；远程预警线在 Windup 内每帧跟手（SetDirection/SetRadius）+ LOS 门控（丢失超 grace 取消并进**半额**冷却）。

### C# 编码陷阱（项目内真实发生过）

**R9 · 协程重叠**：受击闪烁类"改颜色→等→恢复"协程必须互斥 + **基线色持久缓存**（不能从当前 sr.color 读——重叠时会缓存到闪烁色 → 永久变白）。
**R10 · 每帧 GC**：RaycastAll/OverlapCircle 用 NonAlloc 静态缓冲；GetComponentInChildren 目标缓存（单槽）。同帧多命中音效用 `PlayOneShot`（天然叠放）不用多源轮询。
**R11 · 无敌帧口径**：`Health.iFrameUntil`（Time.time 口径），`GrantIFrames(dur)` 授予、TakeDamage 开头跳过；hit-stop 期间 timeScale=0，Time.time 停走（冷却也暂停，符合预期）。
**R12 · 玩家死亡流程**：Health.Die → RunManager.RestartRun（延迟2s）→ ResetHealth + PlayerStats 护甲回满 + PlayerController.Respawn（颜色/速度/ Dash 冷却重置）。**任何新玩家状态字段都要在 Respawn 里重置**。
**R13 · 集合引用比较陷阱**：`FrameAnimator.Play()` 会复制新 List——外部用 `CurrentFrames != want` 之类**引用比较判断"是否在播"永远为真**，导致每帧重播、动画永远卡第一帧。跨对象状态跟踪一律用**枚举/标记**，不用集合引用。
**R14 · Play 模式下改代码不热生效**：Play 中修改 .cs 后必须 `editor_stop → asset_refresh（等编译完成）→ editor_play`，且日志验证要看**格式特征**（如新增字段是否出现）——旧实例的 Awake 日志会残留误导判断。**升级（v0.6.8 连续三次踩坑）**：若编辑器关闭了自动刷新，`asset_refresh` 只刷资产库**不重编译脚本**——需 `debug_force_recompile` 强制编译；且 `editor_play` 返回 success 不代表真的进了 Play，用 `scene_screenshot` 返回的 `isPlaying` 字段确认；"refresh→play"链条会静默失败，安全流程=分步验证每一步。

**R15 · 房门关闭必须等玩家完整进房**：`OnTriggerEnter2D` 只表示碰撞体开始相交，不表示玩家已完整进入触发区；不能据此立刻激活战斗房。`RoomTrigger` 必须在 Enter/Stay 中用玩家世界 `Collider2D.bounds` 四边均落入 `Room.Bounds` 后才 `Room.Enter()`，否则门会在玩家仍横跨门洞时启用实体碰撞并夹住角色。固定内缩量也不能替代完整包围判断（角色碰撞尺寸/偏移会变化）。
**R15 · UnitySkills 按名字查对象会命中第一个同名者**：场景中敌人 prefab 都带 WeaponPivot/WeaponSprite 等同名子物体，`component_get_properties name="WeaponPivot"` 查到的是**敌人的**——验证玩家侧状态必须用 `path="Player/WeaponPivot"` 精确路径，否则拿到假数据白排查。
**R18 · 手写 fileID 必须校验 int64 上限（9223372036854775807）**：999 开头 19 位数溢出 → Unity 场景/prefab 解析崩溃（Failed to convert to signed 64 bit int → Safe Mode）。安全段示例：666xxx（18 位）。且修复要**全项目扫**（场景+prefab+asset 一起）——只修场景漏 prefab 会二次事故。另：纯 Input System 项目的 UI 脚本禁用 UnityEngine.Input.GetKeyDown（InvalidOperationException）——走 PlayerInput.onActionTriggered 订阅。
**R17 · 编译错误修复后必须重新 force_recompile + 场景重载**：一个脚本的编译错会让整个程序集缺失——所有新类不存在（component_add 报 Component type not found 的另一诱因）、场景中对应组件变 missing 且**重编译后不会自动恢复绑定**，需 scene_load 重载。排查顺序：Console error CS → 修复 → force_recompile → scene_load → 再验证。
**R16 · 纯代码 UI 的 RectTransform 属性走 .rectTransform**：`pivot/anchorMin/anchorMax/sizeDelta` 是 RectTransform 的属性，不能写在 Image/Text 组件上（`icon.pivot` 编译错 → 曾触发 **Safe Mode**）。强编译前本地自查要含 API 层（括号检查抓不住）；服务离线超 30 秒先查 `Logs/Editor.log` 的 `error CS`（比等服务快得多）。

### 调试规范（摘要，全文见《开发调试规范》）

- 严禁擅动 CameraFollow.target / 瞬移玩家（必须记录+复位+用 rb.position）/保存场景/留 Debug_ 对象
- 删调试克隆玩家前必须确认正式 Player 实例链路完整（tag=Player 唯一激活）
- 已知调试假象表：LOS 假 false / 假玩家不扣血 / 移动键全失灵（旧内存副本 or 双玩家 or 编译错误）——先 Reload 再排查

## 四、系统速查

**输入键位**（PlayerInputActions，Player map，v0.7.5）：WASD=Move · 左键=Attack（支持长按）· E=Interact（交互+拾取）· F=Skill 小技能 · Q=Ultimate 大招 · R=WeaponSkill · C=UseItem · **Esc=Cancel**（关职业选择 UI / 暂停菜单，PausePanel 消费，v1.0.4）。Dash/Sprint 动作保留在资产但已无消费方。

**关键事件**：`DungeonManager.OnGenerated`（生成完成含传送/相机 Snap；楼层切换/重开同链路）· `Room.OnRoomEntered/OnRoomCleared` · `Health.OnHealthChanged/OnDeath` · `EnemyHealth.OnDeath/OnTakeDamage` · `WeaponHitbox.OnHit`

**反馈挂点**（v1.0.5 恢复）：命中 "hit" 在 WeaponHitbox（+HitStop 0.03s+轻震）；受击 "hurt" 在 Health（实际扣血时）；敌死 "enemyDie" 在 EnemyHealth.Die；开门 "door"；开箱 "chest" 在 ChestInteractable。三场景 AudioManager 已接 Kenney 占位表 + DungeonBGM（正式音效待换）。HitStop 与暂停 UI 经 `SuppressByUI` 协调（PausePanel/DeathPanel 均已接入，timeScale 无冲突）。死亡结算：`DeathPanel`（楼层/击杀/时长，数据来自 `RunTracker`+RunManager.FloorNumber）+ restartDelay 6s 兜底。

**狼人=可选角色外形**（v1.0.6 重接入，与职业独立）：ClassSelectUI"角色外形"行 → `RunStateCarrier.ChosenCharacter`（死亡保留）→ `FrameAnimator.SetWerewolfVisual`（帧在 `Resources/Art/Characters/Werewolf/{Walk_L,Walk_R,Idle_L,Idle_R}`，001.png 命名，左右两套不 flipX；攻击/技能帧未产出回退武器挥砍）。旧变身系统已退役（历史在 git 与 [v0.6.8_狼人角色系统开发文档](v0.6.8_狼人角色系统开发文档.md)）；Beast/Transform 素材未接入；素材源工具仍在 `E:\WeGameApps\The animation of Unity\狼人\`。**统一入口（v1.0.6）**：地牢场景 RunManager.`redirectToPrepWhenNoClass=true`——无职业直连地牢自动回 v0_7_PrepRoom；编辑器 Play 播放的是当前打开场景，别在旧场景里按 Play 当正式流程。

**测试门禁与资源**（v1.0.4）：EditMode 测试 `Assets/Tests/EditMode/`（Missing Script 扫描三场景+全 Prefab、ApplyArmor 边界值、地牢 seed 确定性+连通性），提交前必跑；运行时按路径加载的资产放 `Assets/Resources/`（**路径=编辑器路径镜像**，编辑器与构建同源：Class/Skill/Item SO、Warrior 序列帧、SlotFrame 均已迁入）。误删脚本在原路径重建同名类可复活旧 GUID 自愈场景断链。

**小地图**（v0.6.0）：场景 `MinimapSystem` 挂 MinimapController；消费 OnGenerated 重建；配色=DungeonManager Gizmos 同款。

**遗留债**（详见 v0.5.4.4.4 文档第 4 节 + 长期计划 §4）：巡逻无寻路、Charger 空层配置、DungeonGenerator 保底 NRE、负伤害、Health/EnemyHealth 合并（M4 前）、PatrolSystem 随机源（M5 前）。

## 五、版本与文档索引

| 文档 | 内容 |
|---|---|
| 项目长期计划_Roguelite路线图 | M1-M5 里程碑任务总账（M1 已完成）+ 已拍板 4 决策 |
| v0.6.2_预警与判定对齐修复 | 同源原则断裂与回归（R7/R8 的来历） |
| v0.5.4.4.4_战斗判定与场景恢复修复 | 遗留债 13 项清单 + 场景手术范例 |
| 开发调试规范_场景摄像头位置管控 | 调试红线全文（含 2.5 Reload 节） |
| v0.6.0_小地图系统 / v0.6.1_手感与反馈包 | 对应系统操作与验收 |
| 项目框架文档 / 项目进度报告 | 架构全貌 / 版本史 |

> 维护约定：每次修复/新系统落盘后，若产生新教训 → 更新本文档对应红线/速查；版本文档只增不改。
