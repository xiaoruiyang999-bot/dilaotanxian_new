# 开发必读：核心信息整合

> 状态：v1.2 生效中  
> 更新：2026-09-08  
> 玩法真源：计划书/v1.2.0_俯视横向单向DAG地牢_GDD与开发计划.md  
> 历史完整版：往期版本文档/旧设计与报告/开发必读_核心信息整合_v1.1旧版.md

## 1. 当前产品冻结线

- 游戏为 2D 俯视角房间制 Roguelite。
- 房间内上下左右自由移动；宏观 DAG 只从左向右推进。
- DAG 上下分支是抽象路线，不生成场景上门/下门。
- 战士退役；角色与职业合并为“职业角色”，首发狼人，后续法师、弓箭手。
- 选择职业角色同时确定外观、属性、武器池、技能、职业资源、动画和专属 Build。
- 武器画入角色 Sprite；身体碰撞与攻击 Hitbox 仍分离。
- 新序列帧统一使用 Unity Animation/Animator；FrameAnimator 与 WeaponPivot 只作为旧实现迁移参考。
- 攻击方向尚未最终确认，先做“仅左右”与“四方向离散”灰盒对照，不得提前批量制作最终动画。

## 2. 当前实现状态

现有 Assets/Scripts/now_use 仍是 v1.1.x 体系：四方向房间网格、独立 Class/Character、WeaponPivot、部分 FrameAnimator 与旧战斗链。它可以运行和提供技术资产，但不等于新设计已经实现。

迁移期间遵守两条：

1. 保持一个可运行入口，逐条替换，不一次删除全部旧链路。
2. 旧代码只修阻断迁移的问题，不继续增加与 v1.2 冲突的新功能。

## 3. 新架构红线

1. **数据先于对象**：DungeonGraph、DungeonNode、RunState、Build 和奖励种子是纯 C# 数据；地图逻辑不依赖房间 GameObject 是否激活。
2. **单一职业角色入口**：选择、存档、动画、奖励过滤只消费 PlayableCharacterId；禁止重新并存 CharacterId + ClassId 两套真值。
3. **攻击同源**：预警、动画窗口、Hitbox、伤害与反馈消费同一 AttackDefinition。视觉像素不是判定真源。
4. **碰撞分离**：Body Hurtbox、Movement Collider、Attack Hitbox 分责；武器烘入 Sprite 不得导致身体碰撞扩大。
5. **分层**：输入意图 → 决策/状态 → 执行 → 判定 → 数据 → 表现/UI。Animator 和 UI 不计算伤害或路线逻辑。
6. **一个启动入口**：Bootstrap/GameFlow 负责持久服务和流程，初始化必须可诊断并防止重复实例。
7. **稳定小模块**：Bootstrap、DungeonGraph、RoomRuntime、Combat、Build/Economy、Presentation/UI；禁止继续增加万能 Manager。

## 4. 继续有效的工程红线

- Trigger 只参与逻辑事件；LOS、投射物和移动探测跳过 isTrigger，实体墙才阻挡。
- 高频物理查询使用 NonAlloc + 复用缓冲；每帧缓存 GetComponent，避免 LINQ/字符串分配。
- 玩家死亡走 Respawn；新增运行状态必须在重生/新 Run 时显式重置。
- 事件订阅与退订必须配对；协程和 Tween 必须互斥并绑定生命周期。
- Prefab 实例上临时加组件会被场景重载丢失；正式组件写入 Prefab 本体并验证引用。
- 外部修改 Unity 场景后必须 Reload，再检查编译、唯一 Player、Hierarchy，最后 Play。
- 图片导入显式设置 Sprite Mode；序列帧的 PPU、尺寸与 Pivot 必须统一。
- Editor-only 资源扫描在打包前必须转换为运行时资源索引。
- 场景/YAML 手术必须备份、感知行尾、验证 fileID 双向引用、重载场景。

## 5. 房间与生成门禁

- 一个 DungeonNode 对应一个运行房间；默认左入右出。
- 房间先生成最终 RoomPlan，再一次性绘制。
- 每次生成尝试使用独立临时集合，失败不得污染下一次尝试。
- 入口到出口必须按玩家碰撞体口径可达。
- SpawnCells 至少满足玩家净空、距门、距墙和用途约束，再做 NonAlloc 物理检查。
- Room 激活关门前必须确认玩家 Collider2D.bounds 已完整进入 RoomBounds，不能只依赖 OnTriggerEnter2D。
- 房间阶段触发要可重复初始化；离开节点后不得残留敌人、订阅、Tween 或奖励状态。

## 6. Animator 约束

- 每个职业角色一个基础 Animator Controller，同武器模组用 Animator Override Controller 换 Clip。
- 参数少而稳定；动画 Clip 不直接写游戏数值。
- Animation Event/StateMachineBehaviour 只开关判定、位移或反馈窗口。
- 标准 Clip 必须通过事件完整性检查：攻击开始、Hitbox 开、Hitbox 关、攻击结束。
- 在攻击方向 T-01 定案前，只制作灰盒 Clip。

## 7. 测试与提交门禁

- 脚本编译无错，Console 无新增红错。
- EditMode：DAG 无环/可达/Seed、伤害公式、节点状态、奖励职业过滤、Missing Script。
- PlayMode/人工：职业角色选择、节点进入完成、房间卸载、死亡重开、镜头 Bounds。
- 外部改场景后 Reload 验证。
- 不遗留 Debug_ 对象、临时 Player、错误 Camera target 或脏场景。
- 每次功能落盘一份 v1.2.x 实施记录；玩法决策回写 GDD，不另起竞争版本。

## 8. 文档优先级

1. v1.2.0 GDD：玩法与版本范围。
2. 本文：工程红线。
3. AGENTS.md / project-dev-rules：代理执行摘要与全量规则。
4. 项目框架文档：模块职责。
5. 项目进度报告：实际完成状态。
6. 往期版本文档：只读历史。

