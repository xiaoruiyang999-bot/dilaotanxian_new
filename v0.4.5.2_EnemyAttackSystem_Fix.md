# v0.4.5.2 Enemy 攻击系统统一修复计划

> 版本：v0.4.5.2 EnemyAttackSystem Fix
>
> 目标：让 Enemy 与 Player 使用同一套攻击框架，统一 Windup / Active / Recovery 流程、攻击动画、范围预警与命中判定。

---

## 一、当前问题

Player 已接入统一框架：

```
PlayerCombat
    ↓
WeaponController
    ↓
WeaponAnimator
    ↓
AttackIndicator
    ↓
攻击判定
```

但 Enemy 仍使用旧逻辑：

```
EnemyCombat.TryAttack(target)
    ↓
target.TakeDamage()
```

导致：

1. Enemy 没有攻击动画。
2. Enemy 没有攻击范围预警。
3. Enemy 没有 Active 时间同步。
4. Enemy 无法复用 Player 攻击系统。

---

## 二、修改目标

最终结构：

```
EnemyAI
    ↓
EnemyCombat
    ↓
WeaponController
    ↓
WeaponAnimator
    ↓
AttackIndicator
    ↓
伤害判定
```

即：

- `EnemyAI` 只负责决策（是否进入攻击）。
- `EnemyCombat` 负责完整的攻击流程（Windup / Active / Recovery）。
- `EnemyCombat` 调用 `WeaponController` / `WeaponAnimator` / `AttackIndicator`。
- 命中判定与 Player 使用同一套扇形逻辑。

---

## 三、修改步骤

### Step 1：修改 EnemyCombat

**修改文件：** `Assets/Scripts/v0_4/EnemyCombat.cs`

**操作：**

- 删除直接 `target.TakeDamage()` 的旧逻辑。
- 新增 Windup / Active / Recovery 三阶段状态机。
- 新增 `TryAttack()` 入口，由 `EnemyAI` 调用。
- 在 `TryAttack()` 中启动 Windup。
- Windup 结束时进入 Active，调用 `WeaponAnimator.Play()`。
- Active 中按 `ActiveMomentRatio` 触发伤害。
- Recovery 结束时解锁方向、结束攻击。

**要求：**

- `EnemyCombat` 不直接控制 `WeaponPivot`。
- 只能通过 `WeaponController` / `WeaponAnimator` 间接操作武器。

---

### Step 2：Enemy 增加组件引用

**修改文件：** `Assets/Scripts/v0_4/EnemyCombat.cs`

**新增字段：**

```csharp
[SerializeField] private WeaponAnimator weaponAnimator;
[SerializeField] private WeaponController weaponController;
[SerializeField] private AttackIndicator attackIndicator;
```

**操作：**

- `Awake()` 中自动 `GetComponent` / `GetComponentInChildren` 获取。

---

### Step 3：增加 Enemy 攻击方向锁定

**修改文件：** `Assets/Scripts/v0_4/EnemyCombat.cs`

**操作：**

- 攻击开始（Windup）时调用：
  ```csharp
  weaponController.LockAttackDirection();
  ```
- 攻击结束（Recovery 结束）时调用：
  ```csharp
  weaponController.UnlockAttackDirection();
  ```

---

### Step 4：Enemy 攻击动画

**修改文件：** `Assets/Scripts/v0_4/EnemyCombat.cs`

**操作：**

- `EnemyCombat` 读取 `AttackData.AnimationType`。
- 调用 `WeaponAnimator.Play()` 时传入由 `WeaponController` 计算的角度参数。
- 禁止在 `EnemyCombat` 中硬编码 360° 旋转。
- 支持：
  - `Arc`：根据 `AttackAngle` 挥动。
  - `FullCircle`：完整旋转 360°。
  - `Spin`：多圈旋转。

---

### Step 5：Enemy 攻击判定

**修改文件：** `Assets/Scripts/v0_4/EnemyCombat.cs`

**操作：**

- 复制 `PlayerCombat.PerformFanAttack()` 的判定逻辑。
- 目标层使用 `AttackData.TargetLayer`（Player 层）。
- 判定朝向使用 `WeaponController.GetAimDirection()`。
- 保证：
  - 动画范围 = `AttackRange`
  - 实际判定范围 = `AttackRange`

---

### Step 6：修复 Input System 错误

**修改范围：** 全局搜索 `UnityEngine.Input`

**操作：**

- 禁止使用 `UnityEngine.Input`。
- 全部改为 `UnityEngine.InputSystem`。

---

## 四、EnemyAI 修改

**修改文件：** `Assets/Scripts/v0_4/EnemyAI.cs`

**操作：**

- 删除 EnemyAI 中已有的 Windup / Active / Recovery 子状态机。
- 删除 EnemyAI 中对 `WeaponController` / `WeaponAnimator` / `AttackIndicator` 的直接调用。
- `EnemyAI` 只负责：
  - 追击玩家。
  - 判断是否在攻击范围内。
  - 调用 `EnemyCombat.TryAttack()`。

---

## 五、Prefab 修改

**修改文件：** `Assets/Prefabs/Enemy.prefab`

**操作：**

- `EnemyCombat` 组件新增引用：
  - `weaponAnimator` → `WeaponAnimator`
  - `weaponController` → `WeaponController`
  - `attackIndicator` → `AttackIndicator`

---

## 六、验证标准

### Player

- 鼠标方向瞄准。
- 攻击时武器按照 `AttackAngle` 挥动。

### Enemy

- 发现玩家。
- 面向玩家。
- 攻击预警显示。
- 武器动画播放。
- Active 时间造成伤害。

### 通用

- Player 与 Enemy 代码结构一致。
- 编译 0 警告 0 错误。
