# v1.1.42 狼人冲刺（MCP 分支 Dash 移植 + v0.7.x 架构适配）

## 需求

给狼人增加冲刺效果——MCP 分支有参考（M1·v0.6.1 Dash），移植或重写。

## MCP 原版参数（完整沿用）

速度 18 / 时长 0.15s / 冷却 0.9s（结束起算）/ 收招无敌 +0.06s /
方向 = 当前输入（无输入用面朝向）/ 高速隧穿防护 `collisionDetectionMode = Continuous`（v0.7.1 教训）。

## 架构适配（v0.7.x 与 MCP 版的差异处理）

| 差异 | 适配 |
|---|---|
| MCP 直接在 PlayerController 写速度；v0.7.x 移动写入归 PlayerMovement | 新组件 `WerewolfDash` 直写速度 + `PlayerMovement.SetSuspended(true)` 抑制常规写入（单一写速者原则） |
| MCP 用 `Health.GrantIFrames(dur)`；现行 Health 只有 `SetInvincible(bool)` | 冲刺起置 true + 自管 `invincibleUntil`（Time.time 口径），Update 到点解除 |
| Dash 动作 v0.7.0 下线但有保留 | 输入资产 `Dash` 动作（Space）原样复用——PlayerController 转发至 TryDash |
| 仅狼人可用 | `WerewolfDash.EnsureOn` 三处接线：CharacterSelectUI.Pick / PrepRoomManager / RunManager×2（与 WerewolfTransformation 同点位）；改选战士组件销毁即下线 |

## 接线清单

- `WerewolfDash.cs`（新）：参数/无敌窗口/冷却/单一写速者/隧穿防护；
- `PlayerMovement.SetSuspended(bool)`：冲刺期间让出写速权；
- `PlayerController`：Dash 动作转发（懒查组件兜底后挂时序）、Move 分支维护 facingDirection、Respawn 调 ResetDash；
- 四个狼人应用点挂 EnsureOn；选择页改战士 Destroy 下线。

## 验收

1. 选狼人进准备房间/地牢：按 **Space** 冲刺（有输入朝输入方向、无输入朝面朝向），0.9s 冷却；
2. 冲刺中穿过敌方弹道/攻击不受伤（无敌窗口）；撞墙即停不隧穿；
3. 战士无冲刺（Space 无反应）；改选战士后狼人冲刺下线；死亡重生冷却复位；
4. 蓄力移动减速/移速 Buff 与冲刺互不干扰（冲刺期间常规移速管线挂起）。

## 关联

MCP 分支 M1·v0.6.1（参数原版）/ R11 无敌帧口径（Time.time）变形：bool 开关+自管截止 /
v0.7.0 PlayerMovement 单一写速者注释合同。
