# v0.7.1 操作记录

> 版本主题：减伤甲体系（玩家+怪物通用）+ 删除呼吸回甲（《v0.7_Demo玩法计划书.md》§九 v0.7.1 执行卡，总纲决策 3）。
> 执行文档：`v0.7.1_正式计划.md`。公式口径：《伤害计算公式文档.md》§三/§六。本版无新美术、无场景/prefab YAML 改动。

## 改动文件清单

### 修改脚本（6 + 1 顺路）

- `Combat/DamageResolver.cs`：+`ApplyArmor(damage, armor, reduceMul, lossMul, out armorAfter) → hpDamage` 静态纯函数——有甲：扣血=D×(1−R)、扣甲=D×L（最低扣到 0，溢出不转嫁，本次扣血仍按有甲算完）；无甲（armor≤0）：全额扣血。玩家/怪物共用一份实现，禁止两处各写。
- `Player/PlayerStats.cs`：**删呼吸回甲**（`armorRegenRate`/`armorRegenDelay`/`lastDamageTime`/`isOutOfCombat`/`Update` 护甲回复块/`OnTakeDamage()` 方法全删）；`AbsorbDamageWithArmor` **改写为** `ApplyArmorDamage(float) → float`（调 DamageResolver.ApplyArmor，护甲变化触发 OnStatsChanged）；`ModifyArmor` 保留（甲包/护甲球回甲手段，语义不变）；+OnValidate 钳制 R∈[0,0.9]、L≥0.01。
- `Player/Health.cs`：TakeDamage 内 `stats.AbsorbDamageWithArmor(damage)` → `stats.ApplyArmorDamage(damage)`；删 `stats.OnTakeDamage()` 调用（脱战计时随回甲删除）。
- `Enemy/EnemyStats.cs`：+`maxArmor`(默认 0=普通怪无甲)/`armorReduceMul`(默认 0)/`armorLossMul`(默认 1) 三字段+属性外露；OnValidate 钳制（maxArmor≥0、R∈[0,0.9]、L≥0.01）；ApplyFloorScale 只缩 HP，**护甲不随楼层缩放**（注释说明，【待补充·数值】若后续要缩再开）。
- `Enemy/EnemyHealth.cs`：+`currentArmor`（懒初始化，组件 Awake 顺序无关）/`MaxArmor`（直读 EnemyStats）+ 独立 `OnArmorChanged(current,max)` 事件；TakeDamage 走 DamageResolver.ApplyArmor（无甲敌人 armor=0 天然全额扣血，**普通怪路径数值与旧版完全一致**）；`ResetHealth` 护甲同步回满；+`AddArmor(float)` 预留。`OnHealthChanged`/`OnTakeDamage` 签名未动。
- `Enemy/WorldSpaceHealthBar.cs`：双条化——数据源为 EnemyHealth 且 MaxArmor>0 时，HP 条上方运行时加钢灰护甲细条（#708090，高约 HP 条 1/3，canvas 向上加高、pivot 底部中心不动）；订阅 OnArmorChanged；玩家/无甲敌人路径零变化。
- `Dungeon/Interaction/ShrineInteractable.cs`（顺路，不改逻辑）：注释/日志文案"护甲优先吸收"→"减伤甲结算"（祭坛自伤有甲时按 R 减免并扣甲，计划书 §2.4 预期行为）。

### 数据配置（未做，待用户 Inspector 填写）

- 未改任何 prefab/场景 YAML。精英/Boss 护甲占位值由用户在 Inspector 填：Enemy_Elite maxArmor=10/R=0.2/L=1.0、Enemy_Boss maxArmor=20/R=0.35/L=0.8（全为占位【待补充·数值】）；Enemy.prefab（普通）保持默认 0/0/1 无感。
- 无新脚本文件，csproj 无需补条目。

### 文档

- `Assets/Scripts/now_use/README.md`：版本号 v0.7.1；PlayerStats/Health/DamageResolver/EnemyStats/EnemyHealth/WorldSpaceHealthBar 职责同步。
- `版本实施自检清单.md`：§3 状态条配色行补"敌人护甲条复用钢灰 #708090"；§3 新增"减伤甲口径"行（公式+R/L 占位值+呼吸回甲已删）；§2 本版无新增错误记录。

## 验证情况

- `dotnet build Assembly-CSharp.csproj`：**0 警告 0 错误**（6 文件改完一次性构建）。
- grep `armorRegen|ArmorRegen|AbsorbDamageWithArmor|isOutOfCombat|lastDamageTime`（Assets/Scripts/now_use）：**零残留**。
- grep `OnTakeDamage`：PlayerStats/Health 零残留；剩余匹配均为计划要求保留的 EnemyHealth/ObstacleHealth 事件及其订阅方（EnemyAI/EnemyController/TrainingDummy/DestructibleObstacle），未动。
- 公式四边界逐行核对（DamageResolver.ApplyArmor vs 计划书 §2.1 / 公式文档 §三）：
  - 玩家有甲挨 4 伤（AR=8/R=0.3/L=1.0）：armorAfter=max(0, 8−4×1.0)=4，hpDamage=4×0.7=**2.8** ✓；
  - 护甲 0 后：走 armor≤0 分支，hpDamage=**4** 全额 ✓；
  - 一击 15 打穿 10 甲：armorAfter=max(0,10−15)=0，hpDamage 仍=15×0.7=**10.5**，溢出不转嫁 ✓；
  - 精英（AR=10/R=0.2/L=1.0）挨 15：扣血 15×0.8=**12**、扣甲 15→甲 0 ✓。
- 普通怪路径推算：maxArmor=0 → CurrentArmor=0 → ApplyArmor 走 armor≤0 分支原样返回 damage，armorAfter=0 不触发护甲事件；扣血/事件顺序与旧版逐行一致 ✓。
- 减伤甲实机表现（护甲条显示、扣甲缩短、脱战不回甲）未经编辑器实测，待用户验收。

## 遗留风险

- **prefab 护甲数值未填**：精英/Boss 当前 maxArmor=0（脚本默认值），不填则护甲条不出现、减伤不生效——Play 前请先在 Inspector 填占位值（见下验收清单 2）。
- WorldSpaceHealthBar 双条后 canvas 向上加高（pivot 底部中心不动），精英/Boss 血条整体上移约 1/3 条高，验收目测锚点是否合适。
- 祭坛自伤行为变化：有甲时自伤被 R 减免并扣甲（旧版护甲全额挡），计划书 §2.4 已确认属预期，验收时确认可接受。
- R/L/护甲值全为占位，设计 D 定稿后填《数值与角色设计任务书.md》§一/§四；《伤害计算公式文档.md》§八待补充项不动。
- EnemyHealth.AddArmor 为预留接口，本版无调用方。

## 待用户在编辑器验收清单

> 测试场景：`v0_7_PrepRoom` → `v0_7_ClassWeapon`；回归 `v0_4_EnemySystem`/`v0_5_Dungeon`；Game 视图 1920×1080 或 Free Aspect。

1. **编辑器先刷新**：切回 Unity 导入脚本变更（若编辑器此前开着，勿保存旧内存覆盖 YAML）。
2. **填 prefab 护甲值**：Enemy_Elite.prefab 的 EnemyStats 填 maxArmor=10/R=0.2/L=1.0；Enemy_Boss.prefab 填 maxArmor=20/R=0.35/L=0.8（占位）。
3. **玩家减伤甲**：有甲挨打——扣血=伤害×0.7、护甲同步扣减；护甲归零后全额扣血；脱战站立 10 秒护甲**不回复**（呼吸回甲已删）。
4. **敌人护甲条**：精英/Boss 头顶 HP 条上方出现钢灰细条，随扣甲缩短，甲尽后全额扣血；普通怪没有护甲条且承伤数值与旧版一致。
5. **回归**：木桩场景（v0_4）、商店护甲球回甲（ModifyArmor）、祭坛事件、法力球/宝箱全流程无损。
