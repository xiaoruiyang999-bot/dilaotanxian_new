# v0.4.5.2 执行计划（修订版）

> 说明：本计划是对 `v0.4.5.2_重构计划.md` 的**执行步骤修订**，不推翻整体架构，仅进一步收紧 `WeaponAnimator` 职责，避免其承担攻击逻辑。

---

## 一、总体架构（保持不变）

```
AttackData
    │
    ▼
PlayerCombat / EnemyAI  （读取配置、控制流程、执行判定）
    │
    ▼
WeaponAnimator          （播放动画、管理 WeaponPivot/Sprite）
    │
    ▼
WeaponPivot / WeaponSprite
```

---

## 二、修订后的执行步骤

### Step 1：修订 AttackData

**修改文件：**
- `Assets/Scripts/v0_4/AttackData.cs`

**操作：**
- 保留所有现有字段：`AttackRange`、`AttackAngle`、`AnimationType`、`AttackDamage`、`AttackCooldown`、`TargetLayer`、`WindupTime`、`ActiveDuration`、`RecoveryTime`、`AttackEase`、`ActiveMomentRatio`。
- 新增 `AttackAnimationType` 枚举：`Arc`、`FullCircle`、`Thrust`、`Spin`、`Throw`。
- 确认不新增 `StartAngle` / `EndAngle`。
- 确认不添加任何动画计算逻辑。

**与旧计划差异：**
- 无实质变化，仅明确强调不添加动画算法。

---

### Step 2：重写 WeaponAnimator

**修改文件：**
- `Assets/Scripts/v0_4/WeaponAnimator.cs`

**操作：**
- 保留字段：
  - `Transform weaponPivot`
  - `Transform weaponSprite`
  - `float weaponWidth`
  - `AttackData attackData`（仅用于 `RefreshWeapon()` 读取 `AttackRange`，可选）
- 新增/保留方法：
  - `SetAimDirection(Vector2 direction)`：非攻击状态时 `weaponPivot.right = direction`。
  - `Play(AttackAnimationType type, float arcAngle, float activeDuration, float activeMomentRatio, Ease ease, Action onActiveMoment)`：纯动画执行。
  - `Stop()`：停止 Tween，恢复 `WeaponPivot.localRotation = identity`。
  - `RefreshWeapon()`：根据 `AttackRange` 调整 `WeaponSprite` 长度/位置/宽度。
- `Awake()` 调用 `RefreshWeapon()`。
- **禁止**：读取 `AttackDamage`、`AttackCooldown`、`TargetLayer`、`WindupTime`、`RecoveryTime`。
- **禁止**：在 `Play()` 内部执行伤害判定或访问 `Player` / `Enemy` / `Combat`。

**动画实现：**
- `Arc`：`-arcAngle/2` → `+arcAngle/2`，使用 `RotateMode.Fast`（不使用 `FastBeyond360`）。
- `FullCircle`：`0°` → `360°`，使用 `RotateMode.FastBeyond360`。
- `Thrust` / `Spin` / `Throw`：预留接口，当前不实现。

**与旧计划差异：**
- 旧计划：`PlayAttack()` 内部读取 `AttackData` 获取动画参数。
- 新计划：`Play()` 所有动画参数由调用方（`PlayerCombat` / `EnemyAI`）显式传入。
- 旧计划：`ApplyWeaponScale()` 只在 `Awake()` 调用。
- 新计划：`RefreshWeapon()` 提供外部调用入口，除 `Awake()` 外，未来可在换武器/Buff/AttackRange 变化时调用。

---

### Step 3：修改 PlayerCombat

**修改文件：**
- `Assets/Scripts/v0_3/PlayerCombat.cs`

**操作：**
- 引用 `AttackData` 与 `WeaponAnimator`。
- 维护 `Windup → Active → Recovery` 状态机。
- 新增 `UpdateAiming()`：
  - 读取鼠标世界位置，计算方向。
  - 若 `!weaponAnimator.IsPlaying`，调用 `weaponAnimator.SetAimDirection(direction)`。
- `EnterActive()`：
  - 从 `AttackData` 读取动画参数。
  - 调用 `weaponAnimator.Play(...)` 并传入所有参数。
- `OnActiveMoment()`：
  - 执行扇形伤害判定（使用 `AttackRange` + `AttackAngle` + `TargetLayer` + `AttackDamage`）。
- `UpdateRecovery()` 结束：
  - 调用 `weaponAnimator.Stop()`。

**与旧计划差异：**
- 旧计划未明确 `PlayerCombat` 需显式拆解 `AttackData` 并传给 `WeaponAnimator`。
- 新计划明确：`PlayerCombat` 是攻击逻辑的入口，负责读取配置并驱动 `WeaponAnimator`。

---

### Step 4：修改 EnemyAI

**修改文件：**
- `Assets/Scripts/v0_4/EnemyAI.cs`

**操作：**
- 在 `UpdateChase()` 与 `UpdateAttack()` 中：
  - `controller.FaceTowards(dir)` 后调用 `weaponAnimator.SetAimDirection(dir)`。
- `EnterActive()`：
  - 从 `AttackData` 读取动画参数。
  - 调用 `weaponAnimator.Play(...)`。
- `OnActiveMoment()`：
  - 调用 `EnemyCombat.TryAttack(damageable)`。
- `ChangeState(Attack → Chase)`：
  - 调用 `weaponAnimator.Stop()`。

**与旧计划差异：**
- 旧计划：`EnterActive()` 调用 `weaponAnimator.PlayAttack(OnActiveMoment)`，由 `WeaponAnimator` 内部读 `AttackData`。
- 新计划：`EnterActive()` 显式从 `AttackData` 取参数并调用 `weaponAnimator.Play(...)`。

---

### Step 5：调整 Prefab

**修改文件：**
- `Assets/Prefabs/Player.prefab`
- `Assets/Prefabs/Enemy.prefab`

**操作：**
- `WeaponPivot` 本地坐标保持 `(0, 0, 0)`，位于角色中心。
- `WeaponSprite` 本地旋转保持 `-90°`。
- `WeaponSprite` 长度/位置/宽度由 `WeaponAnimator.RefreshWeapon()` 在运行时根据 `AttackRange` 设置。
- `WeaponAnimator` 组件字段更新为：`weaponPivot`、`weaponSprite`、`weaponWidth`、`attackData`（可选）。

**与旧计划差异：**
- 无实质变化，但明确 `RefreshWeapon()` 是运行时视觉更新的统一入口。

---

### Step 6：更新 Asset 配置

**修改文件：**
- `Assets/Data/PlayerAttackData.asset`
- `Assets/Data/EnemyMeleeAttackData.asset`

**操作：**
- `PlayerAttackData.asset`：
  - `animationType = 0`（Arc）
  - `attackAngle = 90`（或按需求调整）
- `EnemyMeleeAttackData.asset`：
  - `animationType = 1`（FullCircle）
  - `attackAngle` 可保留任意值（FullCircle 不读取它）

**与旧计划差异：**
- 无变化。

---

### Step 7：编译与验证

**操作：**
- 执行 `dotnet build Assembly-CSharp.csproj`。
- 目标：`0 警告，0 错误`。
- 运行时验证清单：
  - Player 武器跟随鼠标。
  - Player Arc 动画正常。
  - Player 攻击结束恢复跟随鼠标。
  - Enemy 武器朝向玩家。
  - Enemy FullCircle 动画完整旋转 360°。
  - 攻击判定与动画同步。
  - `EnemyCombat`、`PlayerController`、移动框架等未被修改。

**与旧计划差异：**
- 无变化。

---

## 三、职责划分最终版

| 模块 | 职责 | 禁止 |
|---|---|---|
| `AttackData` | 保存攻击与动画配置数据 | 运行逻辑、动画计算 |
| `PlayerCombat` / `EnemyAI` | 读取 AttackData、控制攻击流程、决定动画参数、执行判定/伤害 | 直接控制 WeaponPivot/Sprite 的 Tween |
| `WeaponAnimator` | Tween、WeaponPivot、WeaponSprite、SetAimDirection、Play、Stop、RefreshWeapon | 读取 Damage/Cooldown/TargetLayer/Windup/Recovery、执行判定、知道 Player/Enemy/Combat |

---

## 四、与上一版计划的核心 Diff

| 项目 | 上一版计划 | 修订版计划 |
|---|---|---|
| `WeaponAnimator.Play` 参数来源 | `PlayAttack()` 内部读取 `AttackData` | `Play(...)` 由 `PlayerCombat` / `EnemyAI` 显式传参 |
| `WeaponAnimator` 是否可读 `AttackData` | 读取全部动画相关数据 | 仅允许读取表现相关数据（`AttackRange`、`AttackAngle`、`AnimationType`），用于 `RefreshWeapon` / 默认初始化 |
| 视觉缩放接口 | `ApplyWeaponScale()` 仅在 `Awake()` 调用 | `RefreshWeapon()` 提供外部入口，`Awake()` 调用一次，未来支持 Buff/换武器 |
| `PlayerCombat` 职责 | 调用 `PlayAttack` | 拆解 `AttackData` 后显式调用 `Play(...)` |
| `EnemyAI` 职责 | 调用 `PlayAttack` | 拆解 `AttackData` 后显式调用 `Play(...)` |

---

## 五、待确认

1. 是否确认以上修订版执行计划？
2. 是否从 **Step 1** 开始执行代码修改？
3. `PlayerAttackData` 的 `attackAngle` 保持 `90°` 还是改为其他值？
