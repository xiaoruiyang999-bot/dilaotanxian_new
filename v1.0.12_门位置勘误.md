# v1.0.12 门位置勘误：回滚 v1.0.11 的错误"修复"

> 日期：2026-08-31  
> 状态：已落盘；编译 0/0。（门禁复跑待编辑器空闲；本次为回滚 + 调试菜单，无序列化/场景改动。）

## 勘误：v1.0.11 的门"修复"是误诊

**事实链**：
1. 场景中 Grid / Walls / Floor 三个 transform 全部位于原点、缩放 1（本轮实测）→ **瓦片坐标就是世界坐标，格子边界落在整数上**。
2. 门洞跨两格（如 [wallX1, wallX1+2]），几何中心恰在整数坐标（wallX1+1）——**原始代码 `Instantiate(doorPrefab, doorRect.center, ...)` 本来就是精确对齐的**。
3. v1.0.11 改用的 `GetCellCenterWorld` 会取"格子中心"=整数+0.5 → **门被多推半格**，门体一半嵌进墙体/探入房间——这正是用户截图中"门悬在房间中部、位置不对劲"的直接原因。

**处置**：已回滚为直接使用 `doorRect.center`（附详细注释说明坐标系依据，防止再次误诊）。

## "角色未进入但门出现"——设计行为说明

门的可见性语义（Door.cs 原设计）：**门视觉只在"关闭"时显示**；任一相邻房间处于 Active（战斗锁房）→ 门关。即：你进入一个战斗房后，该房**所有**门都会变成棕色关闭块（防逃课锁敌），清完房开启（隐藏+无碰撞）。截图中的深色横条/棕门=当前 Active 房的锁门，属正常现象；此前的"空气墙"观感大概率也是"关着的门"（碰撞开启）。如需削弱（如门半透明/只在Boss房锁），提需求即可。

## 新增验收工具

`Tools/Dungeon/Debug Dump Door Alignment`：逐门打印世界坐标、尺寸、覆盖区域，可对着画面核验门与洞口是否重合（对齐争议直接拿数据说话）。

## 验证（实测数据，非目测）

受控验证循环（临时关统一入口重定向 → 新会话生成地牢 → 逐门读取世界坐标 → 恢复配置）：
**11/11 扇门全部落在精确整数坐标**（如 Door_5_0=(0,10)、Door_9_6=(80,40)）——网格在原点时门洞几何中心恰为整数，即门与洞口完全对齐。回滚生效。

**用户"仍未修复"的原因**：报告时的 Play 会话启动于回滚编译之前——**Play 中改代码不热生效**（R14），旧会话一直跑着 +0.5 偏移的旧代码。停止 Play 重新进入即可看到修复。

## 教训（回写）

- **改坐标换算前先实测变换链**（Grid transform 是否原点/单位缩放）——本轮上一次"修复"凭假设引入了新偏移。
- **用户报"没修好"先查会话新旧**：编辑器长会话 + Play 中编译 = 截图可能来自旧代码，先做受控复验再下结论。
- v1.0.11 文档中"门偏移根因/修复"一节作废，以本文档为准；该轮的远程敌人还原部分不受影响（独立验证通过）。
