# v1.1.23 房内墙模板化 + 墙体掩码自动拼接（元气骑士规范）

## 需求（用户拍板）

1. 随机房内墙可能顶着敌人刷怪点 → 需要几套**设计好的房内墙模板**（元气骑士式设计房）；
2. 提供元气骑士墙体规范：16 张 4 方向掩码瓦片（上=8/右=4/下=2/左=1，**斜角忽略**），
   外圈满墙、门=删格开口、地板下层+墙体上层满格碰撞。

## 实现

### 1. RoomShaper v2：模板套件替换随机段

五套房心相对模板（分辨率无关，任意房尺寸平移落位；25% 概率水平镜像；15% 概率空房调剂）：

| 模板 | 结构 | 设计意图 |
|---|---|---|
| TCross 十字掩体 | 房心四方 4 段横墙 | 中心战场掩体 |
| TColonnade 双柱廊 | 左右 1/3 处 4 根竖墙 | 走廊纵深感 |
| TDiagonalL 对角双 L | 左上/右下 L 形对 | 斜向动线 |
| TRing 中环 | 围而不合的环 | 环形走位 |
| TBroken 双断竖墙 | 两条错位竖墙 | 左右掩体、三路可走 |

三条硬规则（手设计保证 + 离线仿真复核 7 组尺寸×镜像全 PASS）：
① 房心 ±2 永远开阔（**刷怪区无墙**——SpawnPositionHelper 物理选点 + 模板双保险）；
② 段为短凸 stub，不接外墙、互不粘连（段间 ≥2）、不成环——结构上无死 pocket（flood-fill 验证全地板可达）；
③ 掩体不迷宫：最坏绕行 ≤ 半圈。
落位时叠加门洞 ±2 / 房心 ±2 保护带与挖除区避让；段被保护带吃得 <2 格则整段弃。
角部挖除（L/双角/斜切房型）保持 v1.1.22 不变。

### 2. WallAutotiler：墙体 4 方向掩码拼接（基础设施，等美术自动点亮）

- 按用户规范：mask = 上×8+右×4+下×2+左×1（斜向忽略），索引 0~15；
- Build 中**开洞之后**执行（门洞断口自动拼封口/拐角瓦），覆盖外墙/模板墙/挖除缘；
- **素材契约**：`Assets/Resources/Art/Tiles/Wall/wall_00.png ~ wall_15.png`（索引=掩码），
  方形任意分辨率，建议 Point/不压缩/PPU=边长像素（1 格 1 单位）。16 张齐套即生效；
  **缺任一张整体回退现有单块墙瓦**（视觉不变，Console 一次提示，不重复尝试）。

### 3. 随机段版教训（已修）

首版间距判定 `≤1` 误伤同段相邻格 → 模板墙退化孤点。改为**整段为单位**：段内自相连
（成片），段间任一格贴近既有墙（≤1）则弃整段（防粘连成环）。

## 验收

1. 编译无错 → Play：房内墙呈现 5 种模板形态（含镜像变体），房心开阔、敌人全部刷在开阔区；
2. 任意房四向可达无死角（flood-fill 离线已验证；游戏内 F9 走查复核）；
3. 同 seed 重开完全一致；瓦片图就位后墙体自动出封口/拐角/十字拼缝（无需改代码）。

## 关联

v1.1.22（挖除房型/障碍保底保留）；素材契约同 GroundTileset/PanelSprite 的 Resources 模式。
