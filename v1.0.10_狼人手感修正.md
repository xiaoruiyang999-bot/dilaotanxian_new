# v1.0.10 狼人手感修正：容器减半 / 全方向行走循环 / Idle 朝向记忆

> 日期：2026-08-31  
> 状态：已落盘；编译 0/0。（EditMode 门禁复跑因编辑器实时 Play 中暂缺，编译与既有 9 项无回归风险：本次仅改 FrameAnimator 一常量 + 一方法。）

## 三项修正（全部在 FrameAnimator）

1. **容器减半**：`CreateWerewolfVisual` 目标高系数 ×1.95 → **×0.975**（≈0.9 世界单位，与战士视觉等高量级；此前 1.8u 偏大）。等比链条不变——兽化 1080px 帧同缩放自动 ≈1.35u（原 2.64u 的一半）。
2. **全方向行走循环**：`UpdateWerewolfDrive` 改用**速度矢量模长**判定移动（此前只看 vx——纯垂直移动时误播待机）。狼人无正/背帧，任意方向移动统一播左/右行走组。
3. **Idle 朝向记忆**：停步时不再取停步瞬间 vx 符号（≈0，恒偏右——这就是"只用向右待机"的原因），改用 **LastHorizontalInput（最后移动方向）** 选 idle_l/idle_r；纯垂直移动不改变朝向记忆。普通狼与兽化两分支同规则。

## 验证

- 编译 0/0；朝向/移动逻辑为确定性重写（无序列化/场景改动）。
- 待人工实测：①狼人体型减半观感 ②上下移动播行走循环 ③向左走→停步→向左待机帧 ④兽化同样三条。

## 关联

v1.0.9 狼人系统还原（变身/数值/同源判定）不变；本次仅视觉标定与驱动修正。
