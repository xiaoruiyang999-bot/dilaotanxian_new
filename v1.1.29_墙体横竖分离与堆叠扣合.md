# v1.1.29 墙体横竖分离生成（竖列专用 prop_01 堆叠扣合）

## 需求（用户三条）

1. 横向墙与竖向墙生成逻辑不能一致；
2. 竖向墙**只用 prop_01**；
3. 实现"下部的图扣在上一张图上"——墙柱自上而下堆叠、下方块顶部叠在上方块底部。

## 实现

### WallPropTileset 拆分

- `GetVertical()`：**仅 prop_01**（宽归一 1 格、底边贴格底、高 ≈1.47 向上伸 0.47 进上一格）；
- `GetHorizontal(variant)`：其余素材随机引用（同 v1.1.28 归一规则）；
- 兜底链：横池空回退 prop_01 → 仍缺回退白方块。

### DungeonBuilder

- **渲染序 `TilemapRenderer.SortOrder.TopLeft`**（Build 期设置，代码改无需动场景）：墙 Tilemap
  自上而下逐行渲染 → 下方格后画、其向上伸出的部分**盖住上方块的底沿** = "下图扣上图"的堆叠遮挡，
  竖列视觉上连成一根石墙柱；横行块宽恰 1 格无横向重叠，不受渲染序影响。
- **`ReorientWalls()` 终遍定向**（开洞之后执行）：逐格按**最终拓扑**判向——上下有墙邻 = 竖列
  （重铺 prop_01），否则横行（随机池）。门洞打断的墙列端点（只有单侧上/下邻）仍判竖 ✓；
  塑形 stub、角格（四邻皆墙）归竖列 ✓。铺装期 SetWallTile 的临时图全部被终遍覆盖，
  避免绘制顺序导致的误判。

## 验收

Reload → Play：竖向墙为一根根连续堆叠的 prop_01 石柱（下扣上、无断缝）；横向墙随机变体石块；
门洞两侧墙端正确收口。碰撞/门/刷怪逻辑零改动（纯视觉层重构）。

## 关联

v1.1.28（宽归一/整格碰撞/一块厚结构保留）/ R19 平面不受影响。

## v1.1.29.1 紧急修复：碰撞体膨胀盖门（Tile.transform 陷阱）

初版用 `tile.transform` 缩放做宽归一——**TilemapCollider2D 会把 tile.transform 应用到碰撞形状
（ColliderType.Grid 同样受影响）**，缩放系数 ≈1.6~2.0 使每格墙碰撞放大近两倍：膨胀碰撞盒互相
重叠并盖住院门洞（"碰撞体与素材大小不一致、唯一的门过不去"）。
修复：归一化**烘焙进 Sprite**——运行时 `Sprite.Create(texture, rect, pivot, PPU=纹理宽)`：
宽恒 1 格、pivot=(0.5, 0.5/h) 使底边贴格底；Tile 零 transform ⇒ Grid 碰撞严格整格 1×1。
教训（已入必读）：**Tilemap 碰撞体大小 = 素材/transform 语义，任何 tile.transform 缩放都会
同比放大碰撞——需要视觉缩放时改烘焙 Sprite（PPU/pivot），不动 transform。**
