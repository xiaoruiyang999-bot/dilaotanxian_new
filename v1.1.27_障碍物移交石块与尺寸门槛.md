# v1.1.27 障碍物职责移交废墟石块（尺寸门槛收紧）

## 需求（用户拍板）

1. 未使用废墟石头的多余障碍物（旧木箱表）不再生成——障碍物整体由石块承担；
2. 生成障碍物的石块必须有**尺寸限制**：只有真巨石够格，中等石块不当障碍物。

## 改动

- **`StoneDecorSpawner`**：障碍物门槛 `LargeThresholdUnits` 1.06 → **1.2 单位（≈102px）**——
  当前合格 7 张（prop_18/19/20/21/25/29/30，105~146px），97px 以下全部并入无碰撞装饰池（27 张）；
  大石块数量 0~2 → **1~3/房**（木箱停用后障碍密度由石块独扛）。
- **`DungeonBuilder.SpawnContent`**：注释停用 `ObstacleSpawner.Spawn`（旧木箱表休眠保留，
  恢复一行即可混搭）；v1.1.22 的面积保底随调用一并休眠。
- 亮度旋钮 `Brightness` 保留（用户自调，当前 3=强提亮）。

## 验收

编译无错 → Play：房内不再出现木箱；障碍全部为 ≥1.2 单位的大巨石（1~3 个/房，可砍碎）；
小/中石块纯装饰无碰撞；落点防卡墙规则不变。

## 关联

v1.1.26 石块系统 / v1.1.22 面积保底（随表休眠）/ R6 Obstacle 层语义不变。
