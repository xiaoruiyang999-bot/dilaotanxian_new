# v0.7.4 操作记录

> 版本主题：技能框架——SkillData/分支/执行器/四槽接线（《v0.7_Demo玩法计划书.md》§六 + §九 v0.7.4 执行卡）。
> 执行文档：`v0.7.4_正式计划.md`。零场景/prefab/既有 SO 资产 YAML 改动；新资产全部"新增文件 + 手写 .meta 新 GUID"；.inputactions 只增不改。

## 改动文件清单

### 新建脚本（Assets/Scripts/now_use/Skill/，新目录，5 个，.meta 手写新 GUID）

- `SkillType.cs`：技能类型枚举（MeleeAoE 本版唯一实现，扩展位预留）。
- `SkillData.cs`：技能配置 SO（menuName "Skill/Skill Data"）：displayName/skillType/manaCost/cooldown/damageMultiplier/aoeRadius/iconColor/level（默认 1，OnValidate 钳 ≥1）/damageMultiplierByLevel 等级数值表（空表=平直，`GetDamageMultiplier()` 按 level 查表、越界回退基值，供 v0.7.6 天赋升级读）；全 `[SerializeField] private` + 只读属性。
- `SkillBranchData.cs`：分支表 SO（menuName "Skill/Branch Data"）：displayName + `List<SkillData> branches`，`GetBranch(int)` 越界回退 0、空表 null。
- `SkillCatalog.cs`：技能资产目录（ClassCatalog 同模式：编辑器 AssetDatabase 硬编码路径 / 构建 Resources.Load("Skill/...")）；资产名清单单点收口（AssetNames 五份）；`GetBranches(ClassType)` / `GetUltimate(ClassType)` / `GetWeaponSkill(WeaponData)` 三入口，射手/法师未实装返回 null 并 Warning。
- `SkillExecutor.cs`：技能执行器（玩家组件）：三槽 `SkillSlot[3]`（0=小技能 1=大招 2=武器技能，data + CD/红闪计时）；Start 装配（小技能=分支选中 ← `RunStateCarrier.ChosenSkillBranchIndex`；大招=职业；武器技能=当前武器；ClassData/WeaponData 接线值优先、null 走 SkillCatalog 兜底；未选职业的旧场景全槽留空）；Update 递减 CD 并每帧推 SlotBarUI；`TryCastSlot(int)` CD 中/法力不足 → 槽位文本红闪 0.2s 返回，否则 TryConsumeMana + 起 CD + 按类型执行；订阅 `PlayerWeaponHolder.OnWeaponChanged` 武器技能槽整套替换（CD 清零独立计时）。MeleeAoE 执行：`OverlapCircleAll(玩家位置, aoeRadius, Enemy 层)` → 跳过自身与 trigger → `DamageResolver.Deal`（DamageContext：baseAttack 只取 `PlayerStats.Attack` 不并入武器攻击、multiplier=技能倍率、crit 取 PlayerStats）；表现 = 独立 SkillIndicator（AttackIndicator 圆形灰显 0.2s，detachOnShow=false 跟随玩家，半径按 lossyScale 换算与判定同源）+ DOTween 缩放缓圈（SpawnHealRing 同款，全部 SetLink）。

### 新建 SO 资产（Assets/Data/Skill/，5 份，手写 YAML + 新 GUID .meta；数值全【待补充·数值】）

- `Skill_Warrior_StandFirm.asset`：屹立不倒·占位（MeleeAoE，蓝耗 20 / CD 8s / 倍率 1.0 / 半径 2.5，钢灰 #708090）。
- `Skill_Warrior_PowerStrike.asset`：强力一击·占位（蓝耗 20 / CD 6s / 倍率 1.75 / 半径 2.0，橙红 #E74C3C）。
- `Skill_Warrior_Ultimate.asset`：燃命·占位·大招（蓝耗 40 / CD 20s / 倍率 2.5 / 半径 3.5，金红）。
- `Skill_Whirlwind_Weapon.asset`：旋风斩·占位·武器技能（六武器共用，蓝耗 10 / CD 5s / 倍率 1.2 / 半径 2.0，银白）。
- `SkillBranch_Warrior.asset`：战士小技能分支表 = [屹立不倒·占位, 强力一击·占位]（引用走两份技能资产 .meta 的 guid）。

### 修改脚本（5 处）

- `Player/PlayerController.cs`：①Awake 运行时 Get-or-Add 挂 `SkillExecutor`（ItemInventory 同模式）；②Skill 分支删 Debug.Log 占位 → `skillExecutor.TryCastSlot(0)`（补判 health.IsDead）；③新增 `"Ultimate"`/`"WeaponSkill"` 两个 performed 分支 → `TryCastSlot(1/2)`（仿 UseItem 先判死亡）；④ClassSelectUI 屏蔽清单补 "Ultimate"/"WeaponSkill"。
- `Class/ClassData.cs`：+序列化字段 `skillBranches(SkillBranchData)` / `ultimateSkill(SkillData)` + 只读属性（本版既有资产不接线，null 走 SkillCatalog 兜底，未改任何既有 .asset）。
- `Weapon/WeaponData.cs`：+序列化字段 `weaponSkill(SkillData)` + 只读属性（同上）。
- `Class/RunStateCarrier.cs`：+`ChosenSkillBranchIndex`（private-set 属性）+ `SetSkillBranch(int)`（负值钳 0；注释死亡保留与 LastChosenClass 同规则）。
- `Player/SlotBarUI.cs`：**仅注释同步、零逻辑改动**——技能三槽"—"占位改由 SkillExecutor 每帧推送覆盖（Refresh 写的"—"在渲染前同帧被覆盖，无闪烁；数据缺失槽不推送、维持"—"），SetSkillDisplay/SetSkillCooldown 实现不动。

### 输入（Assets/PlayerInputActions.inputactions，JSON 只增不改）

- Player map 新增 `Ultimate`（绑定 `<Keyboard>/q`）/ `WeaponSkill`（绑定 `<Keyboard>/r`）两个 action + binding，id 全部新 GUID；既有 8 个 action 与绑定零改动（Skill=F 保留为小技能键）。

### 工程与文档

- `Assembly-CSharp.csproj`：手工补 5 个 `<Compile Include>`（Skill/ 五脚本，自检 #12）。
- `Assets/Scripts/now_use/README.md`：版本号 v0.7.4；目录结构 +Skill/（5）；清单 83→88；PlayerController/SlotBarUI/ClassData/WeaponData/RunStateCarrier 条目同步；新增 Skill/ 五脚本条目。
- `版本实施自检清单.md`：§3 输入行 8→10 actions（补 Skill=F/Ultimate=Q/WeaponSkill=R → TryCastSlot 槽 0/1/2）；§3 新增"技能框架（v0.7.4）"行；最近更新行 v0.7.4（2026-08-04）。§2 本版无新犯错，未补录。
- `v0.7_Demo玩法计划书.md`：§九 v0.7.4 执行卡标完成（仿 v0.7.3 行格式 + 落地说明 + 遗留）。

## 验证情况

- `dotnet build Assembly-CSharp.csproj`：**0 警告 0 错误**（已用时间 4.8s，5 个新脚本已手工补进 csproj）。
- **GUID 查重**（grep 全 Assets/ProjectSettings/Packages，16 个新 GUID）：无冲突——SkillData.cs guid 出现 5 次 = 自身 .meta + 4 份 SkillData 资产 m_Script 引用（预期）；SkillBranchData.cs guid 2 次 = .meta + 分支表资产（预期）；屹立不倒/强力一击资产 guid 各 2 次 = 自身 .meta + 分支表 branches 引用（预期）；其余各 1 次。
- **inputactions JSON 校验**：解析通过，Player map 10 actions（Move/Attack/Dash/Sprint/Interact/Cancel/Skill/UseItem/Ultimate/WeaponSkill），14 bindings，Ultimate→`<Keyboard>/q`、WeaponSkill→`<Keyboard>/r`。
- **调试日志**：新代码无 Debug.Log（仅 Warning）；PlayerController 的 Skill 占位 Debug.Log 已删。
- 代码审查核对：
  - DamageContext 字段与 WeaponHitbox.cs:133-139 用法一致；`DamageResolver.Deal(IDamageable, ctx)`、`PlayerStats.TryConsumeMana/Attack/CritRate/CritDamage`、`PlayerWeaponHolder.OnWeaponChanged/Current.Data`、`ProjectileVisualBuilder.GetCircleSprite` 签名均已对源码核实 ✓；
  - SkillExecutor/SkillIndicator 运行时 AddComponent：SkillExecutor 无 RequireComponent、AttackIndicator 无 RequireComponent，无抽象 RequireComponent 坑（自检 §1）✓；
  - DOTween 三处 tween（DelayedCall 收指示器 / DOScale / DOFade）全部 SetLink ✓；
  - 输入分发走 action 名字符串（"Skill"/"Ultimate"/"WeaponSkill" 与 JSON 严格一致），无 KeyCode 硬编码 ✓；
  - 中文仅经 SlotBarUI 既有 TMP 文本（TMPFontProvider.Font），无新字体路径 ✓；
  - 范围指示器 sortingOrder=100 与 v0.6.3 既有近战范围显示同组件同层级（既有惯例，非新增违规）；缓圈 sortingOrder=10 不超玩家层 ✓。
- **实机表现（四槽填名、F/Q/R 施放 AOE 掉血扣蓝起 CD、红闪、切武器换技能、分支切换）未经编辑器实测，待用户验收。**

## 与计划的偏差

- **SlotBarUI 零逻辑改动**：计划 2.2 预期改 SlotBarUI 使三槽由 SkillExecutor 驱动；实施发现 Refresh 的"—"写入与 SkillExecutor 每帧推送在同一帧渲染前完成，推送天然覆盖、无闪烁，故只需每帧推送即可达成行为目标，SlotBarUI 仅同步注释。降低了改动面，行为与计划一致。
- **未选职业（旧回归场景 v0_4/v0_5）三槽留空**：计划未明示；旧场景 PlayerStats.maxMana=0 无法力，装配上技能也放不出，故 AssembleSlots 在无职业时直接留空维持"—"，更干净。
- **CD 中红闪的实现**：SetSkillCooldown 不写颜色（按计划"实现不动"），红闪期间改走 SetSkillDisplay 推"秒数+红色"，闪结束补一帧恢复技能色——效果等同"槽位文本闪色"，未改 SlotBarUI 接口。

## 遗留风险

- **编辑器运行中的外部改动需重导入**：新 .cs/.asset/.meta 与 .inputactions 都是外部新增/修改，Play 前必须切回编辑器让其 Focus 重导入（必要时 Asset > Refresh）；并核对 PlayerInput 仍能读到 action 列表（.inputactions 只增不改，风险低）。
- **战士技能全是占位效果**：三分支/大招/武器技能均为旋风斩 MeleeAoE 占位，数值全【待补充·数值】，v0.7.5 按设计稿实装真效果。
- **蓄力中放技能无打断/互斥**：技能分发在 PlayerController 层、不进 PlayerCombat 状态机（计划内从简），v0.7.5 实装时评审。
- **Radial360 CD 扫面未做**：沿用 SlotBarUI 文本秒数（计划书 §4.1 规格，v0.7.11 UI 收口再评）。
- **分支切换入口 UI 未做**：分支只能代码调 `RunStateCarrier.SetSkillBranch(i)` 验证（局外读写、局内锁定）。
- **打包构建**：技能资产需复制到 `Resources/Skill/`（SkillCatalog 已留分支，与 ClassCatalog 同规则）。
- 技能正式图标/VFX 未做（色块+文字占位，资产需求总清单排期内）；伤害数字暴击放大表现未做。

## 待用户在编辑器验收清单

> 测试场景：`v0_7_PrepRoom` → `v0_7_ClassWeapon`；Game 视图 1920×1080 或 Free Aspect。
> **Play 前先切回编辑器**让新脚本/资产/action 重导入（Focus 即触发，必要时 Asset > Refresh）；编辑器此前开着的话勿保存旧内存覆盖 YAML。

1. **四槽显示**：进游戏右下四槽 = 屹立不倒·占位 / 燃命·占位 / 旋风斩·占位 / 道具栏（原为"—"的三槽已填名，各带占位色）。
2. **施放**：F/Q/R 各放出圆形 AOE（灰圈短显 + 技能色缓圈），命中怪掉血（暴击按 PlayerStats 判定）、扣法力、槽位起 CD 秒数倒数；CD 中/法力不足再按无效且槽位文本红闪。
3. **切武器**：地牢里换武器 → 武器技能槽名称/CD 重置（仍为旋风斩·占位但 CD 清零独立），F/Q 两槽不受影响。
4. **分支读写**：代码调 `RunStateCarrier.SetSkillBranch(1)` 后重开一局 → 小技能槽变强力一击·占位（局内锁定，局外可改）。
5. **职业选择 UI**：打开 ClassSelectUI 期间 F/Q/R 不触发技能（与 Attack/UseItem 同屏蔽规则）。
6. **回归**：普攻/蓄力/连发、C 道具栏、E 拾取、换武器入包照旧；`v0_4_EnemySystem` 木桩场景三槽维持"—"无损。
