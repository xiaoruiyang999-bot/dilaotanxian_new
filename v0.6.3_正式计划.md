# v0.6.3 正式计划：六把基础武器（Projectile/蓄力/弹夹换弹/掉落闭环）

> 依据《v0.6_职业与武器系统开发计划书》第五章 v0.6.3 小节 + 4.6/4.4/4.10；美术规格见《v0.6_美术清单》§二/§三/§九。
> 前置：v0.6.2（职业/武器框架/准备场景）已完成。
> 本文是 v0.6.3 的执行文档：设计冻结点 + 数值表 + 文件清单。收尾时另出《v0.6.3操作.md》。

## 〇、用户已确认决策（2026-07-28）

| # | 议题 | 决策 |
|---|------|------|
| 1 | 宝箱奖励 | **武器/法力瓶随机二选一**（各 50%）：武器按玩家当前职业从该职业可用列表随机一把；未选职业（旧场景）退回治疗球 |
| 2 | 武器手持视觉 | **运行时程序化多色块拼接**（沿用 v0.6.2 展台做法，白图 SpriteRenderer 染色，不写 prefab YAML） |
| 3 | 法力瓶投放 | **商店 + 宝箱都掉**（商店补给 Row 陈列新增一件免费法力瓶；宝箱 50% 概率掉） |

计划书既定不再重复确认：闪避保留蓄力、蓄力移动 ×0.5、旧武器原地掉落、职业不符拒拾、法力球 walk-over 吸附、子弹撞墙销毁、对象池不做（技术债）。

## 一、范围与文件清单

### 新建脚本（9）

| 文件 | 职责 |
|------|------|
| `Combat/ProjectileData.cs` | 子弹 SO：速度/伤害/存活/半径/视觉类型/配色/目标层 |
| `Combat/Projectile.cs` | 子弹：直线飞行 + Trigger 命中 IDamageable + 撞墙（Default 层非触发器）销毁 + 存活兜底 + 通用命中特效 |
| `Weapon/WeaponVisualBuilder.cs` | 运行时构建六武器模块化多色块手持视觉 + 地图掉落小图标（含职业色底板） |
| `Weapon/ProjectileVisualBuilder.cs` | 运行时构建子弹视觉（箭矢/弩矢/能量弹/精灵弹）与命中特效色块 |
| `Player/AmmoUI.cs` | 弹药面板：武器名 + 弹夹 x/y + 换弹进度细条（运行时构建，挂 Canvas） |
| `Dungeon/Interaction/ManaOrb.cs` | 击杀掉落小法力球：walk-over 吸附（OnTriggerEnter2D，不占 E 键、不进拾取列表） |
| `Dungeon/Interaction/ManaBottlePickup.cs` | 法力瓶（IPickupable，E 拾取 +40 法力，SO 可调） |

（WeaponBehavior 派生类 RangedWeaponBehavior / SelfCastWeaponBehavior 写在现有 `Weapon/WeaponBehavior.cs` 内，不另立文件。）

### 修改脚本（11）

| 文件 | 改动 |
|------|------|
| `Weapon/WeaponData.cs` | +projectileData、+healAmount、+蓄力参数组（chargeMaxTime/各上限倍率） |
| `Combat/AttackData.cs` | +`CreateRuntimeCopy()` + 运行时副本用 setter（SetRangeAngle/SetDamage），磁盘资产零改动 |
| `Combat/WeaponController.cs` | +SetAttackData（换装同步视觉长度）、+宽度倍率（枪矛蓄力）、+自定义视觉挂载/清除（隐藏默认色块） |
| `Combat/WeaponHitbox.cs` | 判定宽度改读 WeaponController.WeaponWidth 实时值（蓄力宽度缩放生效；判定逻辑零改动） |
| `Player/PlayerCombat.cs` | 核心：近战/远程/自身施法三模式 + 蓄力状态机 + 弹夹/换弹/自动换弹计时 + 弹药事件 |
| `Player/PlayerController.cs` | Attack 由 performed 改 started/canceled 转发（按下/松开），支持长按蓄力与连发 |
| `Player/PlayerMovement.cs` | +蓄力减速（×0.5，蓄力期间不叠加奔跑，减速优先） |
| `Weapon/WeaponBehavior.cs` | Ranged/SelfCast 两个派生类接入分发（替换 TODO） |
| `Weapon/WeaponPickup.cs` | 地图掉落视觉升级：WeaponVisualBuilder 小图标 + 职业色底板（替换白块染色） |
| `Dungeon/Interaction/ChestInteractable.cs` | 奖励改"本职业随机武器 / 法力瓶"二选一（旧治疗球作无职业兜底） |
| `Dungeon/Interaction/SupplyInteractable.cs` | SupplyType +Mana（AddMana，免费占位不变） |

### 数据资产

- 新建：`AttackData_PlayerKnife.asset`、`AttackData_PlayerSpear.asset`（刀/枪矛拆分，不再共用 PlayerAttackData）
- 新建：`ProjectileData_Arrow/Bolt/EnergyOrb/SpiritOrb.asset`（精灵弹为 v0.6.5 预留）
- 更新：六份 `WeaponData`（接 projectileData/healAmount/蓄力参数/新 AttackData 引用）
- 更新：`Enemy_Elite.prefab`（manaOrbValue=8）、`Enemy_Boss.prefab`（manaOrbValue=20）
- 新建：`Supply_ManaBottle.prefab` + `InteractableTable_Shop.asset` 加一行
- `EnemyStats.cs` +manaOrbValue 字段（默认 3，普通怪零改动）

## 二、攻击链路重构（核心）

### 2.1 输入流（PlayerController → PlayerCombat）

```
Attack.started  → combat.OnAttackPressed()    （按下）
Attack.canceled → combat.OnAttackReleased()   （松开）
```

Button 类型无 interactions：started/performed 同时发于按下，canceled 发于松开。原 `TryAttack()` 保留，由 OnAttackPressed 内部调用（无蓄力近战=现状不变）。

### 2.2 PlayerCombat 三模式

```csharp
private enum CombatMode { Melee, Ranged, SelfCast }
// 当前 WeaponInstance（null = 默认近战，行为与 v0.6.2 前完全一致）
// 近战模式：装备武器 = AttackData 运行时副本 + 蓄力规则
// 远程模式：fireCooldown/换弹/闲置计时 + Projectile.Launch
// 自身施法：Heal + 绿环特效 + 弹夹/换弹
```

行为挂接（WeaponBehavior.Apply 分发，签名不变）：
- `MeleeWeaponBehavior` → `combat.SetMeleeWeapon(WeaponInstance)`
- `RangedWeaponBehavior` → `combat.SetRangedWeapon(WeaponInstance)`
- `SelfCastWeaponBehavior` → `combat.SetSelfCastWeapon(WeaponInstance)`
- `PlayerWeaponHolder.Unequip` → 仍调 `SetAttackData(default)`，内部清模式/清实例/销毁运行时副本/还原视觉与宽度。

### 2.3 蓄力状态机（三武器：刀/枪矛/弓箭）

```
OnAttackPressed（可蓄力武器、非攻击中、非换弹中）
  → isCharging = true，ChargeTimer 起计时
  → PlayerMovement.SetChargeSlow(true)（移速 ×0.5，奔跑不叠加）
  → 武器 Effect 部件开始发亮（随蓄力等级 lerp → 白）
蓄力中（Update）：t = clamp01(ChargeTimer / chargeMaxTime)
  闪避不打断：蓄力状态在 PlayerCombat，Dash 全程不触碰（计划书 4.6）
OnAttackReleased
  → 应用缩放到运行时副本/视觉宽度 → TryAttack()（近战）或 Projectile.Launch（弓箭）
  → SetChargeSlow(false)、发光熄灭
  → 近战：当次挥击结束（subPhase 回 None）后副本参数与宽度倍率还原基准值
```

- 刀（FanScale）：range ×(1→1.8)、angle ×(1→1.8) —— 改运行时副本，磁盘资产不动；
- 枪矛（RectScale）：range ×(1→2.0)、武器宽度 ×(1→1.2)（WeaponController.SetWidthMultiplier，WeaponHitbox 实时读）；
- 弓箭（ProjectileBoost）：伤害 ×(1→2.0)、弹速 ×(1→2.0) —— Launch 参数，不动数据。

### 2.4 弹夹/换弹（WeaponInstance + PlayerCombat 计时）

- 射击前 `TryConsumeAmmo`；弹空 → 自动 `BeginReload`（ReloadTimer = reloadTime，Update 递减，到期 FinishReload）；
- 连弩 `autoReloadIdleTime=3`：未开火闲置 3s 且弹夹不满 → 自动换弹；
- 换弹中禁止射击/蓄力；
- 弹药事件（AmmoUI 订阅）：`OnAmmoChanged(current, max)`（max=0 → 隐藏）、`OnReloadProgress(remaining,total)`、`OnWeaponChanged(name, color)`。

### 2.5 远程发射

```
TryFire：验 fireCooldown / IsReloading / 弹药
  → Projectile.Launch(projectileData, 枪口 = WeaponPivot.position + aimDir×0.6,
      aimDir, owner=玩家, damageMul, speedMul)
  → fireCooldown = max(fireInterval, 0.05)；弹空自动换弹
```
连弩：attackHeld 期间每到 fireInterval 自动 TryFire（长按连发）。

### 2.6 自身施法（治疗法杖）

按一下 → 验弹药 → `Health.Heal(healAmount=2)` + 绿色低透明圆环缩放淡出特效（≈0.4s）→ 弹空自动换弹（1s）。无攻击判定、不动 PlayerCombat 三阶段。

## 三、Projectile 系统

```csharp
public static Projectile Launch(ProjectileData data, Vector2 origin, Vector2 dir,
                                GameObject owner, float damageMul = 1f, float speedMul = 1f)
```

- GameObject：Kinematic Rigidbody2D + CircleCollider2D(trigger, radius=data.radius) + Projectile + 视觉子物体（ProjectileVisualBuilder 按 visualKind 构建）；
- Update 直线飞行 `dir × speed×speedMul`；lifetime 到期销毁；
- OnTriggerEnter2D：跳过 owner 及其子物体、跳过 trigger（敌人探测圈）；命中 targetLayer 内 IDamageable → `TakeDamage(damage×damageMul)` + 命中特效 + 销毁；命中 Default 层非触发器（墙/门/关闭的门）→ 命中特效 + 销毁；
- 命中特效：3~5 个小方块 DOTween 飞散+淡出（按子弹色染色，通用一份代码）；
- 对象池不做（技术债，计划书第七章）。

## 四、运行时视觉（WeaponVisualBuilder / ProjectileVisualBuilder）

白图 Sprite（`Texture2D.whiteTexture` + `Sprite.Create`，PrepPedestal 同款全局缓存）+ 调色板（美术清单 1.2）。每部件独立 SpriteRenderer（命名 `Blade/Guard/Handle/Effect…`），Root 挂 WeaponPivot 下、localRotation -90°（与现有 WeaponSprite 同约定，部件沿 +Y 排布）。挂载时隐藏默认武器色块，卸下还原。

| 武器 | 部件（下→上） | 配色 | 尺寸档位 |
|------|--------------|------|----------|
| 刀 | Handle 深棕柄 → Guard 金黄横条+红宝石 → Blade 银灰刃+浅银尖 | 银灰/金/红/棕 | 长 ≈1.2 |
| 枪矛 | ButtEnd → Shaft 深棕长杆 → Collar 暗金环+红缨 → SpearHead 银灰枪头 | 银灰/暗金/红/棕 | 长 ≈1.8 |
| 弓箭 | Grip 绿皮把 → BowLimb×2 深棕弧臂 → BowString 白弦 | 棕/绿/白 | 高 ≈1.1 |
| 连弩 | Grip 绿把 → Rail 深棕机身 → CrossbowArm 金黄双臂 → Magazine 灰弹匣 | 棕/金/灰/绿 | 0.9×0.7 |
| 治疗法杖 | ButtEnd → Shaft 靛蓝杆 → HeadRing 金环 → StaffHead 翠绿珠 | 靛蓝/金/翠绿 | 长 ≈1.3 |
| 能量法杖 | ButtEnd → Shaft 靛蓝杆 → Prongs 金三爪 → OrbCore 蓝珠+浅蓝芯 | 靛蓝/金/蓝 | 长 ≈1.3 |

- 蓄力发光：根部建 `Effect` 部件（刃尖/枪头/箭位色块），PlayerCombat 按蓄力等级 lerp 至白；
- 地图掉落图标：同款缩略构建（0.7×）+ 底部职业色底板小方块（WeaponPickup.Drop 调用）；
- 子弹视觉：箭矢（棕杆+银头+绿羽）/弩矢（短粗深灰+银头）/能量弹（蓝圆+浅蓝芯+白高光）/精灵弹（小蓝圆+金高光，预留）。

## 五、掉落闭环

- **法力球（ManaOrb）**：蓝色小圆点+白高光，trigger 半径 0.4；OnTriggerEnter2D 碰玩家 → `PlayerStats.AddMana(amount)` + 收缩销毁（walk-over 吸附，不占 E、不进列表）。敌人死亡由 `EnemyController.OnEnemyDeath` 掉落，数值读 `EnemyStats.manaOrbValue`（普通 3 / 精英 8 / Boss 20，>0 才掉）；
- **法力瓶（ManaBottlePickup）**：IPickupable，E 拾取 `AddMana(40)`，运行时瓶形视觉；宝箱掉落 + 商店补给各一处；
- **宝箱**：`SpawnItem` 改 50/50——武器（玩家 CurrentClass.AvailableWeapons 随机一把，`WeaponPickup.Drop` pop-in）/ 法力瓶；无职业（v0_4/v0_5 旧场景）退回原 HealPickup 路径；
- **商店**：`SupplyType.Mana` + `Supply_ManaBottle.prefab`（克隆现有补给 prefab 改类型/配色/数值）+ `InteractableTable_Shop` 加一行（Row 陈列尾部）。

## 六、数值表（DPS 配平，基准 = 现平击 1/0.7s ≈ 1.43）

| 武器 | 单发伤害 | 节奏 | 理论 DPS | 倍率 | 备注 |
|------|---------|------|---------|------|------|
| 刀 | 1.1 | 0.18+0.20+0.28=0.66s | 1.67 | 1.17× | 蓄力只加范围（×1.8 上限） |
| 枪矛 | 1.3 | 0.25+0.22+0.33=0.80s | 1.63 | 1.14× | 手长 2.4；蓄力长×2/宽×1.2 |
| 弓箭 | 1.6 | 射+装填 ≈1.0s | 1.60 | 1.12× | 蓄满伤害/弹速×2（50% 蓄力模型 ≈1.4×） |
| 连弩 | 0.5 | 0.15s/发，12 发+2.5s 换弹 | 1.40 | 0.98× | 长按连发，换弹真空期 |
| 能量法杖 | 1.5 | 射+装填 ≈1.1s | 1.36 | 0.95× | 法师主力输出 |
| 治疗法杖 | —（自疗 +2） | 1s 装填 | — | — | 无伤害，功能武器不计配平 |

全部落在 0.8~1.2 倍区间（第六章）。数值写入各资产注释（OnValidate/说明字段），进游戏实测后再调。

## 七、验收清单（计划书第五章 v0.6.3）

1. 六武器机制符合 4.6 表：刀/枪矛蓄力缩放、弓箭蓄力增益、连弩长按连发+双自动换弹、治疗法杖自疗+绿环、能量法杖单发；
2. 蓄力移动 ×0.5；**闪避中蓄力保留**，闪避结束可继续蓄力/释放；
3. 跨职业拾取被拒（"职业不符"）；换武器旧武器原地掉落可捡回（小图标+职业色底板）；
4. 子弹命中敌人/障碍物结算伤害，撞墙销毁，不穿墙；存活时间兜底；
5. 弹药 UI：弹夹 x/y、换弹进度、换武器刷新、无弹夹武器隐藏；
6. 击杀掉法力球（普通+3/精英+8/Boss+20）走近吸附；法力瓶商店可拿、宝箱可掉；
7. 宝箱 50/50 掉本职业武器/法力瓶，旧场景无职业退回治疗球；
8. 木桩实测六武器 DPS ≈ 第六节表 ±20%；
9. 敌人侧近战回归无损（EnemyCombat 链路未动）；v0_4/v0_5 旧场景无武器时默认近战行为不变。

## 八、风险与备注

- **AttackData 运行时副本**：仅近战装备武器创建，卸下销毁；setter 只作用于副本（代码注释明示），磁盘资产不受污染；
- **WeaponHitbox 宽度实时读**：判定逻辑零改动，仅宽度来源从 Awake 缓存改实时读 WeaponController（蓄力宽度需要）；敌人侧无 WeaponController 宽度倍率调用，行为不变；
- **Default 层撞墙**：子弹对 Default 层非触发器一律视为墙销毁（门关闭=挡弹，开门=碰撞体禁用可穿）；owner 子物体排除防自伤；
- **旧场景兼容**：未装备武器时 PlayerCombat 走原有序列化 AttackData 近战路径，v0_4/v0_5 场景零影响；
- **csproj**：新脚本统一补 Assembly-CSharp.csproj 条目（编辑器刷新后自动重生成）。
