# v1.1.31 房间塑形五职责架构重构（缺口实心墙根因修复）

## 根因（用户定位）

`shapeCarved`（角部挖除区）被 `ConvertToWall` 逐格**填成实心墙**——缺口起于房间角部必然连接外墙，
长成截图中的大片实心墙块。正确语义应为"**挖空内部，只画交界处的一格轮廓墙**"。
次要问题：模板约束靠作者保证无运行时验证；变体维度少（仅 25% 镜像）随机感不足。

## 新架构（五职责，v1.1.31）

| 文件 | 职责 |
|---|---|
| `RoomShapeGenerator` | 只决定不可行走格：角部凹口（L/双角）/四角斜切；保护通道（门→中心宽3+中心5×5）内禁挖；挖除 ≤30% |
| `RoomBoundaryBuilder` | **根因修复核心**：Carved ∩ 邻接 Walkable = 一格轮廓墙；深层挖除格保持虚空（不铺地板不填墙） |
| `RoomObstaclePlanner` | 模板骨架+受约束随机变体：7 模板随机 → 旋转 0/90/180/270 → 水平/垂直镜像 → 每段 25% 省略 → 段长 −1/0/+1 → 安全区偏移 ±2 → 20% 空房；落位硬约束：内部边界 ≥2（零贴外墙）、可行走格、保护通道禁入、距房心 ≥3 |
| `RoomLayoutValidator` | BFS 复核：门锚点可达房心 / 可行走 ≥65% / 障碍块 ≤6 格 / 障碍零贴墙；未做全局瓶颈检测（门通道宽3+连通性已覆盖主险，验证失败重试兜底，真实堵死案例再补） |
| `RoomPlan` + `RoomPlanner` | 数据载体 + 编排：失败用**同一房间 RNG 续流重试 ≤10 次** → 降级（保挖除去障碍再验）→ 整房保底。原则：随机提候选，规则拒绝坏方案 |

`DungeonBuilder` 只消费：Walkable→地板/地面格，Outline+Obstacles→墙瓦，深层挖除→不绘制；
`RoomShaper` 删除；`ConvertToWall` 退役（groundCells 只登记可行走格，无需事后移除）。

## 离线仿真（300 次/尺寸）

轮廓墙均值 6~8 格（仅交界，最大 30）；深层虚空格不再成墙；可行走占比 93% ≥65% 红线。

## 验收

Reload → Play：房间角部缺口为**虚空+一圈薄墙轮廓**（不再有实心墙块）；房内墙段形态多样
（旋转/镜像/长短/省略/偏移变化）；所有门可走通、无堵死房；同 seed 复现不变（RNG 续流确定）。

## 关联

v1.1.22~23 旧塑形体系退役（历史在 git 与版本文档）；v1.1.28 一块厚外墙体/门洞/保护格集不变。
