# v1.1.15 石槽音量滑条（五态滑钮 + 黄条填充）

## 素材（源：狼人\音量滑槽与滑钮；双份入库 Assets/UI/PauseMenu + Resources/UI/PauseMenu）

| 资产 | 规格 | 用途 |
|---|---|---|
| slider_knob_default / hover / pressed / selected / disabled | 182×182 | 五态滑钮；**hover 图自带外围光晕**（与 default 差异实测为环形光晕区） |
| slider_track_empty | 1206×147（8.2:1） | 轨道背景（始终显示全可调范围） |
| slider_fill（用户贴图） | 554×21 | 黄条填充，随滑钮实时增减 |

导入规格：Single/Bilinear/不压缩/PPU1（v1.1.10 规范）。track_filled/track_ticks 为视觉参考未接线（需要刻度层时再入）。

## 尺寸适配（等比零失真）

- 轨道显示 **260×32**（显示比例 8.1 ≈ 原生 8.2，Simple 等比缩放无失真）；
- 滑钮 **40×40** = 轨道显示高 × 原生配比 1.24（182/147，美术成对设计的尺寸关系）；
- 黄条高 **16px** 居中于轨道内槽（原生内槽约占轨道高 50%），宽度由 Slider.fillRect 随值驱动；
- 滑钮区左右各留 20（半钮宽），钮心覆盖 [20,240]。

## 实现

| 文件 | 说明 |
|---|---|
| `UI/SliderSprites.cs` | 素材加载器（Resources 缓存式，断链 TrackEmpty=null） |
| `UI/SliderHandleState.cs` | 滑钮指针状态机：Slider 的 Handle 不是独立 Selectable（无 SpriteState），用 IPointerEnter/Exit/Down/Up 换图——**按下 > 悬停(光晕) > 默认**；生命周期回调无需配对退订 |
| `PausePanel.CreateSlider` | 视觉拆分 `BuildArtSlider`（石槽路径）/`BuildPlainSlider`（断链兜底旧纯色观感）；Slider.transition=None 防与换图叠色；数值逻辑（PlayerPrefs + AudioManager 即时生效）零改动 |

## 验收

1. 编译无错 → Esc → 设置 → 音频：两条滑条为石槽 + 石滑钮 + 黄条；
2. 悬停滑钮出现光晕、按下压暗；拖动/点轨道，黄条左右缘始终贴滑钮；
3. 音量即时生效且重开保持（PlayerPrefs）；素材删除时回退纯色滑条不报错。

## 备注

- selected/disabled 滑钮图已入库（SliderHandleState 暂只用三态；做键盘导航/禁用态时在 Apply() 加分支即可）。
- 参考文档建议的 AudioMixer 对数响度（20·log10）属音频手感调校，未在本轮（现行线性 AudioSource.volume 链路不变），需要时单独立项。
