# v0.4.5.2 Enemy 攻击系统统一修复计划（Final2）

> 版本：v0.4.5.2 EnemyAttackSystem Fix Final2
>
> 目标：`EnemyCombat` 完全接管攻击状态机与武器朝向管理，`EnemyAI` 只负责高层决策，且不再长期持有 `WeaponController`。

---

## 一、当前问题

`PlayerCombat` 与 `EnemyCombat` 职责不一致：

- `PlayerCombat`：完整攻击状态机（Windup / Active / Recovery）+ 命中判定 + 武器控制。
- `EnemyCombat`：仅冷却 + 简单伤害调用，不控制武器动画。
- `EnemyAI`：同时承担 AI 决策与攻击子阶段状态机。

导致 Player 与 Enemy 攻击流程无法复用，Enemy 缺少动画、预警与 Active 同步。

---

## 二、最终目标架构

```
PlayerAimController
        │
        ▼
PlayerCombat ─────────────── 攻击状态机 + 命中判定 + 武器控制
        │
        ▼
WeaponController → WeaponAnimator → AttackIndicator


EnemyAI
   │
   │  仅传递目标 + 判断攻击条件
   ▼
EnemyCombat ──────────────── 攻击状态机 + 命中判定 + 武器控制
   │
   ▼
WeaponController → WeaponAnimator → AttackIndicator
```

**关键约定：**

- `PlayerCombat` 与 `EnemyCombat` 处于同一层级。
- 两者都负责：
  - 攻击状态机（Windup / Active / Recovery）
  - 武器朝向管理（普通状态）
  - 攻击方向锁定（攻击状态）
  - 调用 `WeaponAnimator` / `AttackIndicator`
  - 执行命中判定
- `EnemyAI` 只负责：
  - Idle / Chase / ReturnToPatrol 高层状态
  - 发现玩家并维护 `target` 引用
  - 向 `EnemyCombat` 传递目标：`combat.SetTarget(target)`
  - 判断攻击条件：`combat.IsInAttackRange(target) && combat.CanAttack`
  - 触发攻击：`combat.TryAttack(target)`
- `EnemyAI` **不持有** `WeaponController` / `WeaponAnimator` / `AttackIndicator`。

---

## 三、脚本职责表

| 脚本 | 负责 | 不负责 |
|---|---|---|
| **PlayerAimController** | 鼠标/手柄瞄准输入，输出 `AimDirection` | 攻击状态、伤害 |
| **PlayerCombat** | 玩家攻击状态机、命中判定、武器控制 | AI、移动 |
| **EnemyAI** | 高层 AI、目标传递、攻击条件判断 | 武器控制、攻击子阶段、伤害判定 |
| **EnemyCombat** | 敌人攻击状态机、普通状态武器朝向、命中判定、武器控制 | AI 决策、移动 |
| **WeaponController** | 武器朝向、方向锁定、角度计算、Sprite 视觉 | Tween、状态机、伤害 |
| **WeaponAnimator** | 攻击期间 Tween 播放 | 攻击逻辑、方向、Sprite |
| **AttackIndicator** | 攻击范围预警显示 | 状态机、伤害 |

---

## 四、修改文件列表

1. **`Assets/Scripts/v0_4/EnemyCombat.cs`**
   - 新增完整攻击状态机。
   - 新增 `WeaponController` / `WeaponAnimator` / `AttackIndicator` 引用。
   - 新增 `SetTarget(Transform target)` 接口。
   - 新增普通状态自动朝向目标逻辑。
   - 新增 `TryAttack(Transform target)` 入口。
   - 新增 `PerformFanAttack()` 判定方法。

2. **`Assets/Scripts/v0_4/EnemyAI.cs`**
   - 删除内部 `AttackSubPhase`（Windup / Active / Recovery）。
   - 删除对 `WeaponController` / `WeaponAnimator` / `AttackIndicator` 的直接引用和调用。
   - 保留高层状态切换与攻击条件判断。
   - 在 `UpdateChase` 中调用 `combat.SetTarget(target)`。
   - 攻击触发改为 `combat.TryAttack(target)`。
   - 丢失目标或切换非 Chase 状态时，调用 `combat.SetTarget(null)`。

3. **`Assets/Prefabs/Enemy.prefab`**
   - `EnemyCombat` 组件新增引用：
     - `weaponController` → `WeaponController`
     - `weaponAnimator` → `WeaponAnimator`
     - `attackIndicator` → `AttackIndicator`
     - `weaponPivot` → `WeaponPivot`（如需要）
   - `EnemyAI` 组件移除 `weaponController` / `weaponAnimator` 引用。

---

## 五、不修改文件

以下文件保持 Final3 / 当前版本不变：

- `AttackData.cs`
- `PlayerAimController.cs`
- `WeaponController.cs`
- `WeaponAnimator.cs`
- `PlayerCombat.cs`（明确不重构）
- `Player.prefab`
- `PlayerAttackData.asset`
- `EnemyMeleeAttackData.asset`

---

## 六、完整执行步骤

### Step 1：Prefab 结构检查

**操作：**

打开 `Assets/Prefabs/Enemy.prefab`，确认层级：

```
Enemy
 ├ HealthBarAnchor
 ├ AttackIndicator
 ├ WeaponPivot
 │   └ WeaponSprite
 └ （自身组件）
```

确认 `Enemy` 根节点上拥有以下组件：

- `EnemyController`
- `EnemyStats`
- `EnemyHealth`
- `EnemyCombat`
- `EnemyAI`
- `WorldSpaceHealthBar`
- `WeaponController`（新增或已存在）
- `WeaponAnimator`（新增或已存在）

确认 `WeaponPivot`：

- `localPosition = (0, 0, 0)`
- 位于 `Enemy` 中心。

确认 `WeaponSprite`：

- `localRotation = (0, 0, -90)`
- 是 `WeaponPivot` 的子物体。

如果结构不符，先调整 Prefab 层级，再执行后续步骤。

---

### Step 2：修改 `EnemyCombat.cs`

**修改文件：** `Assets/Scripts/v0_4/EnemyCombat.cs`

#### 2.1 新增引用字段

```csharp
[Header("攻击配置")]
[SerializeField] private AttackData attackData;

[Header("组件引用")]
[SerializeField] private WeaponController weaponController;
[SerializeField] private WeaponAnimator weaponAnimator;
[SerializeField] private AttackIndicator attackIndicator;
```

#### 2.2 新增状态字段

```csharp
private enum AttackSubPhase { None, Windup, Active, Recovery }
private AttackSubPhase attackSubPhase = AttackSubPhase.None;

private float windupTimer;
private float activeTimer;
private float recoveryTimer;
private bool activeMomentTriggered;
private Transform currentTarget;
```

#### 2.3 Awake 中自动获取组件

```csharp
void Awake()
{
    if (attackData == null)
        Debug.LogWarning("[EnemyCombat] attackData is null", this);

    if (weaponController == null)
        weaponController = GetComponent<WeaponController>();

    if (weaponAnimator == null)
        weaponAnimator = GetComponentInChildren<WeaponAnimator>(true);

    if (attackIndicator == null)
        attackIndicator = GetComponentInChildren<AttackIndicator>(true);
}
```

#### 2.4 新增目标设置接口

```csharp
/// <summary>
/// 设置当前目标。EnemyAI 每帧调用，用于普通状态下武器朝向。
/// </summary>
public void SetTarget(Transform target)
{
    currentTarget = target;
}
```

#### 2.5 新增 Update 驱动

```csharp
void Update()
{
    UpdateAiming();
    UpdateAttackState();
}

/// <summary>
/// 普通状态下持续朝向目标。
/// 攻击期间方向已锁定，WeaponController 会自动忽略。
/// </summary>
private void UpdateAiming()
{
    if (attackSubPhase != AttackSubPhase.None) return;
    if (currentTarget == null) return;
    if (weaponController == null) return;

    Vector2 dir = (currentTarget.position - transform.position).normalized;
    if (dir.sqrMagnitude < 0.0001f) return;

    weaponController.SetAimDirection(dir);
}

private void UpdateAttackState()
{
    switch (attackSubPhase)
    {
        case AttackSubPhase.Windup:
            UpdateWindup();
            break;
        case AttackSubPhase.Active:
            UpdateActive();
            break;
        case AttackSubPhase.Recovery:
            UpdateRecovery();
            break;
    }
}
```

#### 2.6 修改 TryAttack 入口

```csharp
public bool TryAttack(Transform target)
{
    if (attackSubPhase != AttackSubPhase.None) return false;
    if (!CanAttack) return false;
    if (attackData == null) return false;
    if (target == null) return false;

    currentTarget = target;
    StartWindup();
    return true;
}
```

#### 2.7 新增 Windup / Active / Recovery 流程

```csharp
private void StartWindup()
{
    attackSubPhase = AttackSubPhase.Windup;
    windupTimer = attackData.WindupTime;
    activeMomentTriggered = false;

    weaponController?.LockAttackDirection();

    if (attackIndicator != null)
    {
        attackIndicator.SetRadius(attackData.AttackRange);
        attackIndicator.SetColor(attackIndicator.WarningColor);
        attackIndicator.Show();
    }
}

private void UpdateWindup()
{
    windupTimer -= Time.deltaTime;
    if (windupTimer <= 0f)
        EnterActive();
}

private void EnterActive()
{
    attackSubPhase = AttackSubPhase.Active;
    activeTimer = attackData.ActiveDuration;
    activeMomentTriggered = false;

    if (weaponAnimator != null && weaponController != null)
    {
        weaponAnimator.Play(
            weaponController.GetAttackStartAngle(),
            weaponController.GetAttackEndAngle(),
            attackData.ActiveDuration,
            attackData.AttackEase,
            weaponController.GetAttackRotateMode(),
            attackData.ActiveMomentRatio,
            OnActiveMoment
        );
    }
    else
    {
        OnActiveMoment();
    }
}

private void UpdateActive()
{
    activeTimer -= Time.deltaTime;
    if (activeTimer <= 0f)
        EnterRecovery();
}

private void OnActiveMoment()
{
    if (activeMomentTriggered) return;
    activeMomentTriggered = true;

    attackIndicator?.Hide();
    PerformFanAttack();
}

private void EnterRecovery()
{
    attackSubPhase = AttackSubPhase.Recovery;
    recoveryTimer = attackData.RecoveryTime;
}

private void UpdateRecovery()
{
    recoveryTimer -= Time.deltaTime;
    if (recoveryTimer <= 0f)
    {
        EndAttack();
    }
}

private void EndAttack()
{
    attackSubPhase = AttackSubPhase.None;
    weaponAnimator?.Stop();
    weaponController?.UnlockAttackDirection();
    RecordAttackTime();
}
```

#### 2.8 新增扇形判定

```csharp
private void PerformFanAttack()
{
    if (attackData == null) return;

    Collider2D[] hits = Physics2D.OverlapCircleAll(
        transform.position, attackData.AttackRange, attackData.TargetLayer);

    Vector2 facingDir = weaponController != null
        ? weaponController.GetAimDirection()
        : transform.right;
    float halfAngle = attackData.AttackAngle * 0.5f;

    foreach (Collider2D hit in hits)
    {
        if (hit == null) continue;
        if (hit.isTrigger) continue;
        if (hit.gameObject == gameObject) continue;

        Vector2 closestPoint = hit.ClosestPoint(transform.position);
        Vector2 toPoint = closestPoint - (Vector2)transform.position;
        if (toPoint.magnitude > attackData.AttackRange) continue;

        float angle = Vector2.Angle(facingDir, toPoint.normalized);
        if (angle <= halfAngle)
        {
            if (hit.TryGetComponent<IDamageable>(out var damageable))
            {
                damageable.TakeDamage(attackData.AttackDamage);
            }
        }
    }
}
```

#### 2.9 保留并整理原有冷却逻辑

保留：

- `lastAttackTime`
- `CanAttack`
- `RecordAttackTime()`
- `IsInAttackRange(Transform target)`

删除或废弃：

- 旧的 `TryAttack(IDamageable target)` 逻辑（直接伤害）。

**目的：**

- 让 `EnemyCombat` 与 `PlayerCombat` 结构一致。
- `EnemyCombat` 不复制输入逻辑，方向只来自 `currentTarget`。

---

### Step 3：修改 `EnemyAI.cs`

**修改文件：** `Assets/Scripts/v0_4/EnemyAI.cs`

#### 3.1 删除字段

删除：

```csharp
private WeaponController weaponController;
private WeaponAnimator weaponAnimator;
private AttackSubPhase attackSubPhase;
private float windupTimer;
private float activeTimer;
private float recoveryTimer;
private bool activeMomentTriggered;
```

保留：

```csharp
private EnemyCombat combat;
private EnemyController controller;
private EnemyStats stats;
private EnemyHealth health;
private Transform target;
```

#### 3.2 修改 Awake

删除：

```csharp
weaponController = GetComponent<WeaponController>();
weaponAnimator = GetComponentInChildren<WeaponAnimator>(true);
```

保留：

```csharp
combat = GetComponent<EnemyCombat>();
controller = GetComponent<EnemyController>();
stats = GetComponent<EnemyStats>();
health = GetComponent<EnemyHealth>();
```

#### 3.3 修改 UpdateChase

```csharp
private void UpdateChase(float distToTarget)
{
    Vector2 dir = (target.position - transform.position).normalized;
    controller.MoveTowards(dir);
    controller.FaceTowards(dir);

    // 传递目标给 EnemyCombat，用于普通状态武器朝向
    combat.SetTarget(target);

    if (combat.IsInAttackRange(target) && combat.CanAttack)
    {
        combat.TryAttack(target);
    }
    else if (distToTarget > stats.LosePlayerRange)
    {
        combat.SetTarget(null);
        ChangeState(State.ReturnToPatrol);
    }
}
```

#### 3.4 简化 UpdateAttack

删除整个 `UpdateAttack` 子阶段逻辑。

进入 `Attack` 状态时，只需停止移动：

```csharp
case State.Attack:
    controller.StopMoving();
    break;
```

由于攻击流程已完全交给 `EnemyCombat`，`EnemyAI` 在 `Attack` 状态只需等待 `EnemyCombat` 完成，然后自动回到 Chase。

#### 3.5 修改 ChangeState

删除 Attack 状态进入/退出的复杂逻辑：

```csharp
case State.Attack:
    // 不需要在这里启动 Windup/Active/Recovery
    break;
```

```csharp
case State.Attack:
    // 不需要在这里停止动画或解锁方向
    break;
```

当从 Attack 切回 Chase 时，`EnemyCombat` 已经自行完成 Recovery 并解锁方向。

#### 3.6 处理 Patrol / ReturnToPatrol 状态

在切换到 Patrol 或 ReturnToPatrol 时，清除目标：

```csharp
case State.Patrol:
    combat.SetTarget(null);
    currentPatrolTarget = patrolOrigin;
    patrolWaitTimer = 0f;
    break;
```

```csharp
case State.ReturnToPatrol:
    combat.SetTarget(null);
    break;
```

**目的：**

- `EnemyAI` 只负责高层 AI 决策与目标传递。
- 不直接控制武器动画或方向。

---

### Step 4：调整 `Enemy.prefab`

**操作：**

1. `EnemyCombat` 组件新增/确认引用：
   - `attackData` → `EnemyMeleeAttackData.asset`
   - `weaponController` → `WeaponController`
   - `weaponAnimator` → `WeaponAnimator`
   - `attackIndicator` → `AttackIndicator`

2. `EnemyAI` 组件：
   - 移除 `weaponController` 引用（如果存在）。
   - 移除 `weaponAnimator` 引用（如果存在）。
   - 保留 `combat` 引用。

3. 确认 `WeaponPivot` 层级正确：
   ```
   Enemy
    └ WeaponPivot
         └ WeaponSprite
   ```

---

### Step 5：编译验证

**操作：**

- `dotnet build Assembly-CSharp.csproj`
- 目标：`0 警告，0 错误`

---

## 七、PlayerCombat / EnemyCombat 同层级对比

### PlayerCombat

```csharp
// 输入来源
PlayerAimController aimController

// 攻击流程
UpdateAiming()
TryAttack()
StartWindup() / EnterActive() / EnterRecovery()
PerformFanAttack()

// 武器控制
WeaponController weaponController
WeaponAnimator weaponAnimator
AttackIndicator attackIndicator
```

### EnemyCombat

```csharp
// 目标来源
Transform currentTarget（由 EnemyAI 通过 SetTarget 传入）

// 攻击流程
SetTarget(Transform target)
UpdateAiming()
TryAttack(Transform target)
StartWindup() / EnterActive() / EnterRecovery()
PerformFanAttack()

// 武器控制
WeaponController weaponController
WeaponAnimator weaponAnimator
AttackIndicator attackIndicator
```

**核心差异：**

- Player 方向来自 `PlayerAimController.AimDirection`。
- Enemy 方向来自 `currentTarget.position - transform.position`。
- 其余攻击流程完全一致。

---

## 八、验证标准

### Player（保持 Final3 行为，不修改）

- [ ] 待机时武器跟随鼠标。
- [ ] 攻击时以鼠标方向为中心挥动 120°。
- [ ] 攻击结束恢复跟随鼠标。

### Enemy

- [ ] **Windup**：进入攻击范围后，`AttackIndicator` 显示范围预警。
- [ ] **Active**：`WeaponAnimator` 播放攻击动画（根据 `AnimationType`）。
- [ ] **ActiveMoment**：对扇形/圆形范围内的目标造成伤害。
- [ ] **Recovery**：动画停止，方向解锁，恢复朝向玩家。
- [ ] 丢失目标后，武器停止追踪玩家。

### 架构

- [ ] `EnemyAI` 不持有 `WeaponController` / `WeaponAnimator`。
- [ ] `EnemyCombat` 持有 `WeaponController` / `WeaponAnimator` / `AttackIndicator`。
- [ ] `EnemyCombat` 普通状态自动朝向 `currentTarget`。
- [ ] `PlayerCombat` 未被修改。
- [ ] 编译 0 警告 0 错误。

---

## 九、Input System 问题列表（本次不修复）

以下问题单独记录，不在本次任务中处理：

1. 项目中仍存在使用 `UnityEngine.Input` 的代码（如 `PlayerAimController` 当前使用 `Input.mousePosition`）。
2. 项目已启用新 Input System，但部分代码未迁移。
3. 需要统一使用 `UnityEngine.InputSystem` 的 `Mouse.current.position.ReadValue()` 或 `PlayerInput` 组件。
4. 涉及文件可能包括：
   - `PlayerAimController.cs`
   - `PlayerController.cs`
   - 其它使用 `Input.GetKey` / `Input.GetAxis` 的脚本

建议作为独立任务处理。

---

## 十、待确认

1. 是否确认按本计划执行代码修改？
2. `EnemyMeleeAttackData.asset` 的 `windupTime` / `activeDuration` / `recoveryTime` 是否需要调整？（当前 0.25 / 0.25 / 0.35）
