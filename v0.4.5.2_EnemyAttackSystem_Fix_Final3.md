# v0.4.5.2 Fix Final3 — 攻击系统架构修复计划

> 版本：v0.4.5.2 Fix Final3
>
> 目标：修复 Final3 后攻击系统架构问题，统一 Player 与 Enemy 攻击流程，使 WeaponAnimator 直接读取 AttackData 实现动画与判定同步。

---

## 一、当前问题

1. `EnemyAI` 仍包含攻击状态逻辑（Windup / Active / Recovery）。
2. `EnemyCombat` 只负责伤害，没有完整攻击流程。
3. `WeaponAnimator` / `WeaponController` 已独立，但调用链不统一。
4. Player 与 Enemy 攻击流程不一致。
5. Input System 报错尚未处理（`UnityEngine.Input` 与 New Input System 冲突）。
6. `AttackIndicator` 只支持视觉显示，没有与攻击动画方向同步。

---

## 二、最终目标架构

### Player

```
PlayerController
        ↓
PlayerCombat
        ↓
WeaponController
        ↓
WeaponAnimator
        ↓
AttackIndicator
```

### Enemy

```
EnemyAI
        ↓
EnemyCombat
        ↓
WeaponController
        ↓
WeaponAnimator
        ↓
AttackIndicator
```

---

## 三、职责划分

| 脚本 | 负责 | 不负责 |
|---|---|---|
| **EnemyAI** | 寻找目标、移动、判断距离、决定何时攻击 | 武器动画、攻击阶段、命中检测、伤害 |
| **EnemyCombat** | Windup / Active / Recovery、播放攻击动画、命中检测、调用伤害 | AI 决策、移动 |
| **PlayerCombat** | 玩家攻击状态机、命中检测、调用 WeaponController/WeaponAnimator/AttackIndicator | AI、移动、输入读取 |
| **WeaponController** | 武器方向、WeaponPivot 控制、WeaponSprite 视觉、方向锁定 | 动画曲线、Tween、伤害 |
| **WeaponAnimator** | DOTween 动画播放（Arc / FullCircle / Spin / Thrust） | 攻击逻辑、伤害、目标判断 |
| **AttackIndicator** | 攻击范围显示、攻击方向显示 | 状态机、伤害 |

---

## 四、Step 0：修复 Input System 报错

**优先级：最高，先执行。**

### 错误

```
InvalidOperationException:
You are trying to read Input using UnityEngine.Input
but active Input handling is Input System
```

### 原因

项目已启用 New Input System，但部分代码仍在使用 `UnityEngine.Input`。

### 修改范围

全局搜索以下旧 API 并替换：

| 旧 API | 新 API |
|---|---|
| `Input.mousePosition` | `Mouse.current.position.ReadValue()` |
| `Input.GetKey(KeyCode.X)` | `Keyboard.current.xKey.isPressed` |
| `Input.GetKeyDown(KeyCode.X)` | `Keyboard.current.xKey.wasPressedThisFrame` |
| `Input.GetAxis("Horizontal")` | 使用 `PlayerInput` 组件或 `Keyboard` 直接读取 |

### 需要检查的文件

- `Assets/Scripts/v0_4/PlayerAimController.cs`
- `Assets/Scripts/v0_3/PlayerController.cs`
- `Assets/Scripts/v0_x/CameraController.cs`（如存在）
- 任何其它使用 `UnityEngine.Input` 的脚本

### 验证

- 重新编译后无 `UnityEngine.Input` 相关报错。
- 玩家可以通过鼠标正常瞄准。

---

## 五、Step 1：修复 EnemyCombat 架构

**修改文件：** `Assets/Scripts/v0_4/EnemyCombat.cs`

### 当前问题

`EnemyAI` 包含 Windup / Active / Recovery / 播放动画 / 控制 AttackIndicator。

### 修改

1. **删除 `EnemyAI` 中所有攻击状态代码。**
2. `EnemyAI` 只保留：
   - `UpdateChase()`
   - `UpdateAttackDecision()`
   - `FaceTarget()`
   - `MoveTowards()`
3. `EnemyAI` 新增调用：
   ```csharp
   enemyCombat.TryStartAttack(player);
   ```
4. `EnemyAI` **禁止**：
   - 播放动画
   - 调用 `WeaponAnimator`
   - 控制攻击阶段
   - 直接调用伤害

---

## 六、Step 2：重写 EnemyCombat

**修改文件：** `Assets/Scripts/v0_4/EnemyCombat.cs`

### 新增状态机

```csharp
private enum AttackState { None, Windup, Active, Recovery }
private AttackState currentState = AttackState.None;
private float timer;
```

### 新增字段

```csharp
[Header("攻击配置")]
[SerializeField] private AttackData attackData;

[Header("组件引用")]
[SerializeField] private WeaponController weaponController;
[SerializeField] private WeaponAnimator weaponAnimator;
[SerializeField] private AttackIndicator attackIndicator;
```

### 攻击流程

```
TryStartAttack()
    ↓
进入 Windup
    ↓
显示 AttackIndicator
    ↓
Windup 结束
    ↓
进入 Active
    ↓
weaponController.LockAttackDirection();
weaponAnimator.PlayAttack(attackData, OnAttackActive);
    ↓
Active 时间点
    ↓
OnAttackActive()
    ↓
PerformAttack()
    ↓
Recovery 结束
    ↓
weaponController.UnlockAttackDirection();
weaponAnimator.Stop();
```

### 接口

```csharp
public bool TryStartAttack(Transform target)
{
    if (currentState != AttackState.None) return false;
    if (attackData == null) return false;
    if (!CanAttack) return false;

    currentTarget = target;
    EnterWindup();
    return true;
}
```

---

## 七、Step 3：统一 WeaponAnimator

**修改文件：** `Assets/Scripts/v0_4/WeaponAnimator.cs`

### 当前问题

`WeaponAnimator` 不知道 `AttackData`，导致 Arc 角度、FullCircle 旋转、动画与判定不同步。

### 修改

`WeaponAnimator` 改为直接接收 `AttackData`：

```csharp
public void PlayAttack(AttackData data, Action onHit)
```

内部读取：

- `data.AnimationType`
- `data.AttackAngle`
- `data.ActiveDuration`
- `data.AttackEase`
- `data.ActiveMomentRatio`

### 动画实现

#### Arc

- 攻击角 120°：从 `-60°` 到 `+60°`
- 使用 `RotateMode.Fast`
- 禁止 `FastBeyond360`

#### FullCircle

- 360° 旋转
- 使用 `RotateMode.FastBeyond360`

#### Spin

- 720° 旋转
- 使用 `RotateMode.FastBeyond360`

### 命中回调

在 `ActiveMomentRatio` 处调用 `onHit?.Invoke()`。

---

## 八、Step 4：修复 WeaponController

**修改文件：** `Assets/Scripts/v0_4/WeaponController.cs`

### 当前问题

攻击范围和武器长度一致，但攻击方向锁定混乱。

### 修改

普通状态：

```csharp
SetAimDirection(Vector2 direction)
```

攻击开始：

```csharp
LockAttackDirection()
```

攻击结束：

```csharp
UnlockAttackDirection()
```

攻击期间：

- 禁止 `ApplyAimRotation()`
- 普通状态的 `SetAimDirection` 在锁定后自动忽略

---

## 九、Step 5：修复攻击范围与动画同步

### 统一规则

- `AttackRange`：决定武器长度、攻击检测半径。
- `AttackAngle`：决定动画角度、扇形检测角度。

### Player

- `AttackRange = 1.8`
- `AttackAngle = 120`
- 动画：`-60° → +60°`
- 检测：120° 扇形

### Enemy

- `AttackRange = 1.5`
- `AttackAngle = 360`
- 动画：旋转 360°
- 检测：圆形

---

## 十、Step 6：修复 Enemy 攻击范围不显示

**修改文件：** `Assets/Prefabs/Enemy.prefab`

### 结构检查

```
Enemy
├── WeaponPivot
│   └── WeaponSprite
└── AttackIndicator
```

### AttackIndicator

- 默认 `SetActive(false)` 或通过脚本初始隐藏。

### EnemyCombat 引用

```csharp
[SerializeField] private AttackIndicator attackIndicator;
```

### 攻击开始

```csharp
attackIndicator.SetRadius(attackData.AttackRange);
attackIndicator.Show();
```

### 攻击结束

```csharp
attackIndicator.Hide();
```

---

## 十一、Step 7：Prefab 检查

### Player Prefab

```
Player
├── WeaponPivot
│   └── WeaponSprite
```

- `WeaponPivot` 位置：`(0, 0, 0)`
- `WeaponSprite` 的 `localPosition` / `localScale` 由 `WeaponController` 控制。

### Enemy Prefab

结构同 Player。

禁止 `WeaponSprite` 直接挂在角色根节点。

---

## 十二、Step 8：验证目标

### Player

- [ ] 鼠标移动时武器跟随方向。
- [ ] 攻击时 120° 扇形挥舞。
- [ ] 动画范围 = 攻击范围。

### Enemy

- [ ] AI 追踪玩家。
- [ ] 进入范围后显示红色攻击区域。
- [ ] 攻击时 `WeaponSprite` 旋转。
- [ ] 命中时造成伤害。

### 代码结构

- [ ] `EnemyAI` 只负责 AI。
- [ ] `EnemyCombat` 只负责攻击。
- [ ] `WeaponAnimator` 只负责动画。
- [ ] `WeaponController` 只负责武器。
- [ ] `AttackIndicator` 只负责显示。

---

## 十三、不允许修改的内容

禁止修改：

- `PlayerController` 输入结构
- Health 系统
- Damage 系统
- Stats 系统
- AI 移动逻辑

只允许修改：

- 攻击相关代码。

---

## 十四、最终目标

完成 Fix Final3 后：

- Player 与 Enemy 使用同一套攻击框架。
- 攻击范围、动画范围、武器长度完全同步。
- v0.5 技能系统只需新增 `AttackData`。

---

## 十五、待确认

1. 是否确认按本计划执行？
2. 是否先执行 Step 0（Input System 修复）？
3. `PlayerAttackData.asset` 的 `AttackRange` 是否从 1.5 改为 1.8？
4. `WeaponAnimator` 直接读取 `AttackData` 的设计是否与 Final3 中“WeaponAnimator 为纯 Tween 播放器”冲突？
