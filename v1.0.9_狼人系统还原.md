# v1.0.9 狼人系统还原：变身技能 + 等比例缩放 + 即时换形

> 日期：2026-08-31  
> 状态：已落盘；编译 0/0；门禁 9/9；Debug 菜单 Apply Werewolf/Toggle Beast 执行零错误（视觉终验留给人工，编辑器被实时占用）。

## 一、四个问题的处置

### 1. 选完角色没立刻换形
`CharacterSelectUI.Confirm` 现在**即时应用**：狼人 → `SetWerewolfVisual(true)` + `WerewolfTransformation.EnsureOn`；改回战士 → `SetWerewolfVisual(false)`（销毁狼人视觉/组、恢复战士渲染器）+ 销毁变身组件（OnDestroy 复位判定缩放/数值/血条）。不再等下一次场景加载。

### 2. 狼人变形（根因）+ 等比例修正
根因：v1.0.6 的标定给方形 720px 帧强设了 **0.6 宽高比**（当时基于"玩家根缩放非均匀"的错误前提——正则跨块误读，真实根缩放是均匀 0.362）→ 狼被横向压扁。
修正：**等比例**——两轴同一世界尺寸系数 k=目标高÷7.2，目标高=战士视觉高×1.95≈1.8u（对齐旧版 wolfScale 0.25 的体量），子缩放按 lossyScale 反解，根缩放均匀与否都保持素材原生宽高比。

### 3. 过门问题（排查结论）
角色选择**不碰碰撞体**：WerewolfVisual 是纯 SpriteRenderer 子物体（无 Collider），玩家胶囊（世界尺寸 1.32×1.59）在战士/狼人/兽化下完全相同；门是否可通过与此无关。此前"过不去"的主观感受大概率来自压扁+比例失调的视觉。等比例修正后请重测；若仍卡门，看该门是否为需清房开启的 Door（拍摄门位置反馈）。

### 4. 狼人全部逻辑还原（自 v0.6.8 移植到 v0.7.5 架构）
新 `Player/WerewolfTransformation.cs`（角色=狼人时挂载，**T = 狼↔兽化**，直接轮询 Keyboard.tKey 不动输入资产）：
- **变身演出**：Transform_L/R 帧（4fps，已迁 Resources）覆盖播放 + 视觉逐段膨胀 ×1→1.5（v0.6.5 原逻辑）；演完切兽化帧组并缩放复位——Beast 帧 1080px 画布自带大体型（≈普通狼 1.5×，v0.6.8 原设计）。
- **兽化数值**（原值）：血量等比 ×1.5（`Health.ScaleMaxHealth` 已恢复，退兽化 1/N 还原）、伤害 ×1.3（乘进 `PlayerStats.Attack`）、攻速 ×1.1（乘进 `PlayerCombat.AttackSpeedMul`，与 Buff 叠乘）、移速 ×1.1（乘进 `PlayerStats.MoveSpeed`）。
- **狼形态常驻**：WeaponPivot ×1.5——判定长度/宽度、武器视觉、预警三者派生自 pivot.lossyScale **同源链**，一处放大三处同步（v0.6.7 原设计，不破坏 R7）。改回战士时复位。
- **血条锚点**：狼 1.0 / 兽化 1.45。
- 【美术缺失】BeastBurstFX 变身粒子未随合并保留，待重做接回。

FrameAnimator 扩展：beast_walk/idle_l·r + transform_l/r 组加载、`SetBeastForm`（切组+缩放复位）、`SetWerewolfVisualGrow`（演出膨胀）、`PlayTransform`（覆盖播放）、`LastHorizontalInput`（朝向记忆选 L/R 组）、`SetWerewolfVisual(false)` 还原路径。

## 二、验证

- 编译 0/0；门禁 9/9。
- Debug 菜单：`Tools/Dungeon/Debug Apply Werewolf Visual`（完整应用狼人+变身能力）、`Debug Toggle Beast`（等价按 T 的全链路）——均执行成功、零运行时错误。
- 待人工实测：①准备房选狼人立即变狼 ②T 变身演出与兽化体型/血条 ③兽化打怪伤害/攻速/移速体感 ④过门重测 ⑤改回战士即时还原。

## 三、键位回归

**T = 兽化变身**（仅狼人角色生效；选择页打开/死亡中不响应）。AGENTS/开发必读/SKILL 已同步。

## 四、修改文件

新 `Player/WerewolfTransformation.cs`；`FrameAnimator`（兽化/变身组+等比例+还原路径）、`PlayerStats`（兽化三乘数入 Attack/MoveSpeed）、`PlayerCombat`（AttackSpeedMul 乘数）、`Health`（ScaleMaxHealth 恢复）、`CharacterSelectUI`（即时换形）、`RunManager`/`PrepRoomManager`（挂变身组件+两个 Debug 菜单）；Beast/Transform 六目录帧迁入 Resources。
