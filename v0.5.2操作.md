# v0.5.2 随机内容生成 操作报告

> 本文档记录 v0.5.2 的实际执行内容与结果，对应《v0.5 Dungeon 系统开发计划书》五-C 节与《v0.5.2_正式计划》。
> 前置版本：v0.5.1（房间流程系统，见 `v0.5.1操作.md`）。
> **版本说明**：含原 v0.5.2.1「可破坏障碍物双方可摧毁」——2026-07-22 用户决策并入 v0.5.2，v0.5.2.1 版本号取消；三类箱子框架设计储备并入 `v0.5.2_正式计划.md` 第十章。

## 一、版本目标

敌人、障碍物、装饰全部由 **SpawnTable 配置表**随机生成；改内容只改 SO 资产，不改代码；同 seed 下地图与内容完全一致；可破坏障碍物**玩家和敌人武器均可摧毁**。

随机流：主 seed（`DungeonManager.ActiveSeed`）→ 房间子 seed = `ActiveSeed*7919 + roomId` → 该房间全部内容随机（种类/数量/位置）。

## 二、核心设计落实

| 设计 | 落实情况 |
|---|---|
| 数据驱动 | `SpawnTable`（prefab+权重+数量区间）/ `RoomContentProfile`（三表组合）SO，所有战斗房共用 `RoomContentProfile_Combat` |
| 三个静态 Spawner | `EnemySpawner`（生成即 `RegisterEnemy`，休眠+清房计数零改动接管）、`ObstacleSpawner`（一律不登记，非清房条件）、`DecorationSpawner`（随机 Z 旋转 + 非等比缩放抖动 0.7~1.4 + 颜色深浅抖动 0.85~1.15，均走房间子 seed 可复现） |
| 位置合法性（共用） | `SpawnPositionHelper.TryFind`：距墙 ≥1、距门洞中心 ≥2.5、`OverlapCircle(r=0.4, useTriggers=false)` 防重叠/防刷墙、20 次重试上限失败放弃 |
| 可破坏障碍物 | `ObstacleHealth`（实现 `IDamageable`，HP=刀数，每次命中固定 -1）+ `DestructibleObstacle`（受击闪白 0.15s、随血量颜色变深、破坏即销毁），均放 `now_use/` 非地牢专属 |
| 判定链接入（双方可摧毁） | 新建 `Obstacle` 层（index 7）；玩家 `PlayerAttackData.targetLayer` 64→**192**（Enemy+Obstacle）；敌人 `EnemyMeleeAttackData.targetLayer` 1→**129**（Default+Obstacle）；零新判定代码 |
| 生成时机 | `DungeonBuilder.Build` 末尾（门与 Room 就绪后）对 `ClearCondition==AllEnemiesDead` 的房间生成内容；Start 房不生成 |

## 三、新增文件

### 3.1 脚本

| 文件 | 说明 |
|---|---|
| `Assets/Scripts/v0_5/Content/SpawnTable.cs` | 权重表 SO + `RollCount`/`PickEntry` 抽取逻辑 |
| `Assets/Scripts/v0_5/Content/RoomContentProfile.cs` | 三表组合 SO |
| `Assets/Scripts/v0_5/Content/SpawnPositionHelper.cs` | 位置合法性工具（静态，三 Spawner 共用） |
| `Assets/Scripts/v0_5/Content/EnemySpawner.cs` | 敌人生成（静态） |
| `Assets/Scripts/v0_5/Content/ObstacleSpawner.cs` | 障碍物生成，`destructible` 条目注入 hp |
| `Assets/Scripts/v0_5/Content/DecorationSpawner.cs` | 装饰生成（静态，旋转+缩放+颜色抖动） |
| `Assets/Scripts/now_use/ObstacleHealth.cs` | 可破坏障碍物生命（IDamageable + 三事件） |
| `Assets/Scripts/now_use/DestructibleObstacle.cs` | 受击闪白/血量变深/销毁表现 |

### 3.2 Prefab（`Assets/Prefabs/Dungeon/`，WhiteSquare 白图染色）

- `Obstacle.prefab`：灰石块 (0.45,0.45,0.48)，layer=Obstacle，sortingOrder=2，BoxCollider2D size=1
- `Obstacle_Destructible.prefab`：浅棕木箱 (0.72,0.52,0.30)，同上 + `ObstacleHealth` + `DestructibleObstacle`
- `Decoration_Stain_A.prefab`：深棕污渍 (0.42,0.30,0.18,a=0.55)，scale(1.6,1.0)，无碰撞，sortingOrder=1
- `Decoration_Stain_B.prefab`：暗灰污渍 (0.30,0.27,0.24,a=0.50)，scale(0.9,0.7)，同上

### 3.3 SO 资产（`Assets/Data/Dungeon/`）

- `EnemyTable_Combat.asset`：Enemy.prefab ×1 权重，数量 2~4
- `ObstacleTable_Combat.asset`：Obstacle 权重 2 / Obstacle_Destructible 权重 1（destructible, hp=3），数量 1~3
- `DecorationTable_Combat.asset`：Stain_A / Stain_B 各权重 1，数量 1~3
- `RoomContentProfile_Combat.asset`：三表组合

## 四、修改文件

| 文件 | 改动 |
|---|---|
| `ProjectSettings/TagManager.asset` | layer 7 = `Obstacle` |
| `Assets/Data/PlayerAttackData.asset` | `targetLayer.m_Bits` 64 → 192（Enemy + Obstacle） |
| `Assets/Data/EnemyMeleeAttackData.asset` | `targetLayer.m_Bits` 1 → 129（Default + Obstacle），双方可摧毁 |
| `Assets/Scripts/v0_5/Runtime/Room.cs` | +`Doors` 只读访问器（2 行，位置规则读门洞中心用） |
| `Assets/Scripts/v0_5/Core/DungeonBuilder.cs` | +`defaultContentProfile` 字段；`Build` 加第三参 `layoutSeed`；+`SpawnContent()`（子 seed 派生 + 调三个 Spawner） |
| `Assets/Scripts/v0_5/Core/DungeonManager.cs` | **移除** v0.5.1 调试摆敌 4 字段与 `DebugPopulateEnemies()`；`Build` 调用传 `ActiveSeed` |
| `Assets/Scripts/now_use/WeaponHitbox.cs` | **根因修复**：自身跳过 `hit.transform.root == transform.root` → `hit.transform.IsChildOf(transform)`（见 5.1） |
| `Assets/Scripts/now_use/AttackIndicator.cs` | **潜伏 bug 修复**：`RestoreParent` 加守卫（见 5.2） |
| `Assets/Prefabs/Player.prefab` | `PlayerWorldStatusBar.canvasScale` 0.01→0.014（血条放大 1.4×，验收反馈） |
| `Assets/Scenes/v0_5_Dungeon.unity` | DungeonBuilder 接入 `RoomContentProfile_Combat`；`DungeonManager.seed` 暂存 **12345**（供同 seed 复验，可改回 0=随机） |

未改动：Player/Enemy/Combat/UI 其余现有脚本、物理碰撞矩阵、Enemy.prefab。

## 五、遇到的问题与修复

### 5.1 敌人武器打不到木箱（根因：WeaponHitbox 自身判定误伤）

**现象**：`EnemyMeleeAttackData.targetLayer` 已改为 129，敌人挥击命中木箱仍无伤害。

**根因**：v0.5.2 起敌人与障碍物同挂 `DungeonSystem/DungeonRoot/.../ContentRoot` 下，两者 `transform.root` 都是 `DungeonSystem`——`WeaponHitbox` 的自身跳过 `hit.transform.root == transform.root` 把木箱误判为「攻击者自身」。玩家无此问题（Player 是顶级对象）。

**修复**：改为 `hit.transform.IsChildOf(transform)`——只跳过攻击者自身及其子物体，语义不变、对层级免疫。实测：摆位敌人—木箱—玩家一线，敌人 Attack 状态连续挥击，3 刀打空木箱销毁，Console 0 Error。

### 5.2 敌人前摇中死亡报错（v0.4.5.2 潜伏 bug）

**现象**：Console 报 `Cannot set the parent of 'AttackIndicator' while activating or deactivating 'Enemy_1_1'`。

**根因**：敌人挥击前摇中预警扇形处于脱离父物体状态；此时敌人死亡销毁，`EnemyCombat.OnDisable → AttackIndicator.Hide → RestoreParent → SetParent` 被 Unity 拒绝。休眠制不禁用 GameObject 所以 v0.5.1 没触发，v0.5.2 大量杀敌场景暴露。

**修复**：`RestoreParent` 加守卫——父物体 `activeInHierarchy == false`（停用/销毁中）时放弃归位，指示器随父物体一起销毁。

### 5.3 障碍物碰撞体尺寸漂移（v0.5.1 Door 同类问题复发）

**现象**：REST 添加的 BoxCollider2D 默认 size=0.0001，导致障碍物不挡人、武器打不到、位置重叠检测穿透（首轮验收实测两障碍物中心距 0.47 重叠）。

**修复经过**：先手改 prefab YAML 为 size=1，但 Unity 未重导入外部修改直接进了 Play（内存中仍是旧值）→ 改用 `prefab_set_property` 经 Unity 自身写入并持久化。

**经验**：① REST `component_add` 的 BoxCollider2D 必须显式设 size；② 手改资产 YAML 后必须触发 reimport 才生效，资产修改优先走 REST；③ Play 模式中修改 SO 资产会在停止 Play 后回滚，资产改动必须在非 Play 状态确认。

### 5.4 装饰样式单调（验收反馈驱动）

**现象**：污渍只有 2 种固定轮廓，只随机旋转，"每次生成地图又是那几种形状"。

**修复**：`DecorationSpawner` 增加非等比缩放抖动（X/Y 各 ×0.7~1.4）+ 颜色深浅抖动（RGB ×0.85~1.15，alpha 不动），全部走房间子 seed——同 seed 复现性实测逐位一致。

### 5.5 装饰互相靠近 / 生成点放弃（接受与设计行为）

- 装饰**无碰撞体**（设计如此），OverlapCircle 看不见它们，规则 3 不约束装饰；半透明地面污渍视觉重叠可接受。
- 碰撞体生效后某房一个生成点 20 次重试未找到合法位置，按设计「失败放弃该个，不卡死」（内容总数 50→49）。

### 5.6 REST 验收伪影（v0.5.1 已记录同类）

速度注入后立刻读 Room.State 偶发 Unvisited（FixedUpdate 时序），等待后状态正确转为 Active；真实 WASD 走路无此问题。

## 六、验证清单

### 自动化验收（已通过 ✅；seed=12345，8 房 7 连接）

- [x] 编译 0 Error / 0 Warning（Play 全程 Console 干净）
- [x] 每个战斗房生成 2~4 敌人：实测 6 战斗房+Boss房 = 4/2/4/2/4/2/4，障碍 1~3、装饰 1~3 均在配置区间
- [x] Start 房无内容；Boss 房按战斗房生成（v0.5.3 才上 Boss 占位）
- [x] **同 seed 重进完全一致**：两次 Play 50 个内容物名称+坐标逐一比对零差异；装饰抖动结果逐位一致
- [x] 内容物距门最近 2.98 ≥ 2.5；实体内容物（敌人/障碍物）无重叠、无刷进墙
- [x] 休眠制无回归：未进房敌人 EnemyAI.enabled=False + 刚体 FreezeAll（可见不动）；隔壁房敌人透过门洞可见
- [x] 进房流程无回归：玩家进 Room_1 → State=Active → 三扇门碰撞体+色块同时关闭 → 敌人唤醒追击（截图 `Assets/Screenshots/v052_room_content.png`）
- [x] **敌人武器摧毁木箱**：摆位实测挥击穿箱 3 HP 打空销毁（5.1 修复后）
- [x] Validate 1000 Seeds 回归：失败 0/1000（生成逻辑未动）

### 人工验收（用户已通过 ✅，2026-07-22）

- [x] 障碍物挡移动；武器击毁木箱（闪白→变深→销毁→可通行）；不可破坏灰石块不受武器影响
- [x] **敌人和玩家武器均可摧毁木箱**（双方可摧毁）
- [x] 清房不计障碍物（房内木箱未拆也可开门）；杀光敌人开门无回归
- [x] 装饰抖动后轮廓/深浅有差异；玩家血条放大后观感（`v0521_playerbar.png`）
- [x] 敌人前摇中死亡不再报 AttackIndicator 错误

## 七、扩展预留（已为后续版本埋好的接口）

| 预留 | 用途 |
|---|---|
| `RoomContentProfile` 装配点（`Builder.SpawnContent`） | v0.5.3 按房间类型选 Profile，结构不变 |
| `ObstacleHealth.OnDestroyed` 事件 | 三类箱子 `DestructionEffect` 挂点（设计储备见 `v0.5.2_正式计划.md` 第十章） |
| SpawnTable.Entry（prefab+weight+destructible+hp） | 新敌人/障碍/装饰/箱型 = 做 prefab + 改表，零代码 |
| 三个 Spawner 收口全部 Instantiate | 未来对象池只改一处（计划书 9.3） |
| `Room.Doors` 只读访问器 | v0.5.3 宝箱/商店布局复用「避开门口」规则 |

## 八、后续工作

- **三类可破坏方块**（设计储备，`v0.5.2_正式计划.md` 第十章）：特殊箱（炸药=范围伤害/毒药=持续毒区）、物资箱（掉金币占位/治疗球）、普通箱（=现有木箱）。判定链前提已满足（双方 targetLayer 含 Obstacle），落地时实现 `DestructionEffect` SO 挂点等 7 个预留脚本 + prefab 变体。
- **v0.5.3 房间类型系统**（计划书五-D）：`RoomTypeAssigner`（Boss=最远、特殊房优先叶子）、`RoomTypeConfig`（着色/清房条件/Profile）、`Tilemap.SetColor` 地板着色、Interactable 基类 + 宝箱/祭坛/补给基座、Enemy_Elite / Enemy_Boss prefab 变体。
