# v0.7.3 操作记录

> 版本主题：消耗道具×3——血包（HP+4）/ 甲包（Armor+4）/ 魔力恢复包（Mana+40）（《v0.7_Demo玩法计划书.md》§4.3 + §九 v0.7.3 执行卡）。
> 执行文档：`v0.7.3_正式计划.md`。零场景/prefab/SO YAML 改动（全部运行时投放，编辑器可保持运行）。

## 改动文件清单

### 数据资产（Assets/Data/Item/）

- 新建 `Item_HealPack.asset`（HP +4，红色系占位色块）、`Item_ArmorPack.asset`（Armor +4，钢灰系）、`Item_ManaPack.asset`（Mana +40，蓝色系）。
- 删除 `Consumable_Test.asset`（v0.7.2 测试资产，按计划 §七收尾删除）。

### 修改脚本（4）

- `Item/ItemInventory.cs`：UseActive 前接 `ApplyEffect(data)`——HP → `Health.Heal`、Armor → `PlayerStats.ModifyArmor`、Mana → `PlayerStats.AddMana`；属性已满也照常消耗（简单优先，不拦截）；v0.7.2 遗留的"只扣数不结算"补齐。
- `Class/PrepRoomManager.cs`：
  - 测试投放 SpawnTestItems → **SpawnDemoItems()**：出生位前方横排三种正式消耗包各 1（替代 4 个测试药）；
  - 三包资产名清单**单点收口**为 `internal static ConsumableAssetNames` + `LoadRandomConsumable()`；`LoadConsumable(assetName)` 编辑器 AssetDatabase / 构建 Resources.Load（ClassCatalog 同模式），准备房间投放/宝箱/商店三处共用。
- `Dungeon/Interaction/ChestInteractable.cs`：宝箱奖励二权重扩**三权重**（序列化可调）——本职业随机武器 `weaponChance=0.4` / 法力瓶 `manaBottleChance=0.3` / 随机消耗包 `consumableChance=0.3`（余量归消耗包）；消耗包走 `ItemPickup.Spawn` + `PrepRoomManager.LoadRandomConsumable`（本地资产名清单删除，改引用公共收口）；消耗包资产缺失时退回法力瓶，不开空箱。
- `Dungeon/Content/InteractableSpawner.cs`：**商店 Row 陈列补充消耗包**——SpawnRow 收尾判定 `room.Type == RoomType.Shop`，在补给基座排下方 2 格追加第二排：三种消耗包各 1 个（`ItemPickup.Spawn` 运行时投放，间距 1.5，挂 room.ContentRoot；免费占位与基座同规则，不做货币结算）。

### 商店陈列实现方式说明

商店 SpawnTable（`InteractableTable_Shop.asset`）entries 只持 prefab 引用，而 ItemPickup 是运行时 Spawn 构建、无可序列化 prefab；且编辑器运行中改 SO YAML 会被内存覆盖。因此不改资产，在 `InteractableSpawner.SpawnRow`（Row 布局唯一消费点、商店生成链单点）追加运行时投放钩子，风格对齐 `ItemPickup.Spawn` / `PrepRoomManager.SpawnDemoItems` 既有运行时构建模式。陈列消耗包是 ItemPickup（E 拾取进背包），与 v0.7.2 背包天然联动。

### 文档

- `Assets/Scripts/now_use/README.md`：版本号 v0.7.3；PrepRoomManager / ConsumableData / InteractableSpawner / ChestInteractable 条目同步为现状。
- `版本实施自检清单.md`：§3 背包/槽位行末"消耗品效果与正式投放归 v0.7.3"改已落地；§3 宝箱奖励行改三权重 40/30/30；最近更新行 v0.7.3（2026-08-03）。
- `v0.7_Demo玩法计划书.md`：§九 v0.7.3 执行卡标完成。

## 验证情况

- `dotnet build Assembly-CSharp.csproj`：**0 警告 0 错误**（无新脚本文件，csproj 无需补；修复了宝箱三权重字段 `consumableChance` 只赋值未读取的 CS0414 警告——消耗包掉落日志带出名义权重，字段语义不变）。
- 资产名清单 grep（Assets/Scripts 全部 .cs）：仅 `PrepRoomManager.ConsumableAssetNames` 一份代码清单（ConsumableData.cs 注释提及属文档说明），无第二/三份拷贝。
- 代码审查核对：
  - ApplyEffect 三分支调用的 `Health.Heal` / `PlayerStats.ModifyArmor` / `PlayerStats.AddMana` 签名与既有调用方一致 ✓；
  - 商店第二排位置：基座排 y=房中心，消耗包排 y=中心−2，间距 1.5、3 件总宽 3，房宽 20 内无重叠、不跨门区 ✓；
  - ItemPickup 渲染层级 1~2（交互物层），不超玩家层 ✓；Awake 自动补 CircleCollider2D 触发器，无抽象 RequireComponent 坑 ✓；
  - 宝箱消耗包分支：资产缺失 → Warning + 退回法力瓶；三权重和 <1 余量归消耗包、>1 后段截断（阈值顺序天然保证）✓。
- **实机表现（三种包使用效果、宝箱三分支、商店第二排陈列、满包提示）未经编辑器实测，待用户验收。**

## 遗留风险

- **击杀掉落未做**：计划书 §4.3 标注【待补充·数值】，设计未定，本版不做，归后续版本。
- **占位数值待设计定稿**：+4/+4/+40 为十倍重构前旧口径，对 HP 130 / AR 70 / MP 100 无意义（计划书 §4.3 已标【待补充·数值：设计 D 定稿】），实机验收时观感偏差属预期。
- **满包提示实机验证**（v0.7.2 遗留，本版可触发）：栏+3 包容纳 4 种道具，捡第 4 种应提示"背包已满"且拾取物保留——逻辑代码审查通过，实机待验收。
- 消耗包为色块占位视觉（正式图标在《资产需求总清单.md》排期内）。
- 商店陈列消耗包免费占位、无货币结算（与补给基座同规则，计划内）。

## 待用户在编辑器验收清单

> 测试场景：`v0_7_PrepRoom` → `v0_7_ClassWeapon`；回归 `v0_4_EnemySystem`；Game 视图 1920×1080 或 Free Aspect。

1. **编辑器刷新**：切回 Unity 导入脚本变更；编辑器此前开着的话勿保存旧内存覆盖 YAML。
2. **准备房间**：出生位前方横排三种消耗包各 1（红血包/灰甲包/蓝魔力包，不再是测试药）；E 拾取进背包，道具栏/背包格显示正确。
3. **使用效果**：分别使用三种包——血包 HP+4、甲包护甲+4、魔力包法力+40；道具栏数量 −1，减到 0 槽位清空。
4. **满包提示**：把栏+3 包填到 4 种道具后再捡第 4 种，提示"背包已满"且地上拾取物保留。
5. **宝箱（地牢）**：多次开箱覆盖三分支——本职业武器 / 法力瓶 / 随机消耗包；消耗包 pop-in 弹出、E 可拾取。
6. **商店（地牢）**：商店房补给基座排下方有第二排三种消耗包，E 拾取进背包，与基座不重叠。
7. **回归**：补给基座/法力球/换武器入包/准备场景展台归位照旧；`v0_4_EnemySystem` 木桩场景无损。
