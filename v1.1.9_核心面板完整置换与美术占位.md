# v1.1.9 核心面板完整置换与美术占位

## 本轮目标

新加入的石板面板和三态按钮不再只覆盖旧 UI 的背景，而是替换核心面板的整体层级与布局。业务逻辑、选择结果及跨场景状态不变。

## 已置换界面

| 界面 | 新结构 |
|---|---|
| 角色选择 | 900×620 石板主面板；战士/狼人双列大卡；底部独立确认按钮 |
| 职业选择 | 1040×680 石板主面板；战士/弓手/法师三列职业卡；卡内展示六维数值；底部独立确认按钮 |
| 暂停菜单 | 遮罩内嵌 620×560 石板面板；音量与操作控件全部改为面板局部坐标；操作按钮使用三态石板 |
| 死亡结算 | 遮罩内嵌 720×560 石板面板；结算数据改为面板局部布局 |

## 暂缺美术与文字挂点

- 角色选择：`[角色立绘待补]`，需要战士与狼人各一张透明背景立绘。
- 职业选择：`[职业立绘待补]`，需要战士、弓手、法师各一张透明背景立绘。
- 死亡结算：`[楼层图标待补]`、`[击杀图标待补]`、`[时间图标待补]`。
- 标题目前继续使用 TMP 文字；后续若有标题字效图，可替换 Title 文本节点但不应承载业务逻辑。

这些挂点均为表现层占位。拿到资源后将对应 TMP 节点换成 `Image` 即可，不修改选择、确认或结算流程。

## 验收

- Unity 编译：0 错误。
- 正式入口 `v0_7_PrepRoom` 运行截图确认角色页显示为大石板双列卡片，旧纵向布局已被替换。
- 角色/职业选择状态仍分别写入 `RunStateCarrier.ChosenCharacter` 与 `LastChosenClass`。

## v1.1.9.1 缩放质量修正

- 所有相关运行时 Canvas 统一使用 `Scale With Screen Size`，参考分辨率 `1920×1080`，宽高匹配值 `0.5`。
- 角色面板调整为 `900×555`、职业面板为 `1040×640`，均接近石板原图 `594×366` 的宽高比。
- `456×59` 三态按钮不再充当大卡片背景，仅作为约 `280×44` 的横向操作条；卡片主体改用独立半透明容器。
- 面板、按钮与 MainBar 共 28 张 UI 纹理改用 Bilinear，保持 Sprite Single、无 Mipmap、无纹理压缩，以改善非整数比例缩小时的采样质量。
- 运行截图 `Assets/Screenshots/ui-quality-layout-check.png` 已确认面板、卡片与按钮不再挤压或纵向变形。
