# 开发必读 · 红线归档(v1.1.x 全量,自 v1.2.0 文档重构中析出)

> v1.2.0 起《开发必读_核心信息整合.md》重构成精简 9 节结构,本文件保留旧版全量红线(R1~R25)
> 与完整踩坑教训记录作为历史真源——查询详细背景/旧版本行为时看这里;日常红线以新版为准。

**R19 · 受击碰撞体必须与攻击平面同高（v1.1.1 上半身不受击教训）**：玩家 CapsuleCollider2D offset y=2.5（局部）经根缩放后悬浮在根上方 +0.11~+1.70，而所有攻击几何（敌人 WeaponPivot 判定带 ±0.1、远程弹道 +0.10、IsInAttackRange 根距）都活在根平面高度 → 横向挥击永远差 0.01 打不中、北侧敌人物理接触根距 2.2 > 攻击触发距 1.8 永不出手。**结论：不存在第二层"识别层"，非 Trigger + 正确 Layer + IDamageable 同 GO 的那个碰撞体就是受击层；受击体错位于攻击平面时，"看得见接触却不掉血"。排查工具：ColliderOutlineDebug（F9，红=实体受击层/黄=Trigger/橙=挥击判定盒，编辑器专用）。** **【已修复 v1.1.21】**胶囊 offset 已改 (0,0) 居中于根（世界 [−0.79,+0.79]）——上半身不受击/北侧不出手/贴墙提前被挡（观感"墙体碰撞偏大"）三症同源一并消除；教训保留：受击体必须与攻击平面同高。

### 地牢生成（v1.1.44 新小节）

**R22 · 格级连通 ≠ 玩家可进；骨架=路网，不是大片禁放区**（v1.1.39 骨架重做教训）：
① 格子 BFS 连通只保证"格到格"，玩家胶囊净宽 **1.32**（Player.prefab 3.65×根缩放 0.3618）过不了 1 格净宽（1.0）的缝——障碍与轮廓/障碍与障碍隔 1 格形成的窄区，格级验证通过但玩家物理进不去，而 SpawnPositionHelper 物理选点不查可达会把敌人/宝箱/大石块刷进去 → 卡清房。**房内可达性判定必须走玩家口径**：`RoomLayoutValidator.Reachable2Wide`（通行边=4 邻边两侧至少一侧 2 格带皆 free ⇔ 净宽 ≥2），要求所有 free 格+门锚点全部可达。② "先铺路径骨架再放障碍"的正确形态是**细路网**（中心 3×3+门→中心宽 3 走廊）+ 受约束障碍；v1.1.44 曾用逐段增量验证，v1.1.46 已升级为**完整候选→双口径验证→评分→一次提交**。大片禁放区（十字带+中带+内环）会把障碍挤到均值 0.6 格/房、54~66% 房间验证 10 连败退化空房。③ 内容阶段大石块落点避让骨架格（DungeonBuilder.roomSkeletons → StoneDecorSpawner），防回堵验证留出的入口。

**R24 · 房内生成必须“完整候选后提交”，表现层只按最终 Plan 绘制一次**（v1.1.46 教训）：
① 禁止先铺满地板再对 Carved 格“不做事”——深层挖除必须从 `groundCells`/Floor/Walls 同时撤销，否则轮廓墙会视觉伪装成贴外墙的内部障碍；② 每次 Shape 重试必须 `new HashSet(baseProtect)`，轮廓邻圈只能污染本次尝试，禁止累积到后续随机；③ 障碍用临时 `HashSet` 生成完整小岛候选，跑格级+玩家口径验证并评分后一次 `UnionWith` 提交，禁止重叠段 `Add` 后失败时无条件 `Remove`（会误删旧格）；④ 观感门禁与连通门禁分责：单岛 2~4 格、岛间留 2 格、中心/门廊/外墙净空由 Planner 保证，Validator 只判合法；⑤ 敌人/奖励/装饰落点必须先消费最终 `RoomPlan.SpawnCells` 白名单（3×3 无墙可走地面），再做距门和 NonAlloc 物理重叠，禁止只凭矩形 Bounds+尚未刷新的 TilemapCollider 判定。

**R25 · 固定特殊房必须先锁地图拓扑，再套唯一模板**（v1.1.52 Boss 仪式厅教训）：
所谓“只随机房间位置”不是生成后把普通房换皮：生成器必须先选出满足叶子房、固定跨格尺寸、固定入口方向与最小路径距离的候选，再进入扩格；当前 Boss 仪式厅合同锁死 `2×2 + 南入口`，朝向和尺寸也不得随 seed 或旧配置字段变化。房内 `RoomPlan`、墙地变体、墙体朝向、装饰锚点、敌人和奖励插槽均不得消费地图 seed，普通随机塑形/地皮噪声/随机装饰必须旁路；固定墙定向只能读取本房局部墙拓扑，禁止相邻随机房的墙改变它。表现柱体必须直接消费逻辑柱基求出的中心，敌人与奖励仍遵守 `SpawnCells → 距门 → NonAlloc`；若内容在玩家迁回新 Start 房之前生成，固定敌位复核必须排除这个旧玩家的瞬时位置，不能让上一层状态污染新层模板。偶数跨格大房的视觉中轴可能落在两个粗格交界，门洞不能只追求几何居中：完整门宽必须同时落在真实相邻粗格的重叠段内，否则会出现“门有两格、门后一格仍是墙”的假通路。跨格房边界必须四边完整铺墙、最后仅按真实连接开洞，禁止“任一粗格有邻居就跳过整条边”造成半边漏墙。

### C# 编码陷阱（项目内真实发生过）

**R9 · 协程重叠**：受击闪烁类"改颜色→等→恢复"协程必须互斥 + **基线色持久缓存**（不能从当前 sr.color 读——重叠时会缓存到闪烁色 → 永久变白）。
**R10 · 每帧 GC**：RaycastAll/OverlapCircle 用 NonAlloc 静态缓冲；GetComponentInChildren 目标缓存（单槽）。同帧多命中音效用 `PlayOneShot`（天然叠放）不用多源轮询。
**R11 · 无敌帧口径**：`Health.iFrameUntil`（Time.time 口径），`GrantIFrames(dur)` 授予、TakeDamage 开头跳过；hit-stop 期间 timeScale=0，Time.time 停走（冷却也暂停，符合预期）。
**R12 · 玩家死亡流程**：Health.Die → RunManager.RestartRun（延迟2s）→ ResetHealth + PlayerStats 护甲回满 + PlayerController.Respawn（颜色/速度/ Dash 冷却重置）。**任何新玩家状态字段都要在 Respawn 里重置**。
**R13 · 集合引用比较陷阱**：`FrameAnimator.Play()` 会复制新 List——外部用 `CurrentFrames != want` 之类**引用比较判断"是否在播"永远为真**，导致每帧重播、动画永远卡第一帧。跨对象状态跟踪一律用**枚举/标记**，不用集合引用。
**R14 · Play 模式下改代码不热生效**：Play 中修改 .cs 后必须 `editor_stop → asset_refresh（等编译完成）→ editor_play`，且日志验证要看**格式特征**（如新增字段是否出现）——旧实例的 Awake 日志会残留误导判断。**升级（v0.6.8 连续三次踩坑）**：若编辑器关闭了自动刷新，`asset_refresh` 只刷资产库**不重编译脚本**——需 `debug_force_recompile` 强制编译；且 `editor_play` 返回 success 不代表真的进了 Play，用 `scene_screenshot` 返回的 `isPlaying` 字段确认；"refresh→play"链条会静默失败，安全流程=分步验证每一步。

**R15 · 房门关闭必须等玩家完整进房**：`OnTriggerEnter2D` 只表示碰撞体开始相交，不表示玩家已完整进入触发区；不能据此立刻激活战斗房。`RoomTrigger` 必须在 Enter/Stay 中用玩家世界 `Collider2D.bounds` 四边均落入 `Room.Bounds` 后才 `Room.Enter()`，否则门会在玩家仍横跨门洞时启用实体碰撞并夹住角色。固定内缩量也不能替代完整包围判断（角色碰撞尺寸/偏移会变化）。
**R15 · UnitySkills 按名字查对象会命中第一个同名者**：场景中敌人 prefab 都带 WeaponPivot/WeaponSprite 等同名子物体，`component_get_properties name="WeaponPivot"` 查到的是**敌人的**——验证玩家侧状态必须用 `path="Player/WeaponPivot"` 精确路径，否则拿到假数据白排查。
**R18 · 手写 fileID 必须校验 int64 上限（9223372036854775807）**：999 开头 19 位数溢出 → Unity 场景/prefab 解析崩溃（Failed to convert to signed 64 bit int → Safe Mode）。安全段示例：666xxx（18 位）。且修复要**全项目扫**（场景+prefab+asset 一起）——只修场景漏 prefab 会二次事故。另：纯 Input System 项目的 UI 脚本禁用 UnityEngine.Input.GetKeyDown（InvalidOperationException）——走 PlayerInput.onActionTriggered 订阅。
**R17 · 编译错误修复后必须重新 force_recompile + 场景重载**：一个脚本的编译错会让整个程序集缺失——所有新类不存在（component_add 报 Component type not found 的另一诱因）、场景中对应组件变 missing 且**重编译后不会自动恢复绑定**，需 scene_load 重载。排查顺序：Console error CS → 修复 → force_recompile → scene_load → 再验证。
**R16 · 纯代码 UI 的 RectTransform 属性走 .rectTransform**：`pivot/anchorMin/anchorMax/sizeDelta` 是 RectTransform 的属性，不能写在 Image/Text 组件上（`icon.pivot` 编译错 → 曾触发 **Safe Mode**）。强编译前本地自查要含 API 层（括号检查抓不住）；服务离线超 30 秒先查 `Logs/Editor.log` 的 `error CS`（比等服务快得多）。
**R23 · NUnit `Assert.That(coll, Does.Contain(x))` 对集合线性枚举**（v1.1.44 教训）：批量断言（36 万格级）实测拖到 25 分钟超时——正确写法 `if (!set.Contains(x)) Assert.Fail(...)`（O(1) 快路径，提速 60 倍）。EditMode 测试注意 TestRunner 占主线程期间 REST 服务完全无响应，属预期。

### 调试规范（摘要，全文见《开发调试规范》）

- 严禁擅动 CameraFollow.target / 瞬移玩家（必须记录+复位+用 rb.position）/保存场景/留 Debug_ 对象
- 删调试克隆玩家前必须确认正式 Player 实例链路完整（tag=Player 唯一激活）
- 已知调试假象表：LOS 假 false / 假玩家不扣血 / 移动键全失灵（旧内存副本 or 双玩家 or 编译错误）——先 Reload 再排查

### 验收门禁（v1.1.43，最高优先级）

**改任何 .cs 交付前必跑 `python _dev/compile_check.py`（Unity Roslyn 真编译，exit 0 才算验收）**。
历史教训：CS0117/CS1503/CS0103/CS1061 均为文本复查抓不住的类型级错误，曾全部漏到用户手里。
检查器依赖 Library/ScriptAssemblies（编辑器需成功编译过一次）；检查器自身改动后须重跑阳性对照。

## 四、系统速查

**输入键位**（PlayerInputActions，Player map，v0.7.5）：WASD=Move · 左键=Attack（支持长按）· E=Interact（交互+拾取）· F=Skill 小技能 · Q=Ultimate 大招 · R=WeaponSkill · C=UseItem · **Esc=Cancel**（关职业选择 UI / 暂停菜单，PausePanel 消费，v1.0.4）。**Space=Dash 狼人冲刺**（v1.1.42，仅狼人外形；Sprint 仍无消费方）。

**关键事件**：`DungeonManager.OnGenerated`（生成完成含传送/相机 Snap；楼层切换/重开同链路）· `Room.OnRoomEntered/OnRoomCleared` · `Health.OnHealthChanged/OnDeath` · `EnemyHealth.OnDeath/OnTakeDamage` · `WeaponHitbox.OnHit`

**反馈挂点**（v1.0.5 恢复）：命中 "hit" 在 WeaponHitbox（**仅音效**——v1.1.41 震屏与停帧均已移除，震颤感整体专属战士技能组，入口 ExecuteFeedback.Play=强震0.05/0.08+停帧0.08）；受击 "hurt" 在 Health（实际扣血时）；敌死 "enemyDie" 在 EnemyHealth.Die；开门 "door"；开箱 "chest" 在 ChestInteractable；金币 "coin" 在 CoinDrop（v1.1.3）。**新加 SFX id 必须改 v0_7_PrepRoom 场景 AudioManager 的 sfxEntries**——首实例 DontDestroyOnLoad 常驻，正式流程从 PrepRoom 进，只改地牢场景的表会静默失效（v1.1.3 教训）。三场景 AudioManager 已接 Kenney 占位表 + DungeonBGM（正式音效待换）。HitStop 与暂停 UI 经 `SuppressByUI` 协调（PausePanel/DeathPanel 均已接入，timeScale 无冲突）。死亡结算：`DeathPanel`（楼层/击杀/时长，数据来自 `RunTracker`+RunManager.FloorNumber）+ restartDelay 6s 兜底。

**狼人=可选角色外形**（v1.0.6 重接入，与职业独立）：ClassSelectUI"角色外形"行 → `RunStateCarrier.ChosenCharacter`（死亡保留）→ `FrameAnimator.SetWerewolfVisual`（帧在 `Resources/Art/Characters/Werewolf/{Walk_L,Walk_R,Idle_L,Idle_R}`，001.png 命名，左右两套不 flipX；攻击/技能帧未产出回退武器挥砍）。旧变身系统已退役（历史在 git 与 [v0.6.8_狼人角色系统开发文档](v0.6.8_狼人角色系统开发文档.md)）；Beast/Transform 素材未接入；素材源工具仍在 `E:\WeGameApps\The animation of Unity\狼人\`。**统一入口（v1.0.6）**：地牢场景 RunManager.`redirectToPrepWhenNoClass=true`——无职业直连地牢自动回 v0_7_PrepRoom；编辑器 Play 播放的是当前打开场景，别在旧场景里按 Play 当正式流程。

**狼人能量与兽化门禁**（v1.1.53，覆盖上方“旧变身系统已退役”的过时描述）：`WerewolfTransformation` 已重新接入；`WerewolfEnergyBar` 订阅 `DamageResolver.OnPlayerDamageDealt`，默认按实际伤害×0.5 充能，100 满能后 T 才能兽化。兽化每秒消耗 10，归零自动退兽；重生必须同时调用 `ResetTransformation` 与 `ResetEnergy`。头顶 UI 仅运行时创建在 `WorldUIRoot`，空能显示 large 框+banner 填充，满能才启用 medium 框与双狼头张合，因此不能以编辑态 Player.prefab 子物体判断素材是否接入。资源固定在 `Resources/UI/WerewolfEnergy/`。

**狼人兽化 UI 高度**（v1.1.54）：普通能量条保留 `heightOffset=0.46 / 150×36 / scale=0.01`；运行时快照普通绝对高度，不再继续消费会随兽化跳变的 HealthBarAnchor。Transform 演出必须把同一视觉膨胀进度逐帧传给 WerewolfEnergyBar，使 UI 从普通高度插值到独立 `beastWorldHeight`；主动退兽、耗尽和 Respawn 统一平滑回落。

**测试门禁与资源**（v1.0.4）：EditMode 测试 `Assets/Tests/EditMode/`（Missing Script 扫描三场景+全 Prefab、ApplyArmor 边界值、地牢 seed 确定性+连通性），提交前必跑；运行时按路径加载的资产放 `Assets/Resources/`（**路径=编辑器路径镜像**，编辑器与构建同源：Class/Skill/Item SO、Warrior 序列帧、SlotFrame 均已迁入）。误删脚本在原路径重建同名类可复活旧 GUID 自愈场景断链。

**小地图**（v1.1.36）：场景 `MinimapSystem` 挂 MinimapController；消费 OnGenerated 重建。战争迷雾按 Room.OnRoomEntered 增量揭示：已探索房显示真实房型色，直接相邻未探索房只显示暗色位置，第二层及更远房间/未知房之间连线完全隐藏；出生房首帧直接视为已探索。

**UI 按键接入规范**（v1.1.10 定稿，标准模式）：**功能菜单按钮 = 一功能一张完整按钮图**（图标+文字烘焙进图，`button_<功能>.png`，按菜单分子目录）——双份入库 `Assets/UI/<菜单>/`（母版）+ `Assets/Resources/UI/<菜单>/`（运行时 `Resources.Load<Sprite>`）；导入 Single/**Bilinear**/无Mipmap/PPU1/Border0；显示 `preserveAspect=true`+尺寸贴原图比例；状态反馈用**单图 ColorTint 亮度**（Normal 0.88/Hover 1.0/Pressed 0.65/Selected 0.96/Disabled 0.45α0.75，fade 0.08s），**不叠 TMP 文字、不为状态多要位图**；素材断链回退三态石板+文字（`PanelSprite.ApplyStoneButton`）。代码模板：`PausePanel.CreateMenuButton`。三态石板（456×59）仅做 ~280×44 小操作条与兜底；UI 大图滤镜用 Bilinear（Point 只给像素风地皮/角色帧，v1.1.9）。**含交互控件的代码画布必须 `AddComponent<GraphicRaycaster>()`（v1.1.12 教训：缺失=按钮全灭而 Esc 正常，易误诊）；纯展示画布不加（点击穿透）**。

**UI 石板素材资产索引**（v1.1.47.1 建立，防记忆缺失——素材源在项目外部目录，git 里只有入库副本）：
- **素材源总目录**：`E:\WeGameApps\The animation of Unity\主界面素材\`，子目录：`石板面板_三状态_抠图预览_v2`（456×59 按钮条三态，鹅卵石边风格）、`主菜单按钮_三张`、`主菜单按钮_抠图预览`、`取消按键`、`音量滑槽与滑钮`、`水桶素材`（已入库 v1.1.45）、`石块装饰_*`（已入库 v1.1.26）、`石墙装饰_切割抠图`（已入库 v1.1.28）。
- **项目内石板母版**（双份入库惯例：`Assets/UI/Panel/` 母版 + `Assets/Resources/UI/` 运行时副本，改图两处同步）：
  - `panel_stone.png`（594×366）——9-Slice 大面板，border {34,34,37,49}；接入 `PanelSprite.ApplyStonePanel(img, fallbackColor)`。
  - `btn_stone_normal/hover/pressed.png`（456×59 **按钮三态**：常态/悬停/按下，border {13,11,13,13} 三图统一防切态跳动）——接入 `PanelSprite.ApplyStoneButton(btn, img, fallbackColor)`（SpriteSwap 自动换图；**必须配对传 Button**，btn=null 时直接 return 不生效）。断链回退纯色+ColorTint。
  - ⚠ 外部 v2（鹅卵石边）与项目内三态（不规则石块边）同尺寸不同风格——项目现用后者；若整体替换为 v2 需三图一起换并保持 border 参数（v1.1.47.1 待拍板事项）。

**遗留债**（详见 v0.5.4.4.4 文档第 4 节 + 长期计划 §4）：巡逻无寻路、Charger 空层配置、DungeonGenerator 保底 NRE、负伤害、Health/EnemyHealth 合并（M4 前）、PatrolSystem 随机源（M5 前）。

## 五、版本与文档索引
> v1.1.20 归档：24 份计划书（正式计划/计划书/执行·重构·设计·实施计划/打磨计划/总计划/路线图）已移入根目录 `计划书/` 文件夹（git mv 保留历史）；版本文档（v0.x.y 功能/修复记录）与操作日志仍在根目录。


| 文档 | 内容 |
|---|---|
| 项目长期计划_Roguelite路线图 | M1-M5 里程碑任务总账（M1 已完成）+ 已拍板 4 决策 |
| v0.6.2_预警与判定对齐修复 | 同源原则断裂与回归（R7/R8 的来历） |
| v1.1.1_碰撞体可视化与受击错位诊断 | 上半身不受击根因（R19）+ ColliderOutlineDebug 工具 |
| v1.1.2_战士武器挂载点对齐容器中心 | WeaponPivot 局部 y 0.3→0（对齐狼人视觉挂载/图心），判定安全校验 |
| v1.1.3_金币系统还原与适配 | CoinDrop/钱包/HUD/敌死宝箱产出（MCP 分支还原）；AudioManager 首实例常驻=新 SFX id 要改 PrepRoom 的表 |
| v1.1.4_地皮地形分层系统 | 逻辑(TerrainMask)/视觉(TerrainPainter)分离 + Bitmask 铺装；地皮素材 85px@PPU85=1单位，房间尺寸须整数格 |
| v1.1.5_石板面板9Slice母版 | panel_stone 母版（Border L34/B34/R37/T49 实测）+ PanelSprite 加载器；代码 UI 接入三行式 |
| v1.1.6_选择菜单接入石板面板母版 | ClassSelectUI/CharacterSelectUI 主面板换母版（交互零改动）；标题让出 49px 顶框；新行=向 panel 追加按钮 |
| v1.1.7_选择菜单按钮接入三态石板素材 | btn_stone 三态（统一 Border L13/B11/R13/T13——SpriteSwap 三态 Border 必须一致防跳边）+ ApplyStoneButton 三行复用 |
| v1.1.8_主界面横幅禁用动画 | UIFramePlayer 通用序列帧播放器（unscaled 时间/区间/回调）+ MainMenuBar（点取消播 Disabled 序列后收起，F8 预览） |
| v1.1.9_核心面板完整置换 | 角色/职业选择改为双列与三列石板卡片；暂停/死亡改为石板局部布局；缺少立绘与结算图标用明确文字挂点占位 |
| v1.1.10_ESC暂停菜单三功能按钮 | 导入继续游戏/设置/返回大厅三张完整按钮图；ColorTint 实现 Normal/Hover/Pressed 亮度；设置键展开音量区 |
| v1.1.11_继续游戏按键功能重写 | ContinueGame 独立方法+验证日志；Close 防悬挂引用与 timeScale 强制复位 |
| v1.1.12_暂停死亡面板按钮失效根修 | **代码画布含交互控件必须 AddComponent\<GraphicRaycaster\>**（PausePanel/DeathPanel 缺失=按钮全灭而 Esc 正常）；纯展示画布故意不加防吞点击 |
| v1.1.13_投射物穿墙根修 | **Trigger 子弹 Update 传送式位移会间歇隧穿**（Box2D Continuous 不覆盖 Trigger 形状；帧尖峰单帧位移>墙厚）；修法=每帧位移段 CircleCast（useTriggers=false，单结果零 GC）先测后动 |
| v1.1.14_设置页双标签按钮 | 设置面板改音频/控制双标签（button_{audio,controls}_settings，ColorTint 亮度三态）；CreateMenuButton 尺寸参数化 |
| v1.1.15_石槽音量滑条 | 五态滑钮（hover=光晕图换图，SliderHandleState 指针状态机）+空槽轨道+黄条 fillRect；轨道/滑钮按原生配比等比适配；断链回退纯色 |
| v1.1.16_设置两级导航与代码光晕 | 设置主页两键→音量/操作子页（返回=石板小条）；滑钮光晕改程序生成（64px 径向衰减静态共享），状态图运行时副本已删 |
| v1.1.17_命中震屏手感调参 | 命中震 0.05/0.08→0.025/0.06（击杀观感过强，逐帧随机抖动毛刺感）；受击/Boss 震未动；整体调感走场景 shakeScale |
| v1.1.18_设置页独占隐藏左列 | 进设置隐藏左侧三键、面板居中；主页加返回键补收起出口（设置键已随左列隐藏） |
| v1.1.19_设置页去底板与按键放大 | 移除 v1.1.14 加的设置面板深色底板（完整功能图按钮无需垫底）；两键放大至 520×110 与暂停主键同规格 |
| v1.1.21_玩家胶囊体下沉居中 | R19 修复落地：offset(−0.19,2.5)→(0,0)；"墙体碰撞偏大"实为胶囊悬浮错觉；受击/北侧出手/贴墙手感三症同除 |
| v1.1.22_房间层次感批次 | RoomShaper：角部挖除(L/双角/斜切)+房内墙段/柱（门洞/中心保护带，确定性 seed）；CarveDoor 拆纯计算；障碍按面积保底；RoadMask groundCells 同步移除塑形格 |
| v1.1.23_房内墙模板与墙体掩码拼接 | 五套房心模板替换随机段（房心±2开阔=刷怪安全/无pocket，离线flood-fill验证）；WallAutotiler 4向掩码(上8右4下2左1忽略斜角)等 wall_00~15 素材自动点亮 |
| v1.1.24_玩家胶囊改下半身判定 | offset(0,−1.15)/size(3.65,2.85)→世界跨[−0.93,+0.10]；上沿+0.10保攻击带0.2重叠（R19 不回退）；俯视角脚部判定惯例 |
| v1.1.25_地皮恢复原套件逐格铺装 | 弃用 RoadMask 单底图整层平铺（休眠保留）；Bitmask 边/角/贯通过渡全恢复+新基底/装饰池融合；85px@PPU85 契约自适应 |
| v1.1.26_废墟石块装饰 | StoneDecorSpawner：小石块无碰撞点缀 1~4/房、大石块障碍物替代 0~2/房（Obstacle 层+可破坏）；SpawnPositionHelper 落点防卡墙；长边≥1.06单位自动分类 |
| v1.1.27_障碍物移交石块与尺寸门槛 | 旧木箱障碍表停用（休眠保留）；障碍物门槛收紧至 ≥1.2 单位（≈102px，7 张合格）；大石块 1~3/房独扛障碍密度 |
| v1.1.28_墙体石墙素材与一块厚结构 | CellWidth=roomW+1、西/南必画东/北无邻才画（共享墙线一块厚）；WallPropTileset 宽归一+Grid整格碰撞+随机引用；门洞 1 厚单列打穿 |
| v1.1.29_墙体横竖分离与堆叠扣合 | 竖列仅 prop_01（底对齐上伸0.47）+TilemapRenderer.SortOrder.TopLeft（下行后画=下图扣上图）；横行随机池；ReorientWalls 按最终拓扑（含门洞）终遍定向 |
| v1.1.30_选择页移除确认键 | 点卡片/选择键即选中即确定（整卡 Button 冒泡+选择键同功能）；确认闪烁链退役；Esc 取消语义不变 |
| v1.1.31_房间塑形五职责架构 | **挖除区只画交界一格轮廓墙（填实心墙=大片墙块根因）**；Shape/Boundary/Obstacle/Validator/Planner 五职责+同RNG重试保底；障碍受约束随机变体（旋转/镜像/省略/长度/偏移）+BFS验证（可达/65%/块≤6/零贴墙） |
| v1.1.32_敌人寻路与视野阻挡 | EnemyPathfinder 局部A*+拉绳简化（静态阻挡=墙Tilemap+Obstacle层，动态体不挡路）；直线通畅直走/被挡绕行；视野丢失一次性50%掷骰（继续追/目击点搜索3s后弃目标） |
| v1.1.33_狼人首帧闪现修复 | 外形首帧初始化下沉到 FrameAnimator.Awake（读载体即渲染对应外形）；SetWerewolfVisual 幂等吸收场景侧后续调用 |
| v1.1.34_震屏保留给裸绞斩杀 | 常规命中不再触发震颤；ExecuteFeedback.Play=0.05/0.08强震+停帧0.08，仅裸绞阈值处决或真伤致死后调用 |
| v1.1.35_寻路航点越界修复 | FindPath 契约 true⇒≥2航点（start==goal 早退补起点）；MoveWithPathing 索引越界防御钳制（作废路径自愈，不抛错） |
| v1.1.36_飞行弹owner销毁NRE修复 | Projectile 发射时快照 ownerRoot（射手销毁后 fake-null 放行）+ ResolveHit this==null 守卫 + other 判空 |
| v1.1.37_清理竞态MRE三连修复 | 楼层 ClearAll 同帧残余访问：EnemyAI.Update/UpdateVision this守卫；Room.RefreshDoors 死门引用 RemoveAll 摘除；EnemyCombat 冲锋射线 hit.transform 判空 |
| v1.1.38_选择页语义修正 | 颜色卡点击=选中高亮（可反复比较），"选 择"键=确定应用关闭；v1.1.30 误绑即确定已纠正 |
| v1.1.39_房内全空间可达骨架 | 先铺可达骨架（门走廊+中心十字3宽+半跨度随机中带+边缘带+轮廓交界圈）后置障碍；障碍逐格落位保留合法段；验证器加全连通精确校验（孤岛即重试）**【骨架喧宾夺主+格级口径缺陷，v1.1.44 重做】** |
| v1.1.40_运行时精灵命名修复 | 全项目 Sprite.Create 结果显式命名（WallPropTileset 空名=GetSprite ArgumentException 根因）；R21 红线 |
| v1.1.41_地皮融入骨架与零震颤 | 土斑 80% 吸引吞入路径骨架（RoomPlan.Skeleton→TerrainMask.ApplySkeletonBias，非强制保留噪声形）；常规命中停帧亦移除（震颤感整体移交 ExecuteFeedback/战士技能组） |
| v1.1.42_狼人冲刺 | WerewolfDash（MCP Dash 参数原版 18/0.15/0.9/+0.06）：Space 触发、仅狼人（四接线点 EnsureOn/改战士销毁）、PlayerMovement.SetSuspended 单一写速者、SetInvincible+自管截止、Continuous 防隧穿 |
| v1.1.43_真编译验收门禁 | _dev/compile_check.py（Unity Roslyn+358 DLL 真类型编译，防假通过三保险+阳性对照在案）；首跑抓出并修复 Respawn 括号/容量构造两真 bug；改码必跑 |
| v1.1.42_击杀震颤归属收口 | ExecuteFeedback 正式接入战士裸绞实际击杀；普通击杀与 Boss 死亡均无震屏/停帧；玩家受击震屏保留 |
| v1.1.43_房间尺寸与墙体坠落入场 | 房间内部尺寸唯一改 DefaultDungeonConfig.roomWidth/Height；新层用无碰撞临时 Tilemap 按格错峰弹跳落墙，真实 Walls 碰撞始终在最终位置，结束零持续开销 |
| v1.1.44_房内路径骨架重做_玩家口径可达 | 骨架瘦身为细路网（中心3×3+门走廊宽3）+障碍增量落位（放入即验/尾缩/弃段）+玩家口径验证（净宽≥2 通行边 BFS，所有 free 格+门锚点必须可达，1 宽缝零容忍）；大石块避让骨架格；R22/R23 红线；离线仿真 Tools/RoomSkeletonSim |
| v1.1.45_可破坏木桶装饰 | barrel_NN 36 张入 Resources/Art/Decor/Barrels（平铺：LoadAll 不递归）；bucket.cs：Trigger 装饰桶（<0.85 可穿可打碎 HP3）+实体木箱堆（≥0.85 挡路）；实体件避让骨架格；素材池全 <1.2——照搬石块阈值会让实体档永不触发（阈值要按素材实测分布定） |
| v1.1.46_房内小掩体岛候选评分生成 | 修复预铺地板未撤销/Shape protect 跨重试污染/骨架偏置调用过早；房形收敛为 72% 矩形+小缺角（挖除≤12%）；固定长墙模板改 14 候选小掩体岛（2~4 格、岛间2格、中心/门廊/墙净空）→双口径验证→质量评分一次提交；最终 SpawnCells 白名单接通敌人/奖励/装饰并主动刷新墙 Collider；真实尺寸多 seed 门禁，EditMode 33/33 |
| v1.1.46_战斗房扩容与怪物轮次 | combatCellSpan=2：Combat 房 2×2 优先/2×1 回退（实测 85% ≥2×面积）；combatWaveChance=0.35 战斗房两波制——Room.IWaveProvider 清房扩展点 + RoomWaveController（延迟 0.9s 复用 EnemySpawner.Spawn，幂等）；大房（≥1.9×面积）单波数量 ×1.5 封顶 12 |
| v1.1.48_暂停层数与金币HUD放大 | 暂停石板左上角增加纯 TMP 当前层数（每次打开直读 RunManager.FloorNumber，准备大厅隐藏）；金币 HUD 图标 16→28、字号 16→24，并同步扩大文本容器与间距 |
| v1.1.52_固定Boss仪式厅收敛 | Boss 房固定为南入口叶子 2×2 仪式厅，仅地图位置随机；近中轴真实 2 格门、四边完整封墙后按连接开洞，墙朝向只读本房拓扑；最终 RoomPlan 同源驱动四柱碰撞/表现、固定地墙、装饰、12 敌人与 3 组奖励安全插槽；缺图走明确 ART_SLOT 占位接口 |
| v1.1.53_狼人能量条与满能变身门禁 | 伤害×0.5 充能、100 满能才可 T 兽化；兽化 10/秒耗能并归零自动退出；运行时 WorldUIRoot 能量条与双头张合素材绑定；重生/改战士清理状态与动态 UI |
| v1.1.54_狼人兽化UI动态高度 | 普通 UI 参数保持不变；能量条脱离兽化血条锚点跳变，按 Transform 视觉膨胀进度动态升至独立兽化高度，退兽/耗尽/重生平滑回落 |
| v1.1.36_小地图战争迷雾 | 已探索房保留真实房型；仅揭示一步相邻未探索房的暗色位置；更远房间和未知连接隐藏；出生房首帧直接揭示 |
| v0.5.4.4.4_战斗判定与场景恢复修复 | 遗留债 13 项清单 + 场景手术范例 |
| 开发调试规范_场景摄像头位置管控 | 调试红线全文（含 2.5 Reload 节） |
| v0.6.0_小地图系统 / v0.6.1_手感与反馈包 | 对应系统操作与验收 |
| 项目框架文档 / 项目进度报告 | 架构全貌 / 版本史 |

> 维护约定：每次修复/新系统落盘后，若产生新教训 → 更新本文档对应红线/速查；版本文档只增不改。