# v1.1.48 失落城堡式战斗第一批——横向攻击带 + 长剑三段连击 + 输入缓冲

## 需求(用户)

战斗模型从"单次动作锁死"升级为 beat 'em up 手感:横向攻击带判定(只打左右、上下对齐纵深)、
三段连击(独立时段/判定/踏步)、输入缓冲(提前按键被听见)、点按即出(消灭松手延迟)。
第一轮只做长剑三段;判定入口仍是 WeaponHitbox(同源原则不破)。

## 实现(六文件)

### 1. MeleeComboTable(新)——连段数据层
- `MeleeComboStep`:段前摇/判定/后摇倍率、判定长度倍率、**纵深容错 laneWidth**、伤害倍率、
  **水平踏步冲量**、后摇可取消比例。
- 长剑三段(基准 0.2/0.2/0.3):段1 0.14/0.11/0.15(踏步 2.2)→ 段2 0.12/0.11/0.15(踏步 2.6)
  → 段3 重击 0.22/0.15/0.40(伤×1.45、距×1.3、深后摇晚取消 0.75)。
- Fallback 单段(枪矛等):旧节奏但保留缓冲+可取消(修复对所有近战生效)。
- `ComboWindow = 0.30s`:后摇完毕后的接段窗口,超时段序归零(段3 完成环回段1)。

### 2. WeaponHitbox——玩家路径横向攻击带
- `SetLaneMode(width, facingSign)` + `ComputeSwingBox` 玩家分支:
  `center = 攻击者根位置 + (facingSign×(reach/2+0.35), 0)`,`size = (reach, laneWidth)`,`angle = 0`。
- X=攻击距离、Y=纵深容错("同一水平线"的容错带,取代 0.15 宽的旋转细矩形);
  以玩家根(下半身判定平面)为基准,与武器旋转动画解耦(画面弧线只是表现)。
- **敌人路径(attackerStats==null)保持旧旋转矩形**——本轮只动玩家。
- 段倍率走既有 `LengthMultiplier`/`DamageMultiplier` 通道,BeginSwing 复位不变。

### 3. PlayerCombat——连段状态机
- **输入缓冲**:攻击流程中 `TryAttack` 不再忽略——`bufferedAttack=true`;
  后摇跑到段 `cancelRatio` 即打断接下段(可取消后摇),后摇自然结束时缓冲也直接接段。
- **段间窗口**:`FinishStep(false)` 后 0.30s 内接段(环回),超时归零段 1。
- **点按即出**:可蓄力近战(剑/枪)按下立即出招(连段接管);长按超 0.2s 转蓄力
  (`CancelSwingForCharge` 安全打断当前段)——消灭"松手才出手"的延迟。
- 段参数:StartWindup 取段表快照,三阶段时长=基准×段倍率÷攻速;frameAnimator 时长同源。
- 远程(弓/弩)整链未动(鼠标瞄准、松手直射保持旧行为)。

### 4. PlayerController/朝向——失落城堡式
- `facingDirection` 水平化:有水平输入时 = (±1,0),纯竖直/静止保持旧朝向;
  公开 `FacingDirection` 供攻击带/踏步消费。技能/冲刺同吃水平朝向。
- `UpdateAiming`:近战瞄准=水平朝向(鼠标不再决定近战方向);远程保持鼠标。

### 5. PlayerMovement——踏步通道
- `AddImpulse(velocity, duration)`:常规移速上叠加冲量并线性衰减(~0.12s)。
  速度由本组件每帧统一写入(直写 rb 会被覆盖——冲量必须走统一通道,SetSuspended 同款理由)。

### 6. AttackIndicator——横向带显示
- 玩家范围显示从扇形改 **Box(SetBox:reach×laneWidth)**,方向=水平朝向;
  与 WeaponHitbox 判定共用段参数(同源)。戳击(枪)的伸缩显示同改 Box。

## 旧行为对照(用户分析的六项逐条落地)

| 旧痛点 | 新机制 |
|---|---|
| 攻击中按键被吞 | 输入缓冲(可取消点/段末/窗口三处消费) |
| 整段锁方向 | 方向=水平朝向,段间窗口/可取消点可转向 |
| 只有一个动作 | 长剑三段(独立时段/判定/伤害/踏步) |
| 松手才出手 | 点按即出(按下立即出招,长按才转蓄力) |
| 无攻击位移 | 踏步冲量(段 1/2 明显,段 3 重击小步) |
| 判定细难命中 | 横向攻击带(纵深容错 0.95~1.05,取代 0.15 细矩形) |

## 验收

- `compile_check.py` 真编译 0 错 + 编辑器编译 0 错。
- 全量 EditMode 回归:被编辑器 Play 模式阻塞(TestRunner 限制:Play 中无法恢复场景),
  退出 Play 后需补跑——战斗改动不触碰任何被测逻辑(伤害公式/地牢生成/门触发),预期零回归。
- **Play 手感核对清单**(重点,需实玩):
  1. 按住方向左右攻击——攻击永远朝移动方向水平打出(鼠标不再影响近战方向);
  2. 快速连点左键:三段连贯(段1→段2→段3 重击),提前按不丢招;
  3. 段 1/2 出手瞬间有明显踏步追近;段 3 更重更长;
  4. 停顿 >0.3s 再按回到段 1;段 3 后直接接段 1(环回);
  5. 长按左键 >0.2s:当前段被打断进入蓄力(减速+发光),松手满蓄释放;
  6. 上下贴近敌人不同 y 站位:纵深带内(±~0.5)都能命中,不再"擦边打空";
  7. 敌人攻击/预警不受影响(敌人路径未动);弓/弩远程照旧(鼠标瞄准)。

## 已知边界(后续批次)

- 敌人仍用旧旋转矩形判定+8 向——敌人横向化待第二批;
- 段 3 无击退(伤害链无击退通道,避免动 TakeDamage 接口;后续若做击退需 IDamageable 扩展);
- 普通命中停帧/震动仍维持 v1.1.41 决策(无)——若手感仍"轻",可单独评估恢复轻停帧。

## 关联

v1.1.41(零震颤决策)、R7/R8(同源原则/三态攻击)、v1.1.47.1(石板素材索引)。
