# v1.1.36 小地图战争迷雾

## 目标

小地图不再于楼层生成完成时泄露整层结构。玩家只掌握已经探索的区域，以及从已探索区域下一步可以抵达的房间位置。

## 显示规则

- 已探索房间：显示房间真实类型颜色。
- 与任一已探索房间直接相连的未探索房间：仅显示统一暗灰色位置，不显示 Boss、商店、宝箱等类型。
- 距已探索区域两步及以上的房间：完全隐藏。
- 连线：仅当至少一端已经探索时显示；两个未知房间之间的连接隐藏。
- 出生房：楼层建立首帧即视为已探索，不等待 `RoomTrigger` 的下一次物理回调。
- 玩家进入新房时，由 `Room.OnRoomEntered` 事件增量刷新迷雾，不在 `Update` 中反复扫描地图。

## 实现位置

- `Assets/Scripts/now_use/UI/MinimapController.cs`

## 验收

1. 进入新楼层时，只能看到出生房及其一步相邻房。
2. 相邻未知房统一为暗灰色，不能提前判断房型。
3. 进入相邻房后，该房恢复真实颜色，并揭示它的一步相邻房。
4. 未与已探索区域直接相连的房间和道路始终不可见。
5. 切换楼层后，上一层探索集合被清空，新楼层从出生房重新揭示。
