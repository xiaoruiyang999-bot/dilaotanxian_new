# v1.1.33 狼人进图闪现战士首帧修复（初始化时序）

## 根因（用户定位）

时序链：`FrameAnimator.Awake()` 先按战士渲染首帧 → `RunManager.Start()` 后 `yield return null`
→ 下一帧才读 ChosenCharacter 调 `SetWerewolfVisual(true)`。中间隔一帧渲染 = 战士闪现。
`RunStateCarrier` 默认 Warrior 不是主因；改 Prefab 默认图为狼人会反向闪现（否决）。

## 修复

`FrameAnimator.Awake()` 尾部、首次渲染前：读 `RunStateCarrier.Ensure().ChosenCharacter`，
为 Werewolf 时立即 `SetWerewolfVisual(true)`（Awake 期安全：sprite 加载/`sr.bounds`/建视觉
子物体均无渲染依赖；`SetWerewolfVisual` 幂等——`on == werewolfMode` 早退，
RunManager/PrepRoomManager 后续调用自动 no-op）。武器/属性保持原延迟链路，只对齐视觉首帧。
首次游玩未选择时载体默认 Warrior → 战士首帧，语义不变。

## 验收

选狼人进地牢/准备房间：出场即狼人，无战士闪帧；选战士无变化；变身演出/即时改选（选择页
SetWerewolfVisual(false→true)）不受影响。

## 关联

v1.0.9 狼人系统 / RunStateCarrier 死亡保留语义不变。
