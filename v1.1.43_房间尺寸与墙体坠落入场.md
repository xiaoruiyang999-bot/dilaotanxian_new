# v1.1.43 房间尺寸与墙体坠落入场

## 房间尺寸唯一入口

`Assets/Data/Dungeon/DefaultDungeonConfig.asset` 的 `roomWidth / roomHeight` 是普通单格房间的内部瓦片尺寸，
不包含外墙。当前值为 30×18；Boss/Elite 的 `cellSpan` 会在此基础上扩展，不应直接修改 `DungeonBuilder.roomW/roomH` 缓存。

建议先试 36×22，再根据相机视野、敌人数与装饰密度联调；配置允许范围为 8～48。

## 墙体入场

- 每次 `DungeonBuilder.Build`（首层和 `NextFloor`）完成最终墙体后启动。
- 真实 Walls Tilemap 与 Collider 始终位于最终位置，只临时关闭 Renderer。
- 创建一张无 Collider 的 `WallDropVisual` 临时 Tilemap，复制最终墙格、颜色与矩阵。
- 每格使用 `seed + 坐标 hash` 得到确定性延迟，按 `wallDropStagger` 错峰下落。
- 单格使用轻微弹跳落地曲线；全部完成后恢复真实墙体渲染并销毁临时层。
- 切层或重建途中会先取消旧动画并恢复 Renderer，不遗留视觉层。

## 调参

统一在 `DefaultDungeonConfig`：

- `wallDropEnabled`：总开关；
- `wallDropHeight`：初始高度，默认 7；
- `wallDropDuration`：单块下落时间，默认 0.5 秒；
- `wallDropStagger`：整批错峰范围，默认 1.1 秒。

## 性能边界

没有为每块墙创建 GameObject/DOTween；整层只增加一张短生命周期 Tilemap和一个协程。
协程只在约 1.6 秒入场期更新尚未落地的格子，结束后零持续 Update 成本。
