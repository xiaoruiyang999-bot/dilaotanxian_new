# v1.1.41 地皮融入路径骨架 + 击杀震颤感彻底移除

## 需求（用户两点澄清）

1. **地皮 × 骨架**：支持骨架先行；地皮生成要与骨架更好接入——但**骨架不是土分布的根本生成思路**，
   方向是"**土斑尽量包含路径骨架**"（走路处倾向土径穿草，土斑仍以噪声为根本形态）；
2. **击杀震颤感仍在**：v1.1.34 只移除了震屏，停帧（HitStop 0.03，每次命中的时间冻结感）才是
   残留震颤的真身——**整体**移植到战士技能组（ExecuteFeedback 已含强震+停帧）。

## 实现

### 1. 地皮融入（土斑吞骨架）

- `RoomPlan` 暴露 `Skeleton` 集合（protect 集合中可行走格的快照）；
- `DungeonBuilder.PaintRoom` 收集各房骨架 → `skeletonCells` 合集（ClearAll 清）；
- `TerrainMask.ApplySkeletonBias(cells, seed, chance=0.8)`：骨架格 **80% 概率**置土
  （非强制——保留噪声土斑有机形态，骨架只是强吸引），随后重算距离场（装饰层同步）；
  偏置确定性（seed 派生），在开洞/定向之后、地面绘制之前应用。
- 生成序即用户方向：噪声先决定土斑 → 骨架被土斑"尽量吞入"。

### 2. 常规命中零震颤（震屏+停帧全移除）

`WeaponHitbox` 命中反馈只剩音效；停帧与震屏全部由 `ExecuteFeedback.Play`
（0.05/0.08 强震 + 0.08 停帧）承接，并只由战士裸绞在实际击杀后调用。
玩家受击震屏保留；Boss 死亡不再绕过技能组单独震屏或停帧。

## 验收

1. Play：地面呈现"土径沿走廊"的融入观感（骨架走向偏土），土斑仍是噪声有机形；
2. 常规攻击/击杀零震颤零停帧（只有音效）；仅裸绞击杀触发震颤；玩家受击震屏不变；
3. 同 seed 复现（偏置确定性）。

## 关联

v1.1.39 骨架 / v1.1.25 地皮逐格铺装 / v1.1.34 ExecuteFeedback。
