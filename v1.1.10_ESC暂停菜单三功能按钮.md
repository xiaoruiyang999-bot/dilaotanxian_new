# v1.1.10 ESC 暂停菜单三功能按钮

## 素材与导入

素材源：`E:\WeGameApps\The animation of Unity\狼人\主菜单按钮_三张`。

导入到 `Assets/UI/PauseMenu/` 作为母版，同时复制到 `Assets/Resources/UI/PauseMenu/` 供运行时代码加载。六份资源均配置为 Sprite Single、Bilinear、无 Mipmap、无纹理压缩、PPU 1。

## 功能接线

| 图片 | Esc 菜单功能 |
|---|---|
| `button_continue_game.png` | 关闭暂停面板并恢复游戏 |
| `button_settings.png` | 展开/收起右侧 SFX、BGM 音量设置区 |
| `button_back_lobby.png` | 清空本局武器并返回正式准备场景 `v0_7_PrepRoom` |

图片自身已经包含图标和文字，因此按钮上不再叠加 TMP 文本。资源断链时才回退为石板按钮加文字。

## 状态亮度

采用 Button ColorTint，不生成额外位图：

- Normal：RGB 0.88，静止时稍暗。
- Hover：RGB 1.00，恢复原图亮度。
- Pressed：RGB 0.65，按下时明显压暗。
- Selected：RGB 0.96。
- Disabled：RGB 0.45，Alpha 0.75。
- 过渡时间：0.08 秒。

按钮以 `520×110` 显示，接近原图比例并启用 `Image.preserveAspect`，避免拉伸和清晰度损失。
