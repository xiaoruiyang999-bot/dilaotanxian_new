# v0.6.0 小地图系统（第一版：常显缩略图）

> 状态：**代码已落盘 · 待 Unity 挂载 + 人工验收**
> 日期：2026-08-19
> 需求来源：方便查看角色所在的位置（《项目进度报告》v0.6 候选清单）
> 关联文档：[v0.5.4.4.4_战斗判定与场景恢复修复](v0.5.4.4.4_战斗判定与场景恢复修复.md)

---

## 1. 功能范围（第一版）

屏幕**右上角常显**的楼层缩略图：

- 全楼 8~12 个房间按真实布局等比缩放显示，**配色与编辑器 Gizmos 调试图完全一致**
  （Start 绿 / Combat 白 / Elite 橙 / Treasure 黄 / Shop 蓝 / Event 紫 / Boss 红）
- 房间之间的连线（`Layout.connections`）半透明白色
- **玩家白色标记点**实时跟随（LateUpdate 世界坐标 → 窗口映射）
- **当前所在房间白色描边高亮**（`Room.OnRoomEntered` 驱动，换层/重开后自动兜底）
- 左上角楼层角标 `F1 / F2 / ...`（`DungeonManager.FloorNumber`）
- 楼层切换（踩传送门）/ 死亡重开 → 自动整图重建（同一条 `OnGenerated` 事件链）

## 2. 技术方案

纯代码 UI，无 prefab / 无资产依赖（风格同 `PlayerWorldStatusBar`）：

- 渲染：Screen Space Overlay Canvas + `Image`（不设 Sprite = 纯色矩形，程序员美术）
- 数据：`DungeonManager.Rooms`（id → Room，透传自 `DungeonBuilder.Rooms`）读
  `Room.Bounds/Type`；`Layout.connections` 画连线；整层包围盒 fit 进窗口
- 重建时机：`DungeonManager.OnGenerated`（本次新增的唯一事件，在玩家传送 +
  相机 Snap 之后触发）。`RunManager.NextFloor / RestartRun` 都经由 `Generate()`
  → 一条事件链天然覆盖所有重建场景

## 3. 改动清单

| 文件 | 改动 |
|---|---|
| `Dungeon/Core/DungeonManager.cs` | +`event Action OnGenerated`（Generate 末尾触发）；+`Rooms` 只读透传属性 |
| `UI/MinimapController.cs` | **新增**（约 280 行），完整小地图实现 |

## 4. Unity 挂载步骤（唯一人工操作，约 30 秒）

1. 打开 `v0_5_Dungeon.unity`（若提示外部修改 → Reload）
2. Hierarchy 右键 → **Create Empty**，命名 `MinimapSystem`（建议放在 DungeonSystem 旁边）
3. Add Component → 搜索 **MinimapController** 挂上
4. Inspector 上 `DungeonManager` 字段**留空**即可（运行时自动查找），
   窗口尺寸/边距等参数有默认值，先不用调
5. Play 验收

> 注意：不要把 MinimapSystem 挂进 dungeonRoot 下——楼层切换时 dungeonRoot
> 会被整体清理，小地图也会跟着被销毁。放在场景根即可。

## 5. 验收清单

- [ ] Play 后右上角出现半透明地图窗口，全楼房间色块 + 连线 + `F1` 角标
- [ ] 白色玩家点位置与实际所在位置一致，移动时平滑跟随
- [ ] 走进任一房间，该房间出现白色描边
- [ ] 房间颜色与房型对应（绿=出生、红=Boss、黄=宝箱……与 Scene 视图 Gizmos 一致）
- [ ] 杀 Boss → 踩传送门进下一层 → 地图自动重画，角标变 `F2`，玩家点回到新出生房
- [ ] 玩家死亡 → 2 秒重开 → 地图重画回 `F1`
- [ ] Console 无新增报错/警告

## 6. 后续增强（阶段二，另行排期）

- 探索迷雾：未进入房间半透明（数据 `RoomState.Unvisited` 已就绪）
- 战斗状态：Active 房间红色脉动、Cleared 转暗
- TAB 键放大全图 / 图例说明
