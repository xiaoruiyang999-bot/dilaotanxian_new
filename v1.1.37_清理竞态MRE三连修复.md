# v1.1.37 修复：楼层清理竞态 MissingReferenceException（EnemyController / Door）

## 现象

楼层切换/重开清理（DungeonBuilder.ClearAll Destroy 全部生成物）的同一帧内，
残余的敌人 Update 与物理事件仍访问刚销毁的同类对象——
① `EnemyAI.UpdateVision/Update`：MissingReferenceException（EnemyController 已销毁）；
② 房间触发链路（RoomTrigger→Room.Enter→RefreshDoors）：MissingReferenceException（Door 已销毁）。

## 修复（三处生命周期守卫）

- **EnemyAI.Update**：入口 `this == null || gameObject == null` 拦截销毁残余帧；
  `health == null`（fake-null，同房清理）与 IsDead 一并拦截；UpdateVision 同款 this 守卫；
- **Room.RefreshDoors**：`doors.RemoveAll(d => d == null)`——死门引用**直接摘除**（原逻辑仅
  跳过，死引用留存列表，任何后续遍历路径都可能触达；摘除后根治）；
- **EnemyCombat 冲锋停撞射线**：`hit.transform != null` 守卫（射线缓冲命中本帧已销毁对象）。

三处与 v1.1.36（Projectile owner 销毁）、v1.1.35（寻路越界钳制）同族：
**清理竞态下一律"判空自愈，不向画面抛异常"**。

## 验收

连续换层（传送门过层）与重开本局：Console 无 MissingReferenceException；
敌人追击/关门开门/冲锋行为不变。

## 关联

R12（对象销毁时序）教训族；DungeonBuilder.ClearAll 为销毁源。
