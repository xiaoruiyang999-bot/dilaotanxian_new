# v1.1.53 狼人能量条与满能变身门禁

## 目标

- 狼人实际造成伤害时获得能量，默认 `能量 += 实际伤害 × 0.5`，上限 100。
- 未满能量时 T 不允许兽化；满能量后 T 进入兽化。
- 兽化后每秒消耗 10 点能量，耗尽自动退出；主动按 T 可提前退出。
- 重生时同时重置兽化状态与能量。

## UI 资源与运行态

资源统一放在 `Assets/Resources/UI/WerewolfEnergy/`：

- `frame_bar_large.png`：常态外框
- `fill_banner.png`：横向 Filled 填充
- `frame_bar_medium.png`：满能外框
- `emblem_head.png`、`emblem_wolfhead.png`：满能时交替张合的双头素材

运行时由 `WerewolfEnergyBar` 创建 `WorldUIRoot/WerewolfEnergyCanvas_Player`。空能量时只显示普通框；满能后隐藏 `NormalBar`，启用 `FullBar`、脉动并交替两颗头颅。切换回战士时组件与动态 Canvas 一并销毁。

## 验证

- `_dev/compile_check.py`：145 个脚本，0 错误。
- Unity 编译：0 errors；6 个既有 EditMode 未使用变量警告。
- UnitySkills 运行态：Canvas 和 7 个子对象创建成功，五张 Sprite 均绑定到对应 Image。
- Game View 截图完成，Console：0 errors。

## v1.1.53 UI 尺寸修正

- 普通/满能整体画布由 `96×30` 放大到 `150×36`，世界宽度由约 0.768 提升到 1.20。
- 普通 banner 填充限制在 large 框左侧槽体（X 2.5%~82%），不会穿入右侧银狼头。
- 满能 medium 框按原始 1093:192 比例使用 `132×23.2`，中心偏左 9px。
- 两颗 36×36 头颅统一位于右端 `x=57`，与 medium 右端重叠 18px 遮缝；二者锚点/轴心完全一致，交替时不跳位。

## 满能与兽化状态拆分

- 满能未兽化：`FillFull` 使用 `fill_banner` 且保持 100%，仅显示 `emblem_head`。
- `emblem_wolfhead` 在满能待机阶段保持隐藏。
- 进入兽化后：FullBar 持续显示，FillFull 随能量从 100% 下降；两颗狼头才按 0.35 秒间隔循环替换。
- 能量归零自动退出兽化后，切回普通 large 空条。
