# 资产需求总清单（Demo 全部资产 + 序列帧规范）

> 用途：用户按本清单**逐件提供资产**（做到哪给到哪）；每件标清需要哪些状态的序列帧、帧数、优先级。
> 美术方向见《Demo_美术计划书.md》：俯视逻辑 + 平面画风 Sprite + 原创非人形。

---

## 一、序列帧生产管线与省 token 策略（2026-07-29 定稿）

用户无骨骼动画，采用 **AI 生成视频 → 工具导出序列帧** 的生产方式。程序侧配套**批处理脚本**（写一次复用，后续每张图不再耗 AI token）：

```
AI 视频（纯色/绿幕背景）→ 导出序列帧 → 脚本①抽帧降频（每状态留 2~6 帧）
→ 脚本②批量抠图（chroma key 出透明底）→ 脚本③统一画布 + 脚底中心 pivot 对齐
→ 导入 Unity → FrameAnimator 帧切换器播放
```

**四条硬规矩**（不遵守接进来会抖/会穿帮）：
1. 视频背景必须纯色（绿幕最佳），方便批量抠图；
2. 每状态只要 2~6 帧，抽帧导出，别全量给（体积和 token 都省）；各表所列帧数均为规划参考值，**所有角色/敌人的实际帧数以用户提供的素材帧数为准**（2026-07-31 用户决策）；
3. 角色帧**空手不画武器**——武器走 WeaponPivot 挂载，攻击动效代码驱动；
4. 只画**一个方向**，左右朝向程序镜像翻转。

动效兜底（不受序列帧限制）：受击闪白、死亡淡出、待机呼吸、浮动、UI 动效等仍用 DOTween 代码补间（≈0 成本）。

---

## 二、角色（战士）

> 方向拆分（已确认）：移动/待机 = 正/侧/背三方向（侧向程序镜像出左右）；攻击 = 正面+侧面（背面镜像侧面兜底）；死亡/受击单方向。

| 状态 | 方向 | 每方向帧数 | 小计 |
|------|------|-----------|------|
| Idle 待机 | 正/侧/背 | 2 | 6 |
| Move 移动 | 正/侧/背 | 4 | 12 |
| Attack 普攻（长剑挥击·空挥） | 正/侧 | 3~4 | 6~8 |
| Attack 普攻（长枪戳击·空挥） | 正/侧 | 2~3 | 4~6 |
| 小技能 | 正（+侧【待补充】） | 3~5 | 3~10 |
| 大招 | 正（+侧【待补充】） | 4~6 | 4~12 |
| 受击 | 正 | 1（可省，闪白兜底） | 0~1 |
| 死亡 | 正 | 3 | 3 |

**小计：约 38~48 帧**（攻击全做背面的完整版 +9~12 帧，Demo 不推荐）

**配套（程序侧产出，用户零成本）**：每套攻击序列帧配**逐帧挂点表 JSON**——AI 读空挥序列帧，在起手/挥中/收势关键帧标定握点坐标+武器角度，中间帧程序插值；附 Editor gizmo 微调工具。用户提供空挥帧即可，无需标注任何点位。

## 三、敌人（6 只，全部非人形）

| 怪 | 状态需求 | 帧数 | 说明 |
|----|----------|------|------|
| 小怪 A 近战 | Idle/Move/Attack/Death | ≈12~13（2/4~5/3/3） | **敌人不分方向，单套** |
| 小怪 B 远程 | Idle/Move/蓄力发射/Death | ≈12~13（2/4~5/3/3） | 发射口部件要明显 |
| 小怪 C 冲撞 | Idle/Move/蓄力/冲撞/Death | ≈13~14（2/4~5/2/2/3） | 蓄力前倾+冲撞姿态 |
| 精英 A | **复用小怪 A 序列帧 + 程序换色 + 部件** | +1~2 张部件（角/金环） | 不重画 |
| 精英 B | 复用小怪 B 序列帧 + 程序换色 + 部件 | +1~2 张部件 | 不重画 |
| Boss | Idle/Move/招式×3~4/Death | ≈18~22（2/4~5/每招 3/3~4） | P2 形态程序换色+部件差异，招式以用户设计稿为准 |

**小计：约 57~66 帧（含精英部件图）**

## 四、武器与道具

| 资产 | 数量 | 说明 |
|------|------|------|
| 新手长剑（手持） | 1 张 | 静态持握图 |
| 新手长枪（手持） | 1 张 | 静态 |
| 武器技能图标 ×2 | 2 张 | 小图标 |
| 血包 / 甲包 / 魔力包 图标 | 3 张 | 小图标，掉落物也用同图 |
| 小技能/大招图标（含分支） | 2~4 张 | 视分支数【待补充】 |

**小计：8~10 张**（全部静态单帧）

## 五、场景与 UI

| 资产 | 数量 | 说明 |
|------|------|------|
| 大厅布局图/瓦片 | 【待补充】 | 可沿用现有染色瓦片，正式化后置 |
| 天赋 NPC | 1~2 张 | 占位可长期用 |
| 主 UI 四槽框 + 背包框 | 4~6 张 | 色块可兜底，正式化 P2 |
| Boss 血条框 | 1 张 | P2 |
| 结算界面（通关/死亡） | 1~2 张 | P2，文字面板可兜底 |
| 职业选择 UI 美化 | 1 张 | P2 |

**小计：必须 ≈2 张（NPC+槽框），其余 P2**

## 六、特效（尽量代码补间做，不出图）

| 特效 | 方式 | 需图 |
|------|------|------|
| 技能特效（旋风斩/突进等） | 色块+DOTween，正式化 P2 | 0~2 张 |
| 敌方弹幕 | 沿用程序化圆弹，正式化 1 张 | 0~1 张 |
| 命中/拾取/开箱 | 代码补间，现有 | 0 |
| Boss 转阶段演出 | 代码补间（震屏+变色） | 0 |

## 七、音频（待定）

音效/BGM 是否纳入 Demo【待补充】。若纳入：攻击/受击/拾取/开门/Boss/通关 约 8~10 个音效 + 2 首 BGM（大厅/地牢），可用免费素材站资源，不需原创。

---

## 八、汇总与交付节奏

| 优先级 | 内容 | 序列帧/图量 | 目标周 |
|--------|------|------------|--------|
| P0 | 战士（≈38~48 帧，含三方向）+ 小怪×3（≈37~40 帧） | ≈75~88 帧 | 第 1~2 周 |
| P1 | 精英部件（≈3 张）+ Boss（≈18~22 帧）+ 武器/道具/技能图标（≈8 张）+ NPC（1） | ≈30 帧/张 | 第 2~3 周 |
| P2 | UI 美化、特效图、瓦片 | ≈10 张 | 第 4 周 |

**全 Demo 序列帧总量估算：约 93~105 帧 + 静态图约 20 张。** 帧多不等于成本高——帧从视频批量导出，后处理全走脚本；AI token 只花在最初写批处理脚本上，之后零消耗。

**提供方式约定**：
1. 序列帧命名 `角色名_状态_帧号.png`（如 `Warrior_Move_1.png`），静态图 `物品名.png`；
2. 统一画布尺寸（56×56 或 64×64【待补充】定稿），脚底中心对齐（脚本可代做，但源视频构图尽量居中）；
3. 按"只"或"批"提供均可，程序侧收到即脚本处理并接入（占位→替换零代码改动）；
4. 剪影/设定稿先群里评审识别度，再去生成视频——视频返工比草图返工贵得多。

## 九、非美术资产（程序自行解决，无需用户提供）

- 占位色块/程序化特效：现有管线；
- 存档 JSON、天赋面板 UI、背包 UI：纯代码；
- 数值：见《数值与角色设计任务书.md》由设计 D 填。

---

## 十、token 成本热点标记（2026-07-29 核查）

> 按消耗分级，附对策。核心原则：**脚本写一次复用 ≈ 零边际成本；逐张读图/逐篇长文档才是大头**。

### 🔴 高消耗（必须按对策执行）

| # | 热点 | 为什么贵 | 对策 |
|---|------|----------|------|
| 1 | **攻击挂点标定**（v0.7.12 前/美术接入期） | AI 读图 = vision token，逐帧读 90~100 帧不可接受 | 每套攻击**只读 2~3 张关键帧**标定握点，中间帧程序插值；gizmo 工具人工微调兜底。预计读图 ≈12 次（战士 2 套普攻+技能） |
| 2 | **序列帧逐帧验收** | 同上，逐帧看图核对成本高 | 批处理脚本自动报异常（尺寸漂移/对齐超限/透明底缺失）；程序把每只怪的序列帧**拼成一张雪碧图预览**，AI 一次只看 1 张 |
| 3 | **AI 视频生成**（用户侧工具，非本 AI token，但是用户成本） | 视频生成/返工单价高 | 剪影评审通过再生成；每状态 2~6 帧短片段即可，别生成长视频 |

### 🟡 中消耗（控制次数即可）

| # | 热点 | 对策 |
|---|------|------|
| 4 | **子版本文档写作**：13 个子版本 ×（正式计划+操作记录） | 小子版本合并出文档（如 v0.7.8~7.10 敌人三合写一份框架+差异表）；操作记录只写改动/验收/遗留三段 |
| 5 | **每版开工代码勘察**（grep+读文件） | 沿用自检 §4 开工提示词模板圈定勘察范围；v0.7.0 已完成首轮全面勘察，后续版本只读增量 |
| 6 | **数值平衡迭代**（v0.7.12） | 用户 Play 中 Inspector 调定数值后一次性报值，AI 一次写入 SO；避免"改一轮看一轮"多轮往返 |

### 🟢 低消耗（写一次复用，无需控制）

- 序列帧批处理脚本（抽帧/抠图/对齐/导入 Unity）、FrameAnimator 帧切换器、挂点驱动、存档 JSON、天赋/背包 UI —— 全部纯代码一次性产出；
- 占位色块替换接入 —— Editor 脚本批处理，不逐张操作。
