# v1.1.46 房内小掩体岛候选评分生成

## 目标

把房内障碍墙从“围绕房心套固定长墙模板”改成更接近小型 Roguelite 战斗房的布局：

- 中心战斗区与门廊保持清晰；
- 以多个 2～4 格的小掩体岛提供走位和遮挡；
- 不贴外墙、不贴挖除轮廓、不过度连片；
- 每个可行走空间都能按玩家碰撞体口径进入；
- 仍由房间 seed 驱动，同 seed 完全复现，不同 seed 保留足够变化。

这里借鉴的是“短掩体、清晰战斗区、受约束随机”的关卡语言，不复制任何具体游戏房间。

## 本次找到的真实根因

1. `DungeonBuilder.PaintRoom` 先把整个内部铺成地板，再读取 `RoomPlan`。深层挖除格只是不再绘制，
   却没有撤掉已铺地板，因此挖空从未真正显示为虚空；挖除轮廓便像一条粘在外墙上的内部障碍墙。
2. 旧 `RoomShapeGenerator` 允许 30×18 房间挖出最大约 15×9 的角缺口，且遇到保护格时逐格跳过，
   会留下长 L、指状边缘和大块轮廓墙。
3. 7 套障碍模板全部围绕房心，仅偏移 ±1；许多旋转/镜像结果实际等价。20% 空房和逐段省略又使
   大量房间退化为无墙或单段墙，属于“随机参数很多、结果变化很少”。
4. `RoomPlanner` 在所有形状重试之间复用同一个 `protect`。每次失败形状的轮廓保护圈都会累积到
   下一次尝试，导致可放障碍空间越重试越小。
5. 旧增量放置在候选段与已接纳段重叠时，失败回滚会无条件 `Remove`，可能误删上一段已有格。
6. `TerrainMask.ApplySkeletonBias` 在 `PaintRoom` 收集骨架之前调用，`skeletonCells` 当时永远为空，
   “土径吸附骨架”实际上没有生效。

## 新生成管线

```text
小比例房形变化
    ↓
轮廓墙推导
    ↓
14 个完整小掩体候选（房间 seed）
    ↓
硬约束过滤
    ↓
格级连通 + 玩家净宽 2 格可达验证
    ↓
密度 / 象限覆盖 / 空间展开评分
    ↓
一次性提交最高分候选
```

### 房形

- 72% 保持完整矩形竞技场；
- 20% 尝试一个小角缺口，其余为 1～2 个小切角；
- 单缺口最大约 6×4，总挖除量不超过房间面积 12%；
- 形状碰到门廊/中心保护带时整体拒绝，不再逐格裁切成残片。

### 小掩体岛

| 规则 | 当前值 |
|---|---:|
| 每房候选数 | 14 |
| 真正空旷房概率 | 8% |
| 单岛尺寸 | 2～4 格 |
| 岛间最近格距离 | ≥3（中间至少 2 格地板） |
| 外墙净空 | 小房 2 格，常规/大房 3 格 |
| 挖除轮廓净空 | 2 格 |
| 中心净空 | 房心周围 7×5 |
| 障碍目标量 | 可走面积约 2.8%，限制为 6～26 格 |

形状池只包含短条、L 三连、小转角和 2×2 方块；不再生成切割整间房的长墙。35% 候选会尝试
中心对置的小岛，其余使用全安全区域均匀采样，因此既有一定设计感，又不会只在房心附近重复。

### 合法与好看的职责分离

- `RoomLayoutValidator` 只判定硬合法：门可达、全 free 格可达、玩家净宽 ≥2、单连通块 ≤4；
- `RoomObstaclePlanner` 负责提出完整候选；
- `RoomPlanner` 负责评分择优并一次提交。

这样不会把审美权重塞进连通性验证器，也消除了逐段贪心和回滚污染。

## 表现层修复

`DungeonBuilder.PaintRoom` 现在只按最终 `RoomPlan` 绘制一次：

- Walkable 才铺地；
- Outline/Obstacles 只铺墙并清除该格地板；
- 深层 Carved 同时清除地板、地面登记和墙，真正显示为虚空。

路径骨架收集完成后才调用 `TerrainMask.ApplySkeletonBias`，地皮偏置恢复生效。

## 内容生成与最终布局同源

复审发现旧 `SpawnPositionHelper` 仍只在矩形 `Room.Bounds` 中随机取点。这样即使布局验证通过，
敌人、奖励、装饰和第二波增援仍可能落入 Carved 空洞；建图同帧时 TilemapCollider 批量更新尚未
提交，物理重叠也可能暂时漏掉刚生成的房内墙。

本次把内容落点也纳入 `RoomPlan`：

- `SpawnCells` 仅收录自身及周围 8 格均为无墙可走地面的格子；
- `DungeonBuilder` 把每房白名单交给 `Room`，所有既有 Spawner 继续统一调用
  `SpawnPositionHelper`，无需各自复制地图规则；
- 落点取格心并在 X/Y 各做 ±0.2 格 seed 抖动，保留自然随机感；
- 白名单是第一道数据门禁，距门 ≥2.5 格与 NonAlloc 物理重叠是第二道门禁；
- 内容生成前显式 `ProcessTilemapChanges` 并 `Physics2D.SyncTransforms`，消除同帧碰撞器迟到窗口；
- 正式地牢若白名单为空则少生成、不回退矩形乱放；只有旧手工测试房没有布局数据时才走兼容取样。

## 自动验收

`RoomLayoutTests` 已扩到真实项目尺寸：30×18、横向 Elite 61×18、纵向 Elite 30×37，另保留
18×11 小房回归。测试覆盖：

- 640 个随机门/尺寸组合中，所有 free 格和门都按玩家口径可达；
- 同 seed 的 Walkable / Outline / Obstacles / Skeleton 全部一致；
- 同 seed 的 SpawnCells 顺序一致，且每个内容格的 3×3 邻域均无空洞/墙；
- 障碍不贴外墙、不贴轮廓、不进中心区；
- 单岛 2～4 格，岛间至少两格空地；
- 房形挖除 ≤12%，轮廓墙数量受限；
- 180 seed 的密度、非空率和唯一布局数门禁。

实测结果：

| 房间尺寸 | 非空房 | 平均障碍格 | 唯一布局 / 180 |
|---|---:|---:|---:|
| 30×18 | 92% | 13.9 | 166 |
| 61×18 | 92% | 23.8 | 166 |
| 30×37 | 93% | 24.2 | 168 |

- Unity Roslyn 编译：0 错误、0 警告；
- 全量 EditMode：33/33 通过。

## 后续调参入口

主要参数集中在 `RoomObstaclePlanner` 顶部。若实机试玩后觉得太空或太密，优先只调：

1. `TargetObstacleCells` 的 2.8% 密度；
2. `EmptyRoomChance`；
3. `OuterWallClearance`；
4. 形状池权重/成员。

不要再通过增加长墙模板或放宽连通验证来调观感；那会重新引入大片粘连和不可进入区域。
