# v0.5.1 房间流程系统 操作报告

> 本文档记录 v0.5.1 的实际执行内容与结果，对应《v0.5 Dungeon 系统开发计划书》五-B 节。
> 前置版本：v0.5.0（随机地图生成基础，见 `v0.5.0操作.md`）。

## 一、版本目标

建立完整的**房间流程系统**：房间状态机（未进入 → 战斗中 → 已完成）、进房关门/清房开门、敌人休眠/唤醒。为后续所有房间玩法（战斗、精英、宝箱、Boss）提供统一流程控制。

本阶段敌人由**调试摆敌**放入房间测试（运行时等价于手工摆放），真正的随机生成属 v0.5.2。

进房流程（计划书五-B）：

```
玩家走入房间（RoomTrigger）
  → Room.Enter()：仅 Unvisited 响应
    → state = Active，广播 OnRoomEntered
    → clearCondition == None → 直接 Cleared（门一直开着）
    → clearCondition == AllEnemiesDead →
        唤醒敌人（解除休眠）
        关闭所有 Door
        登记敌人全灭 → Cleared：门全部打开，广播 OnRoomCleared
```

## 二、核心设计

### 2.1 职责解耦

| 模块 | 职责 |
|---|---|
| **RoomState / RoomClearCondition** | 枚举：Unvisited/Active/Cleared；None/AllEnemiesDead（v0.5.3 起按类型由 RoomTypeConfig 数据驱动） |
| **Room** | 状态机唯一真源；持有门列表、ContentRoot、清房条件；`RegisterEnemy` 订阅 `EnemyHealth.OnDeath` 计数；事件 `OnRoomEntered`/`OnRoomCleared`（C# Action，风格同 WeaponHitbox.OnHit） |
| **Door** | 碰撞体 + 色块视觉；`RefreshState()` 重算开关——**仅当相邻两个 Room 都不是 Active 时才开**（门状态由两侧房间推导，Door 不自己决定） |
| **RoomTrigger** | Trigger 覆盖房间内部（**四边内缩 0.5 格**，保证玩家完全进房后才触发，防关门夹人）；只认 Player tag |
| **DungeonBuilder** | 建房间时一并创建 ContentRoot / RoomTrigger；每条连接在门洞矩形处实例化 Door 并登记到两侧 Room |
| **DungeonManager** | 调试摆敌字段（v0.5.2 移除）：往距出生房最近的 N 个战斗房各放 M 个敌人并 `RegisterEnemy` |

### 2.2 关键设计约束

- **休眠制（本版最重要的决策）**：未进房的敌人**可见但不动**——禁用 `EnemyAI`/`EnemyCombat` + 刚体 `FreezeAll`，精灵与碰撞保持原样。玩家能透过门洞看到房内敌人再决定进不进；敌人不会隔墙索敌；禁用组件零每帧开销。
- **Room 不认识 EnemyAI**：只认「注册了若干个会死的东西」（OnDeath 计数），敌人脚本零改动。
- **门状态推导制**：Room 状态变化时调 `RefreshDoors()`，Door 自己重算——避免「隔壁 Active 时门被误开」的竞态。
- **清房条件可配置**：None（无战斗房，进房即完成）/ AllEnemiesDead（杀光才完成）；空战斗房防御分支——0 敌人直接完成。
- **调试摆敌是临时脚手架**：`debugPopulateEnemies` 等 4 个 Inspector 字段，v0.5.2 由 EnemySpawner 取代后移除。

### 2.3 运行时对象结构

```
DungeonSystem/DungeonRoot
├── Room_1_Combat（Room）
│   ├── ContentRoot（始终 active，纯挂载点）
│   │   └── Enemy_Debug_1_0 / _1（休眠：AI/Combat 禁用 + 刚体冻结，可见）
│   └── RoomTrigger（BoxCollider2D isTrigger，内缩 0.5）
├── Door_1_0（Door + SpriteRenderer 棕色块 + BoxCollider2D）
└── ...
```

## 三、新增文件

### 3.1 脚本（`Assets/Scripts/v0_5/`）

| 文件 | 说明 |
|---|---|
| `Runtime/RoomState.cs` | `RoomState` + `RoomClearCondition` 两个枚举 |
| `Runtime/Room.cs` | 房间完整版：`Init(id, type, bounds, clearCondition, contentRoot)`、`Enter()`、`RegisterEnemy()`/`NotifyEnemyDied()`、`SetEnemyDormant()` 休眠开关、`[ContextMenu] Debug Kill All Enemies`（验收/调试用） |
| `Runtime/Door.cs` | `Init(两侧Room, 门洞尺寸)`、`RefreshState()`；关门 = 碰撞体启用 + 色块显示 |
| `Runtime/RoomTrigger.cs` | `OnTriggerEnter2D` 过滤 Player tag → `room.Enter()` |

### 3.2 Prefab

- `Assets/Prefabs/Dungeon/Door.prefab`：SpriteRenderer（WhiteSquare 染棕色，sortingOrder=2）+ BoxCollider2D + Door 脚本（visual/solidCollider 自引用已接）

## 四、修改文件

### 4.1 `Assets/Scripts/v0_5/Core/DungeonBuilder.cs`

1. 新增 `doorPrefab` 序列化引用；
2. `Build()` 流程改为：画房间 → 开门洞（记录门洞矩形）→ 建 Room → 建 Door（Door 需要两侧 Room 已存在）；
3. `CreateRoomObject()`：Room 升级完整版（Init 带清房条件与 ContentRoot），新增 ContentRoot 与 RoomTrigger 子对象；清房条件映射 `Combat/Boss → AllEnemiesDead，其余 → None`（v0.5.3 移交 RoomTypeConfig）；
4. ContentRoot **始终 active**（休眠由 Room 逐敌人禁用实现，见 6.3）。

### 4.2 `Assets/Scripts/v0_5/Core/DungeonManager.cs`

新增「v0.5.1 调试摆敌」4 字段（开关/敌人 prefab/房间数/每房数量）与 `DebugPopulateEnemies()`：按 distanceFromStart 排序取最近 N 个战斗房，每房放 M 个敌人挂在 ContentRoot 下并 `RegisterEnemy`。

### 4.3 `Assets/Prefabs/Enemy.prefab`

删除 AttackIndicator 子物体上的 **SpriteRenderer**（组件块、组件列表条目、脚本序列化引用置空三处）——修复预警扇形不显示 + 两条警告（详见 6.2）。**该修复对 v0_4 场景同样生效**（同一 prefab）。

## 五、未修改文件

以下保持原样，零改动：

- Player 侧全部脚本（PlayerController / PlayerStats / Health / PlayerCombat / PlayerAimController / PlayerUI / PlayerWorldStatusBar）
- Enemy 侧全部脚本（EnemyController / EnemyStats / EnemyHealth / EnemyAI / EnemyCombat / TrainingDummy）
- 武器攻击框架（AttackData / WeaponController / WeaponAnimator / WeaponHitbox / AttackIndicator / AttackQuery）
- `Player.prefab`、两份 AttackData 资产
- 物理碰撞矩阵（2D/3D 均全开，已核查）、层定义、Input Actions、CameraFollow

## 六、遇到的问题与修复

### 6.1 Door 碰撞尺寸漂移

**现象**：验收时 Door 的 BoxCollider2D.size 读数为 `(0.0001, 0.0001)`，关门后碰撞几乎不存在。

**修复**：`Door.Init()` 强制 `solidCollider.size = Vector2.one`（随 transform scale 铺满门洞），不再信任 prefab 上的尺寸。

### 6.2 预警扇形消失 + 两条警告（根因链）

**现象**：敌人攻击时没有黄色扇形预警；Console 每个敌人刷两条警告（`初始化失败，回退到 SpriteRenderer` + `Can't add component 'MeshFilter'...conflicts with SpriteRenderer`）。

**根因**：Enemy.prefab 的 AttackIndicator 子物体上挂有 SpriteRenderer（无 MeshFilter）。运行时脚本 AddComponent<MeshFilter> 被互斥拒绝 → 走回退分支；但回退分支的显示逻辑按「MeshRenderer 非空」又走了 Mesh 路径 → 渲染空网格 → 扇形彻底消失。

**修复**：prefab 删除 SpriteRenderer 三处（见 4.3），Mesh 主路径直接生效，扇形恢复、警告归零。**警告的实际影响**：不是噪音——扇形不渲染 + 每敌人刷日志掩盖真问题。

### 6.3 休眠实现变更（验收反馈驱动，最重要的设计调整）

**现象**：用户验收反馈——未进房时看不到隔壁房间里的敌人，进房关门后敌人才「凭空出现」。

**原因**：v0.5.0 计划/初版实现用 `contentRoot.SetActive(false)` 整体隐藏敌人。

**修复**：休眠改为**逐敌人禁用 `EnemyAI`/`EnemyCombat` + 刚体 FreezeAll**——敌人可见（精灵、碰撞保留、可被武器打到）但不动不攻击；进房瞬间唤醒。ContentRoot 改为始终 active 的纯挂载点。计划书 9.1 休眠制表述已同步更新。

### 6.4 REST 验收伪影（非游戏 bug，记录备查）

**现象**：REST 传送玩家（transform/rb.position）进房间不触发 OnTriggerEnter2D。

**原因**：刚体睡眠（sleepMode 下静止数秒后入睡）+ PlayerController 每帧覆写速度，瞬移唤醒不了睡眠刚体。

**验收手法**：临时禁用 PlayerController → `linearVelocity` 注入推动玩家 → 触发正常。**真实走路（WASD）完全无此问题**，用户人工验收确认。

### 6.5 编辑器崩溃与 VSCode 覆盖（流程事件）

- v0.5.1 编译期 Unity 崩溃重启一次，REST 服务器被模态对话框阻塞，手动处理后恢复，无数据损失。
- VSCode 未关闭已改脚本，保存时旧缓冲区覆盖回滚了 DungeonBuilder.cs 一处修改 → 已重新落盘，并约定**AI 改完代码后先在 VSCode 执行 Revert File 再保存**。

## 七、验证清单

### 自动化验收（已通过 ✅）

- [x] 编译 0 Error / 0 Warning（含 AttackIndicator 警告归零）
- [x] 进战斗房 → 三扇门碰撞体启用 + 棕色色块封门（截图 `Assets/Screenshots/v051_room_active.png`）
- [x] 休眠态：敌人 active（可见）+ EnemyAI/EnemyCombat disabled + 刚体 FreezeAll
- [x] 进房唤醒：EnemyAI 恢复 enabled、门开关联动正确（仅当两侧房间都非 Active 才开）
- [x] 无战斗房/空战斗房：进房自动 Cleared，门常开
- [x] Validate 1000 Seeds 回归不受影响（生成逻辑未动）

### 人工验收（用户已通过 ✅）

- [x] 透过门洞能看到隔壁房敌人站着不动
- [x] 进房关门 + 敌人瞬间激活追击
- [x] 敌人 Windup 黄色扇形预警恢复显示，Console 无 AttackIndicator 警告
- [x] 杀光开门；再进不关门
- [x] 休眠敌人不隔墙索敌
- [x] 战斗手感回归（命中/扣血/护甲/UI 与 v0.4.6 一致）

## 八、扩展预留（已为后续版本埋好的接口）

| 预留 | 用途 |
|---|---|
| `Room.RegisterEnemy()` 入口 | v0.5.2 EnemySpawner 直接复用：生成即登记，登记即休眠 |
| `RoomClearCondition` 枚举 + `ConditionFor()` 映射点 | v0.5.3 移交 RoomTypeConfig 数据驱动 |
| `Room.OnRoomCleared` 事件 | v0.5.4 Boss 房结算（奖励 + 传送门生成）监听点 |
| `ContentRoot` 纯挂载点 | v0.5.2 敌人/障碍物/装饰统一挂载；v0.5.4 Cleanup 一键清理 |
| `Room.Debug Kill All Enemies`（ContextMenu） | 后续版本验收/调试的通用工具 |
| DungeonManager 调试摆敌 4 字段 | v0.5.2 EnemySpawner 上线后移除 |

## 九、后续工作

- **v0.5.2 随机内容生成**（计划书五-C）：`SpawnTable`/`RoomContentProfile` SO（权重表驱动）、`EnemySpawner`/`ObstacleSpawner`/`DecorationSpawner`、生成位置合法性规则（距门 ≥2.5 格、防重叠、重试上限）、房间子 seed 派生（同 seed 下地图与内容完全一致）。
- 开工时移除 DungeonManager 的调试摆敌字段，由 EnemySpawner 正式接管内容生成。
