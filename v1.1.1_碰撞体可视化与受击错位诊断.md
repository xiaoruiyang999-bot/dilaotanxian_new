# v1.1.1 碰撞体可视化工具与"上半身不受击"诊断

## 1. 问题现象

游玩中发现：敌人身体已经贴住角色上半身，角色却不掉血。怀疑"碰撞体之外还有一层真正的识别层没有与碰撞体重合"。

## 2. 根因（代码取证，已闭环）

**结论：不存在第二层"识别层"。** 玩家唯一能被攻击判定识别的碰撞体就是根物体上的 `CapsuleCollider2D`（非 Trigger、Default 层、`Health`(IDamageable) 同 GameObject）。敌人近战 `EnemyMeleeAttackData.targetLayer = Default|Obstacle`（m_Bits 129）包含玩家所在层——**层与组件接线都没有问题，错的是几何**：

| 对象 | 世界几何（根缩放 0.3618 已乘入） |
|---|---|
| 视觉精灵（256px@50PPU，居中于根） | 1.85×1.85，跨 y ∈ [−0.93, +0.93] |
| 玩家胶囊 offset(−0.19, **2.5**) size(3.65, 4.39) | 跨 y ∈ [**+0.11, +1.70**]，x ∈ [−0.73, +0.59] |
| 敌人武器判定盒（WeaponPivot 在敌根） | 长 1.5 × 宽≈0.2，贴根平面：y ∈ [−0.1, +0.1] |
| 敌方远程弹（EnemyBolt 半径 0.1，根高度发射） | 最高触到 y = +0.10 |

三个派生后果：

1. **横向挥击永远差 0.01 打不中**：胶囊底部在根上方 +0.11，武器判定带只到 ±0.10——敌人从左右两侧攻击，判定盒从胶囊底下穿过去（`WeaponHitbox.Tick` 的 OverlapBox 查不到胶囊 = 无伤害）。
2. **北侧敌人根本不出手**：胶囊顶到 +1.70、敌人盒半高 0.5 → 北侧物理接触时根距 ≈ 2.2，而 `EnemyCombat.IsInAttackRange` 要求根距 ≤ 1.5+0.3=1.8 → 敌人被"看不见的墙"挡在攻击距离外，只会一直顶人。
3. **南侧（下方）接近能命中**：胶囊底 +0.11 与敌盒顶衔接，攻击盒纵向扫进胶囊范围——这是"有时能掉血"的原因，也验证了上述模型。

视觉层面同理：胶囊只盖住精灵上半身（y +0.11~+0.93），头顶上方 0.77 是"空气碰撞体"，腿部 56% 视觉区域无碰撞——敌人可以视觉上踩进角色下半身。

## 3. 新工具：ColliderOutlineDebug（本版交付）

`Assets/Scripts/now_use/Debug/ColliderOutlineDebug.cs`，**编辑器专用**（整文件 `#if UNITY_EDITOR`，打包自动剔除），Play 时自装到隐藏常驻对象，不改场景/Prefab。

- **红** = 实体碰撞体（攻击判定的真实受击层）
- **黄** = Trigger（逻辑探测层——敌人半径 5 的感知圈会以黄色现形；`WeaponHitbox` 判定一律跳过 Trigger）
- **橙** = 挥击中的武器判定盒（实时，与 `Tick` 同一计算源）
- **F9** 开关；Player/Enemy/Boss 碰撞体带名称标签；墙体 Tilemap/Composite 不画（噪音）
- 移除方式：删文件即可，无任何残留

配套改造：`WeaponHitbox` 把 Tick 内的判定盒计算抽为 `ComputeSwingBox`，并公开只读 `TryGetSwingBox(out center, out size, out angle)`——可视化消费同一份计算（同源原则），`Tick` 行为逐行等价未变。

## 4. 修复建议（未实施，待拍板）

**方案 A（推荐）：胶囊下沉居中。** Player.prefab 本体（走 `prefab_apply_overrides` 路径，勿在场景实例上改，R1）把 `CapsuleCollider2D` 的 offset 从 (−0.19, 2.5) 改为约 **(0, 0)~(0, 0.5)**（局部值）：
- 世界中心回到根附近 → 与敌人攻击带（±0.1）、远程弹道（+0.10）相交，横向/纵向攻击都能命中；
- 北侧物理接触根距降到 ≈1.16 < 1.8，北侧敌人恢复出手；
- 狼人不受影响（变身只放大 WeaponPivot，不动胶囊）。
- 改完开 F9 验收：红胶囊罩住身体中部、与橙色挥击带重叠。

**方案 B（可选加做）：** 视觉全身受击——胶囊 size 放大到覆盖精灵 ±0.93（约 5.2 局部高度）。取舍：受击面变大变"公平"，但拥挤走廊更容易被卡；建议先 A 后试手感。

## 5. 红线遵守记录

- 同源原则：挥击盒几何唯一真源 = `WeaponHitbox.ComputeSwingBox`（Tick 与可视化共用）；预警链路未触碰。
- R1/R4：零场景/Prefab 改动，工具运行时自建，无序列化残留；`#if UNITY_EDITOR` 保证构建零影响。
- 调试规范：未动相机/玩家；常驻调试对象隐藏且不可序列化。
- EditMode 门禁：改动为纯新增属性 + 等价重构，不影响既有测试断言。
