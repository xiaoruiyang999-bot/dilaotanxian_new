# v1.1.11 继续游戏按键功能重写（暂停面板）

## 现象

用户报告 ESC 暂停菜单"继续游戏"按钮功能丢失（点击无效或游戏未恢复）。

## 处理（PausePanel.cs）

1. **独立 `ContinueGame()` 方法**替代直接绑 `Close`：执行 Close 后输出验证日志
   （面板是否销毁 / timeScale / SuppressByUI 终值），运行时一眼判定接线与状态复位是否成功。
2. **`Close()` 防御性加固**：
   - `settingsRoot` / `menuButtonsRoot` 随 panelRoot 销毁同步置空，杜绝悬挂引用
     （旧版 Close 后 ToggleSettings 仍可能操作已销毁对象）；
   - `Time.timeScale = 1f` 强制复位并注释明确语义：无论 HitStop 时停协程处于何种状态，
     继续游戏必须恢复时间轴（防"面板已关但游戏仍冻结"）。
3. 接线保持 v1.1.10 模式：`CreateMenuButton(..., ContinueGame)`，按钮图 `button_continue_game.png`。

## 排查记录（静态取证）

- 按钮点击链 `Button.onClick → onClick?.Invoke() → ContinueGame → Close` 完整；
- `HitStop`（SuppressByUI + realtime 计时恢复）与暂停交互自洽：暂停期间 Request 被抑制、
  恢复仅在非抑制且 timeScale==0 时触发，不存在"关面板后又被拉回 0"路径；
- 若改后仍不生效，优先怀疑 **Play 中改代码未热生效**（R14：editor_stop → 等编译 → 再 Play），
  或 Console 中 ContinueGame 日志缺失（= 旧代码实例在跑 / EventSystem 异常）。

## 验收

1. 编译无错 → Play 地牢 → Esc 开暂停 → 点"继续游戏"：
   - 面板消失、游戏立即恢复行动；
   - Console 出现 `[Pause] 继续游戏：面板已销毁，timeScale=1，SuppressByUI=False`。
2. 连点继续/反复开关暂停无异常；设置展开状态下点继续同样正常关闭。
