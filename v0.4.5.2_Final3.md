# v0.4.5.2 执行计划（Final3）

> 版本：v0.4.5.2 Final3
>
> 类型：攻击动画框架重构执行计划
>
> 目标：通过新增 `PlayerAimController` 输入层，明确 `WeaponController` 与 `WeaponAnimator` 对 `WeaponPivot` 的权责边界，彻底解决 Transform 控制权冲突；统一 Player 与 Enemy 的攻击动画框架；为 v0.5 技能系统、Boss、武器切换预留扩展接口。

---

## 一、当前问题分析

### 问题 1：WeaponPivot 旋转控制权边界仍不清晰

在 Final2 计划中：

- `WeaponController` 在普通状态修改 `WeaponPivot.localRotation`。
- `WeaponAnimator` 在攻击期间通过 Tween 修改 `WeaponPivot.localRotation`。

虽然时间上错开，但职责边界未显式约定：

- 攻击过程中若 `WeaponController` 仍每帧更新朝向，会覆盖 Tween。
- 攻击结束后 `WeaponAnimator.Stop()` 只 Kill Tween，`WeaponController` 恢复朝向的时机不明确。
- 多个脚本仍可能同时认为自己拥有 `WeaponPivot` 控制权。

### 问题 2：PlayerCombat 仍读取鼠标输入

`PlayerCombat` 作为攻击状态机，不应关心输入来源。

- 当前 `PlayerCombat` 需要读取鼠标位置计算瞄准方向。
- 未来支持手柄、自动瞄准、网络同步时，`PlayerCombat` 会被迫修改。

应新增独立输入层：`PlayerAimController`。

### 问题 3：FullCircle 360° 旋转实现不可靠

`aimAngle → aimAngle + 360` 在 DOTween 中可能被优化为“无变化”。

正确方式：

- 从当前实际角度开始。
- 目标角度 = 当前角度 + 360。
- 使用 `RotateMode.FastBeyond360`。

### 问题 4：攻击判定与动画表现数据来源不统一

当前攻击判定使用 `AttackRange` + `AttackAngle` 做扇形判断，但动画仅依赖 `AttackAngle`。

未来需要支持：

- 圆形范围攻击（旋转斩）。
- 矩形突刺。
- 不同形状对应不同动画。

应在 `AttackData` 中新增 `AttackShape`，让判定与动画来自同一份数据。

---

## 二、Final3 最终架构

```
Input Layer
    │
    ▼
PlayerAimController     EnemyAI（自身计算目标方向）
    │                           │
    └───────────┬───────────────┘
                ▼
        WeaponController
     （普通状态：管理 WeaponPivot / WeaponSprite）
     （攻击状态：计算起止角度、锁定方向）
                │
                ▼
        WeaponAnimator
     （攻击期间：唯一修改 WeaponPivot.localRotation）
                │
                ▼
        WeaponPivot / WeaponSprite
                │
                ▼
        PlayerCombat / EnemyCombat
             （命中判定与伤害）
```

简化视图：

```
AttackData
    │
    ▼
PlayerAimController / EnemyAI
    │
    ▼
WeaponController
    │
    ▼
WeaponAnimator
    │
    ▼
WeaponPivot / WeaponSprite
```

---

## 三、脚本职责表

| 脚本 | 负责 | 不负责 |
|---|---|---|
| **AttackData** | 攻击参数配置：`AttackShape`、`AttackRange`、`AttackAngle`、`AnimationType`、`Damage`、`Cooldown`、`TargetLayer`、`Windup`、`Active`、`Recovery`、`Ease`、`ActiveMomentRatio` | 动画计算、运行逻辑、Transform 操作 |
| **PlayerAimController** | 读取鼠标/手柄/自动瞄准输入，输出 `AimDirection` | 攻击状态、伤害、WeaponPivot 操作 |
| **PlayerCombat** | 玩家攻击状态机（Windup/Active/Recovery）、命中判定、伤害、调用 `WeaponController` / `WeaponAnimator` | 鼠标输入、WeaponPivot 操作、Sprite 缩放 |
| **EnemyAI** | 敌人攻击状态机、计算朝向玩家方向、调用 `WeaponController` / `WeaponAnimator`、调用 `EnemyCombat` | WeaponPivot 操作、Sprite 缩放、Tween |
| **WeaponController** | 普通状态 WeaponPivot 朝向、攻击方向锁定、攻击起止角度计算、WeaponSprite 长度/位置/宽度 | Tween、状态机、伤害、输入读取 |
| **WeaponAnimator** | 攻击期间唯一修改 `WeaponPivot.localRotation` 的 Tween 播放 | 攻击逻辑、方向计算、Sprite 缩放、AttackData 读取 |
| **EnemyCombat** | 冷却、范围判定、伤害 | 动画、朝向 |
| **PlayerController** | 角色移动、翻转 | 武器朝向、攻击逻辑 |

---

## 四、新增文件列表

1. **`Assets/Scripts/v0_4/PlayerAimController.cs`**
   - 玩家瞄准输入层。
   - 输出 `Vector2 AimDirection`。

2. **`Assets/Scripts/v0_4/WeaponController.cs`**
   - 武器控制中间层。
   - `WeaponPivot` 旋转与 `WeaponSprite` 视觉的唯一管理者。

---

## 五、修改文件列表

1. **`Assets/Scripts/v0_4/AttackData.cs`**
   - 新增 `AttackShape` 枚举。
   - 新增 `attackShape` 字段。

2. **`Assets/Scripts/v0_4/WeaponAnimator.cs`**
   - 改为纯本地旋转 Tween 播放器。
   - `Play()` 接收当前起始角度。
   - `Stop()` 只 Kill Tween。

3. **`Assets/Scripts/v0_3/PlayerCombat.cs`**
   - 删除鼠标输入读取。
   - 删除 `weaponPivot` / `weaponSprite` 引用。
   - 新增 `PlayerAimController` 与 `WeaponController` 引用。

4. **`Assets/Scripts/v0_4/EnemyAI.cs`**
   - 删除 `weaponPivot` 直接引用。
   - 新增 `WeaponController` 引用。
   - 攻击流程通过 `WeaponController` / `WeaponAnimator` 进行。

5. **`Assets/Prefabs/Player.prefab`**
   - 新增 `PlayerAimController` 组件。
   - 新增 `WeaponController` 组件。
   - `PlayerCombat` 引用 `PlayerAimController` 与 `WeaponController`。
   - `WeaponAnimator` 仅保留 `weaponPivot`。

6. **`Assets/Prefabs/Enemy.prefab`**
   - 新增 `WeaponController` 组件。
   - `EnemyAI` 引用 `WeaponController`。

7. **`Assets/Data/PlayerAttackData.asset`**
   - `attackShape = Fan`
   - `animationType = Arc`
   - `attackAngle = 90`
   - `attackRange = 1.5`

8. **`Assets/Data/EnemyMeleeAttackData.asset`**
   - `attackShape = Circle`
   - `animationType = FullCircle`
   - `attackRange = 1.2`

---

## 六、AttackData 最终结构

```csharp
public enum AttackShape
{
    Fan,      // 扇形：使用 AttackRange + AttackAngle
    Circle,   // 圆形：仅使用 AttackRange
    Box       // 矩形：预留，暂不实现
}

public enum AttackAnimationType
{
    Arc,         // 扇形挥砍
    FullCircle,  // 360° 旋转
    Thrust,      // 突刺，预留
    Spin,        // 多圈旋转，预留
    Throw        // 投掷，预留
}

[CreateAssetMenu(fileName = "NewAttackData", menuName = "Combat/Attack Data")]
public class AttackData : ScriptableObject
{
    [Header("攻击形状")]
    [SerializeField] private AttackShape attackShape = AttackShape.Fan;
    public AttackShape AttackShape => attackShape;

    [Header("攻击范围")]
    [SerializeField] private float attackRange = 1.5f;
    public float AttackRange => attackRange;

    [SerializeField] private float attackAngle = 90f;
    public float AttackAngle => attackAngle;

    [Header("动画类型")]
    [SerializeField] private AttackAnimationType animationType = AttackAnimationType.Arc;
    public AttackAnimationType AnimationType => animationType;

    [Header("阶段时长")]
    [SerializeField] private float windupTime = 0.2f;
    public float WindupTime => windupTime;

    [SerializeField] private float activeDuration = 0.15f;
    public float ActiveDuration => activeDuration;

    [SerializeField] private float recoveryTime = 0.25f;
    public float RecoveryTime => recoveryTime;

    [SerializeField] private float attackCooldown = 0.5f;
    public float AttackCooldown => attackCooldown;

    [Header("命中与伤害")]
    [SerializeField] private int attackDamage = 10;
    public int AttackDamage => attackDamage;

    [SerializeField][Range(0f, 1f)] private float activeMomentRatio = 0.5f;
    public float ActiveMomentRatio => activeMomentRatio;

    [SerializeField] private LayerMask targetLayer;
    public LayerMask TargetLayer => targetLayer;

    [Header("动画曲线")]
    [SerializeField] private Ease attackEase = Ease.OutQuad;
    public Ease AttackEase => attackEase;
}
```

---

## 七、PlayerAimController 设计

### 职责

- 读取输入（鼠标、手柄、自动瞄准）。
- 输出标准化 `AimDirection`。
- 不控制武器、不参与攻击状态。

### 接口

```csharp
public class PlayerAimController : MonoBehaviour
{
    public Vector2 AimDirection { get; private set; }

    private void Update()
    {
        // 当前版本仅支持鼠标
        Vector2 mouseWorld = Camera.main.ScreenToWorldPoint(Input.mousePosition);
        Vector2 dir = (mouseWorld - (Vector2)transform.position).normalized;
        AimDirection = dir;
    }
}
```

### 扩展预留

未来可新增：

- `InputDevice` 枚举（Mouse、Gamepad、Auto）。
- `AutoAimTarget` 查找最近敌人。
- 网络同步：`AimDirection` 作为同步字段。

---

## 八、WeaponController 职责

### 负责

1. 普通状态管理 `WeaponPivot.localRotation`：
   - 接收 `SetAimDirection(Vector2 direction)`。
   - 只在未锁定时更新朝向。

2. 攻击状态锁定方向：
   - `LockAttackDirection()`：锁定当前 `aimAngle`。
   - `UnlockAttackDirection()`：解锁，恢复普通状态更新。

3. 计算攻击起止角度：
   - `GetAttackStartAngle()`：基于 `AttackShape` 与 `AnimationType`。
   - `GetAttackEndAngle()`：基于 `AttackShape` 与 `AnimationType`。
   - `GetAttackRotateMode()`：`Arc` 用 `Fast`，`FullCircle` 用 `FastBeyond360`。

4. 管理 `WeaponSprite` 视觉：
   - `RefreshWeaponVisual()`：根据 `AttackRange` 设置长度、位置、宽度。

### 不负责

- Tween 播放。
- 状态机。
- 伤害、冷却、判定。
- 读取输入。

### 关键接口

```csharp
public class WeaponController : MonoBehaviour
{
    [SerializeField] private Transform weaponPivot;
    [SerializeField] private Transform weaponSprite;
    [SerializeField] private AttackData attackData;
    [SerializeField] private float weaponWidth = 0.15f;

    private float aimAngle;
    private bool isDirectionLocked;

    public void SetAimDirection(Vector2 direction)
    {
        if (isDirectionLocked) return;
        aimAngle = Mathf.Atan2(direction.y, direction.x) * Mathf.Rad2Deg;
        weaponPivot.localRotation = Quaternion.Euler(0, 0, aimAngle);
    }

    public void LockAttackDirection()
    {
        isDirectionLocked = true;
        RefreshWeaponVisual();
    }

    public void UnlockAttackDirection()
    {
        isDirectionLocked = false;
    }

    public float GetAttackStartAngle()
    {
        switch (attackData.AnimationType)
        {
            case AttackAnimationType.FullCircle:
                return aimAngle;
            case AttackAnimationType.Arc:
            default:
                return aimAngle - attackData.AttackAngle * 0.5f;
        }
    }

    public float GetAttackEndAngle()
    {
        switch (attackData.AnimationType)
        {
            case AttackAnimationType.FullCircle:
                return aimAngle + 360f;
            case AttackAnimationType.Arc:
            default:
                return aimAngle + attackData.AttackAngle * 0.5f;
        }
    }

    public RotateMode GetAttackRotateMode()
    {
        return attackData.AnimationType == AttackAnimationType.FullCircle
            ? RotateMode.FastBeyond360
            : RotateMode.Fast;
    }

    private void RefreshWeaponVisual()
    {
        if (weaponSprite == null || attackData == null) return;
        weaponSprite.localRotation = Quaternion.Euler(0, 0, -90f);
        weaponSprite.localScale = new Vector3(weaponWidth, attackData.AttackRange, 1f);
        weaponSprite.localPosition = new Vector3(attackData.AttackRange * 0.5f, 0f, 0f);
    }
}
```

---

## 九、WeaponAnimator 最终接口

### 职责

- 攻击期间唯一修改 `WeaponPivot.localRotation`。
- 纯 Tween 播放器，不读取 `AttackData`。

### 接口

```csharp
public class WeaponAnimator : MonoBehaviour
{
    [SerializeField] private Transform weaponPivot;

    public bool IsPlaying { get; private set; }

    public void Play(
        float startAngle,
        float endAngle,
        float duration,
        Ease ease,
        RotateMode rotateMode,
        float activeMomentRatio,
        Action onActiveMoment)
    {
        Stop();
        IsPlaying = true;

        weaponPivot.localRotation = Quaternion.Euler(0, 0, startAngle);

        Tween tween = weaponPivot.DOLocalRotate(
            new Vector3(0, 0, endAngle),
            duration,
            rotateMode
        ).SetEase(ease);

        if (onActiveMoment != null)
        {
            tween.OnUpdate(() =>
            {
                // 在 activeMomentRatio 处触发一次
            });
        }

        tween.OnComplete(() => IsPlaying = false);
    }

    public void Stop()
    {
        weaponPivot.DOKill();
        IsPlaying = false;
    }
}
```

### 关键点

- `Play()` 必须在 `WeaponController.LockAttackDirection()` 之后调用。
- `Stop()` 只 Kill Tween，不修改 Transform。
- 使用 `DOLocalRotate`，不操作 `weaponPivot.rotation`。
- `FullCircle` 的 `endAngle` 由调用方传入 `aimAngle + 360`。

---

## 十、完整执行步骤

### Step 1：修改 AttackData.cs

**修改文件：** `Assets/Scripts/v0_4/AttackData.cs`

**操作：**

- 新增 `AttackShape` 枚举：`Fan`、`Circle`、`Box`。
- 新增 `attackShape` 字段与属性。
- 保留现有 `AttackAnimationType` 枚举：`Arc`、`FullCircle`、`Thrust`、`Spin`、`Throw`。
- 保留 `AttackRange`、`AttackAngle`、`AnimationType`、`Damage`、`Cooldown`、`TargetLayer`、`WindupTime`、`ActiveDuration`、`RecoveryTime`、`AttackEase`、`ActiveMomentRatio`。
- 不新增 `StartAngle` / `EndAngle`。
- 不添加动画计算。

**目的：**

- 让攻击判定与动画表现来自同一份数据。
- 保持 `AttackData` 纯配置职责。

---

### Step 2：新增 PlayerAimController.cs

**新增文件：** `Assets/Scripts/v0_4/PlayerAimController.cs`

**操作：**

- 继承 `MonoBehaviour`。
- `public Vector2 AimDirection { get; private set; }`。
- `Update()` 中读取鼠标世界坐标，计算并输出方向。
- 未来可扩展手柄、自动瞄准。

**目的：**

- 将输入层从 `PlayerCombat` 中剥离。
- 为网络同步、多输入设备预留接口。

---

### Step 3：新增 WeaponController.cs

**新增文件：** `Assets/Scripts/v0_4/WeaponController.cs`

**操作：**

- 字段：
  - `Transform weaponPivot`
  - `Transform weaponSprite`
  - `AttackData attackData`
  - `float weaponWidth`
  - `float aimAngle`
  - `bool isDirectionLocked`
- 方法：
  - `SetAimDirection(Vector2 direction)`：未锁定时更新 `aimAngle` 与 `weaponPivot.localRotation`。
  - `LockAttackDirection()`：锁定方向，调用 `RefreshWeaponVisual()`。
  - `UnlockAttackDirection()`：解锁方向。
  - `GetAttackStartAngle()`：基于 `AnimationType` 计算。
  - `GetAttackEndAngle()`：基于 `AnimationType` 计算。
  - `GetAttackRotateMode()`：返回 `RotateMode`。
  - `RefreshWeaponVisual()`：设置 `WeaponSprite` 长度、位置、旋转。

**目的：**

- 成为 `WeaponPivot` / `WeaponSprite` 的唯一管理者。
- 普通状态控制朝向，攻击状态只计算角度不抢 Tween。

---

### Step 4：重写 WeaponAnimator.cs

**修改文件：** `Assets/Scripts/v0_4/WeaponAnimator.cs`

**操作：**

- 删除 `AttackData` 引用。
- 删除 `weaponSprite` 引用。
- 删除 `SetAimDirection()`。
- 删除 `RefreshWeapon()`。
- 删除所有角度计算逻辑。
- 仅保留：
  - `Transform weaponPivot`
  - `bool IsPlaying`
  - `Play(float startAngle, float endAngle, float duration, Ease ease, RotateMode rotateMode, float activeMomentRatio, Action onActiveMoment)`
  - `Stop()`
- `Play()` 内部：
  - 先 `Stop()`。
  - `weaponPivot.localRotation = Quaternion.Euler(0, 0, startAngle)`。
  - `weaponPivot.DOLocalRotate(new Vector3(0, 0, endAngle), duration, rotateMode).SetEase(ease)`。
  - 在 `activeMomentRatio` 处触发 `onActiveMoment`。
  - `OnComplete` 设置 `IsPlaying = false`。
- `Stop()` 内部：
  - `weaponPivot.DOKill()`。
  - `IsPlaying = false`。
  - 不修改 `weaponPivot` 任何 Transform。

**目的：**

- 彻底纯化为本地旋转 Tween 播放器。
- 攻击期间唯一拥有 `WeaponPivot.localRotation` 修改权。

---

### Step 5：修改 PlayerCombat.cs

**修改文件：** `Assets/Scripts/v0_3/PlayerCombat.cs`

**操作：**

- 删除 `weaponPivot` / `weaponSprite` 直接引用。
- 删除鼠标输入读取代码。
- 新增引用：
  - `PlayerAimController aimController`
  - `WeaponController weaponController`
  - `WeaponAnimator weaponAnimator`
- `Update()`：
  - 若未攻击，调用 `weaponController.SetAimDirection(aimController.AimDirection)`。
- `StartWindup()`：
  - 调用 `weaponController.LockAttackDirection()`。
- `EnterActive()`：
  - 调用 `weaponAnimator.Play(...)`：
    ```csharp
    weaponAnimator.Play(
        weaponController.GetAttackStartAngle(),
        weaponController.GetAttackEndAngle(),
        attackData.ActiveDuration,
        attackData.AttackEase,
        weaponController.GetAttackRotateMode(),
        attackData.ActiveMomentRatio,
        OnActiveMoment
    );
    ```
- 攻击结束（Recovery 完成）：
  - 调用 `weaponAnimator.Stop()`。
  - 调用 `weaponController.UnlockAttackDirection()`。

**目的：**

- `PlayerCombat` 只负责攻击状态机与判定。
- 输入、朝向、动画执行全部解耦。

---

### Step 6：修改 EnemyAI.cs

**修改文件：** `Assets/Scripts/v0_4/EnemyAI.cs`

**操作：**

- 删除 `weaponPivot` 直接引用。
- 新增 `WeaponController weaponController` 引用。
- 保留 `WeaponAnimator weaponAnimator` 引用。
- `UpdateChase()`：
  - 计算玩家方向，调用 `weaponController.SetAimDirection(dir)`。
- `UpdateAttack()`：
  - `Windup` 阶段可继续调用 `weaponController.SetAimDirection(dir)`。
  - `Active` / `Recovery` 阶段不调用（方向已锁定）。
- 进入 `Attack` 状态时：
  - 调用 `weaponController.LockAttackDirection()`。
- `EnterActive()`：
  - 调用 `weaponAnimator.Play(...)`，参数来源同 `PlayerCombat`。
- 退出 `Attack` 状态时：
  - 调用 `weaponAnimator.Stop()`。
  - 调用 `weaponController.UnlockAttackDirection()`。

**目的：**

- `EnemyAI` 只负责状态机与目标方向计算。
- 武器朝向、动画由统一框架处理。

---

### Step 7：调整 Prefab

**修改文件：**

- `Assets/Prefabs/Player.prefab`
- `Assets/Prefabs/Enemy.prefab`

**Player.prefab 操作：**

- 在 `Player` 根上新增 `PlayerAimController` 组件。
- 在 `Player` 根上新增 `WeaponController` 组件：
  - `weaponPivot` → `Player/WeaponPivot`
  - `weaponSprite` → `Player/WeaponPivot/WeaponSprite`
  - `attackData` → `Assets/Data/PlayerAttackData.asset`
  - `weaponWidth` → `0.15`
- `WeaponAnimator` 组件：
  - 仅保留 `weaponPivot` → `Player/WeaponPivot`
- `PlayerCombat` 组件：
  - 新增 `aimController` → `PlayerAimController`
  - 新增 `weaponController` → `WeaponController`
  - 新增 `weaponAnimator` → `WeaponAnimator`
  - 移除 `weaponPivot` / `weaponSprite` 引用
- `WeaponPivot`：
  - `localPosition = (0, 0, 0)`
- `WeaponSprite`：
  - `localRotation = (0, 0, -90)`
  - `localScale` / `localPosition` 由 `WeaponController` 运行时设置

**Enemy.prefab 操作：**

- 在 `Enemy` 根上新增 `WeaponController` 组件：
  - `weaponPivot` → `Enemy/WeaponPivot`
  - `weaponSprite` → `Enemy/WeaponPivot/WeaponSprite`
  - `attackData` → `Assets/Data/EnemyMeleeAttackData.asset`
  - `weaponWidth` → `0.2`
- `WeaponAnimator` 组件：
  - 仅保留 `weaponPivot` → `Enemy/WeaponPivot`
- `EnemyAI` 组件：
  - 新增 `weaponController` → `WeaponController`
  - 新增 `weaponAnimator` → `WeaponAnimator`
  - 移除 `weaponPivot` 引用
- `WeaponPivot`：
  - `localPosition = (0, 0, 0)`
- `WeaponSprite`：
  - `localRotation = (0, 0, -90)`

**目的：**

- 组件关系与新的四层架构对齐。
- 编辑器配置一次性正确。

---

### Step 8：更新 AttackData Asset

**修改文件：**

- `Assets/Data/PlayerAttackData.asset`
- `Assets/Data/EnemyMeleeAttackData.asset`

**PlayerAttackData.asset 操作：**

- `attackShape = Fan`
- `animationType = Arc`
- `attackAngle = 90`
- `attackRange = 1.5`
- `windupTime = 0.2`
- `activeDuration = 0.15`
- `recoveryTime = 0.25`
- `activeMomentRatio = 0.5`
- `attackEase = OutQuad`

**EnemyMeleeAttackData.asset 操作：**

- `attackShape = Circle`
- `animationType = FullCircle`
- `attackRange = 1.2`
- `attackAngle = 360`（可选，Circle 形状下不用于判定）
- `windupTime = 0.3`
- `activeDuration = 0.4`
- `recoveryTime = 0.3`
- `activeMomentRatio = 0.5`
- `attackEase = Linear`

**目的：**

- 玩家使用扇形攻击，敌人使用 360° 旋转攻击。
- 判定形状与动画类型来自同一份配置。

---

### Step 9：编译验证

**操作：**

- 执行 `dotnet build Assembly-CSharp.csproj`。
- 目标：`0 警告，0 错误`。

---

## 十一、Player 攻击流程

```
[普通状态]
PlayerAimController.Update()
    ↓ 输出 AimDirection
WeaponController.SetAimDirection(AimDirection)
    ↓ 每帧更新 WeaponPivot.localRotation
PlayerCombat.Update() 检测攻击输入

[Windup 开始]
PlayerCombat.StartWindup()
    ↓
WeaponController.LockAttackDirection()
    ↓ 锁定 aimAngle，刷新 WeaponSprite

[Active 开始]
PlayerCombat.EnterActive()
    ↓
weaponAnimator.Play(
    weaponController.GetAttackStartAngle(),   // θ - 45°
    weaponController.GetAttackEndAngle(),     // θ + 45°
    ...
)
    ↓ Tween 独占 WeaponPivot.localRotation
OnActiveMoment()
    ↓ 命中判定与伤害

[Recovery 结束]
PlayerCombat.EndAttack()
    ↓
weaponAnimator.Stop()
    ↓ 只 Kill Tween
WeaponController.UnlockAttackDirection()
    ↓ 恢复普通状态朝向更新
```

---

## 十二、Enemy 攻击流程

```
[普通状态]
EnemyAI.UpdateChase()
    ↓ 计算玩家方向
WeaponController.SetAimDirection(dir)
    ↓ 每帧更新 WeaponPivot.localRotation

[进入 Attack 状态]
EnemyAI.ChangeState(State.Attack)
    ↓
WeaponController.LockAttackDirection()

[Windup 结束 → Active]
EnemyAI.EnterActive()
    ↓
weaponAnimator.Play(
    weaponController.GetAttackStartAngle(),   // θ
    weaponController.GetAttackEndAngle(),     // θ + 360°
    ...
)
    ↓ Tween 独占 WeaponPivot.localRotation
OnActiveMoment()
    ↓ EnemyCombat.TryAttack()

[Recovery 结束 → Chase]
EnemyAI.ChangeState(State.Chase)
    ↓
weaponAnimator.Stop()
WeaponController.UnlockAttackDirection()
    ↓ 恢复朝向玩家
```

---

## 十三、为什么新方案不会导致旧问题

### 为什么不会三个系统抢 Rotation？

- **普通状态**：只有 `WeaponController.SetAimDirection()` 修改 `WeaponPivot.localRotation`。
- **攻击状态**：`WeaponController` 锁定方向后不再修改 `WeaponPivot.localRotation`。
- **Tween 期间**：只有 `WeaponAnimator` 修改 `WeaponPivot.localRotation`。
- **攻击结束**：`WeaponAnimator.Stop()` 只 Kill Tween；`WeaponController.UnlockAttackDirection()` 恢复普通更新。

### 为什么 Arc 围绕瞄准方向？

- `WeaponController.LockAttackDirection()` 锁定当前 `aimAngle`。
- `GetAttackStartAngle()` = `aimAngle - AttackAngle/2`。
- `GetAttackEndAngle()` = `aimAngle + AttackAngle/2`。
- `WeaponAnimator` 使用 `DOLocalRotate` 在本地空间精确播放。

### 为什么 FullCircle 能完整 360°？

- `GetAttackStartAngle()` = `aimAngle`。
- `GetAttackEndAngle()` = `aimAngle + 360`。
- `GetAttackRotateMode()` = `RotateMode.FastBeyond360`。
- `WeaponAnimator` 从当前实际角度播放到 `+360`。

### 为什么攻击结束不会跳角度？

- `WeaponAnimator.Stop()` 只 Kill Tween，不重置 `localRotation`。
- `WeaponController.UnlockAttackDirection()` 后，下一帧 `SetAimDirection()` 平滑接上当前朝向。

---

## 十四、测试检查列表

### Player

- [ ] 待机时武器跟随鼠标方向。
- [ ] `Arc` 动画以攻击瞬间方向为中心。
- [ ] 攻击角度 = `AttackAngle`。
- [ ] 使用 `RotateMode.Fast`。
- [ ] 攻击结束后平滑恢复跟随鼠标。
- [ ] 无角度跳跃、无瞬移、无漂移。

### Enemy

- [ ] 待机时武器朝向玩家。
- [ ] `FullCircle` 从当前朝向开始，完整旋转 360°。
- [ ] 使用 `RotateMode.FastBeyond360`。
- [ ] 动画结束后恢复朝向玩家。
- [ ] 攻击判定与动画范围一致。

### 架构

- [ ] `PlayerAimController` 只输出方向，不参与攻击。
- [ ] `WeaponController` 是 `WeaponPivot` / `WeaponSprite` 唯一管理者。
- [ ] `WeaponAnimator` 攻击期间唯一修改 `WeaponPivot.localRotation`。
- [ ] `WeaponAnimator` 不读取 `AttackData`。
- [ ] `PlayerCombat` / `EnemyAI` 不直接操作 `WeaponPivot` / `WeaponSprite`。
- [ ] `EnemyCombat`、`PlayerController`、移动框架未被修改。
- [ ] `dotnet build Assembly-CSharp.csproj`：`0 警告，0 错误`。

---

## 十五、v0.5 技能系统预留接口

### 新增 SkillData

- 继承或扩展 `AttackData`。
- 可配置：
  - `AttackShape`
  - `AttackAnimationType`
  - 多段攻击列表
  - 蓄力时间

### PlayerCombat 扩展

- 维护技能队列。
- 每个技能复用同一套：
  - `PlayerAimController`
  - `WeaponController`
  - `WeaponAnimator`

### 示例

- **蓄力斩**：延长 `Windup`，增大 `AttackAngle`。
- **旋风斩**：`AnimationType = FullCircle`，连续播放两次。
- **长枪突刺**：`AnimationType = Thrust`，`AttackShape = Box`。
- **法师法杖**：`AnimationType = Arc`，`AttackShape = Fan`，远程判定。

### 无需修改的脚本

- `WeaponAnimator`：新增动画类型无需修改。
- `WeaponController`：新增形状/动画类型只需扩展分支。
- `PlayerAimController`：输入来源变化不影响其它层。

---

## 十六、待确认

1. 是否确认 `v0.4.5.2_Final3.md` 执行计划？
2. 是否可以从 **Step 1** 开始执行代码修改？
3. `PlayerAttackData.asset` 的 `attackAngle` 保持 `90°` 还是改为 `120°`？
