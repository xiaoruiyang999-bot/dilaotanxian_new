# v0.6.1 操作记录

> 版本主题：E 键交互统一 + 两段式拾取（计划书 4.3 / 第五章 v0.6.1）。
> 全部交互（宝箱/祭坛/补给/传送门）由 walk-over 改为走近按 E；可拾取物支持单物品直拾与多物品列表选择。

## 改动文件清单

新建（3）：
- `Assets/Scripts/now_use/Player/PlayerInteractor.cs`（+ .meta）— E 键交互器 + 两段式拾取
- `Assets/Scripts/now_use/Dungeon/Interaction/IPickupable.cs`（+ .meta）— 可拾取物接口
- `Assets/Scripts/now_use/Dungeon/Interaction/HealPickup.cs`（+ .meta）— 治疗球拾取物（验证用）
- `v0.6.1操作.md`（本文件）

修改（9）：
- `Assets/Scripts/now_use/Dungeon/Interaction/Interactable.cs` — 删 walk-over，加 `Interact(Collider2D)` 入口 + `IsConsumed`
- `Assets/Scripts/now_use/Dungeon/Interaction/ChestInteractable.cs` — 开箱掉真实 HealPickup（见"行为变更"）
- `Assets/Scripts/now_use/Dungeon/Interaction/SupplyInteractable.cs` / `ShrineInteractable.cs` / `PortalInteractable.cs` — 仅注释同步（walk-over → 按 E），逻辑零改动
- `Assets/Scripts/now_use/Player/PlayerController.cs` — Interact/Cancel 分发 + 运行时挂载 PlayerInteractor
- `Assets/PlayerInputActions.inputactions` — 新增 Interact(E) / Cancel(Escape)
- `Assets/Scripts/now_use/README.md` — 清单 51→54

未动任何场景/预制体 YAML（编辑器运行中）。

## 实现要点

### 1. Interactable 基类
```csharp
public virtual void Interact(Collider2D player)
{
    if (consumed) return;
    consumed = true;
    OnConsumed(player);   // 效果 + 压暗，钩子与派生类零改动
}
```
`OnTriggerEnter2D` 删除；新增 `IsConsumed` 供候选过滤。

### 2. PlayerInteractor（核心）
- 每 0.1s `Physics2D.OverlapCircle(pos, 1.2f, contactFilter, buffer)`（Unity 6 ContactFilter2D + 32 缓冲，useTriggers=true，全层组件过滤，与 WeaponHitbox 同风格），取最近的未消耗 Interactable 或 IPickupable 为候选；
- 候选反馈：DOTween `DOScale(1.1×, 0.6s).SetLoops(-1, Yoyo).SetLink(go)` 呼吸放大，换候选/失候选 Kill 并复原原始 scale；
- "按 E"标签：运行时自建 World Space Canvas 挂 WorldUIRoot（复用 PlayerWorldStatusBar 模式），白字深底（72×26，0.7 黑底，Legacy Text + 微软雅黑动态字体），LateUpdate 跟随候选头顶；
- E 分发：普通交互物 → `Interact()`；可拾取物仅 1 个 → 直接 `OnPickedUp()`；≥2 个 → 弹列表。

### 3. 拾取列表（屏幕空间纯文字）
- ScreenSpaceOverlay Canvas（sortingOrder 100），深色面板 `WorldToScreenPoint(玩家)` 右侧跟随 + 屏幕边缘钳制；
- 物品按距离升序，默认高亮最近项（黄色 #F4D03F + "▶" 前缀）；滚轮（0.15s 节流）/数字键 1-9 切换；E 拾取高亮项；Esc 或玩家距最近物品 > 2.5 自动关闭；
- 每帧剔除已失效物品（被拾取/换层销毁），空列表自动关闭。

### 4. 输入
- `Interact`（Button，`<Keyboard>/e`）/ `Cancel`（Button，`<Keyboard>/escape`），全新 GUID；PlayerController performed 分发到 `interactor.OnInteractPressed()/OnCancelPressed()`；无 KeyCode 硬编码（滚轮/数字键走 Input System 的 `Mouse.current`/`Keyboard.current`）。
- PlayerInteractor 由 PlayerController.Awake 运行时 `AddComponent`（不改 prefab；prefab 已挂则直接用）。

### 5. 验证用拾取物（ChestItem_Heal → HealPickup）
- `HealPickup : MonoBehaviour, IPickupable`，DisplayName "治疗球 +2HP"，`OnPickedUp` → `Health.Heal(2)` + 销毁；
- ChestItem_Heal prefab 纯视觉无碰撞体，HealPickup.Awake 运行时补 CircleCollider2D(trigger, r=0.5) 供探测；
- **行为变更（注意）**：宝箱不再"开盖即回血"。开盖动画结束掉出真实治疗球留在原地，+2HP 改在按 E 拾取时结算——这是两段式拾取的设计本意（计划书 4.3），否则开箱无法实测拾取。宝箱总奖励不变（+2HP）。若需恢复旧行为，把 `ChestInteractable.SpawnItem` 末尾改回调 `ApplyEffect` 即可。

### 6. 传送门回归
PortalInteractable 逻辑零改动：候选 → 按 E → `Interact()` → `OnConsumed`（覆盖，不压暗）→ `RunManager.NextFloor()`。代码路径已走查确认，运行时验证待用户在编辑器执行。

## 验证情况

- 编译：`dotnet build Assembly-CSharp.csproj` 通过，0 警告 0 错误（新文件已手工补进 csproj 条目；编辑器下次刷新会自动重生成，无残留影响）；
- inputactions JSON：node `JSON.parse` 通过，6 个 action，Interact→e / Cancel→escape 绑定正确；
- 静态走查：无残留对旧 walk-over 路径的引用（RoomTrigger 的 OnTriggerEnter2D 是进房检测，与交互无关，保留）。

## 待用户在编辑器验收

1. 四个交互物回归：宝箱（E 开箱 → 掉治疗球 → 再按 E 拾取 +2HP）、祭坛（E 随机±）、补给（E +HP/+甲）、**传送门（E 换层，关键路径）**；
2. 候选反馈：靠近交互物呼吸放大 + 头顶"按 E"，走远复原消失；
3. 多物品列表测试：把两个宝箱摆在一起（房间内容 SpawnTable 或场景手动复制两个 Chest，间距 <2.4），全开完后两个治疗球相邻 → 走近弹列表，滚轮/数字键切换，E 拾取，Esc 关闭；走远自动关闭；
4. 死亡/换层时列表与高亮正确清理。

## 补丁：提示标签调参与渲染层级修复（2026-07-27）
- "按 E"标签用户调定值固化：`Hint Panel Size = 20×10`、`Hint World Scale = 0.09`（prefab 与脚本默认值同步）。
- 修复玩家被箱子/传送门盖住：Player.prefab 身体 SpriteRenderer `m_SortingOrder` 2→10、武器 3→11（交互物最高为传送门 5）。
