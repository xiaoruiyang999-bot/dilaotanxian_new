# 版本实施自检清单：错误记录与实施规范

> 用途：**每个子版本开工前，实施者（人或 AI）必须先过一遍 §1 自检清单；§2 是截至 v0.6.2 的全部犯错记录（现象/根因/修正/预防）；§3 是已核实的项目环境事实（防止引用过时参数）；§4 是可复用的提示词模板。**
> 维护规则：每版本收尾时把本版本新犯的错补进 §2，新核实的环境事实补进 §3。
> 最近更新：v0.6.2（2026-07-28）

---

## §1 开工前自检清单（每个子版本实施前必读）

### 编辑器与场景
- [ ] 用户的 Unity 编辑器**是否正在运行**？运行中**禁止改场景/预制体 YAML**（会被内存覆盖或不生效），优先代码运行时构建；必须改 YAML 时，改完提醒用户"切回编辑器核对、勿保存旧内存"。
- [ ] 改动涉及 UI/场景内容时，**用户会在哪个场景测试**？多场景项目里，UI 元素一律用"运行时查找 + 找不到就代码创建"的模式，保证任何场景都能显示；交付时写明"请打开 X 场景验收"。
- [ ] 新增场景：.unity 复制后**必须换 .meta 的 GUID**；场景要在 EditorBuildSettings 里（外部改该文件后提醒用户核对 File > Build Settings 或重启编辑器）。
- [ ] 新增脚本文件后，dotnet build 前确认新文件**已在 .csproj 里**（编辑器未刷新时要手工补 `<Compile Include>`）。

### 运行时创建组件（本项目的雷区 Top1）
- [ ] 运行时 `AddComponent<T>()` 之前，检查 T 及其基类的 `RequireComponent`：**如果依赖的是抽象类型（如 Collider2D），Unity 自动补全会抛 NullReferenceException**——必须先显式 `AddComponent<具体类型>()`。（已犯两次：PrepPedestal、PrepPortalInteractable）
- [ ] 修复一个 bug 后，**立刻全项目 grep 同类模式**，不要等第二次爆（抽象 RequireComponent 就是修一处漏一处，二次犯错）。

### UI 与文字
- [ ] 运行时创建且**缓存复用**的 UI（创建一次、只开关不重建）：外观参数（字号/尺寸/缩放/颜色）必须**每帧或每次显示时应用**，否则 Inspector 调参不生效（"按 E"标签字号调不动的教训）。
- [ ] 同一 UI 元素的定位逻辑**单点收口**：不允许"场景 YAML 摆一份、代码再算一份"两条路径（体力条位置错的教训）。
- [ ] 屏幕面板布局：按钮坐标先对**面板半高**做边界核算，布局完用截图验证（ClassSelectUI 按钮溢出的教训）。
- [ ] 中文文字：TMP 字体必须从**字体文件**加载（`new Font("C:/Windows/Fonts/...")`），**禁止** `CreateDynamicFontFromOSFont`（不含字体数据 → 回退 LiberationSans → 中文变方块 + 满屏警告）。统一走 `TMPFontProvider.Font`（微软雅黑→黑体→宋体→内置 fallback）。
- [ ] "文字/血条看不清"类报告，先查**测试环境**：Game 视图分辨率与 scale（固定 QHD 在 1.0 时中心裁剪会挡住角落 UI）、相机 ortho size（实际 =10，不是旧文档的 6）、元素世界尺寸——再怀疑代码。

### 渲染与交互
- [ ] 新增可见物体先对**渲染层级表**（§3）：玩家身体 10 / 武器 11 是最高层，新内容不得更高；交互物 1~5、地面装饰 1。
- [ ] 同一机制跨场景/跨阶段可能有**不同规则**（如换武器：准备场景=归位展台，地牢=原地掉落）——规则要带作用域开关（`dropOldWeaponOnEquip` 式），并防"脚下掉落物抢最近候选"这类自循环。
- [ ] "玩家无法操作"类 bug，先查候选优先级与输入分发链路，再查功能本身。

### 收尾
- [ ] 提交前清调试 `Debug.Log`（保留 Error/Warning）。
- [ ] Unity 6 新 API 注意：`linearVelocity`（旧 velocity）、`FindObjectsInactive.Exclude`（FindObjectsSortMode 已弃用）、`GlyphRenderMode` 在 `UnityEngine.TextCore.LowLevel` 命名空间。
- [ ] 所有 DOTween tween 必须 `SetLink`（v0.5.4 规范）。
- [ ] 操作文档写清：改动清单、验收清单（含测试场景、分辨率设置）、遗留风险。

---

## §2 错误记录（截至 v0.6.2，按时间序）

| # | 版本 | 现象 | 根因 | 修正方法 | 预防规则 |
|---|------|------|------|----------|----------|
| 1 | v0.6.0 | 用户看不到体力条 | 体力条只加在 v0_6 新场景，用户测的是 v0_5 场景 | 改代码运行时创建，任何场景都有 | UI 改动用"查找+运行时创建"模式；交付写明测试场景 |
| 2 | v0.6.0 | 体力条位置在护甲条上方 | 场景 YAML 摆一份位置、运行时代码又算一份，两路径不一致 | `RepositionStatBars` 单点收口，启动时强制重定位 | 定位逻辑单点收口，不信场景 YAML |
| 3 | v0.6.1 | "按 E"字号怎么调都没效果 | 标签创建一次后缓存复用，字号只在创建时读一次 | 字号/底框/缩放每帧应用 | 缓存复用的 UI，外观参数每帧（或显示时）应用 |
| 4 | v0.6.1 | 汉字 Game 里"几个小点" | 三因素叠加：Game 视图 QHD+1.0 中心裁剪、相机 size=10（文档误记 6）、标签世界尺寸小 | 分辨率改 1920×1080、标签世界缩放 0.01→0.02（用户终调 0.09） | 显示问题先查测试环境再查代码；文档参数要核实 |
| 5 | v0.6.1 | 玩家被箱子盖住 | 箱子 sortingOrder=4、传送门=5 > 玩家身体 2、武器 3 | 玩家身体→10、武器→11 | 新物体先对渲染层级表；玩家层固定最高 |
| 6 | v0.6.2 | 启动报错 NRE，展台不生成（PrepPedestal.cs:60） | 基类 `RequireComponent(typeof(Collider2D))` 是抽象类，运行时 AddComponent 触发自动补全崩溃 | 先显式 `AddComponent<CircleCollider2D>()` 再加组件 | 见 §1"运行时创建组件"；修后全项目 grep 同类 |
| 7 | v0.6.2 | 同类 NRE 二次爆发（PrepPortalInteractable.cs:29） | 同 #6——修了一处没排查同类 | 同样修法 + 全项目排查（WeaponPickup/HealPickup 是 IPickupable 不受影响） | **同一 bug 修一次就必须全项目排查同类模式** |
| 8 | v0.6.2 | 选择 UI 按钮溢出面板、确认键叠在按钮上 | 面板 460×430，按钮 y 坐标超出半高 215 | 面板 600×500，按钮重排，边界核算 | 布局先算半高边界，截图验证 |
| 9 | v0.6.2 | 满屏 LiberationSans 警告、中文变方块 | `CreateDynamicFontFromOSFont` 按名创建的 Font 不含字体数据，TMP 无法栅格化 | 改从字体文件加载（msyh.ttc→simhei→simsun fallback） | TMP 字体必须含数据；中文走 TMPFontProvider |
| 10 | v0.6.2 | 拾取初始武器后"无法再拾取" | 准备阶段串用地牢规则"旧武器原地掉脚下"→ 脚下掉落物永远是最近候选 → 捡起-掉落-捡回死循环 | 规则作用域开关 + 准备场景改"自动归位回展台" | 跨场景规则要开关；"无法操作"先查候选优先级 |
| 11 | v0.6.2 | （风险）改 EditorBuildSettings 后 Play 报 scene not in build settings | 编辑器内存未重载外部修改的配置文件 | 提醒用户核对 Build Settings 或重启编辑器 | 改 ProjectSettings 后必须提醒 |
| 12 | 全程 | dotnet build 漏编新文件（假通过） | 编辑器未刷新时新脚本不在 .csproj | 手工补 `<Compile Include>`，编辑器重生成后自动正规化 | 编译验证先确认新文件在 csproj |
| 13 | v0.5 遗留 | Health/PlayerUI 调试日志刷屏 | 调试残留 | v0.6.0 已清理 | 提交前清 Debug.Log |
| 14 | v0.6.2 | Unity 6 API 报错两处 | FindObjectsSortMode 弃用；GlyphRenderMode 命名空间迁移 | 换 `FindObjectsInactive.Exclude` / `UnityEngine.TextCore.LowLevel` | Unity 6 写代码先确认 API 归属 |
| 15 | v0.6.2 | 用户在场景里手调状态条布局，进 Play 被代码覆盖 | PlayerUI 的 RepositionStatBars/PositionBarBelow/面板自动扩展每次启动强制重定位，与用户场景编辑冲突 | 删除全部强制重定位；代码只在场景缺条时兜底创建（创建时给整齐默认布局），四条+背景条统一进场景 YAML | **布局类代码只兜底创建、永不改已存在元素 Transform**；布局归场景编辑 |

**典型流程失误（非代码）**：
- P1：改动场景 ≠ 用户测试场景，交付时没说清 → 用户"看不到"误判为没做。规则：交付必写"请打开 X 场景、分辨率设 Y 验收"。
- P2：编辑器运行时改 YAML 被内存覆盖的风险被低估。规则：编辑器运行 = 代码侧优先，YAML 改动必提醒。
- P3：显示类问题先改代码后查环境，绕了弯路（"按 E"字号三轮才定位到 Game 视图设置）。规则：先核对 §3 环境事实表。

---

## §3 已核实的项目环境事实（防引用过时参数）

| 项 | 值 | 备注 |
|----|-----|------|
| 相机 | Orthographic **size=10**（可视高 20 单位） | 框架文档曾误记 6，v0.6 已更正 |
| 渲染层级 sortingOrder | 玩家身体 **10**、武器 **11**（最高）；传送门 5、箱子 4、展台/障碍物 1~2、地面装饰 1 | 新内容不得超过玩家层 |
| 字体 | TMP + `TMPFontProvider.Font`（C:/Windows/Fonts 微软雅黑→黑体→宋体）；Legacy Text 已全部迁移 | 禁 CreateDynamicFontFromOSFont |
| "按 E"标签调定值 | Panel 40×30、WorldScale 0.03、FontSize 16（用户调定 2026-07-28，PlayerInteractor 序列化字段，prefab 与脚本默认值已同步） | 勿回退 |
| 展台名签 | nameLabelFontSize/PanelSize/WorldScale 序列化可调（默认 16/120×22/0.03） | v0.6.2 阶段 C |
| 场景清单 | 主测：`v0_6_ClassWeapon`（地牢）、`v0_6_PrepRoom`（准备）；回归：`v0_5_Dungeon`、`v0_4_EnemySystem` | Build Settings 0/1 位 = 准备/地牢 |
| 测试环境约定 | Game 视图 1920×1080 或 Free Aspect；**不要**固定 QHD+scale 1.0（中心裁剪） | 显示问题先查此项 |
| 输入 | inputactions 7 actions：Move/Attack 固定，Dash/Sprint/Interact/Cancel/Skill 可重绑 | 禁 KeyCode 硬编码 |
| 体力/闪避数值 | 体力 100、回 30/s（停耗 0.8s 后）；闪避距 3/时 0.18s/耗 35/CD 0.5s（动作结束后起算）；奔跑 ×1.6 耗 20/s | Player.prefab 上序列化可调 |
| 状态条配色 | HP 红 `(0.9,0.1,0.1)`、护甲钢灰 `#708090`、体力黄 `#F4D03F`、法力蓝 `#3498DB`；条背景统一深色 `(0.12,0.12,0.12,0.9)` | 护甲色 v0.6.2 由钢蓝 #5D6D7E 改钢灰（与法力蓝拉开） |
| 状态条布局 | 布局归场景编辑：PlayerUI 只兜底创建缺失条，永不改已存在条 Transform（自检 #15）；v0_6 两场景 YAML 四条+背景条统一（190 宽、左缘 5、等距） | 用户可在 Scene 里自由调整 |
| 跨场景载体 | `RunStateCarrier`（DontDestroyOnLoad）：LastChosenClass/LastWeapon/HasLoadout | 准备⇄地牢、死亡回流共用 |
| 换武器规则 | 准备场景=自动归位展台（只能一把）；地牢=旧武器原地掉落可捡回 | 作用域开关 dropOldWeaponOnEquip |
| ClassCatalog | 编辑器走 AssetDatabase；**打包构建需把 ClassData 复制到 Resources/Class/** | 代码已留分支 |

---

## §4 提示词模板（实施任务可直接复用）

**开工提示词（派给实施者/AI 时附上）**：

> 用户的 Unity 编辑器可能正在运行：脚本随便改，不要编辑场景/预制体 YAML，除非确实必要且改完提醒用户核对。运行时 AddComponent 任何组件前，检查基类 RequireComponent 是否抽象类型（本项目 Interactable 基类 RequireComponent(Collider2D) 是抽象类，必须先显式加具体碰撞体，已因此炸过两次）。中文 UI 一律用 TMPFontProvider.Font，禁 CreateDynamicFontFromOSFont。运行时创建且缓存复用的 UI，外观参数每帧应用。DOTween 必须 SetLink。禁 KeyCode 硬编码，走 inputactions。新增脚本确认进了 csproj 再 dotnet build 验证。改完更新 now_use/README.md 和操作文档，验收清单写明测试场景与分辨率设置。

**交付/验收提示词**：

> 交付时写清：①请打开哪个场景验收；②Game 视图分辨率设为 1920×1080 或 Free Aspect；③Build Settings 是否需核对；④Play 中 Inspector 可调参数在 Hierarchy 的 Player 实例上改（改 Project 窗口 prefab 不影响运行实例），满意后记值退出 Play 填到 prefab。

**自查提示词（修 bug 后执行）**：

> 这个 bug 的模式（抽象 RequireComponent / 缓存 UI 参数 / 跨场景规则串场 / 渲染层级 / 字体数据）在项目里还有同类吗？grep 一遍再收工。
