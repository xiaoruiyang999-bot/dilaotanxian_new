# v0.6.3–v0.6.8 狼人角色系统开发文档

> 状态：**已上线 · 开局即狼 / T 键兽化闭环运行中**
> 日期：2026-08-20 ~ 08-22
> 关联：[开发必读_核心信息整合](开发必读_核心信息整合.md)（红线 R1-R15 大半源于本系统）/ [v0.6.2_预警与判定对齐修复](v0.6.2_预警与判定对齐修复.md)
> 素材源：`E:\WeGameApps\The animation of Unity\狼人\`

---

## 1. 系统现状（一图流）

```
开局 ──→ 普通狼（待机7帧循环 ⇄ 走路3帧循环，按移动/朝向自动切换）
              │ 按 T
              ▼
        变身动画（6帧 × 0.25s = 1.5s，画布720→1080 + 容器逐帧膨胀补偿）
              │ 播完
              ▼
        兽化形态（待机5帧循环；移动暂同套循环）──按 T──→ 直接回普通狼
```

- 绿球（人形态）已退出 T 循环；`startAsWolf=false` 保留人形态开局通道（仅代码层面）
- 狼形态攻击体系自动 ×1.5（判定/武器视觉/预警同源链同步放大）
- 武器、头顶血条、攻击预警在所有形态正常显示

## 2. 素材资产清单（Assets/Art/Werewolf/，全部 720/1080 + 白边 4px + Single 模式）

| 目录 | 帧数 | 画布 | 用途 | 来源处理 |
|---|---|---|---|---|
| Walk_L / Walk_R | 3+3 | 720 | 普通狼走路循环 | remove.bg 抠图 → resize 720+白边（源：走路帧序列） |
| Idle_L / Idle_R | 7+7 | 720 | 普通狼待机循环 | 用户剥离 → 项目内补 Single |
| Transform_L | 6 | 720→1080 递增 | 变身动画（无右版，不镜像） | 用户制作（画布递增=体型演出） |
| BeastIdle_L / BeastIdle_R | 5+5 | 1080 | 兽化待机（移动暂共用） | 用户剥离（已透明）→ 内联补白边+镜像 |
| （备份）素材源目录 | — | — | 狼人走路9帧_手绘_备份 | 被替换的原始手绘版 |

**命名规范**：`001.png` 起三位递增（`EditorLoadSprites` 按文件名 CompareOrdinal 排序——中文/括号后缀会乱序，必须规范命名）。
**导入红线**：必须 `spriteMode=Single`（默认 Multiple 自动切片 → 引用变透明碎片，"一点痕迹都没有"，R3）。

## 3. 素材处理管线

**三个工具**（素材源目录下，用户已将 mirror/resize 改为"配置区"模式——脚本内写死 SRC/DST 常量直接跑）：

| 脚本 | 功能 | 要点 |
|---|---|---|
| `remove_bg.py <源> <目标> [--tol --outline]` | 纯色背景板去除 + 白描边 | 边缘中位色检测 + **连通域保护**（角色内部浅色不误删）；背景去除率 45-65% 为健康区间 |
| `mirror_frames.py` | 水平镜像（左右版） | 左右两套实体素材，**不做 flipX**（防白描边内部镜像错位） |
| `resize_frames.py` | 尺寸统一（LANCZOS 重采样）+ 可选白边/改名 | "压大小不压品质"；幂等（已达标跳过） |

**新素材标准流程**：剥离/抠图（remove.bg 或 remove_bg.py）→ `resize_frames --outline 4 --rename` → 镜像 → 复制进对应目录 → **UnitySkills 批量设 Sprite+Single** → 重进 Play 自动生效（帧数变化零代码）。

**已知限制**：渐变夜空背景无法自动去除（变身 005-011 原版曾尝试：全局色差删不动、区域生长渗入角色碎成 732 块）——需源头出透明版。

## 4. 代码架构（两个组件，纯代码零资产依赖）

### 4.1 FrameAnimator（Common/，通用序列帧播放器）
- `frames/fps/loop` 序列化字段 + `Play(IReadOnlyList<Sprite>, loop)` / `Pause/Resume`
- `OnFinished` 回调（单次模式播完触发——变身接待机用）
- 公开属性：`CurrentFrames`（状态比较）、`CurrentSprite`（v0.6.5 体型补偿用）、`Fps` 读写
- 编辑器便利 `framesFolder`（Awake 按目录 AssetDatabase 加载，**打包前需资源化**）
- **坑（R13）**：`Play()` 复制新 List——外部用引用比较"是否在播"恒不等 → 每帧重播卡帧 0。状态跟踪必须用枚举

### 4.2 WerewolfTransformation（Player/，挂 Player 根）
- **状态机**：`WolfAnim { None, Transforming, WalkL/R, IdleL/R, BeastIdleL/R }` + `IsWolf/IsBeast/pendingBeast/transforming`
- **素材加载**：7 个目录字段（编辑器 AssetDatabase 便利），Awake 加载 + Start 日志（格式特征验证法）
- **EnterWolf/StartBeastTransform/OnTransformFinished/ExitWolf**：见 §1 流程
- **Update**：按 `IsBeast` → 兽化循环；否则按 `rb.linearVelocity`（移动）+ `FacingDirection.x`（朝向）切 Walk/Idle 左右
- **LateUpdate（v0.6.5 体型补偿）**：变身期间每帧 `scale = wolfScale × (当前帧画布宽/首帧画布宽)`——素材画布递增但角色像素前期不变，纯渲染看不出变大，容器按画布比补偿后才呈现"首帧标准体型逐帧膨胀"
- **OnTransformFinished 重置**：进兽化时 scale 回基准 wolfScale（兽化帧 1080 画布自带大体型，与膨胀末帧视觉衔接）
- **引用自愈**：`werewolfVisual` 未接线时 `transform.Find("WerewolfVisual")`（防 prefab 迁移丢引用）

### 4.3 场景/Prefab 结构
- `Player`（PrefabInstance）下 `WerewolfVisual`（GO + SpriteRenderer + FrameAnimator）——**在 prefab 本体**（R1：往 prefab 实例加组件/子物体会被 scene_load 丢弃，走 `prefab_apply_overrides` 路径写入）
- 输入：`PlayerInputActions` 的 `Transform` action → **T 键**（同 map：Move/Attack/Dash=Space）

## 5. 渲染与层级体系

| 层 | sortingOrder | 说明 |
|---|---|---|
| 地牢 Tilemap | 0/1 | |
| 普通狼/兽化 sprite | **2** | 代码强制（曾为 50，盖住武器 3 和血条 10——v0.6.4 修复） |
| 武器 WeaponSprite | 3 | 挂在狼身上=狼人持武器 |
| 头顶血条 | 10 | 世界空间 Canvas |
| 攻击预警 | 100 | |

- **体型**：`wolfScale=0.25`（720 画布→1.8 世界单位；1080 兽化→2.7）；全部素材尺寸统一后无跳变
- **攻击体系联动（v0.6.7）**：狼形态 `WeaponPivot.localScale ×= wolfAttackScale(1.5)`——判定长度/宽度（`lossyScale`）、武器视觉（子物体）、攻击预警（`CurrentBoxGeometry`）**四者同源自动同步**，变回人形态还原

## 6. 演进史（做了什么、为什么）

| 版本 | 内容 | 触发问题/需求 |
|---|---|---|
| v0.6.3 初版 | FrameAnimator + WerewolfTransformation + WerewolfVisual + T 键 + 三态动画 | "做一个独立的变身技能按键" |
| — | 素材三工具 + 22 帧管线 | 镜像/去背景板/白描边需求 |
| — | **R3 事故**：textureType=Sprite 默认 Multiple 自动切片，渲染"一点痕迹都没有" | sprite 引用实为透明碎片；修复=显式 Single + meta 主图注册 |
| — | **R1 事故**：prefab 实例组件被 scene_load 丢弃（"按 T 角色消失"） | 改走 prefab_apply_overrides 写入 Player.prefab 本体 |
| v0.6.4 | 只藏玩家本体 SR（不藏武器/血条）+ 狼排序 2 | "变身图盖住攻击范围和武器" |
| v0.6.4 续 | 待机从单张模板图 → 帧循环（idleFps=5） | 用户提供待机序列 |
| v0.6.5 | LateUpdate 画布比例体型补偿 | "画布改大但游戏里没变化"（角色像素未变） |
| v0.6.6 | `startAsWolf` 开局即狼 | "idle/walk 要一开始就渲染，不是小球" |
| v0.6.7 | WeaponPivot 同源放大 ×1.5 | "狼形态攻击范围与显示不一致"（体型与细条判定错配） |
| v0.6.8 | **T 键语义改造：狼↔兽化**（绿球退出）；BeastIdle 素材管线 | 新增"变身后待机动作序列帧" |
| 过程修复 | 帧率参数 8→2→4fps（每帧 0.25s）；R14 升级（auto-refresh 关闭时 asset_refresh 不编译，需 debug_force_recompile） | "没变化"反复出现的根因 |

## 7. 配置速查（Inspector：Player → Werewolf Transformation）

| 字段 | 默认 | 说明 |
|---|---|---|
| Werewolf Visual | (自愈) | 狼人视觉子物体 |
| 7 个素材目录字段 | Assets/Art/Werewolf/... | 帧目录（改目录名时同步） |
| Start As Wolf | ✔ | 开局即狼 |
| Transform Fps | 4 | 变身帧率（1/每帧秒数） |
| Walk Fps / Idle Fps | 10 / 5 | |
| Wolf Scale | 0.25 | 720 画布→1.8 单位 |
| Wolf Attack Scale | 1.5 | 狼形态攻击体系放大 |

## 8. 待办与扩展点

- [ ] **兽化走路帧**（BeastWalk_L/R）：用户提供后 Update 兽化分支拆分移动/静止（现为同套循环）
- [ ] 变身动画右版（Transform_R，背向变身镜像）
- [ ] **打包前资源化**：所有 `#if UNITY_EDITOR` AssetDatabase 帧加载改 Resources/Addressables（搜 `EditorLoadSprites`）
- [ ] 兽化数值体系（攻击力/速度加成）接 M2 的 PlayerStats 乘数
- [ ] 变身冷却/资源消耗（当前无限制）
- [ ] 素材画风统一（手绘版 9 帧备份在源目录，像素版现行——未来整体替换）
