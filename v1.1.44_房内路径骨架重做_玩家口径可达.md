# v1.1.44 房内路径骨架重做——玩家口径可达(障碍墙围出的每个空间必有 ≥2 格宽入口)

> 后续状态：玩家口径可达规则继续保留；本版的“逐段增量落位”已在 v1.1.46 升级为
> “完整小掩体岛候选 → 双口径验证 → 质量评分 → 一次提交”，以解决固定模板重复、视觉粘连和回滚污染。

## 需求(用户)

> 我所说的路径骨架是能够到达房间中的每一个空间——尤其指由房间中的**障碍墙构成的新的区域**:
> 由障碍墙不能形成不可进入的新区域,一定要留有空间。

即:v1.1.39 的"骨架"实现偏离本意——骨架应是**细的路网**(通往每个空间),而不是占满房间的大片禁放区;
可达性的判定必须是**玩家物理口径**,不是格子 BFS 连通。

## 根因(离线仿真复现,Tools/RoomSkeletonSim,1000 房/尺寸)

| 症状 | 旧管线数据 | 根因 |
|---|---|---|
| 玩家进不去的空间仍有发生 | 窄区房 0.4%(18×11)~1.6%(28×15) | 验证⑤是**格级** BFS:障碍与轮廓/障碍与障碍隔 1 格形成的 1 宽缝,格级连通但玩家胶囊净宽 **1.32**(Player.prefab 3.65×scale0.3618)物理过不去(1 格净宽=1.0);SpawnPositionHelper 物理选点不查可达,敌人/宝箱/大石块会刷进缝里 → 卡清房/拿不到奖励 |
| 过半房间空空荡荡 | Plain 保底房 54%~66% | 骨架(protect)铺了十字带+中带+内环 2 圈+走廊+轮廓环,几乎覆盖全房,障碍均值仅 0.6~2.9 格;再叠加房心 ≥3 独立保护与 ±2 偏移切碎,验证 10 连败退化 Plain |

## 实现(四文件 + 测试)

### 1. RoomPlan.cs · RoomPlanner——细骨架 + 增量落位
- **骨架瘦身为路网**:中心 3×3 + 门锚点→中心宽 3 走廊(删十字带/中带/内环)。
  Skeleton 快照(地皮"土径穿草")随之变成真正的路径形态。
- **障碍增量落位**:每段(模板变体产物)放入即全量复验,失败从段尾逐格收缩重试,<2 格弃段——
  最终态天然合法,不再依赖整案复核。弃段不影响其余段(同 v1.1.42 语义)。
- 降级链不变(10 次重试 → 去障碍 → 整房 Plain)。

### 2. RoomLayoutValidator.cs——新增玩家口径 ValidatePlayerGauge
- **通行边判定**:4 邻边 (u,v) 两侧至少一侧的 2 格带皆 free ⇔ 该处通道净宽 ≥2(2.0 > 1.32 可过)。
- **Reachable2Wide**:从中心沿通行边 BFS 得玩家可达集。
- 验证规则:**所有 free 格 + 全部门锚点 ∈ 可达集**(1 宽缝零容忍——内容生成器会往任何 free 缝里刷东西)。
- 格级 Validate(面积比/障碍块 ≤6/零贴墙/格级全连通)保留作基础门槛。

### 3. RoomObstaclePlanner.cs——段列表化
- Plan() → **PlanSegments()**(只做模板/旋转/镜像/抖动/±1 偏移变换,消费 rng 顺序不变,同 seed 复现门禁不变);
- 取消独立的房心 ≥3 保护(中心 3×3 已在骨架内,双重保护曾把段切碎);偏移 ±2→±1;
- 落位硬约束(距墙 ≥2/非轮廓/骨架禁入)保留,新增公开 Rasterize 供增量落位。

### 4. DungeonBuilder + StoneDecorSpawner——大石块避让骨架
- PaintRoom 收集 per-room 骨架 → SpawnContent 传入;大石块(有碰撞)落点 ∈ 骨架格则重试(≤6 次)——
  内容阶段不回堵玩家口径验证留出的路网与入口(石块可破坏本不硬卡,堵主路体验差)。

## 数据(离线仿真 + EditMode 实测)

| 指标 | 旧 | 新 |
|---|---|---|
| 窄区房(全格口径) | 0.4%~1.6% | **0 / 1000**(仿真与 900 房 EditMode 实测均为 0) |
| Plain 保底房 | 54%~66% | 38%~40%(=自然无挖除率,验证失败退化≈0) |
| 障碍均值(18×11 / 28×15 / 36×21) | 0.6 / 2.1 / 2.9 | 1.5 / 3.8 / 4.7(Unity 实测均值 3.24) |

## 验收

- EditMode `RoomLayoutTests`(新增):900 房(3 尺寸×300 seed×随机门)玩家口径全过 + 门锚点可达;
  同 seed 双跑 Obstacles/Outline/Walkable 全等。
- 全量 EditMode **31/31 通过**;编译 0 错。
- Play 走查建议:同 seed 复现不变;F9 实走各房,障碍短墙观感密度应明显回升,不再出现"看得见进不去"的围合区。

## 教训(已回写开发必读)

1. **格级连通 ≠ 玩家可进**:格子验证的口径必须对齐物理碰撞体尺寸(净宽 ≥⌈胶囊宽⌉ 格)。
2. **骨架=路,不是禁放区**:"先铺路径再放障碍"的正确形态是细路网+落位验证,大片禁放区会让障碍退化、
   房间空旷(旧管线 2/3 房退化 Plain)。
3. **NUnit `Assert.That(coll, Does.Contain(x))` 对集合线性枚举**——36 万格级断言会拖到超时(1539s),
   批量断言改 `if (!set.Contains(x)) Assert.Fail(...)`(提速 60 倍,26s)。

## 关联

- v1.1.31 五职责架构、v1.1.39 旧骨架(本次重做)、v1.1.41 Skeleton 地皮融入(语义不变,快照变细路网)。
- 离线仿真工具:`Tools/RoomSkeletonSim/`(Unity 自带 dotnet 运行,OLD 复刻对照 + V2 调参;不参与 Unity 编译)。
