# v0.4.5.2 执行计划（Final2）

> 版本：v0.4.5.2 Final2
>
> 目标：通过新增 `WeaponController` 层，彻底解决 `WeaponPivot` 旋转控制权混乱问题，使 `WeaponAnimator` 成为真正可复用的本地旋转 Tween 播放器，并为 v0.5 技能、Boss、武器、多段攻击预留清晰扩展接口。

---

## 一、当前问题分析

### 问题 1：WeaponPivot 旋转控制权不唯一

在上一版计划中：

- `PlayerCombat` 直接修改 `WeaponPivot.rotation`
- `EnemyAI` 直接修改 `WeaponPivot.rotation`
- `WeaponAnimator` 播放 Tween 修改 `WeaponPivot.rotation`

三个系统同时控制同一个 `Transform`，导致：

- Tween 被每帧 `Update` 覆盖。
- 攻击结束后角度跳跃。
- 武器初始角度错误。
- `FullCircle` 旋转不足或超过 360°。

### 问题 2：WeaponSprite 长度管理权不唯一

上一版中 `PlayerCombat` / `EnemyAI` 都直接修改 `WeaponSprite.localScale` / `localPosition`，未来技能、Buff、换武器时容易遗漏或冲突。

### 问题 3：WeaponAnimator 仍隐含业务知识

虽然上一版已移除 `AttackData` 引用，但 `PlayerCombat` / `EnemyAI` 仍需计算 `Arc` / `FullCircle` 起止角度，这部分属于"武器表现逻辑"，应与"攻击状态机"分离。

---

## 二、最终架构

```
AttackData
    │
    ▼
PlayerCombat / EnemyAI  （攻击状态机、命中判定、伤害）
    │
    ▼
WeaponController        （武器朝向、攻击角度计算、Sprite 长度）
    │
    ▼
WeaponAnimator          （纯本地旋转 Tween 播放器）
    │
    ▼
WeaponPivot / WeaponSprite
```

**关键约定：**

- `WeaponController` 是 `WeaponPivot` 旋转与 `WeaponSprite` 视觉的**唯一管理者**。
- `WeaponAnimator` 只接收起止角度，执行 `DOLocalRotate`。
- `PlayerCombat` / `EnemyAI` 只负责攻击状态机与判定，不直接操作 `WeaponPivot` / `WeaponSprite`。

---

## 三、脚本职责

| 脚本 | 负责 | 不负责 |
|---|---|---|
| **AttackData** | 攻击参数配置：`AttackRange`、`AttackAngle`、`AnimationType`、`Damage`、`Cooldown`、`TargetLayer`、`Windup`、`Active`、`Recovery`、`Ease`、`ActiveMomentRatio` | 动画计算、运行逻辑 |
| **PlayerCombat** | 玩家攻击状态机、命中判定、伤害、调用 `WeaponController` / `WeaponAnimator` | 武器朝向、Sprite 缩放、Tween |
| **EnemyAI** | 敌人攻击状态机、调用 `EnemyCombat`、调用 `WeaponController` / `WeaponAnimator` | 武器朝向、Sprite 缩放、Tween |
| **WeaponController** | 武器朝向管理、攻击起止角度计算、`WeaponSprite` 长度/位置/宽度、攻击方向锁定 | Tween、状态机、伤害 |
| **WeaponAnimator** | 本地旋转 Tween 播放 | 攻击逻辑、方向、角度计算、Sprite 缩放 |
| **EnemyCombat** | 冷却、范围判定、伤害 | 动画 |

---

## 四、新增文件

1. **`Assets/Scripts/v0_4/WeaponController.cs`**
   - 新增武器控制中间层。
   - 管理 `WeaponPivot` 朝向、`WeaponSprite` 视觉、攻击角度计算。

---

## 五、修改文件

1. **`Assets/Scripts/v0_4/AttackData.cs`**
   - 保持现状，纯配置。

2. **`Assets/Scripts/v0_4/WeaponAnimator.cs`**
   - 改为纯 Tween 播放器。
   - 使用 `DOLocalRotate`。
   - 不读取 `AttackData`、不管理方向、不管理 Sprite。

3. **`Assets/Scripts/v0_3/PlayerCombat.cs`**
   - 引用 `WeaponController` 替代直接操作 `WeaponPivot`。
   - 不再读取鼠标，通过 `WeaponController.SetAimDirection` 通知方向。
   - 攻击流程调用 `WeaponController` / `WeaponAnimator`。

4. **`Assets/Scripts/v0_4/EnemyAI.cs`**
   - 引用 `WeaponController` 替代直接操作 `WeaponPivot`。
   - 通过 `WeaponController.SetAimDirection` 通知玩家方向。
   - 攻击流程调用 `WeaponController` / `WeaponAnimator`。

5. **`Assets/Prefabs/Player.prefab`**
   - 新增 `WeaponController` 组件。
   - `PlayerCombat` 引用 `WeaponController`。
   - `WeaponAnimator` 只保留 `weaponPivot`。

6. **`Assets/Prefabs/Enemy.prefab`**
   - 新增 `WeaponController` 组件。
   - `EnemyAI` 引用 `WeaponController`。

7. **`Assets/Data/PlayerAttackData.asset`**
   - `animationType = 0`（Arc）。

8. **`Assets/Data/EnemyMeleeAttackData.asset`**
   - `animationType = 1`（FullCircle）。

---

## 六、完整执行步骤

### Step 1：保持 AttackData

**修改文件：** `Assets/Scripts/v0_4/AttackData.cs`

**操作：**
- 保留所有配置字段。
- `AttackAnimationType` 枚举：`Arc`、`FullCircle`、`Thrust`、`Spin`、`Throw`。
- 不新增 `StartAngle` / `EndAngle`。
- 不添加动画计算。

**目的：**
- 维持纯配置职责。

---

### Step 2：新增 WeaponController

**新增文件：** `Assets/Scripts/v0_4/WeaponController.cs`

**操作：**
- 字段：
  - `Transform weaponPivot`
  - `Transform weaponSprite`
  - `float weaponWidth`
  - `AttackData attackData`
- 方法：
  - `SetAimDirection(Vector2 direction)`：
    - 若未锁定，计算相对于父对象的目标角度 `aimAngle`。
    - 设置 `weaponPivot.localRotation = Quaternion.Euler(0, 0, aimAngle)`。
  - `PrepareAttack()`：
    - 锁定当前 `aimAngle`。
    - 根据 `AttackData.AttackRange` 设置 `WeaponSprite` 长度与位置。
  - `EndAttack()`：
    - 解锁瞄准。
    - 恢复 `weaponPivot.localRotation` 到当前 `aimAngle`。
  - `GetAttackStartAngle()`：
    - `Arc`：`aimAngle - AttackAngle/2`
    - `FullCircle`：`aimAngle`
  - `GetAttackEndAngle()`：
    - `Arc`：`aimAngle + AttackAngle/2`
    - `FullCircle`：`aimAngle + 360`
  - `GetAttackRotateMode()`：
    - `Arc`：`RotateMode.Fast`
    - `FullCircle`：`RotateMode.FastBeyond360`
  - `SetWeaponLength(float range)`：
    - `weaponSprite.localScale = (weaponWidth, range, 1)`
    - `weaponSprite.localPosition = (range/2, 0, 0)`

**目的：**
- 成为 `WeaponPivot` 与 `WeaponSprite` 的唯一管理者。
- 集中计算攻击起止角度。

---

### Step 3：重写 WeaponAnimator

**修改文件：** `Assets/Scripts/v0_4/WeaponAnimator.cs`

**操作：**
- 仅保留字段：
  - `Transform weaponPivot`
- 仅保留方法：
  - `Play(float startAngle, float endAngle, float duration, Ease ease, RotateMode rotateMode, float activeMomentRatio, Action onActiveMoment)`
  - `Stop()`
- `Play()` 内部：
  - `weaponPivot.localRotation = Quaternion.Euler(0, 0, startAngle)`
  - `weaponPivot.DOLocalRotate(new Vector3(0, 0, endAngle), duration, rotateMode)`
  - 按 `activeMomentRatio` 触发回调。
- `Stop()` 内部：
  - 仅 `Kill` Tween。
  - **不修改** `weaponPivot` 的 `rotation` / `localRotation` / `position` / `scale`。

**目的：**
- 彻底降维为纯本地旋转 Tween 播放器。

---

### Step 4：修改 PlayerCombat

**修改文件：** `Assets/Scripts/v0_3/PlayerCombat.cs`

**操作：**
- 删除 `weaponPivot` / `weaponSprite` 直接引用。
- 新增 `WeaponController weaponController` 引用。
- `Update()`：
  - 若未攻击，计算鼠标方向并调用 `weaponController.SetAimDirection(mouseDir)`。
- `StartWindup()`：
  - 调用 `weaponController.PrepareAttack()`。
- `EnterActive()`：
  - 调用 `weaponAnimator.Play(...)`：
    ```csharp
    weaponAnimator.Play(
        weaponController.GetAttackStartAngle(),
        weaponController.GetAttackEndAngle(),
        attackData.ActiveDuration,
        attackData.AttackEase,
        weaponController.GetAttackRotateMode(),
        attackData.ActiveMomentRatio,
        OnActiveMoment
    );
    ```
- `UpdateRecovery()` 结束：
  - 调用 `weaponAnimator.Stop()`。
  - 调用 `weaponController.EndAttack()`。

**目的：**
- `PlayerCombat` 只负责状态机与判定，不再碰 `WeaponPivot`。

---

### Step 5：修改 EnemyAI

**修改文件：** `Assets/Scripts/v0_4/EnemyAI.cs`

**操作：**
- 删除 `weaponPivot` 直接引用。
- 新增 `WeaponController weaponController` 引用。
- `UpdateChase()`：
  - 计算玩家方向并调用 `weaponController.SetAimDirection(dir)`。
- `UpdateAttack()`：
  - `Windup` 阶段调用 `weaponController.SetAimDirection(dir)`。
  - `Active` / `Recovery` 阶段不调用。
- `ChangeState(State.Attack)`：
  - 调用 `weaponController.PrepareAttack()`。
- `EnterActive()`：
  - 调用 `weaponAnimator.Play(...)`（同 PlayerCombat）。
- `ChangeState(Attack → Chase)`：
  - 调用 `weaponAnimator.Stop()`。
  - 调用 `weaponController.EndAttack()`。

**目的：**
- `EnemyAI` 只负责状态机，不再碰 `WeaponPivot`。

---

### Step 6：调整 Prefab

**修改文件：**
- `Assets/Prefabs/Player.prefab`
- `Assets/Prefabs/Enemy.prefab`

**操作：**
- 在 `Player` / `Enemy` 根上新增 `WeaponController` 组件：
  - `weaponPivot`：引用 `WeaponPivot`
  - `weaponSprite`：引用 `WeaponSprite`
  - `weaponWidth`：0.15
  - `attackData`：引用对应 Asset
- `WeaponAnimator` 组件：
  - 仅保留 `weaponPivot`。
- `PlayerCombat` 组件：
  - 新增 `weaponController` 引用。
  - 移除 `weaponPivot` / `weaponSprite` 引用（如果有）。
- `EnemyAI` 组件：
  - 新增 `weaponController` 引用。
  - 移除 `weaponPivot` 引用（如果有）。
- `WeaponPivot`：
  - `localPosition = (0, 0, 0)`。
- `WeaponSprite`：
  - `localRotation = -90°`。
  - `localScale` / `localPosition` 由 `WeaponController` 运行时设置。

**目的：**
- 组件关系与新的三层架构对齐。

---

### Step 7：更新 Asset 配置

**修改文件：**
- `Assets/Data/PlayerAttackData.asset`
- `Assets/Data/EnemyMeleeAttackData.asset`

**操作：**
- `PlayerAttackData.asset`：
  - `animationType = 0`（Arc）
  - `attackAngle = 90`
  - `attackRange = 1.5`
- `EnemyMeleeAttackData.asset`：
  - `animationType = 1`（FullCircle）
  - `attackRange = 1.2`

**目的：**
- 玩家使用扇形，敌人使用 360° 旋转。

---

### Step 8：编译与验证

**操作：**
- `dotnet build Assembly-CSharp.csproj`。
- 目标：`0 警告，0 错误`。

**测试标准：**

#### Player
- [ ] 待机时武器跟随鼠标方向。
- [ ] `Arc` 动画以攻击瞬间方向为中心，左右各挥 `AttackAngle/2`。
- [ ] 使用 `RotateMode.Fast`。
- [ ] 攻击结束后平滑恢复跟随鼠标。
- [ ] 无角度跳跃。

#### Enemy
- [ ] 待机时武器朝向玩家。
- [ ] `FullCircle` 从当前朝向开始，完整旋转 360°。
- [ ] 使用 `RotateMode.FastBeyond360`。
- [ ] 动画结束后恢复朝向玩家。

#### 架构
- [ ] `WeaponController` 是 `WeaponPivot` / `WeaponSprite` 唯一管理者。
- [ ] `WeaponAnimator` 不读取 `AttackData`。
- [ ] `WeaponAnimator` 不管理方向、长度、恢复。
- [ ] `PlayerCombat` / `EnemyAI` 不直接操作 `WeaponPivot` / `WeaponSprite`。
- [ ] `EnemyCombat`、`PlayerController`、移动框架未被修改。

---

## 七、为什么新方案不会导致旧问题

### 为什么不会三个系统抢 Rotation？

- `WeaponController` 是 `WeaponPivot` 旋转的唯一管理者。
- `PlayerCombat` / `EnemyAI` 只调用 `WeaponController.SetAimDirection()`，不直接修改 Transform。
- `WeaponAnimator` 只在 `Play()` 期间通过 Tween 修改 `localRotation`。
- `WeaponController` 在 `PrepareAttack()` 时锁定方向，`EndAttack()` 时恢复，避免 Tween 与 Update 冲突。

### 为什么 Arc 围绕瞄准方向？

- `WeaponController` 在 `PrepareAttack()` 时锁定当前 `aimAngle`。
- `GetAttackStartAngle()` / `GetAttackEndAngle()` 以 `aimAngle` 为中心计算。
- `WeaponAnimator` 使用 `DOLocalRotate` 在本地空间播放，中心精确。

### 为什么 FullCircle 能完整 360°？

- `GetAttackStartAngle()` = `aimAngle`
- `GetAttackEndAngle()` = `aimAngle + 360`
- `GetAttackRotateMode()` = `RotateMode.FastBeyond360`
- `WeaponAnimator` 仅执行，不转换角度。

### 为什么攻击结束不会跳角度？

- `WeaponAnimator.Stop()` 只 Kill Tween，不修改 Transform。
- `WeaponController.EndAttack()` 负责将 `localRotation` 恢复为当前 `aimAngle`。
- 恢复逻辑在 `WeaponController` 中统一处理。

---

## 八、未来扩展方案

### v0.5 技能系统

- 新增 `SkillData` 继承或扩展 `AttackData`。
- `PlayerCombat` 支持技能队列，每个技能使用同一个 `WeaponController` / `WeaponAnimator`。
- 例如：
  - 蓄力斩：`Windup` 延长，`Arc` 角度加大。
  - 旋风斩：`FullCircle` 连续播放两次。
- 无需修改 `WeaponAnimator`。

### v0.6 Boss 系统

- 新增 `BossAI`。
- `BossAI` 控制攻击状态机。
- `WeaponController` 计算特殊起止角度：
  - 扇形扫射：`θ - 110°` → `θ + 110°`
  - 双圈旋风：`θ` → `θ + 720°`
- 调用同一个 `WeaponAnimator.Play(...)`。
- 无需修改 `WeaponAnimator`。

### v0.7 武器系统

- 新增 `WeaponView` 组件。
- `WeaponController` 将 `SetWeaponLength` 等视觉逻辑委托给 `WeaponView`。
- 换武器时替换 `WeaponView` 或修改 `WeaponSprite`。
- `WeaponAnimator` 无需改动。

### 多段攻击

- `PlayerCombat` 维护连击计数。
- 每段攻击调用 `WeaponController.PrepareAttack()` 与 `WeaponAnimator.Play()`。
- 每段可配置不同的 `AttackData`（如第一段 Arc 90°，第二段 Arc 120°）。
- `WeaponAnimator` 无需改动。

---

## 九、待确认

1. 是否确认 `v0.4.5.2_Final2.md` 执行计划？
2. 是否可以从 **Step 1** 开始执行代码修改？
3. `PlayerAttackData` 的 `attackAngle` 保持 `90°` 还是改为 `120°`？
