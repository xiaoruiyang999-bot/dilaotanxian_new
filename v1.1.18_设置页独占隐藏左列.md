# v1.1.18 设置页独占（隐藏左侧暂停三键）

## 需求

进入设置后，左侧"继续游戏/设置/返回大厅"三个暂停主键整体隐藏，画面只保留设置内容（音量设置/操作设置两键与其子页）。

## 改动（PausePanel）

- `ToggleSettings`：显示设置 = `menuButtonsRoot.gameObject.SetActive(false)` 整列隐藏（原为平移到 x=-245 让位）；收起设置 = 恢复左列。
- 设置面板 `anchoredPosition` (300,-25) → **(0,-25) 居中独占**。
- 设置主页新增**返 回**键（回暂停主键）：因"设置"按钮随左列隐藏，收起设置需要显式出口——
  `BuildBackButton` 参数化点击目标（子页缺省回设置主页，主页传 `ToggleSettings`）。
- 音量页移除"再次点击'设置'收起"提示（该键已不可见）；主页两键位置调整为 +70/-20，返回键 -150。

## 验收

1. Esc → 设置：左列三键消失，设置面板居中，仅"音量设置/操作设置/返 回"；
2. 主页"返 回"→ 恢复左侧三键（设置收起）；子页"返 回"→ 回设置主页；
3. 继续游戏/返回大厅/Esc 行为不变。

## 关联

v1.1.16 两级导航结构不变；v1.1.12 GraphicRaycaster（新增返回键自动可点）。
