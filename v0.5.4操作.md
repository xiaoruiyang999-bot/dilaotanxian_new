# v0.5.4 完整楼层循环 操作报告

> 本文档记录 v0.5.4 的实际执行内容与结果，对应《v0.5 Dungeon 系统开发计划书》五-E 节与《v0.5.4_正式计划》。
> 前置版本：v0.5.3（房间类型系统，见 `v0.5.3操作.md`）。

## 一、版本目标

1. 楼层循环闭环：Boss Cleared → 奖励宝箱 + 传送门 → 下一层（单场景重建）→ 难度递增 → 死亡重开；
2. 传送门占位美术：灰石块中央嵌蓝色漩涡（双层差速旋转）；
3. 宝箱占位美术升级：三段式（暗盒身 + 亮金双盖片）+ 开箱动画 + 缺口刷新道具。

## 二、核心设计落实

| 设计 | 落实情况 |
|---|---|
| RunManager 楼层总控 | 持有 FloorNumber/MainSeed；订阅 Boss 房 `OnRoomCleared` → 房中心 ±1.5 生成奖励宝箱 + 传送门（挂 contentRoot，随 dungeonRoot 清理）；Start 延迟一帧初始化，与 DungeonManager.Start 顺序无关 |
| 楼层 seed | `floorSeed = mainSeed + floor × 104729`，实测 seed 公式两轮验证正确（9419531→9628989、178761797→178971255） |
| 单场景重建 | `DungeonManager.Cleanup()` = `Builder.ClearAll()`（清双 Tilemap + 销毁 dungeonRoot 全部子物体 + 清注册表）+ Layout 置空；Generate(seed) 重载入参，玩家传送 + 相机 Snap 沿用 v0.5.0 链路 |
| 难度注入 | `EnemySpawner.Spawn` 加可选参（floorNumber/config）：数量 +1/层（封顶 8），HP 经 `EnemyStats.ApplyFloorScale` → `EnemyHealth.ScaleMaxHealth` 缩放（实测第 2 层 3.45/23、第 3 层精英 7.8）；dmgMul 预留恒 1 |
| 死亡重开 | 订阅 `Health.OnDeath` → 2 秒 → floor=1 + mainSeed 重roll + Cleanup/Generate + `Health.ResetHealth` + `ModifyArmor` 回满 + `PlayerController.Respawn()`（颜色/速度恢复） |
| 传送门美术 | Portal.prefab：StoneBase 灰石 (1.4×1.8) + 碎石×2 + 蓝圆盘 + 浅蓝白旋转臂（2s/圈）+ 亮蓝核（3.5s 反向），DOTween 无限旋转；消耗态不压暗（覆盖 `OnConsumed`） |
| 宝箱三段式 | Chest.prefab 重构：BoxBody 暗金棕 1.0×0.6 + LidTop/LidBottom 亮金各 1.0×0.3（合计与盒身等大）；walk-over → 盖片上下各移 0.25（保持 0.05 重合区）→ 缺口刷新绿球道具（pop-in）→ 结算 +2HP → 道具淡出、箱保开启态 |
| Interactable 基类钩子 | `OnTriggerEnter2D` 收口到新增 `protected virtual OnConsumed`（默认 = 旧行为），Shrine/Supply 零改动零影响 |

## 三、新增/修改文件

### 3.1 脚本

| 文件 | 说明 |
|---|---|
| `Core/RunManager.cs`（新建） | 楼层总控 + 两个 Debug 菜单（Clear Boss Room / Kill Player，仅 Editor） |
| `Interaction/PortalInteractable.cs`（新建） | 传送门交互 + 漩涡旋转动效 |
| `Interaction/ChestInteractable.cs`（重写） | 开箱动画流程（盖片分开 → 道具 pop-in → 结算 → 淡出） |
| `Interaction/Interactable.cs` | +`OnConsumed` 虚方法钩子（2 行改动 + 1 方法），`SetConsumedVisual` 升 protected |
| `Core/DungeonManager.cs` | +`Cleanup()`、`Generate(int seedOverride)`、`BossRoom`、`FloorNumber` 透传属性 |
| `Core/DungeonBuilder.cs` | `Build`/`SpawnContent` 透传 floorNumber + config |
| `Content/EnemySpawner.cs` | 楼层数量递增 + HP 缩放注入（可选参数，旧调用兼容） |
| `now_use/EnemyStats.cs` | +`ApplyFloorScale(hpMul, dmgMul)`（HP 转调 EnemyHealth；dmgMul 预留） |
| `now_use/EnemyHealth.cs` | +`ScaleMaxHealth(mul)`（上限倍乘 + CurrentHealth 同步刷新） |
| `now_use/PlayerController.cs` | +`initialColor` 缓存 + `Respawn()`（死亡重开恢复） |

### 3.2 Prefab / 场景

- 新建 `Prefabs/Dungeon/Portal.prefab`（石块+漩涡六件结构）、`Prefabs/Dungeon/ChestItem_Heal.prefab`（绿圆道具占位）
- 重构 `Prefabs/Dungeon/Chest.prefab`（三段式；同路径覆盖保持 GUID，SO 引用不断）
- 场景：DungeonSystem +RunManager（dungeonManager/rewardChestPrefab/portalPrefab 接线），场景已保存
- **用户反馈调整**：宝箱/传送门根节点 scale → **2.0**（先 1.4 后用户改为 2.0；几何比例不变，lidOffset 在局部空间，重合区不变量自动保持）

## 四、验收记录（自动化 + 截图，2026-07-23）

| 验收项 | 结果 |
|---|---|
| 编译 | ✅ 0 Error / 0 Warning |
| 宝箱开箱 | ✅ walk-over 触发、盖片上下各移 0.25（实测 local y ±0.15→±0.40）、+2HP 结算、道具 pop-in 后淡出；截图 `v054_chest_closed.png` / `v054_chest_open.png` |
| 传送门美术 | ✅ 灰石块+蓝色漩涡、旋转臂持续转动（截图抓拍到旋转中间态）；`v054_portal.png` |
| Boss 结算 | ✅ 清 Boss 房 → 宝箱 + 传送门在房中心 ±1.5 生成，挂 contentRoot |
| 踩门换层 | ✅ 旧楼层零残留（Portal 查找 =0）、新图生成、玩家到起始房、floorSeed 公式实测正确 |
| 难度递增 | ✅ 第 2 层普通 3.45（3×1.15）/ Boss 23（20×1.15）；第 3 层精英 7.8（6×1.3）；数量 +1/层 |
| 玩家状态跨层 | ✅ HP/护甲不自动回复 |
| 死亡重开 | ✅ 受控测试 + 3 次实战（玩家滑行误入敌房被打死）：floor=1、mainSeed 重roll、HP/护甲回满、颜色恢复绿 (0.2,0.8,0.3) |
| 连续 3 层 | ✅ 1→2→3 层推进无泄漏、无 Missing 引用、无 Console 报错 |
| Validate 1000 Seeds | ✅ 失败 0/1000，统计与 v0.5.3 一致（生成链路零回归） |
| DOTween safe mode 警告 | ✅ 修复后全套流程（开箱 + 踩门换层）零警告复现 |

### 待人工 Play Mode 验收

- [ ] 传送门/宝箱 2.0× 放大后的观感；宝箱开箱动画手感
- [ ] 完整打一局：杀 Boss → 开箱 → 踩门 → 下层难度体感 → 死亡重开

## 五、遇到的问题与修复

1. **DOTween safe mode 报 missing target（用户反馈，已修两轮）**：第一轮——传送门无限旋转 / 道具淡出 tween 在对象销毁时未自动 kill，全部加 `SetLink`；第二轮（人工验收期复现 1 条）——根因是 v0.4.5.1 遗留的 `WeaponAnimator.DOLocalRotate`（挥砍动画）无 SetLink，敌人挥刀途中死亡、武器目标销毁即触发，`rotationTween`/`momentTween` 均已补 `SetLink`。**工程经验固化：本项目一切 tween 创建即 SetLink。**
2. **敌人 HP 缩放打错字段（计划期发现，设计规避）**：`EnemyHealth` 持有独立 `maxHealth`（Awake 初始化 CurrentHealth），`EnemyStats.maxHealth` 只是手工同步字段——缩放落点定为 EnemyHealth 并同步刷新当前血量，EnemyStats 那份一并缩放保持一致。
3. **REST transform 参数踩坑**：`gameobject_set_transform` 的参数名是 `posX/posY/posZ`（不是 x/y/z），错误参数静默失败导致一度以为「传送不回弹」；刚体传送优先用 Transform 写（rb.position 写在某些时序下 body/transform 不同步）。
4. **Play 循环冻结（环境，非游戏 bug）**：第 2、3 局 Play 时间停止（物理/相机/触发全停但 REST 与渲染正常），用户经编辑器 UI 停开 Play 后恢复；第 4 局全部验收在健康循环下完成。
5. **验收期玩家三次意外死亡**：拖玩家滑行测试时误入敌房被打死——反而三次实战验证了死亡重开全流程。
6. **验收手法固化**：walk-over 触发用「禁用 PlayerController + linearVelocity 注入」（沿用 v0.5.1 经验）；新增两个 Debug 菜单（Clear Boss Room / Kill Player）解决「Boss 杀不死、玩家死不了」的验收难题；中文菜单名在 curl 传输乱码，菜单路径一律用英文。

## 六、扩展预留（为后续版本埋好的接口）

| 预留 | 用途 |
|---|---|
| `ChestInteractable.ApplyEffect` + `itemPrefab` | 技能/装备系统：换 prefab = 换奖励表现，ApplyEffect = 奖励发放点 |
| `Interactable.OnConsumed` 钩子 | 任何「先演后结算」的交互物（祭坛动画、购买演出） |
| `ApplyFloorScale(hpMul, dmgMul)` 的 dmgMul | 攻击递增（需 AttackData 实例化策略，属未来版本） |
| `RunManager`（floor/mainSeed/订阅点） | 存档（主seed+floor+已清房id+玩家状态即可还原整局）、楼层主题 |
| `PortalInteractable` | 分支传送门（选层/挑战房） |
| Debug 菜单两枚 | 后续版本验收复用 |
