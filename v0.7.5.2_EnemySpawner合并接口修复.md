# v0.7.5.2 EnemySpawner 合并接口修复

## 问题

战斗/MCP 分支与 main v0.7.5 合并后，`EnemySpawner` 保留了旧战斗实现的接口调用，但当前 main 中对应组件已经没有这些接口，导致三个 `CS1061` 编译错误：

- `EnemyHealth.ApplyMaxHealthMultiplier`
- `EnemyAI.SetBehaviorRng`
- `EnemyCombat.SetCombatRng`

这些代码位于未产生文本冲突的自动合并区域，因此 Git 合并阶段没有提示。

## 修复

- Boss 血量强化改为当前生命组件的正式接口 `EnemyHealth.ScaleMaxHealth(1.5f)`。
- 删除行为与战斗随机源注入。当前 `EnemyAI` 不接收外部随机源，`EnemyCombat` 也没有随机招式选择，保留空兼容接口会掩盖架构差异。
- 全局搜索 `Assets/Scripts/now_use`，确认上述三个旧接口已无引用。

## 验证

- `EnemySpawner.cs` 三个截图所示 `CS1061` 的调用点已消除。
- `git diff --check` 通过。
- UnitySkills 本地服务当时未监听 8090–8100，未能调用编译反馈接口；Unity `Editor.log` 末尾未发现新的 `CS` 编译错误。

