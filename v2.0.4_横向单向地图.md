# v2.0.4 横向单向地图(LinearHorizontal 拓扑)

> 对应《无名之地_完整游戏开发文档_V2》§4(微观二维宏观一维)/§10(DAG)/§22(v2.0.4 横向 RoomPlan)。
> 用户诉求:地图必须呈现横版形态——旧 v1.x"四向网格迷宫"不是 V2 要的东西。

## 诊断

V2 文档的空间模型是「**DAG 地图 UI(战略选择)+ 横向长房间逐节点推进(左入右出)**」,
不是一张全连通大迷宫;而工程跑的还是 v1.x 四向网格生成器(8~12 房上下左右互连)。
分叉选择属于 v2.0.5(地图 UI);本轮先把**空间形态**改对——横向链拓扑落地。

## 实现(零推翻,最大化复用)

- **`DungeonConfig`**:新增 `topology` 枚举(`LinearHorizontal` **默认** / `LegacyGrid` 回退口)
  + `linearRoomWidth=34 × linearRoomHeight=12`(长宽比 ~2.8:1,V2 §4.3 契约 2.2~4:1,约 2 屏宽);
- **`DungeonGenerator.BuildLinearChain`**:房间 `gridPos=(列,0)` 依次右移,相邻两房建立唯一
  连接——**下游全复用**:共享墙线由既有 Builder 开门(=左入右出)、房间塑形/内容/轮次/门触发
  /相机 Bounds 全部继承;Start 最左、Boss 最右;seed 确定性复现;
- **Boss 固定仪式厅拓扑隔离**:2×2 南入口合同是旧迷宫体系专属,Linear 下 Boss 走普通塑形
  (横向 Boss 厅模板待 v2.0.8);
- **测试**:旧四向迷宫语义门禁全部显式锁 `LegacyGrid`(不受默认值漂移);新增 3 项 Linear 门禁
  ——全连接严格水平(垂直门=违反左入右出,直接红)/纯链无分叉/Boss 最右/Start 最左/
  同 seed 确定性/全可达。

## 验证

Roslyn 0 错 + EditMode **111/111**(旧 108 + 新 3)。

## Play 走查预期

进地牢:一条**从左向右的长廊链**——每间是 2 屏宽长矩形,右墙中央门通下一间,一路向右推进到
最右 Boss 房;相机随推进持续右移(边界锁定贴房间边);怪物轮次/障碍/商店宝箱房照常。
要回旧四向迷宫:DungeonConfig 资产把 Topology 切 LegacyGrid。

## 遗留(按 V2 §22 顺序)

- v2.0.5:DungeonGraph 纯数据(分叉/汇聚/节点状态)+ 地图 UI(节点选择)——当前水平链是
  DAG 的 MVP 特例,升级时 BuildLinearChain 换成图驱动布局,Builder 零改动;
- 横向 Boss 厅模板(v2.0.8);房间内部段落节奏(推进段/战斗段/奖励段,V2 §4.4)。
