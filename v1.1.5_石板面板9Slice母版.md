# v1.1.5 石板面板 9-Slice 母版入库

## 1. 资产

| 路径 | 角色 |
|---|---|
| `Assets/UI/Panel/panel_stone.png` | **母版**（按用户指定路径；Sprite Editor 里可直接微调 Border） |
| `Assets/Resources/UI/panel_stone.png` | **运行时副本**（代码 UI 走 Resources 惯例——SlotFrame 同模式；改图后两处同步） |
| `Assets/Scripts/now_use/UI/PanelSprite.cs` | 统一加载器 + 三行接入助手 |

源图：`E:\WeGameApps\The animation of Unity\狼人\石板界面_面板裁剪.png`（594×366 RGBA）。

## 2. 导入设置（两份同款）

- Texture Type = **Sprite (2D and UI)**，Sprite Mode = **Single**（R3：显式声明）
- Filter Mode = **Point**（像素风）；Compression = **None**（DefaultTexturePlatform textureCompression 0）
- PPU = **1**：UI 精灵 Set Native Size = 原始像素尺寸（594×366）
- **Sprite Border = L34 / B34 / R37 / T49**（像素级实测砖框内沿，含内侧阴影环；四边非对称但 9-Slice 各边独立合法）

## 3. Border 实测方法（复现用）

对整图逐行/列求亮度均值，以"中央暗区平均亮度 44.4 ± 8"为界，从四边向内找首条持续 8 行/列落入该亮度的位置：
上 49px（含顶部高光檐口，故最厚）/ 下 34px / 左 34px / 右 37px；中央可拉伸区 522×282px。
内侧阴影环划入边框带（随单轴拉伸呈渐变延伸，观感正确）。

## 4. 使用方式

Inspector 手搭：Image 组件 → Source Image = panel_stone → **Image Type = Sliced**。

代码构建 UI（PausePanel/DeathPanel/ClassSelectUI 等均为运行时代码构建）三行接入：

```csharp
Image img = go.AddComponent<Image>();
img.sprite = PanelSprite.Stone;          // 或 PanelSprite.ApplyStonePanel(img, 兜底色)
img.type = Image.Type.Sliced;
```

断链安全：素材缺失时 Stone 为 null，ApplyStonePanel 落到纯色兜底，不抛错。

## 5. 面板改造注意（下一轮接入暂停/设置/背包/结算时的设计约束）

- 现有面板内容多用**全屏比例锚点**（如 PausePanel 标题在屏幕 0.78 高度）——套面板需要把内容改为面板局部坐标（统一挂在 Panel RectTransform 下重排），不是换背景一张图的事。
- 面板 RectTransform 尺寸建议 ≥ 边框和的 2 倍（水平 ≥71×2、垂直 ≥83×2）并留出中央内容区；拉伸只发生在砖框内侧的暗区。
- 按钮若也要石板风格，可另切小图做第二张 9-Slice（本母版边框过厚不适合小按钮）。

## 6. 红线遵守

- R3：手写 meta 显式 spriteMode=Single + internalIDToNameTable/nameFileIdTable 注册文件名（214 坑位核对无误）。
- 零场景手术：纯资产+代码新增，Play 即生效（Unity 需先导入新素材）。
- 兜底路径：所有消费点素材缺失自动回退纯色，不破坏现有 UI。
