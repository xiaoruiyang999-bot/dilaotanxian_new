# v1.1.28 墙体替换石墙素材 + 一块厚墙结构

## 需求（用户三条）

1. 墙体现用石墙素材替换（`主界面素材\石墙装饰_切割抠图`，8 张 prop，41~53px 宽/64~79px 高）；
2. 素材大小不一致但可用——**按宽归一**（宽恒 = 1 格，高按原比例 1.3~1.7 单位墙身），
   **碰撞体以 prop_01 宽度基准**（= 整格 1×1）；
3. **墙体只有一块厚**（原结构相邻房两条墙线 = 两块厚）。

## 实现

### 1. WallPropTileset（新）

`Resources/Art/Decor/WallProps/prop_NN` 加载（Single/Point/不压缩/PPU85，与地皮同契约）：
Tile.transform 缩放 = 1/精灵宽（宽归一）+ 上抬 (h−1)/2（底边贴格底、墙身向上生长）；
**ColliderType = Grid**（整格 1×1 满碰撞——不随精灵透明形状走，R19 攻击平面对齐不受影响）。
缺素材回退白方块墙瓦。墙体变体按格坐标哈希随机引用（同 seed 复现）。

### 2. 一块厚墙结构（DungeonBuilder 几何重构）

- `CellWidth/Height` = roomW+2 → **roomW+1**（格 = 西/南墙 1 + 内部；房间排布收紧 1 单位）；
- 墙线责任划分：**西列与南行本房必画；东列/北行仅当该方向无邻房时画在自身外沿**（占用集按
  各房 span 覆盖粗格判定）——任意两房之间恰好一条墙线（离线仿真 3×3：无重叠/无缝/门对齐 ✓）；
- 内部 = [xMin+1, xMax) × [yMin+1, yMax)（`InteriorRect` 公式同步，Room.Bounds 尺寸不变仍 roomW×roomH）；
- **门洞单列打穿**：共享墙列 = 西房 xMax（即东房 xMin）；门矩形 2 厚 → **1 厚**
  （`Door.Init(doorRect.size)` 自动缩放门体与碰撞）；
- 墙体铺装走 `SetWallTile`（随机石墙引用），v1.1.23 WallAutotiler（16 掩码方案）休眠未调用
  （石墙随机引用方案与其互斥，保留待选）。

## 验收

1. Reload → Play：墙为石墙贴图（随机变体、宽=墙厚、高约 1.3~1.7 向上），**房间间隔一块厚**；
   F9 验证墙碰撞 = 整格、门洞 1 格深；
2. 贴墙行走/敌人攻击/子弹撞墙正常（碰撞几何仍是整格 1×1，R19 平面不变）；
3. 同 seed 重开一致；房间内部尺寸（18×11）不变，仅整体地图更紧凑。

## 关联

R19（碰撞平面）/ v1.1.22~23（塑形保护带与门格集自适应 1 厚门洞，无需改）/ v1.1.24 下半身胶囊（对整格墙无影响）。
