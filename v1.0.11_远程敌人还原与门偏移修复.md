# v1.0.11 远程敌人还原 + 门偏移修复

> 日期：2026-08-31  
> 状态：已落盘；编译 0/0；门禁 **10/10**（新增远程接线防回归项）；实例化验证 attackData/projectileData 双双就位。

## 一、远程射手攻击 AI 丢失——双重根因

### 根因 A：合并丢失远程代码路径
v0.7.5 合并把 EnemyCombat 换成纯近战三态机（旧版远程链 ProjectileEmitter/旧 Projectile 被当"v0.5 遗产"删除）；WeaponBehavior.Ranged 只服务玩家。远程敌人实际在做一个 8 码隐形近战挥砍。

### 根因 B：Enemy_Ranged 变体 prefab 从未正确接线（更致命）
变体的 `attackData` 覆盖目标 fileID 指向的是 **WeaponController / AttackQuery** 组件——**EnemyCombat.attackData 一直是 null**（Awake 即警告"未配置 AttackData"、TryStartAttack 恒 false）→ 远程敌人从未发起过任何攻击。另有指向已删组件的死条目（muzzle→ProjectileEmitter；Enemy_Boss×3→BossPhaseController）。该变体带有手改痕迹（假 fileID 风格与此前 AttackQuery 假 GUID 同源）。

### 修复
1. **EnemyCombat 远程分支**（v1.0.11）：新增 `[SerializeField] ProjectileData projectileData`（null=近战，全部既有敌人零变化）。远程时：预警照常走 AttackData（同源），Windup 朝向目标锁定方向，**Active 开始即发射** `Projectile.Launch`（MCP 分支 a7f358d 的修复结论：预警=发射同刻同向、无二次 LOS 门控——当年门控不一致正是打不出弹的根因）；不 BeginSwing/不播挥砍。
2. **新资产 `ProjectileData_EnemyBolt`**（敌方弩矢）：仿 Bolt，targetLayer **Default+Obstacle**（现有四个投射物资产都是玩家向 Enemy+Obstacle，敌人直用会打不到玩家）、damage 10、speed 10（占位数值待调）。
3. **变体重接线**：EnemyCombat.attackData→AttackData_Ranged、projectileData→EnemyBolt（目标真实 fileID 8337342438853369050）；删除全部死条目。
4. **新门禁测试** `RangedEnemyWiringTests`：断言 Enemy_Ranged.IsRanged——接线再断立刻红。

## 二、门偏移（"空气墙"）修复

`DungeonBuilder.CreateDoor` 把**瓦片坐标**的 doorRect.center 直接当**世界坐标**实例化门——与真实门洞差了网格原点+半格偏移，门体/碰撞与洞口错位（也解释了此前狼人"卡门"观感）。改经 `wallsTilemap.GetCellCenterWorld` 换算到门洞中央格子中心。

## 三、本轮教训（回写）

- **Python `re.sub(count=0)` 是"不限次数"不是"不替换"**——本次把资产内部 m_Script guid 误换成资产自身 guid（自引用→导入失败"Asset not found"）。
- **CRLF prefab 里插 LF 行尾的条目会被 Unity 静默丢弃**（本次 projectileData 首次插入失效的根因）；手写 YAML 必须先探测文件行尾。
- 手改 prefab 变体的假 fileID 会静默失效（覆盖不生效无任何报错）——**变体覆盖必须核对社会组件真实 fileID**，批量审计脚本已随本次产出（可复用：遍历变体条目比对基座 ID 集）。

## 四、验证

- 编译 0/0；门禁 10/10（9 项既有 + 远程接线新项）。
- 实例化 Enemy_Ranged 直读：`attackData=AttackData_Ranged`、`projectileData=ProjectileData_EnemyBolt` ✓（实例已清理、场景已存）。
- 待人工实测：远程射手预警→发射弩矢→命中玩家掉血；门与洞口对齐、无空气墙。

## 五、修改文件

`EnemyCombat`（远程分支）、`DungeonBuilder`（门坐标换算）、新 `ProjectileData_EnemyBolt.asset`、`Enemy_Ranged.prefab`（重接线+清死条目）、`Enemy_Boss.prefab`（清死条目）、新 `RangedEnemyWiringTests.cs`。
