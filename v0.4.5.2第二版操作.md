# v0.4.5.2 第二版操作：攻击动画框架统一重构

## 版本信息
- **版本号**：v0.4.5.2
- **版本名称**：攻击动画框架统一重构
- **前置版本**：v0.4.5.1
- **目标**：将 WeaponAnimator 从 Enemy 专用改为 Player/Enemy/Boss 通用，统一 AttackData 为唯一攻击配置来源。

---

## 一、脚本层重构

### 1. AttackData 升级为唯一攻击配置源
**文件**：`Assets/Scripts/v0_4/AttackData.cs`

- 移除固定字段 `attackStartAngle` / `attackEndAngle`
- 新增攻击核心字段：
  - `attackRange`：攻击距离（扇形半径）
  - `attackAngle`：攻击扇形全角
  - `attackDamage`：伤害
  - `attackCooldown`：冷却
  - `targetLayer`：可命中目标层
- 自动计算属性：
  - `StartAngle = -attackAngle / 2`
  - `EndAngle = attackAngle / 2`

### 2. WeaponAnimator 通用化
**文件**：`Assets/Scripts/v0_4/WeaponAnimator.cs`

- 移除固定 `-70°/70°` 角度
- 根据 `AttackData.AttackAngle` 自动计算旋转范围
- 新增 `weaponSprite` 引用，根据 `AttackData.AttackRange` 自动缩放武器长度
- 保留 `ActiveMomentRatio` 命中回调机制
- 可复用于 Player / Enemy / Boss / 召唤物

### 3. PlayerCombat 重写
**文件**：`Assets/Scripts/v0_3/PlayerCombat.cs`

- 移除对 `IAttack` / `WarriorAttack` 的依赖
- 使用 `AttackData` + `WeaponAnimator`
- 实现完整 Windup → Active → Recovery 三段式状态机
- Active 阶段调用 `WeaponAnimator.PlayAttack()`
- 命中回调执行扇形攻击判定（范围/角度/目标层均来自 AttackData）

### 4. EnemyCombat 读取 AttackData
**文件**：`Assets/Scripts/v0_4/EnemyCombat.cs`

- 攻击范围、伤害、冷却均从 `AttackData` 读取
- 保留 `IsInAttackRange` / `TryAttack` / `CanAttack` 接口
- 新增 `SetAttackData()` 供 v0.5 技能切换

### 5. EnemyAI 适配
**文件**：`Assets/Scripts/v0_4/EnemyAI.cs`

- 攻击指示器半径从 `AttackData.AttackRange` 读取
- 移除对 `EnemyStats.AttackRange` 的依赖

### 6. EnemyStats 兼容性处理
**文件**：`Assets/Scripts/v0_4/EnemyStats.cs`

- 攻击字段（AttackRange / AttackDamage / AttackCooldown）标记 `[Obsolete]`
- 保留属性仅作兼容 fallback，推荐通过 AttackData 配置

### 7. 旧系统弃用
**文件**：
- `Assets/Scripts/v0_3/WarriorAttack.cs`：置空并标记 `[Obsolete]`
- `Assets/Scripts/v0_3/Interfaces/IAttack.cs`：标记 `[Obsolete]`

---

## 二、资源配置

### 新建 PlayerAttackData
**路径**：`Assets/Data/PlayerAttackData.asset`

| 字段 | 值 | 说明 |
|------|-----|------|
| windupTime | 0.2 | 前摇 |
| activeDuration | 0.2 | 攻击释放 |
| recoveryTime | 0.3 | 后摇 |
| attackRange | 1.5 | 玩家攻击距离 |
| attackAngle | 90 | 扇形角度 |
| attackDamage | 1 | 伤害 |
| attackCooldown | 0.5 | 冷却 |
| targetLayer | Enemy | 命中敌人层 |

### 更新 EnemyMeleeAttackData
**路径**：`Assets/Data/EnemyMeleeAttackData.asset`

| 字段 | 值 | 说明 |
|------|-----|------|
| windupTime | 0.25 | 前摇 |
| activeDuration | 0.25 | 攻击释放 |
| recoveryTime | 0.35 | 后摇 |
| attackRange | 1.2 | 敌人攻击距离 |
| attackAngle | 140 | 扇形角度 |
| attackDamage | 1 | 伤害 |
| attackCooldown | 1 | 冷却 |
| targetLayer | Player | 命中玩家层 |

---

## 三、场景与 Prefab 改造

### Player
- 新增 `WeaponPivot` 子物体
- `WeaponPivot` 下新增 `WeaponSprite`（SpriteRenderer）
- Player 根物体新增 `WeaponAnimator` 组件
- 删除旧 `Profession` / `WarriorAttack` 子物体
- `PlayerCombat` 配置 `AttackData` + `WeaponAnimator`

### Enemy
- 已有 `WeaponPivot` / `WeaponSprite` 结构保留
- 根物体新增 `WeaponAnimator` 组件
- `EnemyCombat` / `EnemyAI` 配置 `AttackData`

### Prefab
- 新建 `Assets/Prefabs/Player.prefab`
- 新建 `Assets/Prefabs/Enemy.prefab`
- 场景中 Player / Enemy 均为 Prefab 实例

---

## 四、Bug 修复

### Enemy 初始血条为黑色
**文件**：`Assets/Scripts/v0_4/EnemyHealth.cs`

**原因**：`WorldSpaceHealthBar.OnEnable()` 执行顺序可能早于 `EnemyHealth.Awake()`，导致血条第一次刷新时读到 `CurrentHealth = 0`。

**修复**：在 `Awake()` 初始化后主动触发一次 `OnHealthChanged` 事件。

```csharp
void Awake()
{
    CurrentHealth = maxHealth;
    IsDead = false;
    OnHealthChanged?.Invoke(CurrentHealth, maxHealth); // 新增
}
```

---

## 五、验证结果

- [x] WeaponAnimator 不再使用固定角度
- [x] StartAngle / EndAngle 由 AttackAngle 自动计算
- [x] WeaponSprite 长度由 AttackRange 自动缩放
- [x] Player / Enemy 结构完全一致
- [x] PlayerCombat / EnemyCombat / WeaponAnimator 读取同一份 AttackData
- [x] ActiveMomentRatio 命中回调保留
- [x] EnemyAI Windup/Active/Recovery 状态机未改动
- [x] Unity Console 无 Error / 无 Warning
- [x] Play Mode 运行正常
- [x] Player.prefab / Enemy.prefab 引用完整
- [x] Enemy 初始血条正常显示红色

---

## 六、v0.5 技能系统扩展方式

新增 Boss / 技能 / 武器时：

1. 新建 `AttackData` asset，配置新的 `AttackRange` / `AttackAngle` / `AttackDamage` / `AttackCooldown` / `TargetLayer`
2. 通过 `SetAttackData()` 或 Inspector 赋值给对应 `PlayerCombat` / `EnemyCombat` / `EnemyAI` / `WeaponAnimator`
3. **无需修改 WeaponAnimator 或战斗代码**

---

## 修改文件清单

| 路径 | 操作 |
|------|------|
| `Assets/Scripts/v0_4/AttackData.cs` | 重写 |
| `Assets/Scripts/v0_4/WeaponAnimator.cs` | 重写 |
| `Assets/Scripts/v0_3/PlayerCombat.cs` | 重写 |
| `Assets/Scripts/v0_4/EnemyCombat.cs` | 重写 |
| `Assets/Scripts/v0_4/EnemyAI.cs` | 修改 |
| `Assets/Scripts/v0_4/EnemyStats.cs` | 修改 |
| `Assets/Scripts/v0_4/EnemyHealth.cs` | 修改 |
| `Assets/Scripts/v0_3/WarriorAttack.cs` | 置空弃用 |
| `Assets/Scripts/v0_3/Interfaces/IAttack.cs` | 置空弃用 |
| `Assets/Data/PlayerAttackData.asset` | 新建 |
| `Assets/Data/EnemyMeleeAttackData.asset` | 更新 |
| `Assets/Prefabs/Player.prefab` | 新建 |
| `Assets/Prefabs/Enemy.prefab` | 新建 |
| `Assets/Scenes/v0_4_EnemySystem.unity` | 修改 |
