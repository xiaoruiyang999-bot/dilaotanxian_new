# v0.5.0 随机地图生成基础 操作报告

> 本文档记录 v0.5.0 的实际执行内容与结果，对应《v0.5 Dungeon 系统开发计划书》五-A 节。
> Git 提交：`02160d8`（v0.4.6 遗留补交）+ `0bfb1cd`（v0.5.0 版本），已推送 GitHub。

## 一、版本目标

建立随机地牢的**地图生成基础**：每次进入随机生成 8~12 个连通房间，确定玩家出生房与 Boss 房位置，种子可复现。

本阶段**只生成地图**：不生成敌人、不做房间状态机、不做门开关交互（门洞常开）。为 v0.5.1 房间流程系统提供 Room 对象与布局数据。

数据流（计划书 3.2）：

```
seed ──► DungeonGenerator.Generate(config, seed)   【纯 C#，不碰场景】
           ──► DungeonLayout（房间节点 + 连接边 + BFS距离 + Boss选址）
             ──► DungeonBuilder.Build(layout)       【实例化】
                   ──► 画 Floor/Walls Tilemap + 开门洞
                   ──► 每房间建 Room 空壳（挂 DungeonRoot 下）
                   ──► 玩家传送至起始房中心 + 相机瞬移
```

## 二、核心设计

### 2.1 职责解耦

| 模块 | 类型 | 职责 |
|---|---|---|
| **DungeonGenerator** | 纯 C# 静态类 | 网格邻接生长算法生成布局；连通性由构造保证（每个新房贴在已有房上）；BFS 填距离；Boss 房=最远（并列叶子优先） |
| **DungeonLayout / RoomNode / RoomConnection** | 纯 C# 数据 | 布局的唯一数据表示；RoomNode.id 为生成器分配的稳定 ID（同 seed 重生成保持一致） |
| **DungeonConfig** | ScriptableObject | 地图参数唯一数据源（房间数 8~12、内部 18×11 瓦片、门洞宽 2、特殊房/楼层缩放字段预留给 v0.5.3/v0.5.4） |
| **DungeonBuilder** | MonoBehaviour | 读布局画 Tilemap（共享 Floor/Walls 两张）、在共用墙开门洞、建 Room 空壳；只负责「表现」，零生成逻辑 |
| **Room** | MonoBehaviour（基础版） | 持有 Id / Type / 世界边界 Rect（唯一边界来源，布局算术得出，禁止从 Tilemap 反算）；本版无状态机 |
| **DungeonManager** | MonoBehaviour（门面） | 持有 config 与 seed，串起 生成→实例化→玩家传送；Gizmos 布局可视化；Validate 1000 Seeds 离线自检 |
| **CameraFollow** | 修改（+2 行） | 新增 `SnapToTarget()`：出生时相机瞬移，避免镜头横穿全图 |

### 2.2 关键设计约束

- **数据与表现分离**：Generator 不依赖场景/MonoBehaviour，可离线批量自检；Builder 是唯一实例化入口。
- **种子可复现**：一切随机走独立 `System.Random` 实例，禁止 `UnityEngine.Random`；同 config + 同 seed → 同一张图。
- **格子换算**（计划书 4.4）：粗网格格子 = (roomWidth+2) × (roomHeight+2) 瓦片（内部 + 四周各 1 格墙）；瓦片 (x,y) 占世界 [x,x+1]×[y,y+1]。
- **门洞**：相邻房间的共用墙中段打穿两侧墙线、铺地板，宽 doorWidth=2 格。
- **统一挂载**：所有 Room 对象挂 `DungeonSystem/DungeonRoot` 下，为 v0.5.4 Cleanup 销毁根做准备。
- **Boss 选址语义**：全局 BFS 最远房即「尽力满足 bossMinDistance」——若最远房都不达标，则不存在达标房。

## 三、新增文件

### 3.1 脚本（`Assets/Scripts/v0_5/`）

| 文件 | 说明 |
|---|---|
| `Generation/DungeonLayout.cs` | RoomType 枚举 + RoomNode + RoomConnection + DungeonLayout（同文件，纯 C#） |
| `Generation/DungeonGenerator.cs` | 静态生成器：`Generate(DungeonConfig, int seed) → DungeonLayout`；50 次防御性重roll兜底 |
| `Core/DungeonConfig.cs` | `[CreateAssetMenu(menuName="Dungeon/DungeonConfig")]`，字段默认值即计划书 4.2 数值 |
| `Core/DungeonBuilder.cs` | `Build()` / `ClearAll()` / `GetRoomCenterWorld()`；`Rooms` 只读字典供查询 |
| `Core/DungeonManager.cs` | `Start()` 自动生成；`[ContextMenu]` + `[MenuItem("Tools/Dungeon/Validate 1000 Seeds")]` 双入口自检；OnDrawGizmos（Start 绿 / Boss 红 / 连接青线） |
| `Runtime/Room.cs` | 基础版：`Init(id, type, bounds)`；Center 属性 |

### 3.2 配置与场景

- `Assets/Data/Dungeon/DefaultDungeonConfig.asset`（SO 默认值：房间数 8~12、房间 18×11、门洞 2、bossMinDistance 3）
- `Assets/Scenes/v0_5_Dungeon.unity`（复制自 v0_4_EnemySystem）
- 目录：`Assets/Scripts/v0_5/{Core,Generation,Runtime,Content,Interaction}`、`Assets/Data/Dungeon/`、`Assets/Prefabs/Dungeon/`
- 验收截图：`Assets/Screenshots/v050_play.png`（出生房视角）、`v050_fullmap.png`（全图）

### 3.3 场景结构（v0_5_Dungeon.unity）

```
v0_5_Dungeon.unity
├── Main Camera（Camera + CameraFollow，不变）
├── Global Light 2D（不变）
├── Grid（新建，空）
│   ├── Floor（Tilemap + TilemapRenderer，sortingOrder=0）
│   └── Walls（Tilemap + TilemapRenderer(sortingOrder=1)
│              + TilemapCollider2D(usedByComposite=true)
│              + CompositeCollider2D + Rigidbody2D(Static)）   ← 碰撞结构照抄 v0.1
├── DungeonSystem（新建）
│   ├── DungeonBuilder 组件（引用已接：Floor/Walls Tilemap、FloorTile/WallTile 资产、DungeonRoot）
│   ├── DungeonManager 组件（引用已接：DefaultDungeonConfig、builder、player）
│   └── DungeonRoot（空，生成物挂载点）
├── Player（不变，运行时由 DungeonManager 传送至起始房）
├── Canvas / EventSystem（不变）
└── （已删除：小屋旧 Grid、TrainingDummy、Enemy —— 木桩与敌人留在 v0_4 场景供回归测试）
```

## 四、修改文件

### 4.1 `Assets/Scripts/now_use/CameraFollow.cs`

新增 1 个方法（共 6 行，其余零改动）：

```csharp
/// <summary>瞬移到目标位置（出生 / 楼层切换时调用，避免镜头横穿全图）。</summary>
public void SnapToTarget()
{
    if (target == null) return;
    transform.position = target.position + offset;
    currentVelocity = Vector3.zero;
}
```

### 4.2 场景复制操作

1. 先保存 `v0_4_EnemySystem.unity`（编辑器内存在 v0.4.6 未保存改动，需先固化）；
2. 另存为 `v0_5_Dungeon.unity`；
3. 在 v0_5 中删除旧 Grid / TrainingDummy / Enemy；
4. 新建空 Grid 结构与 DungeonSystem（见 3.3），接线 8 个序列化引用；
5. 保存场景。

## 五、未修改文件

以下保持原样，零改动：

- Player 侧全部脚本（PlayerController / PlayerStats / Health / PlayerCombat / PlayerAimController / PlayerUI / PlayerWorldStatusBar）
- Enemy 侧全部脚本（EnemyController / EnemyStats / EnemyHealth / EnemyAI / EnemyCombat / TrainingDummy）
- 武器攻击框架（AttackData / WeaponController / WeaponAnimator / WeaponHitbox / AttackIndicator / AttackQuery）
- `Player.prefab` / `Enemy.prefab`、两份 AttackData 资产
- 物理碰撞矩阵、层定义、Input Actions、Camera（仅加方法，未改既有逻辑）

## 六、遇到的问题与修复

### 问题 1：CS0618 编译警告

**现象**：`DungeonManager.cs(126,19): warning CS0618: 'Object.FindFirstObjectByType<T>()' is obsolete`

**原因**：Unity 6000.5 弃用 `FindFirstObjectByType`（依赖实例 ID 排序）。

**修复**：改为 `FindAnyObjectByType<DungeonManager>()`（不依赖排序，语义对本场景的单例查找等价）。修复后 0 Error / 0 Warning。

### 问题 2：REST 批量接线引用全部失败

**现象**：`component_set_serialized_property_batch` 用 `value` 字段传引用，8 项全部报 `Scene reference not found`。

**原因**：该接口的引用不走 `value`，走独立字段——场景引用用 `referencePath`、项目资产用 `assetPath`。

**修复**：改用正确字段后 8 项全部成功（Tilemap×2、Tile 资产×2、DungeonRoot、Config、builder、player）。

### 问题 3：场景初始 dirty 状态

**现象**：开工时 `v0_4_EnemySystem.unity` 处于未保存状态（v0.4.6 改动未固化）。

**处理**：先保存 v0_4 再另存 v0_5；提交 git 时将 v0.4.6 遗留（Enemy.prefab + v0_4 场景 + 项目框架文档）单独提交为 `02160d8`，与 v0.5.0 提交 `0bfb1cd` 分开，保持「每版本一提交」惯例。

## 七、验证清单

### 自动化验收（已通过 ✅）

- [x] 编译 0 Error / 0 Warning
- [x] **Validate 1000 Seeds：失败 0/1000**，平均房间数 10.0，Boss 距离 min=2 / avg=3.9 / max=7，耗时 30ms
- [x] Play 模式自动生成无报错（日志：`seed=25172078 rooms=10 connections=11 bossRoom=#7 bossDist=3`）
- [x] 玩家落点 = 起始房中心（实测 (10, 6.5)，即房间 (0,0) 内部中心）
- [x] Room 空壳 ×10 挂载 `DungeonSystem/DungeonRoot` 下（Room_0_Start ~ Room_6_Boss，命名含类型）
- [x] 全图截图目视：房间连通、墙体封闭、门洞位置正确（`v050_fullmap.png`）
- [x] Boss 距离 min=2 < bossMinDistance=3 符合「尽力满足」语义（该布局最远房即 2，不存在更远选择）

### 待人工 Play 验收（操作：打开 `v0_5_Dungeon.unity` 进 Play）

- [ ] WASD 走动穿门洞，能进入相邻房间
- [ ] 墙体碰撞生效：走不出地图外边界
- [ ] DungeonManager 的 seed 填固定值（如 12345）→ 重进 Play 布局完全一致；填 0 → 每次不同
- [ ] Scene 视图 Gizmos：Start 房绿框、Boss 房红框、连接青线显示正确

## 八、扩展预留（已为后续版本埋好的接口）

| 预留 | 用途 |
|---|---|
| `RoomNode.distanceFromStart` / `IsLeaf` | v0.5.3 特殊房选址（宝箱/商店优先叶子房） |
| `RoomType` 枚举（7 种已全） | v0.5.3 房间类型系统 |
| `DungeonRoot` 统一挂载 | v0.5.4 Cleanup 一键清理 |
| `Room.Bounds` / `Center` | v0.5.1 RoomTrigger 覆盖区域、v0.5.2 生成位置校验、小地图共用边界 |
| `DungeonConfig` 特殊房/楼层缩放字段 | v0.5.3 / v0.5.4 直接启用，无需改结构 |
| `builder.Rooms` 只读字典 / `Layout` 属性 | 门登记、小地图、传送门定位的数据入口 |
| 种子系统（ActiveSeed + 固定 seed 调试） | 每日挑战、存档还原 |

## 九、后续工作

- **v0.5.1 房间流程系统**（计划书五-B）：Room 升级完整状态机（Unvisited/Active/Cleared）、Door 模块（碰撞+视觉，仅当相邻两房都非 Active 才开）、RoomTrigger（内缩 0.5 格）、敌人休眠制（contentRoot.SetActive）、清房条件枚举（None/AllEnemiesDead）。
- 人工验收通过后开工；手工摆敌人进 contentRoot 验证「进门关门→杀光开门」全流程。
