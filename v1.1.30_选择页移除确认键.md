# v1.1.30 选择页移除确认键（点击即选中即确定）

## 需求

角色选择页与职业选择页的"确 认"按钮删除——点击已有的"选 择"键即选中即确定；
颜色卡区域（整卡）同样实现选择功能。

## 改动（CharacterSelectUI / ClassSelectUI 对称）

- **确认键移除**：BuildConfirmButton 及调用删除；ClassSelectUI 的确认闪烁 DOTween 链
  （confirmFrame/Confirm/confirming）整体退役，`using DG.Tweening` 移除；
- **选中即确定**："选 择"键点击 → 高亮 + 立即结算关闭：
  - 角色页 `Pick(cardName)`：写 ChosenCharacter + 即时换形（SetWerewolfVisual/兽化组件）
    + 关闭 + 未选职业接续职业页；
  - 职业页 `Select + ApplyAndClose`：ApplyClass + SetClass + 刷新展台 + 关闭；
- **整卡可点**：颜色卡 frame 加 Button（targetGraphic=卡框，ColorTint 悬停微亮/按下压暗，
  fade 0.08s），子物体点击事件向上冒泡触发；与"选 择"键同功能；
- 布局微调：两页卡片各下移（−2→−30 / −4→−40）填补确认键空位；
- Esc 关闭语义不变（不选择不生效）；默认高亮（上次角色/职业）保留为当前选择指示。

## 验收

编译无错 → Play 准备房间：点战士/狼人卡（或其选择键）直接生效关闭并接续职业页；
点职业卡（或选择键）直接应用职业、关闭、展台刷新；Esc 取消不变；无确认按钮。

## 关联

v1.1.9 卡片布局 / v1.1.12 GraphicRaycaster（新整卡按钮自动可点）。
