# v0.7.0 操作记录

> 版本主题：拆除与重构 —— 体力/闪避下线 + 六维属性 + 伤害管线与暴击（《v0.7_Demo玩法计划书.md》§九 v0.7.0 执行卡，决策 2/3/6）。
> 执行文档：`v0.7.0_正式计划.md`。本版不加新玩法、不换美术，只拆旧账、立新规。

## 改动文件清单

### 新建脚本（2，含 .meta，csproj 已补）
- `Combat/DamageContext.cs`（guid `f3c782f3…`）：伤害上下文 struct——baseAttack（角色攻击+武器攻击）/multiplier（倍率区）/critRate/critDamage；`Roll()` 一次暴击判定返回最终伤害，`IsCrit` 外露供表现层
- `Combat/DamageResolver.cs`（guid `4727c933…`）：静态结算入口 `Deal(target, ctx)` → `TakeDamage(ctx.Roll())`，单点收口（v0.7.1 在此分流减伤甲结算）

### 修改脚本（9）
- `Player/PlayerStats.cs`：删体力全部内容（maxStamina/CurrentStamina/回复计时/TryConsumeStamina/ConsumeStaminaOverTime/Update 回复段）；删 AbsorbDamageWithArmor 残留 Debug.Log；加六维 `attack`(5)/`critRate`(0.2)/`critDamage`(1.5)/`armorReduceMul`(0.3)/`armorLossMul`(1.0)（占位，R/L 本版不接线）；ApplyClass 写入新字段；护甲脱战回复保留（v0.7.1 删）
- `Player/PlayerMovement.cs`：重写为纯移动（226→76 行）——常规移动 + SetChargeSlow 蓄力减速 ×0.5；闪避/无敌帧/残影/奔跑×1.6/体力消耗全删
- `Player/PlayerController.cs`：删 Dash/Sprint 输入分发与 ClassSelectUI 屏蔽名单中的 Dash；Skill 占位保留；TakeDamage 转发不变
- `Player/PlayerUI.cs`：删体力条创建/更新逻辑；Awake 新增 `HideLegacyStaminaBar()`——场景 YAML 残留 StaminaBar 对象运行时 SetActive(false) 兜底隐藏（代码不删场景对象）；法力条兜底创建改克隆护甲条
- `Player/PlayerWorldStatusBar.cs`：删头顶黄体力条，保留钢灰护甲 + 红 HP 双条
- `Class/ClassData.cs`：+`attack`/`critRate`/`critDamage`/`armorReduceMul`/`armorLossMul` 五字段（序列化，占位默认值同 PlayerStats）
- `Class/ClassSelectUI.cs`：职业按钮描述从"一句话特色"改六维数值行（HP/护甲/攻击/魔力/暴击%/暴伤×，`BuildStatLine` 数据驱动，字号 12、宽 336 不溢出 420 宽按钮框）
- `Combat/WeaponHitbox.cs`：+`DamageMultiplier`（默认 1，BeginSwing 复位，LengthMultiplier 同款先例）；Awake 缓存根上 PlayerStats 做玩家/敌人分流——玩家走 `DamageResolver.Deal`，敌人保持 `TakeDamage(attackData.AttackDamage)` 原路径
- `Combat/Projectile.cs`：Init 时 owner 根查 PlayerStats；玩家子弹走 DamageResolver（`damageMul` 映射 `ctx.multiplier`，弓箭蓄力 ×2 语义不变），敌人子弹原路径

### 蓄力倍率归位（计划书 §2.3，公式口径修正）
- `Player/PlayerCombat.cs`：满蓄不再 `SetDamage(base×1.5)` 改副本（会把角色攻击力一起放大，违反公式文档 §2.1 倍率区独立）；改设 `WeaponHitbox.DamageMultiplier = ChargeFullDamageMul`，挥击结束随 BeginSwing 复位；`meleeBaseDamage` 字段随之删除；范围/角度/宽度缩放走副本的逻辑不动

### 数据资产 / 输入
- 三份 `Assets/Data/Class/*.asset` 与 `Player.prefab` 未改 YAML：新字段取脚本默认值，下次编辑器序列化自动补；被删体力字段 YAML 残留无害（Unity 忽略失配字段）
- `PlayerInputActions.inputactions` 未动：Dash/Sprint action 保留备用

### 文档
- `Assets/Scripts/now_use/README.md`：+DamageContext/DamageResolver，PlayerStats/PlayerMovement/PlayerUI/PlayerWorldStatusBar/PlayerController/WeaponHitbox/Projectile/ClassData/ClassSelectUI 职责同步，清单 76→78
- `版本实施自检清单.md`：§3 体力/闪避数值行标注已下线（历史值存档）、状态条配色/蓄力规则/输入行同步；§2 本版无新增错误记录
- `Assembly-CSharp.csproj` 补 2 条 Compile Include

## 验证情况

- `dotnet build Assembly-CSharp.csproj`：**0 警告 0 错误**（2 个新文件已补 csproj 条目）。
- 全项目 grep `stamina|Stamina|Dash|Sprint`（Assets/Scripts/now_use）：代码逻辑**零残留**；剩余匹配均为计划要求的兜底代码（`HideLegacyStaminaBar` 及其查找路径字符串）与说明性注释（标注"已下线"），README 文档行已同步更新。
- 外部引用核查：now_use 之外无任何脚本引用被删 API（CurrentStamina/TryDash/SetSprintHeld 等），编译通过佐证。
- 运行时行为（暴击数值、蓄力手感、UI 隐藏效果）未经编辑器实测，待用户验收。

## 待用户在编辑器验收清单

> 测试场景：`v0_6_PrepRoom` 开局 → 传送门进 `v0_6_ClassWeapon`；回归 `v0_4_EnemySystem`；Game 视图 1920×1080 或 Free Aspect。

1. **编辑器先刷新**：切回 Unity 导入新脚本（若编辑器此前开着，勿保存旧内存覆盖 YAML）。
2. **体力零残留**：屏幕面板与头顶均无体力条（场景残留对象已被代码运行时隐藏；可在方便时进场景手动删除 StaminaBar 对象，不删不影响运行）；Space/Shift 按下无任何效果。
3. **移动**：常速移动正常、移速恒定（无奔跑）；近战蓄力中移速 ×0.5 正常。
4. **六维**：职业选择界面按钮显示六维数值行；选战士后 PlayerStats Inspector 可见 攻击/暴击率/暴伤/R/L 字段（占位 5/0.2/1.5/0.3/1.0）。
5. **暴击**（木桩，v0_4_EnemySystem）：CR=0 时伤害恒为 (5+武器)；CR=1 时恒 ×1.5；长剑满蓄 = (5+武器)×1.5（再乘暴击）；与《伤害计算公式文档.md》§2.4 算例一致。
6. **回归**：敌人近战/精英/Boss 行为与伤害不变；祭坛自伤/治疗球/法力球/宝箱/换武器全通。

## 遗留与技术债

- 暴击仅数值生效，`DamageContext.IsCrit` 已外露但无表现（大字/闪白记入 v0.7.11 UI 完善）；
- 护甲双倍率 R/L 字段已上架未接线（v0.7.1 减伤甲结算，DamageResolver 单点分流）；护甲脱战自动回复本版保留（v0.7.1 删）；
- 场景 YAML 残留的 StaminaBar 对象与 prefab 体力字段无害，待用户编辑器内顺手清理；
- 六维占位数值（5/0.2/1.5/0.3/1.0）仅跑链路用，待设计 D 定稿后填《数值与角色设计任务书.md》§一；
- 敌人侧伤害/护甲/攻击力接入在 v0.7.1/v0.7.8，本版敌人路径原样未动。
