# v1.1.46 战斗房扩容与怪物轮次

## 需求(用户)

1. 战斗房间大小**至少增大到现在的一倍**;
2. 某些战斗房加入**怪物轮次**(增加战斗时长和体验感),目前两轮。

## 实现

### 1. 战斗房整格扩展(RoomSizeExpander + DungeonConfig)

- `DungeonConfig` 新增 `combatCellSpan=2`(与 bossCellSpan 同款"尽力满足"语义)。
- 扩展顺序:Boss(2×2)→ **Combat(2×2 优先 → 2×1/1×2 回退 → 保 1×1)** → Elite(2×1)。
- 尺寸核算(内部可行走区):1×1 = 18×11(198 格);2×1 = 37×11 或 18×23(≈2.05× 面积,恰好一倍);
  2×2 = 37×23(851 格,≈4.3×)。
- 非战斗房(Start/Treasure/Shop/Event)保持小巧——探索节奏对比:小走廊房 ↔ 大竞技场。
- **实测达成率(EditMode 门禁,100 seed/538 Combat 房):85% 扩成 ≥2 倍面积,其中 36% 为 2×2**;
  15% 因邻接生长布局无空闲角保 1×1(与 Boss 房同策略;中心挤迫处的偶发小房反而形成节奏调剂)。

### 2. 怪物轮次(Room 波次扩展点 + RoomWaveController)

- `Room.cs`:新增 `IWaveProvider` 接口(`HasPendingWave`/`RequestNextWave`)+ `RegisterWaveProvider`;
  `TryClearRoom` 在"本波全灭"时先询问波次——还有波则请求刷出并保持关门,无波才 `SetCleared`。
  未注册 provider 的房间行为与旧版逐字节一致(零回归)。
- `RoomWaveController`(新文件,挂掷中轮次的战斗房):实现接口;`RequestNextWave` 幂等
  (inFlight 拦截 NotifyEnemyDied/LateUpdate 双路径重复请求);延迟 0.9s(清场喘息+换位窗口)后
  **复用 `EnemySpawner.Spawn`** 刷第二波——楼层缩放/词缀/AI 独立随机源整套逻辑零重写;
  Active 态注册即活跃(休眠只发生在 Unvisited)。挂 dungeonRoot 下,楼层重建销毁即协程终止。
- `DungeonConfig.combatWaveChance=0.35`:`DungeonBuilder.SpawnContent` 对普通 Combat 房掷 rng
  ( Elite/Boss 不参与——各有自己的强化体系);波次 rng 由房 rng 派生子流,同 seed 复现不变。

### 3. 大房敌人数量自适应(EnemySpawner)

面积 ≥1.9× 标准房(即 2×1 起)的波次数量 ×1.5 向上取整、封顶 12(独立于楼层递增的 8 封顶)——
否则大竞技场里 2~4 只怪显空。轮次 ×2 + 大房 ×1.5,战斗时长约 3 倍于旧版普通战斗房。

## 关键设计说明

- **为什么走 span 扩展而不是全局 roomWidth×2**:全局翻倍会把 Start/Shop/Treasure 一起放大
  (走路时间×、地图面积×4)且违背 v1.1.43"内部尺寸唯一改 DefaultDungeonConfig"的口径;
  span 扩展只放大战斗房,非战斗房保持紧凑,层次感更好。
- **为什么第二波复用 EnemySpawner.Spawn**:生成即正确——位置规则(距墙/距门/重叠检测)、
  楼层难度、词缀、行为随机源全部同源,波次只是"再调一次"。

## 验收

- 全量 EditMode **33/33 通过**(新增 `CombatRooms_AtLeastDoubleSize_MajorityOfSeeds`:100 seed
  统计 + ≥80% 达成率断言);`compile_check.py` 真编译 0 错。
- Play 走查建议:进 Combat 房看开阔度(约 1/3 战斗房是 4 倍大竞技场);约 35% 战斗房杀完第一波
  0.9s 后刷增援,两波全灭门才开;Console 有 `[Wave]` 登场日志;Boss/Elite 房无轮次。

## 关联

v0.5.3.1 RoomSizeExpander(span 扩展机制)、v1.1.44 玩家口径验证(37×23 大房障碍布局已仿真覆盖)、
v1.1.45(同批内容管线)。
