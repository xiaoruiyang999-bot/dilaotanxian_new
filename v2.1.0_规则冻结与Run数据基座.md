# v2.1.0 规则冻结与 Run 数据基座：实施记录

> 日期：2026-09-17
> 依据：`计划书/v2.1.0_规则冻结与唯一Run数据基座.md`
> 状态：数据合同与迁移已实现；Unity Test Runner 实跑待完成。

## 实际改动

- 扩展 `DungeonGraphNode`：保留旧枚举值，末尾新增 Supply/Sage；增加房间原型、Room/Encounter/Reward/Shop Seed 与 Selected、ObjectiveCompleted、RewardResolved 状态。节点完成必须先完成目标并结算奖励。
- 现有 DAG 生成器为每节点派生四条稳定 Seed 流；不改变旧房间生成入口和节点类型分布。
- 新增 13 个稳定刻印 ID、固定谱系/品阶、职业与标签过滤入口、同名升级至 3 级、按不同 ID 计算 2/4 共鸣阶段。刻印实际战斗效果留给 v2.1.3。
- 新增唯一五级六档解铭权重矩阵及逐级价格；过滤空品阶后只归一化正权重，0% 不回流。新增纯数据升阶事务和稳定事务 ID；只允许当前已进入的商店节点升阶，重复事务不重复扣款。
- `SaveService.ActiveRunData` 升至 Schema v2，保存完整图、当前节点、角色资源、Build、待选揭印、商店、背包、短时 Buff 与交易记录。v1 档保留原字段并标记待路线初始化，不虚构旧档不存在的 DAG。新 Run 创建时生成独立 RunId。
- SaveService 的 EditMode 测试改用独立临时目录，并在该模式下跳过真实 PlayerPrefs 迁移，避免改动玩家存档或偏好数据。
- 同步 V2 主开发文档与长期路线图；旧 `RunManager`、场景、Prefab、UI 均未接入新链。

## 验证

- `dotnet build EditModeTests.csproj`：成功，0 错误；7 条现有未使用变量/字段警告。构建时用临时 MSBuild target 纳入 Unity 尚未刷新到生成项目文件的新脚本与测试，验证后已删除临时文件。
- 独立纯 C# 合同检查：13 ID 唯一、同名升级与共鸣计数、五级矩阵合计、0% 过滤、升阶幂等、DAG 节点领奖门禁均通过；补充验证离开商店后升阶被拒绝。临时检查文件已删除。
- `git diff --check`：通过。
- 已编写 `SaveServiceTests`、`V210RunDataTests` 和 `DungeonGraphGeneratorTests` 的新增 EditMode 用例；Unity 编辑器正在使用该项目，本地 UnitySkills 测试接口未启用，因此尚未在 Unity Test Runner 中实跑，也未做 PlayMode 走查。

## 遗留与下一步

- v2.1.1 接管唯一 DAG 真值、节点进入与单 Active 房间时，需要将 `routeInitializationPending` 清零、填入图与当前节点，并在节点/交易边界调用 `SaveRun`。
- v2.1.3 需将刻印静态数据扩展为逐级数值与实际战斗效果；本版本只有稳定 ID、品阶和持有状态。
- v2.1.4～v2.1.6 需真正生成与恢复候选、物资、商店库存并在支付/领奖后立即存档。
- 在继续接入运行流程前，先在 Unity Test Runner 实跑三组 EditMode 用例，并检查 Console 编译与序列化结果。
