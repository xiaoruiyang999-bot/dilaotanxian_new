# v1.1.7 选择菜单按钮接入三态石板素材

## 1. 素材入库

源：`E:\WeGameApps\The animation of Unity\狼人\石板面板_三状态\stone_panel_{normal,hover,pressed}.png`（456×59）。
导入：`Assets/UI/Panel/btn_stone_*.png`（母版）+ `Assets/Resources/UI/btn_stone_*.png`（运行时副本），
设置同母版（Single/Point/不压缩/PPU1）；**三图统一 Border L13/B11/R13/T13**（各边取三图实测最大值——
SpriteSwap 换图不重排 RectTransform，三态 Border 不一致会在切态瞬间跳边，必须统一）。
三态亮度实测：normal 41 / hover 55 / pressed 28（中央区），明暗关系正确。

## 2. PanelSprite 扩展

新增 `BtnNormal / BtnHover / BtnPressed` 缓存属性 + `ApplyStoneButton(Button, Image, fallbackColor)`：
- Sliced 底图 + `Button.Transition.SpriteTransition`（highlighted=hover、pressed=pressed、
  selectedHighlight 复用 hover 供键盘/手柄导航）；
- `targetGraphic` 指到按钮底图（换图发生在底图上，外部高亮装饰框不受影响）；
- 素材缺失落回纯色 + 默认 ColorTint，不抛错。

## 3. 接线点（4 处，两个选择菜单全部按钮）

| UI | 按钮 | 说明 |
|---|---|---|
| ClassSelectUI | 三个职业按钮 | 职业色外框（选中高亮）+ 内嵌三态石板底；选择高亮与悬停反馈正交互补 |
| ClassSelectUI | 确认按钮 | 金色外框（确认闪烁 DOTween 不受影响）+ 三态石板底 |
| CharacterSelectUI | 战士/狼人按钮 | 角色 tint 外框（选中高亮）+ 三态石板底 |
| CharacterSelectUI | 确认按钮 | 纯绿平铺 → 三态石板（缺素材回退绿） |

交互逻辑（选择高亮、确认闪烁、Esc、输入屏蔽、接续弹页）零改动。

## 4. 验收

1. 编译无错 → Play 准备房间：角色选择 → 职业选择，所有按钮为石板质感。
2. 悬停变亮、按下变暗（三态自动换图），切态无边缘跳动。
3. 选中态仍由彩色外框表达；确认按钮点击仍有金色闪烁。

## 5. 后续

- 暂停/设置/背包/结算面板接入时，按钮直接 `PanelSprite.ApplyStoneButton` 三行复用。
- 改图需同步 `Assets/UI/Panel` 与 `Assets/Resources/UI` 两处（母版/运行时副本约定同 v1.1.5）。
