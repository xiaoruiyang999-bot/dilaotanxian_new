# v0.4.5.2 武器动画框架设计文档（最终版）

## 1. 设计定位

本次为 **v0.4.5.1 的彻底架构重整**，不是功能新增，而是解决当前已出现的职责混乱：

- 多个脚本同时修改 `WeaponPivot`。
- `WeaponAnimator` 负责了过多非动画事务。
- 攻击判定与动画表现耦合。
- Prefab 需要反复手动调整。

**新架构核心原则：一个变量只能有一个脚本负责。**

---

## 2. 第一原则：单一职责

| Transform 属性 | 唯一负责者 | 说明 |
|---|---|---|
| `WeaponPivot.rotation` / `WeaponPivot.localRotation` | `WeaponAnimator`（仅在 Active 阶段） | 其他时间保持 `identity`，跟随父对象 |
| `WeaponPivot.position` | Prefab | 固定于角色中心，代码不修改 |
| `WeaponSprite.position` | Prefab | 固定初始位置，代码不修改 |
| `WeaponSprite.rotation` | Prefab | 固定初始旋转，代码不修改 |
| `WeaponSprite.localScale` | Prefab | 固定缩放，代码不修改 |
| 角色朝向（父对象旋转） | `PlayerController` / `EnemyController` | 独立控制，不与其他系统耦合 |

**禁止行为：**

- `PlayerCombat` 直接修改 `WeaponPivot`。
- `EnemyAI` 直接修改 `WeaponPivot`。
- `WeaponAnimator` 读取 `AttackData`。
- `WeaponAnimator` 处理鼠标/敌人方向/攻击范围/碰撞。
- `AttackData` 包含动画逻辑或推导方法。

---

## 3. 最终职责划分

### 3.1 AttackData

**唯一职责：纯数据配置。**

保存：
- 阶段时长：Windup / Active / Recovery
- 判定参数：AttackRange、AttackAngle、AttackDamage、AttackCooldown、TargetLayer
- 动画类型：AnimationType
- 动画曲线：AttackEase
- 命中比例：ActiveMomentRatio

**不保存、不计算：**
- 动画起始/结束角度（v0.5 再扩展）。
- 视觉长度。
- 任何动画逻辑。

### 3.2 WeaponAnimator

**唯一职责：播放武器旋转动画。**

提供接口：
- `Play(...)`：根据传入参数播放一次 DOTween 旋转动画。
- `Stop()`：停止 Tween，并将 `WeaponPivot.localRotation` 重置为 `identity`（恢复跟随父对象）。

**不负责：**
- 鼠标方向、敌人方向。
- `AttackData` 解析。
- 攻击判定、目标、碰撞。
- `WeaponSprite` 缩放/位置/旋转。
- AI、状态机、Combat。

### 3.3 PlayerController

**唯一职责：控制玩家角色本体。**

- 移动输入处理。
- 角色朝向鼠标。
- 死亡处理。

**不控制 `WeaponPivot`。**

### 3.4 EnemyController

**唯一职责：控制敌人角色本体。**

- 移动。
- `FaceTowards(dir)`：面向目标。

**不控制 `WeaponPivot`。**

### 3.5 PlayerCombat

**唯一职责：玩家攻击流程与判定。**

- 维护 Windup / Active / Recovery 状态。
- 在 Active 开始时，解析 `AttackData`，将动画参数传给 `WeaponAnimator.Play(...)`。
- 在命中回调时执行扇形伤害判定。
- **不控制 `WeaponPivot`。**

### 3.6 EnemyAI

**唯一职责：敌人 AI 状态机与攻击流程。**

- 维护 Patrol / Chase / Attack 状态机。
- 在 Active 开始时，解析 `AttackData`，将动画参数传给 `WeaponAnimator.Play(...)`。
- 在命中回调时调用 `EnemyCombat.TryAttack`。
- **不控制 `WeaponPivot`。**

### 3.7 EnemyCombat

**保持原样。**

- 冷却计时。
- 范围判定（当前圆形，不影响本次架构）。
- 伤害执行。

### 3.8 AttackIndicator

**保持原样。**

- 纯视觉预警。

---

## 4. AttackData 最终结构

```csharp
using UnityEngine;
using DG.Tweening;

public enum AttackAnimationType
{
    Slash,      // 扇形挥砍
    FullCircle  // 360° 旋转
    // v0.5 扩展：Stab, Spin, Projectile, BossSkill
}

[CreateAssetMenu(fileName = "AttackData", menuName = "Combat/Attack Data")]
public class AttackData : ScriptableObject
{
    [Header("阶段时长")]
    [SerializeField] private float windupTime = 0.25f;
    [SerializeField] private float activeDuration = 0.25f;
    [SerializeField] private float recoveryTime = 0.35f;

    [Header("攻击判定")]
    [Tooltip("攻击判定半径")]
    [SerializeField] private float attackRange = 1.5f;

    [Tooltip("攻击判定扇形全角（度）。Slash 动画的挥动幅度也引用此值。")]
    [Range(0f, 360f)]
    [SerializeField] private float attackAngle = 140f;

    [SerializeField] private float attackDamage = 1f;
    [SerializeField] private float attackCooldown = 0.5f;

    [Tooltip("可命中目标的 LayerMask")]
    [SerializeField] private LayerMask targetLayer;

    [Header("动画")]
    [Tooltip("动画播放方式")]
    [SerializeField] private AttackAnimationType animationType = AttackAnimationType.Slash;

    [SerializeField] private Ease attackEase = Ease.OutQuad;

    [Header("命中触发")]
    [Tooltip("Active阶段中触发命中的时间点比例")]
    [Range(0f, 1f)]
    [SerializeField] private float activeMomentRatio = 0.5f;

    public float WindupTime => windupTime;
    public float ActiveDuration => activeDuration;
    public float RecoveryTime => recoveryTime;

    public float AttackRange => attackRange;
    public float AttackAngle => attackAngle;
    public float AttackDamage => attackDamage;
    public float AttackCooldown => attackCooldown;
    public LayerMask TargetLayer => targetLayer;

    public AttackAnimationType AnimationType => animationType;
    public Ease AttackEase => attackEase;
    public float ActiveMomentRatio => activeMomentRatio;

    void OnValidate()
    {
        attackAngle = Mathf.Clamp(attackAngle, 0f, 360f);
        activeMomentRatio = Mathf.Clamp01(activeMomentRatio);
        windupTime = Mathf.Max(0.001f, windupTime);
        activeDuration = Mathf.Max(0.001f, activeDuration);
        recoveryTime = Mathf.Max(0.001f, recoveryTime);
        attackRange = Mathf.Max(0.01f, attackRange);
        attackCooldown = Mathf.Max(0f, attackCooldown);
    }
}
```

**说明：**

- 删除原 `StartAngle` / `EndAngle` 属性。
- `AttackRange` 仅用于判定。
- `AttackAngle` 用于判定，同时作为 Slash 动画的挥动幅度（v0.5 引入自定义角度后将完全解耦）。
- 不实现 `AnimationStartAngle` / `AnimationEndAngle`，仅作为 v0.5 预留概念。

---

## 5. WeaponAnimator 最终结构

```csharp
using UnityEngine;
using DG.Tweening;
using System;

/// <summary>
/// 纯动画播放器。
/// 只负责按调用方传入的参数播放 WeaponPivot 的旋转动画。
/// 不读取 AttackData，不处理输入/AI/判定。
/// </summary>
public class WeaponAnimator : MonoBehaviour
{
    [SerializeField] private Transform weaponPivot;

    private Tween rotationTween;
    private Tween momentTween;

    public bool IsPlaying => rotationTween != null 
        && rotationTween.IsActive() 
        && rotationTween.IsPlaying();

    /// <summary>
    /// 播放一次攻击动画。
    /// </summary>
    /// <param name="type">动画类型</param>
    /// <param name="arcAngle">Slash 挥动角度；FullCircle 可忽略</param>
    /// <param name="activeDuration">动画持续时间</param>
    /// <param name="activeMomentRatio">命中触发比例</param>
    /// <param name="ease">动画曲线</param>
    /// <param name="onActiveMoment">命中回调</param>
    public void Play(
        AttackAnimationType type,
        float arcAngle,
        float activeDuration,
        float activeMomentRatio,
        Ease ease,
        Action onActiveMoment = null)
    {
        Stop();

        if (weaponPivot == null)
        {
            onActiveMoment?.Invoke();
            return;
        }

        float startAngle = 0f;
        float endAngle = 0f;

        switch (type)
        {
            case AttackAnimationType.Slash:
                float half = arcAngle * 0.5f;
                startAngle = -half;
                endAngle = half;
                break;

            case AttackAnimationType.FullCircle:
                startAngle = 0f;
                endAngle = 360f;
                break;
        }

        weaponPivot.localRotation = Quaternion.Euler(0, 0, startAngle);

        if (onActiveMoment != null)
        {
            float momentDelay = activeDuration * activeMomentRatio;
            momentTween = DOVirtual.DelayedCall(momentDelay, () =>
            {
                onActiveMoment?.Invoke();
                momentTween = null;
            });
        }

        rotationTween = weaponPivot
            .DOLocalRotate(new Vector3(0, 0, endAngle), activeDuration, RotateMode.FastBeyond360)
            .SetEase(ease)
            .OnComplete(() => rotationTween = null);
    }

    /// <summary>
    /// 停止动画，并将 WeaponPivot 恢复为 identity（跟随父对象）。
    /// </summary>
    public void Stop()
    {
        rotationTween?.Kill(complete: false);
        momentTween?.Kill(complete: false);
        rotationTween = null;
        momentTween = null;

        if (weaponPivot != null)
            weaponPivot.localRotation = Quaternion.identity;
    }
}
```

**关键约定：**

- `WeaponPivot` 是 `Player` / `Enemy` 的子对象，本地坐标 `(0, 0, 0)`。
- Idle 时 `localRotation = identity`，武器自然跟随父对象朝向。
- Active 期间 `WeaponAnimator` 对 `localRotation` 做相对旋转 Tween。
- `Stop()` 时回归 `identity`，武器重新跟随父对象。
- 不修改 `WeaponSprite` 的任何属性。

---

## 6. PlayerCombat 修改点

```csharp
public class PlayerCombat : MonoBehaviour
{
    [SerializeField] private AttackData attackData;
    [SerializeField] private WeaponAnimator weaponAnimator;
    [SerializeField] private AttackIndicator attackIndicator;

    // 状态机字段保持不变

    void Update()
    {
        UpdateAttackState();
    }

    private void EnterActive()
    {
        subPhase = SubPhase.Active;
        activeTimer = attackData.ActiveDuration;
        activeMomentTriggered = false;

        weaponAnimator?.Play(
            attackData.AnimationType,
            attackData.AttackAngle,
            attackData.ActiveDuration,
            attackData.ActiveMomentRatio,
            attackData.AttackEase,
            OnActiveMoment
        );
    }

    private void OnActiveMoment()
    {
        if (activeMomentTriggered) return;
        activeMomentTriggered = true;

        if (attackIndicator != null)
            attackIndicator.Hide();

        PerformFanAttack();
    }

    private void PerformFanAttack()
    {
        // 保持现有扇形判定逻辑不变
    }

    // Recovery 结束时 OnAttackEnd 保持不变
}
```

**修改要点：**

- 删除任何直接操作 `WeaponPivot` 的代码。
- 不计算鼠标方向，不控制武器朝向。
- 仅在 Active 开始时调用 `WeaponAnimator.Play(...)`。

---

## 7. EnemyAI 修改点

```csharp
public class EnemyAI : MonoBehaviour
{
    // 引用与状态机保持不变

    private void EnterActive()
    {
        attackSubPhase = AttackSubPhase.Active;
        activeTimer = ActiveDuration;
        activeMomentTriggered = false;

        if (attackData != null)
        {
            weaponAnimator?.Play(
                attackData.AnimationType,
                attackData.AttackAngle,
                attackData.ActiveDuration,
                attackData.ActiveMomentRatio,
                attackData.AttackEase,
                OnActiveMoment
            );
        }
        else
        {
            OnActiveMoment();
        }
    }

    private void OnActiveMoment()
    {
        if (activeMomentTriggered) return;
        activeMomentTriggered = true;

        if (indicator != null)
            indicator.Hide();

        if (combat.IsInAttackRange(target))
        {
            if (target.TryGetComponent<IDamageable>(out var damageable))
                combat.TryAttack(damageable);
        }
    }
}
```

**修改要点：**

- 删除任何直接操作 `WeaponPivot` 的代码。
- 删除 `SetAimDirection` 调用。
- 仅在 Active 开始时调用 `WeaponAnimator.Play(...)`。
- 敌人朝向由 `EnemyController.FaceTowards` 负责，`WeaponPivot` 作为子对象自然跟随。

---

## 8. PlayerController / EnemyController 职责

### PlayerController

**保持现有逻辑，不新增内容。**

- `Update()` 中继续调用 `HandleMouseAiming()` 旋转角色本体。
- 角色朝向鼠标 ⇒ `WeaponPivot` 作为子对象自动跟随。

### EnemyController

**保持现有逻辑，不新增内容。**

- `FaceTowards(dir)` 继续旋转敌人本体。
- 敌人朝向玩家 ⇒ `WeaponPivot` 作为子对象自动跟随。

### 攻击期间是否锁定方向？

**设计决策：不锁定。**

- `PlayerController` 与 `EnemyController` 持续控制本体朝向。
- `WeaponAnimator` 只控制 `WeaponPivot` 的相对旋转。
- 优点：职责完全分离，无需新增 Controller 接口，无循环依赖。
- 效果：Slash 会随身体转向扫过更大范围；FullCircle 仍然是完整 360° 相对旋转。

如未来需要锁定方向，在 v0.5 中通过 `PlayerController.LockOrientation()` / `EnemyController.LockOrientation()` 扩展，不影响本次架构。

---

## 9. Prefab 设计

### 层级结构

```
Player / Enemy
├── SpriteRenderer (角色本体)
├── WeaponPivot
│     └── WeaponSprite
│           └── SpriteRenderer (武器)
```

### WeaponPivot

- `localPosition = (0, 0, 0)`
- `localRotation = identity`
- `localScale = (1, 1, 1)`

### WeaponSprite

- `localPosition`：固定值，例如 `(0.5, 0, 0)`（表示武器从中心伸出 0.5 单位）。
- `localRotation`：固定值，例如 `-90°`（使 Sprite 沿 WeaponPivot +X 方向伸出）。
- `localScale`：固定值，例如 `(0.15, 1, 1)`。

**代码不修改 WeaponSprite 任何 Transform 属性。**

### 不同武器怎么办？

- 长矛：使用更长的 WeaponSprite Prefab 变体。
- 短刀：使用更短的 WeaponSprite Prefab 变体。
- 换武器时替换 `WeaponSprite` 子对象或 Sprite，无需改代码。

---

## 10. 数据流

```
PlayerController.Update
  │
  └─ 旋转 Player 本体（朝向鼠标）
        │
        ▼
   WeaponPivot（子对象，localRotation=identity）
        │
        ▼ 攻击触发
   PlayerCombat.EnterActive()
        │
        ├─ 读取 AttackData 参数
        │
        └─ WeaponAnimator.Play(type, arcAngle, duration, ratio, ease, OnActiveMoment)
              │
              ├─ Stop() → localRotation = identity
              ├─ 计算 start/end 相对角度
              ├─ Tween localRotation
              └─ ActiveMoment → OnActiveMoment()
                    │
                    ▼
              PlayerCombat.PerformFanAttack()
                    │
                    └─ 使用 AttackRange + AttackAngle 判定
```

```
EnemyAI.UpdateChase
  │
  └─ EnemyController.FaceTowards(playerDir)（旋转 Enemy 本体）
        │
        ▼
   WeaponPivot（子对象，localRotation=identity）
        │
        ▼ 攻击触发
   EnemyAI.EnterActive()
        │
        ├─ 读取 AttackData 参数
        │
        └─ WeaponAnimator.Play(...)
              │
              └─ ActiveMoment → OnActiveMoment()
                    │
                    ▼
              EnemyCombat.TryAttack(damageable)
```

---

## 11. 调用流程

### 11.1 玩家攻击流程

```
输入 Attack
  │
  ▼
PlayerCombat.TryAttack()
  │
  ▼
StartWindup() — subPhase = Windup
  │
  ▼ 时间到
EnterActive()
  │
  ├─ weaponAnimator.Play(Slash, 90°, 0.2s, 0.5, OutQuad, OnActiveMoment)
  │     │
  │     ├─ Stop() → localRotation = identity
  │     ├─ start = -45°, end = +45°
  │     └─ Tween 播放
  │
  ▼ 50% 时刻
OnActiveMoment()
  │
  └─ PerformFanAttack()（使用 AttackRange + AttackAngle）
  │
  ▼ 时间到
EnterRecovery() — subPhase = Recovery
  │
  ▼ 时间到
subPhase = None
  │
  ▼ 下一帧
PlayerController 继续旋转本体，WeaponPivot 自动跟随
```

### 11.2 敌人攻击流程

```
EnemyAI.UpdateChase
  │
  ├─ FaceTowards(player)
  └─ 判定进入攻击范围
        │
        ▼
ChangeState(Attack) — subPhase = Windup
  │
  ▼
EnterActive()
  │
  ├─ weaponAnimator.Play(FullCircle, 360°, 0.25s, 0.5, OutQuad, OnActiveMoment)
  │     │
  │     ├─ Stop() → localRotation = identity
  │     ├─ start = 0°, end = 360°
  │     └─ Tween 播放（真正旋转 360°）
  │
  ▼ 50% 时刻
OnActiveMoment()
  │
  └─ combat.TryAttack(damageable)
  │
  ▼ 时间到
EnterRecovery()
  │
  ▼ 时间到
ChangeState(Chase)
  │
  ▼
EnemyController.FaceTowards 继续工作，WeaponPivot 自动跟随
```

---

## 12. 流程图

### 12.1 架构职责图

```
┌─────────────────────────────────────────────────────────────┐
│                        父对象旋转层                          │
│   PlayerController ── 角色朝向鼠标                           │
│   EnemyController  ── 敌人朝向玩家                           │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
              ┌───────────────────────────────┐
              │        WeaponPivot              │
              │   Idle: localRotation = identity │
              │   自动跟随父对象朝向             │
              └───────────────────────────────┘
                              │
            Active 期间控制   │   其他时间 identity
                              ▼
              ┌───────────────────────────────┐
              │      WeaponAnimator             │
              │   - Play(type, arcAngle, ...)   │
              │   - Stop()                      │
              │   仅修改 WeaponPivot.localRotation│
              └───────────────────────────────┘
                              │
                              ▼
              ┌───────────────────────────────┐
              │        WeaponSprite             │
              │   Prefab 固定：position/rotation/scale │
              │   代码不修改                    │
              └───────────────────────────────┘
                              │
                              ▼
              ┌───────────────────────────────┐
              │   PlayerCombat / EnemyAI        │
              │   - 维护 Windup/Active/Recovery │
              │   - 调用 WeaponAnimator.Play()  │
              │   - 命中时执行判定              │
              └───────────────────────────────┘
                              │
                              ▼
              ┌───────────────────────────────┐
              │        AttackData               │
              │   纯配置：range/angle/damage/   │
              │   cooldown/type/ease/ratio      │
              └───────────────────────────────┘
```

### 12.2 Slash vs FullCircle 旋转示意

```
Slash (AttackAngle = 90°):

    -45°   0°   +45°
      \    |    /
       \   |   /
        \  |  /
         \ | /
          [角色]

FullCircle:

        360°
         │
    270°─┼─ 90°
         │
        180°
         [角色]
```

---

## 13. 脚本修改清单

| 脚本 | 修改类型 | 修改内容 |
|---|---|---|
| `AttackData` | 修改 | 新增 `AttackAnimationType` 枚举与字段；删除 `StartAngle` / `EndAngle` 属性 |
| `WeaponAnimator` | 重写 | 移除 `AttackData` 引用、缩放逻辑、朝向逻辑；改为纯 `Play` / `Stop` 播放器 |
| `PlayerCombat` | 修改 | 不控制 `WeaponPivot`；Active 时调用 `WeaponAnimator.Play(...)` |
| `EnemyAI` | 修改 | 删除 `SetAimDirection` 调用；Active 时调用 `WeaponAnimator.Play(...)` |
| `PlayerController` | 不动 | 保持角色朝向逻辑 |
| `EnemyController` | 不动 | 保持 `FaceTowards` 逻辑 |
| `EnemyCombat` | 不动 | 保持原样 |
| `AttackIndicator` | 不动 | 保持原样 |

---

## 14. 不影响范围

- `EnemyCombat.cs`：判定逻辑、冷却、伤害数值均不变。
- `EnemyController.cs`：移动、转向接口不变。
- `PlayerController.cs`：移动、输入绑定、死亡处理、鼠标朝向不变。
- `AttackIndicator.cs`：显示逻辑不变。
- 移动框架：`MoveSpeed`、`Rigidbody` 控制不变。
- 状态机整体结构：`EnemyAI` 的 Patrol / Chase / Attack / ReturnToPatrol 不变。
- 玩家伤害判定：`PlayerCombat.PerformFanAttack` 内部判定逻辑不变。

---

## 15. 实施计划

### Step 1：重写 AttackData
- 新增 `AttackAnimationType` 枚举（`Slash`, `FullCircle`）。
- 新增 `animationType` 字段。
- 删除 `StartAngle` / `EndAngle` 属性。

### Step 2：重写 WeaponAnimator
- 移除 `AttackData`、`weaponSprite`、`weaponWidth` 字段。
- 仅保留 `weaponPivot`。
- 实现 `Play(AttackAnimationType, float, float, float, Ease, Action)`。
- 实现 `Stop()`：Kill Tween + `localRotation = identity`。

### Step 3：修改 PlayerCombat
- 删除鼠标方向计算（如果存在）。
- 不直接操作 `WeaponPivot`。
- 在 `EnterActive()` 中调用 `WeaponAnimator.Play(...)`。

### Step 4：修改 EnemyAI
- 删除 `SetAimDirection` 调用。
- 不直接操作 `WeaponPivot`。
- 在 `EnterActive()` 中调用 `WeaponAnimator.Play(...)`。

### Step 5：调整 Prefab
- 确保 `WeaponPivot` 位于 `Player` / `Enemy` 中心，`localPosition = (0, 0, 0)`。
- 确保 `WeaponSprite` 子对象位置/旋转/缩放正确，代码不再修改。

### Step 6：配置 Asset
- `PlayerAttackData.asset`：`animationType = Slash`。
- `EnemyMeleeAttackData.asset`：`animationType = Slash` 或 `FullCircle`（按敌人类型配置）。

### Step 7：验证
- 编译通过。
- 玩家待机武器跟随鼠标。
- 玩家 Slash 挥动幅度与 `AttackAngle` 一致。
- 敌人 FullCircle 真正旋转 360°。
- 攻击结束后武器平滑回到跟随状态，无角度跳跃。
- 无 Prefab 反复调整需求。

---

## 16. v0.5 扩展预留

### 16.1 动画类型扩展

在 `AttackAnimationType` 中新增：
- `Stab`：突刺
- `Spin`：回旋斩
- `Projectile`：投射物
- `BossSkill`：Boss 技能

### 16.2 自定义角度

未来在 `AttackData` 中新增：
- `animationStartAngle`
- `animationEndAngle`

`WeaponAnimator.Play` 根据类型选择：
- `Slash` 使用 `AttackAngle`。
- `Custom` 使用 `AnimationStartAngle` / `AnimationEndAngle`。

### 16.3 锁定方向（可选）

如未来需要攻击期间锁定本体方向：
- `PlayerController` 新增 `LockOrientation()` / `UnlockOrientation()`。
- `EnemyController` 新增 `LockOrientation()` / `UnlockOrientation()`。
- `PlayerCombat` / `EnemyAI` 在 Active 开始时锁定，Recovery 结束时解锁。

本次不实现，保持架构简单。

---

## 17. 待确认事项

1. **EnemyMeleeAttackData 的 `animationType` 默认用 `Slash` 还是 `FullCircle`？**（建议普通敌人用 `Slash`，Boss 未来用 `FullCircle`。）
2. **是否确认以上架构设计，可以从 Step 1 开始实施？**
