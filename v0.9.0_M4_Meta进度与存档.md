# v0.9.0 M4「Meta 进度与存档」+ 暴击系统

> 状态：**代码已落盘 · Play 验证通过（存档载入日志 ✓）· 待实战验收**
> 日期：2026-08-23
> 里程碑：M4·v0.9.x（[长期计划](计划书/项目长期计划_Roguelite路线图.md) § M4）
> 附带：暴击系统（数值书 §12.1 缺口补全）

## 交付清单

| 任务 | 实现 |
|---|---|
| 4.1 存档系统 | `SaveSystem`（JSON + persistentDataPath + 版本校验 + 损坏 .bak 兜底 + ContextMenu 自检）。**只存 Meta 数据**——局内不存档（敌人随层重生成无序列化需求） |
| 4.2 魂货币 | 死亡结算 `层数×10` / 通关 `+100`（`MetaManager.SettleRun`），跨局保留 |
| 4.3 永久升级树 | **U 键魂商店**（时停面板）：强健之魂 +20HP(100魂) / 富有之魂 +30开局金币(80) / 老练之魂 永久伤害×1.1(150)；购买即写档，开局自动注入 |
| 4.4 中断续玩 | **跳过**（v1 决策：局内不存档，无续玩语义） |
| 4.5 Health/EnemyHealth 合并 | **跳过**（决策记录：敌人不进存档，合并失去必要性，省大回归风险） |
| 4.6 自动化测试 | **降级为内置自检**（SaveSystem ContextMenu 往返校验；正式测试框架留 M5） |
| 附：暴击系统 | `PlayerStats.CritChance 12% / CritDamage 150%`（数值书狼人基准）；PlayerCombat 每次攻击 roll 一次（整刀暴击），`WeaponHitbox.CritMultiplier` 进伤害管线 |

## 数据流

```
死亡/通关 → SettleRun（魂入账+最深层数+总局数）→ JSON 落盘
开局 → MetaManager.ApplyOwnedUpgrades（已购注入 HP/金币/伤害乘数）
伤害 = (角色ATK+武器) × DamageMultiplier(升级×兽化×永久) × CritMultiplier(roll) 
```

## 操作

- **U 键**：打开/关闭魂商店（时停）
- 死亡后 Console 可见 `[Meta] 结算：到达 X 层 → 魂 +Y`
- 存档位置：`%USERPROFILE%\AppData\LocalLow\<公司名>\<产品名>\save_v1.json`

## 验收清单

- [ ] Play 启动 Console：`[Meta] 存档载入` ✓（已验证）
- [ ] 死亡一次 → Console 魂结算 → 重开后按 U 看到魂余额
- [ ] U 商店买"富有之魂"→ 死亡重开 → 开局金币 30
- [ ] 关游戏重开 → 魂和升级还在（存档持久性）
- [ ] 暴击：战斗中偶尔出现 1.5 倍伤害飘字

## 过程教训（红线补充）

- **编译错误修复后必须重新 force_recompile + 场景重载**：SaveSystem 的 ContextMenu 拼错导致整个程序集缺失 → MetaManager 类不存在 → component_add 失败（"Component type not found"的另一诱因）→ 修完编译错后直接 add 成功。绕了一圈 YAML 手术（结果组件被 Unity 加载丢弃，无残留）。
