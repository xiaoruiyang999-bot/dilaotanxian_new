# v1.1.4 地皮地形分层系统（逻辑/视觉分离 + Bitmask 自动铺装）

## 1. 素材理解结论（E:\...\地皮素材\classified_tiles，24 张）

- 全部 **85×85 RGBA 不透明**；视觉语言 = **草底 + 棕色土斑**（clean 2~5% 土屑 ≈ 纯草；mottled 6~28% 土斑）。
- manifest 分类：`clean`（纯草 6 变体）、`edge_{top/bottom/left/right}`（土斑贴对应边）、`corner_{top_left,bottom_left}`、`multi_top_bottom`（上下贯通土带）、`interior`（土斑不贴边）。
- **没有纯土图块**：土区视觉用斑驳内部图块表达（草侵蚀观感），后续补纯土素材时在 `GroundTileset.PoolOf` 加池即可。
- 像素验证：manifest 分类与实测土斑位置基本吻合（个别贴边判定 borderline，不影响按池选型）。

## 2. 架构（严格按四步流水线，逻辑与视觉彻底分离）

```
DungeonBuilder.Build(layoutSeed)
 ├─ TerrainMask.Generate(布局并集矩形, seed=layoutSeed^0x7EA9)      ← 第1步 逻辑层
 │   世界绝对坐标 ValueNoise FBM（可播种，Perlin 不可播种故自实现）
 │   → 二值图（草0/土1）→ 小簇清理（土簇<4→草，草洞<3→土）→ 距离场 D（多源BFS）
 ├─ PaintRoom / OpenDoorTile 逐格 → TerrainPainter.PaintCell          ← 第2步 映射 + 第3步 装饰
 │   土格 → interior 池
 │   草格·有土邻 → 8邻域 Bitmask 分类：仅对角→corner｜单侧正向→edge｜
 │                 相邻双侧→corner｜相对双侧→multi(±90°)｜≥3侧→interior
 │   草格·无土邻 → 装饰层：D=2 → 30% 斑驳｜D≥3 → 12% 斑驳 + clean 变体轮换
 │   （概率/变体 = 独立噪声 decoSeed=layoutSeed^0xC0FFEE，与地形场不同源）
 └─ GroundTileset（Resources/Art/Tiles/Ground 运行时加载，FrameAnimator 同模式）
     语义命名即合同：grass_clean_N / dirt_interior_N / grass_edge_{top..}_N /
     grass_corner_tl_N / grass_corner_bl_N / grass_multi_v_N
     旋转补全：corner_tl rot0/90/180/270 = TL/BL/BR/TR；multi_v rot90 = 横向贯通
```

## 3. 文件清单

| 文件 | 说明 |
|---|---|
| `Resources/Art/Tiles/Ground/*.png` ×24 | 素材入库（语义重命名）；meta：**PPU 85**（=1×1 世界单位）、**spriteMode Single**（R3）、Point 滤镜、**不压缩**（图块接缝无缝）、无物理形状 |
| `Dungeon/Generation/TerrainMask.cs` | 逻辑层：可播种 ValueNoise FBM + 簇清理 + 距离场；纯数据零视觉依赖；数组预分配确定性（seed 复现门禁不变） |
| `Dungeon/Tiles/GroundTileset.cs` | 素材库：Resources 运行时加载 + 语义池 + 旋转 Tile 缓存（Tile.transform 矩阵）；缺素材返回 null |
| `Dungeon/Tiles/TerrainPainter.cs` | 视觉层：Bitmask 分类 + 距离场装饰；只读消费逻辑层，绝不反写 |
| `DungeonBuilder.cs` | Build 头部生成 mask；PaintRoom 内部格 / OpenDoorTile 门洞格走地形绘制；素材缺失自动回退旧平色地板+房型着色 |
| `DungeonConfig.cs` | roomWidth/Height Tooltip 补地皮尺寸契约说明 |

## 4. 房间尺寸 × 地皮比例（用户核心诉求）

- 地皮 85px @ PPU85 = **1×1 世界单位 = Tilemap 一格**；房间尺寸（roomWidth/Height）本就是整数瓦片 → **铺设天然零错位**。
- 契约已写入 DungeonConfig Tooltip：房间内部尺寸必须为整数（=地皮图块数）；doorWidth 同理。
- 跨格房（Boss 2×2 等）经由 TileRect 联合矩形采样，土斑跨房间连续（世界绝对坐标采样，门洞/接缝不割裂）。

## 5. 与现有系统的关系

- **着色**：地形路径不再叠房型/主题 tint（草地自带色彩，叠色会脏）；素材缺失回退路径保留旧 tint 逻辑。
- **墙体**：wallTile 原路径不变（border 格）。
- **seed 复现**：地形/装饰 seed 均派生自 layoutSeed（^0x7EA9 / ^0xC0FFEE），EditMode 地牢 seed 测试语义不变。
- **零 GC**：逐格绘制只有哈希运算 + Tile 缓存字典查找，无每帧分配（mask/距离场在 Build 一次性预分配）。

## 6. 参数调校入口（都在 TerrainMask 常量）

- `DefaultFrequency 0.24`：波长 ≈4.2 格 → 土斑直径 2~5 格；调大=更碎，调小=更大片。
- `DefaultThreshold 0.58`：离线仿真土覆盖 29~36%；调低=土更多。
- `DefaultMinDirtCluster 4 / MinGrassHole 3`：噪点清理力度。
- 装饰概率：TerrainPainter 顶部 `BaldPatchChanceD2/Far`（0.30/0.12）。
- 变体丰富度：往对应语义池加图即可（命名前缀对齐），无需改代码。

## 7. 验收清单

1. Unity 编译无错（新素材 24 张导入 Console 无红）。
2. Play v0_7 正式流程进地牢：地板为草地+土斑，土斑跨越门洞与房间边界连续。
3. 边缘观察：土斑边缘有贴边/转角过渡图块，无"半格错位"；远离土斑的草地大部分为纯草（~88%）+ 偶发斑驳。
4. 同 seed 重开地图地形完全一致；换层地形变化。
5. EditMode 测试全绿。

## 8. 遗留与后续

- 素材方向补齐：现有 edge 各方向已有；corner 有 tl(2)+bl(1) 张，其余象限靠旋转（视觉上土斑形状会镜像/旋转，同素材重复感轻微）——后续补 tr/br 原画可零代码接入。
- 用户规格中的"区域连接随机游走"未实现（噪声团块已自然相连，需要侵蚀带/裂隙时再加）。
- 纯土图块缺位：大面积土区观感为重斑驳草地；补 `dirt_full_N` 素材后可在 PaintCell 土格分支优先取该池。
