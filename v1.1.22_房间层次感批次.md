# v1.1.22 房间层次感批次：非矩形房型 + 房内墙体/柱阵 + 障碍密度保底

## 需求

用户：房型不应只有长方形；房内障碍太少、没有成片的墙体；需要层次感。

## 实现

### 1. RoomShaper（新文件，Dungeon/Generation）

在矩形房间内部做两类结构化改造，输出"改墙格集合"由 PaintRoom 统一铺墙块瓦片
（TilemapCollider2D 自动出碰撞；SpawnPositionHelper 物理选点自动避开——三类 Spawner 零改动）：

- **角部挖除**（房型多样性）：50% 概率出形——单角凹口（L 形）30% / 双角凹口 20% / 四角斜切（八角观感）12%；
- **房内墙体段 + 石柱**（成片墙体）：每房 2~4 段 2~5 格墙段（横竖随机）+ 40% 概率 1~2×2 石柱。

安全规则（不可能产生死区/堵门）：门洞格 Chebyshev ≤2 保护带不动（门→中心通路永通）；
房中心 ±2 保护（出生/传送/展台）；墙段距外墙 ≥2（四周永留环形通路）；墙段间距 ≥2 不与挖除重叠；
挖除总量 ≤ 面积 35%。确定性 rng（layoutSeed×31+roomId×911），同 seed 复现（EditMode seed 门禁不变）。
离线仿真 200 次/尺寸：挖除均值 6~7%（上限合规），房内墙格 18×11→3.1 / 36×19→7.1 随尺寸递增。

### 2. DungeonBuilder 接线

- `CarveDoor` 拆分为 `ComputeDoorRect`（纯几何）+ `OpenDoor`（开洞）——Build 先算全部门洞矩形，
  收集各房"门洞保护带格集"（两侧房都收），PaintRoom 带保护带塑形后再统一开洞（行为顺序等价）；
- `PaintRoom`：Start（出生安全）与 Boss（竞技场可读性）保持完整矩形，其余房型塑形；
- `ConvertToWall`：墙瓦片 + 撤地板 + **从 groundCells 移除**（RoadMask 地面 Mask 不得画草地到新墙上）。

### 3. ObstacleSpawner 密度保底

每 45 格房内面积至少 1 个障碍（18×11 ≈ 198 格 → ≥5 个），表 roll 高于保底则尊重表值——
SO 资产零改动，后续调密度仍以表为主。

## 验收

1. 编译无错 → Play：进不同房间应看到 L 形/切角房型、房内墙段与石柱、明显更多障碍；
2. 任意房间四向可达（门→中心通路无阻断）、F9 走两圈无卡死角；
3. 敌人/宝箱/商店不生成在墙里（物理选点自动避开）；Start 房与 Boss 房仍为完整矩形；
4. 同 seed 重开地图布局与塑形完全一致。

## 后续可调参数（RoomShaper 常量）

形状概率 / 保护半径 / 墙段数量区间 / 挖除上限；障碍密度（ObstacleSpawner.AreaPerObstacle）。
