# v0.5.4.2 怪物行为框架 操作报告

> 后续主方向已经进入 v0.5.4.3“战斗生态闭环”。架构、技术选型、阶段边界与验收标准见 [`v0.5.4.3_战斗生态闭环设计与实施计划.md`](./v0.5.4.3_战斗生态闭环设计与实施计划.md)。

## 版本信息
- **版本号**：v0.5.4.2
- **版本名称**：怪物行为框架（5种AI行为 + 投射物 + 多招系统）
- **前置版本**：v0.5.4.1（怪物多招式攻击系统）
- **状态**：✅ 代码、资源刷新与 Play Mode 基础冒烟已通过；远程/召唤资源绑定和战斗专项验收待完成
- **日期**：2026-07-24（初版创建 + 二次补齐）；2026-08-14（结构审查与稳定性修复）

## 一、交付资产总览

| 类别 | 数量 | 内容 |
|------|------|------|
| 新增脚本 | 2 | `EnemyBehaviorConfig.cs`, `Projectile.cs` |
| 改造脚本 | 6 | `AttackData.cs`, `EnemyAI.cs`, `EnemyCombat.cs`, `WeaponController.cs`, `EnemySpawner.cs`, `Projectile.cs` |
| 新增 AttackData | 4 | `AttackData_Ranged/Skirmisher/Charger/Summoner.asset` |
| 新增 Prefab | 1 | `Projectile.prefab` |
| 新增 Prefab Variant | 4 | `Enemy_Ranged/Skirmisher/Charger/Summoner.prefab` |
| 已有 SO | 4 | `BehaviorConfig_Ranged/_Skirmisher/_Charger/_Summoner.asset`（初版已创建） |
| 更新 SpawnTable | 1 | `EnemyTable_Combat.asset` |

## 二、5种敌人行为

| 类型 | Prefab Variant | 颜色 | HP | 移速 | 索敌 | 攻击类型 |
|------|----------------|------|----|------|------|----------|
| Melee | Enemy.prefab（不变） | 红 | 3 | 3 | 5 | 近战平A（v0.4.6 即存在） |
| Ranged | Enemy_Ranged | 蓝 | 2 | 2 | 8 | isProjectile=1 远程射击，保持 preferredDistance（代码已实现，`projectilePrefab` 尚未绑定） |
| Skirmisher | Enemy_Skirmisher | 橙 | 3 | 4 | 6 | 近战突进后自动后退 retreatDuration 秒，循环 |
| Charger | Enemy_Charger | 黄 | 4 | 2.5 | 7 | isCharge=1 蓄力前摇→直线冲锋位移，撞墙/撞人停止 |
| Summoner | Enemy_Summoner | 紫 | 5 | 1.5 | 10 | isSummon=1 远离玩家、定期召唤小兵，上限 maxMinionsAlive=4（代码已实现，`summonPrefab` 尚未绑定） |

## 三、今日补齐（2026-07-24 第二部分）

> 初版创建后发现 6 个问题，逐一修复。

### 修复清单

| # | 文件 | 问题 | 严重度 | 修复 |
|---|------|------|--------|------|
| 1 | EnemyCombat.cs | `attackData` 重复声明（public + `[SerializeField] private`）→ C# 编译错误 | 🔴 | 删除重复的 private 字段 |
| 2 | EnemyAI.cs | `attackData` 字段声明但从未被 EnemyAI 使用 → 死代码 | 🟡 | 删除字段 |
| 3 | Projectile.cs + AttackData.cs | `wallLayer` 字段定义了但 `OnTriggerEnter2D` 从未检查 → 投射物穿墙 | 🔴 | `Launch()` 新增 `obstacleLayer` 参数；命中墙体直接销毁；AttackData 新增 `obstacleLayer` 字段 |
| 4 | EnemyAI.cs + EnemyCombat.cs | `BehaviorConfig.maxMinionsAlive=4` + `aliveMinions` 字段都有，从不检查/更新 → 召唤无上限 | 🟡 | 新增 `RegisterMinion()` / `CanSummonMore()`；召唤前检查上限；小兵死亡 OnDeath 回调减计数 |
| 5 | EnemyAI.cs | Skirmisher 在 `TryStartAttack` 成功时**立即**标记撤退，而不是攻击结束后 → 攻击与后退重叠 | 🟡 | 撤退标记 `MarkSkirmishRetreat()` 移到 `ChangeState` Attack→Chase 退出时触发 |
| 6 | EnemyCombat.cs | Charger 冲锋期间无碰撞检测 → 穿墙不停 | 🟡 | Active 阶段每帧 Raycast（障碍物层+目标层），命中即停 + isCharging 置 false |

### 序列化清理

| # | 文件 | 清理 |
|---|------|------|
| 7 | Enemy.prefab | 删除 EnemyAI 组件上残留的 `attackData: {fileID: ...}` 序列化数据（字段已从代码删除，残留导致反序列化异常） |

### 新建资产

| # | 文件 | 内容 |
|---|------|------|
| 8 | AttackData_Ranged.asset | isProjectile=true, projectileSpeed=8, cooldown=1.5, obstacleLayer=256 |
| 9 | AttackData_Skirmisher.asset | 近战快速打击，cooldown=0.6, angle=100° |
| 10 | AttackData_Charger.asset | isCharge=true, chargeSpeedMultiplier=3, windup=0.5, damage=2, chargerCollisionLayer=256（普通 obstacleLayer=0） |
| 11 | AttackData_Summoner.asset | isSummon=true, summonCount=2, cooldown=8 |
| 12 | Enemy_Ranged.prefab | Variant of Enemy.prefab, 蓝色, HP=2, Speed=2, Detection=8, BehaviorConfig_Ranged, AttackData_Ranged |
| 13 | Enemy_Skirmisher.prefab | Variant of Enemy.prefab, 橙色, HP=3, Speed=4, Detection=6, BehaviorConfig_Skirmisher, AttackData_Skirmisher |
| 14 | Enemy_Charger.prefab | Variant of Enemy.prefab, 黄色, HP=4, Speed=2.5, Detection=7, BehaviorConfig_Charger, AttackData_Charger |
| 15 | Enemy_Summoner.prefab | Variant of Enemy.prefab, 紫色, HP=5, Speed=1.5, Detection=10, BehaviorConfig_Summoner, AttackData_Summoner |
| 16 | Projectile.prefab | WhiteCircle 精灵 + SpriteRenderer(浅蓝) + Rigidbody2D(Kinematic) + CircleCollider2D(Trigger r=0.5) + Projectile.cs |
| 17 | EnemyTable_Combat.asset 更新 | entries 从 1 条(Melee)扩展到 5 条(Melee w10 + Ranged w6 + Skirmisher w6 + Charger w4 + Summoner w3) |
| 18 | BehaviorConfig GUID 首轮修正 | 首轮修正 4 份 .meta；后续实机修复累计清理 11 份非法 GUID，并同步更新 Prefab 引用 |

## 四、仍待完成的资源绑定与专项验收

1. **Minion.prefab**：基于 Enemy.prefab 创建 Variant → Scale(0.6,0.6,1) → Color 浅紫 → HP=2, Speed=2.5, Detection=6
2. **AttackData_Ranged.projectilePrefab** → 拖入 `Projectile.prefab`
3. **AttackData_Summoner.summonPrefab** → 拖入 `Minion.prefab`
4. **确认碰撞层**：Ranged 的 `obstacleLayer.m_Bits=256`、Charger 的 `chargerCollisionLayer.m_Bits=256` 对应实际 Wall/障碍物 Layer
5. **专项 Play 验收**：基础地牢冒烟已经通过；仍需逐一触发 4 种敌人的完整战斗行为

## 四-A、结构审查与稳定性修复（2026-08-14）

> 本节记录已经写入代码/项目配置的功能。Unity MCP 已完成资源刷新、编译与基础 Play Mode 冒烟；下列专项交互场景仍需逐项验收，不能仅凭基础冒烟视为全部通过。

| # | 范围 | 已实现内容 |
|---|------|------------|
| 1 | 构建入口 | `EditorBuildSettings.asset` 的启用场景由 `SampleScene` 改为 `Assets/Scenes/v0_5_Dungeon.unity` |
| 2 | 房间休眠 | `Room` 注册敌人时保存 Rigidbody2D 原始约束；休眠时 FreezeAll，唤醒时恢复各 Prefab 原始约束，并清零线速度/角速度 |
| 3 | 召唤与清房 | 召唤物通过父级找到所属 `Room` 并调用 `RegisterEnemy()`；召唤物存活时计入 `AllEnemiesDead` 清房条件 |
| 4 | 召唤上限 | 召唤物死亡后立即解绑事件并从召唤师追踪集合移除；集合数量作为存活数，且阻止重复注册 |
| 5 | 近战判定 | `WeaponHitbox` 从“按 Collider 去重”改为“按 IDamageable 去重”，一个角色有多个 Collider 时每次挥击仍只受伤一次 |
| 6 | 投射物判定 | 投射物从命中 Collider 开始向父级查找 `IDamageable`，支持 Collider 在子对象、生命组件在角色根对象的 Prefab |
| 7 | 攻击中断 | `EnemyCombat.OnDisable()` 统一停止 Hitbox、预警、武器动画、冲锋和刚体移动，并复位武器朝向与攻击状态 |
| 8 | 冲锋健壮性 | `EnemyCombat` 要求 `EnemyController` 组件；冲锋位移增加空引用保护 |
| 9 | 地牢重建 | Play Mode 清理旧地牢时先停用旧对象，再延迟 Destroy，避免旧 Collider 在同一帧干扰新内容的重叠检测 |
| 10 | 生成配置 | `SpawnTable.RollCount()` 使用局部安全边界，不再在运行时修改 ScriptableObject 的 `countMax`，并把负数最小数量钳制到 0 |
| 11 | 日志与生命周期 | 移除游击后退、伤害、护甲恢复和 UI 数值变化的高频日志；删除 `PlayerController` 空 `Update()` |
| 12 | 工程整理 | `.gitignore` 增加根目录 `comfy*.log` 忽略规则 |
| 13 | 清房开门 | `Room` 不再只依赖一次性 `EnemyHealth.OnDeath` 回调；增加死亡回调解绑和 Active 状态逐帧清理，自动移除已死亡/已销毁的残留敌人引用，计数归零后可靠切换到 Cleared 并刷新房门 |
| 14 | Variant 死亡清理 | `EnemyController` 移除 0.5 秒灰色尸体停留；死亡时立即停止协程和移动、禁用 AI/Combat、关闭全部子 Collider/Renderer、停用对象，并在帧末销毁 |
| 15 | Skirmisher 激活状态 | `Enemy_Skirmisher.prefab` 根对象错误保存为 `m_IsActive=0`，导致 Awake/死亡订阅不执行；已修正为激活 |

### 2026-08-14 专项验收清单

1. 一个敌人配置两个非 Trigger Collider，玩家一次挥击只能扣一次血。
2. 将目标 Collider 放在子对象、`EnemyHealth`/`Health` 放在父对象，投射物仍能正确造成一次伤害。
3. 召唤师召出小兵后先击杀召唤师：小兵仍存活时房门不得打开；全部小兵死亡后房门打开。
4. 让不同敌人 Prefab 使用不同 Rigidbody2D Constraints，进房唤醒后应恢复各自原值。
5. 在 Play Mode 连续调用两次 `DungeonManager.Generate()`，新一层内容数量和固定 seed 结果不应受旧 Collider 干扰。
6. 敌人在 Windup、Active/冲锋和 Recovery 阶段分别被禁用，均不得残留判定框、预警、动画或移动速度。
7. 检查 Build Settings 第一场景为 `v0_5_Dungeon`，执行一次目标平台 Development Build。

### 2026-08-14 Unity MCP 实机修复与验证

- 修复 11 份 `.meta` 中长度错误或含非十六进制字符的占位 GUID，并同步更新现有 Prefab 引用。
- 通过 Unity `AssetDatabase.LoadAssetAtPath<GameObject>()` 重新绑定 `EnemyTable_Combat` 的 5 个敌人 Prefab；Unity 已写入合法 Variant 根对象 fileID，消除 `PrefabInstance → GameObject` 的 `InvalidCastException`。
- Unity MCP 实际进入 Play Mode：地牢成功生成 `11 rooms / 11 connections`，Boss 距离为 4。
- 本次 Play Mode Console：0 Error、0 Warning；随后已正常退出 Play Mode。
- 尚未自动模拟 WASD，玩家键盘移动仍需在 Game 窗口获得焦点后人工验收。
- 清房开门隔离测试已通过：正常死亡事件路径会从 Active 切换到 Cleared 并开门；敌人未发出死亡事件而直接销毁时，主动校验路径也会在下一帧开门。
- Variant 死亡专项测试已通过：Ranged、Skirmisher、Charger、Summoner 均在死亡当帧停用 AI/Combat、全部碰撞体和视觉，后续帧对象数量归零；黄色 Charger 不再留下灰色尸体或继续运行状态机。

## 五、战斗逻辑文件速查

> 全部战斗逻辑脚本集中在 `Assets/Scripts/now_use/`。
> 详见 [v0.5.4.2_战斗逻辑目录.md](./v0.5.4.2_战斗逻辑目录.md)

| 脚本 | 职责 |
|------|------|
| [`EnemyCombat.cs`](Assets/Scripts/now_use/EnemyCombat.cs) | 攻击状态机 + 多招选择 + 投射物发射 + 召唤小兵 + 冲锋位移与碰撞 |
| [`EnemyAI.cs`](Assets/Scripts/now_use/EnemyAI.cs) | 高层 FSM（Patrol/Chase/Attack/Return）+ 5 种行为 + 召唤物上限与生命周期追踪 |
| [`EnemyBehaviorConfig.cs`](Assets/Scripts/now_use/EnemyBehaviorConfig.cs) | 行为配置 SO（类型 + 距离偏好 + 速度倍率 + 召唤参数） |
| [`AttackData.cs`](Assets/Scripts/now_use/AttackData.cs) | 攻击参数容器（阶段时长/范围/伤害/isProjectile/isCharge/isSummon/obstacleLayer） |
| [`Projectile.cs`](Assets/Scripts/now_use/Projectile.cs) | 投射物实体（直线飞行 + 父级伤害组件查找 + 目标层命中伤害 + 墙体层销毁） |
| [`EnemyController.cs`](Assets/Scripts/now_use/EnemyController.cs) | 物理移动/朝向 + SetChargeVelocity 冲锋接口 |
| [`WeaponHitbox.cs`](Assets/Scripts/now_use/WeaponHitbox.cs) | 近战矩形检测 + 按伤害对象去重，兼容多 Collider 目标 |
| [`Room.cs`](Assets/Scripts/v0_5/Runtime/Room.cs) | 敌人清房注册 + 召唤物清房计数 + 休眠/唤醒时恢复刚体原始约束 |

## 六、未开始的工作

- v0.5.4 主线：楼层循环（RunManager / Boss 结算 / 传送门 / 死亡重开 / 难度缩放）——属计划书五-E，完全未动
- Elite/Boss 相关非法 `.meta` GUID 已在本轮累计 11 份修复中清理；其多招 AttackData 的行为正确性仍待专项 Play Mode 验证
