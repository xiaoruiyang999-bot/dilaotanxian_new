# v1.1.26 废墟石块装饰（小=无碰撞点缀，大=障碍物替代）

## 素材

源：`E:\WeGameApps\The animation of Unity\狼人\石块装饰_新版预览_gap6\prop_01~34.png`（29×25 ~ 146×75，
34 张透明底，manifest 切片自生成图）。导入 `Assets/Resources/Art/Decor/Stones/`（34 张），
Single/Point/不压缩/**PPU85 与地皮同契约**（85px=1 单位——大小自适应：素材像素尺寸即世界尺寸）。

分类（运行时按 bounds 长边自动判，无需人工分组）：**≥1.06 单位（≈90px）= 大石块**（8 张：
18/19/20/21/23/25/29/30），其余 26 张为小石块。后续加图零代码——放对目录自动入池分类。

## StoneDecorSpawner（SpawnContent 挂点，与房内容同 rng 确定性）

- **小石块**：1~4 个/房，**无碰撞纯装饰**（sortingOrder 3，法力球上金币下），50% 水平翻转变体；
- **大石块**：0~2 个/房，**障碍物替代**——Obstacle 层 + BoxCollider2D（sprite bounds ×0.8 内缩，
  圆石留角部余量防卡人）+ ObstacleHealth(3) 可破坏；语义同 Obstacle prefab（挡移动、不进清房计数）；
- **防卡墙**：落点全走 SpawnPositionHelper.TryFind（距墙 ≥1、距门 ≥2.5、实体重叠检测——
  v1.1.23 房内模板墙/挖除角/其他障碍自动避开）；
- **数量克制**：小 1~4、大 0~2（含 0 概率留白）；挂 Room.ContentRoot 随房销毁/休眠。

## 验收

1. 编译无错 → Play：地面出现零散小石块（可踩过，无碰撞）、少量大石块（挡路、可砍碎）；
2. 石块不嵌墙/不堵门/不与敌人障碍重叠；随机翻转与选图每次进图不同、同 seed 一致；
3. 破坏大石块有障碍破坏反馈、不计入清房条件。

## 关联

v1.1.22 障碍密度保底 / v1.1.23 塑形保护带（石块落点自动兼容）/ R6 Obstacle 层语义。
