# v1.1.47 技能树页面(石板素材 · M4 meta 层首块)

## 需求(用户)

制作技能树页面,使用石板素材(PanelSprite 母版体系)。

## 系统定位

技能树 = **局外 meta 成长**:准备室技能石碑(E)或暂停菜单"技能树"进入,消耗**魂晶**解锁
永久属性加成,效果跨局生效。这是项目第一块持久化 meta 系统(此前只有音量走 PlayerPrefs)。

## 实现(8 处)

### 数据层(Meta/)

- **SkillTreeSave**:PlayerPrefs 持久化——已解锁节点 id(`skilltree_unlocked` 逗号串)+
  魂晶余额(`skilltree_essence`)+ 首启赠送 10 魂晶的一次性标记。魂晶来源:死亡结算每击杀 1 枚
  (DeathPanel.Show 入账,panelRoot 判重保证只发一次)。
- **SkillTreeDef**:纯代码树定义(3 支线×4 层,线内线性前置,跨线独立)+ 效果聚合 Aggregate。
  - 力量:伤+8%/10%/12% → 暴伤+25%(3/5/8/12 魂晶)
  - 迅捷:移+6% → 攻速+8% → 移+8% → 攻速+12%
  - 坚韧:HP+20 → 甲+10 → HP+30 → 甲+15
  全树毕业:+30% 伤/+14% 移/+20% 攻速/+50 HP/+25 甲/+25% 暴伤(数局一层,全线=长期目标)。

### 属性挂点

- **PlayerStats**:新增 Perm 六通道(DamageMult/MoveSpeedMult/AttackSpeedMult/CritDamageBonus/
  HpBonus/ArmorBonus),getter 乘/加进 Attack/MoveSpeed/CritDamage/MaxHP/MaxArmor;
  `ApplyClass` 末尾聚合写入(Initialize 含 HP 加成);`RefreshSkillTreeBonuses()` 供 UI 解锁后
  即时生效(HP 节点经 Health.Initialize 回满——**设计选择:加点视为休整**)。
- **PlayerCombat.AttackSpeedMul**:乘入 PermAttackSpeedMult(Buff×兽化×技能树三乘)。

### UI(石板整版)

- **SkillTreeUI**:静态 Open/Close(230 sortingOrder,盖暂停菜单 220 之上;不碰 timeScale——
  从暂停进入时由暂停面板继续管停时)。1560×860 石板主面板;节点=btn_stone 切片底 + 支线色图标 +
  名称/效果/状态三行;三态染色:已解锁=暖金、可解锁=白(可点,Button ColorTint hover)、
  锁定=灰(无 Button,hover 无反馈=不可用语义);连线按解锁态点亮(金/支线暗阶)。
  文本/图标 raycastTarget=false(不拦节点点击)。解锁→存档+RefreshSkillTreeBonuses+整树重建。
- **SkillTreeBeacon**(准备室):传送门西侧石碑(Interactable 可重复交互,E 开关技能树),
  碑体两叠石块 + 三支线色符文 DOTween 呼吸(PrepPortal 同视觉语言)。

### 接线

- **PlayerController**:Cancel 链最前加 SkillTreeUI(Esc 最上层优先);**选择 UI 屏蔽链加
  SkillTreeUI.IsOpen**——不拦的话开着页面按 E 会触发石碑 Interact 把页面当场关掉(自查发现的真 bug)。
- **PausePanel**:Toggle 让位规则保留(技能树开着时 Esc 归它,防止关树同时弹出暂停面板)。
  **入口不进暂停菜单**(v1.1.47.2 用户拍板:暂停面板保持原三键格式)——技能树唯一入口=准备室石碑。
- **DeathPanel**:结算新增金色行"获得魂晶 +N(已入账)"。

## 门禁

- `compile_check.py` 真编译 0 错(135 源文件)。
- 新增 `SkillTreeTests`(5 项:树定义 id 唯一/坐标覆盖、前置链线内线性、全解锁聚合值、
  RequirementMet 行为、存档读写往返;PlayerPrefs 键 Setup/TearDown 清理)。
  **注:编辑器重启后 UnitySkills 服务未自动恢复,EditMode 远程执行待服务开启后补跑
  (Unity: Window > UnitySkills 面板开服务,或直接 Test Runner 全量)。**

## Play 走查清单

1. 准备室传送门西侧见石碑(三色符文呼吸),E 打开技能树,再 E/Esc 关闭;
2. 暂停菜单(Esc)保持原三键格式,技能树不在此入口(v1.1.47.2);
3. 首启有 10 魂晶:点"重击 I"立即生效(Attack 提升);锁定节点显示前置名且不可点;
4. 魂晶不足红字提示;死亡结算见"获得魂晶 +N",回准备室余额增加(重进游戏仍在=持久化);
5. 解锁 HP 节点血条上限提升且回满(设计选择)。

## 关联

v1.1.5~7 石板母版体系(PanelSprite)、v1.0.8 TMP 文本规范、v1.1.12 GraphicRaycaster 红线、
v0.9.0 M4(meta 层启动块)。后续可演化:节点 SO 化、重置返还、更多支线、魂晶其他来源。
