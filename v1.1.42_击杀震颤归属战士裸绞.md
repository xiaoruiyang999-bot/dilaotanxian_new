# v1.1.42 击杀震颤归属战士裸绞

## 目标

击杀震颤不是全局敌人死亡反馈，而是战士技能组中「裸绞」的专属斩杀表现。

## 实现

- `EnemyHealth`、`WeaponHitbox` 不触发击杀震屏或停帧。
- `BossPhaseController` 移除 Boss 死亡时独立的大停顿与大震屏。
- `SkillExecutor.ResolveGarroteHit` 在裸绞实际造成击杀后调用 `ExecuteFeedback.Play`：
  - 达到普通/精英阈值并直接处决时触发；
  - 未达到阈值、但本次真实伤害正好致死时也触发；
  - 未击杀、目标此前已死亡、普通攻击或其他技能均不触发。
- 玩家受伤的受击震屏不变，它不属于敌人击杀反馈。

## 验收

1. 普通攻击击杀普通怪、精英与 Boss：无震屏、无停帧。
2. 战士裸绞命中但未击杀：无震屏、无停帧。
3. 战士裸绞阈值处决或真伤致死：触发 0.05 强度/0.08 秒震屏和 0.08 秒停帧。
4. 玩家受到伤害时仍有原受击震屏。
