# v1.0.14 敌人 AI 与战斗合并恢复验收

> 日期：2026-09-01  
> 状态：已修复并完成 EditMode 回归；待人工 Play 手感验收。

## 根因

战斗/MCP 分支合并到 main 时，五行为 `EnemyAI` 曾被 main 的纯近战版本覆盖，导致远程、游击、冲锋、召唤四类怪物失去专属决策逻辑。工作区虽然已经恢复多行为代码，但 Prefab 仍存在静默断链：

- `Enemy_Skirmisher.behaviorConfig` 为空，运行时退化为默认 Melee。
- `Enemy_Skirmisher`、`Enemy_Charger`、`Enemy_Summoner` 的 `EnemyCombat.attackData` 为空，AI 能追击但 `TryStartAttack` 无法启动攻击。
- `EnemySpawner` 未再向恢复后的 AI/Combat 注入房间 seed 派生随机源。

## 本次修复

- `Enemy_Skirmisher.prefab` 接回 `BehaviorConfig_Skirmisher`。
- 三个特殊近战 Prefab 的 `EnemyCombat` 分别接回：
  - `AttackData_Skirmisher`
  - `AttackData_Charger`
  - `AttackData_Summoner`
- `EnemyCombat` 恢复 `SetCombatRng(System.Random)`。
- `EnemySpawner` 为每只敌人注入独立的行为/战斗子随机源，使横移、招式和召唤落点同 seed 可复现且不同敌人不同步。
- 恢复远程怪蓄力期间的持续 LOS 检查：短暂丢失目标使用宽限时间，超时取消本次攻击并进入半段冷却；仍可见时预警方向与半径继续跟随目标。
- 恢复精英/Boss 的多招式池选择：支持 Random、Distance、Sequence、Weighted 四种模式，并重新消费 `AttackData` 的距离区间与权重。
- 新增 `EnemyArchetypeWiringTests`，覆盖四种行为配置、远程/游击/冲锋/召唤关键接线，以及精英/Boss 招式池配置。

## 验证结果

- Unity 编译：0 个 C# 错误。
- 四怪专项 EditMode：8/8 通过。
- 全套 EditMode：20/20 通过。
- 验证内容：
  - Ranged → Ranged 行为 + ProjectileData。
  - Skirmisher → Skirmisher 行为 + Combat AttackData。
  - Charger → Charger 行为 + `IsCharge=true`。
  - Summoner → Summoner 行为 + `IsSummon=true` + Minion Prefab。
  - Elite → Distance 模式 + 至少 3 个招式。
  - Boss → Weighted 模式 + 至少 3 个基础招式 + 阶段二招式池。

## 其余功能审查结论

- 当前活动场景健康检查无 Missing Script、无丢失序列化引用错误；Build Settings 只保留有效的准备房与职业武器场景。
- 暂停、死亡面板、音频/BGM、命中反馈、角色选择、狼人变身、远程敌人与 Boss 阶段功能已有对应实现及既有恢复记录，本轮未发现新的确定性合并断链。
- 金币/局外成长/三选一/Victory 属于当前 v0.7.5 产品范围中明确延期的功能，不作为“合并丢失”擅自恢复，避免把旧归档代码重新引入正式目录。

## 待人工 Play 验收

- 远程怪在理想距离停火、近身后撤、墙边横移。
- 游击怪命中后强制后撤，再重新接近。
- 冲锋怪蓄力后沿锁定方向冲锋，撞墙/玩家停止。
- 召唤师保持距离，召唤小兵且不超过上限，小兵计入清房。
