# v0.6.1 手感与反馈包（M1）

> 状态：**代码已落盘 · 待 Unity 挂载 AudioManager + 人工验收**
> 日期：2026-08-20
> 里程碑：M1·手感与反馈包（[项目长期计划_Roguelite路线图](计划书/项目长期计划_Roguelite路线图.md) § M1）
> 关联文档：[v0.6.0_小地图系统](v0.6.0_小地图系统.md) / [v0.5.4.4.4_战斗判定与场景恢复修复](v0.5.4.4.4_战斗判定与场景恢复修复.md)

---

## 1. 本次交付（10/10 项全部完成）

| # | 任务 | 状态 |
|---|---|---|
| 1.1 | 闪避 Dash（Space，朝移动方向，无输入用最近朝向兜底） | ✅ |
| 1.2 | Dash 无敌帧（冲刺全程 + 0.06s 收招缓冲，`Health.GrantIFrames`） | ✅ |
| 1.3 | AudioManager（PlayOneShot 天然叠放 + SFX/BGM 双路音量 + 缺配置静默） | ✅ |
| 1.4 | 音频素材：Kenney Impact Sounds（CC0，130 个 ogg 已入 `Assets/Audio/`） | ✅ |
| 1.5 | 五处挂点：命中 / 受击 / 敌死 / 开关门 / 开箱 | ✅ |
| 1.6 | Hit-stop（timeScale 归零 0.03s，隐藏 Driver 自动恢复，防卡死） | ✅ |
| 1.7 | 屏幕震动（`CameraFollow.Shake` + 静态 `ShakeMain`，unscaled 衰减与 hit-stop 叠加） | ✅ |
| 1.8 | 还债：受击闪烁协程重叠永久变白（基线色 + 互斥） | ✅ |
| 1.9 | 还债：Windup LOS 取消进**半额**冷却（消除蓄力抖动，短暂遮挡不按整 CD 罚） | ✅ |
| 1.10 | 还债：EnemyPerception 三处查询 NonAlloc + 目标碰撞体缓存（零每帧 GC） | ✅ |

## 2. 改动文件清单

**新增**：`Common/AudioManager.cs`、`Common/HitStop.cs`
**修改**：`PlayerInputActions.inputactions`（+Dash action）、`PlayerController.cs`（冲刺）、
`Health.cs`（无敌帧 + 受击反馈）、`CameraFollow.cs`（震动）、`WeaponHitbox.cs`（命中三件套）、
`EnemyController.cs`（死亡音效 + 闪烁修复）、`Door.cs`（状态跟踪 + 开关音）、
`ChestInteractable.cs`（开箱音）、`EnemyCombat.cs`（CancelAttack 冷却倍率）、
`EnemyPerception.cs`（NonAlloc）
**素材**：`Assets/Audio/Kenney_ImpactSounds/Audio/`（130 个 ogg，CC0 公有领域）

## 3. Unity 挂载步骤（唯一人工操作，约 2 分钟）

1. 打开 Unity（场景外部修改提示 → Reload），确认 Console 无编译错误
2. Hierarchy 右键 → Create Empty → 命名 `AudioSystem`（场景根，**不要**放 dungeonRoot 下）
3. Add Component → **AudioManager**
4. Inspector 配置 SFX Entries（5 行）——推荐文件在 `Assets/Audio/Kenney_ImpactSounds/Audio/`：

| id（必须精确一致） | 推荐文件 | volume |
|---|---|---|
| `hit` | impactPunch_medium_000.ogg | 0.8 |
| `hurt` | impactSoft_heavy_000.ogg | 0.9 |
| `enemyDie` | impactWood_heavy_000.ogg | 0.9 |
| `door` | impactPlank_medium_000.ogg | 0.7 |
| `chest` | impactPlate_medium_000.ogg | 0.8 |

5. BGM 字段可留空（本包无音乐）；后续从 Kenney 音乐包/OpenGameArt 选一首拖入即自动循环
6. Play 验收

> 未挂 AudioManager 时游戏照常运行，只是静音——所有挂点都是静默安全调用。

## 4. 验收清单

- [ ] Console 无编译错误；未配音效前游戏行为正常（静音）
- [ ] 挂载并配置 AudioManager 后：命中 / 受击 / 敌死 / 开关门 / 开箱有声音
- [ ] **Dash**：Space 向移动方向短冲刺；无移动输入时向最近朝向冲刺；有冷却（约 0.9s）
- [ ] **无敌帧**：冲刺穿过敌人预警中的攻击范围不掉血（时间窗口内）
- [ ] **打击感**：命中瞬间画面微停 + 轻震；受击重震——砍怪"变重了"
- [ ] 连续受击的敌人不再永久变白
- [ ] 远程敌人在视野边缘不再"蓄力→取消→再蓄力"抖动
- [ ] 8 敌同房：Profiler 无每帧 GC 尖峰、帧率平稳
- [ ] 死亡重开后 Dash 冷却已重置（立即可冲刺）
- [ ] 小地图 / 楼层循环等 v0.6.0 前功能无回归

## 5. 设计备注

- **HitStop 与暂停**：当前 timeScale 只有 0↔1 两档；M5 做暂停菜单时需重构为多档管理（代码内已注释标记）。
- **WeaponHitbox 反馈对双方生效**：玩家砍敌人、敌人砍玩家都触发 hit 音效 + 停顿；受击（hurt + 重震）仅玩家有——敌人受击走闪烁反馈，层次分明。
- **Door 音效条件**：`RefreshState` 会被频繁幂等调用，音效挂在"状态真正变化"时（新增 `isOpenState` 跟踪），不会刷屏。
- **素材许可**：Kenney Impact Sounds 为 CC0（公有领域），可商用无需署名。
