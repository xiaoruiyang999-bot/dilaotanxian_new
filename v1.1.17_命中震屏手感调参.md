# v1.1.17 命中/击杀震屏手感调参

## 现象

击杀敌人时屏幕震颤/抖动观感过强。

## 取证

- 震源唯一：`WeaponHitbox` 命中三件套的轻震 `ShakeMain(0.05, 0.08)`（击杀瞬间 = 该次命中的震 + 0.03s 停帧）；
  敌人死亡本身（EnemyHealth.Die）**无**独立震屏；远程弹命中无震屏。
- 两正式场景 `shakeScale=1`（无全局放大）；障碍物/其他系统无震源。
- 观感偏猛的原因：CameraFollow 的震是**逐帧全随机方向**（insideUnitCircle，非平滑噪声），
  0.05 世界单位的纯随机高频抖动 + 击杀停帧的前后对比，体感强烈。

## 调整

`WeaponHitbox` 命中震：**幅度 0.05 → 0.025（减半），时长 0.08 → 0.06**。
受击震（玩家受伤 0.18/0.2）与 Boss 阶段震（0.5/0.8）**未动**（本次仅针对击杀场景手感）。

## 后续调参入口

- 整体手感：场景 CameraFollow 的 `shakeScale`（Inspector 直接调，0=全关）；
- 单点数值：各反馈挂点处的 ShakeMain 参数（见开发必读"反馈挂点"速查）；
- 若减半后仍觉抖动"毛刺"，可把 CameraFollow 的随机抖动换成平滑噪声（Perlin），属独立小改可随时做。

## 验收

编译无错 → 近战连续击杀：震屏幅度明显收敛、无眩晕感；命中音效与停帧不变。
