# 开发调试规范 —— 场景 / 摄像头 / 玩家位置变更管控

> 状态：**生效中 · 强制要求**
> 日期：2026-08-18（2026-08-19 增补 2.5 节与第 4 节"方向键全部失灵"假象）
> 适用范围：所有通过 MCP / 脚本 / 编辑器对 Unity 场景进行的自动化调试与修改
> 关联：[v0.5.4.4.2_远程AI攻击失效修复](v0.5.4.4.2_远程AI攻击失效修复.md) / [v0.5.4.4.4_战斗判定与场景恢复修复](v0.5.4.4.4_战斗判定与场景恢复修复.md)

---

## 1. 为什么要有这份规范

在 v0.5.4.4.x「远程攻击失效」的调试过程中，为了测试子弹命中、验证 LOS、触发攻击，
多次通过 MCP 移动了玩家位置、瞬移玩家、进出 Play 模式，导致：

- **摄像头（CameraFollow.target）被改动**，相机不再跟随玩家
- **玩家被瞬移到远离出生点的位置**，且未复位
- **场景被频繁标记为 dirty**，退出时混乱

这些"测试遗留"污染了场景，给用户造成"摄像机被移动""结果不理想"的困扰，
**干扰了真正的 bug 排查**（如子弹不生成其实是 LOS 被 RoomTrigger 挡住，
但被"玩家在障碍物后面"的测试假象掩盖）。

为防止重蹈覆辙，以下规则**对所有开发调试**强制生效。

---

## 2. 硬性要求（红线，不可违反）

### 2.1 摄像头 · CameraFollow.target【最高优先级】

> **🚫 严禁移动摄像头 / 修改主相机的 `CameraFollow.target` 指向，除非用户明确要求修相机本身。**

- **只在"相机跟随有问题且用户要求修复相机"时**，才允许修改 `Main Camera` 上的
  `CameraFollow.target`（应始终指向场景里的 Player）。
- **调试攻击、子弹、LOS、敌人 AI 时，一律不得触碰相机。**
- 若调试过程"意外"发现相机 target 为空，**记录它、报告给用户**，而不是擅自修改并
  因此打乱场景；是否修相机由用户决定。

### 2.2 玩家位置【红色】

> **🚫 严禁为调试需要瞬移/传送玩家，除非得到用户明确许可，且调试完成后必须把玩家复位到正常出生位置。**

- 调试用 `transform.position` 或 `Rigidbody2D.position` 移动玩家前，**先记录原始位置**。
- **完成后必须恢复**（复位到调试前位置）。
- 若无法恢复，**立即报告用户并说明影响**，不得留待用户自己发现。

### 2.3 场景保存【红色】

> **🚫 手动调用场景保存（save）属于高影响操作；只有用户明确说"保存场景"时才做。**

- 调试期间的修改（移动玩家、创建/销毁对象、改组件）**默认不保存**。
- 调试对象（Debug_* 临时实例、Projectile(Clone) 等）**用完后必须删除**，不留垃圾。
- 退出会话前，若场景仍被标记 dirty，**报告用户**让其决定是否保存。

### 2.4 调试残留对象【红色】

> **🚫 调试用临时对象（Debug_*、Projectile(Clone)、假玩家等）用完即删，不得留在场景。**

- 命名规范：临时对象统一加 `Debug_` 前缀，便于识别和清理。
- 每次调试结束，自清理所有 Debug_ 对象。
- **删除调试克隆玩家（如 Debug_RealPlayer）前，必须先确认正式 Player 实例链路完整**：
  处于激活状态、`PlayerInput` 正常、`CameraFollow.target` 与 `DungeonManager.player`
  已指向它——否则删掉克隆后场景里只剩一个"残废玩家"，所有输入全部失灵。

### 2.5 外部修改场景文件后的 Reload【红色 · 2026-08-19 新增】

> **🚫 任何外部工具（Python 脚本 / MCP / CLI）直接改写 `.unity` 场景文件后，
> 正在运行的 Unity 编辑器内存里仍是旧场景。未 Reload 前 Play，测的全是旧内存副本，
> 结果完全不可信，且会造成"修复没生效 / 输入失灵 / 一切都被覆盖了"的假象。**

- 外部修改场景后，**必须在 Unity 中重新加载场景**：弹出"Scene has been changed
  externally"对话框时选 **Reload**；没弹窗就 `File → Open Scene` 重新打开该场景。
- Reload 之后的验证顺序：
  1. **Console 无红色编译错误**（有编译错误 = 所有 MonoBehaviour 失效，
     任何脚本功能——包括移动——必然全部失灵，先修编译再谈别的）；
  2. Hierarchy 中确认对象变化已生效（残留已删、该有的组件已挂）；
  3. 再进 Play 验收。
- **双玩家陷阱**：若场景中"调试克隆 + 正式 Player 实例"同时激活且都 tag=Player，
  两个 `PlayerInput` 会争抢键盘设备，叠加相机 target 为空时，键盘控制的可能是
  "看不见的另一个玩家"——观感即"方向移动键全部失灵"。Play 前用 tag 搜索确认
  场景中有且只有一个激活的 Player。
- 外部改写场景文件**必须先备份**（本次惯例：`_backup/` 目录 + 手术脚本留档），
  且写回后立即做块引用完整性验证，验证不过立即回滚。

---

## 3. 调试流程规范

### 3.1 调试前

1. **记录当前场景基准**：相机 target、玩家位置、场景 dirty 状态、出 play 前保存状态。
2. 明确本次要验证的东西，**尽量用不污染场景的方式**（如编辑模式直接调用组件方法，
   而不是进 Play 移动玩家）。

### 3.2 需要移动玩家时

1. 先记录当前位置。
2. 用 `Rigidbody2D.position`（而非 `transform.position`）移动，保证物理/视觉同步，
   否则会出现"射线打不到玩家"的假象。
3. 用完之后**复位到记录位置**。

### 3.3 调试后

1. 清理所有 `Debug_` 临时对象。
2. 恢复玩家位置、相机 target 到调试前状态。
3. 若场景被标记 dirty 且不是本次的正式修改，**撤销或报告**。

---

## 4. 已知的调试假象（避免误判）

这些"现象"不是代码 bug，而是测试/调试方式造成的假象，排查时要注意：

| 假象 | 真实原因 | 正确做法 |
|------|---------|---------|
| LOS 明明很近却返回 false | 玩家被瞬移后物理体（collider）没同步到新位置 | 用 `rb.position` 而非 `transform.position` |
| 子弹明明命中却不扣血 | 假玩家（Cube）没有 Health/IDamageable | 用真实 Player prefab 测试 |
| 敌人永远不攻击 | RoomTrigger（trigger collider）挡了 LOS 射线（已修复：跳过 isTrigger） | 确认 LOS 射线不被 trigger 拦截 |
| 玩家血量显示 0 | 访问的是假玩家实例的 Health，非真实 Player | 用 `Resource.FindObjectsOfType` 过滤真实 Player |
| **方向移动键全部失灵**（2026-08-19） | ① 外部改场景后 Unity 未 Reload，Play 的是旧内存副本；② 场景里"调试克隆+正式 Player"双激活抢键盘，控制的其实是看不见的那个；③ Console 有编译错误导致所有脚本失效 | 按 2.5 节顺序排查：Reload 场景 → 看 Console 编译错误 → tag 搜 Player 确认唯一激活 |

---

## 5. 摄像头相关操作 checklist（每次调试前过一遍）

- [ ] 场景是否被外部工具改过？→ 是 → 先在 Unity 中 Reload 场景，再看 Console 有无编译错误
- [ ] 场景中是否只有一个激活的 Player（tag 搜索确认）？→ 有调试克隆 → 按 2.4 确认正式玩家链路后删除
- [ ] 我是否要移动玩家？→ 是 → 记录位置 + 用 rb.position + 完成后复位
- [ ] 我是否要修改 CameraFollow.target？→ 是 → 确认用户要求修相机
- [ ] 我是否要保存场景？→ 是 → 确认用户明确说"保存"
- [ ] 我是否创建了 Debug_ 临时对象？→ 是 → 用完删除
- [ ] 调试后可场景是否还有未复位的位置/dirty 状态？→ 是 → 复位或报告

---

## 6. 违反后果

违反上述硬性要求的操作，被视为**破坏工作区/场景状态的高危行为**，需用户介入清理。
这些教训应被后续所有涉及场景的自动化操作遵守，避免再次污染用户的项目。

---

**本规范从 2026-08-18 起对所有开发调试生效。**
