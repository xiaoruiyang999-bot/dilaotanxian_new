# v0.1 任务文档：初始小屋搭建（程序员美术版）

## 版本信息
- **版本号**：v0.1
- **版本名称**：初始小屋
- **美术策略**：纯色几何图形（色块=墙壁，圆球=角色，方块=敌人）
- **目标**：搭建可行走、有墙壁碰撞的初始小屋场景
- **预计工时**：1-2小时（新手）
- **前置要求**：Unity 2022.3 LTS已安装，已打开本项目

---

## 总体目标
创建一个由纯色几何图形构成的小屋场景。玩家（绿色圆球测试对象）可以在屋内自由行走，碰到灰色墙壁会被阻挡。所有资源使用Unity内置几何图形，**零外部素材导入**，为后续v0.2的角色系统打好基础。

---

## 美术策略说明（本版本及后续）

| 对象类型 | 当前表现 | Unity实现方式 | 后续替换方式 |
|----------|----------|---------------|--------------|
| 墙壁 | 灰色方块 | Tilemap + 纯色瓦片 | 替换瓦片Sprite为正式墙壁素材 |
| 地板 | 浅色方块 | Tilemap + 纯色瓦片 | 替换瓦片Sprite为正式地板素材 |
| 角色（战士/法师/弓箭手） | 不同颜色的圆球 | Sprite Renderer + Circle Sprite + 染色 | 替换为正式角色Sprite + Animator |
| 敌人 | 红色方块 | Sprite Renderer + Square Sprite + 染色 | 替换为正式敌人Sprite + Animator |
| 道具/武器 | 彩色几何图形 | Sprite Renderer + 基础形状 | 替换为正式道具Sprite |

> 💡 **关键优势**：现在搭建的**Tilemap地图结构**、**碰撞系统**、**场景层级**在美术替换时完全不需要改动——只需把纯色瓦片换成正式Sprite即可。

---

## 前置依赖
- [ ] Unity Hub和Unity Editor 2022.3.x 已安装
- [ ] 已克隆/打开本项目 `dilaotanxian_new`
- [ ] 了解Unity基本操作（Hierarchy窗口、Inspector窗口、Scene视图、Project窗口）

---

## 技术选型确认

| 决策项 | 选定方案 | 说明 |
|--------|----------|------|
| 地图绘制 | **Tilemap（纯色瓦片）** | 后续地牢生成的基础，美术替换时只换瓦片Sprite |
| 墙壁碰撞 | **Tilemap Collider 2D + Composite Collider 2D** | 画墙自动产生碰撞 |
| 相机 | **普通Orthographic Camera** | 不需要像素完美，纯色块放大不会模糊 |
| 角色/敌人图形 | **Unity内置Sprite Shape**（Circle/Square） | 零素材，通过Color属性区分对象 |
| PPU | 默认 `100` | 无需调整 |

> ⚠️ **关于为什么保留Tilemap**：即使不用像素美术，Tilemap仍是2D地图最高效的工作方式。后续做随机地牢生成时，Tilemap是唯一可扩展的方案。纯色瓦片 → 正式瓦片的替换只需1秒钟。

---

## 任务清单

### 任务1：创建版本文件夹结构
**目标**：在Project窗口中创建资源文件夹。

**步骤**：
1. 在Project窗口中，右键 `Assets` → `Create` → `Folder`，命名为 `Sprites`
2. 在 `Sprites` 中创建子文件夹 `v0_1`
3. 同样在 `Assets` 下创建文件夹 `Tilemaps`，并在其中创建 `v0_1`
4. 同样在 `Assets` 下创建文件夹 `Palettes`，并在其中创建 `v0_1`
5. 在 `Assets` 下创建文件夹 `Scripts`，并在其中创建 `v0_1`
6. （已有`Scenes`文件夹）保存当前场景：`File` → `Save Scene As...`，选择路径 `Assets/Scenes/`，命名为 `v0_1_InitialHouse`

**验收标准**：
- [ ] Project窗口中可以看到以下结构：
  ```
  Assets/
  ├── Scenes/
  │   └── v0_1_InitialHouse.unity
  ├── Scripts/
  │   └── v0_1/
  ├── Sprites/
  │   └── v0_1/
  ├── Tilemaps/
  │   └── v0_1/
  └── Palettes/
      └── v0_1/
  ```

---

### 任务2：创建纯色瓦片Sprite（白色底图，靠Color染色）
**目标**：创建两张纯色底图，一张用于地板，一张用于墙壁。使用染色技术，一张底图可以染成任意颜色。

**步骤**：
1. 在Project窗口中，右键 `Assets/Sprites/v0_1/` → `Create` → `2D` → `Sprites` → `Square`
   - 这会创建一个名为 `Square` 的白色方形Sprite
2. 将其重命名为 `WhiteSquare`
3. 选中 `WhiteSquare`，在Inspector中确认：
   - **Sprite Mode**: `Single`（默认）
   - **Pixels Per Unit**: `100`（默认，无需修改）
   - **Filter Mode**: 任意（纯色块不存在模糊问题，保持默认即可）
4. 再创建一张同样的白色方块：选中 `WhiteSquare`，按 `Ctrl+D` 复制，重命名为 `WhiteSquare_Wall`
   > 之所以复制两张，是为了在Palette中直观区分（也可只用一张，靠颜色属性区分）

**验收标准**：
- [ ] `Assets/Sprites/v0_1/` 中有两张白色方块Sprite：`WhiteSquare` 和 `WhiteSquare_Wall`

---

### 任务3：创建瓦片(Tile)资源并染色
**目标**：将白色Sprite转换为Tile，并给瓦片染上颜色。

**步骤**：
1. 在Project窗口中，右键 `Assets/Tilemaps/v0_1/` → `Create` → `2D` → `Tiles` → `Rectangular Tile`
2. 命名为 `FloorTile`
3. 选中 `FloorTile`，在Inspector中：
   - 将 `WhiteSquare` Sprite 拖到 **Sprite** 字段
   - 修改 **Color**：R=0.85, G=0.75, B=0.65（浅木色）或直接用颜色选择器选一个浅色
4. 重复步骤1-3，创建 `WallTile`
   - Sprite字段使用 `WhiteSquare_Wall`
   - **Color**：R=0.3, G=0.3, B=0.3（深灰色）

**验收标准**：
- [ ] `Assets/Tilemaps/v0_1/` 中有 `FloorTile.asset` 和 `WallTile.asset`
- [ ] `FloorTile` 为浅色，`WallTile` 为深色
- [ ] 两张瓦片的Sprite字段已正确设置

---

### 任务4：创建Tile Palette（调色板）
**目标**：在Unity编辑器中创建一个可用来"画笔"绘制地图的调色板。

**步骤**：
1. 打开 **Tile Palette** 窗口：`Window` → `2D` → `Tile Palette`
2. 在Tile Palette窗口中，点击 **Create New Palette** 按钮
3. 命名为 `InitialHouse`，保存到 `Assets/Palettes/v0_1/`
4. 从Project窗口中，将 `FloorTile` 和 `WallTile` 拖入Tile Palette窗口
5. 选择调色板中的 `FloorTile`（此时鼠标变成画笔）

**验收标准**：
- [ ] Tile Palette窗口显示 `InitialHouse` 调色板
- [ ] 调色板中有两个瓦片：FloorTile（浅色）和 WallTile（深色）

---

### 任务5：创建Tilemap并绘制小屋
**目标**：在场景中创建网格，绘制一个简单的小屋房间。

**步骤**：
1. 在Hierarchy窗口中，右键 → `2D Object` → `Tilemap` → `Rectangular`
   - 这会创建一个 `Grid` 对象，下面有一个 `Tilemap` 子对象
2. 选中 `Grid` 对象，在Inspector中确认 **Cell Size** 为 X=1, Y=1（默认）
3. 选中 `Tilemap` 子对象，重命名为 `Floor`
4. 在Tile Palette窗口中，确保**Active Tilemap**下拉菜单选择的是 `Floor`
5. 选择 `FloorTile`，用画笔在Scene视图中画一个 **10×8格** 的矩形地板区域
6. 右键 `Grid` → `2D Object` → `Tilemap` → `Rectangular`，创建第二个Tilemap，命名为 `Walls`
7. 在Tile Palette窗口中，将Active Tilemap切换为 `Walls`
8. 选择 `WallTile`，在地板外围画一圈墙壁，形成封闭的房间
   - 建议房间尺寸：内部8×6格为可走区域，外围1格为墙

**绘制参考**（W=墙, .=地板）：
```
WWWWWWWWWW
W........W
W........W
W........W
W........W
W........W
W........W
WWWWWWWWWW
```

**验收标准**：
- [ ] Hierarchy中有 `Grid`，包含 `Floor` 和 `Walls` 两个Tilemap
- [ ] Scene视图中可以看到一个由浅色地板和深色墙壁组成的小屋
- [ ] 墙壁完全封闭房间（没有缺口）

---

### 任务6：设置墙壁碰撞（核心任务）
**目标**：让玩家（以及后续的角色）不能穿过墙壁。

**步骤**：
1. 在Hierarchy中选中 `Walls` Tilemap
2. 在Inspector中，点击 **Add Component**
3. 搜索并添加 **Tilemap Collider 2D**
   - 添加后，Scene视图中每个墙壁瓦片上会出现绿色轮廓线（碰撞形状）
4. 再次点击Add Component，搜索并添加 **Composite Collider 2D**
   - 这会提示你需要Rigidbody2D，点击 **Add Rigidbody2D**
5. 选中自动添加的 **Rigidbody 2D** 组件，修改：
   - **Body Type**: `Static` ← 必须！否则墙壁会受重力掉落
6. 回到 **Tilemap Collider 2D** 组件，勾选 **Used By Composite**
   - 勾选后，相邻墙壁的碰撞会合并成一个大碰撞体，性能更好

> ⚠️ **如果不改Body Type为Static**：运行时墙壁会像物理物体一样掉落出屏幕！

**验收标准**：
- [ ] `Walls` 有3个组件：Tilemap Collider 2D、Composite Collider 2D、Rigidbody 2D (Static)
- [ ] Tilemap Collider 2D的 **Used By Composite** 已勾选
- [ ] Rigidbody 2D的 **Body Type** 为 **Static**
- [ ] Scene视图中墙壁显示绿色碰撞轮廓

---

### 任务7：配置相机
**目标**：调整相机角度和视野，使小屋完整可见。

**步骤**：
1. 在Hierarchy中，选中 `Main Camera`
2. 在Inspector中，确认以下设置：
   - **Projection**: `Orthographic`（2D游戏必须用正交相机）
   - **Clear Flags**: `Solid Color`
   - **Background**: 选一个深色（如 R=0.1, G=0.1, B=0.15），作为小屋外的背景色
3. 调整 **Size**：根据房间大小调整，使Game视图中能看到完整小屋
   - 小屋宽10格 → Size大约设为 `6` 即可
   - 可以在Scene视图中拖动Camera的Z轴位置来调整视野

> 💡 不需要Pixel Perfect Camera！纯色块放大缩小不会模糊，简化了很多配置。

**验收标准**：
- [ ] Camera Projection = Orthographic
- [ ] Game视图中能看到完整的小屋（墙壁+地板）
- [ ] 小屋外有深色背景，形成房间在黑暗空间中的视觉效果

---

### 任务8：创建测试对象（绿色圆球）验证碰撞
**目标**：创建一个临时圆球，给它添加物理，手动拖动验证墙壁碰撞是否工作。

**步骤**：
1. 在Hierarchy中，右键 → `2D Object` → `Sprites` → `Circle`
2. 命名为 `TestPlayer`
3. 在Inspector中，修改Sprite Renderer组件：
   - **Color**: R=0.2, G=0.8, B=0.3（绿色，表示"玩家"）
   - 或选你喜欢的任何颜色，只要和墙壁/地板明显区分
4. 将Transform的Scale设为 (0.6, 0.6, 1) —— 让角色比一格小一点，便于行走
5. 点击Add Component，添加 **Rigidbody 2D**
   - **Gravity Scale**: `0`（俯视角游戏不需要重力）
   - **Constraints** → **Freeze Rotation**: ✅ 勾选（防止碰撞后旋转）
6. 确认已有 **Circle Collider 2D**（创建Circle Sprite时自动附加）

**手动测试碰撞**：
1. 点击工具栏的 **Play** 按钮运行游戏
2. 在Scene视图中（不是Game视图），选中 `TestPlayer`
3. 用鼠标拖动Transform的X/Y箭头，尝试把圆球推向墙壁
4. **期望结果**：圆球被墙壁挡住，不能穿过深色墙壁区域
5. 再次点击Play停止运行

**验收标准**：
- [ ] 运行游戏时，`TestPlayer` 不会掉落（Gravity Scale=0）
- [ ] 手动拖动 `TestPlayer` 时，不能穿过墙壁
- [ ] `TestPlayer` 碰到墙壁后不会旋转（Freeze Rotation生效）

---

### 任务9：最终整理与场景保存
**目标**：清理场景，保存所有工作。

**步骤**：
1. 确认Hierarchy整洁：
   ```
   Main Camera
   Grid
   ├── Floor
   └── Walls
   TestPlayer
   ```
2. 删除场景中不需要的默认对象（如 `SampleScene` 中的其他东西）
3. `File` → `Save Scene`
4. `File` → `Save Project`

**验收标准**：
- [ ] 场景已保存
- [ ] 项目已保存
- [ ] 关闭Unity后重新打开，场景内容完整无误

---

## 完整验收清单

完成所有任务后，确认以下项目全部通过：

- [ ] **场景可见**：运行游戏，Game视图中能看到完整的浅色地板+深色墙壁小屋
- [ ] **碰撞有效**：绿色圆球 `TestPlayer` 无法穿过墙壁，被完全阻挡
- [ ] **无报错**：Console窗口没有任何红色错误（Warning黄色提示可以忽略）
- [ ] **场景整洁**：Hierarchy中对象命名规范，没有多余的默认对象
- [ ] **零外部素材**：项目中没有任何外部导入的图片文件

---

## 常见问题排查

### Q1：墙壁没有碰撞，TestPlayer能直接穿过
**排查步骤**：
1. 确认 `Walls` 有 **Tilemap Collider 2D** 组件
2. 确认 `Walls` 的Rigidbody 2D是 **Static**
3. 确认 `TestPlayer` 有 **Rigidbody 2D** 和 **Circle Collider 2D**
4. 确认 `TestPlayer` 的Rigidbody不是Kinematic（除非你自己写代码控制）

### Q2：Tile Palette里不显示我的瓦片
**排查步骤**：
1. 确认你已经创建了Palette（不是只打开了窗口）
2. 从Project窗口拖入的是 `.asset` 文件（Tile），不是 `.png` 文件（Sprite）

### Q3：运行时墙壁掉落了
**排查步骤**：
1. 确认 `Walls` 的Rigidbody 2D的 **Body Type** 是 `Static`
2. 确认 `Walls` 的Tilemap Collider 2D的 **Used By Composite** 已勾选

### Q4：地板和墙壁颜色区分不明显
**排查步骤**：
1. 选中 `FloorTile` asset，在Inspector中把Color调得更浅
2. 选中 `WallTile` asset，在Inspector中把Color调得更深
3. 或者给墙壁加一点点蓝色/绿色色调，使其与暖色地板形成冷暖对比

---

## v0.1 → v0.2 衔接说明

v0.1完成后，你将拥有一个可碰撞的小屋场景。v0.2将在此基础上：
- 给 `TestPlayer` 添加WASD移动脚本（绿色圆球变成"战士"）
- 通过Color区分不同职业（战士=绿色，法师=蓝色，弓箭手=黄色）
- 小屋将作为战士的"出生点"和"准备区域"
- 地图结构完全不需要改动

因此v0.1的重点是**Tilemap工作流和碰撞系统的正确搭建**，而不是视觉效果。

---

## 文档变更记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2025-07-09 | v0.1 | 初始版本（像素美术版） |
| 2025-07-09 | v0.1-r2 | 重写为程序员美术版（纯色几何图形） |

