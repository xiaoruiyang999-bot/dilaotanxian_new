# v0.5.4.4.1 远程攻击执行链解耦计划

## 1. 修订原因

人工体验发现 Ranged 虽然使用 Projectile 数据，但攻击表现与执行仍经过近战 `WeaponController / WeaponAnimator / WeaponPivot` 模板，造成远程攻击缺乏独立发射表现，并存在近战挥动语义残留。

本版本是 v0.5.4.4 的阻断级修订。在本修订验收完成前，v0.5.4.4 不标记全部完成。

## 2. 目标

- Projectile 攻击由独立 `ProjectileEmitter` 执行。
- 发射点、发射方向、Projectile 创建和参数注入不依赖近战 WeaponPivot。
- Projectile Windup 保留方向预警，但不播放近战挥动动画。
- Projectile 不启动 `WeaponHitbox`。
- 没有 `WeaponController` 时，Ranged 仍可发射。
- Melee、Charge、Summon 保持原有执行方式。

## 3. 架构

```text
EnemyCombat（阶段编排）
├─ Melee → WeaponController + WeaponAnimator + WeaponHitbox
├─ Projectile → ProjectileEmitter
├─ Charge → EnemyController
└─ Summon → SummonMinions
```

`EnemyCombat` 负责 Windup/Active/Recovery、预警、取消与冷却；具体 Projectile 创建下沉到 `ProjectileEmitter`。

## 4. ProjectileEmitter 职责

- 保存可选 `muzzle` 发射点。
- 无 muzzle 时从敌人中心沿锁定方向使用安全偏移。
- 实例化 `AttackData.ProjectilePrefab`。
- 调用 `Projectile.Launch()` 注入伤害、速度、目标层、障碍层和发射者。
- 配置错误时返回失败并输出明确 Warning。
- 不管理攻击冷却、AI、LOS、预警或房间状态。

## 5. 分阶段实施

### 阶段 A：执行器分流（已完成）

- 新增 `ProjectileEmitter`。
- `EnemyCombat` 的 Projectile Active 分支调用 Emitter。
- Projectile 分支跳过 WeaponController 锁向、WeaponHitbox 和 WeaponAnimator。

### 阶段 B：Prefab 与发射点（已完成）

- 基础 Enemy Prefab 添加 `ProjectileEmitter`。
- Ranged 可使用 `ProjectileMuzzle` 子节点；缺失时保留安全偏移回退。
- 其他 Variant 继承组件但不会在非 Projectile 攻击中调用。

### 阶段 C：专项与回归（已完成自动验证）

- Ranged 在禁用 WeaponController/WeaponAnimator 后仍能生成 Projectile。
- 发射位置来自 muzzle 或回退偏移。
- 预警方向与 Projectile 速度方向一致。
- Melee 仍启动 Hitbox；Charger 仍位移；Summoner 仍生成 Minion。
- 死亡、LOS 取消、清房和门状态回归通过。

## 6. 验收标准

| 场景 | 通过标准 |
|---|---|
| Ranged 正常射击 | 生成一枚 Projectile |
| 禁用近战工具组件 | Ranged 仍能射击 |
| Projectile Active | 不调用 WeaponHitbox，不播放 WeaponAnimator |
| LOS 取消 | 不生成 Projectile，预警清理 |
| Melee 回归 | 近战工具链不受影响 |
| Summoner 回归 | Minion 上限和清房不受影响 |
| 完整地牢 | Console 0 Error/Warning，全灭开门 |

## 7. 当前状态（2026-08-14）

- 新增独立 `ProjectileEmitter`，Projectile 创建与参数注入已从 `EnemyCombat` 下沉。
- 基础 Enemy Prefab 已配置 `ProjectileMuzzle`，全部 Variant 继承；仍保留安全偏移回退。
- Projectile 分支已跳过 WeaponController 锁向、WeaponHitbox 和 WeaponAnimator。
- 禁用三种近战工具后，Ranged 仍能生成一枚 Projectile，发射位置和方向验证通过。
- Melee 仍进入 WeaponHitbox 挥击；Summoner 仍一次生成 2 只 Minion。
- 完整地牢回归：8 个战斗类房间 Cleared 失败 0，10 扇门仍关闭 0。
- 项目代码 Console：0 Error；出现 1 条 MCP WebSocket 未初始化的编辑器插件警告，与游戏代码无关。
- 自动验证完成；下一项为恢复 v0.5.4.4 的人工远程手感验收。

## 8. 运行时缺陷修订（2026-08-14）

人工体验发现“已进入 Projectile 分支”不等于弹体在真实物理环境中可用，补充修复如下：

- 根因：Projectile Prefab 使用 Kinematic `Rigidbody2D`，当前 Unity 6 项目配置下仅设置 `linearVelocity` 后速度字段有值，但位置没有推进。
- 修复：Projectile 在 `FixedUpdate` 中使用 `Rigidbody2D.MovePosition` 显式推进，碰撞仍由 2D Trigger 负责。
- `AttackData_Ranged.attackRange` 从 1.5 修正为 8，避免远程攻击继续显示为近战尺度。
- `targetLayer` 从 `Default + Obstacle` 收敛为 `Default`；`obstacleLayer` 从错误的未命名 Layer 8 修正为实际 `Obstacle` Layer 7。
- Projectile 提升渲染排序、改为方向性弹体比例，并自动配置蓝色 TrailRenderer，保证 QHD Game View 下可识别。
- 命中层判断同时检查 Collider 与根节点 Layer，兼容 Player 子碰撞体未同步 Layer 的情况。
- Unity 手动物理步进验证：弹体从发射点推进并命中测试目标，HP `5 → 4`，Console 0 Error。

### 8.1 人工截图复核后的二次修订

- 截图中的白色长条确认为 Projectile Windup 的路线预警，不是弹体拖尾。
- 当时 Editor 实际处于 `isPlaying=true / isPaused=true`，Windup 被暂停，因此预警冻结且不会进入发射阶段。
- 新增连续 CircleCast 后发现其会命中 Projectile 自身 Collider，导致弹体第一帧自毁；现已明确排除自身与发射者全部 Collider。
- Projectile 改为 Transform 确定性推进，CircleCast 负责高速连续命中，不再依赖 Kinematic Rigidbody2D 的速度/MovePosition 更新模式。
- 弹体排序提高到 220，高于预警 Mesh 的 100；拖尾排序 219，拖尾时间收敛至 0.1 秒，起始宽度 0.14。
- Projectile 发射前立即隐藏预警，防止预警面覆盖弹体。
- 新增世界空间 `DamagePopup`：命中显示红色伤害数字，持续 0.55 秒并向上淡出。
- 最终专项测试：`HP 5 → 4`、Projectile consumed、DamagePopup created，Console 0 Error。

## 9. 开源实现复核与运动模型收敛

参考 Unity-Technologies/PhysicsExamples2D 以及开源 2D Shooter 的 Projectile/Travel 实现后，确认此前同时混用 `linearVelocity`、`MovePosition`、Transform、Trigger 和 CircleCast 属于过度复杂实现。

最终约束为：

- `Projectile.Update()` 是唯一时间入口。
- `Advance(deltaTime)` 是唯一运动入口，在同一方法中完成 CircleCast 与 Transform 推进。
- Rigidbody2D 不再负责运动，仅保留 Collider Trigger 的兼容回调。
- `consumed` 是单次命中/回收真源，避免 Trigger 与扫掠重复结算。
- Projectile 自身及发射者 Collider 必须在所有命中路径中过滤。
- 生命周期、视觉拖尾与伤害反馈不参与攻击状态机。

真实资源整链测试使用 `Enemy_Ranged.prefab` 和 `AttackData_Ranged`，结果为：

`TryStartAttack=True → ProjectileCreated=True → Target HP 5→4 → DamagePopup=True`
