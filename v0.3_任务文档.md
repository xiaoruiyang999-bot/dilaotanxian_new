# v0.3 任务文档：角色系统基础架构（正式版）

## 版本信息
- **版本号**：v0.3
- **版本名称**：角色系统基础架构
- **美术策略**：程序员美术（纯色几何图形）
- **目标**：重构v0.2为模块化组件架构，实现扇形近战攻击+测试敌人+护甲自动恢复
- **前置版本**：v0.2（移动+相机+UI已完成，Input System已配置）

---

## 项目当前状态（v0.2完成度验证）

### ✅ 已完成的v0.2资产（保留复用）
| 资产 | 路径 | 处理方式 |
|------|------|----------|
| 场景 | `Assets/Scenes/v0_2_Warrior.unity` | 复制为v0.3基础 |
| Input Actions | `Assets/PlayerInputActions.inputactions` | 保留，添加Attack Action |
| 相机脚本 | `Scripts/v0_2/CameraFollow.cs` | **保留不变** |
| 小屋Tilemap | v0.1场景中的Grid/Floor/Walls | 保留复用 |
| 素材 | `Sprites/v0_1/WhiteCircle.asset` | 保留复用 |
| UI Canvas | v0.2场景中的Canvas+PlayerStatsPanel | 保留，替换PlayerUI脚本 |

### ❌ 将被删除/替换的v0.2资产
| 资产 | 路径 | 处理方式 |
|------|------|----------|
| WarriorCharacter.cs | `Scripts/v0_2/WarriorCharacter.cs` | **删除**（替换为PlayerController+PlayerStats+Health+PlayerCombat） |
| CharacterInput.cs | `Scripts/v0_2/CharacterInput.cs` | **删除**（功能合并到PlayerController中） |
| PlayerUI.cs | `Scripts/v0_2/PlayerUI.cs` | **删除**（替换为v0.3版本） |
| 脚本文件夹 | `Scripts/v0_2/` | 保留，但组件需从场景对象上移除 |

> ⚠️ **Kimi Code执行前必读**：本版本是**重构**，不是从零新建。核心工作是在v0.2的Player对象上移除旧组件，挂载新组件，并保留场景中的地图、相机、UI画布。

---

## 总体目标

将v0.2的 `WarriorCharacter`（单体脚本）拆分为**模块化组件架构**，同时实现：

1. **PlayerController**（门面）：接收输入、驱动移动、鼠标瞄准、持有所有子组件引用
2. **PlayerStats**（属性）：HP/护甲/移速配置，**脱战3秒后护甲自动恢复**
3. **Health**（生命）：HP变化、受伤/死亡事件，**HP不会自动恢复**
4. **PlayerCombat**（攻击管理）：冷却计时、触发攻击、调用职业攻击
5. **WarriorAttack**（战士攻击）：**前方扇形OverlapCircle判定**，攻击时生成**半透明红色圆形**指示范围
6. **测试敌人**：红色方块，挂载**Health** + **WorldSpaceHealthBar**（世界空间血条），可受伤、闪烁反馈、死亡销毁
7. **PlayerUI**：**玩家固定UI**（屏幕左下角），适配新架构，监听Health和PlayerStats
8. **WorldSpaceHealthBar**：敌人头顶的**世界空间血条**，跟随敌人移动

---

## 架构设计

### 组件依赖图

```
Player (GameObject)
├── SpriteRenderer (绿色圆球)
├── Rigidbody2D (Dynamic, Gravity=0, FreezeRotation)
├── CircleCollider2D
├── PlayerInput (Input System, Invoke Unity Events)
│
├── PlayerController ───────┐
│   • OnMove / OnAttack     │ 门面持有引用
│   • 鼠标瞄准旋转          │
│   • rb.velocity移动       │
│                           ▼
├── PlayerStats ←───────────┤
│   • maxHP/Armor/Speed     │ 提供属性
│   • 护甲自动恢复          │
│   • OnStatsChanged        │
│                           ▼
├── Health ←────────────────┤
│   • TakeDamage / Heal   │ 生命管理
│   • OnHealthChanged     │
│   • OnDeath             │
│                           ▼
├── PlayerCombat ←──────────┤
│   • TryAttack           │ 攻击管理
│   • 冷却计时            │
│   • OnAttackStart/End   │
│                           ▼
└── Profession (子对象)
    ├── Transform (0,0,0)
    └── WarriorAttack
        • Execute() ──→ 扇形Overlap检测
        • 半透明红色圆形指示器
        • 对IDamageable造成伤害
```

### 前方扇形攻击设计

```
              鼠标指向（角色面向方向）
                    ↑
                    │
    ════════════════●════════════════  ← 攻击范围 = 1.5
          ╲         │         ╱
            ╲       │       ╱
              ╲     │     ╱
                ╲   │   ╱
                  ╲ │ ╱
                    ●     ← 角色位置
                  扇形角度 = 90度
```

**判定逻辑**：
1. 先检测角色周围 `attackRange` 圆形内的所有敌人
2. 对每个敌人，计算 `角色→敌人` 向量与 `角色面向方向` 的夹角
3. 夹角 ≤ `attackAngle/2`（半角）时判定命中
4. 命中敌人调用 `TakeDamage(attackDamage)`

---

## 技术选型确认

| 决策项 | 选定方案 | 说明 |
|--------|----------|------|
| 攻击判定 | **OverlapCircleAll + 角度过滤** | 先圆形筛选，再扇形过滤，性能与精确兼顾 |
| 扇形参数 | **attackRange=1.5, attackAngle=90°** | 前方90度扇形 |
| 攻击指示器 | **动态SpriteRenderer圆形** | 半透明红色，持续0.2秒，攻击瞬间显示 |
| 护甲恢复 | **PlayerStats.Update中，脱战3秒后恢复** | 受伤时调用`OnTakeDamage()`重置计时，rate=0.5/秒，上限=MaxArmor |
| 鼠标瞄准 | **PlayerController.Update中旋转** | 计算鼠标角度，设置transform.rotation (Z轴) |
| 死亡处理 | **Health.OnDeath → PlayerController停止移动+攻击** | 角色变灰，rb.velocity=0，禁用攻击 |

---

## 任务清单

---

### 任务0：创建v0.3场景并清理旧组件
**目标**：从v0.2复制新场景，移除旧脚本，建立新代码文件夹。

**步骤**：
1. 打开Unity，加载 `v0_2_Warrior.unity`
2. 确认Hierarchy中现有对象：
   - `Main Camera`（含CameraFollow）✅保留
   - `Global Light 2D` ✅保留
   - `Grid` → `Floor` + `Walls` ✅保留
   - `Warrior`（含WarriorCharacter + CharacterInput + PlayerInput等）→ **将重构**
   - `Canvas` → `PlayerStatsPanel` + `EventSystem` ✅保留
3. 复制场景：在Project窗口中，`Ctrl+D` 复制 `v0_2_Warrior.unity`，重命名为 `v0_3_CharacterSystem.unity`
4. 双击打开 `v0_3_CharacterSystem.unity`
5. 创建文件夹：`Assets/Scripts/v0_3/`、`Assets/Scripts/v0_3/Interfaces/`
6. 在Hierarchy中，选中 `Warrior`：
   - **移除** `WarriorCharacter` 组件（右键 → Remove Component）
   - **移除** `CharacterInput` 组件（右键 → Remove Component）
   - 保留：SpriteRenderer、Rigidbody2D、CircleCollider2D、Player Input
7. 将 `Warrior` 重命名为 `Player`（推荐）
8. 在Hierarchy中，选中 `Canvas`：
   - **移除** 旧的 `PlayerUI` 组件（v0.2版本）
9. 保存场景：`Ctrl+S`

**验收标准**：
- [ ] `v0_3_CharacterSystem.unity` 已创建并打开
- [ ] `Assets/Scripts/v0_3/` 和 `Interfaces/` 文件夹存在
- [ ] `Player`（原Warrior）上已移除WarriorCharacter和CharacterInput
- [ ] `Canvas` 上已移除旧PlayerUI
- [ ] Console无红色报错

---

### 任务1：在Input Actions中添加Attack Action
**目标**：为攻击输入添加鼠标左键绑定。

**步骤**：
1. 在Project窗口中，双击 `PlayerInputActions` 打开Input Actions编辑器
2. 在 `Player` Action Map中，点击 `+` 添加新Action
3. 命名为 `Attack`
4. 设置：Action Type = `Button`
5. 在Bindings中点击 `+` → `Listen` → 按鼠标左键 → 自动识别为 `Mouse/leftButton`
6. 保存资产（Ctrl+S）
7. 关闭编辑器

**验收标准**：
- [ ] `Player` Action Map中有 `Move`（Vector2）和 `Attack`（Button）两个Action
- [ ] `Attack` 绑定到 `Mouse/leftButton`

---

### 任务2：编写接口层（IAttack + IDamageable）
**目标**：定义攻击和受伤的统一接口。

**文件**：`Assets/Scripts/v0_3/Interfaces/IAttack.cs`
```csharp
using UnityEngine;

public interface IAttack
{
    float AttackRange { get; }
    float AttackDamage { get; }
    void Execute();
}
```

**文件**：`Assets/Scripts/v0_3/Interfaces/IDamageable.cs`
```csharp
public interface IDamageable
{
    void TakeDamage(float damage);
}
```

**创建步骤**：
1. 创建文件夹 `Assets/Scripts/v0_3/Interfaces/`
2. 创建两个脚本，替换为上述代码

**验收标准**：
- [ ] 两个接口文件存在，编译无报错

---

### 任务3：编写PlayerStats.cs（属性 + 护甲自动恢复）
**目标**：集中管理属性，**护甲随时间自动恢复**。

**文件**：`Assets/Scripts/v0_3/PlayerStats.cs`
```csharp
using UnityEngine;

public class PlayerStats : MonoBehaviour
{
    [Header("基础属性")]
    [SerializeField] private float maxHP = 5f;
    [SerializeField] private float maxArmor = 5f;
    [SerializeField] private float moveSpeed = 5f;

    [Header("护甲恢复")]
    [SerializeField] private float armorRegenRate = 0.5f;       // 每秒恢复0.5点
    [SerializeField] private float armorRegenDelay = 3f;        // 脱战3秒后开始恢复
    [SerializeField] private float armorRegenCooldown = 1f;    // 每次恢复1点护甲的间隔（可选，当前每帧平滑恢复）

    private float lastDamageTime = -999f;  // 上次受伤时间（负值表示开局未受伤）
    private bool isOutOfCombat => Time.time - lastDamageTime >= armorRegenDelay;

    void Awake()
    {
        CurrentArmor = maxArmor;
    }

    void Update()
    {
        // 脱战3秒后，护甲开始自动恢复
        if (CurrentArmor < maxArmor && isOutOfCombat)
        {
            CurrentArmor = Mathf.Min(CurrentArmor + armorRegenRate * Time.deltaTime, maxArmor);
            OnStatsChanged?.Invoke();
        }
    }

    /// <summary>
    /// 标记受到伤害（重置脱战计时器）
    /// </summary>
    public void OnTakeDamage()
    {
        lastDamageTime = Time.time;
    }

    public void ModifyArmor(float delta)
    {
        CurrentArmor = Mathf.Clamp(CurrentArmor + delta, 0, maxArmor);
        OnStatsChanged?.Invoke();
    }
}
```

**创建步骤**：右键 `Scripts/v0_3/` → `C# Script` → `PlayerStats` → 替换代码 → 保存

**验收标准**：
- [ ] `PlayerStats.cs` 存在，编译无报错
- [ ] `CurrentArmor` 为公开只读属性
- [ ] `Update` 中护甲自动恢复（rate=0.5/秒）
- [ ] 包含 `OnStatsChanged` 事件和 `ModifyArmor` 方法

---

### 任务4：编写Health.cs（生命管理，HP不自动恢复）
**目标**：管理HP变化，**HP不会自动恢复**，死亡时触发事件。

**文件**：`Assets/Scripts/v0_3/Health.cs`
```csharp
using UnityEngine;

public class Health : MonoBehaviour, IDamageable
{
    [SerializeField] private float maxHealth = 5f;

    public float CurrentHealth { get; private set; }
    public float MaxHealth => maxHealth;
    public float HealthRatio => maxHealth > 0 ? CurrentHealth / maxHealth : 0f;
    public bool IsDead { get; private set; }

    public System.Action<float, float> OnHealthChanged;
    public System.Action OnDeath;

    void Awake()
    {
        CurrentHealth = maxHealth;
        IsDead = false;
    }

    public void TakeDamage(float damage)
    {
        if (IsDead) return;
        CurrentHealth = Mathf.Max(CurrentHealth - damage, 0f);
        OnHealthChanged?.Invoke(CurrentHealth, maxHealth);
        
        // 通知PlayerStats重置脱战计时
        if (TryGetComponent<PlayerStats>(out var stats))
            stats.OnTakeDamage();
        
        if (CurrentHealth <= 0f) Die();
    }

    public void Heal(float amount)
    {
        if (IsDead) return;
        CurrentHealth = Mathf.Min(CurrentHealth + amount, maxHealth);
        OnHealthChanged?.Invoke(CurrentHealth, maxHealth);
    }

    public void ResetHealth()
    {
        CurrentHealth = maxHealth;
        IsDead = false;
        OnHealthChanged?.Invoke(CurrentHealth, maxHealth);
    }

    private void Die()
    {
        IsDead = true;
        OnDeath?.Invoke();
    }
}
```

**验收标准**：
- [ ] `Health.cs` 存在，实现 `IDamageable`
- [ ] `TakeDamage` 减少HP，触发 `OnHealthChanged` 和 `OnDeath`
- [ ] `Heal` 增加HP，但不超过上限
- [ ] 无自动恢复逻辑（HP不会自己涨）

---

### 任务5：编写WarriorAttack.cs（扇形攻击 + 红色指示器）
**目标**：实现前方扇形攻击，攻击时显示**半透明红色圆形**指示范围。

**文件**：`Assets/Scripts/v0_3/WarriorAttack.cs`
```csharp
using System.Collections;
using UnityEngine;

public class WarriorAttack : MonoBehaviour, IAttack
{
    [Header("攻击属性")]
    [SerializeField] private float attackRange = 1.5f;
    [SerializeField] private float attackDamage = 1f;
    [SerializeField] private float attackAngle = 90f; // 扇形角度（全角）
    [SerializeField] private LayerMask enemyLayer;

    [Header("视觉指示器")]
    [SerializeField] private Sprite attackIndicatorSprite; // 可选：引用WhiteCircle.asset
    [SerializeField] private float indicatorDuration = 0.2f;

    public float AttackRange => attackRange;
    public float AttackDamage => attackDamage;

    void Awake()
    {
        if (enemyLayer == 0)
            enemyLayer = LayerMask.GetMask("Enemy");
    }

    public void Execute()
    {
        ShowAttackIndicator();
        PerformFanAttack();
    }

    private void PerformFanAttack()
    {
        // 1. 获取圆形范围内所有敌人
        Collider2D[] hits = Physics2D.OverlapCircleAll(
            transform.position, attackRange, enemyLayer);

        // 2. 获取角色面向方向（父对象Player的transform.right）
        Vector2 facingDir = transform.parent != null 
            ? transform.parent.right 
            : Vector2.right;

        float halfAngle = attackAngle * 0.5f;

        foreach (Collider2D hit in hits)
        {
            // 3. 计算敌人与角色面向方向的夹角
            Vector2 toEnemy = (hit.transform.position - transform.position).normalized;
            float angle = Vector2.Angle(facingDir, toEnemy);

            // 4. 夹角在扇形半角内则命中
            if (angle <= halfAngle)
            {
                if (hit.TryGetComponent<IDamageable>(out IDamageable dmg))
                {
                    dmg.TakeDamage(attackDamage);
                }
            }
        }
    }

    private void ShowAttackIndicator()
    {
        // 创建攻击范围指示器（半透明红色圆形）
        GameObject indicator = new GameObject("AttackIndicator");
        indicator.transform.position = transform.position;
        indicator.transform.SetParent(transform);

        SpriteRenderer sr = indicator.AddComponent<SpriteRenderer>();
        
        // 使用WhiteCircle素材或默认Circle
        sr.sprite = attackIndicatorSprite != null 
            ? attackIndicatorSprite 
            : Resources.GetBuiltinResource<Sprite>("UI/Sprite/UISprite"); // 备选
        
        sr.color = new Color(1f, 0f, 0f, 0.3f); // 红色半透明
        
        // Circle Sprite直径=1，所以scale = range * 2
        indicator.transform.localScale = new Vector3(attackRange * 2f, attackRange * 2f, 1f);
        sr.sortingOrder = 5; // 确保在角色和敌人之上显示

        // 短暂显示后销毁
        StartCoroutine(DestroyIndicator(indicator, indicatorDuration));
    }

    private IEnumerator DestroyIndicator(GameObject indicator, float delay)
    {
        yield return new WaitForSeconds(delay);
        if (indicator != null) Destroy(indicator);
    }

    void OnDrawGizmosSelected()
    {
        // 在Scene视图中显示扇形攻击范围
        Vector2 origin = transform.position;
        Vector2 facingDir = transform.parent != null ? transform.parent.right : Vector2.right;
        float halfAngle = attackAngle * 0.5f;

        Gizmos.color = new Color(1f, 0f, 0f, 0.3f);
        Gizmos.DrawWireSphere(origin, attackRange);

        // 绘制扇形边界线
        Vector2 leftDir = Quaternion.Euler(0, 0, halfAngle) * facingDir;
        Vector2 rightDir = Quaternion.Euler(0, 0, -halfAngle) * facingDir;
        Gizmos.color = Color.red;
        Gizmos.DrawLine(origin, origin + leftDir * attackRange);
        Gizmos.DrawLine(origin, origin + rightDir * attackRange);
    }
}
```

> 💡 关于 `attackIndicatorSprite`：如果引用v0.1的 `WhiteCircle.asset` 不方便，可以在代码中创建临时Texture2D生成圆形。但v0.1已有WhiteCircle.asset，直接从Project窗口拖到Inspector字段即可。

**创建步骤**：
1. 右键 `Scripts/v0_3/` → `C# Script` → `WarriorAttack` → 替换代码
2. 保存后，从Project窗口将 `WhiteCircle.asset` 拖到 `attackIndicatorSprite` 字段（如需要）

**验收标准**：
- [ ] `WarriorAttack.cs` 存在，实现 `IAttack`
- [ ] `Execute()` 包含 `OverlapCircleAll` + `Vector2.Angle` 扇形过滤
- [ ] `ShowAttackIndicator()` 创建红色半透明圆形，持续0.2秒后销毁
- [ ] `OnDrawGizmosSelected` 显示扇形范围（红色扇形边界）

---

### 任务6：编写PlayerCombat.cs（攻击管理）
**目标**：管理攻击冷却和触发，与v0.3草稿保持一致。

**文件**：`Assets/Scripts/v0_3/PlayerCombat.cs`
```csharp
using UnityEngine;

public class PlayerCombat : MonoBehaviour
{
    [SerializeField] private float attackCooldown = 0.5f;

    private float cooldownTimer = 0f;
    private bool canAttack = true;
    private IAttack currentAttack;

    public System.Action OnAttackStart;
    public System.Action OnAttackEnd;

    void Awake()
    {
        currentAttack = GetComponentInChildren<IAttack>();
        if (currentAttack == null)
            Debug.LogWarning("[PlayerCombat] 未找到IAttack实现！");
    }

    void Update()
    {
        if (!canAttack)
        {
            cooldownTimer -= Time.deltaTime;
            if (cooldownTimer <= 0f) canAttack = true;
        }
    }

    public void TryAttack()
    {
        if (!canAttack) return;
        if (currentAttack == null) return;

        OnAttackStart?.Invoke();
        currentAttack.Execute();
        OnAttackEnd?.Invoke();

        canAttack = false;
        cooldownTimer = attackCooldown;
    }

    public void SetAttack(IAttack newAttack) => currentAttack = newAttack;
}
```

**验收标准**：
- [ ] `PlayerCombat.cs` 存在，编译无报错
- [ ] `Awake` 自动查找 `GetComponentInChildren<IAttack>()`
- [ ] `TryAttack()` 包含冷却检查 + 攻击触发 + 冷却重置
- [ ] `SetAttack(IAttack)` 接口保留（后续切换职业用）

---

### 任务7：编写PlayerController.cs（门面 + 鼠标瞄准）
**目标**：整合所有子组件，处理输入、移动、鼠标瞄准。替换v0.2的WarriorCharacter+CharacterInput。

**文件**：`Assets/Scripts/v0_3/PlayerController.cs`
```csharp
using UnityEngine;
using UnityEngine.InputSystem;

[RequireComponent(typeof(Rigidbody2D))]
[RequireComponent(typeof(PlayerInput))]
[RequireComponent(typeof(PlayerStats))]
[RequireComponent(typeof(Health))]
[RequireComponent(typeof(PlayerCombat))]
public class PlayerController : MonoBehaviour
{
    // 子组件
    private Rigidbody2D rb;
    private PlayerStats stats;
    private Health health;
    private PlayerCombat combat;

    // 输入
    private Vector2 moveInput;

    void Awake()
    {
        rb = GetComponent<Rigidbody2D>();
        stats = GetComponent<PlayerStats>();
        health = GetComponent<Health>();
        combat = GetComponent<PlayerCombat>();

        // 监听死亡事件
        health.OnDeath += OnPlayerDeath;
    }

    void OnDestroy()
    {
        if (health != null) health.OnDeath -= OnPlayerDeath;
    }

    // ========== Input System 回调 ==========

    public void OnMove(InputAction.CallbackContext context)
    {
        moveInput = context.ReadValue<Vector2>();
    }

    public void OnAttack(InputAction.CallbackContext context)
    {
        if (context.performed && !health.IsDead)
        {
            combat.TryAttack();
        }
    }

    // ========== 更新循环 ==========

    void Update()
    {
        if (!health.IsDead)
        {
            HandleMouseAiming();
        }
    }

    void FixedUpdate()
    {
        if (health.IsDead)
        {
            rb.velocity = Vector2.zero;
            return;
        }

        rb.velocity = moveInput.normalized * stats.MoveSpeed;
    }

    // ========== 鼠标瞄准 ==========

    private void HandleMouseAiming()
    {
        Vector3 mouseWorldPos = Camera.main.ScreenToWorldPoint(Input.mousePosition);
        Vector2 direction = mouseWorldPos - transform.position;
        float angle = Mathf.Atan2(direction.y, direction.x) * Mathf.Rad2Deg;
        transform.rotation = Quaternion.Euler(0, 0, angle);
    }

    // ========== 死亡处理 ==========

    private void OnPlayerDeath()
    {
        rb.velocity = Vector2.zero;
        // 变灰表现
        if (TryGetComponent<SpriteRenderer>(out var sr))
            sr.color = new Color(0.3f, 0.3f, 0.3f, 1f);
    }

    void OnDisable()
    {
        rb.velocity = Vector2.zero;
    }

    // 外部访问接口
    public PlayerStats GetStats() => stats;
    public Health GetHealth() => health;
    public PlayerCombat GetCombat() => combat;

    public void TakeDamage(float damage) => health.TakeDamage(damage);
}
```

**创建步骤**：
1. 右键 `Scripts/v0_3/` → `C# Script` → `PlayerController` → 替换代码
2. 保存

**验收标准**：
- [ ] `PlayerController.cs` 存在，编译无报错
- [ ] `Awake` 获取所有子组件 + 监听 `health.OnDeath`
- [ ] `OnMove` / `OnAttack` 输入回调
- [ ] `Update` 中 `HandleMouseAiming` 使角色面向鼠标
- [ ] `FixedUpdate` 中死亡时停止移动
- [ ] `OnPlayerDeath` 中SpriteRenderer变灰

---

### 任务8：配置Player GameObject
**目标**：在Player对象上挂载所有新组件，创建Profession子对象，重新绑定输入事件。

**步骤**：
1. 选中Hierarchy中的 `Player`（原Warrior）
2. 确认已移除：WarriorCharacter、CharacterInput
3. 挂载新组件：
   - `PlayerController.cs` → 拖到Player上
   - `PlayerStats.cs` → 拖到Player上
   - `Health.cs` → 拖到Player上
   - `PlayerCombat.cs` → 拖到Player上
4. 创建子对象：
   - 右键 `Player` → `Create Empty` → 命名 `Profession`
   - `Profession` 的Transform本地坐标设为 (0, 0, 0)
   - 将 `WarriorAttack.cs` 拖到 `Profession` 上
5. 绑定Player Input事件：
   - 选中 `Player`，找到Player Input组件
   - **Move** 事件：绑定 `Player` → `PlayerController.OnMove`
   - **Attack** 事件：绑定 `Player` → `PlayerController.OnAttack`
   - 删除旧绑定（如果有）
6. 配置WarriorAttack的 `attackIndicatorSprite`：
   - 将 `Assets/Sprites/v0_1/WhiteCircle.asset` 拖到 `Attack Indicator Sprite` 字段

**Player组件最终状态**：
```
Player
  - SpriteRenderer (绿色圆球)
  - Rigidbody2D (Dynamic, Gravity=0, FreezeRotation)
  - CircleCollider2D
  - PlayerInput (Actions=PlayerInputActions, Behavior=Invoke Unity Events)
  - PlayerController
  - PlayerStats (maxHP=5, maxArmor=5, moveSpeed=5, armorRegenRate=0.5)
  - Health (maxHealth=5)
  - PlayerCombat (attackCooldown=0.5)
  - Profession (子对象)
      - WarriorAttack (attackRange=1.5, attackDamage=1, attackAngle=90)
```

**验收标准**：
- [ ] Player上组件：PlayerController + PlayerStats + Health + PlayerCombat
- [ ] Player下子对象：Profession（本地坐标0,0,0）
- [ ] Profession上挂载：WarriorAttack
- [ ] Player Input的Move → `PlayerController.OnMove`
- [ ] Player Input的Attack → `PlayerController.OnAttack`

---

### 任务9：创建测试敌人 + 世界空间血条
**目标**：创建红色方块敌人，挂载Health组件和**世界空间Canvas血条**（跟随敌人头顶），可受伤、闪烁、死亡。

#### 9A. 编写WorldSpaceHealthBar.cs
**目标**：创建可复用的世界空间血条组件，挂载在需要血条的物体上。

**文件**：`Assets/Scripts/v0_3/WorldSpaceHealthBar.cs`
```csharp
using UnityEngine;
using UnityEngine.UI;

/// <summary>
/// 世界空间血条。挂载在敌人（或任何有Health的对象）上，在头顶显示血条。
/// </summary>
[RequireComponent(typeof(Health))]
public class WorldSpaceHealthBar : MonoBehaviour
{
    [Header("血条UI预制")]
    [SerializeField] private Sprite backgroundSprite;   // 可选：WhiteSquare.asset
    [SerializeField] private Color backgroundColor = new Color(0.2f, 0.2f, 0.2f, 1f); // 深灰
    [SerializeField] private Color fillColor = new Color(0.9f, 0.1f, 0.1f, 1f);       // 红色

    [Header("血条尺寸")]
    [SerializeField] private float barWidth = 0.8f;
    [SerializeField] private float barHeight = 0.12f;
    [SerializeField] private float yOffset = 0.6f; // 血条在敌人头顶上方

    private Health health;
    private Transform canvasTransform;
    private Image fillImage;
    private Camera mainCamera;

    void Awake()
    {
        health = GetComponent<Health>();
        mainCamera = Camera.main;
        CreateHealthBar();
    }

    void OnEnable()
    {
        if (health != null)
            health.OnHealthChanged += OnHealthChanged;
    }

    void OnDisable()
    {
        if (health != null)
            health.OnHealthChanged -= OnHealthChanged;
    }

    void LateUpdate()
    {
        // 血条位置跟随父对象头顶
        if (canvasTransform != null)
        {
            canvasTransform.position = transform.position + Vector3.up * yOffset;
            // 让血条始终面向相机（2D中可省略，但保持水平）
        }
    }

    private void CreateHealthBar()
    {
        // 创建世界空间Canvas
        GameObject canvasGo = new GameObject("HealthBarCanvas");
        canvasGo.transform.SetParent(transform);
        canvasTransform = canvasGo.transform;
        canvasTransform.localPosition = new Vector3(0, yOffset, 0);

        Canvas canvas = canvasGo.AddComponent<Canvas>();
        canvas.renderMode = RenderMode.WorldSpace;
        canvas.sortingOrder = 10; // 在角色和敌人之上

        CanvasScaler scaler = canvasGo.AddComponent<CanvasScaler>();
        scaler.dynamicPixelsPerUnit = 100f;

        // 创建背景Image
        GameObject bgGo = new GameObject("Background");
        bgGo.transform.SetParent(canvasGo.transform, false);
        Image bgImage = bgGo.AddComponent<Image>();
        bgImage.color = backgroundColor;
        if (backgroundSprite != null) bgImage.sprite = backgroundSprite;
        bgImage.rectTransform.sizeDelta = new Vector2(barWidth * 100f, barHeight * 100f);

        // 创建填充Image
        GameObject fillGo = new GameObject("Fill");
        fillGo.transform.SetParent(bgGo.transform, false);
        fillImage = fillGo.AddComponent<Image>();
        fillImage.color = fillColor;
        if (backgroundSprite != null) fillImage.sprite = backgroundSprite;
        fillImage.type = Image.Type.Filled;
        fillImage.fillMethod = Image.FillMethod.Horizontal;
        fillImage.fillOrigin = 0; // Left
        fillImage.fillAmount = 1f;

        // 填充填满父对象
        RectTransform fillRect = fillImage.rectTransform;
        fillRect.anchorMin = Vector2.zero;
        fillRect.anchorMax = Vector2.one;
        fillRect.offsetMin = Vector2.zero;
        fillRect.offsetMax = Vector2.zero;
    }

    private void OnHealthChanged(float current, float max)
    {
        if (fillImage != null)
            fillImage.fillAmount = max > 0 ? current / max : 0f;
    }
}
```

**创建步骤**：
1. 右键 `Scripts/v0_3/` → `C# Script` → `WorldSpaceHealthBar` → 替换代码

#### 9B. 编写Enemy.cs（重构版，使用Health组件）
**目标**：敌人也使用Health组件管理生命，配合WorldSpaceHealthBar显示血条。

**文件**：`Assets/Scripts/v0_3/Enemy.cs`
```csharp
using System.Collections;
using UnityEngine;

/// <summary>
/// 测试敌人。使用Health组件管理生命，WorldSpaceHealthBar显示头顶血条。
/// </summary>
[RequireComponent(typeof(SpriteRenderer))]
[RequireComponent(typeof(Health))]
public class Enemy : MonoBehaviour, IDamageable
{
    [Header("敌人属性")]
    [SerializeField] private float maxHealth = 3f;
    [SerializeField] private float hitFlashDuration = 0.15f;

    private SpriteRenderer sr;
    private Color originalColor;
    private Health health;

    void Awake()
    {
        sr = GetComponent<SpriteRenderer>();
        originalColor = sr.color;
        health = GetComponent<Health>();

        // 初始化生命
        health.Initialize(maxHealth);
        health.OnDeath += OnEnemyDeath;
    }

    void OnDestroy()
    {
        if (health != null) health.OnDeath -= OnEnemyDeath;
    }

    /// <summary>
    /// 实现IDamageable接口
    /// </summary>
    public void TakeDamage(float damage)
    {
        if (health.IsDead) return;

        health.TakeDamage(damage);
        StartCoroutine(HitFlashCoroutine());
        Debug.Log($"[Enemy] 受伤 {damage}，剩余 {health.CurrentHealth}/{health.MaxHealth}");
    }

    private IEnumerator HitFlashCoroutine()
    {
        sr.color = Color.white;
        yield return new WaitForSeconds(hitFlashDuration);
        sr.color = originalColor;
    }

    private void OnEnemyDeath()
    {
        sr.color = Color.gray;
        StartCoroutine(DeathCoroutine());
    }

    private IEnumerator DeathCoroutine()
    {
        float elapsed = 0f;
        float duration = 0.3f;
        Vector3 startScale = transform.localScale;
        while (elapsed < duration)
        {
            elapsed += Time.deltaTime;
            transform.localScale = Vector3.Lerp(startScale, Vector3.zero, elapsed / duration);
            yield return null;
        }
        Destroy(gameObject);
    }
}
```

#### 9C. 创建Enemy_Test对象
**步骤**：
1. Hierarchy → 右键 → `2D Object` → `Sprites` → `Square` → 命名 `Enemy_Test`
2. Transform：Position=(6, 3, 0), Scale=(0.8, 0.8, 1)
3. SpriteRenderer：Color=(0.8, 0.2, 0.2, 1)，SortingOrder=2
4. 添加组件：
   - `Rigidbody2D`（Dynamic, Gravity=0, FreezeRotation）
   - 确认 `BoxCollider2D`（自带）
   - `Health.cs`（maxHealth=3）
   - `WorldSpaceHealthBar.cs`（barWidth=0.8, barHeight=0.12, yOffset=0.6）
   - `Enemy.cs`
5. 设置Layer：Inspector顶部 → `Add Layer...` → 创建 `Enemy` → 将对象设为 `Enemy` Layer
6. 配置WorldSpaceHealthBar的 `backgroundSprite`：将 `WhiteSquare.asset` 拖到字段（可选）

**敌人对象结构**：
```
Enemy_Test
  - SpriteRenderer (红色方块)
  - Rigidbody2D (Dynamic, Gravity=0, FreezeRotation)
  - BoxCollider2D
  - Health (maxHealth=3)
  - WorldSpaceHealthBar (barWidth=0.8, barHeight=0.12, yOffset=0.6)
    └── HealthBarCanvas (WorldSpace Canvas)
        ├── Background (Image: 深灰色)
        └── Fill (Image: 红色, Filled)
  - Enemy
```

**验收标准**：
- [ ] `Enemy_Test` 存在，红色方块，Layer=Enemy
- [ ] 有Rigidbody2D + BoxCollider2D + Health + WorldSpaceHealthBar + Enemy
- [ ] 敌人头顶有深灰色背景 + 红色填充血条（世界空间，跟随移动）
- [ ] 血条初始为满（FillAmount=1）
- [ ] `Enemy.cs` 实现 `IDamageable`，调用 `health.TakeDamage`
- [ ] 受伤时闪烁白色，死亡时变灰缩小销毁
- [ ] 死亡时头顶血条随对象一起销毁

---

### 任务10：编写PlayerUI.cs（玩家固定UI）
**目标**：监听Health和PlayerStats，更新**屏幕左下角的玩家固定血条/护甲条**。

> 💡 玩家的血条为**Screen Space Overlay**（屏幕固定UI），与敌人的**World Space**血条（头顶跟随）是不同的系统。

**文件**：`Assets/Scripts/v0_3/PlayerUI.cs`
```csharp
using UnityEngine;
using UnityEngine.UI;

public class PlayerUI : MonoBehaviour
{
    [Header("UI引用")]
    [SerializeField] private Image hpBar;
    [SerializeField] private Image armorBar;

    [Header("角色引用")]
    [SerializeField] private PlayerController player;

    private Health health;
    private PlayerStats stats;

    void Start()
    {
        if (player == null) { Debug.LogWarning("[PlayerUI] PlayerController未设置"); return; }

        health = player.GetHealth();
        stats = player.GetStats();

        if (health != null)
        {
            health.OnHealthChanged += OnHealthChanged;
            OnHealthChanged(health.CurrentHealth, health.MaxHealth);
        }

        if (stats != null)
        {
            stats.OnStatsChanged += OnStatsChanged;
            OnStatsChanged();
        }
    }

    void OnDestroy()
    {
        if (health != null) health.OnHealthChanged -= OnHealthChanged;
        if (stats != null) stats.OnStatsChanged -= OnStatsChanged;
    }

    private void OnHealthChanged(float current, float max)
    {
        if (hpBar != null) hpBar.fillAmount = max > 0 ? current / max : 0f;
    }

    private void OnStatsChanged()
    {
        if (armorBar != null && stats != null)
            armorBar.fillAmount = stats.MaxArmor > 0 ? stats.CurrentArmor / stats.MaxArmor : 0f;
    }
}
```

**步骤**：
1. 创建脚本 → 保存
2. 选中 `Canvas` → 移除旧PlayerUI → 添加新 `PlayerUI.cs`
3. Inspector配置：
   - HP Bar → 拖入 `HPBar`
   - Armor Bar → 拖入 `ArmorBar`
   - Player → 拖入 `Player`

**验收标准**：
- [ ] `PlayerUI.cs` 监听 `Health.OnHealthChanged` + `PlayerStats.OnStatsChanged`
- [ ] 血条红色、护甲条蓝色、背景灰色
- [ ] 初始状态均为满（FillAmount=1）

---

### 任务11：运行测试与验收
**测试步骤**：
1. Play运行
2. **移动**：WASD正常，墙壁碰撞正常
3. **瞄准**：鼠标移动，角色旋转面向鼠标
4. **UI**：左下角红条蓝条满，灰色背景
5. **攻击**：走近敌人→鼠标左键→红色半透明圆形闪现→敌人闪烁白色→Console日志
6. **扇形验证**：角色背对敌人时攻击，敌人不受伤（扇形过滤生效）
7. **冷却**：快速点击，确认0.5秒间隔
8. **死亡**：3次攻击后敌人变灰缩小销毁
9. **护甲恢复**：等待几秒，观察护甲条是否逐渐回满（因恢复缓慢，可观察数值或在PlayerStats中临时提高armorRegenRate测试）

**验收清单**：
- [ ] WASD移动 + 墙壁碰撞
- [ ] 鼠标瞄准，角色面向鼠标
- [ ] **玩家UI**：屏幕左下角有固定血条（红色）+ 护甲条（蓝色）+ 灰色背景
- [ ] **敌人血条**：红色方块头顶有世界空间血条（深灰背景+红色填充），跟随敌人移动
- [ ] 攻击：红色半透明圆形指示器闪现0.2秒
- [ ] 扇形判定：前方90度内敌人受伤，背后不受伤
- [ ] 敌人受击闪烁白色，Console有伤害日志，头顶血条同步减少
- [ ] 攻击有0.5秒冷却
- [ ] 敌人3次攻击后死亡（变灰缩小销毁，头顶血条随对象销毁）
- [ ] 血条满，不会自动减少/恢复
- [ ] **护甲脱战恢复**：受伤后等待3秒，护甲开始自动恢复（rate=0.5/秒）
- [ ] Console无红色报错

---

## 脚本清单

| 脚本 | 路径 | 挂载对象 | 职责 |
|------|------|----------|------|
| IAttack | `Interfaces/IAttack.cs` | — | 接口：Execute/AttackRange/AttackDamage |
| IDamageable | `Interfaces/IDamageable.cs` | — | 接口：TakeDamage |
| PlayerStats | `v0_3/PlayerStats.cs` | Player | 属性 + 脱战3秒后护甲恢复 |
| Health | `v0_3/Health.cs` | Player / Enemy_Test | 生命管理（HP不自动恢复） |
| PlayerCombat | `v0_3/PlayerCombat.cs` | Player | 攻击冷却管理 |
| WarriorAttack | `v0_3/WarriorAttack.cs` | Profession子对象 | 扇形攻击 + 红色指示器 |
| PlayerController | `v0_3/PlayerController.cs` | Player | 门面：输入 + 移动 + 瞄准 + 死亡 |
| Enemy | `v0_3/Enemy.cs` | Enemy_Test | 受伤 / 闪烁 / 死亡（复用Health组件） |
| **WorldSpaceHealthBar** | **`v0_3/WorldSpaceHealthBar.cs`** | **Enemy_Test** | **敌人头顶世界空间血条（深灰背景+红色填充）** |
| PlayerUI | `v0_3/PlayerUI.cs` | Canvas | **玩家固定UI**：屏幕左下角血条 + 护甲条 |

---

## 文档变更记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2025-07-09 | v0.3-draft | 初稿 |
| 2025-07-09 | v0.3-r2 | 正式版：扇形攻击、红色指示器、护甲自动恢复、删除旧脚本、鼠标瞄准 |
| 2025-07-09 | v0.3-r3 | 修改：敌人世界空间血条(WorldSpaceHealthBar)、玩家固定UI(ScreenSpace)、护甲脱战3秒后恢复 |

