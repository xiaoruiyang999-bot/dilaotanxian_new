# v1.0.0 M5 批次一「暂停菜单 + 教程 + 音量持久化」

> 状态：**已完成 · Play 零错误验证** | 日期：2026-08-23 | 里程碑：M5（[长期计划](项目长期计划_Roguelite路线图.md)）

## 交付

| 系统 | 说明 |
|---|---|
| 暂停菜单（**Esc**） | 时停面板：SFX/BGM 音量滑条（PlayerPrefs 持久化）+ 继续游戏 + 重开本局（魂照常结算，走 RunManager.DebugRestartFromPause 与死亡同链路） |
| 教程牌 | 每局出生房 3 块 TextMesh：操作/系统/目标三组提示（TutorialSigns，DungeonManager.Generate 挂点） |
| 音量系统 | AudioManager.SetSfxVolume/SetBgmVolume + 启动读档 |

## 技术要点

- Pause 走 InputActions 的 Pause action（Esc）+ PlayerInput 订阅（**纯 Input System 项目禁用旧 Input 类**——首版用 Input.GetKeyDown 触发 InvalidOperationException，已改）
- 时停互斥：其他面板开着时 Esc 不抢（Time.timeScale==0 检查）；HitStop.SuppressByUI 协调
- 滑条纯代码拼装（Background/Fill/Handle 三件套 + Slider.fillRect/handleRect 接线）

## 事故记录（红线 R18）

- fileID 溢出：999 开头 19 位数 > int64 上限 → 场景+prefab 解析崩溃连环 Safe Mode（修复需全项目扫，只修场景漏 prefab 二次事故）
- 教训链：MetaManager 手术(999段) → 部分宽容解析侥幸工作 → PauseSystem 手术(999段) → SceneRoots 崩 → 修复漏 prefab → 二次 Safe Mode → 全项目清扫 666 段解决
