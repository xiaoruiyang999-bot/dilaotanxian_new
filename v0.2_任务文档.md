# v0.2 任务文档：战士角色——移动、相机跟随与属性UI（正式版 v2）

## 版本信息
- **版本号**：v0.2
- **版本名称**：战士角色基础
- **美术策略**：程序员美术（纯色几何图形）
- **目标**：在v0.1小屋中，用WASD操控绿色圆球移动，相机平滑跟随，屏幕显示红色血条/蓝色护甲条/灰色背景，所有数值=5
- **前置版本**：v0.1（小屋场景 + Tilemap碰撞已就绪，Input System包已安装）
- **架构要求**：输入解耦 — `Input System → CharacterInput → WarriorCharacter`
- **本版本不做**：战斗系统（攻击、伤害、敌人、死亡逻辑）放到后续版本

---

## 项目当前状态（截至本次文档编写）

### v0.1 已完成 ✅
- 场景：`Assets/Scenes/v0_1_InitialHouse.unity`
- 地图：10×8小屋（8×6可行走），Tilemap + Walls碰撞完整
- TestPlayer：绿色圆球，SpriteRenderer + Rigidbody2D(gravity=0, freezeRotation) + CircleCollider2D
- 相机：Orthographic, size=6, 位置(4.5, 3.5, -10)
- 素材：WhiteSquare(64×64, PPU=64), WhiteCircle(64×64) — 由 `V0_1_InitialHouseBuilder.cs` 生成
- Input System包：`com.unity.inputsystem: 1.14.2` 已安装

### v0.2 尚未开始 ❌
- 无 `Scripts/v0_2/` 文件夹
- 无 `UI/` 文件夹
- 无 `v0_2_Warrior.unity` 场景
- 无 Input Actions 资产
- 未确认 `Active Input Handling` 设置
- 无角色/输入/相机/UI脚本

> 💡 Kimi Code执行前必读此节。实际文件系统状态可能变化，执行时应以当前项目状态为准。

---

## 总体目标

将v0.1的 `TestPlayer` 升级为正式的战士角色，采用**输入解耦架构**，实现：

1. **WASD移动**：Unity Input System → `CharacterInput` → `WarriorCharacter` → `Rigidbody2D.velocity`，碰到墙壁被阻挡
2. **相机跟随**：`CameraFollow` 脚本使Main Camera平滑跟随战士
3. **属性UI**：屏幕左下角显示红色血条（当前HP/最大HP）和蓝色护甲条（当前护甲/最大护甲），灰色背景框包裹
4. **所有数值**：HP、MaxHP、护甲、Max护甲 均初始化为 **5**

> 💡 本版本不包含战斗。战士的朝向、攻击、受伤/死亡逻辑将在后续版本中添加。

---

## 架构设计：输入解耦 `Input → CharacterInput → Character`

### 为什么要解耦？

| 层级 | 职责 | 依赖谁 | 被谁依赖 |
|------|------|--------|----------|
| **Input System** | 读取键盘/鼠标/手柄原始输入 | 无 | CharacterInput |
| **CharacterInput** | 接收Input System事件，转换为游戏意图调用 | Input System | WarriorCharacter |
| **WarriorCharacter** | 核心角色逻辑：移动、属性、状态 | CharacterInput（通过方法调用） | PlayerUI、CameraFollow |

**好处**：
- `WarriorCharacter` **不直接引用任何输入系统代码**。后续做AI敌人时，AI脚本直接调用`SetMoveInput()`即可控制移动
- 输入系统升级/替换时，只需修改 `CharacterInput`，`WarriorCharacter` 完全不动
- 网络对战时，从网络消息调用 `SetMoveInput()`，复用同一套角色逻辑

### 数据流向图

```
┌─────────────────┐     Unity Event      ┌──────────────────┐     方法调用      ┌────────────────────┐
│  PlayerInput    │ ──────────────────→ │  CharacterInput  │ ───────────────→ │  WarriorCharacter  │
│  (组件)         │   OnMove(context)   │  (脚本)          │  SetMoveInput()  │  (脚本)            │
│  Actions=资产   │                     │  GetComponent<>   │                  │  移动/属性/事件    │
└─────────────────┘                     └──────────────────┘                  └────────────────────┘
                                                                                        │
                                                                                        │ OnStatsChanged 事件
                                                                                        ↓
                                                                               ┌────────────────────┐
                                                                               │  PlayerUI          │
                                                                               │  (脚本)            │
                                                                               │  更新Image.fillAmount│
                                                                               └────────────────────┘
```

---

## 技术选型确认

| 决策项 | 选定方案 | 说明 |
|--------|----------|------|
| 输入系统 | **Unity Input System 1.14.2**（已安装） | 创建Input Actions资产，绑定WASD |
| 输入模式 | **Invoke Unity Events** | PlayerInput组件用Unity Event模式调用CharacterInput的方法 |
| 移动方式 | **Rigidbody2D.velocity** | 复用v0.1碰撞，物理驱动，被墙自然阻挡 |
| 相机跟随 | **LateUpdate + SmoothDamp** | 平滑跟随，避免抖动 |
| 血条UI | **UGUI Image（FillAmount）** | 红色=HP，蓝色=Armor，灰色=背景 |
| 数值设计 | **所有属性初始值 = 5** | HP=5, MaxHP=5, Armor=5, MaxArmor=5 |
| 素材复用 | **WhiteCircle.asset（v0.1已有）** | 64×64白色圆形，PPU=64，染色为绿色 |

---

## 任务清单

---

### 任务1：确认Input System环境并创建v0.2场景
**目标**：确保Input System可用，从v0.1复制出新场景，建立代码文件夹。

**步骤**：
1. 打开Unity，加载项目 `dilaotanxian_new`
2. 确认Input System已启用：
   - `Edit` → `Project Settings` → `Player` → 滚动到 `Configuration` 部分
   - 确认 `Active Input Handling` 为 `Input System Package (New)` 或 `Both`
   - 如果显示 `Input Manager (Old)`，改为 `Input System Package (New)`，Unity会提示重启，点击 **Apply** 并重启
3. 在Project窗口中，找到 `Assets/Scenes/v0_1_InitialHouse.unity`
4. 选中它，按 `Ctrl+D` 复制，重命名为 `v0_2_Warrior.unity`
5. 双击 `v0_2_Warrior.unity` 打开该场景
6. 创建脚本文件夹：右键 `Assets/Scripts/` → `Create` → `Folder`，命名为 `v0_2`
7. 创建UI文件夹：右键 `Assets/` → `Create` → `Folder`，命名为 `UI`
8. 保存场景：`Ctrl+S`

**验收标准**：
- [ ] `Active Input Handling` 设置为 `Input System Package (New)` 或 `Both`
- [ ] `Assets/Scenes/v0_2_Warrior.unity` 存在且已打开
- [ ] `Assets/Scripts/v0_2/` 文件夹存在
- [ ] `Assets/UI/` 文件夹存在
- [ ] Console无Input System相关红色报错

---

### 任务2：创建Input Actions资产
**目标**：创建`PlayerInputActions`资产，定义WASD移动Action。

**步骤**：
1. 在Project窗口中，右键 `Assets/` → `Create` → `Input Actions`
2. 命名为 `PlayerInputActions`
3. 双击 `PlayerInputActions` 打开Input Actions编辑器窗口
4. 在编辑器中，确认默认有一个Action Map，将其重命名为 `Player`（如不是此名）
5. 删除默认的 `Fire` Action（本版本不需要攻击）
6. 保留/创建 `Move` Action，设置如下：
   - **Action Name**: `Move`
   - **Action Type**: `Value`
   - **Control Type**: `Vector2`
7. 在 `Move` 的Bindings中，添加一个 **2D Vector Composite** 绑定：
   - 点击 `Move` 旁的 `+` 或 `Add Binding`
   - 选择 `2D Vector`
   - 展开四个方向子绑定，分别设置：
     - **Up**: `W` [Keyboard]
     - **Down**: `S` [Keyboard]
     - **Left**: `A` [Keyboard]
     - **Right**: `D` [Keyboard]
8. 保存Input Actions资产：点击编辑器左上角的 **Save Asset** 按钮（或按 `Ctrl+S`）
9. 关闭Input Actions编辑器窗口

**验收标准**：
- [ ] `Assets/PlayerInputActions.inputactions` 存在
- [ ] 资产内有一个名为 `Player` 的Action Map
- [ ] `Player` Action Map内有一个名为 `Move` 的Action
- [ ] `Move` 的Action Type = `Value`，Control Type = `Vector2`
- [ ] `Move` 的绑定为2D Vector Composite，包含WASD四向键

---

### 任务3：配置战士GameObject（重命名+添加组件）
**目标**：将v0.1的`TestPlayer`升级为v0.2的战士，配置所有必要组件。

**步骤**：
1. 在Hierarchy中，找到 `TestPlayer`，**重命名**为 `Warrior`
2. 选中 `Warrior`，在Inspector中确认已有组件：
   - **SpriteRenderer**：sprite = WhiteCircle, color = 绿色(0.2, 0.8, 0.3), sortingOrder = 2
   - **Rigidbody2D**：bodyType = Dynamic, gravityScale = 0, constraints = FreezeRotation
   - **CircleCollider2D**
3. 如果SpriteRenderer的sprite不是WhiteCircle，请从 `Assets/Sprites/v0_1/WhiteCircle.asset` 拖入
4. 添加 **Player Input** 组件：点击Add Component → 搜索 `Player Input`
   - **Actions**: 将 `Assets/PlayerInputActions` 拖到该字段
   - **Default Map**: `Player`
   - **Behavior**: `Invoke Unity Events`（关键！必须选此模式）
5. 在Player Input组件底部的 **Events** 区域中：
   - 展开 `Player`
   - 找到 `Move`
   - 点击 `Move` 旁的 `+` 添加一个事件条目
   - 暂时留空（等任务4创建CharacterInput脚本后再绑定）

**验收标准**：
- [ ] Hierarchy中 `TestPlayer` 已重命名为 `Warrior`
- [ ] `Warrior` 组件列表：SpriteRenderer + Rigidbody2D + CircleCollider2D + **Player Input**
- [ ] Player Input的Actions字段引用 `PlayerInputActions`
- [ ] Default Map = `Player`
- [ ] Behavior = `Invoke Unity Events`

---

### 任务4：编写 WarriorCharacter.cs（核心角色逻辑）
**目标**：创建角色核心脚本，管理移动和属性。**此脚本不直接引用输入系统**。

**文件路径**：`Assets/Scripts/v0_2/WarriorCharacter.cs`

**代码**：
```csharp
using UnityEngine;

[RequireComponent(typeof(Rigidbody2D))]
public class WarriorCharacter : MonoBehaviour
{
    [Header("移动属性")]
    [SerializeField] private float moveSpeed = 5f;

    [Header("角色数值")]
    [SerializeField] private float maxHP = 5f;
    [SerializeField] private float maxArmor = 5f;

    // ========== 运行时属性（只读，供外部读取）==========
    public float CurrentHP { get; private set; }
    public float CurrentArmor { get; private set; }
    public float MaxHP => maxHP;
    public float MaxArmor => maxArmor;

    // ========== 事件：数值变化时触发，供UI监听 ==========
    public System.Action OnStatsChanged;

    // ========== 内部组件 ==========
    private Rigidbody2D rb;

    // ========== 移动输入（由CharacterInput调用）==========
    private Vector2 moveInput;

    void Awake()
    {
        rb = GetComponent<Rigidbody2D>();

        // 初始化数值（全部=5）
        CurrentHP = maxHP;
        CurrentArmor = maxArmor;
    }

    // ========== 输入接口：由CharacterInput调用 ==========

    /// <summary>
    /// 设置移动输入方向。由CharacterInput脚本调用，不直接读取输入系统。
    /// </summary>
    public void SetMoveInput(Vector2 direction)
    {
        moveInput = direction;
    }

    // ========== 物理更新 ==========

    void FixedUpdate()
    {
        // 使用Rigidbody2D.velocity移动，与Tilemap Collider正确交互
        rb.velocity = moveInput.normalized * moveSpeed;
    }

    // ========== 属性修改接口（后续版本受伤/恢复时调用）==========

    /// <summary>
    /// 修改HP。正值为治疗，负值为伤害。
    /// </summary>
    public void ModifyHP(float delta)
    {
        CurrentHP = Mathf.Clamp(CurrentHP + delta, 0, maxHP);
        OnStatsChanged?.Invoke();
    }

    /// <summary>
    /// 修改护甲。正值为恢复，负值为消耗。
    /// </summary>
    public void ModifyArmor(float delta)
    {
        CurrentArmor = Mathf.Clamp(CurrentArmor + delta, 0, maxArmor);
        OnStatsChanged?.Invoke();
    }

    void OnDisable()
    {
        // 对象禁用时停止移动
        rb.velocity = Vector2.zero;
    }
}
```

**创建步骤**：
1. 右键 `Assets/Scripts/v0_2/` → `Create` → `C# Script` → 命名 `WarriorCharacter`
2. 双击打开，替换为上述代码
3. 保存（`Ctrl+S`）
4. 将 `WarriorCharacter.cs` 拖到Hierarchy中的 `Warrior` 上

**验收标准**：
- [ ] `Assets/Scripts/v0_2/WarriorCharacter.cs` 存在且内容正确
- [ ] `Warrior` 对象已挂载 `WarriorCharacter` 脚本
- [ ] Inspector中 `Move Speed = 5`，`Max HP = 5`，`Max Armor = 5`

---

### 任务5：编写 CharacterInput.cs（输入中间层）
**目标**：创建输入中间层脚本，接收Input System事件并转发给WarriorCharacter。

**文件路径**：`Assets/Scripts/v0_2/CharacterInput.cs`

**代码**：
```csharp
using UnityEngine;
using UnityEngine.InputSystem;

[RequireComponent(typeof(PlayerInput))]
[RequireComponent(typeof(WarriorCharacter))]
public class CharacterInput : MonoBehaviour
{
    private WarriorCharacter character;

    void Awake()
    {
        character = GetComponent<WarriorCharacter>();
    }

    /// <summary>
    /// 由Player Input组件的Unity Event调用。
    /// 接收Input System的Move输入，转发给WarriorCharacter。
    /// </summary>
    public void OnMove(InputAction.CallbackContext context)
    {
        // 读取Vector2输入（WASD的X和Y）
        Vector2 moveInput = context.ReadValue<Vector2>();
        character.SetMoveInput(moveInput);
    }
}
```

**创建步骤**：
1. 右键 `Assets/Scripts/v0_2/` → `Create` → `C# Script` → 命名 `CharacterInput`
2. 双击打开，替换为上述代码
3. 保存（`Ctrl+S`）
4. 将 `CharacterInput.cs` 拖到Hierarchy中的 `Warrior` 上
5. **绑定Input事件**：
   - 选中 `Warrior`，在Inspector中找到 **Player Input** 组件
   - 展开 **Events** → **Player** → **Move**
   - 点击 `+` 添加事件条目
   - 将Hierarchy中的 `Warrior` 拖到槽位中
   - 下拉选择 `CharacterInput` → `OnMove`
   - （如有旧的事件绑定，删除或替换）

> ⚠️ **关键**：Player Input组件的Behavior必须是 `Invoke Unity Events`，否则无法绑定。

**验收标准**：
- [ ] `Assets/Scripts/v0_2/CharacterInput.cs` 存在且内容正确
- [ ] `Warrior` 对象已挂载 `CharacterInput` 脚本
- [ ] Player Input组件 → Events → Player → Move 已绑定到 `Warrior` → `CharacterInput.OnMove`

---

### 任务6：编写 CameraFollow.cs（相机跟随）
**目标**：Main Camera平滑跟随战士，保持角色在画面中心。

**文件路径**：`Assets/Scripts/v0_2/CameraFollow.cs`

**代码**：
```csharp
using UnityEngine;

public class CameraFollow : MonoBehaviour
{
    [Header("跟随目标")]
    [SerializeField] private Transform target;

    [Header("平滑参数")]
    [SerializeField] private float smoothSpeed = 10f;
    [SerializeField] private Vector3 offset = new Vector3(0f, 0f, -10f);

    private Vector3 currentVelocity;

    void LateUpdate()
    {
        if (target == null) return;

        // 目标位置 = 战士位置 + 偏移（Z轴保持-10，确保相机在2D平面之上）
        Vector3 targetPosition = target.position + offset;

        // 使用SmoothDamp实现平滑跟随，避免生硬抖动
        transform.position = Vector3.SmoothDamp(
            transform.position,
            targetPosition,
            ref currentVelocity,
            1f / smoothSpeed
        );
    }
}
```

**创建步骤**：
1. 右键 `Assets/Scripts/v0_2/` → `Create` → `C# Script` → 命名 `CameraFollow`
2. 双击打开，替换为上述代码
3. 保存
4. 在Hierarchy中，选中 `Main Camera`
5. 将 `CameraFollow.cs` 拖到 `Main Camera` 上
6. 在CameraFollow组件中，将 `Warrior` 拖到 **Target** 字段
7. 确认 **Offset** 的Z = -10

**验收标准**：
- [ ] `Assets/Scripts/v0_2/CameraFollow.cs` 存在且内容正确
- [ ] `Main Camera` 已挂载 `CameraFollow` 脚本
- [ ] CameraFollow的Target字段引用 `Warrior`

---

### 任务7：制作UI——血条、护甲条、背景（Canvas + Image）
**目标**：在屏幕左下角创建属性面板，包含灰色背景、红色血条、蓝色护甲条。

**UI结构说明**：
```
Canvas (Screen Space - Overlay, Scale With Screen Size)
└── PlayerStatsPanel (锚点=左下角, 位置距边20px)
    ├── Background (Image, 灰色, 填满父对象)
    ├── HPBar (Image, 红色, Filled, Horizontal, 偏下)
    └── ArmorBar (Image, 蓝色, Filled, Horizontal, 偏上, 更细)
```

**步骤**：
1. 在Hierarchy中，右键 → `UI` → `Canvas`
   - 这会同时创建一个 `EventSystem`
2. 选中 `Canvas`，在Inspector中确认/修改：
   - **Render Mode**: `Screen Space - Overlay`
   - **Canvas Scaler** → **UI Scale Mode**: `Scale With Screen Size`
   - **Reference Resolution**: X=`1920`, Y=`1080`
3. 右键 `Canvas` → `UI` → `Image`，命名为 `PlayerStatsPanel`
4. 设置 `PlayerStatsPanel` 的 **Rect Transform**：
   - 点击 **Anchor Presets** 按钮（左上角小方块图标）
   - 按住 **Alt+Shift**，点击 **Bottom-Left**（左下角）
   - **Pos X**: `20`
   - **Pos Y**: `20`
   - **Width**: `200`
   - **Height**: `50`
5. 右键 `PlayerStatsPanel` → `UI` → `Image`，命名为 `Background`
6. 设置 `Background`：
   - **Color**: R=0.3, G=0.3, B=0.3（灰色）
   - **Rect Transform**：
     - 点击Anchor Presets，按住 **Alt+Shift**，点击 **Stretch-Stretch**（中间全拉伸图标）
     - 或手动设：Left=0, Right=0, Top=0, Bottom=0
7. 右键 `PlayerStatsPanel` → `UI` → `Image`，命名为 `HPBar`
8. 设置 `HPBar`：
   - **Color**: R=0.9, G=0.1, B=0.1（红色）
   - **Image** 组件中：
     - **Image Type**: `Filled`
     - **Fill Method**: `Horizontal`
     - **Fill Origin**: `Left`
     - **Fill Amount**: `1`
   - **Rect Transform**：
     - 点击Anchor Presets，按住 **Alt+Shift**，点击 **Left-Middle**（左中，垂直居中偏左）
     - 或直接设：**Pos X**: `0`, **Pos Y**: `-8`, **Width**: `200`, **Height**: `20`
     - **Left**: `0`, **Right**: `0`（拉伸到父对象宽度）
9. 右键 `PlayerStatsPanel` → `UI` → `Image`，命名为 `ArmorBar`
10. 设置 `ArmorBar`：
    - **Color**: R=0.2, G=0.5, B=0.9（蓝色）
    - **Image** 组件中：
      - **Image Type**: `Filled`
      - **Fill Method**: `Horizontal`
      - **Fill Origin**: `Left`
      - **Fill Amount**: `1`
    - **Rect Transform**：
      - 点击Anchor Presets，按住 **Alt+Shift**，点击 **Left-Middle**
      - 或直接设：**Pos X**: `0`, **Pos Y**: `12`, **Width**: `200`, **Height**: `10`
      - **Left**: `0`, **Right**: `0`

> 💡 **层级关系**：HPBar和ArmorBar是PlayerStatsPanel的子对象，Background也是子对象。三个Image重叠显示：Background在最底层（灰色底），HPBar在中间（红色条），ArmorBar在最上层（蓝色条）。

**验收标准**：
- [ ] Hierarchy中有 `Canvas` → `PlayerStatsPanel` → `Background` + `HPBar` + `ArmorBar`
- [ ] Game视图中左下角显示灰色矩形框
- [ ] 灰色框内有红色横条（较粗，偏下）和蓝色横条（较细，偏上）
- [ ] 红色和蓝色横条初始均为填满状态（Fill Amount = 1）

---

### 任务8：编写 PlayerUI.cs（UI数据绑定）
**目标**：监听WarriorCharacter的数值变化事件，实时同步到UI Image的Fill Amount。

**文件路径**：`Assets/Scripts/v0_2/PlayerUI.cs`

**代码**：
```csharp
using UnityEngine;
using UnityEngine.UI;

public class PlayerUI : MonoBehaviour
{
    [Header("UI引用")]
    [SerializeField] private Image hpBar;      // 红色血条Image
    [SerializeField] private Image armorBar;   // 蓝色护甲条Image

    [Header("角色引用")]
    [SerializeField] private WarriorCharacter character;

    void Start()
    {
        // 监听WarriorCharacter的属性变化事件
        if (character != null)
        {
            character.OnStatsChanged += UpdateUI;
            // 初始化显示
            UpdateUI();
        }
        else
        {
            Debug.LogWarning("[PlayerUI] WarriorCharacter reference is missing!");
        }
    }

    void OnDestroy()
    {
        if (character != null)
            character.OnStatsChanged -= UpdateUI;
    }

    private void UpdateUI()
    {
        if (character == null) return;

        // 计算Fill Amount（0~1）
        float hpRatio = character.MaxHP > 0 ? character.CurrentHP / character.MaxHP : 0;
        float armorRatio = character.MaxArmor > 0 ? character.CurrentArmor / character.MaxArmor : 0;

        if (hpBar != null)
            hpBar.fillAmount = hpRatio;

        if (armorBar != null)
            armorBar.fillAmount = armorRatio;
    }
}
```

**创建步骤**：
1. 右键 `Assets/Scripts/v0_2/` → `Create` → `C# Script` → 命名 `PlayerUI`
2. 双击打开，替换为上述代码
3. 保存
4. 在Hierarchy中，选中 `Canvas`（或 `PlayerStatsPanel`）
5. 将 `PlayerUI.cs` 拖到选中的对象上
6. 在Inspector中配置 `PlayerUI` 组件：
   - **HP Bar**: 将Hierarchy中的 `HPBar` 拖入
   - **Armor Bar**: 将Hierarchy中的 `ArmorBar` 拖入
   - **Character**: 将Hierarchy中的 `Warrior` 拖入

**验收标准**：
- [ ] `Assets/Scripts/v0_2/PlayerUI.cs` 存在且内容正确
- [ ] Canvas（或PlayerStatsPanel）已挂载 `PlayerUI` 脚本
- [ ] PlayerUI的三个字段（HPBar、ArmorBar、Character）在Inspector中已正确引用对应对象

---

### 任务9：运行测试与最终整理
**目标**：完整测试所有功能，确保无报错，场景保存完整。

**测试步骤**：
1. 点击工具栏 **Play** 按钮运行
2. **测试移动**：按WASD，观察绿色圆球是否向对应方向移动
3. **测试碰撞**：将圆球推向墙壁，确认被阻挡不穿透
4. **测试停止**：松开WASD按键，确认圆球立刻停止
5. **测试相机**：移动时观察相机是否平滑跟随，战士保持在画面中心
6. **测试UI**：确认左下角UI显示正常（灰色背景+红色满条+蓝色满条）
7. 点击 **Play** 停止

**Hierarchy最终结构确认**：
```
Main Camera (Camera + CameraFollow)
Global Light 2D
Grid
├── Floor (Tilemap)
└── Walls (Tilemap + Colliders)
Warrior (SpriteRenderer + Rigidbody2D + CircleCollider2D + PlayerInput + WarriorCharacter + CharacterInput)
Canvas (Canvas + CanvasScaler + GraphicRaycaster)
├── EventSystem
└── PlayerStatsPanel (RectTransform)
    ├── Background (Image: 灰色)
    ├── HPBar (Image: 红色, Filled)
    └── ArmorBar (Image: 蓝色, Filled)
```

**保存**：
1. `Ctrl+S` 保存场景
2. `File` → `Save Project` 保存项目

---

## 完整验收清单

完成所有任务后，逐项确认：

| # | 验收项 | 标准 |
|---|--------|------|
| 1 | **Input System启用** | `Active Input Handling` = Input System Package (New) 或 Both |
| 2 | **Input Actions资产** | `PlayerInputActions` 存在，含Player/Move(WASD) |
| 3 | **战士对象** | `Warrior`（原TestPlayer重命名），组件完整 |
| 4 | **输入解耦** | CharacterInput.OnMove → WarriorCharacter.SetMoveInput，无直接Input.GetKey |
| 5 | **WASD移动** | 按键后战士向对应方向移动 |
| 6 | **墙壁碰撞** | 碰到墙壁被阻挡，不穿透 |
| 7 | **相机跟随** | 相机平滑跟随，战士在画面中心 |
| 8 | **UI显示** | 左下角有灰色背景框+红色血条+蓝色护甲条 |
| 9 | **UI初始状态** | 红条和蓝条均为填满（Fill Amount = 1） |
| 10 | **数值正确** | WarriorCharacter: MaxHP=5, MaxArmor=5, CurrentHP=5, CurrentArmor=5 |
| 11 | **无报错** | Console无红色Error |
| 12 | **场景保存** | v0_2_Warrior.unity已保存，重开无异常 |

---

## 常见问题排查

### Q1：WASD按了但角色不动
**排查步骤**：
1. 确认 `Active Input Handling` = `Input System Package (New)` 或 `Both`
2. 确认 `Warrior` 有 **Player Input** 组件，且Actions引用 `PlayerInputActions`
3. 确认Player Input的 **Behavior** = `Invoke Unity Events`
4. 确认Player Input → Events → Player → Move 已绑定到 `Warrior` → `CharacterInput.OnMove`
5. 确认 `CharacterInput` 和 `WarriorCharacter` 都已挂载到 `Warrior`
6. 确认 `WarriorCharacter` 的 `moveSpeed` 不为0
7. 确认Rigidbody2D的Body Type = Dynamic，Gravity Scale = 0

### Q2：角色移动但相机不跟随
**排查**：
1. 确认CameraFollow已挂载到Main Camera
2. 确认CameraFollow的Target引用 `Warrior`
3. 确认CameraFollow的Offset Z = -10

### Q3：UI不显示
**排查**：
1. 确认Canvas的Render Mode = Screen Space - Overlay
2. 确认Game视图分辨率不是极小
3. 确认PlayerStatsPanel锚点=Bottom-Left
4. 确认HPBar/ArmorBar的Image Type = Filled

### Q4：角色移动时穿过墙壁
**排查**：
1. 确认Walls有TilemapCollider2D + CompositeCollider2D + Rigidbody2D(Static)
2. 确认Warrior有Rigidbody2D(Dynamic) + CircleCollider2D
3. 确认两者不在同一忽略碰撞层

---

## 脚本架构速查

| 脚本 | 挂载对象 | 职责 | 关键方法/事件 |
|------|----------|------|---------------|
| **WarriorCharacter** | Warrior | 核心角色逻辑、移动、属性 | `SetMoveInput()`、`ModifyHP()`、`ModifyArmor()`、`OnStatsChanged` |
| **CharacterInput** | Warrior | 接收Input System事件并转发 | `OnMove(InputAction.CallbackContext)` → 调用 `character.SetMoveInput()` |
| **CameraFollow** | Main Camera | 平滑跟随战士 | `LateUpdate()` + `SmoothDamp()` |
| **PlayerUI** | Canvas | 监听数值变化，更新UI | 监听 `OnStatsChanged`，更新 `Image.fillAmount` |
| **PlayerInput** | Warrior (组件) | Unity Input System组件 | Actions=PlayerInputActions, Behavior=Invoke Unity Events |

---

## v0.2 → v0.3 衔接说明

v0.2完成后，以下资产可直接复用：

| v0.2产出 | v0.3复用方式 |
|----------|-------------|
| Input System + CharacterInput架构 | 添加 `OnAttack` 事件回调，绑定鼠标左键 |
| WarriorCharacter | 添加 `Attack()` 方法和攻击冷却逻辑 |
| WarriorCharacter属性系统 | 添加受伤调用 `ModifyHP(-damage)` |
| PlayerUI | 血条减少时自动更新，无需改动 |
| CameraFollow | 直接复用 |
| 地图场景 | 直接复用 |

v0.3将在v0.2基础上添加：
- 鼠标瞄准/角色朝向（旋转Z轴面向鼠标）
- 鼠标左键攻击（CharacterInput添加OnAttack → WarriorCharacter.Attack()）
- 攻击范围检测（OverlapCircle）
- 测试敌人（红色方块）+ 受伤反馈
- 死亡/重生逻辑

---

## 文档变更记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2025-07-09 | v0.2-draft | 初稿（含战斗系统，Input.GetKey） |
| 2025-07-09 | v0.2-r2 | 正式版：移除战斗，改用Input System |
| 2025-07-09 | v0.2-r3 | 重写：采用Input→CharacterInput→Character解耦架构，添加项目进度验证 |

