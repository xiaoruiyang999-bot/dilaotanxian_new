# v0.6.3 操作记录

> 版本主题：六把基础武器 —— Projectile 系统 + 蓄力 + 弹夹/换弹 + 掉落闭环（计划书 4.6/4.4/4.10，第五章 v0.6.3）。
> 执行文档：`v0.6.3_正式计划.md`（含用户三项确认：宝箱 50/50、武器视觉运行时程序化拼接、法力瓶商店+宝箱）。

## 改动文件清单

### 新建脚本（7，含 .meta）
- `Combat/ProjectileData.cs`（guid `3053127e…`）：子弹 SO（速度/伤害/存活/半径/视觉类型/配色/目标层）
- `Combat/Projectile.cs`（guid `d558f714…`）：直线飞行 + Trigger 命中 + 撞墙销毁 + 存活兜底
- `Weapon/ProjectileVisualBuilder.cs`（guid `21e3b791…`）：四种子弹视觉 + 命中特效 + 共享白图/圆图 Sprite 缓存
- `Weapon/WeaponVisualBuilder.cs`（guid `f6f16960…`）：六武器模块化手持视觉（含 Effect 蓄力发光部件）+ 地图掉落小图标（职业色底板）
- `Player/AmmoUI.cs`（guid `50d36539…`）：弹药面板（武器名+色块、x/y、换弹细条）
- `Dungeon/Interaction/ManaOrb.cs`（guid `a84b7634…`）：击杀法力球 walk-over 吸附
- `Dungeon/Interaction/ManaBottlePickup.cs`（guid `ac86b383…`）：法力瓶 E 拾取 +40

### 修改脚本（13）
- `Combat/AttackData.cs`：+CreateRuntimeCopy/SetRangeAngle/SetDamage（仅供运行时副本）
- `Combat/WeaponController.cs`：+SetAttackData/+宽度倍率/+SetCustomVisual/ClearCustomVisual
- `Combat/WeaponHitbox.cs`：判定宽度改实时读 WeaponController.WeaponWidth（逻辑零改动）
- `Player/PlayerCombat.cs`：三模式（Melee/Ranged/SelfCast）+ 蓄力状态机 + 弹夹/换弹/闲置自动换弹 + 弹药事件（218→735 行）
- `Player/PlayerController.cs`：Attack 改 started/canceled 转发
- `Player/PlayerMovement.cs`：+SetChargeSlow（蓄力 ×0.5，优先于奔跑）
- `Weapon/WeaponData.cs`：+projectileData/healAmount/蓄力参数组（chargeMaxTime 等 7 字段）
- `Weapon/WeaponBehavior.cs`：Ranged/SelfCast 派生接入分发（去 TODO）
- `Weapon/WeaponPickup.cs`：Drop 视觉改 WeaponVisualBuilder 小图标 + 职业色底板
- `Enemy/EnemyStats.cs`：+manaOrbValue（默认 3）
- `Enemy/EnemyController.cs`：死亡掉 ManaOrb
- `Dungeon/Interaction/SupplyInteractable.cs`：SupplyType +Mana
- `Dungeon/Interaction/ChestInteractable.cs`：奖励 50/50（武器/法力瓶），无职业兜底治疗球

### 数据资产
- 新建：`Assets/Data/AttackData_PlayerKnife.asset`（`efd2bd61…`）、`AttackData_PlayerSpear.asset`（`5e972b97…`）——刀/枪矛拆分，不再共用 PlayerAttackData
- 新建：`Assets/Data/ProjectileData_Arrow/Bolt/EnergyOrb/SpiritOrb.asset`（`a3cf46c5…`/`89877083…`/`07fa79e3…`/`c90035a4…`，精灵弹为 v0.6.5 预留）
- 更新：六份 `Assets/Data/Weapon/*.asset`（接新 AttackData/ProjectileData/healAmount/蓄力参数；弓箭 fireInterval 0.4、能量法杖 0.3）
- 更新：`Enemy_Elite.prefab` manaOrbValue=8、`Enemy_Boss.prefab`=20（PrefabInstance m_Modifications 插入，Enemy.prefab 不动默认 3）
- 新建：`Assets/Prefabs/Dungeon/Supply_ManaBottle.prefab`（新 guid `4be98d59…`，克隆 Supply_Heal 改 supplyType=2/amount=40/蓝色）
- 更新：`InteractableTable_Shop.asset` 加一行（Row 陈列第 4 件；Row 模式忽略 count 区间，无需改 countMin/Max）
- `Assembly-CSharp.csproj` 补 7 条 Compile Include

## 实现要点

- **三模式攻击链**：PlayerCombat 保持近战三阶段状态机原样，weapon==null 走默认近战（v0_4/v0_5 旧场景零影响）；装备武器由 WeaponBehavior 三派生接入（SetMeleeWeapon/SetRangedWeapon/SetSelfCastWeapon），Unequip 经 SetAttackData(default) 全复位。
- **蓄力**：Attack started/canceled → 按下 BeginCharge（换弹中禁止）→ 松开按 chargeRule 结算。刀=副本 range/angle ×1.8；枪矛=副本 range ×2 + WeaponController 宽度倍率 ×1.2（WeaponHitbox 实时读）；弓箭=Launch 伤害/弹速 ×2。蓄力移动 ×0.5（PlayerMovement.SetChargeSlow，不叠加奔跑）；**闪避不触碰蓄力状态**（蓄力在 PlayerCombat，Dash 在 PlayerMovement，天然隔离）；挥击结束（subPhase 回 None）副本与宽度还原基准。Effect 部件随蓄力 lerp → 白。
- **弹夹/换弹**：ReloadTimer 归 WeaponInstance，PlayerCombat 每帧递减并广播 OnReloadProgress；弹空自动换弹；连弩闲置 3s 自动换弹；换弹中禁止射击/蓄力/施法。
- **Projectile**：Kinematic RB2D + trigger 圆碰撞；命中过滤四级：owner 子物体排除 → 跳过 trigger（敌人探测圈）→ targetLayer(192) 内 IDamageable 结算 → Default 层固体撞墙销毁；lifetime 兜底；命中特效 3~5 方块染色飞散。
- **运行时视觉**：全部程序化拼接（白图/圆图 Sprite 缓存 + 调色板染色），六武器各 5~7 部件 + Effect 发光件；手持视觉挂 WeaponPivot（-90°约定），默认色块隐藏；地图掉落 = 0.7× 小图标 + 职业色底板。
- **掉落闭环**：敌人死亡按 manaOrbValue 掉 ManaOrb（walk-over 吸附）；宝箱 50/50 掉本职业随机武器（ClassData.AvailableWeapons）/法力瓶，pop-in 弹出；商店 Row 陈列第 4 件法力瓶（SupplyType.Mana → AddMana(40)）。
- **AmmoUI**：订阅 PlayerCombat 三事件，面板定位参考 Canvas/PlayerStatsPanel（只读其位置不改 Transform，自检 #15 规则）；近战/卸下自动隐藏。

## 验证情况

- `dotnet build Assembly-CSharp.csproj`：**0 警告 0 错误**（7 个新文件已补 csproj 条目）。
- YAML 核对：两 AttackData/四 ProjectileData/六 WeaponData 字段与脚本序列化顺序一致；prefab override 目标 fileID 6461820464174567309 已核为 EnemyStats（guid d6116f85…）；Supply_ManaBottle 与 Supply_Heal 仅 3 处 diff；全部新 guid 全库查重通过。
- 运行时行为（蓄力手感/子弹飞行/掉落/UI）未经编辑器实测，待用户验收。

## 待用户在编辑器验收清单

> 测试场景：`v0_6_PrepRoom` 开局 → 传送门进 `v0_6_ClassWeapon`；Game 视图 1920×1080 或 Free Aspect。

1. **编辑器先刷新**：切回 Unity 让其导入新脚本/资产（若编辑器此前开着，勿保存旧内存覆盖 YAML）。
2. **刀/枪矛**：展台拿起后手持模块化视觉正确；单击=普通挥击；长按蓄力（移动变慢 ×0.5、Effect 渐亮）松手=大范围/长戳；蓄力中按 Space 闪避，蓄力保留，闪避结束松手仍放出蓄力击。
3. **弓箭**：单击=直接射箭；长按蓄力松手=强化箭（伤害/弹速 ×2）；射后自动装填 0.6s，AmmoUI 显示 0/1→1/1。
4. **连弩**：长按左键连发（0.15s/发，12 发）；弹空自动换弹 2.5s（进度条可见）；停手闲置 3s 也自动换弹。
5. **能量法杖**：单发能量弹，0.8s 装填循环；**治疗法杖**：按一下绿环闪 + HP+2，1s 装填。
6. **子弹命中**：箭/弹命中敌人扣血（受击闪白）、命中障碍物可破坏、撞墙消失不穿墙。
7. **换武器**：地牢内捡新武器，旧武器原地掉落（小图标+职业色底板）可捡回；跨职业武器提示"职业不符"。
8. **宝箱**：Treasure 房开箱，掉本职业武器或法力瓶（约 50/50），均可 E 拾取。
9. **法力**：击杀敌人掉小蓝球，走近自动吸附回蓝（普通+3/精英+8/Boss+20）；商店第 4 件法力瓶 +40。
10. **回归**：v0_4_EnemySystem 木桩场景默认近战不变；敌人近战（普通/精英/Boss）行为不变。

## 遗留与技术债

- 子弹无对象池（计划书第七章既定，连弩高频触发再议）；
- 六武器理论 DPS 0.95~1.17×（计划 §六表），木桩实测后微调；
- RangedWeaponBehavior 在 ProjectileData 漏配时静默不接入（无警告）；
- 能量/治疗法杖在旧场景（未选职业）不可获得，无影响。

---

## 补丁：武器视觉外移 + 渲染层级对齐（2026-07-28）

- **现象**：手持武器被角色圆球遮住后半截——视觉挂在球心（WeaponPivot 原点），且 builder 部件 sortingOrder 3~5 低于角色身体 10。
- **修复**（`WeaponController.SetCustomVisual`）：
  1. 视觉根沿攻击方向外移 `customVisualGripOffset`（序列化，默认 0.35 世界单位 ≈ 角色半径 0.3），除以父级 lossyScale 抵消 Player 根 0.6 缩放——武器从圆球边缘伸出展示全貌；
  2. 部件 sortingOrder 整体平移到默认武器色块层级（11）——以 builder 基准 3 算 delta，保持部件间相对层次（部件实际 11~13）。
- 地图掉落图标（WeaponPickup）不受影响：世界空间摆放，层级 2~5 不变。
- 外移量手感可在 Player 的 WeaponController Inspector 上直接调（Play 中改实例，记值后填 prefab）。

---

## 补丁 2：枪矛戳击 + 弓朝向 + 武器改名/名签 + 近战范围显示（2026-07-28，用户反馈）

### 改动清单
- `WeaponVisualBuilder.cs`：视觉分发由 DisplayName 字符串改为**数据驱动**（职业×行为×蓄力规则，改名不再影响视觉）；`BuildBow` 重建——弓臂沿 builder ±X（-90° 挂载后与弹道垂直，竖持弓），弓弦在后、箭矢指向目标，修复"竖琴"问题。
- 六份 WeaponData 资产改名：**新手长剑 / 新手长枪 / 新手弓 / 新手弩 / 新手回复法杖 / 新手伤害法杖**。
- `PlayerInteractor.cs`：候选为可拾取物时，提示标签显示"武器名\n按 E"（底框按名字长度自动加宽加高，每帧应用）。
- `AttackIndicator.cs`：+`detachOnShow`（敌人默认 true 不变；玩家 false=跟随玩家）；+Box 形状（`SetBox(length,width)` 矩形 Mesh，枪矛用）。
- `WeaponHitbox.cs`：+`LengthMultiplier`（判定长度倍率，默认 1，BeginSwing 复位；敌人侧不受影响）。
- `WeaponController.cs`：+`SetCustomVisualScale(width,length)`（蓄力模型尺寸适配）+`SetCustomVisualThrustOffset`（戳击活塞位移）。
- `PlayerCombat.cs`：
  - **近战灰色范围显示**（rangeDisplayColor 序列化可调）：点放 = Windup 显示、Active 结束收起；蓄力 = 蓄力中随等级实时变化、持续到挥击动画结束；尺寸换算按 pivot lossyScale（判定同源，不再"显示比判定大"）。
  - **武器模型适配范围**：蓄力中自定义视觉按范围/宽度倍率同步缩放，挥击结束还原。
  - **枪矛戳击**（方案一：戳出段判定）：Windup 逐帧后拉（thrustPullBack）；Active 活塞前冲（伸展度 sin 曲线），判定长度 = AttackRange × 伸展度，**仅前半程（progress<0.5）执行 Tick**，收回段无判定；范围矩形跟随判定长度实时伸缩。
- `AttackData_PlayerSpear.asset`：animationType 0(Arc) → **2(Thrust)**。

### 用户决策记录
- 枪矛判定：采纳**方案一**（戳出段有判定、收回段无判定）——判定与视觉一致；实现为 PlayerCombat 阶段门控 + 判定长度倍率，判定层逻辑零改动。
- 职业角色/敌人美术重做：**计划书漏排期**（美术清单 §四/§五 无归属子版本），待用户定插入点（建议 v0.6.4.5 专项或并入 v0.6.5）。

### 验证
- `dotnet build Assembly-CSharp.csproj`：0 警告 0 错误。
- 敌人预警链路（EnemyCombat → AttackIndicator）未动，detachOnShow 默认 true 保持原行为。

### 待验收
1. 枪矛：点按 = 后拉→前戳→收回，灰色矩形随戳伸缩，戳出段命中扣血、收回段穿模不扣血；蓄力戳更长更宽。
2. 长剑：蓄力时灰色扇形随蓄力变大、剑模型同步变长，松手挥击后扇形消失；挥击弧不再超出扇形。
3. 新手弓：手持竖持（弓臂与弹道垂直），不再是"竖琴"。
4. 靠近地上/展台武器：头顶显示"新手长剑 等名字 + 按 E"。
5. 敌人红色预警扇形回归无损。

---

## 补丁 3：范围显示不生效 + 蓄力挥击反向 + 弓臂交叠（2026-07-28，用户验收反馈）

### 根因与修复
1. **灰色范围框看不到**：**Player.prefab 上根本没有 AttackIndicator 组件**（13 个组件 guid 核对无 fc57a216…）——旧代码的指示器调用一直是死路径。修复：`PlayerCombat.Awake` 找不到时**运行时补建**（AddComponent，组件自初始化 Mesh 渲染器），并置 detachOnShow=false。
2. **蓄力攻击反向（动画+判定都反向）**：蓄满扇形角 120°×1.8=216° > 180°，`WeaponAnimator` 用 `RotateMode.Fast` 从 aim-108° 转到 aim+108° 时 DOTween **抄近路**（-144°）从玩家背后扫过；未蓄力 120° 不超 180° 所以正常。修复：目标角改为 `startZ + (endAngle - startAngle)` 的**数值展开形式** + `FastBeyond360`，保证按挥击方向正向扫满全程；≤180° 的挥击（含敌人全部近战）数值路径不变，零回归。
3. **弓"拿反/弓把倒着"**：两段弓臂各绕自身中心反向旋转 ±15°，在握把处一上一下交叠成"X"。修复：`BuildBow` 改**直臂长弓**——弓把/两臂沿 builder ±X 一条直线不旋转，梢端加反曲小片，弓弦在后、箭矢指目标。

### 验证
- `dotnet build`：0 警告 0 错误。
- 蓄力 216° 挥击路径推算：start=aim-108° → 目标展开为 start+216°，经 aim 正向扫过，判定随 pivot 同步。

### 待验收
1. 长剑点按/蓄力：灰色扇形可见，蓄力挥击朝鼠标方向（不再反向），满蓄大扇形从正前方扫过；
2. 枪矛戳击：灰色矩形随戳伸缩可见；
3. 新手弓：竖持直臂弓、弦靠玩家、箭指目标；
4. 敌人近战挥击与预警无变化。

---

## 补丁 4：挥击反向根治 + 点按/长按分离 + 满蓄伤害 + 长剑角度封顶（2026-07-28，用户验收反馈 ×2）

### 反向根因（补丁 3 修了一半的真相）
补丁 3 的"展开目标角"思路漏了 Unity 归一化：`localRotation = Euler(startZ)` 写入后角度被归一化到 [0,360)，起始角为负（aim < angle/2，即瞄准右上方区间）时 tween 起点被改，DOTween 依旧反向绕背。普通攻击 aim<60° 反向、蓄力满 aim<108° 反向，与用户实测区间完全吻合。
**根治**（`WeaponAnimator.Play`）：弃用欧拉角 tween，改 `DOVirtual.Float(0, sweep, …)` 每帧直写 `localRotation = Euler(startZ + v)`——不经过任何归一化，任意瞄准方向、任意扫幅（含 >180°）都正向扫满。

### 点按/长按分离（用户拍板：阈值 0.2s）
`PlayerCombat` 新增 `chargeHoldThreshold`（序列化 0.2s）与"待蓄力"状态：按下攻击键 → 只记 pendingCharge 与计时；**按住超过 0.2s 才 BeginCharge**；未到阈值松开 = 点按（近战 TryAttack / 弓箭 TryFire 直射）。死亡/失活/换装/换模式全部清理 pendingCharge。连弩（无蓄力规则）按住连发不受影响。

### 近战满蓄加伤害（用户拍板：满蓄一次性 ×1.5）
`WeaponData` +`chargeFullDamageMul = 1.5`（仅近战两把用）；`PlayerCombat` 缓存副本基准伤害，蓄力释放时 t≥1 对副本 `SetDamage(base × 1.5)`，挥击结束还原。配平影响：满蓄一刀 1.65 / 一枪 1.95，DPS 模型按"50% 时间蓄力"仍在 ±20% 带内边缘，待木桩实测复核。

### 长剑角度封顶（用户拍板：改数据）
`Weapon_Warrior_Knife.asset` chargeAngleMul 1.8 → **1.5**（120°×1.5=180°，蓄力扇形不超 180°）。

### 验证
- `dotnet build`：0 警告 0 错误。
- 六份 WeaponData 未显式写 chargeFullDamageMul 字段——按序列化默认取脚本初始值 1.5，仅近战两把生效（其余武器该字段无读取方）。

### 待验收
1. 任意方向（尤其瞄准右上方）点按/蓄力挥击均朝鼠标方向，无反向；
2. 快速点按 = 无蓄力范围的普通攻击；按住 ≥0.2s 才出现范围增长；
3. 蓄满释放长剑/长枪伤害明显提高（1.65/1.95），未满蓄无加成；
4. 长剑满蓄扇形角 = 180° 不超界；
5. 弓箭点按直射、长按蓄力；连弩连发与换弹不受影响。

---

## 补丁 5：弓箭蓄力"消失"（2026-07-28，用户验收反馈）

- **现象**：弓箭长按蓄力后松手无箭射出。
- **根因**：弓箭 FireInterval 配了 0.4（点射节奏），被"按住连发"分支（原条件仅 `FireInterval > 0`）命中——按住弓的 0.2s 待蓄力窗口内连发分支抢先射出普通箭、打空 1 发弹夹进入装填；到点 BeginCharge 因装填中直接返回，蓄力从未开始。
- **修复**（`PlayerCombat.UpdateRanged`）：连发分支加 `ChargeRule == ChargeRule.None && !pendingCharge`——只有无蓄力规则的远程（连弩/能量法杖）才允许按住连发。
- **另说明**：同次测试报的 `UnityConnectWebRequestException` 是 Unity 编辑器账户/云服务后台 token 刷新失败（UnityEditor.Connect 命名空间），与游戏代码无关，忽略或退出编辑器登录即可。
- 验证：`dotnet build` 0 警告 0 错误。待验收：弓箭点按直射、长按蓄力放出强化箭；连弩连发不受影响。

---

## 补丁 6：近战满蓄超时自动释放（2026-07-28，用户追加需求）

- **需求**：近战武器蓄力满后继续握住一段时间自动释放攻击。
- **设计结论（用户确认）**：近战蓄力与远程蓄力**不需要拆两套**——蓄力与连发本就由 `ChargeRule` 数据分流、互斥（连弩/能量法杖 `ChargeRule.None` 永不进蓄力态；蓄力武器永不进连发分支），自动释放不会误伤连发。
- **实现**（`PlayerCombat`）：+`chargeAutoReleaseDelay`（序列化，默认 1.0s，0=关闭）+ `overchargeTimer`；`UpdateCharge` 中 `mode == Melee && t >= 1` 起计时，超时自动 `ReleaseCharge()`（等同松手，满蓄伤害 ×1.5 照常生效）；`EndCharge` 复位计时。**仅近战两把生效，弓箭可一直蓄着不放**（用户拍板）。
- 验证：`dotnet build` 0 警告 0 错误。待验收：长剑/长枪满蓄后 1 秒自动出手；弓箭满蓄不自动释放；连弩连发不受影响。
