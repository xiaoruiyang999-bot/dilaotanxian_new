# v0.4.5.1 Attack Timing Framework 初版操作报告

## 一、版本目标

建立一套可扩展的攻击时机系统（Attack Timing Framework），将敌人攻击从「立即造成伤害」改造为：

```
TryAttack()
  ↓
进入攻击流程
  ↓
Windup（前摇）
  ↓
Active（攻击释放）
  ↓
播放武器动画
  ↓
到达 Execute 时机
  ↓
仍然调用原来的伤害判定（EnemyCombat.TryAttack）
  ↓
Recovery（后摇）
  ↓
Cooldown（冷却）
  ↓
结束
```

本次不修改伤害判定方式，真正的碰撞检测留到 v0.4.5.2 实现。

## 二、核心设计

### 2.1 职责解耦

| 模块 | 职责 |
|---|---|
| **AttackData** | 纯配置 ScriptableObject，保存 Windup/Active/Recovery 时长、武器角度、命中触发比例 |
| **WeaponAnimator** | 通用武器动画播放器，只负责旋转 WeaponPivot 并在配置时间点触发命中回调 |
| **EnemyAI** | 维护 Windup / Active / Recovery 三阶段计时，协调动画与伤害判定 |
| **EnemyCombat** | 保持原样，负责范围判定、伤害计算、冷却计时 |
| **AttackIndicator** | 保持原样，纯视觉层，由 EnemyAI 调用 Show/Hide/SetColor/SetRadius |

### 2.2 关键设计约束

- `WeaponAnimator` 不引用 `EnemyAI` 或任何 Enemy 类型，未来可复用于 Player / Boss / Summon。
- `EnemyAI` 自己维护 `windupTimer` / `activeTimer` / `recoveryTimer`，不依赖 Tween.OnComplete 切换状态。
- 命中时机由 `AttackData.ActiveMomentRatio` 配置，例如 `0.5` 表示动画播放到 50% 时触发。
- `AttackIndicator` 持续显示到真正挥击瞬间（OnActiveMoment）才隐藏。

## 三、新增文件

### 3.1 脚本

- `Assets/Scripts/v0_4/AttackData.cs`
  - 通用攻击配置 ScriptableObject
  - 字段：WindupTime、ActiveDuration、RecoveryTime、AttackStartAngle、AttackEndAngle、AttackEase、ActiveMomentRatio

- `Assets/Scripts/v0_4/WeaponAnimator.cs`
  - 通用武器动画播放器
  - 方法：`PlayAttack(Action onActiveMoment)`、`Stop()`
  - 使用 DOTween 旋转 WeaponPivot，并在 `ActiveDuration * ActiveMomentRatio` 触发命中回调

### 3.2 配置文件

- `Assets/Data/EnemyMeleeAttackData.asset`
  - 敌人默认近战攻击配置实例
  - 默认参数：
    - WindupTime: 0.25s
    - ActiveDuration: 0.25s
    - RecoveryTime: 0.35s
    - AttackStartAngle: -70°
    - AttackEndAngle: 70°
    - AttackEase: OutQuad
    - ActiveMomentRatio: 0.5

### 3.3 Meta 文件

- `Assets/Scripts/v0_4/AttackData.cs.meta`
- `Assets/Scripts/v0_4/WeaponAnimator.cs.meta`
- `Assets/Data/EnemyMeleeAttackData.asset.meta`

## 四、修改文件

### 4.1 `Assets/Scripts/v0_4/EnemyAI.cs`

主要改动：

1. 移除 Inspector 中的 `windupTime`、`recoveryTime` 字段
2. 新增 `AttackData attackData` 配置引用
3. 新增 `WeaponAnimator weaponAnimator` 引用
4. 扩展 Attack 子阶段为 `Windup / Active / Recovery`
5. 使用独立的 `windupTimer` / `activeTimer` / `recoveryTimer`
6. Windup 结束时进入 Active，调用 `weaponAnimator.PlayAttack(OnActiveMoment)`
7. `OnActiveMoment` 中隐藏 AttackIndicator，执行范围判定并调用 `combat.TryAttack`
8. Active 计时结束后进入 Recovery
9. Recovery 计时结束后回到 Chase
10. 退出 Attack 状态时调用 `weaponAnimator.Stop()` 清理动画

### 4.2 `Assets/Scenes/v0_4_EnemySystem.unity`

主要改动：

1. 在 `Enemy` GameObject 下新增 `WeaponPivot` 子对象
2. 在 `WeaponPivot` 下新增 `WeaponSprite` 子对象（带 SpriteRenderer）
3. 在 `Enemy` 根上新增 `WeaponAnimator` 组件
4. 配置 `EnemyAI` 的 `attackData` 引用指向 `EnemyMeleeAttackData.asset`
5. 配置 `WeaponAnimator` 的 `attackData` 和 `weaponPivot` 引用

Prefab 结构：

```
Enemy
├── HealthBarAnchor
├── AttackIndicator
└── WeaponPivot
      └── WeaponSprite
```

## 五、未修改文件

以下脚本与系统保持原样，未做任何修改：

- `Assets/Scripts/v0_4/EnemyCombat.cs`
- `Assets/Scripts/v0_4/EnemyController.cs`
- `Assets/Scripts/v0_4/EnemyStats.cs`
- `Assets/Scripts/v0_4/EnemyHealth.cs`
- `Assets/Scripts/v0_4/AttackIndicator.cs`
- `Assets/Scripts/v0_4/TrainingDummy.cs`
- `Assets/Scripts/v0_3/PlayerCombat.cs`
- `Assets/Scripts/v0_3/PlayerStats.cs`
- `Assets/Scripts/v0_3/Health.cs`
- `Assets/Scripts/v0_3/PlayerController.cs`
- `Assets/Scripts/v0_3/WorldSpaceHealthBar.cs`
- Camera、UI、Input、Physics、Rigidbody、Time 设置

## 六、遇到的问题与修复

### 问题 1：WeaponAnimator 编译报错

**现象**：

```
Assets\Scripts\v0_4\WeaponAnimator.cs(52,27): error CS0266:
Cannot implicitly convert type 'DG.Tweening.Tween' to 'DG.Tweening.Tweener'.
```

**原因**：`DOVirtual.DelayedCall` 返回 `DG.Tweening.Tween` 类型，不能隐式转换为 `Tweener`。

**修复**：将 `rotationTween` 与 `momentTween` 的字段类型从 `Tweener` 改为 `Tween`。

```csharp
private Tween rotationTween;
private Tween momentTween;
```

## 七、验证清单

请在 Unity 中打开 `v0_4_EnemySystem.unity` 场景，逐项验证：

- [ ] 玩家移动正常
- [ ] 敌人巡逻正常
- [ ] 敌人追击正常
- [ ] 敌人进入攻击范围后进入 Windup
- [ ] Windup 期间敌人停止移动并面向玩家
- [ ] Windup 期间显示黄色 AttackIndicator
- [ ] Windup 最后 `dangerThreshold` 秒 AttackIndicator 变红
- [ ] Windup 期间没有伤害
- [ ] Active 阶段开始播放武器挥砍动画
- [ ] 动画中间触发命中回调
- [ ] 命中瞬间 AttackIndicator 隐藏
- [ ] 命中时如果目标在范围内，造成一次伤害
- [ ] 命中只造成一次伤害
- [ ] Active 阶段敌人停止移动
- [ ] Recovery 阶段敌人停止移动
- [ ] Recovery 结束后敌人重新追击
- [ ] 攻击冷却保持正常
- [ ] 玩家死亡后不能移动 / 不能攻击
- [ ] Enemy 不会推动玩家
- [ ] UI、血条、Camera、WorldSpaceHealthBar、TrainingDummy 均正常
- [ ] 没有修改移动框架

## 八、v0.5 扩展预留

本次实现已预留以下扩展能力：

- `AttackData` 命名通用，未来可用于玩家普攻、Boss 技能、精英怪变体。
- `WeaponAnimator` 完全独立于 `EnemyAI`，可挂载于任意对象。
- `EnemyAI` 的阶段计时与动画完全解耦，未来支持慢动作、技能暂停、技能取消等。
- 武器 Sprite 替换只需修改 `WeaponSprite` 的 Sprite 与 Scale，无需改动 `WeaponAnimator`。

## 九、后续工作

- v0.4.5.2 将用 WeaponCollider / HitBox 替换 `EnemyCombat.TryAttack` 的伤害判定。
- 可继续丰富 AttackData，添加音效、特效、多段攻击、远程投射物等配置。
