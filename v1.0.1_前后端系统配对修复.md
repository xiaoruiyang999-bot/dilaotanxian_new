# v1.0.1 前后端系统配对修复

> 日期：2026-08-24  
> 状态：代码与主场景已落盘；编译零错误；8 秒 Play 初始化验证零运行时错误。

## 修复目标

把已经存在的存档、Meta、升级、结算与音频后端接到玩家可见、可操作的前端，并修复跨局状态断链。

## 配对结果

| 后端 | 前端 / 入口 | 本次连接 |
|---|---|---|
| `SaveSystem` Meta JSON | 右下角 Meta HUD、U 键魂商店、结算提示 | HUD 显示魂/最深层；结算后立即刷新并提示已入账 |
| `MetaManager` 永久升级 | `RunManager` 开局/重开链 | 首次开局注入永久 HP/金币/伤害；重开重新注入金币/伤害且避免 HP 重复叠加 |
| `PlayerStats` 局内资产 | 金币 HUD、三选一升级 | 新一局清零金币及局内乘数，事件主动刷新前端 |
| `UpgradePanel.Show` | `UpgradeSystem` 场景组件 | 补齐主场景挂载，宝箱/商店升级券不再被静默忽略 |
| `AudioManager.PlaySFX` | `AudioSystem` 场景组件 | 补齐主场景挂载；音频素材表仍需在 Inspector 配置实际 Clip |
| 第 9 层结算 | `VictoryPanel` | 显示本局魂收益和写档状态；“进入无尽模式”按钮接 `RunManager.NextFloor` |
| `MetaShop` Input Action | U 键魂商店 | 修复 PlayerInput 找到后未订阅事件的问题 |

## 修改文件

- `Assets/Scripts/now_use/UI/MetaManager.cs`
- `Assets/Scripts/now_use/UI/VictoryPanel.cs`
- `Assets/Scripts/now_use/Player/PlayerStats.cs`
- `Assets/Scripts/now_use/Dungeon/Core/RunManager.cs`
- `Assets/Scenes/v0_5_Dungeon.unity`

## 验证

- Unity 编译：0 error / 0 warning。
- 主场景保存后重新加载：`UpgradeSystem/UpgradePanel`、`AudioSystem/AudioManager` 均保留。
- Play 捕获 8 秒：`healthy=true`，0 个运行时错误。

## 后续人工验收

1. U 键打开魂商店，购买后 HUD 魂数即时减少。
2. 死亡结算显示“魂 +N”，重开后金币/局内升级清零，永久升级仍生效。
3. 宝箱或商店触发升级券，三选一面板正常显示并可点击。
4. 第 9 层结算显示魂收益，点击按钮后进入第 10 层。
5. 给 `AudioSystem` 配置 SFX/BGM Clip 后检查暂停菜单音量滑条。
