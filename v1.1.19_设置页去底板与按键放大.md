# v1.1.19 设置页去底板 + 按键放大

## 现象与根因

用户指出两个设置按键后面有一块"原图没有的颜色背景"。取证：
- 原图 `button_{audio,controls}_settings.png` 为 93% 不透明**满幅石条**（仅边缘 4~5% 透明），无底衬；
- 游戏里看到的深色矩形是 v1.1.14 我给 SettingsPanel 加的**半透明深蓝底板**
  （Image color 0.025/0.028/0.032 @0.82）——画蛇添足，按钮本身已是完整石条。

## 改动（PausePanel.BuildSettingsPanel）

- **底板移除**：SettingsPanel Image 改为全透明（保留零透明 Image 作点击阻挡层，不影响视觉），
  设置内容直接浮在石板主面板上（与左列三键同视觉语言）；
- **按键放大**：两个功能键 300×65 → **520×110**（=暂停主键同规格，原图 4.63:1 等比）；
- 布局容器 390×390 → 560×430；主页按键 y +120/-10、返回键 -170（标题区无碰撞，已核算）；
- 子页（音量/操作）为容器全拉伸，内容随容器放大自然获得更宽行距。

## 验收

1. Esc → 设置：无深色底板，两个 520×110 大键直接浮在石板面板上；亮度三态正常；
2. 子页滑条/键位表布局正常；返回链路（子页→主页→暂停三键）不变。

## 关联

v1.1.10 按键规范（完整功能图+ColorTint）；v1.1.18 设置独占居中。
