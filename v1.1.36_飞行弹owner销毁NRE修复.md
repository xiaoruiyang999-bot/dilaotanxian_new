# v1.1.36 修复：飞行弹结算 NRE（射手销毁后 owner 访问）

## 现象

敌方射手死亡销毁后，其仍在飞行的子弹命中时 `Projectile.OnTriggerEnter2D` 抛
NullReferenceException（owner 已销毁，`owner.transform.root` 访问假 null 对象）。

## 修复（Projectile.cs）

- **发射时快照 `ownerRoot = owner.transform.root`**（Init）：结算期只访问快照——
  射手已销毁时 ownerRoot 为 fake-null，`ownerRoot != null`（Unity 重载）自动放行正常结算，
  不再触碰已销毁对象；Update 扫掠与 ResolveHit 两处防自伤判定统一改用快照；
- **ResolveHit 头部销毁态守卫**：`this == null`（同帧 Destroy 后物理事件仍可能送达）；
- **OnTriggerEnter2D 的 other 判空**（物理极端帧）；
- `BuffManager.DamageDealtMulOf` 已容忍假 null（返回 1），无需改动。

## 验收

远程敌人开火后立刻击杀它：飞行弹命中玩家/墙正常结算，无 NRE；玩家弹行为不变。

## 关联

v1.1.13 位移段扫掠（同源双通道结算）；R12 对象销毁时序教训同族。
