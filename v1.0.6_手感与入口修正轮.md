# v1.0.6 手感与入口修正轮：攻击范围/武器挂点/统一入口/狼人外形/技能键标注

> 日期：2026-08-31  
> 状态：已落盘；编译 0 错误 0 警告；EditMode 门禁 9/9；三处功能冒烟通过（v0_5 直连重定向 ✓ / 角色外形行 ✓ / 狼人视觉管线 ✓）。

## 一、四项需求落地

### 1. 战士攻击范围 + 武器挂点
- `AttackData_PlayerKnife.attackRange` 1.8 → **2.4**、`AttackData_PlayerSpear.attackRange` 2.4 → **3.2**（约 +33%；预警=判定=武器视觉同源消费同一份 AttackData，自动同步）。
- `Player.prefab` 的 `WeaponPivot` localPosition (0,0,0) → **(0, 0.3, 0)**（武器上移至胸肩高度；三场景无实例覆盖，prefab 改动全局生效）。
- 事故记录：`prefab_set_property` 技能把子物体路径解析到了错误对象（改了一个 (0.82,5.33,0) 的无关 Transform），已 `git checkout` 恢复后改用**块感知 YAML 手术**（定位 WeaponPivot GO fileID → 其 Transform 块 → 单行替换 y）。教训：prefab 子物体属性优先 YAML 手术或先核对技能返回的 gameObject 名。

### 2. 偶发进入 v0.5 初始房间（统一入口收口）
- 根因：编辑器 Play 播放的是**当前打开的场景**——打开 v0_5_Dungeon（旧直连测试场景）时按 Play 就直接进旧地牢，无职业、走 PlayerStats 兜底数值。
- 修复：RunManager 新增 `redirectToPrepWhenNoClass`（默认 false，两个地牢场景实例置 true）——无职业直连地牢时自动 `LoadScene(v0_7_PrepRoom)` 回正式流程。
- 冒烟：Play v0_5_Dungeon → 自动落到 v0_7_PrepRoom 且职业选择自动弹出 ✓。

### 3. 狼人并入角色选择页（外形 ≠ 职业，互不冲突）
- `RunStateCarrier` 新增 `ChosenCharacter`（enum `CharacterSkin { Warrior, Werewolf }`，死亡保留、与职业独立）。
- `ClassSelectUI` 新增"角色外形"行：战士/狼人两按钮（即时写入载体，选中绿框高亮），布局在职业按钮与确认按钮之间。
- `FrameAnimator.SetWerewolfVisual(true)`（单向）：加载 `Resources/Art/Characters/Werewolf/{Walk_L,Walk_R,Idle_L,Idle_R}`（001.png 起连续编号，已从 Assets/Art/Werewolf 迁入 Resources 同构目录）；左右两套帧组**不做 flipX**；驱动目标切到运行时创建的 `WerewolfVisual` 子物体渲染器（战士自身 sr 隐藏）；体型按"渲染高 ≈ 战士视觉高×1.1、宽 ≈ 高×0.6"用 lossyScale 反解校准（玩家根缩放非均匀，不能均匀缩放直接套）。
- 应用链：`RunManager.ApplyLoadoutFromCarrier`（地牢）+ `PrepRoomManager.Start`（准备房，选完外形回看即所见）。
- 验证：Debug 菜单 `Tools/Dungeon/Debug Apply Werewolf Visual` 实测——WerewolfVisual 子物体生成、0 运行时错误。
- 【美术资产缺失/待办】狼人攻击/技能/四方向帧未产出：攻击回退 DOTween 武器挥砍（武器可见），仅侧面行走/待机；死亡灰显对狼人渲染器暂不生效；BeastIdle/BeastWalk/Transform 素材仍在 Assets/Art/Werewolf/ 未接入（未来"兽化"形态用）。

### 4. 技能槽按键标注
- `SlotBarUI` 四主槽左下角新增金色按键角标：小技能 **F** / 大招 **Q** / 武器技能 **R** / 道具栏 **C**（TMP 粗体，随槽创建一次）。

## 二、修改文件

代码：`SlotBarUI`（键标）、`RunStateCarrier`（外形）、`RunManager`（重定向+狼人应用+调试菜单）、`PrepRoomManager`（狼人应用）、`ClassSelectUI`（外形行）、`FrameAnimator`（狼人模式：TargetRenderer 路由+左右组驱动+帧加载+体型校准）。  
资产：两份 AttackData（attackRange）、`Player.prefab`（WeaponPivot y=0.3，块感知手术）、狼人 4 目录 20 帧迁入 `Assets/Resources/Art/Characters/Werewolf/`。  
场景：v0_7_ClassWeapon / v0_5_Dungeon 的 RunManager.`redirectToPrepWhenNoClass=true`。

## 三、验证

- 编译 0/0；门禁 9/9（场景扫描含全部本轮改动无 Missing Script）。
- 冒烟：v0_5 直连 → 重定向准备房 ✓；ClassSelectUI 层级含 CharLabel/Btn_Skin_Warrior/Btn_Skin_Werewolf ✓；Debug Apply Werewolf Visual → WerewolfVisual 生成 + 0 错误 ✓。
- 待人工实测：狼人实际视觉比例（体型系数在 WerewolfVisual 子物体 localScale 可微调）、武器上移后的手感、攻击范围体感。

## 四、遗留（衔接 v1.0.5 延期清单）

正式音效替换、金币/三选一/通关/Meta 重设计、Dash/Sprint 输入残留清理、PlayMode 冒烟、独立构建实测、狼人形态帧（Beast/Transform）接入、狼人攻击帧组。
