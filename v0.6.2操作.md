# v0.6.2 操作记录

> 版本主题：职业系统 + 法力值 + 武器框架 + 初始准备房间（计划书 4.4/4.5/4.6，第五章 v0.6.2）。
> 阶段 A（数据与系统层）+ 阶段 B（展台/选择 UI/TMP）合并记录。

## 改动文件清单

### 阶段 A
新建脚本（7）：`Class/ClassType.cs`、`Class/ClassData.cs`、`Weapon/WeaponData.cs`、`Weapon/WeaponInstance.cs`、`Weapon/WeaponBehavior.cs`、`Weapon/WeaponPickup.cs`、`Weapon/PlayerWeaponHolder.cs`
新建资产（9）：`Assets/Data/Class/` 三份 ClassData、`Assets/Data/Weapon/` 六份 WeaponData
修改：`PlayerStats.cs`（法力 + ApplyClass）、`PlayerUI.cs`（法力条 + 面板自动扩展）、`PlayerInteractor.cs`（ShowTemporaryHint）、`PlayerController.cs`（Skill 占位分发）、`PlayerInputActions.inputactions`（+Skill→F）、`README.md`

### 阶段 B
新建脚本（5）：`Common/TMPFontProvider.cs`、`Class/ClassCatalog.cs`、`Class/PrepPedestal.cs`、`Class/PrepRoomPlacer.cs`、`Class/ClassSelectUI.cs`
修改：`PlayerInteractor.cs`（Legacy Text → TMP 迁移）、`PlayerController.cs`（UI 打开屏蔽 + Esc 路由）、`Dungeon/Core/RunManager.cs`（展台注入 + LastChosenClass + 死亡重置武器）、`Weapon/PlayerWeaponHolder.cs`（Unequip + 默认攻击恢复）、`PlayerCombat.cs`（CurrentAttackData 访问器）、`README.md`

全程未改任何场景/预制体 YAML；不做 git 操作。

## 实现要点

### 阶段 A
- **职业系统**：ClassData SO（三属性上限/职业色/可用武器列表）；`PlayerStats.ApplyClass` 写上限并回满（HP 经 `Health.Initialize`），`CurrentClass` 记录，不选职业保持现状。
- **法力**：`AddMana`/`TryConsumeMana`，**无自动回复**；屏幕面板法力条（#3498DB 窄条，克隆体力条模式，面板高度不足时向下扩展、顶边不动）。
- **武器框架**：WeaponData（职业/行为/蓄力/弹夹射速/染色/图标）；WeaponInstance（弹夹/换弹/蓄力计时）；WeaponBehavior 分发骨架（近战→`PlayerCombat.SetAttackData`，远程/自身 TODO v0.6.3）；`PlayerWeaponHolder.Equip` 旧武器原地掉落 `WeaponPickup.Drop`（0.7×+染色）；WeaponPickup 职业校验"职业不符"拒绝。
- **Skill action**：F 键占位（v0.6.4 接 SkillExecutor）。

### 阶段 B
- **TMP 迁移**：`TMPFontProvider` 运行时创建微软雅黑动态 TMP_FontAsset（`TMP_FontAsset.CreateFontAsset(Font,…)`，Dynamic 多图集；注意 GlyphRenderMode 在 `UnityEngine.TextCore.LowLevel` 命名空间）。PlayerInteractor 的"按 E"标签/拾取列表/底注全部 Legacy Text → TMP_Text，**数值沿用用户调定**（面板 20×10、worldScale 0.09、字号 40，TMP 观感待复核）。
- **展台（PrepPedestal，运行时多色块，无 prefab）**：
  - 职业选择台：三层石灰基座 + 顶部悬浮三色棱晶（红/绿/蓝紫，DOTween 缓转+浮动，SetLink）；继承 Interactable 接入 E 候选，覆盖 `Interact` 不消耗（可重复打开 ClassSelectUI）；走近 3 格头顶显示 TMP 名称"职业选择台"（16pt）。
  - 武器展示台 ×2：矮基座 + 金色托盘 + 悬浮展示位；碰撞体禁用（E 由展示武器 WeaponPickup 承担，不抢候选）；`ShowWeapon(WeaponData)` 生成展示武器（浮动 tween），被拾走后托盘压暗空置。
- **ClassSelectUI**：屏幕 Overlay + GraphicRaycaster（无 EventSystem 运行时补 InputSystemUIInputModule.AssignDefaultActions）；深色面板 + 标题 28pt + 三职业按钮（职业色方块 + 名 22pt + 特色小字，选中=职业色边框高亮）+ 确认按钮 20pt（金色边框 DOTween 闪烁 ≈0.3s）→ ApplyClass → `RunManager.SetChosenClass` → `PrepRoomPlacer.RefreshWeapons`；默认高亮上次职业；Esc 关闭不生效。
- **输入屏蔽**：`ClassSelectUI.IsOpen` 静态标记，PlayerController 分发前拦截 Attack/Skill/Interact/Dash（点按钮不触发攻击），Cancel 优先关 UI。
- **RunManager**：`LastChosenClass`（一局内跨楼层与死亡保留）；`TryPlacePrepPedestals` 仅 floor==1 在出生房（房中心上方 y+2.4 横排三展台，避开出生点）生成，每次 Generate 后调用；死亡重开：`PlayerWeaponHolder.Unequip()`（武器不保留，恢复 prefab 默认近战）→ 展台重建并按上次职业刷新武器。
- **ClassData 加载**：`ClassCatalog` 编辑器走 AssetDatabase 按路径加载（场景无接线点且不改 YAML）；**打包构建需把 Assets/Data/Class/ 三份资产复制到 Resources/Class/**（代码已留 Resources.Load 分支）。

## 资产 GUID（备查）

- ClassData：Warrior `803882cfa2c21d0cb7193760f01de992`、Archer `7522a866b4344a3823342261139c6d87`、Mage `7087b99698180243b7923dd340d11b92`
- WeaponData：刀 `1cef4d0b9c8bb0ec6296a4fabcf765dc`、枪矛 `aa19f9cc77565094299ace416a3756f2`、弓箭 `cbb8b7dec5e2747fdcd181e4d37b3250`、连弩 `848cb5407571e72724a8db35afbd280c`、治疗法杖 `1f4be4f75f0b766e81b715bf80612db5`、能量法杖 `3f1f330344dca679a379e8aab51741df`

## 验证情况

- `dotnet build Assembly-CSharp.csproj`：0 警告 0 错误（新文件已补 csproj 条目；编辑器刷新后会自动重生成）。
- inputactions JSON：校验通过（7 actions，含 Skill→f）。
- 运行时行为（展台流程/TMP 观感/选择 UI）未经编辑器实测，待用户验收。

## 待用户在编辑器验收清单

1. **展台外观区分度**：职业选择台（三层基座+三色棱晶缓转浮动）vs 武器展示台（矮平顶+金托盘），是否一眼可区分；
2. **选择流程**：走近职业台头顶显示"职业选择台"→ E 打开 UI → 点职业按钮边框高亮（其余熄灭）→ 点确认边框闪烁 → UI 关闭 → 属性/法力条变为职业数值 → 两个武器展台出现对应武器（浮动展示）；
3. **输入屏蔽**：UI 打开期间点击按钮不触发攻击；Esc 关闭不生效、可再开；
4. **武器拾取**：E 拾取展台武器（职业不符提示）；拾走后托盘压暗空置；换武器旧武器原地掉落可捡回；
5. **死亡重开**：回 1 层展台重建，打开 UI 默认高亮上次职业，可直接确认；武器展台已刷新，玩家武器已卸下需重新拾取；
6. **TMP 观感**：中文渲染正常；"按 E"标签/拾取列表字号与 v0.6.1 Legacy 版观感对比（数值沿用，如偏差改 PlayerInteractor 的 hintFontSize/hintPanelSize/hintWorldScale）；
7. **法力**：法师选完法力条 100 满值；无自动回复；
8. 换层（floor>1）出生房无展台。

---

# 阶段 C（计划书 R4 修订）：独立准备场景 + 三项修复

## 改动文件清单

新建脚本（3，含 .meta）：`Class/RunStateCarrier.cs`、`Class/PrepRoomManager.cs`、`Class/PrepPortalInteractable.cs`
新建场景：`Assets/Scenes/v0_6_PrepRoom.unity`（+ 新 GUID meta `a5155c5a…`）
修改：
- `Class/ClassSelectUI.cs` — 布局修复（任务1）+ 改用 RunStateCarrier + OnDestroy 复位 IsOpen
- `Class/PrepPedestal.cs` — 名签参数序列化（任务2）
- `Class/PrepRoomPlacer.cs` — 重构 Spawn 签名（位置/父级）+ 展台→武器映射 + ReturnWeapon 归位
- `Weapon/PlayerWeaponHolder.cs` — `dropOldWeaponOnEquip` 开关 + `OnWeaponChanged(old,new)` 事件
- `Weapon/WeaponPickup.cs` — 拾取成功写入 RunStateCarrier.SetWeapon
- `Dungeon/Core/RunManager.cs` — 删 TryPlacePrepPedestals；Start 应用载体配置；死亡 → 加载准备场景
- `ProjectSettings/EditorBuildSettings.asset` — 加入两个场景（见下）
- `README.md`（清单 66→69）

## 流程图（R4）

```
启动 → v0_6_PrepRoom（准备场景：玩家 + 三展台 + 传送门）
  → 选职业（可反复重选，展台武器刷新）→ 拿一把初始武器（可反复换，旧武器自动归位展台）
  → E 传送门（未选职业/未拿武器 → 提示"请先选择职业和武器"拒绝）
  → LoadScene v0_6_ClassWeapon（地牢）→ RunManager.Start 读 RunStateCarrier 应用职业+武器
死亡 → 2s → LoadScene v0_6_PrepRoom（职业默认上次预置高亮；ClearWeapon 需重拿；展台按上次职业已刷新）
```

## 各项实现要点

### 任务 1：ClassSelectUI 布局修复
根因：面板 460×430 而按钮中心锚点 y=-110/-196/-282（面板半高仅 215），后两个按钮溢出面板下缘；确认按钮 y=-186 与第二行按钮（-196±36）重叠。修复：面板 600×500，标题锚顶 y=-40，按钮中心锚点 y=+120/+30/-60（间距 90、420×72），确认 y=-160，全部在面板内互不重叠。

### 任务 2：名签序列化
`nameLabelFontSize=16 / nameLabelPanelSize=120×22 / nameLabelWorldScale=0.03`（原硬编码 0.01 太小，默认调大到 0.03），标签可见时每帧应用（Inspector 即时生效，同 PlayerInteractor hint 模式）。

### 任务 3：初始武器只能一把 + 自动归位（bug 根因）
Editor.log 显示换武器循环其实发生（装备刀→拾取枪矛→拾取刀…）。根因：准备阶段沿用地牢规则"旧武器原地掉落在玩家脚下"——①展台被拿空后永不恢复；②脚下掉落物永远成为最近候选，按 E 立刻把刚换下的旧武器又捡回来，形成捡起-掉落-捡回循环，表象即"没法在展台换另一把"。修复：PlayerWeaponHolder 加 `dropOldWeaponOnEquip` 与 `OnWeaponChanged` 事件；PrepRoomManager 订阅并置开关=false，PrepRoomPlacer 持展台→武器映射，`ReturnWeapon(oldData)` 调对应展台 `ShowWeapon(oldData)` 归位。地牢内规则不变（开关默认 true）。

### 任务 4：独立准备场景
- RunStateCarrier（DontDestroyOnLoad 单例）：LastChosenClass/LastWeapon/HasLoadout；ClassSelectUI 确认写 SetClass、WeaponPickup 拾取写 SetWeapon、RunManager 死亡 ClearWeapon。
- RunManager：删地牢展台注入；Start `ApplyLoadoutFromCarrier`（ApplyClass + Equip，新玩家 Current 空不触发掉落）；死亡 2s 后 `SceneManager.LoadScene("v0_6_PrepRoom")`（旧清理逻辑删除——新场景新玩家天然满状态）。
- v0_6_PrepRoom.unity：由 v0_6_ClassWeapon 复制（新 GUID），删除 DungeonSystem/DungeonRoot/Grid/Floor/Walls 共 21 个 YAML 文档（悬空引用检查通过），保留 Player/Canvas(含 PlayerUI 与体力条)/主相机/Global Light 2D/EventSystem，新增 PrepRoomManager 对象（SceneRoots 同步更新）。
- PrepRoomManager.Start：建房间地板围墙（深灰蓝/深色块）→ 三展台（中心 (0,1.5)）→ 传送门 (0,4.6) → 玩家摆 (0,-2.2) → 置归位规则 → 若载体有上次职业立即 RefreshWeapons。
- EditorBuildSettings：v0_6_PrepRoom 第 0 位、v0_6_ClassWeapon 第 1 位（SampleScene 保留在后）。
- 静态状态：ClassSelectUI.OnDestroy 复位 instance/IsOpen；PrepRoomPlacer 静态展台引用随场景销毁天然失效、Spawn 重建。

## 验证情况

- `dotnet build Assembly-CSharp.csproj`：0 警告 0 错误（新文件已补 csproj）。
- 场景 YAML：删除 21 文档后无悬空 fileID；SceneRoots 与对象清单核对通过；两场景均在 Build Settings。

## 待用户在编辑器验收清单

1. **先核对 File > Build Settings**：应含 v0_6_PrepRoom(0) 与 v0_6_ClassWeapon(1)。若 Play 中 LoadScene 报"scene not in build settings"，是编辑器未重载该文件——重启编辑器或手动把两个场景拖入列表。
2. 打开 v0_6_PrepRoom：房间地板围墙、三展台外观区分、职业台名签大小（0.03 观感）、玩家出生在展台下方。
3. 职业选择 UI 布局：面板居中，三按钮在面板内纵向等距，确认按钮不重叠，色块/文字对齐。
4. 选职业→展台刷武器→拿一把→拿另一把时**手上武器自动归位原展台**（展示位恢复、无地上掉落）→可反复换。
5. 未选齐按 E 传送门 → 提示"请先选择职业和武器"；选齐后 E → 加载地牢，属性/法力条与手持武器正确。
6. 地牢内换武器仍是原地掉落规则（与准备场景不同）。
7. 死亡 → 回准备场景：UI 默认高亮上次职业、展台武器已摆好、玩家无武器需重拿、传送门放行。

---

# 追加：状态条布局改"场景可编辑" + 护甲色改钢灰

## 改动清单
- `PlayerUI.cs`：**删除全部强制重定位**（RepositionStatBars/PositionBarBelow/面板自动扩展及 Start 调用）；代码只做 ①路径查找绑定 ②缺条兜底创建（`CreateBarBelow`：与上方条同宽同锚点、间距 2、5px 窄条，创建后不再碰 Transform）③事件刷 fillAmount。
- 两个场景 YAML（v0_6_ClassWeapon / v0_6_PrepRoom）：补齐四条+各自背景条统一默认布局；ManaBar 补入；PlayerUI 组件补 manaBar 引用；护甲条色 → 钢灰。
- `PlayerWorldStatusBar.cs` armorColor 默认值 + `Player.prefab` armorColor 序列化值 → 钢灰 #708090。
- 文档：美术清单调色板护甲行 → #708090；自检清单 §2 补 #15、§3 补状态条配色/布局两行；本文件。

## 默认布局参数（两场景一致）
面板 PlayerStatsPanel 200×60（左下 20,20）；每行 = 深色背景条 + 彩色填充条（同位同尺寸叠加）：
- 行序（上→下）：护甲 8px / HP 14px / 体力 5px / 法力 5px；**统一宽 190、左缘 x=5、行距 6、顶距 5**；
- 背景条统一深色 (0.12,0.12,0.12,0.9)；填充色：护甲钢灰 #708090、HP 红、体力黄 #F4D03F、法力蓝 #3498DB；
- 锚点统一左上（0,1）pivot（0,1），场景里可自由再调，代码不再覆盖。

## 护甲色改动落点
两场景 ArmorBar fill 色（YAML）、PlayerWorldStatusBar.cs 代码默认值、Player.prefab armorColor 序列化值、美术清单调色板、自检清单 §3。（计划书无护甲色描述，无需改；敌人侧不涉及。）

## 验证
- `dotnet build Assembly-CSharp.csproj`：0 警告 0 错误；
- 场景 YAML 自检：两场景 ManaBar 等 5 组新文档完整、PlayerUI manaBar 引用在、无悬空 fileID、护甲色已更新。

## 用户提醒
- **编辑器正在运行且可能开着这两个场景之一：切回编辑器让 Unity 重新导入场景；若编辑器里有这两个场景的未保存修改，请先放弃（或另存备份），否则旧内存会覆盖本次 YAML 改动。**
- 布局现在完全归你：Scene 里直接拖/改 RectTransform 即可，进 Play 不会被代码重置；旧场景（v0_5 等）缺体力/法力条时代码仍会兜底创建（一次性默认布局）。

## 补丁：PlayerInteractor 提示标签参数同步（2026-07-28）
- 用户在 Inspector 调定新值：`Hint Font Size=16`、`Hint Panel Size=40×30`、`Hint World Scale=0.03`（原 40/20×10/0.09）。
- 已同步：Player.prefab（用户侧已存）、`PlayerInteractor.cs` 脚本默认值、《版本实施自检清单》§3 调定值行。
