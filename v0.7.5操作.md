# v0.7.5 操作记录

> 版本主题：战士完整实装 + 美术管线启动（《v0.7_Demo玩法计划书.md》§九 执行卡 + 《数值与角色设计任务书》§一~§四）。
> 执行文档：`v0.7.5_正式计划.md`（含 6 项用户决策记录与并行文件归属表）。
> 执行模式：主代理派单 + 多子代理并行（Track A 美术线 / Track B Buff 系统+双分支 / Track C 裸绞燃命被动 / 后续专项），主代理核验收口。

## 改动文件清单

### 技能线（Track B/C + 专项）

**新建脚本**
- `Skill/BuffManager.cs`：Buff 运行时系统。AddBuff（同 id 刷新替换）/RemoveBuff/ClearAll/SetImmune；四通道只读属性（AttackSpeed/MoveSpeed/DamageTaken/DamageDealtMultiplier，空表返回 1）；静态 `DamageDealtMulOf(GameObject)`；虚弱链=自然到期自动挂 id+"_Weakness" 负值 buff，不递归。
- `Weapon/WeaponPassives.cs`：长剑连击加速（命中 +5% 攻速/层、15% 封顶、1.5s 断连清零）+ 长枪贯穿（配合 WeaponHitbox 命中计数）。武器识别走 `WeaponData.ChargeRule`（FanScale=剑/RectScale=枪），不认名字。

**修改脚本**
- `Skill/SkillType.cs`：追加 Buff=1/DashExecute=2/BurnLife=3（只追加，旧资产映射不变）。
- `Skill/SkillData.cs`：+Buff 区 8 字段（时长/减伤/攻速/移速/输出/虚弱三项）+裸绞区 7 字段（冲刺距离/时长/减伤/双阈值/真伤/判定半径）+燃命区 8 字段（免疫/窗口/三分支强化数值），全序列化可配 + OnValidate 钳制。
- `Skill/SkillExecutor.cs`：Buff/DashExecute/BurnLife 三个执行分支；裸绞=rb.DOMove 冲刺（SetLink）+终点 OverlapCircle 取最近+先阈值后真伤；燃命=ClearAll+免疫窗口+按 `RunStateCarrier.ChosenSkillBranchIndex` 联动（窗口内下次施放消耗强化）；订阅 `PlayerStats.OnClassApplied` 重装配三槽（准备房间选职业后立即可用，修复"Start 一次性装配"缺陷）；Awake 挂 BuffManager/WeaponPassives。
- `Combat/DamageContext.cs`：+`trueDamage` 通道（默认 0，既有构建点零差异）；`Combat/DamageResolver.cs`：真伤分支跳护甲；`Enemy/EnemyHealth.cs`：+`TakeTrueDamage`。
- `Combat/WeaponHitbox.cs`：+输出倍率叠乘（DamageDealtMulOf）+`MultiHitDamageMul`（贯穿：第 2 目标起加倍率并追补首目标）；`Combat/Projectile.cs`：命中时刻叠乘输出倍率。
- `Player/PlayerCombat.cs`：攻速通道（三阶段时长/射击间隔 ÷ AttackSpeedMultiplier）；`Player/PlayerMovement.cs`：移速通道；`Player/Health.cs`：受击减伤通道（护甲结算之前）。
- `Player/PlayerStats.cs`：+`OnClassApplied` 事件（ApplyClass 末尾发）；**去重复**：删全部被 ApplyClass 覆盖的序列化假入口字段（**七维唯一真值=ClassData**，moveSpeed 后收编为第七维，组件零序列化字段）；R/L 钳制从 OnValidate 迁入 ApplyClass。
- `Class/ClassData.cs`：+`moveSpeed`（第七维，默认 5）；三份职业资产同步补字段（战士 5.0，任务书 §一 暂定）。
- `Player/PlayerInteractor.cs`：highlightScale 脚本默认 1.1→1.25（与 prefab 生效值同步，审计 #10）。

### 美术线（Track A + 素材专项）

- `Player/FrameAnimator.cs`（新建）：通用帧切换器（命名动画组 Sprite[]+fps+loop+pingPong、Play/Stop/flipX）；自读 rb.linearVelocity 驱动：动→walk（右向素材 flipX 镜像）、停→idle 呼吸 pingPong；fps 序列化可调（**用户调定 walk=12/idle=3**）；帧缺失保留既有视觉兜底；序列化组空时 AssetDatabase 硬编码路径兜底（打包需 Resources/Art/Characters/Warrior/）。
- `Assets/Art/Characters/Warrior/Walk/warrior_walk_1_1~6.png`：行走 6 帧（2048 白底原图经 tools 管线抠图→256×256 透明，PPU 50/Point/脚底 pivot；**素材实为右向**，flipX 镜像）。
- `Assets/Art/Characters/Warrior/idle/warrior_idle_0~4.png`：待机呼吸 5 帧（同管线抠图，同导入设置；正面帧停步播放不镜像）。
- `Player/PlayerController.cs`：Awake Get-or-Add 挂 FrameAnimator。
- **四方向+攻击/技能骨架（v0.7.5 美术线续）**：FrameAnimator 扩 479 行——四方向驱动（主轴判定+0.1s 滞回，walk_front/walk_back 目录扫描加载 1~8 帧可变，缺失两级回退）；`PlayAttack(isSpear,duration)`/`PlaySkill(name,duration)` 覆盖播放（帧数÷时长精确取帧、播放期隐藏 WeaponPivot 武器视觉、播完回驱动）；PlayerCombat.StartWindup 触发攻击组（剑/枪按 ChargeRule 识别，时长=三阶段合计÷攻速倍率）。素材目录规范：WalkFront/WalkBack/AttackSword/AttackSpear/Skill，前缀+1 连续编号。编辑器内模拟验证（REST+临时 Editor 脚本，已清理）：现状零回归 7 断言 + 全组模拟 19 断言全过。
- **素材管线**：`tools/cutout_white_bg.py`（白底→透明：洪水填充去背景/阴影/水印+连通域+边缘 bleed+LANCZOS 缩 256）、`tools/cleanup_frames.py`（噪点连通域过滤+边缘白边 bleed，白色部件结构保护）；项目根 `.venv`（pillow/numpy/scipy，已入 .gitignore）；备份 `tools/backup/`、预览 `tools/preview/`。

### 消耗品统一（专项）

- `Item/ItemPickup.cs`：视觉分岔——`ConsumableData.Icon != null` 显 sprite（无染色，按 bounds 缩放 0.45 单位），空走色块+高光条。
- `Item/ConsumableData.cs`：复用既有 icon 字段（Tooltip 更新），未加重复字段。
- `Dungeon/Content/InteractableSpawner.cs`：商店改造——第一排展台 ConvertSupplyToConsumable（留展台壳、拆 Orb、按 SupplyType 映射摆测试药三件套）；撤 v0.7.3 第二排 SpawnShopConsumables。
- `Dungeon/Interaction/ChestInteractable.cs`：两处 ManaBottlePickup.Spawn → ItemPickup.Spawn(Item_ManaPack)（行为变为入包按 C，用户已确认）。
- `Dungeon/Interaction/ManaBottlePickup.cs`：删除（引用清零，csproj 同步移除）。
- `Assets/Data/Dungeon/InteractableTable_Shop.asset`：删重复的第 4 条（现血/甲/法力三座展台）。

### 资产数值（任务书回填）

- `Class/Class_Warrior.asset`：HP 8→130、AR 8→70、MP 30→100、ATK→40、CR→0.1、CD→1.5、R→0.4、L→1（缺失字段全部显式落盘）。
- `AttackData_PlayerKnife.asset` attackDamage 1.1→20；`AttackData_PlayerSpear.asset` 1.3→30；`Weapon_Warrior_Spear.asset` chargeFullDamageMul→2.2。
- `Data/Skill/`：屹立不倒（CD12/5s/MP0/减伤50%/攻速1.25/移速1.2/虚弱3s 0.5·0.65）、强力一击（CD10/5s/×1.75）改 Buff 型并回填；新建 `Skill_Warrior_Garrote.asset`（裸绞：CD30/MP25/冲刺3u·0.2s·减伤0.3/阈值0.3·0.15/真伤45）；燃命改造（CD45/MP30/免疫3s/窗口10s/三分支强化数值）；`SkillBranch_Warrior.asset` 扩 3 条；SkillCatalog 清单同步。

### 文档

- `now_use/README.md`（各 Track 同步）；`版本实施自检清单.md` §2 #24~28、§3 七行新增（本文件）；`美术策略变更说明.md` v2.1；`v0.7_Demo玩法计划书.md` 执行卡标完成；`项目进度报告.md` 更新。

## 验证情况

- `dotnet build Assembly-CSharp.csproj`：**0 错误 0 警告**（每 Track 独立验证 + 主代理合并后复跑）。
- 新 GUID（BuffManager/WeaponPassives/FrameAnimator/裸绞 SO/6 帧）全项目查重无冲突；csproj Include 无重复无遗漏（并行交叉补丁已收口核验）。
- 主代理核验：四通道接入点、斩杀顺序逻辑、分支表裸绞引用、数值落盘逐项 grep 确认；素材处理 before/after 接触表+边缘放大图目检（噪点/白边/水印清除，胸毛/眼睛/挂坠无误伤）。
- **REST 实机核验（unity-skills，2026-08-06）**：asset_refresh 重导入 → 编译 0 错 0 警；`v0_7_PrepRoom` Play 截图（`Assets/Screenshots/v075_idle_play.png` / `v075_idle_c.png`）：idle 造型渲染正常、三测试药拾取列表/四槽/状态条显示正常、运行期零报错；Play 会话已退出。
- 其余实机手感项（技能数值/被动/镜像/呼吸动态）待用户 Play 验收。

## 与计划的偏差

- PPU 用 50 而非计划 170：玩家身体已是 warrior_idle（512×488 PPU100），walk 帧按画布世界尺寸对齐取值 50（角色≈1.5 单位高）。
- 裸绞从"独立槽"改为"小技能第三分支"（用户决策，同时消解燃命双联动悬案）；武器技能槽保留占位（用户决策）。
- 商店第二排（v0.7.3 地面三件套）撤除——用户决策"展台上放测试药三件套"，一排无重复。
- ConsumableData 未新增 iconSprite 字段——既有 icon 字段语义相同，复用。
- 素材白描边实际与背景连通，抠图第一步即去除，剥皮兜底未触发。

## 遗留风险

- **木桩 DPS 实测记录未做**（验收时用户报值回填任务书 §七）。
- 燃命联动裸绞适配值（真伤 75/斩杀回血 10）待评审确认。
- 裸绞冲刺可穿墙（占位，未做碰撞截停）；蓄力中放技能无打断互斥（v0.7.4 遗留）。
- Player.prefab 有 9 个已删字段的 YAML 残留键（七维 8 个 + moveSpeed，无害，编辑器打开保存即剥离）。
- 数值重复审计 🔴 排期项：敌人 HP 双字段（#3）/attackData 五处引用（#5）→ v0.7.8；EnemyStats 弃用死值（#4）/职业色三处（#11）→ 中期；🟡 补给口径 2vs4（#15）/燃命强化独立维护（#16）→ 设计 D。
- 打包构建：技能/职业/道具资产需复制 Resources/（Skill/Class/Item 三目录），walk 帧需复制 Resources/Art/Characters/Warrior/Walk/。

## 待用户在编辑器验收清单

> 场景 `v0_7_PrepRoom` → `v0_7_ClassWeapon`；Game 视图 1920×1080 或 Free Aspect；Play 前切回编辑器重导入。

1. **行走**：6 帧循环、左右镜像正确（素材右向，左走镜像）、停步定格；角色≈1.5 单位高。
2. **准备房间技能**：选战士确认 → 三槽立即显示（屹立不倒/燃命/—）；展台拿武器 → R 槽旋风斩；F/Q/R 扣蓝起 CD 可施放。
3. **屹立不倒**：承伤减半、攻速移速提升、结束 3s 虚弱；**强力一击**（分支1）：木桩 60→105；**裸绞**（分支2）：150HP 怪 ≤45 斩杀、>45 真伤 45、Boss 只真伤不死；**燃命**：清虚弱+对应分支强化。
4. **武器被动**：长剑连砍攻速递增（15% 封顶、停手 1.5s 回落）；长枪戳双目标 ×1.15。
5. **消耗品**：商店一排三展台测试药、宝箱法力包入包按 C、准备房间地面三件套不变。
6. **新数值**：战士 HP130/AR70/MP100、长剑普攻 60、满蓄+强力一击 157.5。
7. **回归**：v0_4/v0_5 旧场景三槽"—"、行为不变（无职业数值失效为已接受项）。
