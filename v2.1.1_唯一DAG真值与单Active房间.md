# v2.1.1 唯一 DAG 真值与单 Active 房间实施记录

> 日期：2026-09-20
> 对应计划：`计划书/v2.1.1_唯一DAG真值与单Active房间.md`

## 已落地

- `DungeonGraphGenerator` 生成 10～12 层、固定 3 条独立主分支的左到右 DAG，只投放 Start、物资、贤者、商店和 Boss。每条主干有 1～2 个持续 1～3 层的局部分叉，支路只汇回原主干；三条主分支仅共享 Start/Boss。
- `ActiveRunData` 持有唯一图、当前节点和资源。新局从 Start 进入；待路线初始化的旧档保留资源，抛弃旧预览图，按主 Seed 建新 DAG 从 Start 进入。已有合法新图按原 Seed、节点和完成状态恢复。
- `DungeonBuilder.BuildSingle` 每次只绘制当前节点一间房，用 RoomPlan 验证左侧入口到右侧出口的玩家口径通路。物资/贤者房复用战斗内容；商店保留现有货架。旧实体地牢生成器仅保留兼容，不再作为新 Run 入口。
- `DungeonManager` 用当前节点驱动房间、奖励、右出口和存档。清房只完成目标；物资/贤者房须在现有三选一界面领取临时奖励后才能选直接后继。地图从 ActiveRun 读取状态并提供合法后继按钮，Tab 可查看。
- 格兰仪式厅在新 DAG 中按左入口绘制，结算宝箱在中右部、通关门靠右侧。恢复已清 Boss 节点时重建厅内装饰和奖励插槽，不重复刷敌人。
- `RunManager` 恢复有效生命、护甲和法力快照；缺少新标记的旧档仍保留真实资源，只有 5 生命、0 护甲、0 法力的 Prefab 坏快照会先应用职业满状态再建立有效快照。怒痕、金币、击杀、遗物与临时攻击加成继续保留。

## 验证

- Unity 6000.5.4f1 编译无错误。
- EditMode：199/199 通过；其中含 10,000 Seed 三主分支隔离、局部分叉数量、狼人坏快照修复与真实旧资源保留、墙外黑边镜头钳制、含墙边界、左右 Boss 模板以及正式场景五类单节点构建。
- PlayMode 正式场景冒烟走查通过：模拟旧版 5 点生命、0 护甲的狼人存档，确认进入后生命与护甲均为上限，再从 Start 连续推进 10 个节点至 Boss；每次只保留当前一间房，Boss 节点 ActiveRun 可回读。
- 最终完整 EditMode 重跑 199/199 通过；场景构建测试使用 PreviewScene，最终 Unity Console 错误数为 0。
- `git diff --check` 通过。`.vscode/settings.json` 是会话前已有修改，未触碰。

## 延续事项

- v2.1.2 接统一暂停与模态焦点；v2.1.4、v2.1.5 将临时三选一替换成刻印和物资正式奖励；v2.1.6 接入正式商店库存与付费揭印。
- 本版商店沿用货架原型，跨中途强退的购买事务持久化仍由 v2.1.6 补齐。
- 自动走查直接调用规则层完成战斗/奖励与路线选择；鼠标点击三选一、地图按钮和手动 Boss 战的画面体验仍需在编辑器中人工验收。

## 2026-09-21 镜头边界与路线分支修正

- 相机从仅限制下、左、右三边改为限制上、下、左、右四边，并把最终边界钳制放在像素对齐和震动之后，避免最终偏移再次越界。
- `DungeonBuilder` 的相机范围纳入右墙与上墙的完整一格；新 DAG 商店房使用完整横向房高度，确保 9 单位正交半高的视口能够被围墙包住。旧生成器的商店支线尺寸保持兼容。
- 路线图升级为固定三主分支 DAG：起点固定展开 3 条互斥主干，每条主干生成 1～2 个局部分叉并汇回自身。节点携带 `MainBranchId`，内部边禁止跨分支；移除 Start/Boss 后必须恰好得到三个连通分量。
- 路线图增加生成版本。载入旧稀疏图时保留生命、护甲、法力、职业资源、星蓝币、遗物和击杀等资源，按原主 Seed 重建新版图并从 Start 进入。
- 新增四边视口钳制、地图含墙边界和新 DAG 商店房尺寸测试。图形结构只在创建新 Run 时确定，已经保存的 DAG 不会被中途重掷。
- 补充场景卸载清理的空引用防护，避免 Unity 先销毁 Tilemap、后销毁 `DungeonManager` 时产生 `MissingReferenceException`。
- 增加 `resourcesInitialized` 存档标记；旧档保留已有真实资源，Prefab 默认的 5 生命、0 护甲、0 法力不再覆盖狼人职业属性，修复后首帧建立有效快照。
