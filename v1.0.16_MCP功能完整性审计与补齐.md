# v1.0.16 MCP 功能完整性审计与补齐

> 日期：2026-09-01  
> 状态：代码与接线修复已落盘；UnitySkills 服务当前离线，等待编辑器恢复后执行编译与回归。

## 审计范围

以 `feature/battle-logic-loop` 的 `a7f358d` 和 MCP 汇总分支 `0788d23` 为基线，按代码、数据资产、Prefab 接线和运行时可达性检查：五行为 AI、感知/LOS、多招式、远程弹、冲锋、召唤、词缀、距离预算编队、确定性随机源、Boss 阶段。

## 已确认保留

- Melee / Ranged / Skirmisher / Charger / Summoner 五行为及四份 BehaviorConfig。
- EnemyPerception 的 LOS、后撤/横移实体探测与 Trigger 过滤。
- Random / Distance / Sequence / Weighted 多招式池。
- 远程蓄力 LOS 宽限与同源预警方向。
- 冲锋位移、召唤落点实体校验、小兵上限与清房登记。
- Swift / Tough 词缀、按房间距离递增的词缀概率。
- Encounter budget、距离分带、角色编队与高压组合限制。
- AI/Combat 独立 seed 派生随机源。
- Boss 二阶段攻击池与冷却倍率。

## 本轮确认缺失并补齐

- 统一 `Projectile` 恢复按碰撞体或根对象 Layer 识别目标。
- 恢复从命中子碰撞体向父级查找 `IDamageable`。
- 恢复 MCP 投射物伤害数字 `DamagePopup`。
- 修正非 Trigger 实体挡弹规则：墙、门、障碍和无受伤接口实体均会终止投射物，不再只认 Default 层。
- `Enemy.prefab` 恢复 `EnemyPerception` 组件，所有特殊怪 Variant 继承 LOS 与移动方向探测；修复“脚本存在但 Prefab 未挂载”导致远程攻击永远被 LOS 门控拒绝的问题。
- 新增 `McpFeatureCompletenessTests`，锁定五类编队/词缀和四类特殊怪 Prefab 的核心接线。

## 门逻辑

按用户最新要求，本轮不修改门逻辑；保留当前本地 `RoomTrigger` 与既有门回归测试。
