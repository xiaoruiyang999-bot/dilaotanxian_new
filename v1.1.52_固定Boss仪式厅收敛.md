# v1.1.52 固定 Boss 仪式厅收敛

## 目标

参考暗石祭祀厅构图，将 Boss 房实现为一间手工定义的固定特殊房：地图 seed 只决定它在地牢中的位置，房间尺寸、入口约束、地面与墙体、法阵、四柱、祭坛、火把、杂物、敌人和结算奖励的位置均保持一致。

## 已落地的房间契约

- Boss 房只能选在满足最小路径距离、邻房位于下方的叶子节点，固定占用完整 `2×2` 粗格；正式生成与保底生成都只有南入口，地图 seed 只能改变它的位置。
- 原 Boss 格固定作为 `2×2` 左下锚点；旧 `bossCellSpan` 序列化字段即使被误改也不会改变手工房尺寸。
- 门洞固定为真实 `2` 格宽：优先靠近整厅中轴，同时将完整门宽钳在建立连接时的相邻粗格内部，避免半边门后仍是墙。
- 每个房间先完整铺四边墙，共享 Tilemap 格允许重复写，最后只按真实 `RoomConnection` 开门；修复跨格房仅半边接邻房时另一半外墙被整体漏掉的问题。
- Boss 房不走普通房随机塑形、随机障碍、随机装饰与世界坐标地皮噪声；地板和墙体变体使用固定索引，墙体横竖朝向只读取 Boss 自身局部拓扑，不会被相邻普通房随机轮廓墙污染；`Resources.LoadAll` 结果先按名称排序，跨导入环境也保持稳定。
- `BossRitualRoomTemplate` 只产出一份最终 `RoomPlan`。四个柱基各占 `2×2` 逻辑格，表现柱中心直接从这 16 个障碍格求得；碰撞、寻路和画面不再各写一套坐标。
- 中央法阵、四柱、顶端台阶/祭坛/挂旗、六盏火把、两侧桶与碎石、尸骨位均挂在 `BossRitualRoom_Fixed` 固定层级；没有正式图片时生成明确命名的占位视觉，不添加装饰碰撞体。
- Boss 固定内容流使用常量 seed，与地图 seed 解耦。最多 12 个敌人插槽全部先吸附最终 `RoomPlan.SpawnCells`，生成前再做距门与 NonAlloc 实体复核；若某个固定点临时被墙、敌人或障碍占用，会顺序尝试下一个固定安全点。建层期间仅忽略随后会被 `DungeonManager` 迁回新 Start 房的旧玩家碰撞，避免上层位置改变新层 Boss 槽位。
- 清房宝箱与传送门提供三组固定候选插槽；按固定优先级选择通过 `SpawnCells + NonAlloc` 复核的位置，再使用同样的最终白名单作确定性兜底。两者使用固定世界格偏移并额外保留至少 `2.75` 世界单位中心距，覆盖当前宝箱 2.4 宽与传送门 2.8 直径的 Trigger 体；从 17×17 最小正式房到 97×97 极限房都不会叠放或随房宽散开。
- 物理复核统一使用缓存 LayerMask 与静态 16 槽 Collider 缓冲区，Trigger 仍不参与实体阻挡，固定插槽循环不产生逐次分配。

## 正式素材接口

把透明 PNG 以以下精确文件名放入 `Assets/Resources/Art/SpecialRooms/BossRitual/`，现有代码会自动替换对应的 `_MISSING` 占位物，无需再改生成逻辑：

| 文件 | 用途 | 当前回退 |
|---|---|---|
| `rune.png` | 中央圆形符文/法阵 | LineRenderer 圆环与双三角 |
| `pillar.png` | 四根独立石柱 | `WallProps/prop_01` + `Stones/prop_31` |
| `stairs.png` | 顶端台阶 | `Stones/prop_29` |
| `altar.png` | 祭坛或王座基座 | `Stones/prop_30` |
| `banner.png` | 顶墙挂旗/帷幕 | 程序色块与符号 |
| `torch.png` | 壁挂火把；当前接口先接静态首帧 | 程序灯座与火焰色块 |
| `bones.png` | 尸骨/残骸堆 | 程序骨条 |

导入约定：`Sprite Mode = Single`、透明背景、Point 过滤、关闭 Mipmap、建议 `PPU = 85`。图片统一按“南入口、祭坛在上方”的正向构图制作，运行时不再随机旋转房间。若后续提供火把序列帧，再在相同槽位上补统一播放器，不改变房间模板。

项目现有素材已经承担暗石地板、石墙、桶与碎石；木箱、骷髅头、Boss 门楣可作为第二批氛围增强素材，不影响当前房间完整生成。

## 验证记录

- Unity Roslyn 真编译：`Assets/Scripts/now_use` 共 143 个脚本，0 个编译错误。
- 最终源码另经 Unity Roslyn 编译为独立运行时/测试程序集并执行 `BossRitualRoomTemplateTests`，结果为 `8/8` 通过、`333` 次断言；覆盖 17×17 最小正式尺寸、97×97 极限尺寸及四组常用长宽组合。
- 固定模板纯数据复核：四柱中心 `4` 个、柱基障碍 `16` 格、敌人插槽 `12` 个、奖励候选 `6` 个；所有内容插槽均属于最终 `SpawnCells`。
- 门洞复核：默认 61 格宽内部的南门起点为 `29`，门心距整厅中轴 `1.5` 格，完整 2 格门宽处于真实相邻粗格内；模板对异常北入口的防御映射也有独立测试，但正式生成不会采用该方向。
- 生成器离线 1000 seed 门禁：`0` 失败，Boss `100%` 为满足距离要求的南入口叶子、完整 2×2，且不与扩格房重叠；Boss 距离 `3..7`，位置覆盖 `39` 个不同粗格坐标，Combat 扩格率 `86.2%`（4722/5478）。

本机的 headless Editor 授权不可用，因此未用批处理模式擅自进入 Play；模板测试改为用 Unity 自带 Roslyn 编译最新源码后直接执行 NUnit。运行时画面仍需从正式流程自然进入 Boss 房验收，补入正式素材后沿同一路径复验即可。
