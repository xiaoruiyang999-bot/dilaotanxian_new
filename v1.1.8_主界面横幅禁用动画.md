# v1.1.8 主界面横幅禁用动画（序列帧）与通用 UI 帧播放器

## 1. 素材

源：`E:\WeGameApps\The animation of Unity\主界面素材\001..010.png`（1242×170 十帧）。
实测动画结构（整条均匀变化）：001-005 增亮（亮度 42→52，激活/闪亮）→ 006-007 保持 → 008-010 变暗+边角碎裂（不透明度 90%→75%，禁用/碎裂）——一段完整的"Disabled"序列（暗起→亮闪→碎裂）。

导入：`Assets/UI/MainBar/`（母版）+ `Assets/Resources/UI/MainBar/`（运行时）各 10 张，
Single/Point/不压缩/**PPU 1**（原生像素 1242×170）/Border 0（整帧动画不做 9-Slice）。

## 2. 新组件

| 文件 | 说明 |
|---|---|
| `UI/UIFramePlayer.cs` | 通用 UI 序列帧播放器：`LoadFrames(dir)`（001..NNN 数字扫描，编辑器/构建同构）；`Setup(frames, image, index)`；`Play(from, to, onDone)`（含两端、可倒放、播放互斥、完成回调可再 Play）；`ShowFrame/Stop`。**unscaledDeltaTime**（timeScale=0 暂停期间照播）；每帧零分配 |
| `UI/MainMenuBar.cs` | 主界面横幅：底部居中 1242×170 石板条 + 右端取消按钮（三态石板+TMP"取 消"）。**点取消 → 播完整 Disabled 序列（001→010）→ 完成后收起**；播放期间按钮禁用防连点。Open 静止显示亮态帧 004。`MainMenuBar.Open()/Close()` 静态入口；**编辑器 F8 预览开关**。帧缺失回退纯色条 |

## 3. 关键决策

- 取消播**全序列**而非仅 006-010：素材集即"Disabled 动画"整体（亮闪→碎裂是完整 VFX）；001-004 的暗起作为闪亮前奏。要改成"只播碎裂段"改 `OnCancelClicked` 里 `player.Play(4, frames.Length-1, Hide)` 一处即可。
- canvas sortingOrder=190：选择菜单(200/205)/暂停(220)之下，金币(90)/小地图(100)之上。
- 播放互斥 + 按钮 interactable=false：连点取消不会叠加动画或提前关闭。
- 未绑 Esc（PausePanel 已占用项目 Esc=取消/关 UI 契约，独立横幅不抢键）。

## 4. 验收

1. 编译无错 → Play 任意场景按 **F8**：底部出现横幅（亮态）+ 右端取消按钮。
2. 点取消：横幅播放 暗起→亮闪→碎裂 十帧（12fps ≈0.83s）后整条收起。
3. 播放期间按钮无响应（防连点）；再次 F8 可重新打开（回到亮态静止帧）。

## 5. 后续接入

正式入口 `MainMenuBar.Open()`（如准备房间/主菜单场景自启）；F8 仅为编辑器预览键（#if UNITY_EDITOR，构建无此键位）。
其余 UI 若需序列帧动画（如面板开关演出），直接复用 `UIFramePlayer`。
