# v1.0.13 四种怪物类型还原（MCP 分支五行为敌人系统）

> 日期：2026-08-31  
> 状态：已落盘；编译 0/0；门禁 10/10；实例化核验：Charger 行为配置✓、Boss 阶段控制器+P2 招式池✓、Summoner 召唤物 prefab✓（资产层本来就接好）。

## 根因

v0.7.5 合并用 256 行的纯近战 EnemyAI 替换了 MCP 分支 564 行的**五行为 AI**，并删除了 BossPhaseController；AttackData 的特殊攻击标记字段（isCharge/isSummon 等）也从类里消失——但**数据资产与变体接线一直完好**（BehaviorConfig×4、Charger/Skirmisher/Summoner 变体的 behaviorConfig/attackData 覆盖、Summoner 的 summonPrefab→Enemy_Minion 都在），只是没有代码消费它们。

## 还原内容

### 1. EnemyAI（分支版整体回归，565 行）
五种战斗行为：**Melee** 追击 / **Ranged** 风筝（保持距离+后撤+横移探测）/ **Skirmisher** 游击（突进→攻击→强制后撤）/ **Charger** 冲锋（进范围蓄力直冲）/ **Summoner** 召唤师（保持距离+定期召唤+仆从存活追踪与上限）。含受击仇恨、返回巡逻、面朝目标等既有语义。

### 2. EnemyCombat 特殊攻击层
- **召唤**（isSummon）：Active 开始生成小兵——落点实体碰撞校验（8 次重摇防嵌墙卡清房，v0.5.4.4.4 教训保留）、登记 Room.RegisterEnemy（计入清房）+ AI.RegisterMinion（存活追踪）、上限检查。
- **冲锋**（isCharge）：Active 期间沿锁定方向高速位移（移速×chargeSpeedMultiplier），撞墙/撞目标（ChargerCollisionLayer|TargetLayer 射线）即停；近战判定照常。
- **Boss 招式池**：SetAttackPool（池内随机换招）+ CooldownScale（P2 攻速乘数）。
- IsAttacking 属性 + HasProjectileLineOfSight（远程 AI 开火前问询，perception 驱动）。

### 3. 配套
- AttackData 类补回 isCharge/chargeSpeedMultiplier/chargerCollisionLayer/isSummon/summonPrefab/summonCount/summonRadius/obstacleLayer 字段（资产里的同名字段数据无缝复活）。
- EnemyController 补 MoveTowards(方向, 倍率) 重载 + SetChargeVelocity。
- BossPhaseController 文件+meta 从 git 原样还原（原 guid）。

### 4. Prefab 手术（Boss）
- 基座 Enemy.prefab：重新挂 BossPhaseController（**沿用旧 fileID 6660000000000000001**，默认禁用——普通敌人不受影响）。
- Enemy_Boss 变体：恢复三条覆盖（m_Enabled=1 + P2 招式池 [EnemyBossMelee, BossBig]），并**补 Array.size=2 显式覆盖**。
- 实例化终验：Boss 16 组件含启用的 BossPhaseController，phase2Attacks=[EnemyBossMeleeAttackData, AttackData_BossBig] ✓。

## 新教训（回写）

- **空数组的变体元素覆盖必须带显式 `Array.size` 修改**，否则静默丢弃（Boss P2 招式池第一轮没生效的根因）。
- 手写 guid 后必须数位数（本轮多打一位 8 导致组件被静默丢弃，靠实例化核验才抓到）。

## 待人工实测

冲锋怪蓄力冲撞撞墙停止、游击怪打一下就后撤、召唤师保持距离召唤小兵（上限 4）、Boss 半血进 P2（染红脉冲+换招+攻速提升）、远程射手风筝走位。数值调参入口：BehaviorConfig_* 资产（Data/Dungeon/）与 AttackData_* 资产。

## 修改文件

EnemyAI（分支版整体）、EnemyCombat（特殊攻击层+Boss API）、EnemyController（两个接口）、AttackData（8 字段+属性）、BossPhaseController（git 还原）、Enemy.prefab（基座组件）、Enemy_Boss.prefab（三条覆盖+size）。
