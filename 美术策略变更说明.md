# 美术策略变更说明：程序员美术（Programmer Art）

## 变更摘要

| 项目 | 原方案 | 新方案 |
|------|--------|--------|
| 墙壁 | 像素瓦片（32×32 PNG） | 纯色方块（Unity内置Square Sprite + 灰色染色） |
| 地板 | 像素瓦片（32×32 PNG） | 纯色方块（Unity内置Square Sprite + 浅色染色） |
| 角色 | 像素角色Sprite + 帧动画 | 纯色圆球（Unity内置Circle Sprite + 职业色染色） |
| 敌人 | 像素敌人Sprite + 帧动画 | 纯色方块（Unity内置Square Sprite + 红色染色） |
| 相机 | Pixel Perfect Camera + PPU配置 | 普通Orthographic Camera |
| 道具/武器 | 像素图标Sprite | 基础几何形状（三角形、菱形等）+ 染色 |

> 💡 **核心原则**：游戏性验证阶段，用颜色和形状区分所有对象；美术替换阶段，只需把纯色Sprite换成正式Sprite，其他代码和结构完全不变。

---

## 为什么这个策略很好

### 1. 开发速度提升 3-5 倍
- 不需要等待/制作任何美术资源
- 不需要调整PPU、Filter Mode、像素对齐等参数
- 不需要导入/管理Sprite图集
- 新增一个敌人？复制一个方块，改个颜色，5秒钟

### 2. 游戏性迭代零摩擦
- 想调整角色大小？改Scale即可
- 想新增一种敌人？改Color区分
- 想测试新武器？用三角形Sprite代替
- 不会因为"美术还没好"而卡住任何功能开发

### 3. 美术替换成本极低
- Tilemap结构不变 → 换瓦片Asset即可
- 角色代码不变 → 换Sprite + 加Animator即可
- 碰撞系统不变 → 不需要任何调整
- 场景布局不变 → 不需要重新摆放

### 4. 与正式版100%兼容
Unity内置的Square/Circle Sprite本质上和外部导入的Sprite是同一种组件（SpriteRenderer）。区别只在于：
- 现在：`SpriteRenderer.sprite = 白色方块`
- 以后：`SpriteRenderer.sprite = 正式角色图`

代码中的 `GetComponent<SpriteRenderer>()` 一行都不需要改。

---

## 各版本受影响分析

### v0.1 初始小屋 ✅ 已完成适配
- **墙壁/地板**：Unity内置Square → Tilemap纯色瓦片
- **相机**：普通Orthographic，无需Pixel Perfect
- **测试玩家**：Unity内置Circle，绿色
- **影响**：正向简化，开发时间从2-4小时缩短到1-2小时

---

### v0.2 战士角色
**原计划**：导入战士Sprite图集，制作Idle/Walk/Attack/Death四向帧动画

**新方案**：
- 角色 = 绿色圆球（Circle Sprite + Color=绿色）
- **移动**：WASD控制Rigidbody2D.velocity
- **朝向**：不需要四向动画，只需让圆球面向鼠标方向（可用Transform.rotation或简单箭头指示）
- **攻击**：攻击时圆球缩放脉冲（Scale从1→1.2→1），或闪烁白色
- **死亡**：圆球变灰并缩小至消失
- **动画系统**：完全不需要Animator，用协程(Coroutine)或代码控制变换

**后续替换**：
1. 把绿色圆球的Sprite从Circle换成战士SpriteSheet
2. 添加Animator组件，接入Walk/Attack/Death动画状态机
3. 攻击代码从"缩放脉冲"改为"触发Animator.SetTrigger()"
4. 其他移动、碰撞、属性代码**一行不改**

---

### v0.3 法师与弓箭手
**原计划**：导入法师/弓箭手Sprite，各自做四向帧动画

**新方案**：
- 法师 = 蓝色圆球（Circle Sprite + Color=蓝色）
- 弓箭手 = 黄色圆球（Circle Sprite + Color=黄色）
- 三个职业代码结构完全相同，区别只有：
  - Color属性不同
  - 攻击方式不同（战士=近战碰撞，法师=发射蓝色圆形子弹，弓箭手=发射黄色直线子弹）
- 所有职业共用同一个PlayerController脚本，通过**ScriptableObject**或**继承**来区分攻击方式

**后续替换**：
1. 蓝/黄圆球换成法师/弓箭手Sprite
2. 添加各自的Animator
3. 子弹从纯色圆形换成正式子弹Sprite
4. 代码中的攻击逻辑、伤害计算、属性系统**一行不改**

---

### v0.4 敌人系统
**原计划**：设计敌人状态机，导入敌人Sprite，制作动画

**新方案**：
- 敌人 = 红色方块（Square Sprite + Color=红色）
- 不同敌人用不同颜色/大小区分：
  - 普通怪 = 红色小方块 (Scale=0.8)
  - 精英怪 = 紫色中方块 (Scale=1.0)
  - Boss = 黑色大方块 (Scale=1.5)
- **状态机**：仍然需要！只是状态转换的表现从"播放动画"变成"改变颜色/缩放"：
  - Idle → 颜色正常
  - Chase → 颜色变深/缩放微动
  - Attack → 快速闪烁 + 向前突进
  - Hurt → 变白0.1秒
  - Death → 变灰 + 缩小消失
- **分层状态机**：仍然推荐！底层=基础移动/寻路，上层=行为逻辑（巡逻/追击/攻击）

**后续替换**：
1. 红色方块换成正式敌人Sprite
2. 状态机中的"改变颜色"节点替换为"播放动画"节点
3. 寻路、AI逻辑、伤害判定代码**一行不改**

> ⚠️ **关键提醒**：即使在程序员美术阶段，**敌人AI状态机仍然是必需的**，因为AI逻辑与美术表现是完全解耦的。现在用代码驱动表现，以后换动画即可。

---

### v0.5 道具系统
**原计划**：导入武器/药水/装备图标Sprite

**新方案**：
- 武器 = 基础几何形状 + 颜色：
  - 剑 = 细长矩形，银色
  - 法杖 = 圆形顶部+细长矩形柄，紫色
  - 弓 = 弧线+矩形，棕色
  - 药水 = 圆形（小），红色/蓝色/绿色
- 拾取提示 = 方块上方显示彩色小三角
- 装备栏UI = 6个方块格子，放入的道具显示对应颜色和形状

**后续替换**：
1. 所有几何形状替换为正式图标Sprite
2. UI方块替换为正式装备栏背景图
3. 道具属性、掉落逻辑、装备栏代码**一行不改**

---

### v0.6+ 地图生成
**原计划**：使用BSP/Cellular Automata等算法生成像素地牢

**新方案**：
- 算法完全不变！BSP生成的是"哪些格子是地板/墙壁"的数据结构
- Tilemap根据数据自动铺设纯色瓦片
- 房间、走廊、障碍物全部由颜色深浅区分
- 可以先用不同颜色表示不同地形（普通地板=浅色，陷阱=红色格子，安全区=绿色格子）

**后续替换**：
1. 瓦片Asset的Sprite从纯色方块换成正式地砖/墙壁素材
2. 可以添加Rule Tile实现自动转角、边缘衔接
3. BSP生成算法、房间连通逻辑、怪物刷新点代码**一行不改**

---

### v0.x 最终版 美术替换总清单

当游戏性完全验证后，一次性美术替换的工作量：

| 替换对象 | 工作量 | 代码改动 |
|----------|--------|----------|
| 地板/墙壁瓦片Sprite | 2个Asset替换 | 0行 |
| 角色Sprite + Animator | 3个职业 × 4状态 | 仅动画触发代码 |
| 敌人Sprite + Animator | N种敌人 | 仅动画触发代码 |
| 道具/武器图标Sprite | 逐个替换 | 0行 |
| UI背景/按钮/面板 | 替换UI素材 | 0行 |
| 添加特效（粒子系统） | 新增，不影响现有代码 | 0行 |
| 添加音效 | 新增，不影响现有代码 | 0行 |
| 添加Pixel Perfect Camera | 新增一个组件 | 0行 |

**结论**：游戏性代码在美术替换阶段几乎不需要改动。所有工作集中在"导入素材 + 配置Animator"上。

---

## 程序员美术的配色建议

为了让纯色几何图形在游戏中清晰可辨，建议遵循以下配色规则：

### 核心对象配色
| 对象 | 推荐颜色 | RGB值 | 理由 |
|------|----------|-------|------|
| 玩家（战士）| 绿色 | #33CC33 | 玩家=生命/安全，绿色最直观 |
| 法师 | 蓝色 | #3399FF | 魔法=蓝色，经典配色 |
| 弓箭手 | 黄色 | #FFCC00 | 远程/敏捷，暖色区分 |
| 敌人（普通）| 红色 | #CC3333 | 危险/敌对，红色最直观 |
| 敌人（精英）| 紫色 | #9933CC | 高级/魔法感 |
| 敌人（Boss）| 黑色/深灰 | #222222 | 压迫感，可以加红色描边 |
| 墙壁 | 深灰 | #555555 | 中性，不抢视觉焦点 |
| 地板 | 浅棕/米色 | #D4C5B5 | 温暖底色，衬托角色 |
| 血瓶 | 红色圆球 | #FF4444 | 红色=生命 |
| 护盾/护甲 | 蓝色方块 | #4488FF | 蓝色=防御 |
| 金币/宝物 | 黄色菱形 | #FFD700 | 金色=贵重 |

### 视觉区分技巧
1. **形状 + 颜色双重编码**：不要只靠颜色，形状也要区分
   - 玩家=圆球（友善），敌人=方块（危险），道具=三角形（可收集）
2. **尺寸暗示等级**：大=强，小=弱
3. **描边/发光**：用SpriteRenderer的Material做简单描边，增强轮廓
4. **背景对比**：地板用浅色，墙壁用深色，角色用鲜艳色，形成三层对比

---

## 何时开始美术替换？

建议在以下条件全部满足后再启动美术：

- [ ] 三个职业的核心玩法都已完成（战士/法师/弓箭手都能玩）
- [ ] 至少3种敌人类型，AI行为基本稳定
- [ ] 道具系统跑通，玩家能理解装备栏和道具使用
- [ ] 地图生成算法稳定，能连续生成多层关卡
- [ ] 核心循环验证：玩家能"打怪→掉宝→变强→打更强的怪"
- [ ] 死亡机制和重开机制完成
- [ ] 自己（和可能的朋友）玩过10小时以上，确认游戏性有趣

> 🎯 **标准**：当"没有美术"不再是阻碍你判断游戏好坏的因素时，就是上美术的时候。

---

## 文档变更记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2025-07-09 | v1.0 | 初始版本，基于用户"美术完全滞后"的决策撰写 |

