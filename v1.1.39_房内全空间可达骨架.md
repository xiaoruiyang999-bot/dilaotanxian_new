# v1.1.39 房内全空间可达（先铺可达骨架，后置障碍）

## 需求（用户）

房内不再出现成片墙，但**内部小空间仍可能不可达**（孤岛）——要求：**先生成可达路径，
再基于路径骨架布置随机障碍**，保证房内各个小空间都有可达路径。

## 实现（三处）

### 1. RoomPlanner：可达路径骨架（先铺）

保护集升级为骨架网络，障碍只能在骨架之外的空隙生成：
- 门锚点→中心 宽3走廊（原有）+ 中心 5×5（原有）；
- **中心十字带**（中心行/列 ±1 全跨度，3 宽——玩家胶囊 1.32 需 ≥2 宽通道）；
- **随机中带**（三等分点 2 宽，**半跨度**：随机从左/右、下/上边缘带接到十字——调参记录：
  全跨度中带把空隙切碎，障碍退化至均值 1.5 格；半跨度后 18×11 均值 2.5、大房 7.8）；
- **边缘 2 圈带** + 挖除轮廓交界的可行走侧 1 圈（缺口绕行保通）。

### 2. RoomObstaclePlanner：逐格落位

模板段遇骨架/贴墙/贴轮廓的**违规格跳过保留合法段**（原来一违规弃全部障碍），
<2 格碎片弃段——空隙里仍能成出 2~6 格短墙。

### 3. RoomLayoutValidator：全连通校验（兜底）

新增 ⑤：**所有**"可行走且非障碍"格必须从中心可达（精确等值比较，非比例）——
出现任何孤岛即整案重试（同 RNG ≤10 次 → 去障碍 → 整房保底链不变）。

## 仿真（1000 房/尺寸）

18×11 与 36×21 均 **1000/1000 全连通**；障碍均值 2.5 / 7.8 格（短墙观感保留）。

## 验收

Play 走查各房：F9 + 实走确认房内每个角落可达；宝箱/敌人不会刷进孤岛（物理选点 + 全连通双保险）；
障碍短墙仍随机多样；同 seed 复现不变。

## 关联

v1.1.31 五职责架构（本次为其补全"路径先行"生成序与全连通校验）。
