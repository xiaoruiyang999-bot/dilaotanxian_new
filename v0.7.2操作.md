# v0.7.2 操作记录

> 版本主题：主 UI 四槽 + 道具背包（3 格）+ 武器背包（1 格）（《v0.7_Demo玩法计划书.md》§九 v0.7.2 执行卡，总纲决策 4）。
> 执行文档：`v0.7.2_正式计划.md`。本版是 UI/数据框架版：技能三槽空占位（v0.7.4 填内容）、消耗品效果与正式投放归 v0.7.3。无场景/prefab YAML 改动（测试道具全部运行时投放）。

## 改动文件清单

### 新建脚本（5，含 .meta；编辑器已自动补 csproj）

- `Item/ConsumableData.cs`：消耗品 SO——displayName / effectType(HP/Armor/Mana) / value / iconColor / 占位图标。纯数据容器，效果接线归 v0.7.3。
- `Item/ItemInventory.cs`：玩家道具背包（纯数据 + OnChanged 事件）——道具栏激活位 1 格 + 背包 3 格（ItemStack = data + count，同类叠加无上限）；`Add` 分流：栏同类叠加 → 栏空入栏 → 包同类叠加 → 包空位 → 满返回 false；`UseActive()` 扣数、清零出槽（未装备无副作用）；`SwapWithBackpack(index)` 点击互换。
- `Item/ItemPickup.cs`：消耗品拾取物（IPickupable）——E 拾取 → ItemInventory.Add；满则 PlayerInteractor 提示"背包已满"且拾取物留地不消耗；运行时色块占位视觉 + `Spawn(data, pos)` 静态构建。
- `Item/WeaponSatchel.cs`：武器背包（1 格纯 C# 数据类，PlayerWeaponHolder 持有）——`Store(data)` 返回被挤出者（可空）；`Clear()` 死亡重开清空。本版无 UI。
- `Player/SlotBarUI.cs`：主 UI 四槽 + 背包 3 格（RuntimeInitializeOnLoadMethod 自举，全部运行时构建，锚屏幕右下留 20px，面板 274×126）——技能三槽"—"空占位（预留 SetSkillDisplay/SetSkillCooldown 给 v0.7.4）；道具栏显示色块+数量角标（14pt，count≥2 显示，超 99 显示 99+）；背包格为 Button，点击 → SwapWithBackpack；订阅 ItemInventory.OnChanged 刷新，外观参数每次刷新应用。

### 修改脚本（3）

- `Weapon/PlayerWeaponHolder.cs`：**`dropOldWeaponOnEquip` 退役删除**，换代为 `storeOldWeaponInSatchel`（默认 true=地牢规则）；+`Satchel`（WeaponSatchel）；Equip 换武器：旧武器入包，包满被挤出者原地 `WeaponPickup.Drop` 可捡回；Unequip 同步 `Satchel.Clear()`（死亡重开武器不保留）。
- `Player/PlayerController.cs`：Awake 运行时挂载 ItemInventory（同 PlayerInteractor 模式）；+UseItem action 分发（performed → ItemInventory.UseActive()，死亡时不响应）；ClassSelectUI 打开期间屏蔽列表 +UseItem（选职业期间不消耗道具）。
- `Class/PrepRoomManager.cs`：`dropOldWeaponOnEquip=false` 改置 `storeOldWeaponInSatchel=false`（归位订阅 OnWeaponChanged 逻辑零改动，展台归位规则不变）；+测试投放 SpawnTestItems()：出生位前方横排 4 个测试消耗品（运行时创建，Consumable_Test 走 AssetDatabase 编辑器路径加载，与 ClassCatalog 同模式）。

### 输入与数据资产

- `Assets/PlayerInputActions.inputactions`：+`UseItem` action（Button，默认 `<Keyboard>/c`，可重绑，结构与既有 Skill action 同构）。
- `Assets/Data/Item/Consumable_Test.asset`（+文件夹 .meta）：测试药——effectType=Mana、value=10、蓝色 #3498DB 占位。**仅本版验证用，v0.7.3 建正式三包后连同测试投放一并删除**。

### 文档

- `Assets/Scripts/now_use/README.md`：版本号 v0.7.2；+Item/ 目录（4 脚本）；Player/ 10→11（+SlotBarUI）；清单 78→83；PlayerController/PlayerWeaponHolder/PrepRoomManager 职责同步。
- `版本实施自检清单.md`：§3 输入行 7→8 actions（+UseItem 默认 C）；§3 新增"背包/槽位（v0.7.2）"行；§3 换武器规则行与 §1 示例改引 storeOldWeaponInSatchel。
- `v0.7_Demo玩法计划书.md`：§九 v0.7.2 执行卡标完成。

## 验证情况

- `dotnet build Assembly-CSharp.csproj`：**0 警告 0 错误**（全部改完一次性构建；5 个新文件 csproj 条目由运行中的编辑器自动生成，构建前已核对在册）。
- grep `dropOldWeaponOnEquip`（Assets/Scripts 全部 .cs）：**零残留**（仅历史文档 v0.6.2操作.md 等留档提及，属历史记录不改写）。
- .inputactions JSON 合法性：PowerShell ConvertFrom-Json 解析通过；actions = Move/Attack/Dash/Sprint/Interact/Cancel/Skill/**UseItem**（8 个），UseItem 绑定 `<Keyboard>/c`，action/binding 字段与既有 Skill 同构。
- 计划书 §六 可在编辑器外核对项逐条推算：
  - 布局避让：SlotBarUI 274×126 锚 BottomRight（占 x∈[1626,1900]、y∈[20,146] @1920×1080）；AmmoUI 在屏幕**左下**（PlayerStatsPanel 上方），无交叠 ✓；
  - 拾取分流：Add 五级分支顺序与计划书 §一.2 逐行一致；满时 Add=false → ItemPickup 提示"背包已满"且 **不 Destroy**（拾取物保留）✓；
  - 使用/互换：UseActive count−1、≤0 清槽；SwapWithBackpack 直接交换两槽引用（空格互换=取出/放入）✓；
  - 换武器入包：地牢（storeOldWeaponInSatchel 默认 true）旧武器 Store 入包、挤出者 Drop；准备场景（PrepRoomManager 置 false）不入包不掉落，OnWeaponChanged → PrepRoomPlacer.ReturnWeapon 归位照旧 ✓；
  - 死亡重开：Unequip +Satchel.Clear()；且死亡回准备场景整场景卸载、RunStateCarrier.ClearWeapon()，武器/背包天然清空 ✓；
  - 回归面：宝箱/法力球/祭坛/补给/换武器战斗链路未触碰 ✓。

## 遗留风险

- **满包提示本版无法实机触发**：测试投放只有 1 种消耗品（计划书限定 1 份测试资产），同类叠加无上限 → 4 格永远填不满。"背包已满"分支已按规格实现，但实机验证需等 v0.7.3 三种正式道具（栏+3 包容纳 4 种即满）。（计划书 §六.3 该步顺延。）
- UseActive 只扣数不结算效果（计划内：v0.7.3 接 Health/PlayerStats），Console 打"[Item] 使用道具…"日志。
- 武器背包本版无 UI：包内武器玩家不可见，只能等下次换武器时被挤出掉地捡回（计划 §五确认规则）。
- 测试资产/投放残留风险：v0.7.3 收尾须删 Consumable_Test.asset 与 PrepRoomManager.SpawnTestItems 块（已写进计划 §七）。
- SlotBarUI 自举依赖场景存在屏幕空间 Canvas（v0_7/v0_4/v0_5 场景均有 PlayerUI Canvas）；极端无 Canvas 场景会自建 ScreenSpaceOverlay Canvas + EventSystem（InputSystemUIInputModule）。
- 实机表现（四槽显示、角标、点击互换、入包/挤出掉落）未经编辑器实测，待用户验收。

## 待用户在编辑器验收清单

> 测试场景：`v0_7_PrepRoom`（主测，测试道具在此）→ `v0_7_ClassWeapon`；回归 `v0_4_EnemySystem`；Game 视图 1920×1080 或 Free Aspect。

1. **编辑器刷新**：切回 Unity 导入新脚本与 .inputactions 变更（Input System 资产重新导入后，PlayerInput 组件上应能看到 UseItem action）；编辑器此前开着的话勿保存旧内存覆盖 YAML。
2. **四槽**：Play 后屏幕右下出现四槽（小技能/大招/武器技能/道具栏，技能三槽显示"—"）+ 道具栏上方背包 3 格；不压弹药 UI（弹药面板在左下）。
3. **拾取分流**：准备场景出生位前方有 4 个蓝色测试药——E 捡第 1 个入道具栏（色块出现）；继续捡同类叠加，道具栏右下角角标 2/3/4。
4. **使用**：按 C 道具栏数量 −1；减到 0 槽位清空恢复"—"。点击背包格可与道具栏互换（把道具调入背包、再调回）。
5. **武器背包（地牢）**：进 `v0_7_ClassWeapon`，开宝箱/捡新武器 → 旧武器**入包**（原地无掉落物）；再捡一把 → 包内武器原地掉落、可 E 捡回。
6. **准备场景归位**：展台换初始武器仍自动归位（不生成掉落物、不入包）。
7. **死亡重开**：地牢死亡回准备场景，武器与背包清空、展台重置照旧（可用 Tools/Dungeon/Debug Kill Player）。
8. **回归**：`v0_4_EnemySystem` 木桩场景无损；宝箱/法力球/祭坛/补给流程全通。

---

## 补丁 1：SlotBarUI 场景切换消失（2026-07-29，用户验收反馈）

- **现象**：准备场景走传送门进地牢后槽位条消失。
- **根因**：SlotBarUI 挂在场景 Canvas 上，场景卸载即销毁，且 RuntimeInitializeOnLoadMethod 每次 Play 只跑一次不会重建；另发现常驻 UI 的 ItemInventory 引用会随场景切换失效（指向已销毁的旧场景玩家）。
- **修复**：SlotBarUI 改**自建常驻 Canvas**（DontDestroyOnLoad + sortingOrder 50，不依附场景 Canvas）+ sceneLoaded 自检重建 + `RebindInventory()` 场景切换重绑数据源。
- **经验已录自检 §2 #21**：运行时全局 UI 一律自建常驻 Canvas；跨场景数据引用随场景切换重绑。

## 补丁 2：拾取列表实时化 + 字体警告修复（2026-07-29，用户需求）

- **字体警告**：列表选中前缀 `▶`（U+25B6）微软雅黑 TMP 字体无字形 → 改 `>`。
- **实时拾取列表**（`PlayerInteractor.cs` 重构，两段式拾取退役）：
  - 可拾取物**实时进出列表**：靠近（detectRadius）自动进、走远（detectRadius+listRemoveBuffer 滞回）自动出、全空自动隐藏；成员变化才重建行，不重排既有行（选中不跳）；
  - **选中项场景呼吸放大**（1.15× Yoyo，序列化 selectedScale/selectedDuration 可调），滚轮/数字键切换即时重指，旧项还原缩放；
  - 普通交互物（宝箱/祭坛/补给/传送门）候选逻辑不变（呼吸高亮+"按 E"标签直接触发）；
  - 行为变化：单物品也显示一行列表（原"单物品直拾不弹列表"取消）；可拾取物头顶"物品名+按 E"名签移除（名字由列表承担）；
  - Esc 关闭列表取消（列表自动管理显隐），OnCancelPressed 入口保留备用。
- 验证：`dotnet build` 0 警告 0 错误。README PlayerInteractor 行已同步。
